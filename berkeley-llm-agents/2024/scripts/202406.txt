(3) CS 194/294-196 (LLM Agents) - Lecture 6, Graham Neubig - YouTube
https://www.youtube.com/watch?v=f9L9Fkq-8K4&list=PLS01nW3RtgopsNLeM936V4TNSsvvVglLc&index=7

Transcript:
(0:00) GRAHAM NEUBIG: Hi, everyone. I'm happy to be here. Thank you for the invitation. And I'd like to talk about agents for software development. So to give my profile, very quickly, to ground the discussion, I'm a professor at CMU. And I've been there for about eight years working on various research topics, including the ones that Shannon mentioned.
(0:25) I'm also chief scientist at All Hand AI, which is a company that started very recently building open source coding agents. And it's a maintainer of software library called Open Hands, which used to be called Open Devon. So a lot of my discussion and examples will be based on this. I'm also a software developer.
(0:49) I like my GitHub to be green basically. So today I'm very happy to talk about my two passions, which are research for artificial intelligence and software development. And I get to talk about both of them in one talk so I'm very happy to do that. So to give some grounding of this discussion, I'd first like to talk about the importance of agents for software development in particular.
(1:16) And this is from a very famous essay by Marc Andreessen called Why Software Is Eating The World. And this happened in 2011. And it basically says, "More and more major businesses and industries are being run on software and delivered as online services, from movies to agriculture to national defense. Over the next 10 years I expect many more industries to be disrupted by software.
(1:38) " So this was written in 2011. I think this mostly came true. I think it would be very weird for us to think about movies not being delivered through software. Like, how many people have gone and rented a physical movie in the past five years? Nobody. So you can see more and more of our life is happening through software.
(2:04) So one of my motivating things is if we gave everybody the ability to quickly write software to achieve their goals, what would people be able to do. And I think the answer, especially for people here in computer science, the answer is a lot. This is also timely because if you look at the Nobel Prize for physics and the Nobel Prize for chemistry this year, two of these Nobel prizes were awarded for people who wrote software.
(2:36) And so if we think about this, this has pretty big implications. It used to be that this was given to people who were using test tubes and running cyclotrons and other things like this, and those are still important, of course, but now a lot more of our science and human progress is going to happen in software.
(2:59) I'm not just going to be talking about models for coding today, although that's mostly what I'm going to be talking about, but do people know how much time the average software engineer spends coding in terms of producing new code every day? It's 15%. And this is from a study in-- from Microsoft in 2019. Maybe this is more or less nowadays.
(3:27) But it's not just coding that people are doing, they also do bug fixing, testing, documentation and reviews. 36% of their time is spent on communication of some variety, and 17% of the time is spent on other things like going to the bathroom. So another thing I'd like to point out is how can we support software-- support developers.
(3:56) And I came up with a kind of imprecise but maybe helpful categorization of automation with respect to things like self-driving where self-driving, we have this well-known classification of no automation to full automation. And self-driving, we have manual driving at no automation, and then we have things like adaptive cruise control, braking, Tesla's autopilot which at the time of writing was partial automation, conditional automation, automation in certain things, and then high automation, which would be like cruise or Waymo self-driving vehicles.
(4:36) And in terms of software development, I think there's also different kind of levels of automation that we could think about where zero would be just writing all your code by hand, one is smart autocomplete, things like Copilot or Cursor. Number two would be like Copilot or Cursor chat where you refactor larger amounts of data.
(5:01) Conditional automation would be automating particular tasks that people want to do, and then high automation would be more autonomous things that solve full software development tasks. And this time I want to be talking about the last one. But I think if people are familiar with GitHub, Copilot or Cursor, I think this is something that a lot of people use.
(5:24) It's something I use every day. And basically what this is doing is it's completing your next thought. So as a programmer you write a few lines or something like this and it goes in and fills in the next line for you. Very, very useful. I tried coding on the plane on the way over here and I had no internet and it was miserable so I stopped because I can't code without autocomplete anymore.
(5:48) But at the same time it works synchronously with developers. The thing I'm going to talk about more this time is something like a more autonomous agent. So this is an example of our software Open Hands. And the way it works is you fill in a large kind of command of what you want to do. This is resolving a GitHub issue.
(6:12) And so basically I told it, it needs to go into llm.py and essentially fix a bug in llm.py. So it goes and downloads the GitHub repo, makes additional tests-- sorry, this is not fixing a bug. This is adding tests to a file. So it makes a repo. It creates some tests. But it created them for the init function of the LLM class so now it's waiting for a little bit.
(6:41) So it created the tests, now what it's doing is it's installing the dependencies for the project. It is running the tests according to the directions that I gave it. It ran the tests, the tests did not-- sorry, you got an error in poetry. It couldn't find the pyproject.toml file. It ran the test, the tests worked so now it's checking out a branch.
(7:14) It's committing the branch, add unit tests. It's pushing using the credentials that I provided it. And now starting here, this is under human control so I click on the link that it gave to me and I open up the page. I can review the code and stuff like this and make sure it's OK and incorporated if it makes sense.
(7:39) So I think you can see the difference between completion-- just completing individual tasks and actually solving tasks end to end. Another thing that is really popular nowadays is autonomous issue resolution. And this is another example of autonomous issue resolution within GitHub where you basically tag an issue with the words fix me, the agent goes in and does its work.
(8:04) And if it works, if the agent decided that it did a good job, it would send it in a pull request so you could go in and accept it or not accept it. So these are some examples. So there's a question of how promising this is. And this is actually old data, but it's still relatively compelling data. And there was a study where GitHub Copilot did a study with a sample size of 95 developers where they had some developers use GitHub Copilot and some developers not use GitHub Copilot.
(8:40) And 78% of the developers or 8% more of the developers using Copilot finished the task. And they finished it more-- 56% faster. So basically this is doubling the speed of coding for people using GitHub Copilot versus not using Copilot. And now we have even better code completion. Like in my opinion, Cursors code completion is better than GitHub so it might be even faster.
(9:11) And that means you cut into a very big chunk of the 15% of time developers spend on coding, which is great. Like let's say we could make developers 7% more productive, make them enjoy coding more, I think that's wonderful. But that doesn't solve all of software development, as I mentioned before. So the rest of the time here I'd like to spend talking about challenges in coding agents and how we evaluate coding agents-- or how we build coding agents and how we evaluate them.
(9:45) So some challenges that I see in this area are, number one, defining an environment that the agents can work in and be tested in, designing a space of observations and actions. So this is an agent's class so I think you're very familiar with the fact that one of the things that defines agents is how they can interact with the environment.
(10:07) So how do we define that for coding and software development tasks? More specifically, how do we specifically generate code? What are the language models we use for code? How do we build them? File localization, so how do we identify which parts of a code base that we would want to be editing. This has parallels to reinforcement learning because in reinforcement learning, we have the idea of exploring our environment.
(10:35) And I think file localization is a lot like this. Another thing is planning and error recovery. So how do we make plans about what we want to do and how do we recover from errors. And, finally, safety. So if we're actually interacting with environments through software, how do we ensure that this happens in a safe way? So first I'd like to talk about software development environments.
(10:56) And there's a number of types of environments. If we think about the actual environments that software developers act in, they can be things like manipulating source repositories like GitHub, GitLab, other things like this. They can also be interacting with task management software. So this could be things like Jira or Linear or GitHub issues or other things like this.
(11:22) So we would want agents to be able to do that as well. Software developers also use office software. They use like Google Docs or Microsoft Office to exchange information about requirements and other things like this. So how could they-- how could they interact with this software? Communication tools like Gmail and Slack.
(11:42) So I think if I enumerate these four tools, that covers most of the environments that I interact with in some way when I'm developing software. There might be other ones like interacting with a server, logging into servers, manipulating cloud infrastructure, other stuff like that, but these are main ones. In contrast, if we look at the environments that are available now for testing, a lot of these are focused mostly on code, on generating code.
(12:13) So I'm going to mostly be talking about these. I'd like to note that developers do more. And they do things like browsing the web, but I'm not going to talk about that because I know next session I think you're having somebody talk about web browsing agents. So that's super important for software development, but I'll not focus on that here.
(12:32) So the first thing is simple coding. And simple coding, basically what this is it's just testing the ability of language models to go from a specification to code. And the most common examples of this are things like HumanEval of MBPP. And these are things like here you can look at the problem they have here, given a non-empty list of integers, return the sum of all of the odd elements that are in even positions.
(13:05) So I think these are kind of good in some way because you should be able to solve these if you're a good language model, but this is kind of like asking LeetCode questions on a coding interview. I'm sure everybody if you've done a coding interview, you've done LeetCode questions and you've been thinking, why am I doing this? I never do this in my everyday coding experience.
(13:32) It's testing algorithmic knowledge, but not necessarily knowledge of software development or software engineering. But, yeah, anyway, these include the examples of usage of the Python standard library, docstring, other things like this. This is probably a necessary condition for being a good coding model, but not a sufficient one.
(13:51) There's also some work on building coding benchmarks for broader domains, and both of these are papers that I did at some point. But the first one is CoNaLa. And this is broader data that was scraped from Stack Overflow. And so basically what we did is we scraped data from Stack Overflow by getting questions from the Stack Overflow titles context in the code snippet that answered-- that basically solved this context.
(14:22) And originally we just measured this using the overlap between the generated code, but we also recently added execution-based evaluation. And this covers a relatively wide variety of libraries that people use in actual kind of data science or other things. So you can see it covers like Pandas, NumPy, Regex, OX, Collections, other stuff like this.
(14:50) And that's in contrast two things like apps or HumanEval or something like that basically use only very standard libraries. So this moves a step beyond the existing ones and says, OK, can you interact with the whole Python ecosystem? Another thing is data science notebooks. And so this is another-- this is a great paper by people at Google.
(15:15) And basically they use data science notebooks to allow for incremental implementation and evaluation of code in context. And so you have a notebook and you have people going through the notebook and then you need to generate the next cells based on the context you have already. So this moves another step, adding context, adding practical long implementations.
(15:41) Then another data set that's relatively recent compared to these other ones is called SWEBench. It's a very popular data set nowadays so I think a lot of people have heard of it. But the way it works is they scraped issues from GitHub and the code bases. And the output of the model is a pull request. So it's very similar to the situation that I talked about before where we have agents who actually go in and send a pull request to solve your GitHub issues.
(16:13) But basically here's an example, you have data leak and GDBT due to warm start. This is about non-histogram based version of dadadadadada. You get a code base and then you want the language model to go in and modify a few files. And then your evaluation is measured based on whether you can pass tests that were introduced together with the pull request.
(16:39) So this is a really, really nice benchmark in some ways. It requires long context understanding. It requires you to understand the whole code base. It requires you to implement things in a very precise way so that they can pass tests. There are some problems with it though. So one problem is that it's kind of limited only to high quality repos where on PRs where they introduced new tests.
(17:11) And so these are very heavily biased towards, for example, bug fixing PRs. They're very heavily biased towards non-documentation or non-refactoring or other stuff like this. So it's a great data set and people should definitely use it, but it does have its limitations in covering software development tasks.
(17:34) Another problem is a lot of the data has leaked into language models because the language models are trained on data from GitHub so, actually, there's some concerns about leakage there too. So given these environments, if we look at how we measure success, some of the most common metrics are something called Pass@K.
(17:56)  So this is basically if we generate K examples, will at least one of them pass unit tests? And this is a little bit of a small detail but if you generate only K, it will result in high variance because you might get-- if you generate only one, you might get it right like 70% of the time and wrong 30% of the time, or something like this.
(18:17) So what they actually do is they generate n and then they use statistics to figure out what it would be at K. So this is, I think, by far the most common. The only disadvantage to this is it requires unit tests so you're limited to doing things where you have unit tests. So there's also other methods based on lexical or semantic overlap.
(18:41) And what this means is you basically look at how well the generated code overlaps with gold standard code created by programmers. And there's a bunch of different ways to do this. There's a method called BLUE, which has been used for a long time in machine translation, which basically looks at the n-gram overlap between the generated code and the reference code.
(19:07) And then you also have embedding based methods like CodeBERTScore or something like this. But these are details. So as I mentioned in SWEBench, leakage of data sets is a big problem for things that were created on public code. Nobody has really convincingly-- or, no, actually, sorry. There's a paper very, very recent called SWEBench Plus which evaluated this on SWEBench.
(19:38) But I only learned about it since I created my slides so it's not on my slides. But another compelling example of this is in ARCADE. And ARCADE basically, it's the data science notebook problem data set that I talked about before. And they demonstrated that when they evaluated on existing data sets from the internet, they got kind of like scores in the 30s or 40s, but then they created new data sets just by getting people at Google to create data science notebooks.
(20:18) And the accuracy dropped by like 20% or 40% or something like this. So leakage is definitely a big issue there. Another example is LiveCodeBench which shows that some code LMs outperform on particular data sets that are very widely benchmarked on. So HumanEval is a very widely used data set. And so if you look at the scores on a HumanEvel on the y-axis and compare them to LiveCodeBench on the x-axis, you can see a cluster of data sets where the scores on a HumanEvel and the scores on LiveCodeBench correlate.
(21:03) And then you can see another cluster where the scores on a HumanEvel are very high but the scores on LiveCodeBench are not very high. So these ones are ones that maybe in some way overfit to the data set and managed to get a very high score on it. A final exciting direction that people are working on recently is multi-modal coding models.
(21:24) And there's a data set by people at Stanford called Design2Code. And the way it works is they do code generation from websites. So they have a bunch of websites that have various designs, like the ones down here. And based on that need to-- they generate output code. And also very recently, again-- so recently that I haven't been able to add it to my slides-- there's a multimodal version of SWEBench that is also similar in that way.
(21:57) One interesting thing that they do in this data set for design to code is they measure the visual similarity of the websites. So they generate the websites, they take screenshots of the websites, and then they do visual similarity both on the high level, according to embeddings over the whole image, and then on the low level, recalling each elements visual similarity.
(22:23) So this is kind of a whirlwind tour of how people are evaluating models and what environments people are testing these sorts of coding agents in. So now we'll move on to the actual modeling part. And I'd like to start out by talking about observation in action spaces. And so if we think about what coding agents must do, they must do things like understanding repository structure, read an existing code, modify or produce code, and run code and debug.
(22:59) And this is separate from all of the other stuff that software engineers do which might require a web browsing or other things like this. And I'd like to give-- there's a bunch of coding agents. I'd like to give just a few examples of the observations in action spaces that are used in some of the popular ones.
(23:15) So one of the popular ones that's used pretty widely is one that I mentioned here. It's called CodeAct. It's a method that essentially interacts with environments by writing code. This is also the one we use in Open Hands, our coding model, our coding platform, by the way. But the way it works is traditional agents, which I think you have probably talked about multiple times in this class already, one of the things they do is tool use.
(23:52) And typically when you use a tool, the idea is that at every time step, you call a tool and you get the result back. And then based on that you make the next action. So this is the traditional paradigm. And so if we look at an example-- sorry, this is very small on the screen so I'll read it aloud. It says, determine the most cost effective country to purchase the smartphone model CodeAct 1.
(24:17) The countries to consider are the US, Japan, Germany and India. And so if you have these available APIs, what a traditional model would do is it would do look up rates Germany, it would do look up phone price Germany, convert in tax, look up rates Japan. And then it would step like over and over and over again to try to generate-- do all of these APIs calls.
(24:44) And you might need 15 API calls before you can get the final answer. The idea of what CodeAct proposed is basically moving beyond just individual step by step tool use and actually allowing the models to act by writing programs. And if you do this, you can essentially write a for loop over all of the countries that you're supposed to be examining, and run for loop and get the answer.
(25:12) And this doesn't mean you can't get intermediate feedback, of course. Like, for example, let's say this for loop didn't work because instead of USA, you would need to write U.S.A. You would get an error when you tried to run the code and you could go back and modify the code and then run it again. So you can get multi-step feedback, but it's at a much less granular level so you can do more things in fewer steps.
(25:42) In the original formulation of this, it did-- it executed bash commands and Jupyter commands. And they found that this gave a faster resolution, higher success rate than direct tool use. So this kind of sets the stage for coding agents. It allows you to run bash, it allows you to generate code. But one other important thing that we need to do is not just generate code, but actually go in and modify code because most coding tasks are-- like any serious code repository, you spend much more time modifying code
(26:16) than you do generating code de novo. And SWE-Agent, which was kind of the follow up modeling work after SWEBench, basically did this by defining specialized tools that make it possible to efficiently explore repositories and edit code. So what it does is you have the LM agent, it is given a set of LM friendly commands.
(26:45) The LM friendly commands interact with the computer and terminal and file system and you get LM friendly environmental feedback. And if you want to look at all the details, you can go through the paper but here's an example. So you could get a observation, which is-- you would get this observation from one of the commands like show file.
(27:12) And let's say you called show file and then 405 for line 405. If you do this, basically you will have a program that will go in and parse the file. It will get lines from 401 to 409 because you get a window around the file-- window around the line that you selected within the file. And this will be returned back to the LM.
(27:40) And in reality this isn't like four lines above and four lines below. It's actually like 50 lines above and 50 lines below or 100 lines above and 100 lines below. But what this allows you to do is even if you have files that are like thousands of lines in your code repository, you can still parse them in a reasonable size in the LM context window without expanding your entire context length without spending too much money, other things like this.
(28:11) Then the basic response after doing this is you get a thought. So it's like chain of thought reasoning. You get a thought about what you need to do next. And then you need to go in and edit the files. So you now call an edit command where you edit lines 404 to 407, and then you have an end of edit thing here.
(28:40) So then this will edit the lines and insert this information here. And there's actually a bunch of different ways to do this, like the SWE-Agent way is just one way, and then there's other ways as well, which I'll probably talk about in a second. So within OpenHands, we do something very similar. It's kind of like a combination of CodeAct and the SWE-Agent kind of editing and observation actions.
(29:15) So we execute all of the actions as calling code. So you can do something like let's say you run an observation action or you run a find command, and you find that there is a-- you find that there's 10 examples of something that you want to replace in various places in your code base, it could then execute 10 of the SWE-Agent actions and go in and replace all of the lines of the file.
(29:50) So this essentially gives you a programmatic interface. You call it by calling Python programs, which all LLMs are very familiar with, and it allows you to do things write a for loop over file editing commands and other stuff like this. So that's the general way that we do this. And then you run this and then you get a observation from this that is like the results of executing the commands and other things like this.
(30:19) So I've talked a little bit about the observation in action space. One really, really important thing in order to make these models work well is to have good code-based LLMs because all of the success of these models depends on the ability to write code. And so I'd like to talk a little bit about the methods that people use for generating-- or for creating code-based LLMs.
(30:46) So, yeah, the basic method is you feed in the instructions or input code into the LLM. Recently, virtually all serious LLMs are trained on code nowadays, so it doesn't matter if you're talking about GPT or Claude or Gemini or Lama or whatever else. But some are more specialized. And unfortunately for closed models and even for some open models, there's not a whole lot of information about what goes into training these models, but I can tell you about some things that they definitely are doing.
(31:22) So the first thing that everybody is doing is training on lots and lots of code data. So lots of code is added to the data mixtures. This is both to improve coding ability and because some prior research has demonstrated that adding code improves the reasoning ability of models in general. But just to give one example of coding data set that we know that's open and has actually been used in training open models, this is The Stack 2.
(31:50) And this is a code pre-training data set. They also looked carefully into licensing considerations. And the reason why is because a lot of code is licensed, and if people don't want you to be using the code in training language models, it could theoretically cause legal problems later. So they basically went through and did the license cleansing and other stuff like this.
(32:16) So they checked if the licenses are available and other things. And, again, this is really tiny over here. I'm sure you probably can't see this on the right side of the page well, but one interesting thing to look at is the distribution of the programming languages that are included in data sets like this.
(32:34) And the first thing is if you look at the largest number of data that's included in here, the data sets that-- or the languages that are most well represented are Python, PHP, Markdown, JavaScript, Java, C++, C sharp and C, which is great. These are popular programming languages that a lot of people want to work on.
(33:05) There's a lot of less well represented languages that are really important, like Docker files. A lot of deployment works on Docker files. I don't even see Terraform represented here and Terraform is a really important thing for deploying cloud infrastructure and stuff like this. So there's kind of a very large difference between the availability of data for different programming languages.
(33:32) Another one is COBOL. Nobody programs in COBOL anymore but a lot of banks have legacy systems written in COBOL that they would love to have translated to new programming languages and stuff. So that's a very low resource. So another method that's kind of specific to code language models that is really, really important for code but maybe a little bit less important for other places is infilling.
(33:55) And in code generation, because we're not generating stuff de novo, we're doing lots of editing, we often want to fill in code. And so the way that this is done is you can take original documents, like the documents here. And you can take a coherent section of this document and basically mask out that section.
(34:20) And then move that mask zero to the end of the document and generate the masked data there. So this is kind of like a pre-training method that people use to train models to be good at infilling data. And so this is super important for code. Every code model does this nowadays-- or every model that seriously wants to handle code does this nowadays so this is pretty important.
(34:43) Another thing that's done in every code model and also more and more models-- almost every model is doing this now-- is some variety of long context extension. And so language models, they typically train on relatively short contexts for efficiency reasons. So they'll train on like 4,000 token chunks when they actually do the pre-training of the model.
(35:12) But when you try to take the model and generalize beyond the context that was trained on at training time, it does a really poor job at generalizing. So part of the reason why is because in transformer models we have positional encodings. I think many people have heard of these, but just in case you haven't, there are things that you append to each token based on the position of the token in the sequence.
(35:37) And the most common variety of this is something called rope. And the way rope works is essentially you have a theta where the theta depends on the length of the output. And so if you train here, you would have 40-- like let's say you were training on 4,096 context links, you would have one half of that. So it'd be 2048 different kind of theta values.
(36:05) But then if you expanded beyond that you would get a whole bunch of theta values that you've never seen before. So there's methods that fix this. And one way they do this is by multiplying this by a constant scaling factor. And what this does is this makes sure that all of this, the theta values that you've seen before, get scaled in a range that it saw at pre-training time.
(36:28) And then another method is something called the neural tangent kernel-based method. And basically what this does is these are like cosine and sine values. So you have some high frequency components and some low frequency components. And so they scale the low frequency components but maintain the high frequency components, which makes a tension over long distances work, work well but keeps the local context.
(36:54) So there's a lot of examples like this. And this paper, Lu et al, goes into much more detail. It's a paper on long context extension, and you can take a look. So another thing I'd like to point out is that there's lots of context that's available for coding. And coding models, specifically like in the Copilot setting where you're interacting with a model within your IDE, pull in lots of different information.
(37:27) So some examples of information that you could be pulling in are things like current coding context, the description of an issue to fix, context about the repo in some way, also open tabs in an IDE or something like this. And so what information should you be using to generate the next code? It depends a lot if you're working on agents or working on code like Copilot style models, but agents at the moment largely use the things that are like in the SWEBench, SWE-Agent style tools.
(38:08) But separately from this, there's this really good blog post on deconstructing Copilot and what information Copilot is pulling in. And basically the way it works is it extracts a prompt from a current doc in cursor position, identifies the relative path in language that the model is occurring in. It finds the most recently accessed 20 files of the same language.
(38:33) So if you've been programming in Python, it will look at the most recently accessed 20 files. It includes text before and text after the cursor, similar files, imported files, metadata about the language in path. And basically, long story short, they did a bunch of prompt engineering to pull in all of the relevant context into the prompt, and that seems to work well for recommending code to human coders who are working on it.
(39:01) I think the agent space has not quite gone this far in prompt engineering based on the current contexts and things like this. So I think that's an area for potential improvement in our models going forward. So another thing I'd like to talk about is file localization. So file localization is a big problem.
(39:21) It might even be the biggest problem in coding agents nowadays. And what I mean by this is that we want to find the correct files given a user intent. So let's say we have an intent that looks like this. So this is an actual example of an issue from our repos. And I think it's a reasonably good example the level of detail that people actually write issues in.
(39:49) So it says, when in confirmation mode, it's not possible to give instructions in between steps. You have to reject an action, and it seems like it doesn't know that the action was rejected. And then describe the UX of the solution that you'd like. We'd like to have a third option confirm action in wait. This action is confirmed but before it tries to take the next step, you are able to give some feedback.
(40:13) So this is a feature request. Basically it's a request to add another option that doesn't exist already. But it doesn't tell you anything about where confirmation mode is implemented, it doesn't tell you whether this needs to be implemented in the front end code, the back end code or both, or other things like this.
(40:29) And models and agents really struggle to do very well at this. And I think one interesting thing is that this is analogous to understanding the environment and embodied agents. So embodied agents, let's say you have a robot that needs to work in your kitchen or something like this, the first thing that it should do before it starts solving any problems that you have in your kitchen is go in like open all the kitchen drawers or drawers or cabinets or whatever else.
(41:02) And I feel like this localization problem is that, it needs to go in and either go in and open all the kitchen drawers or all the files and understand what's going on in them, or it needs to have a really good system to retrieve at retrieval time. But, anyway, so solution one, this is offload to the user. And basically what this requires is this requires that you have a user that's very familiar with the capabilities of agents and what they can do and how you can help them out in doing their job well.
(41:40) Which I think is reasonable if it's somebody who interacts with agents. So this issue was written by me. And I'm very familiar with how agents can work so I told it specifically which files it should be manipulating. But this isn't obviously a final solution because not all users are like that. They might not know the code base.
(41:59) They might know that there's a bug but not know where the bug is-- what is causing the bug and other stuff like this, so this is limited in its value even though it is helpful in cases where it's applicable. So the next is to prompt the agent with search tools. And so SWE-Agent is one example of something that does this, it provides a tool for searching repositories.
(42:23) And specifically this tool is named Search. So it will search for the thing that says PV system. As a result of that it will list out all the files that had PV system in them. Then if it doesn't find the thing it wants, it will click like next and search for the next one. Another thing is you can search and then summarize the search results for the agent and provide the information as necessary.
(42:55) So I think this is good. I think it's actually very nice for agent-based systems that have the ability to use tools as well. But it's not like the equivalent to going in and opening all the cabinets before you start, and so there's other methods that do something like this. So the first thing is creating a map of the repository that you're working in, of the code base that you're working in and prompting the agent with it.
(43:26) So there's a tool that a lot of people-- it's kind of an open source tool that people use called Ada. And the way Ada does this is it creates a tree structured map of the repository a priori. The human user has the ability to add files to the map to make them more prominent, and then it summarizes the maps so it's a decent size.
(43:51) And then taking this a step further, there's a method called agentless. And this does a hierarchical search for every issue. So the way this looks is you essentially start out with a project code base, you feed the agent a very, very simple tree of all of the code that's in the code base. And this is fed into the language model.
(44:16) Then based on this, the agent basically will-- or the language model will respond. I want to open this file, and it will open all of the files. And in each of the files it will get a summary of the methods and classes that are implemented in that file. And then after it does that, the agent then needs to localize to specific functions.
(44:38) It will say the functions it needs to edit or modify, and then those functions will be displayed in full to the agent in order for it to finally generate the code. So this is a pretty effective approach. The final thing is retrieval augmented code generation. And the way this works is you basically retrieve similar code.
(45:02) So you have an embedding based model, and you can feed in the retrieved code to the model and then just generate the outputs. Particularly in code there's also documentation which can be retrieved. So one difficulty in retrieval-based code generation is that the code itself is in Python where the natural language description is in English.
(45:27) And there can be a disconnect between the two of them and so sometimes it's more effective to directly try to retrieve the documentation and generate the output based on that. So there are some references for this. My impression is that there aren't a lot of approaches that have really effectively used this in the agentless setting yet.
(45:48) I think people will probably be moving in this direction, and it's possible that there's some new archive papers that I've missed. But I think leveraging this appropriately is kind of like an unsolved problem and we'll want to work towards improving that. So the next thing I'd like to talk about is planning and error recovery.
(46:08) So solving coding tasks can be difficult. And in order to solve the tasks, you want to have some concept of-- you might want to have some concept of a plan and keeping track of when you're on track with your plan or other things like this. And a lot of the top coding models or a lot of the top code agent models that have high scores on leaderboards like SWEBench have a hard coded task completion process.
(46:42) And, for example, agentless is an example of this. It's not actually so much of an agent because it mostly just goes through this process, but it just contains three steps. So file localization, like I talked about before, it has this hierarchical localization process. And then function localization, it then generates patches.
(47:11) And it generates a whole bunch of patches, runs them against a linter or runs tests against them to make sure that they are appropriately formatted, don't have syntax errors, pass existing tests. And then they take the best one, the one that passes the tests and they apply it. And if there's multiple ones that pass the test, they use a similarity metric to try to pick the one that looks the best compared to the others.
(47:39) So this is very effective, actually. Agentless gets good results for how simple of a method it is, it also costs less than some agent style planning methods, but the problem is it's very inflexible. So let's say now you wanted to go online and read the documentation before you solve the task. In some cases you wouldn't be able to do this because this is hard coded.
(48:04) Another thing that's a little bit less inflexible are multi-agent style methods or methods that use an LLM generated plan beforehand. Actually, it doesn't need to be multi-agent. It could be just a single agent that generates a plan beforehand and then steps through the plan. This is an example from a multi-agent system called coder.
(48:28) And basically the way it works is it generates a plan. This plan is based on the fact that this manager has a whole bunch of subagents like a reproducer, a fault localizer, an editor and a verifier. And it has a semi-hard coated structure between all of these agents where you first generate a plan that includes the reproducer step, the fault localization step, editor step, verifier step.
(48:59) And then you have a hard coded control flow where the reproducer runs it. If they're able to reproduce it, then they send it to the fault localizer, if not, they send it directly to the editor. Other things like this. So there's a lot of works that have this kind of thing that's highly tailored towards solving GitHub issues.
(49:19) One problem with this sort of thing, though, is. I've noticed that language model agents very often they'll generate a plan. And then after they've generated the plan, they realize that their plan was not the greatest plan in the first place. Like, their underlying assumptions were not correct. Like, the test directory that they were supposed to use doesn't exist in the first place so they need to go back and modify it, or other things like this.
(49:48) So there's an example of a work-- this was tested on web agents, not coding agents but I think it's applicable to coding agents as well and we've tried it out a little bit. But basically it has the ability to kick the plan back to the planners. So if the agent that's executing the plan fails at doing any of the steps in the plan, it can kick it back to the planner and the planner can say, I need to generate a new plan.
(50:13) I think if you're going to have this kind of structured architecture, this is really, really important in doing a good job. Another really important thing is having the ability to fix outputs based on error messages. And one of the advantages of using the agents versus using an agent list framework is that the agents can actually run the code, see the results of the code and go back and fix the code appropriately.
(50:47) And there's not a whole lot of work on doing this, especially with the strong modern language models, as far as I can tell. I kind of hope that there's more work in this general direction. This inter code is one example of this. But even our strongest models are not super great at this. Just to give an example, GP 4.
(51:12) 0, one of the issues we found is it will attempt to fix an issue, it will fail at fixing an issue, then it will attempt to fix the issue in the same way and get stuck in a loop and just do this over and over and over again until you run out of turns. For some reason Claude is better at not doing this and trying different approaches. I don't know why because the cloud people have not told me what they did.
(51:35) So if any of the cloud people are listening, please tell me. But I think methods that train models to do really well at error recovery are going to be a big research topic going forward. And I think people are working on this now as well. A final thing I'd like to talk about is safety. And this is really important because coding models can cause harm.
(52:01) Right now at the moment I'm most worried about-- because I use coding models in my everyday coding experience, I'm most worried about them causing harm by accident because I told it to do something it either made a mistake or it misinterpreted my commands and it fixed-- and it did some damage. So to give some examples that have actually happened to me before, you have a coding model and you ask it to commit it to your GitHub repository.
(52:36) And you never said you want it to push it onto GitHub, but it just assumes that you want to push it onto GitHub so it will commit things and push them directly to your main branch on GitHub and then you need to go clean up your code repository. You're in the worst case, let's say there's bugs, it causes major issues for people who would be using the repository downstream.
(53:00) This was a really crazy one that I was very surprised by, which was I told the model that the tests needed to pass. And it was having trouble making the test pass so it just deleted the tests. And this is a little bit scary. It's doing something that's pretty obviously harmful in order to obtain its objectives, so we don't want to do that.
(53:30) Another thing is intentional misuse. And there have been essentially papers that have demonstrated that coding agents can be used for hacking and other things like this. And I think this is-- we need to be careful in making sure that we have tools that are able to mitigate these risks. I don't think that means don't build tools because if we don't build tools, then only hackers will have these tools, but we do need to be very conscientious of that.
(54:02) So the first safety mitigation, one that I think is absolutely essential if you're using coding agents in your everyday life is sandboxing. And we can improve safety by basically limiting the environment that the agent has access to. Just to give one example, Open Hands executes all actions within a Docker environment that's isolated from the main system that you're using right then.
(54:28) So it only has access to the files that you put into that Docker environment and other things like this. Also with Docker you can limit access to the internet, you can limit access to any other variety of things. So one general theme with respect to safety is that we shouldn't-- according to my philosophy anyway is we've already thought a lot about safety of software in non-agent scenarios, and so we can reuse a lot of the tools that we already know are important and useful in those sorts of code security, in non-agent scenarios
(55:09) and kind of reuse them for safety. And Docker is one example of that. The next thing is credentialing. And there's a very important concept in security that I think many or most people have heard of called the principle of least privilege, which is that people should only have privileges that are necessary to do their job.
(55:32) Services should only have privileges that are necessary to do their jobs. And just to give an example, there are GitHub access tokens that now have very fine grained privileges and allow you to only do certain things on GitHub repositories. The example on the right is very small so let me read it out loud. The first thing it says is repository access.
(56:02) So you can either make it a public-- give it access to all public repositories with only read only access. You can give it access to all repositories that you have access to or only select repositories. So you can decide which like GitHub repositories it's allowed to access. You can also figure out whether it can run GitHub actions, do administration, for example, modify content, open issues and other things like this.
(56:30) So what you can do here is you can basically prevent agents from having access to more privileges than they need but still give them the privileges that they need. And actually one thing I forgot to say is it's very easy to create a completely useless agent. You just put it in a sandbox and don't give it access to anything.
(56:47) And you'll have an agent that can't really do anything other than work as a calculator or something like this. But at the same time we want to build agents that are helpful while also not causing undue harm, so this is another thing that we need to think about. So another thing that we have looked at is creating post-hoc auditing, auditing for models.
(57:20) And post-hoc auditing for models, the way you can do this is can generate actions. And then based on the actions, you can either decide to execute them if they're deemed to be harmless actions. And if they are deemed to be harmful actions, you don't execute them and you return basically a, like, this is dangerous.
(57:43) You shouldn't be executing that action message back to the agent. And so this can use LLMs, this can use static analysis tools, vulnerability detection tools or other things. And I should mention that this is actually joint work with people at invariant labs and ETH. But this is a conclusion. In summary I think we've already demonstrated that Copilots are very useful, code agents are getting there.
(58:13) I have repositories where I have done half of the commits with a coding agent or at least it's drafted half of the commits and then I've gone in and fixed them, and other stuff like this. So I think in the next one or two years there's going to be the ability to use coding agents in many of our mundane tasks, ones that don't require very much thought and they'll gradually get better at doing things that require more thought and more care.
(58:38) I think current challenges include coming up with good code, LLMs, editing methods, localization, planning and safety. In terms of future directions, I think there's a lot of interesting directions in this area. In addition to all of the current challenges, the first one is agentic training methods. Training on agent style data that will give us the ability to adhere to agent formats, do kind of better planning, recover from errors better.
(59:14) Another thing is human in the loop methods for evaluating agents. So one of the nice things about agents is they can communicate with humans. And I'm much more effective at solving tasks if I'm willing to spend the time interacting with the agent than just asking it to do it on the fly. I can do things like watch the agent do its work and pause it when it seems to be making a mistake and just go in and push it in the right direction and it will correct course.
(59:40) But we really don't know like the best modality for this and we don't have any good evaluation benchmarks to evaluate how well models are able to do at this, so I think that's another big thing. Also broader software tasks than coding. I think there's basically no or at least very few benchmarks that tell us how good we're doing at this in the first place.
(60:05) And software toolkits like Devin and Open Hands can do this. They can do web browsing, they can do other stuff like this, but it's not clear how well they can do at any of these tasks and how much work there is still to be done. So, yeah, that's my summary. Sorry, this is a very broad field. I know I went through a lot of stuff in an hour.
(60:30) But if you want to try it out yourself, you can download our software toolkit. And it does all the things I talked about here. And we'd love to have feedback or collaborate with people and things like that as well. So, yeah, thank you.
