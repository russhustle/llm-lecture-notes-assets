(3) CS 194/294-196 (LLM Agents) - Lecture 5, Omar Khattab - YouTube
https://www.youtube.com/watch?v=JEMYuzrKLUw&list=PLS01nW3RtgopsNLeM936V4TNSsvvVglLc&index=8

Transcript:
ALEXANDER PAN: This is work with a very large number of people, over 200 contributors, and I include a lot of slides adapted from Krista and Michael. The context of this lecture is that it's never been easier, as we're all aware, to build really impressive AI demos. One of the best examples of this is ChatGPT.

It's an interface where you can ask just about any question that comes to mind, and not only will the system find answers that suit your questions, it will synthesize them on the fly conversationally. We're all taking that for granted, but let's marvel at how amazing that is. What makes it really cool and a big step forward compared to before is that it can also help us with tasks.

For example, I asked ChatGPT to help with a piece of code. I wanted to parallelize a sequential Python loop, and it did exactly as I asked. At this stage, we're all extremely familiar with the primary weakness of these language models, which is, in some sense, precisely their key strength: they are so fluent that when they make mistakes, those mistakes can be incredibly hard to detect.

Stanford was not founded in 1891, although it's close. Maybe using that example at Berkeley is weird, but anyway. The whole reason I asked for help with this piece of code was to avoid data races. When I was given code that naively parallelized the loop and had a bug, I was glad I didn't trust the model and didn't simply plug this code into my codebase.

The big picture is that even though it's incredibly easy to build impressive demos, turning monolithic language models into reliable AI systems remains really hard. That's what the rest of this talk will focus on. These are not just problems you see in your personal interactions with a chatbot; they have real implications everywhere. That's just one example of this happening in practice.

I'm not saying ChatGPT or language models are bad because they make mistakes. Every AI system and everything we'll talk about today will always make mistakes. What I'm saying is that, fundamentally, the monolithic nature of language models makes them particularly hard to control when we're building systems, hard to debug when they make mistakes, and hard to improve when we want to iterate on our systems. Increasingly, the way people are tackling this, as we had in the title of the lecture, is by building compound AI systems.

If you haven't heard of this term before, it just means modular systems or modular programs in which language models are not serving as the user-facing end-to-end system, but they're actually playing specialized modular roles inside this bigger architecture. A very familiar example of that, which I'm sure prior talks discussed a lot in this class, is retrieval-augmented systems.

Instead of building a system that takes a question like this and just gives it to your monolithic language model, just a black box, deep neural network, and basically hopes that the system does the right thing, you might break it down into smaller pieces. You have a retrieval model that consults a massive corpus with the question as a search query, retrieves the top key relevant results, feeds those as context to the language model, and then basically prompts the model to use the information there and cite it in its answer.

Now, there are lots of reasons that might be attractive. One of those is transparency. This system might still make mistakes, or it might say the right thing. But either way, we can always inspect the trace of the system behavior, see what it retrieved, and see why it generated the information it did. That would help us see when it's right.

It says something justified based on a citation that's factual. It could also help us see when it's wrong. For example, it made an incorrect inference based on a relevant piece of information, or maybe it simply retrieved something irrelevant and extrapolated in a weird way. There's another reason this is really attractive, which is that a system like this can be a lot more efficient.

It has more steps, for sure. But now, the language model does not necessarily need to know everything about all topics, because we've sort of offloaded that knowledge and a lot of that basically step-by-step control flow, either to a knowledge base from which we're retrieving, or to simply a program that is executing these steps.

So we've gained a lot from having these sorts of compositional approaches. Another example of a compound AI system beyond RAG and something slightly more sophisticated is taking that to the next step and asking, well, one of the biggest powers of language models is that they are not limited to answering questions they've seen exactly before.

They can synthesize or compose information, or at least that's the hope. This is exactly what a compound AI system that does multi-hop reasoning or multi-hop retrieval-augmented generation would do. It's the fact that we can now take questions, instead of simply giving them to a dumb search engine, we can ask our language model itself to act as a module that breaks down this question into little pieces, finds information about each piece, and then basically returns that to the language model, whose job is to then take whatever got retrieved

and produce answers that may be synthesized information in a more holistic way. What's really great about compound AI systems, continuing that line of thought, is that we have a lot of control as the people building this system. You're not bottlenecked at the next release of a language model. Whether you're the one building it, which might be unlikely or not, you actually have a lot of control over the development of your architecture.

And that allows you to iterate much faster and to build systems like this system here, Baleen, that I built near the start of my PhD, that do things that your individual language models at that time absolutely couldn't do. Another example is to take this another step further and to basically say, could we have these systems generate long reports, like maybe articles with citations, like a Wikipedia page? And that's where maybe you could say something, write me an article on the ColBERT retrieval model, and a system like this, which is STORM from Stanford,

could then basically have a lot of modular components that are at a much finer level of granularity. So it could brainstorm. It could generate outlines it could revise these outlines. It could then basically ask a model to generate questions about each part of the outline, retrieve sources, synthesize information in a much more systematic way than a simpler architecture would.

And what's really great here is that because we have this ability to compose better-scoped uses of our language models, we can really iterate on the quality we get out of these systems. So it's much easier for a language model to take information that's given to it and synthesize something reasonable as opposed to maybe tackle a bigger problem of trying to remember facts, for example.

So by being able to manage all of these little compositions, we can do a lot more in terms of quality. And lastly, the sort of final advantage of building these types of systems is inference-time scaling. So basically, you have a language model. It's pretty clear at this point that if you intelligently spend more compute at test time, so you have a question, and you're trying to leverage the language model maybe to search over a large space of potential paths, for example, that's something that can really help.

So an example of a compound AI system where this really helps here is AlphaCodium, which basically is a system that is targeted at generating code. But instead of simply asking a model to produce a piece of code, it has all these steps in which it would, for example, reflect on the problem, reflect on public tests that maybe exist for a given task that you have, generate various solutions, rank them based on their performance, and then basically iterate from there.

And it's not unintuitive that this type of decomposition, which reflects the way you might instruct an intern or a friend or something to approach a task like this, it's not unintuitive that that can really help quite a bit. And that's what this type of work shows. And this is something that keeps popping up in many, many, many applications in many cases, also in task-agnostic form, in the form of methods that basically can scale compute independently of a particular task.

So we talked about how amazing compounding AI systems are and how they give us these five things, from quality to control to transparency to inference-time scaling. So they're awesome. But the problem is, unfortunately, we're working with highly limited modules at the end of the day. Our language models themselves are extremely sensitive to how you ask them to play these roles of these little things you're trying to compose.

And because of that, under the hood, these really beautiful diagrams we looked at, they are typically implemented-- and we're all guilty of this, myself included-- they're typically implemented with thousands of tokens of English or some other natural language trying to coerce a language model to play to play that role of each of these modules.

So this is something we see across all types of tasks, where we're writing sort of tens of kilobytes of strings in JSON files, trying to define our language model systems. And of course, if we could just do that and then succeed in a way that is sort of highly portable and in a way that's highly general, maybe it's fine.

Maybe it's a fine price to pay. But the problem is that it's not. It's not something that will generalize or sort of will systematically allow us to compose these systems. So the problem here at a high level is that each of these prompts implementing each of the modules we looked at is sort of coupling five different roles.

First role it's trying to cover is the role of a signature, the role of, I want a function that's supplying this module. And essentially, it's just a specification of, here are your inputs, and here's what they mean, and here's the transformation I want you to make to give me the outputs. It's also specifying the computation that tries to specialize this signature with some kind of inference-time strategy.

So you might tell the model, hey, please think step-by-step, or you might tell the model, hey, you're an agent, and you have these tools, and you should call these tools. And when you call them, I'll intercept it. I'll give you back the response. Or you might say something like, I'll generate 10 different things and explore all of them with some kind of reward model, for example.

So in all of these cases, we're sort of expressing a lot of that in the prompt and in the code around the prompt. The prompt is also trying to couple with all of that the computation that's formatting all of the inputs we want to give to this function or the signature and is also encapsulating the logic for how we want to get several things and parse them, and cast them to the right types, and maybe retry if they're not really formatted properly, and all that stuff.

It's also, on top of that, expressing, well, the objective. You're not simply describing what the inputs and output behavior is. You're really also trying to encode in your prompt a lot of information about do's and don'ts, and basically what you're trying to maximize-- be factual, or don't hallucinate, or don't cite pages that don't exist, or whatever.

And so that's essentially a different notion from simply declaring what that module is doing, as we see in the diagrams. And lastly, for any given language model, as we said, they are super sensitive, and these pipelines are really complex. So you're going to see in practice that people work really hard to coerce the model to basically do the right thing through a lot of trial and error in English or in some natural language.

And maybe there's also fine-tuning of the weights around that if you're doing a more advanced application. So the bigger picture here across these five roles is that existing compound AI systems are awesome. They have a lot of potential to be really modular and to solve problems we can't solve with neural networks alone or with language models alone.

But the problem is they're too strictly typed, if you will. They couple the fundamental architecture of the system design that the developer wants to express, which nicely is the thing we see in the diagrams we saw of compound AI systems. They couple that with incidental choices that are super specific to the specific pipeline you have or the language model choice you've made, which are things that change all the time.

So you have a pipeline that has five pieces. You go in, and you want to slightly change the objective or introduce a six module or maybe change the language model to a cheaper thing that just came out that is supposed to be as good, good luck. Your entire sequence of thousands of words of prompts are basically irrelevant at that point.

And this is something that really blocks adoption in practice, as well as portability of a lot of the cool systems we covered, where people in industry, for example, could approach you and say, well, we really love that paper. But it's clear that the prompts there were tuned and developed with a particular data set or benchmark in mind.

That's not our use case. We have no visibility or clue how you decided to arrive at those prompts. So we're basically practically unable to use the stuff you built. So an argument I'll be making today and I'll be relying on in the rest of this lecture is that we do know how to iteratively and controllably build systems and improve them in a modular way.

And that sort of elusive concept is called programming. So a lot of the rest of this talk will be about this ambition of, what if we could build compound systems as programs, just computer code, standard Python, but with fuzzy natural language functions or natural language modules that can learn their behavior from data? So you're basically thinking of just writing code, and then inserting these little pieces that are supposed to represent the boxes we saw earlier that are exhibiting intelligent behavior.

And then you specify an objective. And you have the system try to learn behavior around the natural language specifications that you're putting around these modules. If we could do that, we could lose a lot of the problems that we saw with existing compound AI systems, where you're not going to have to be writing these prompts.

You're going to be building a general design that could be ported from a language model to another language model. And in this analogy, the portability is sort of like working with hardware. If you write a piece of code in C on a certain CPU architecture, and you moved to a different CPU architecture, a good compiler is going to take your same high-level C, high-level between codes, and then compile it to potentially very different lower-level machine code on different architectures that makes sure that it works.

So here, the ambition that we have is that we want to write code in higher-level abstractions that are sort of independent as much as possible of these low-level details of how to get a language model to do the thing you want it to do. And then we're going to build compilers or optimizers that take that and spit out specific strategies for working in a given infrastructure, which in this case means a language model or other things.

These are some of the systems we saw earlier. And so the approach that we will take towards this vision is called DSPy. Someone said, I think Alex said, Python has nothing to do with data science. It's a long story. But DS is declaratively self-improving, declaratively self-improving Python. So it's a Python, but it's smarter.

And the way in which we're going to do this is we are going to say, well, let's define this thing called the language model program, which is just a Python program, just a Python class or function. And it's a function that takes inputs in natural language, maybe a question that we want to answer, or maybe like a report we want to summarize, or a topic we want to generate, I don't know, an email about or something.

And the output is also a natural language, maybe the answer, or the report, or the email, or whatever it is. And maybe the output could be several pieces of these. And what makes this interesting as a function is that in the normal course of its execution, it just kind of loops and exceptions and go-to statements and all the things you shouldn't use and all that stuff.

But in the course of its execution, it's calling these functions that are modules that are fuzzy. So it's basically making calls like, hey, generate a search query for me, or turn this natural language into SQL, or whatever it is. So it's making these kind of high-level natural language claims, which look like prompts, but they're much more structured and much shorter, because they're only expressing the stuff that you need to declare, like, what is the actual behavior, not how should the model do it.

So we have this function in which, in its course of execution, it's calling these modules. And each module is basically defined by simply a declaration of what it should do, what inputs it takes, and what outputs it produces. Usually, that's one-liner, basically, in many cases. So here's an example of an actual simple DSPy run I put before the talk.

So basically, you can say, I want a fact-checking module. Before that, there might be like loops and exceptions and commented out code and all sorts of mess. And then I want to apply a chain of thoughts strategy over this function signature that says, I want you to take a set of claims and give me a list of Booleans that is the verdicts of, are these claims true or not.

And then I get a module, that says function. I can give it a list of claims. And it can basically think. And then it can tell me, well, the first one is true, and the second one is false. So, so far, this is basically just kind of an interface of a function that is declared with natural language types. We're basically just saying the keywords here are not special, but I can understand them because I understand English as a language model, and I can see that, OK, the verdicts here is going to suggest that probably,

until I optimize this or learn somehow, this is a true statement. And Python is not a compiled language in standard usage. So this basically defines for us an optimization problem under the hood, which is basically, you're given this set of modules in our program that your program calls. And for every module, our goal is going to be to basically decide how should we take this call and actually call the language model, which means coming up with the string that goes into the model, which might be a lot more sophisticated or just very different from how it

looked at the specification level we saw earlier in the signature. The other thing we also can control in some cases, although we'll not cover a lot of that today, is, well, what are the weight settings that we want to assign our language model in the sense of if we want to fine-tune the model to perform better at our task.

The idea is now we have this function with all these modules. And if you can give me a training set of inputs to that function and maybe some kind of hint or find a label or some kind of metadata, I should be able to basically try to find the settings of the prompts and the weights such that, on average, or in expectation, some kind of metric of calling the program with modules assigned in this way is maximized.

That's the optimization problem that we will be dealing with once someone writes a program in an abstraction that could support this type of behavior, which is DSPy. Now, the problem here is that this is really hard. So we don't have, and we're not asking anywhere, for gradients across a system. System might generate code and execute it.

It might call external tools. It might run a calculator. So we don't necessarily know how to optimize a system like this, certainly not directly with gradients. And we can't cheat and try to optimize each module on its own, because we are not, in general, asking for labels for every step of the system. If we were to ask for labels for every step of the system, the whole argument about iterative development and modularity would collapse, because I added a module, and now you're asking me to go label some data.

Whereas the ideal development cycle that we want is that you're building a system. You know there's a need, you notice a need to add a module of some sort, or you want to experiment. You just inject it. You recompile. You see what happens. You see if your system is better or not. So basically, we can't assume the existence of labels for each of these modules.

So that's a problem, because it's not obvious how to go about optimizing this. And that's one of the things we'll talk about in the rest of these slides. And let's take a concrete example here so that the rest of this sort of is developed in a way we can follow. So suppose that we wanted to build this simple multi-hop retrieval-augmented generation pipeline.

And have it in visual form here, although the DSPy code is basically the same. It's a function that takes a question, which is a string, and it's going to output an answer, which is also a string. And in that function, the last step is, this question that we got was some context. We want to give it to a language model in some way and basically have it generate our answer.

But the question is, how do we get this context? So the multi-hop thing here is that we will have a loop. Maybe the loop could be smarter. It could decide when to stop or something. But for now, we'll just hard code that it goes twice. And in every iteration, we'll ask our language model to take whatever context we've built up so far, which is empty at the beginning, generate a search query, take this query, dump it into the context, maybe append it or something.

And then basically we can loop again so that the system is generating queries to seek things that we haven't found so far. So a question that would come up in this scope would be something like, I don't know, how many floors are in the castle that David Gregory inherited, an example I like to use. So the first query you might ask is, well, who is David Gregory? And then you might learn that he's a Scottish guy from the 1600s or something, and that he inherited the castle called Kinnordy Castle.

Then you might ask, well, how many floors are in Kinnordy Castle given that the original question was trying to seek that. And so that gives you the behavior that we're seeing here in terms of the multi-hop behavior. So that sort of visual function can be written in DSPy code, like here. We're not going to look at too much code, but this is just a module.

If you're familiar with something like PyTorch or just deep neural networks in general, we're kind of borrowing some of the syntax. But in this module, we're going to have an initialization sort of method that just declares the submodules that we have in our compound AI system. So we're going to have a sort of a chain-of-thought strategy express our signature that says, you take some context, you take a question, and you generate a search query.

And all of these are strings, so we're not telling you they're strings because that's the default. And then there's another module that takes the same inputs or same types of inputs and generates an answer. Now, the part of your code that is the actual program logic is here, where simply the loop we saw in the last slide can be expressed.

So that's the forward method can define the program logic that we have. We talked about signatures. So they are telling the system what the module should do as opposed to working really hard with a certain language model as to how it should express that behavior. And the modules, as we said, there are many sort of strategies to spend compute given the language model to define a general sort of approach, to take a signature and actually express it in terms of a language model.

And those help improve quality in many settings. So they are heuristics that we can apply, very similar to layers in neural networks. So I'll go sort of on a tangent here and say, you could, if you want, and this is sort of like-- this is not essential to understanding the rest of the talk, but sort of intuitively, you could think of this as these are normally chain-of-thought or other things, just conceptual things that you can't actually compose because a chain-of-thought, in the original paper, which is super cool,

is basically just a bunch of prompts they wrote for individual tasks. So for answering math questions, you write some examples, and you ask them all to do stuff. If you want to apply it to a different task, you write different prompts. But what we're saying here is, why can't we borrow from layers in neural networks, where you give them dimensions that sort of just describe what types of tensors they can accept and what types of tensors they give you, and then have that behavior be expressed? So in a neural network architecture, you could say,

I want the tension that takes the following sort of vectors and gives me those other vectors. And you could have that. Or you could have an RNN do this, or a linear layer, or convolution, or whatever. So similarly, here, we're asking, why can't we take this sort of inference strategies and make them general in a sort of meta-programming sense, and define them over these signatures, and have them express that behavior in a general way that can be composed? Awesome.

So the question here is, this is the abstraction. This is how you write the program. What are we supposed to do so this actually works as a system? Because this looks pretty. To me, it looks pretty, maybe not to everybody. But to me, this looks really elegant. But the question is, how do we take this and actually give you a system that you could actually deploy and that you're happy with and you could iterate on and all that? So iterating on it is easy, because you could come in and add one more module, add an if statement,

throw an exception and catch it, and you could do all kinds of things. But the question is, really, how do we translate these strategies with signatures into actual prompts under the hood? So you can take this here. And the very first step is pretty trivial, although it's important to do it right, which is that, internally, we can translate this into a basic prompt, one that will not necessarily work really well or anything-- there's no guarantees about it-- through built-in adapters and predictors.

So predictors are just this guy, which is basically another module that has some logic inside. And really what it does, fundamentally, is it adds one more output here, another field here that asks for reasoning. But the adapter is going to take this guy, and is going to say, well, depending on how you implement it, or which one you choose or whatever, it's going to say, well, I have a model here that's maybe a chat model or maybe it's an instruct model.

And I want to format these fields in a certain way just to kickstart the process. So it's going to basically give us a basic prompt under the hood. That's something like, hey, given a field context and question, give me a query. And here is the format you want to follow, because I want to be able to parse the stuff that you give me.

Nothing about this is going to work particularly well, but it's going to get us started. Now, the role of optimizers on top of this, which is sort of a different component, many algorithms we'll discuss, is to take this initial prompt and to take the whole program in which there are many of these modules, many of these prompts, and to look at them as parameters or look at them as having a lot of variables that we can tinker with, and figure out how to maximize the metric that we saw in the objective we wanted to maximize a few slides ago.

So we might start here. And maybe the system would perform at 37% accuracy on a given strict metric. So the actual quality might be a bit higher. But let's say this is something that we're trying to have a metric that's highly precise but has somewhat lower recall. And we might then basically just ask, hey, I like the MIPROv2 optimizer in DSPy much the same way as in a neural network sort of situation, you might say, I like Adam, or I like RMSProp.

You give it some data, you give it the program, you give it a metric, and you ask it to do a good job, and it spits out a better prompt, maybe fancier instructions, and gives it some examples, and sticks them into the prompt of one of the modules or several of the modules such that the quality is substantially higher.

So on a certain real task, instead of tweaking, 2,000 tokens of prompts that look something like this, and at the end of the day, of getting an accuracy like 33% with a certain model from OpenAI, we can explore a much larger design space and get higher scores in interesting ways through these compositions.

So let's look at a results table here on the multi-hop question answering task that we've been using as a running example. The simplest thing you could do in DSPy or sort of just when you're thinking about a compound AI system is the trivial compound AI system that's not actually compound in which you just ask the system to take the input and just predict the output.

And you don't bother optimizing it. So if you do that, we did this a while back, this is exactly a year ago, this result, you can get GPT 3.5 to perform sort of at this level and a Llama 2 model sort of is a little bit behind. But you could say, well, we know that a decomposition like RAG could help, especially that this is a factual task.

We're answering questions. So we could do a RAG system. And the interesting thing is, well, we could also try running one of the optimizers that we'll talk about in a minute. And what you start seeing is the cool thing that already a small model can start to perform better than a larger model with a sort of simpler architecture or even with a sophisticated architecture without optimization.

But the large model can preserve a lot of its advantage here as well. Now, you could iterate on your program and build a multi-hop compound AI system. And there, you can see that, with optimization, you can boost the quality quite a bit with large models as well as with open models. And with more recent models, all these numbers are substantially higher nowadays.

Now, what's really cool, although we're not going to be able to spend a lot of time on that, is the very same program that we wrote here, which is giving us all of these results across models and across optimization decisions that we can make, we can also use optimizers that sort of update the weights that do fine-tuning.

Essentially, a whole space of reinforcement learning is open in front of us. And we can get very small models to sort of imitate these bigger ones. And we can have a model beneath a billion parameters that's many years old-- T5 is 2019-- score competitively, in fact, better than closed at that time frontier models on the given task.

So question that we will be looking at over the rest of the lecture is, what is the space of optimizers like? And what are the interesting choices we can make? And what works? And what doesn't? So in general, there are too many DSPy optimizers to discuss today. They vary in how they tune the prompts and weights in a program, but there's actually kind of a general pattern that you can see in many of them, in most of them.

The first thing is, in general, as we saw earlier, you're going to, for every module, just guess an initial prompt. And this is actually something in DSPy happening outside the optimizer in general. It's happening through what we call the adapter that just takes your signature and makes a sort of a deterministic construction, most of the time deterministic, sometimes the language model, of what the initial prompt should look like.

And then the next thing we'll do is rejection sampling. So we're going to take the inputs that you gave us in your problem specification, maybe we'll raise the temperature of the language model, maybe we'll plug-in a large model, maybe we'll just keep the system simple, and then we'll basically run through your program with these basic prompts to try to collect trajectories throughout all of these steps that lead your metric to assign high scores.

So you're basically giving us this program. You're giving us a bunch of inputs. And you're giving us a metric that can assess when things are good or bad. Maybe it's just checking if the answer is correct, or maybe it's asking a language model or maybe asking a DSPy program to evaluate the system on a rubric or any number of other design choices that you're allowed to experiment with.

So we're going to use those in order to basically start to collect traces of every module or examples of every module of its inputs and outputs when they're chained together sort of lead to high scores through your metric. We can then use these examples in a bunch of different ways to update the modules of your program.

The simplest thing you could think of is if you have examples of every module that have sort of proven to work in the past-- it doesn't mean that they're correct, but it means that maybe there's some hope that they're useful. The simplest thing you could do is try to stick them into the prompt as demonstrations, as examples, to just say, hey, when I got this input, and I did this behavior, it seemed to work pretty well in the past.

And once you do that, maybe you can actually start exploring, well, which of the examples, and can we try to be intelligent about this or not, which of these examples that we just built can be useful for actually taking this module, plugging it into a program, and getting the program to work better on a bunch of other examples? Another thing you can do is you could try to induct instructions.

If you have a lot of examples of a module, you'll have a better sense of understanding of what the module should do and when that leads to good behavior and when that doesn't lead to good behavior. And you can give those to a language model and ask, basically, what is the thing that we're trying to ask here? And the thing to keep in mind is that in all of these cases, language models are being used to build these components, but we're not assuming that language models are good at it.

Language models are going to give you 20 different things, and 19 of them are going to suck. But because we can try so many, and we can explore that space to varying levels of intelligence, we can basically strike gold fairly often and find the combination of these pieces that leads to better prompts in a way that is basically independent of a particular setup that you have.

So you can adjust your program or your language model choices, rerun the system, and have optimization happen. And then, of course, once you build these examples, if you have enough of them, you could basically fine-tune the model on them, or do reinforcement learning, depending on how you're sampling, or do preference fine-tuning and other approaches there as well.

There are various papers that we have that explore these types of strategies. The two ones I requested in the suggested readings were the one introducing MIPRO and the one introducing a strategy for combining prompt optimization and fine-tuning. And the rest of this talk will be about the MIPRO paper. So before we discuss MIPRO, this stuff this is sort of-- we discussed a sequence of abstractions, and then, at a high level, how a set of algorithms that we call optimizers take programs written with these abstractions

and give you compound AI systems that work well, but you're expressing in a way that is more portable and, I think, a lot more elegant. But what's cool here is that these things actually work pretty well in practice. So a few months ago, University of Toronto researchers participated in this MEDIQA competition against 15 other schools and industry groups and whatnot.

And they won the competition in building question-answering systems for medical domains by a 20-point margin against the next-best system. And one of the biggest differences between their approach and others is they use DSPy to express their system. And they used DSPy prompt optimizers. They used an early version of MIPRO, in fact, to achieve this result on three different-- they sort of won three different settings of that competition, which had three settings.

A month later, folks at the University of Maryland, whose lead author sort of is responsible or is leading this prompting guide with, I think, tens of thousands of users or more, sort of worked on a suicide detection task that I believe he got from industry. They wrote a really nice paper about it. He worked for 20 hours.

They documented all of the prompting strategies that he explored for building this system for suicide detection. Basically, it's a glorified classifier. And he documented all the approaches he tried, the prompts he explored. And then in his words, he applied DSPy in 10 minutes. And it sort of outperformed his best system by 40% to 50%.

So they have a really nice paper about this from the University of Maryland. And it has enabled sort of a lot of state-of-the-art systems from PATH for training retrieval models, where we're optimizing prompts that synthesize the data that is used to train small models. Under the hood, I think there's a lot of interesting ideas there.

To IReRa, which is a system for classifying with language models when you have like 50 labels, but 10,000 classes, which I thought was an impossible thing. It just didn't make sense for language models. But Carl built this in DSPy and showed that it's possible. And then STORM, which generates Wikipedia articles from a topic that you give it.

So now, sort of having seen some of these examples, let's look more closely at the paper Optimizing Instructions and Demonstrations for Multi-stage Language Model Programs. My co-first author is Krista Opsahl-Ong and Michael Ryan at Stanford. And a lot of the slides that I have next are sort of borrowed from Krista and Michael in the DSPy work.

So the problem setting, just to restate it, is we are given a bunch of inputs, examples of a task, so maybe questions, let's say. And we are given that the developer built a language model program of the sort we discussed earlier, just a function with a bunch of modules expressed in natural language. And each of them might look like this.

So we looked at the context, question, give me an answer. And it's kind of just trying to do this behavior. I don't know if an example is very useful at that point, but The Victorians is a documentary series written by an author born in what year? So, maybe you want to answer that in 1950 by looking at context that you retrieved in the earlier loop that we have here.

We're also asking for a metric. Some metrics by nature require labels, like, for example, here. If we know that the answer is 1950, we don't need labels for the whole thing. But if we know the answer is 1950, evaluation is easy. Other times, maybe the metric is like, I don't actually know what the answer is, but I want to get answers that are grounded in the context that got retrieved, because I trust the context.

And if the answer is grounded in it and is relevant, then that probably means it's correct. That's a different metric that does not need labels. So there are a very large scope of things, but it's really important that you explicitly define what is it that you're trying to maximize. And once you've done that, you don't do this as part of your prompts.

You do this in a way that is independent of your program so that you can explore along both axes pretty modularly. So that's something important that you should keep in mind. And the abstraction really forces you to do that. So the goal is to give us an optimized language model program P prime by, in the case of the optimizers we look at next, we're going to keep the weights frozen.

In fact, we'll assume we don't have access to them. Instead, we are going to optimize the instructions, so the descriptions of the task that goes to the model, as well as the demonstrations, which are these few-shot examples that just show the model in the prompt, here are some inputs and here's the corresponding behavior we saw in the outputs that seemed to work well in the past.

Some assumptions that we're making is that we don't want to assume any access to log probabilities or model weights because you want to iterate sort of fast. You want to work in natural language space with APIs that are high level. We have a lot of work where we relax this assumption, and we look into fine-tuning.

And actually, we have work that shows you really need both to get the best performance in many cases. But if you had to pick one, prompt optimization tends to be more powerful. We also assume no intermediate metrics or labels, as we said earlier. And we want to be very budget-conscious. So we want to minimize two things.

We don't want to ask you for a lot of example inputs of your task because it's hard to create inputs, although, technically, inputs is the easiest thing to scale, because you can build a demo and then put it in front of your friends, and then you have a lot of inputs when they ask questions, if your privacy policy allows you to do that.

But we don't want to ask for a lot of input. So maybe we want 50 or a couple hundred, not tens of thousands, for example. And we don't want to call the language model too many times, because it takes time, or is expensive, et cetera. So these are the assumptions that we want or the constraints that we have on this problem.

Now, there are two types of problems we want to tackle in general. The first is that, well, what's a prompt? It's this long sort of combinatorial string. And it's anything really goes. It's all valid. So how do you even explore this space, especially if you don't have gradients? And if you don't want to call the model too many times, it seems basically hopeless.

And the second problem is, well, if you have so many modules, which, really, this is kind of an think an even worse one when you combine the two together, is if you have so many modules and you're changing things all over the place, how do you know what is leading to improvement or what's sort of hurting? Because these pieces interact.

You might improve the prompt in one part locally, but actually hurts because the other part made an assumption about the output of the first part. And now. You sort of have to account for these types of blame assignment. So I'll cover three methods. This is not exhaustive by any stretch of the imagination, but these three are really good different methods of tackling this problem.

So the simplest thing you could do, in my opinion, at least for the value it gives you, is to bootstrap or self-generate a few-shot examples by running a dumb version of your program to build examples and then to plug them in and to search over that space. And the cool thing is you can iterate that. You can take the better program, build even better m build ensembles of them, use larger models to build examples.

It's a very large compositional space. But in a simplest form, you have the program, you take a training input, you basically say, like, I'll execute the Python in here. Some of these are just normal Python return statements, or loops, or I don't know, code executor or whatever. But some of them are modules, and so they are special.

We're sort of tracking them and tracing their inputs and outputs. And basically, we say, well, these things are generating search queries and generating answers. And if the metric at the end ends up liking the answer and tells me that's good, well, that whole trajectory of inputs and outputs seems interesting enough to keep it around.

Now, this can be a set of demonstrations of all our modules, and we can simply basically say, what if we take those, we plug them into the modules of our program, and then we could do, in the simplest case, just a random search, take kind of a bag of a subset of these demonstrations, plug them into the respective three or four prompts, and take that, run it on a small validation set.

Maybe you could try to be smart about how you're doing that evaluation. And then basically look at that score and try to maximize that score. So that's, I think, the simplest successful thing you could try. A different approach is to look into the prompt optimization literature, not for language model programs, but for language model calls.

And those folks sort have a lot of rich, concurrent sort of research, but under much stronger constraints. So in the prompt optimization literature, which is, I mean, these things are sort of, of course, morphing closer together, but they start to pretty separately. You are assuming that you have labels, so you have inputs and outputs, and it's one prompt.

That's the whole system. There's no program involved. And you're assuming that there's this little part, like a one-liner somewhere, that you want to search over. The space of this sort of exploration is very large. People do all sorts of things from gradient-based exploration to other sorts of forms of reinforcement learning, and all types of stuff.

But one approach that's really cool and really high level is called optimization through prompting from DeepMind. And the way that works is basically they want to plug-in a little prefix at the end of the prompt to just get the model on the right track. And so, they start with things step by step, but they want to end up at the end with, I don't know, take a deep breath and think step by step.

And they show that could actually help some models do a lot better. It's a much smaller sort of scope, but it's an interesting space of what to explore. And we want to see if we could take this and apply it to language model programs with the weaker assumptions we have. So the way OPRO works for a single prompt is they basically go to a model and say, here is the original thing.

Give me 10 variants or 100 variants, and let me basically just evaluate all of them on a little sort of evaluation set. And let me basically look at which ones worked best. Maybe the top 10 go back to the model and say, here's the top 10 here, here's how well they performed. Give me more. And the idea now is the model might be smart enough to say, I see some patterns between the successful things and the unsuccessful instructions that I can sort of do essentially a form of you might call that basically mutating these instructions.

And I can basically try them again and repeat this process. So that's the OPRO approach. So how could you take OPRO and naively apply it to a language model program? Well, the simplest thing, I think, is coordinate ascent. So what that means is, and we're not going to spend too much time here. I don't know if the figure is big enough.

But let's say you have two modules. The simplest thing you could do is basically go to the model, generate a lot of these proposals, plug one at a time, keeping the other modules fixed at the initial instruction, evaluate all 10 approaches, and then basically see which one worked best. And maybe freeze it and sort of repeat for optimizing the next stage.

And you can loop through this process of optimization. The problem is this is horrendously expensive, because every time you're exploring optimizing one module, you're sort of running the whole program while freezing the other modules, and you're making this big greedy assumption that the best sort of choice at any given stage is going to be useful once you freeze it and try to optimize the rest of the parts.

You're not co-optimizing these pieces. It's very expensive and actually doesn't even work that well. A different approach here would be to give up an assumption or to make an assumption. So you could give up on the assumption of explicit credit assignment. You don't really care to figure out fixing all variables and changing one at a time.

You could just ask the models to generate prompts for every stage all at once and sort of hope that that process converges in a nice place. Or you could go to the model and say, actually, it's a bit of a harder task. It's a bit of a harder ask. But here's the sequence of all prompts of all models, and I want you to propose new prompts for all of the other models.

And you could try to do it jointly, which, of course, has the risk of potentially confusing the model. It's a much harder task but is something that, let's say, with an Oracle language model, it's strictly more powerful by far. Now, if you want this whole thing to work well in a language model program, it is really important that you're not myopic about optimization.

So you don't just look at the signature, just saying generate a search query, and the user didn't say search query. The user just said query. Is it a SQL query? A search query for Google? For a retrieval model that is you have offline? These things are different. And you are at the mercy of the quality of the proposals you're getting, so you might as well try to make sure that they're contextualized appropriately with respect to the program you have.

So that's this notion of you want the proposals to be grounded. So sort of in a traditional prompt optimization setting, there's the history of the prior steps, how we got here, what things we tried, how well they performed. And then there is maybe a training set of explicit examples that you sort of got from the user because your whole system doesn't have any missing pieces.

It's just input and output. So we have this intuition that to take this and to generalize it to language model programs, it would be very useful to provide a lot of contextual information about the program setup that we have. And so the first thing is, because we don't have these training input-output labels for every module in general or for any modules, in many cases, we can start plugging in the bootstrap demos that we looked at earlier.

So they're basically as good, for all we know. And they seem to be successful when you're doing bootstrap few-shot. So maybe we could just plug them in as the examples that we'll use for constructing these prompts. Now, that by itself gives you a lot, because you could start to see what works and potentially what doesn't.

And here's an example of a question. You want to generate a search query and the reasoning that the model generates when it's building the search query that it produced that eventually sort of led to a trajectory that was successful on a certain question. The other thing is, well, you want to give the model sort of an understanding of the whole task.

So it would be useful if the model building your instructions could see a summary of the data set that we're playing with. And so this is sort of an example of that, where the model basically says, looks like this is on the multi-hop data set consists of factual trivia-style questions across a wide range of topics, et cetera.

So you basically have a component that builds up the summary by essentially some sort of you can think of it as a MapReduce over the data set with a language model. And then an interesting thing that you might want to ground your system in is, well, could it actually see the whole pipeline, so it understands the role of every module in it? And this basically works by literally inspecting the code that you have for a program and then giving it to the language model that understands the syntax.

And basically, it builds a representation of the program here in natural language that says, hey, we have a program that appears to be designed to answer complex questions by retrieving and processing information from multiple sources. In this case, it's setup for two hops, et cetera. And the module in this program is responsible for generating the search query, et cetera.

And you might also try to maximize the diversity of the stuff here by just sampling a random instruction from the standard literature and just kind of plugging those in for your proposer. So z hey, don't be afraid to be creative or all these things. We could just take them out of the loop. You don't have to write them.

We will write them once, and then we can basically reuse them for building these systems. So we just discussed extending optimization through proposing with language models. So we've covered two general strategies for optimization here. One is building examples and searching over them. The second is giving the model as much grounding as possible in the language model program and asking it to propose instructions that we can iterate over through learning from the previous attempts.

And then the last approach here is going to be a little bit different, where our goal is to optimize both the instructions and the few-shot examples, but in an efficient way of dealing with credit assignment. The intuition here is, language models are not particularly good yet at dealing with credit assignment themselves.

But because we're working with all of these sort of spaces of discrete proposals, like a lot of instructions or a lot of examples that we're building, we could actually borrow a lot of intuition from the literature on hyperparameter optimization. And we can build what is called a surrogate model, a model that is basically optimized to predict the quality of any configuration possible of our system, and that we can use to sample proposals for the entire system that we can actually test.

So MIPRO optimizer works in three steps. First thing is it bootstrap demonstrations of the task. We've explained what that means. It builds candidate instructions using a language model program inside the optimizer that has all these pieces we looked at earlier, like the data set summarizer, the language model program describer, all these little pieces.

And that handles this form of credit assignment here by relying on just a simple probabilistic model from the hyperparameter optimization literature. So what we have here is a language model program with two modules. And in every module, we have essentially two bulky parameters. One is what's the string describing the task, which is the instruction.

And we get a basic one from the adapter that we discussed earlier. And then no examples at first, but this is the list that we can learn as a bunch of these input-output examples. And that fact that we've done the bootstrapping and the candidate proposal in a grounded way means that we can basically start exploring this discrete space of what is the right combination across these modules of instructions and of sets of examples or lists of examples in each module that would lead to the highest quality? And of course, we can't try all of them.

So we're going to rely on this Bayesian optimizer to basically help give us an acquisition function that allows us to make good guesses as to which combinations we should try. Once we pick a combination, we can plug it into a version of the program and then basically evaluate it on a mini-batch of the data set that we have, on the validation set that we have.

So maybe the data set has 200 examples. We could sample 30 of them, get a score, and then go back with that score, let's say, 75%, 50%, whatever, and feed it back to update the surrogate model that we have. So it's like, hey, if this is the set of combinations I chose, that gets me, I think, 50% because it depends on the random sample in the mini-batch.

So in the future, we're trying to sort of get an acquisition function that sort of has, let's say, a property, like, I would like to get the most promising improvement in future proposals. And there are, of course, many sorts of such choices you could make. And we can repeat this process and trials until we find a really good combination at the end of the day.

And what's happening here is that this thing gets smarter over time. So the combinations it proposes tend to go up over time as opposed to, say, random search, where you're just trying combinations. So how do these things compare? We looked earlier at a slide that had the results with pretty powerful optimizers with yes optimizing or just using the off-the-shelf thing.

We also looked at the results with people writing prompts by their own hand. But it's also sometimes instructive to try to think of ways of benchmarking these different optimizers against each other. It's actually really hard, and it's a place where we need more contributions, because you want to build tasks that are representative, tasks that are hard, not overfit by given language models, but where you also can build interesting and realistic compound AI systems that we can optimize and explore.

So an attempt at this is LangProBe, language model program benchmark, that sort of has these tasks, so like multi-hop stuff, classification, inference, whatever. And some of them have two modules, some have four modules, some have one module. So here are some sort of initial part of the test set results on a bunch of these tasks.

And we're looking at optimizing instructions only through we looked at module level OPRO we discussed before without grounding and with grounding. And then zero-shot MIPRO that's not allowed to use examples, but only uses instructions. And the first thing that jumps at you is that, well, on average, optimizing instructions compared to using a basic kind of adapter can help quite a bit.

So you can get a few points here or a bunch of points there, can get some nice bump there. Sometimes, it overfits to the training set if it's really small, and it's actually worse. But in general, it really does struggle with a lot of these tasks to optimize prompts and instructions alone. And it's not really obvious which of these wins.

If you look into optimizing with demonstrations, that picture is pretty different. You can already get 10-point jumps, several point jumps here, several here as well, it overfits here. And it seems that instructions are actually quite powerful. And here, you can get very large gains as well. And the interesting thing is, when you look under the hood at this process of just random search over bootstrap demonstrations that the system generated, it's wild how much they vary.

You have stuff that's worse here than the initial zero-shot approach, but you have stuff sort of on the frontier that's way better. And it's really interesting that all of these were generated in the same way by running the program and basically doing a rejection sampling. So it is interesting to see how big of a difference they can make when you plug them into the program.

And the last thing is, well, you could take MIPRO, which does few-shot instruction optimization both together, and you can get the best results quite often, most of the time, with some nice jumps across various pieces of these. An interesting thing is, well, optimizing examples tends to be a winner, but there is sort of patterns where you can spot that focusing on instruction optimization is actually leading to more visible improvements, which is basically cases where the task has this property of a conditional pattern, where

seeing one example doesn't actually teach you the whole pattern, or seeing two examples doesn't teach you the whole pattern, because there are more cases than you can cover with just examples, or the precise threshold or the precise kind of region in which a rule applies is not clear from examples by themselves.

So kind of concluding here and maybe opening for questions more explicitly since they're open anyway, sort of just some set of lessons on what you might call natural language programming, which is what this set of abstractions in DSPy allow us to do. A big lesson not to forget compound AI systems is that programs can be a lot more accurate, more controllable, and more transparent than using deep neural networks alone, models alone.

And we sort of just need declarative programs. You don't need to sit down and write 10,000 token sequence of five prompts. You can really write 10 lines of code, just express an objective, and pick an optimizer, and run this thing on your favorite language model. And the high-level optimizers can bootstrap examples for prompts or propose instructions and explore that space.

And that they are pretty bad at any individual proposal on average. But you can run a large scale search on this that does actually find things that, in some cases, in many cases, outperform the stuff you can do by hand. But the real power here is because you're freed from the choices of, how do I tune each module, you can explore the space of compound AI systems more directly, where you're getting a lot of value from iterating on the modules you're building, how they're connected together, what is the exact right objective that I

want to maximize. In many cases, it's not obvious, et cetera, et cetera. So DSPy makes it possible to approach this natural language programming by sort of yanking out handwritten prompts and giving us this notion of signatures, throwing away prompting techniques and inference-time strategies that are really fuzzy and what is a chain-of-thought really, and giving you actual predictors you can compose as modules that can take your signatures and apply in a meta-programming sense the strategy on top.

Manual prompt engineering, you throw that away, and you work with optimized programs, where you're tuning the instructions or the weights in a given strategy. And this is something that's being widely used in production and in open source. Everything is a DSPy AI, including links to all the papers I've discussed or didn't discuss.

So let's use a JetBlue, Databricks, Walmart, VMware, Replit, Hayes Labs, Sephora, and Moody's, and other stuff. You can find a nice list of public-facing use cases, people that are sort of fine going on the record to learn what people are actually doing with this. And it's a really great way, actually, to learn not just what people are doing with DSPy, but what kinds of compound AI systems are getting actually deployed, and who's deploying them, and how are they optimizing them.

Many of these folks go on podcasts and describe how they're optimizing their systems for law firms or other things. So this is sort of a nice, great folks in the open source community, cool collaborators that make all this stuff possible. And you could pip install DSPy and get started right now. Just to conclude, key lessons on optimization in natural language-- in many cases, in isolation, nothing seems to be building good examples of the task automatically.

What we call bootstrapping seems to be this notion show, don't tell. But on tasks where there are these conditional rules that are of scoped and hard to detect ways, optimizing instructions seems powerful. And I should make it super clear that the biggest, coolest thing in DSPy is that we've isolated signatures from optimizers, from adapters, from metrics, and basically from inference-time strategies, and that you can iterate on any of these four or five things independently and compose with everyone else.

So all of the programs that exist right now, if you decide to build an RL-based prompt optimizer, or if you decide to introduce an inference-time strategy as a predictor, everybody could just change one line in their code, and then you could see boosts across the entire set of use cases that exist out there if you do it well.

And there are all sorts of tricks that make this work that we've discussed here. All right. So actually, I had this slide, and I like to use this slide. But right before the talk or the lecture, someone told me, many of the other lecturers-- and maybe I'm counted with that, proudly-- but sort of are from large, closed labs that don't publish.

They're certainly not academics, certainly not anymore. And they seem to be leading all of the progress and whatnot. And so I really want to say that a big goal of DSPy, sort of a meta goal, is to enable open research to again lead AI progress. And open research here has a lot of advantages for us in terms of why we'd want that to be the case.

It's not part of the scope of this talk, but I'll tell you how this DSPy really makes a difference in this space. And it's to basically show that that progress is really going to come through modularity. So we're not asking people to figure out how to fund billion-dollar runs in isolation or these ad-hoc tricks that apply, and then two weeks later, the model changes, and it's not really very relevant anymore.

But instead, we've sort outlined this space that I hope I convinced you of basically how do we scope out your programs well, how do you develop general inference-time strategies that act as predictors that anyone could apply to their signatures, and how we can devise new optimizers that could apply to any of these programs that give us systems that are stronger than the sum of their parts, if you will.

And through that, I hope that a lot of open research on optimizers, predictors, modules, and whatnot can lead to the type of progress we saw with neural networks, where different people developed attention transformers, convolution, and other things in a way that is highly distributed and was obviously incredibly successful as opposed to maybe the way in which maybe we iterate on large language models in a closed way nowadays.
