(3) CS 194/294-196 (LLM Agents) - Lecture 8, Yuandong Tian - YouTube
https://www.youtube.com/watch?v=wm9-7VBpdEo&list=PLS01nW3RtgopsNLeM936V4TNSsvvVglLc&index=5

Transcript:
(0:00) YUANDONG TIAN: My name is Yuandong Tian. I'm a research scientist director in Meta AI FAIR. Today I'm going to talk about our recent work on the reasoning and planning domain and also agent domain. So the title is, towards a unified framework of neural and symbolic decision making. OK, so we all know that language model is being used very extensively in many different scenarios.
(0:24) So here I'm basically going to list a few possible applications that larger model has already proven to be super useful. So people have been using language models for conversational AIs to generate content, and also use that to play against each other, to basically do roleplay, and also do AI agents as well.
(0:45) And recently, a large amount has also been used for reasoning and planning. And we also see that by using [INAUDIBLE] of sorts, the reasoning capability of larger model actually become much better. And at the same time, we also see issues with action models when we are trying to leverage that in different planning tasks.
(1:06) So in this lecture, I'm going to be focused on the unit and planning component. Since in Meta, I have been working with a bunch of very creative and very talented people together, working on larger model with reasoning and planning and trying to find a way to make their planning and reasoning technique and strength become much stronger to the next level.
(1:32) So let's see. What are the interesting components that we can do for reading and planning? So one thing that we want to think about is that in order to improve the existing reasoning, planning capabilities, of course, we want to understand what is the weakness, and how can we overcome? How can we overcome such weakness with different approaches? Here's the one example that I probably use it for many different scenarios for showing that the larger model really does not plan very well in different scenarios, especially in scenario that is common in our daily life.
(2:11) So one an example is the travel planning problems. So for travel planning, the task is super simple. We just ask the model about a travel plan. So I want to say, I want to go to Hawaii. I want to go to Seattle. I want to go to California at some specific date with some specific budget. And then I want them to give me a very clear, executable, feasible and even optimal itinerary.
(2:41) So this is the task we want to go. So in order to achieve this task, then people may still start to think about some identity behaviors. So of course, we first want language model to query a few informations, a lot of information, from outside, from external world. So for example, you can ask language model to do a search, and it will give you some of the possible directions, the possible destination you want to go.
(3:11) You also want to ask the model to do a flight search or a hotel search, and you get multiple entries of possible hotels, as well as basically the-- and also the flight information, as well. So once you have all this information, put it together, then, of course, you want the language model to plan. So given all this information, hopefully the model can figure out a path, an itinerary, that will connect all the existing informations together into a clear path or itinerary that can be executed.
(3:49) So the figure to the bottom right, basically the figure, basically the real plan, that we want to use in order to execute the plan. But interestingly, what happened is that if we actually use the current language model to do this planning task, it's actually not doing very well. So this is a paper that we published in this year's ICML as a spotlight.
(4:20) And in this paper, this is the main table that we put to show the incapability of language models for solving these kind of problems. So first of all, the top chunk of the table basically shows that even with the GPT 4 Turbo, which is the model that is the strongest at that time, we actually see that the final pass rate of the entire travel itinerary is only 0.6%.
(4:48) So here you have a percentage here where we don't show in the top. But even if we give the language model the ground truth information of the tools and we ask them all to directly plan the travel, then the final pass rate is also only like 4.4%. So the pass rate definition is very simple. Given the itinerary, you really want to see whether every stage of the travel satisfy the constraints.
(5:19) We say the constraint means that you might have a criteria that you don't want to be in a smoking room. So you might want to say, I have a budget constraint. The budget cannot exceed some fixed amount of numbers, et cetera. So you can basically have some code to check whether the travel itinerary really pass all these criterias.
(5:42) But unfortunately, the final plan quality is not great, and you have to really have a human involved in order to find a plan that is really executable. Of course, I mean, people will say, OK, this is like a paper-- that basic paper that is being published at the early this year. And in luxury model era, every month counts.
(6:06) Every week counts. And the people published, like, half a year ago basically means it's ages old, and people may not care anymore. So how about using old ones, like the current models for reasoning and planning? And thanks for the courtesy of the first author for this paper. We also get some overall numbers with this short planning benchmark.
(6:31) Then what happened here is that you still see that the overall pass rate is not great for our mini and also on preview. They are even worse than the model that is fine-tuned to that particular task. So it's not really solving the problem for the travel planning problems. And you can actually see the same thing for the LLM plan and in other scenarios.
(6:56) So you can see that-- so the figure to the left is actually from the DeepMind Gemini team. So in that paper, what they show here is that for planning problems like trip planning and also meeting planning, you see that the performance actually dropped substantially with more complexity in term of the number of variables and also the scale of the problem for the planning task.
(7:28) So while a Gemini is still doing great in these benchmarks, you will see this worrying trend all the way down to zero where you have more and more cities or more and more people showing that the problem scale goes up. OK. So the figure to the left basically shows the performance before release. But the figure to the right actually shows what happened after release.
(7:54) Of course, you see that O1 is actually doing a very great job. So you see super high performance for when these are applied to smaller-scale problems, like big gap between O1 and also existing models. However, if the scale of the planning problem goes up-- so the plan learns from going from 2 all the way to 14 or 16, then all of a sudden you see the curve also drops to 0.
(8:26) So this is actually from OSU. The paper is called the [INAUDIBLE] Plan. It's a study on that. So this actually basically gives you this kind of sense that while you can see the bias term of the curve is moving up substantially, the trend of the entire curve still goes down substantially. And the trend does not change over time.
(8:51) So this is actually, in some sense, a very worrisome kind of curve that even the best models that we have encountered have not solved these kind of problems. Of course, the next slide will be, what are the solutions? What are the solutions? So in this lecture, I'm going to talk about the three possible different categories of solutions.
(9:14) And they are listed there. So the solution one, of course, is the scaling law. And we say with more data, more compute and larger models, and hopefully one day, then, we're going to see better performance and going to eventually solve all these problems with all these bigger models. So of course, they are very expensive.
(9:35) And then this also involve a lot of engineering challenges, but may not necessarily involve a new kind of thinking and thoughts about how we solve these problems. That's definitely one path. I don't want to comment on how good or bad of this path, but this is just one option, something that you can think about.
(9:56) There's a second option that's called hybrid systems. In that option, what we can think about is that, OK, so we can see it's very clear that there are two complementary paths. If we use deep models like foundation models, they are very good at understanding the natural language instructions, but they are not doing very well in doing very good planning tasks.
(10:22) So they will have bad solutions. The solution is not high quality, and et cetera. So on the other hand, there's another one called the combinatorial solvers, or symbolic solvers. So these solvers basically can give you optimal solutions in many different scenarios and will have guarantees and often run super fast compared to neural networks and do not require GPUs in many cases.
(10:49) And these are basically very good solvers for complicated planning problems, but at the same time, these solvers don't understand natural language. They don't understand the user's request. They don't understand many flexible scenarios. So maybe we should combine them together into the same system. Then you're going to see that these two can be put together.
(11:13) And it can give you something that is stronger than both of the side. So on these options, there are several different ways of combining them, which we're going to talk about in the following slides. And that's actually option 3, which is actually, say that there's actually a way to understand the neural talk and find that there's a symbolic representation that emerges automatically from the green descent optimization.
(11:44) So that's something that could be super interesting. And recently this is something that we have found in our very recent research, but I'm going to talk about that later after the option two. So let's start with this one. So there's different ways of combining deep models with solvers. And of course, the first one that we can do is tool use.
(12:05) So for this, we actually have follow-up work for the trial planner about how can we leverage mixed integer linear programming and neural network, larger models, together in order to solve the travel planning problems. So here, instead of asking the larger model to directly give you the optimal itineraries, which is very hard and even infeasible given the current problems of issues of luxury models, how about that? We can ask the language model to first convert the natural language input into JSON file.
(12:48) So here's the JSON file. And given the JSON file, it gave you all the symbolic representation of the request for the user. And given this request, combined with the flight and hotel information, you can actually call a solver. Here we are using mixed integer linear programming solver. And that solver will give you some symbolic description of the optimal itinerary.
(13:16) And you can use the optimal itinerary, and you can convert that back to the actual sentences. And this will be used as the response of the agent. So you can do this. Instead of doing a direct pass, you can take a detour through the symbolic solver, and that detour will give you some solution that is guaranteed to work better.
(13:38) So we actually built a demo on top of that. And our demo actually was just accepted in this EMNRP demo session to be held in Miami. If you are interested and people are joining NRP, please go ahead and take a look. So this entire thing can be a demo because it's in real time. So here's basically a breakdown of the speed.
(14:03) So we actually fine-tune instruction translator. That will take, like, two seconds, or 2.5 seconds, to translate from natural language all the way to the JSON file. And then we also have a MIRP solver, intelligent programming solver, that will basically solve the problem. And the solving is actually very, very fast.
(14:27) It takes 0.7 seconds to solve the problem. So this will make the entire demo real time. And people can actually play with it. And we also have human evaluations, and basically human subjects ask about these questions, whether the system is useful, whether it satisfied the request, whether they offer a good amount of value for the money, and whether this is efficient.
(14:55) And you can see that most of the human subjects, they are, in fact, positive on our system compared to the baselines. So that's actually quite interesting. Of course, the next step would be mostly around dialogue. So here in the previous systems, what do we do here is that the agent basically take the single-round dialogue, take the user request at one single round of the request, and send this request to the neural network, to the larger models, and we will convert to a JSON file.
(15:37) But in practice, people are not doing that, right? People always start with some rough idea about travel. So I want to go to Hawaii. I have no idea what else, but I want to go Hawaii. But then the agent will try to ask a little bit more about details. How much budget? Are you a people who really care about money, or are you people who care about experience? So there's actually a lot of detail questions you really want to figure out.
(16:03) And this is actually a natural scenario for multi-round dialogues and discussions. So in order to achieve that, we actually train an agent that can basically proactively ask questions to the agent, to the travelers, and traveler will answer these questions accordingly. And the agent is aiming to efficiently get all the information with the minimum amount of dialogue and then convert them into JSON and compare this JSON with the ground truth JSON that's being created privately from the data generation process.
(16:42) And how we achieve this agent is that we actually design our agent constitution in order to allow the agent to follow these instructions. So the constitution that we develop is called APEC constitution. So it consists of four different component. So first you want agent to be super accurate. And the second, you want agent to be proactive, means that you want agent to ask question proactively to get answers.
(17:16) You don't want agent to ask random questions. That is not interesting. So you also don't want agent to be very impressive. So agent needs to find what are the interesting questions you want to ask and then answer them. And we also want agent to be efficient, which means that agent don't want to waste time and effort to evaluate questions, but most important questions.
(17:38) The agent also need to be adaptive to different types of the travelers. So we actually set, like, 50 different personas of the travelers. Each persona of the traveler will deposit different behaviors. And these traveler will have private hidden spreadsheet of most critical information. They really want to get it correct.
(18:03) And then we want to compare whether the agent can actually get all this information in a short amount of time. And finally, we also want the agent to be credible, which means less generalization, less inconsistency, et cetera. So we can use all these constitutions, and we can fine-tune the models, and we can see that after several rounds of fine-tuning using DPO, our models actually become a little bit more efficient in basically two categories.
(18:34) One is called overall. Overall means all the entries. And critical means that the critical entries for particular persona of the travelers out of, like, 50 personas. And we also want to optimize the agent [INAUDIBLE] scores, [INAUDIBLE] classification, et cetera. And we see that after few rounds of DPOs, the performance actually goes up, and the system also doing a good job here.
(19:02) So it's interesting, showing that if you use agent constitution, it's possible that we can improve the agent performance along different axes simultaneously with very simple techniques. And this can be also better than existing approaches, like if you just use Llama-3.1-B, [INAUDIBLE] models, the performance is actually not great because it's not been fine-tuned along these directions.
(19:28) So recently, there's a separate paper that also involved multiple Meta authors, is that alternatively we might also use agent as a judge instead of having humans as a judge. So in this paper, what we do here is that traditionally, we basically use larger model as a judge to judge the behavior of agent. And we may only check the final results of agent given all the outcomes.
(19:58) But in this paper, we are trying to use agent as a judge, and the agent will try to check every step of the target agent and to see whether they are doing very well or not. And doing that, we were able to give you more refined and more rich feedbacks for the Meta agent to improve. OK? OK. So we are finishing this first part of using deep models to call the solvers in order to finish the task.
(20:33) So let's proceed with the second part. So the second part is that maybe there's different ways of combining agent, combining deep models, with solvers. The second part is like, how about we use the solver to provide data to deep models to train better? For this one, we actually have a bunch of direction tasks-- sorry, a bunch of research directions, that actually are showing this is also a very interesting and nice trend to follow.
(21:03) So for this, we're going to introduce this search former results. And for search former, what we do here is that the goal is to basically predict the outcome of the planning task. So here is a very simple planning task. You have a maze. You have multiple walls. And you want to find what is the shortest path between the starting point and the goal point.
(21:28) So here the maze is 3 by 3, and the shortest path is very simple. You just go all the way down and to the right. Sorry, and to the left. And then you go to the destination. Very simple. But if before you actually get to the destination and get to the optimal plan, if you execute our symbolic solver, here the symbolic solver is A star search.
(21:52) So A star search is a very seminal work that trying to use a heuristic function to find what is the optimal path from the beginning state all the way to the end state. It's been used very extensively in many different scenarios. And here we are just to use the-- we use this approach to find the shortest path.
(22:15) So if you think about how A star search solve the problem, before it actually gives you the final optimal plan, it also gives you all the intermediate steps in order for you to reach the optimal plan. So, for example, in this particular case, what happened is that A star search will start to create a new node during the search and explore different parts of the maze to see which paths can lead to the destination.
(22:45) So this entire procedure will create these intermediate steps, which is shown in purple, and also the teal colors. So this basically is a kind of internal steps of the search. After the search procedure was finished, then we actually find what is the optimal plan for the entire problems. So the idea of search former is that instead of trying to model that directly, predict the plan given the prompt, prompt is basically the description of the problem, which this is something that people often do.
(23:21) So we have some description of the problem, and then we directly predict what is the outcome, optimal plan of the problem. In our approach, what we do is that if we start from the initial prompt, we can actually first predict the trace, the search trace, of the problems. And then, given the search trace, we can predict the final output plan.
(23:46) So that's called the search augmented model. That's the model that we propose in our paper as the first stage of models. So if you check how many tokens we are using, so for these two settings, of course the solution only models typically will only generate maybe 100 to 400 tokens. But the search augmented model may generate much longer tokens, number of tokens, because it will also output the trace, how you do the search and then the plan.
(24:19) So people may wonder, OK, now you have some kind of prediction problems that need to take a detour to the final destination. Do you think that's a good idea? The answer is yes. You can actually find this a very nice screen log curve associated with the search augmented models. So first, let's check out here the solution only curve.
(24:43) So this is basically the model that can be used in many different cases. But also you see a scaling law curve is not so perfect. So for this curve, what happens is that you need to have a million number of training sequences in order to reach something that is super high in term of the performance on a test set.
(25:10) And this basically is using 175 million parameters in the solution only models for 30 by 30 maze navigation. OK, not great. However, if you use search augmented models and you check, basically, the curve, and you see that the curve is actually very different. So all of a sudden, what you see is that with a certain augmented model of only 15 million parameters, 10x smaller than the solution-only models, you see a training curve that is much better than the original solution-only models.
(25:50) So with only, like, 50k samples, then what you see is that the performance of the final score is already around 80% compared to the 20% with the solution-only models. So where you have more and more training sequences, the performance is getting even better. So now what you will see here is that this search-augmented model becomes much more parameter efficient and much more data efficient.
(26:18) It gives you approximately same performance with a 10x fewer number of samples, 100k versus 1,000,000, and 10x fewer number of parameters, like 50 million versus 175 million. So with this simple trick, by adding the search trace, all of a sudden the performance actually getting much, much better. You can also do the same thing for the so-called [INAUDIBLE] game.
(26:47) So this is a game called sokoban. The game is very simple. So you have one agent, and you want to push the boxes to different destinations. And the only action you can take is to push the box forward. So you cannot pull the box back from a corner or from a wall, and you have to basically start over from the beginning.
(27:09) So then this problem basically requires a substantial planning because you cannot just do these things randomly. If you push the box to the corner, then there's no way you can put a box back later. And this will basically conclude a failure stage of the game. You have to basically restart, and you basically see very clear, also similar trends, for the solution-only versus the search-augmented models.
(27:37) OK, so now we can see the scale of the two kind of models are very different, given that we first predict the search trace and then we predict the final plan. Then people may wonder, how can we go beyond? So what we did is just the imitation learning. So we started from solver search trace, and then we use the search trace to train.
(28:00) The transformer will teach a forcing to get the search augmented model. And this is already data efficient, and also time efficient, as well. What you can do further is that given the search-augmented models, which is already end-to-end differentiable models, what we can do here is to fine-tune this model to achieve shorter trace, but still leads to optimal plan.
(28:28) So this is basically, in some sense, a very typical reinforcement learning task, and that is a search former model that we end up with. So what we want to say is that we don't do, really, a reinforcement task, but what we do is very simple. We just do expert activation. What it does is that once you have this search augmented model, you can actually sample this model many, many times with some diversity.
(28:55) And this sampling actually will lead to many different choices and many different plans. You pick the plan that is optimal. And then given all the optimal plans, check the associated searches. we want to find which search phrase is the shortest. You basically get all the shortest trace with optimal plan, and then use these data, use subset of data, as a training set for the next generation of the search form.
(29:21) You can do this iteratively. Then what happens is that you still keep the same or even better optimality rate. But at the same time, the distribution of the sequence lengths per test task now actually becomes shorter and shorter. OK, so you can see that at the beginning of the top of the figure, you see that the augmented model and A star, they are sharing a similar distribution of the sequence length average per task because the search augmented model is imitation learning results of the original A star searches.
(30:02) So this is not surprising. But once you have done multiple step of this bootstrapping iteratively by creating a lot of traces and their plans and picking the trace that is shortest but still leads to optimal plan and put them back into the training set and retrain the model, or refine the model, then you can see that over time, the distribution of the sequence length average per task actually shrinks to the left, means that it's become shorter and shorter.
(30:30) That's why this paper is called Beyond A Star, which means that we are able to find shorter trace lengths while keeping the optimality of the final solutions. And in terms of numbers, we actually also see improvements. So here we define a metric called ILR, which is the improved length ratio. That's the length ratio of the optimal of the A star search trace divided by the trace that is discovered by the neural networks.
(31:06) So what you see here is that over time, the improved length ratio actually become higher and higher, which means that the final trace that is output by neural networks become shorter and shorter over time when we go through a step of bootstrapping. And then people may ask about, OK, what happens if you check the optimality rate? It's possible that the trace for optimal results, optimal solution, is shorter, but it's also possible that maybe the number of optimality also goes down.
(31:44) But actually, it's not the case. You see the numbers, and you will see that the optimality actually goes up a little bit every time when you do the steps of tuitions, which is something that's kind of mysteries, but it's still interesting to see. So after this number of steps, iterations, you see the optimality rate goes up, and the same time, the search trace has become much more efficient.
(32:11) And you see that this number of parameters, the performance is actually comparable when you use this search former tuition procedure with 24 million parameters compared to the solution-only models that has a lot of large parameters, so several hundred million. And you can see that the optimality rate become comparable after this bootstrapping process.
(32:38) Yeah. So that actually shows that this is interesting steps, and our search former performance, surprisingly, after this kind of bootstrap steps, actually doing very well. So recently, we also have a follow-up paper called DualFormer. That paper is a V2 version of search formers. So what do we do here is very simple.
(33:01) So one of the weakness of the search former is that the trace is often very long. Basically it takes, like, thousand tokens, or maybe 10,000 tokens, before it actually give you the final outcome of the solutions. And what should we do? So in the DualFormer, what we do here is that we are trying to reduce the length of the search trace by dropping randomly some portion of it.
(33:31) So we define different levels of dropping. So we can say, maybe the close costs in A star search is not important. So we can get rid of them. Maybe the cost tokens in the create center, the create clause, is not important. We drop them. We can also drop the entire query costs. So we have a different level of the dropping.
(33:56) And we also define the final level, the level 4, is basically drop the entire trace, and we just use solution-only data to train the models. So then what happened is that if you train the model with all different combinations of these levels of traces, then what happens is that the performance is actually getting better compared to the two cases that you train with only solutions, and you train with the complete trace.
(34:27) That's very interesting. So we actually test this on a maze of different sizes, and also the global task that we used in the previous slides. We actually see much better performance compared to these cases. And at the same time, the diversity of the outcome of the traces is also getting much higher. So diversity basically defines as how many clusters you see out of 64, the number of rollouts that you have generated from the models.
(35:01) The reason that it is important, especially for the test time compute, because if you have high diversity during the generation, then if you scale up your test time compute, then the performance will also get much better. So in this paper, it looks like it's very simple. This simple approach will automatically give you high diversity because the input data is also very diverse.
(35:29) And more interestingly, there is a finding that is also very surprising is that this approach, this final result called dual former, it will automatically switch between fast mode and slow mode by itself. It depends on the problem specifications, and it will work better for dedicated models in either of the situations.
(35:51) So we all know that usually people will talk about system 1 and system 2. So for system 1 settings, what happens is that the model will directly predict final solutions without any thinkings. And for system 2, usually the model will first think around, or what should we do, so there's some search process, and then come up with other final solutions.
(36:15) So usually for system 2, we think there are two different discrete mode of operating, and we want two different models to deal with these two kind of thinkings separately. And we can basically control on top of that to ensure which one to use. But now with the dual former, we actually see the surprising behavior that once the model trained on this mixture of data, it will automatically switch between system 1 and system 2.
(36:46) So sometimes the dual former will output the right token. So once the dual former output [INAUDIBLE] token, which means that the model start is kind of slow-thinking process, and here it gives you the actual plan. Sometimes the model will give you a plan token at the beginning, which means that the model will directly give you the answer.
(37:09) That's actually quite interesting. So basically, you see that this kind of behavior automatically emerges from the training and without basically having human giving explicit evaluation, explicit signals there. So here you see that for the dual former bracket auto, this means that this model is doing an auto mode.
(37:31) We are free to let the dual former output any kind of tokens. In my output, a plan token, which is the system 1 settings, and may also output a [INAUDIBLE] token, which is the system 2 settings. And in the same time, you see that the performance is getting better than if your model only trained on system 1 and the model trained on system 2.
(37:55) And moreover, it's interesting to see that we can actually control how the former is operating, but can only control the first tokens. So if we say the dual former is forced to generate the first token called create, then dual former is forced to be entering the system 2 settings, which are slow settings. If the dual former is forced to create a token called a plan, then it will basically give you a system 1 output.
(38:25) And interestingly, when you compare this dual former fast, which means that the former is forced to operate in a fast mode, with the dedicated fast models, that is notified by solution only. You see the performance is also getting better. If you compare the dual former, which is forced to be in the slow mode, and you can see that its performance is also better than the baselines that is trained on complete trace, which means that it is forced to-- the model is dedicated to the slow mode.
(38:59) So it's interesting that the model can switch to both modes at the same time. If the model is forced to use one mode, it's also still better than the dedicated models trained on particular data. And we also try this idea in math problems. So this problem is more realistic. So for math problems, what do we do here is that, so we start from this AugMATH data set, which has a very long explanation about how we arrive at the answers.
(39:31) And then we can basically randomly decimate sentences from the channel of sorts. And then we can train the models. And the resulting model is actually quite interesting. It gives you much shorter and concise steps. At the same time, it still gave you a little bit better performance. So here is the examples of the performance that you get.
(39:58) So you see that in the previous work, people already discover that by augmenting the chain of [INAUDIBLE] with existing language models, we create a data set called AugMATH, which is our baseline. So this AugMATH already give you better performance compared to the original mass data set. But with our approach with different ratio of dropping, we actually see even better performance.
(40:26) And at the same time, the trace length is also shorter. So in the end, we have these kind of comparisons between the dual form and also the old one. So you actually will see that the dual form will give you the correct path towards the destination with all the export states, as shown in the red, and the old one, without any training, does not really understand what's going on inside these main settings.
(40:56) And we only give you a pass that basically break all these situations with all the words. So in this case, we show that after the training, this dual former is able to do good planning, and the planning is reasonable, and it's better than the [INAUDIBLE] preview in these particular problems. But I will say this is not a fair comparison because ideally there should be a fine-tuned version on that particular data set and see how good the performance is.
(41:27) OK. So now I conclude the second part of the option two. So let's go to the last part of the option two, how we can train the models and the solver at the same time in end-to-end manner. So for this one, we're talking about a little bit different settings. This setting is also using combinatorial solvers, but it's applied to real-world problems that requires optimization.
(41:56) So think about, you want to optimize very complicated functions. They are differentiable, but at the same time, it also need to subject to the combinatorial constraints. So you have some combinatorial constraints, and the objective is non-linear. There are many different real-world domains that actually fall into these categories.
(42:23) So, for example, you want to optimize some computer systems. You want to optimize some antenna designs, optimize some devices in physics. So all these problems, usually you cannot write down the optimization problem in linear form, and this will basically prevent you from directly calling combinatorial solvers to solve the problems.
(42:43) So here's the one example that we actually encountered in our productions. So this problem is like inviting table placement. So suppose you have k tables that are [INAUDIBLE] tables, and they have identical devices. Here, devices means GPUs. And then you want to place these k tables to identical devices such that each device will have a lot of tables, but does not exceed the memory capacities.
(43:17) And the goal is to minimize the latency, which is estimated by a neural network. So if you want to formulate that problem in a mathematical way, you actually end up with these formulations. So here, x is a binary variable representing that this table I is being placed to device J. OK, so of course, like for each table I, we want this one to be placed with device J, some device J.
(43:46) So that's why you have some J. Xij equals 1. And another constraint is that for all the device J, you want to basically-- you don't want the final memory consumption exceeding any memory budget. That's why you have this sigma Xij and I smaller than or equal to MJ. So you have these three constraints. And because Xij is a binary, this makes this entire problem combinatorial.
(44:16) And at the same time, the loss function is non-linear because of system issues. So there's a batching communication, different issues that will lead to highly nonlinear objective functions. That is pretty hard and tricky to deal with. Remember, if you use any kind of optimization technique-- for example, genetic algorithms, to solve this problem, it may take forever to even find the feasible solutions.
(44:45) So then the question is that, maybe we should still get a good optimization solutions, and at the same time, leveraging the existing combinatorial solvers. So then what we can do is to install solving the original problem in the original space that is non-linear. We can actually find a latent space such that the problem becomes a little bit linear in the latent space.
(45:14) So what do we do here is that we first basically try to learn a surrogate cause C. And basically, this surrogate cause C is a function of the problem description y. So y is some description saying, like, maybe how many GPUs do you have, what are their memory capabilities. So these are basically the problem descriptions.
(45:39) So we first predict some surrogate cost. And then given the surrogate cost, you want to solve a linear problem, which is shown on the right. The linear problem will give you a solution x star, which is a function of y. We really want this linear problem, the final solution, to optimize the original non-linear optimization problems as much as possible.
(46:02) So you have this kind of loop. So how are you going to solve this problem? It's actually very simple. You can just leverage descent optimization. So here's basically the computational graph of the [INAUDIBLE] optimizations. So you first start from the problem description y. And that y, you first get into the C equals to Cy, and this will basically give you a surrogate coefficient C.
(46:29) And that C will be plugged into a solver. And that solver will give you a solution. That solution will be plugged into our objective, which gives you the loss function. So in order to find what is the best way to represent the surrogate coefficients, what we can do is that we can do back propagation. So our goal is to find, OK, what is the best way to minimize our loss? And I don't care about all the intermediate stages, but I just want to find, what is the best way to minimize the loss subject so that the entire pipeline will
(47:07) be working? In order to do that, you want to find out, what is the best way to specify the coefficients? But what we can do is we can propagate all the way down, and you can actually find the optimal weights for that associate C equal to Cy parametrizations. So we actually try these ideas in this embedding table sharding.
(47:30) And this is actually doing quite well. So what we can see here is that, so here there's two tables, or two figures. The left figure basically shows the solution quality. So we want the solution to be as low loss as possible. Ideally, you want them to be zero. That would be the best one. And on the right, you want also want to care about the deployment runtime.
(47:57) You don't want the deployment to be too slow. So you also want to also reduce the deployment runtime. So in that case, we can actually leverage multiple variations of our approach. So we have these three variations. One is called circle 0, which is basically, do the back propagation on the test time, which is slow, of course, but it will give you good solutions.
(48:20) We also have the circle prior, which is another variation that directly learned this C equals to Cy mapping and used the mapping to directly predict the C and call the solver once. Then you get a solution. You also have a hybrid version of it, which means that you first call the prior module to find the initial value of C, and you continue optimizing the C over time.
(48:47) So then what we do, what we see here is that our three variations actually give you a very good trade-off between the solution loss and the deployment time. It's actually much better in solution loss compared to the existing approaches. So this includes domestic, heuristic, greedy, and also the dream shot. This is a kind of approach that uses reinforcement learning to optimize these non-linear optimization problems.
(49:12) On the other hand, in term of deployment runtime, our three approaches is also doing quite well, especially for the circle prior, because you only need to predict the coefficient C once, and it's actually much, much faster. And at the same time the performance is also really, really good. We also apply the same idea to other design problems.
(49:38) So one initial design problem is called inverse photonic design. So for that problem, what we do here is that you have this kind of a very small 80 by 80 grid. So this is shown on the top middle. So this grid is super small. It's in nanometers. And the goal of this design is that once you send the lights from the top, you want the design to change the trace of the light so that it will land onto different parts of the bottom, depending on their wavelength.
(50:13) So the reason why this can be done is because once you put design in this very small nanometer scale plate, then the light will basically behave like waves. And the waves will interfere in the diffraction and diffract at different places so that the light will go to different places at different frequencies.
(50:38) So the reason why we are not using any mirror to do that is because the place is so small that you cannot place all the mirrors there. You have to use diffraction and interference in order to make this happen. We idealize two different routes. Then the optimization problem obviously is very complicated. So this optimization loss functions you want to optimize, you have to first do a simulation.
(51:07) Given the current setup of the design and given the simulation, you want to compute the frequency response, and then compute cutoff frequencies and compute all these equations to find overall loss quantities. OK, [INAUDIBLE]. But at the same time, the design space is combinatorial because at every entry of this 80 by 80 grid, you can choose either to put 1 or put 0 into this grid.
(51:36) And this is a binary problem, of course, but also you have a linear constraints, which means that you want to find a feasible solution. So the design has to be implemented by a brush. So when you manufacture the object, you have a brush to move these things around in order to carve all these designs onto a plate.
(51:59) So the size of the brush become constraints. Now, you don't want to have one single dot out of nowhere to be manufactured. This is non-manufacturable. So this actually gives you a different constraint that can be written as a linear constraint, but then the entire problem actually become quite complicated. So there are several different benchmarks that people are thinking about.
(52:24) So you can try to bend the light from the top all the way to the left by using this kind of very specific and nice aligner patterns. You can also basically split the light into two different streams. It's called a beam splitter. You can also do other kind of works like the wavelength multiplier. So then we are actually using our approach.
(52:50) We're able to find a good designs using this optimization technique. And basically, this curve basically show that our approach called circle zero and circle hybrid have a much smoother basically convergence curve compared to existing approaches, which is called the pass-through. So this is also a good [INAUDIBLE] based approach, but there's no neural talk involved, and the curve looks very unstable, and it takes a long time to converge.
(53:20) Of course, there are limitations of such approaches. So first of all, if the function f is not differentiable, what should we do? And also, during the training, we also we actually need to call the solver every time, which is slow. And you also require a [INAUDIBLE]. For these two, we actually have several follow-up work in order to achieve these problems.
(53:48) Due to time limitation, I won't be able to tell you the details, but if you are interested, please feel free to reach out. Feel free to read these papers. And the code is already online. OK, so this concludes the second part, which is basically hybrid systems, the hybrid system that can use both neural network, the larger models, and also the solvers.
(54:16) In the last part of the lecture, I'm going to talk a little bit about option 3, whether these models can actually converge to anything symbolic. So this is actually something that can be very interesting because we all know that there's actually two different conflicting narrations in our daily debates in Twitter or X.
(54:38) So there's one part of the people who actually say, luxury model can do everything. When the parameter of the models become larger, what you see is you're going to see so many emergent behaviors with more and more parameters. So when you have more and more parameters, then we will enjoy all these capabilities without too much worry.
(54:57) Then we can just sit down and do our usual work, and the model will do [INAUDIBLE] later. That's one way of seeing the entire situations. But that's another different direction, different kind of opinions. And people actually also think about otherwise, because for many experiments, what people actually see is that maybe language model is not doing anything really intelligent.
(55:26) So maybe language model is basically just doing retrievals. The reason why it is doing reasonably well is because in training set, you already see good examples. So recently in Apple, Apple actually have one paper called The [INAUDIBLE] Symbolic. That paper actually gave you this kind of examples that by adding irrelevant topics, irrelevant sentences into the GSM [INAUDIBLE] data set, all of a sudden you see a huge drop of all the models' performance on the test set.
(56:00) So it's actually very surprising because the final answer is not supposed to be changed after these random sentences being inserted. But unfortunately, all these models will experience huge performance drop. Then, how can we actually address these problems? How can we understand better why and what kind of representation language model is used to do the reasoning and planning? That's one of the core problems that people may want to ponder on during their career and their studies.
(56:37) So in order to achieve that, so here I give you one example about how we should analyze and understand the representation that is learned by the natural language-- sorry, by the training of the language models. Here's the one simple case, which is called the modular addition. This is a very simple reading task.
(57:01) It's very simple. So you basically give A and B. You want to predict what is C. So basically predict the answer of addition. But the addition is modular, which means that there's a mode D here. So if the C is too big, it will wrap back to the smaller number that is in between 0 and D minus 1. So for this task, you may wonder, it's very simple, right? So there's only D squared number of possible inputs.
(57:31) So this should be giving you something that's super simple, that maybe the network after training this model will have implicit tables to do retrievals. But actually, it's not. So what happened here is that people actually discover the learned representation of such a simple problem is a flawed basis. So there's many papers that already show that large model, once you train on this addition task, they will actually show these very nice sine cosine curves as a representation for different numbers.
(58:08) The question is why? It's very simple why you can see that. So in order to explore what's happening in the very recent work that I did, we actually just used the two layer networks to understand the behavior better. So we have two layer networks, bottom layer and top layer. And the bottom layer basically takes one representation of A and B.
(58:34) And they are basically sending to the two weights matrices. And they add together, and then they send to [INAUDIBLE] different hidden nodes. Each was quadratic activations. And then these activations will be sent to the top layer, and it will give you a one [INAUDIBLE] representation. As it will give you the output, the output will be compared with the 100 representaiton of C.
(58:57) Very simple settings. And surprisingly, you actually also see a [INAUDIBLE] basis that is emerging from the training. So in order to understand what's going on, let's convert everything to Fourier domain. Obviously, you should do that. And that will make things much easier to analyze. So you basically do the Fourier transform, and you get Za, Zb, Zc.
(59:18) You have all these complex number coefficients. Because this original Wa, Wb, Wc are real, there are some additional conditions that need to hold which the permission conditions for the associated complex numbers, Za, Zb, and Zc. So given all these settings, let's look at what the gradient descent solution look like.
(59:45) It actually very regular. So here that the figure is two-dimensional, the x-axis is a hidden node index. So here you have very simple settings. You have D equals to 7 and Q equals 20. So Q is the number of hidden neurons. So these hidden neurons index from 0 all the way to 19. And for y-axis, that's the frequency of the Fourier transform.
(60:13) So what you can see here is that for each frequency, there's actually a few hidden neurons that will show strong frequencies at that particular frequency k, a strong response of that frequency k. So if you check how many neurons are showing strong correspondence, strong response for that particular frequency, you can see that that number of neurons is having a very interesting structure.
(60:44) So if you check for each frequency k-- so for frequency k equals to 1, you see there's 1, 2, 3, 4, 5, 6, neurons that show super high response of that frequency. For k equals 2, you also get 1, 2, 3, 4, 5, 6. You get all the six solutions. Of course, you also will see all the four solutions. So this is another example.
(61:10) So for [INAUDIBLE] 1, you see 1, 2, 3, 4. There's four solutions. But you also see some bias terms across the other neurons. And so far you have seen all of six or four. But is there any others? Also is interesting, though, if you check more statistics on gradient descent solutions, so here the statistics on D equals 23.
(61:34) So the figure to the left shows all the training curves. And the right shows distribution of the orders at different frequencies. And you see very sharp peaks at 4 or 6, 4, and 6 in these figures. Very clear. And what happened to larger setups? Now we have studio settings. So D equals to 23, D equals to 71, D equals to 127.
(62:02) And also with different weight decays. So you see that most of the cases, you still see two peaks, order 6 and order 4, unless that you have a very strong weight decay in which all the solutions, collapse into order 0, which means that there's no solutions or everything is 0. And other than that, you see strong peaks there.
(62:27) OK, then why? So in this paper, what we see is that we actually come with-- we find a nice structure of the loss function solutions. That structure can be useful for expanding what's happening there. So first of all, you want to rewrite this MSE loss into this form. It has quite complicated terms. It has 1 minus term and also three terms that are sum of squares.
(62:54) And this term is representing in this way. So you have the red one and the blue one. Sorry, and the green one. And you might wonder, OK, why don't you write this MSE loss into such an ugly form? That's does seem to be interesting. But interestingly, given these forms, we first can come up with a sufficient condition of the global solutions.
(63:19) Obviously, if you set this [INAUDIBLE] to 1 and all the sum of squared terms to 0, you get global solutions. But then the question is like, why and how to achieve that? The objective is highly non-linear. However, in order to achieve that, you can actually leverage some very nice algebraic structure that's happening in the two-layer network.
(63:45) First of all, we consider all the spaces of two-layer networks, but with different number of hidden nodes. So this space is big Z. It actually have nice structures. The structure is called semiring structure. If you define a ring addition to being a concatenation of two hidden neurons and multiplication being a Kronecker product along the hidden dimensions, then this entire structure becomes semiring.
(64:13) And what makes things interesting is that these two terms that is happening in the previous loss functions, they are actually the ring homomorphisms. So what is ring homomorphism? It's actually very simple. Basically, you want that-- basically this function is a ring homomorphism. If that particular function respect the addition and multiplication across ring and normal addition and multiplication, then they are ring homomorphisms.
(64:45) So what does that implication is that you actually will see, you can start with partial solutions. So suppose Z1 is a partial solution that is only satisfy one single term of your loss functions, and Z2 is another partial solution that only satisfy another single term of your loss solutions. They are part solutions.
(65:10) They are not the entire solution. But interestingly, because of the ring structure, if you multiply these two together, then the resulting Z will satisfy both of the constraints at the same time. So what does that mean? What does that mean is that you can actually start with some partial solutions that are very partial.
(65:32) They only satisfy some part of the constraints, but you can gradually construct more and more complicated solutions with your multiplication and your addition. And then it will actually give you the global optimization, global solutions, to your original loss functions. So the entire process does not require gradient descent.
(65:53) You just construct this algebraically, and then you will get a solution that is global optimal to the original loss functions. So you can construct a solution this way. You can construct a solution like order 6 by multiplying all those three and all the two solutions together and add over all the possible frequencies.
(66:16) You can also construct all the four solutions. You can also construct solutions that correspond to the perfect memorization, which is actually all these solutions. So these control solutions are nice, but people may wonder, OK, how is this related to the real world? This is, in some sense, a symbolic and abstract world created by mathematicians.
(66:38) We have nothing to do with the real world. But actually, what happened here is that if you compare the gradient descent solutions with the solution that are constructed previously, we can actually see an exact match between the two. So first of all, in these particular settings, you can see all the different solutions.
(67:00) There are either four or six, which is already shown in this histogram. And also, if you pick this order six or order four solutions and you try to factorize them according to the previous defined ring multiplication, you realize that 95% of the solution, they can be factorized into either 2 by 3 or 2 by 2, which is the same as the construction that we previously did in our theoretical construction.
(67:31) And their factorization error is small. And finally, if you check every entry of the factorization, what you see is that 98% of solutions actually can be factorizable into the constructive forms. They actually fall into the form that we constructed previously, which means that the gradient descent solution matches very well with the construction from the theoretical side.
(67:58) That is quite interesting. And also, if you check distribution of the parameter in the solutions, you actually see very nice, regular structures. The sharp peaks and different angles that correspond to the higher symmetries. So this is something that's quite interesting that haven't been explored yet, but that can be nice for our papers to understand them better.
(68:22) So what are all these implications? So first of all, so it's kind of surprising that neural networks, if you run this on top of that, it will give you a solution that is very similar to symbolic representation. So maybe in general, the neural network or maybe large models will also converge to some solution that is symbolic, but we don't know or we haven't constructed yet.
(68:49) But here we just give you an example, a reasoning task, will give you a solution that is symbolic after the training was given assent. And second, it will give you an alternative path to what's the solution other than gradient descent. So you can think about gradient descent can give you a solution, but you can also have some advanced algebraic operation that is also giving you the same kind of solutions.
(69:13) So maybe in the future, we don't need a gradient descent any more. We just need a geometric constructions to combine small solution to the larger solutions. And then maybe gradient descent will become obsolete eventually. So that's why I want to introduce this part of work. So this might suggest a different path compared to the existing path that we have.
(69:33) In the future, instead of having people complaining about the distributed system not working well, we can open the black box and have a peaceful approach to do this. Thanks for your attention.
