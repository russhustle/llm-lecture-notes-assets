CS 194/294-196 (LLM Agents) - Lecture 3, Chi Wang and Jerry Liu - YouTube
https://www.youtube.com/watch?v=OOdtmCMSOo4&list=PLS01nW3RtgopsNLeM936V4TNSsvvVglLc&index=10

Transcript:
(0:01) CHI WANG: OK, so yeah, here's the agenda. It's very simple. You have two parts, as a general, and then we'll dive into the AutoGen framework. For the both parts, there are two big motivating questions I want to cover. Number one is, what are the future AI applications like? And then number two is, how do we empower every developer to build them? So let's start with the first question.
(0:26) What are the future AI applications like? So starting from around the year 2022, we see the big power of the language models and other generative models. So the generative AI, people have seen a superior capability of generating content like text and images. They are much better than the generative techniques 20 years ago or 15 years ago, where I was doing my PhD and the study like topic modeling and other older generative modeling techniques.
(0:58) Apparently, these days the new technique has passed the bar that we can think about more creative applications, higher level of AI capabilities. And so what's the next? What are the best way to leverage these generative AI techniques? So starting from last year, early last year, we started to think about that question.
(1:18) And we made a lot of technical bets along the way. The most important bet would be that we think the future AI applications can be agentic. And the agents can be a new way for human to interact with the digital world and help us execute more complex tasks on behalf of a human and get to higher and higher level of complexity.
(1:44) And at that time, many people still have doubts about whether agentics AI can be a viable notion. But as time goes by, we are seeing more and more confirmation and evidence, including earlier this year, there's an article published by Berkeley talking about how we are observing more and more AI results shifting from using a simple language model to building compound AI systems.
(2:11) So that's one of the similar observations and technical bets we made. So if we think about the examples of agentic AI, you'll notice that building AI agents like personal assistants, autonomous bots, or gaming agents, there's the notion of agent AI is not new. But what's new is that with the power of the new generative AI techniques and the language models, for example, we're able to build some of the old agentic applications much easier and much more capable.
(2:50) And at the same time, we are also seeing some very new novel agentic applications that go beyond the imagination we had before, such as some science agents that can do science discovery automatically, web agents that can automate some web browsing and web automation tasks, and also software agents that can build even software from beginning to the scratch.
(3:16) I bet you have seen a lot of demos before. But today, let me show you a very recent demo I just got this morning from a [INAUDIBLE] called Zinley. This is an example of building a website to extract the models from Hugging Face and download automatically. So after you give the request to the AI, they started working on them automatically, starting from analyzing the task, looking for information, and installing the necessary dependencies.
(3:49) They do use a multiagent framework to have agents of different roles to finish the task and can do multistep of tasks. Now, we are seeing that AI is creating different files. All the reasoning between the agents are done automatically using both language models and tools. The agents are talking to each other now.
(4:19) They are developing the website. And finally, they are trying to build and compile the website. OK, here we go. This is a website built by the AI. It looks like functioning. There are many models already listed in the website. And users can search model and export model data and download the data, download the models.
(5:09) It looks like the video is playing in the slower speed than normal. I'm not sure if that's because of the internet issue. And furthermore, if you happen to make some mistakes, for example, if you remove the very critical line of code from the files that are created, let's see what happens. So we are running the AI again for executing the same task.
(5:41) So this time, the AI is able to-- trying to also perform the same task. But because of that removal of the critical lines of code, we're supposed to hit some error. Yeah, so we see our error message there. It's missing script. So let's see what AI will do. So just within a few steps, the AI is able to correct the mistake and finish the exact line we just removed.
(6:16) So complete the task. So that shows some self-healing and self-recovery ability of AI agents. So that would give us an idea about the potential kind of promise the agents can give us. In future, we may build the software in a totally different way. So to summarize the key benefits of agentic AI, number one is many people understand that AI is able to use more natural interface to talk with humans.
(6:46) So you can tell them what you want exactly. In this case, we just tell them we want to build a website with certain requirements. And then you can iterate over the natural language. And also, by giving the agents more capability, they are able to operate and finish more complex tasks with minimal human supervision.
(7:04) So that has tremendous value in terms of automation. And the third point, I think-- it's less talked about among people. But I have a strong [? emphasis-- ?] that is, agentic AI can be also a very useful software architecture to enable us to build software in a totally different way so that you can have multiple agents working with each other and to finish much more tasks in a recursive way.
(7:31) So I will use a simple example to go over these benefits. So let's look at one particular example application about solving some supply chain optimization on cloud. This application was built by my former colleague from Microsoft Research. It allows users like coffee shop owners to answer questions like, what if we change the shipping constraints? How would that affect my operating cost? So this is a difficult question.
(8:00) They can't get answers from ChatGPT because the AI needs to understand the user's specific data constraints and even some optimization tools to be able to understand the question and get the answer. But in this case, they used AutoGen to build three agents that can solve this question very nicely. The three agents are commander, writer, and safeguard.
(8:26) So let's see how that works. So initially, the user sends a question. What if we prohibit shipping from supplier 1 to roastery 2? The commander received that question. Before it tries to answer that question, it will hold the current conversation and initiate another nested chat between the commander and the writer.
(8:46) The writer, in this case, is using a large language model as a backend, try to understand the question and proposes some code that can be used to answer this question. In this case, it suggests some Python code of adding a specific constraint into the optimization problem. And the returns that code to commander.
(9:06) The commander sees that code. Again, it holds the conversation with writer and starts another nested chat between the commander and the safeguard. The safeguard is another agent using language model to check whether the code is safe. So in this case, it turns safe. The commander will finish that conversation with the safeguard and start executing the code.
(9:29) So here's that code execution result. It runs the optimization program with updated constraint and checks the new result. So the commander will then go back to the previous conversation with writer, sends the execution result back to the writer. Here are the execution results. And after the writer sees that, it tries to come up with the final answer.
(9:51) This is a simple example of using multiple agents and multiple steps to finish the task. In general, there can be all sorts of problems, like the code can be unsafe. In that case, the commander will not execute the code. And in case there's an execution error, the commander will return the execution error back to the writer, and the writer needs to rewrite the code.
(10:12) So in general, there can be multiple turns back and forth to finish the complex task. But here, we're just showing one simple use case that is at a smooth trace. And you can tell how the end user now is able to still use a relatively simple [INAUDIBLE] to know there are multiple agents running at the backend.
(10:34) They just ask a simple question and then get an answer in natural language back. The user doesn't need to know and understand any knowledge about the coding or optimization. So this is an example of using multiple agents. We allow the end user to achieve tasks that are much harder than before. Now, if you look at the programming perspective, how do they construct such a program? The way they do this is also very different from traditional programs.
(11:03) There are several steps. The first step is to create these several agents. So in this case, they're creating the writer, safeguard, commander, and user using one line for each. And then they need to define the interaction patterns among these agents. So here, they registered two nested chats to commander. For each [? nested ?] chat, we need to define, who are the agents involved in the chat? Who are the sender? Who are the receiver? And what's the summarization method from that conversation? And then their behavior patterns are defined in this way.
(11:37) And finally, we just initiate the chat from the user proxy agent to the commander agent by sending the initial task requirements. And then every other steps autofollow automatically. So the framework will handle all the steps and the nested chats and eventually return the result back. So hopefully, that gives us a intuitive idea what the agentic programming means.
(12:06) And also, I'm using two slides to summarize their benefits. Number one is to enable us using these multiple agents who handle more complex tasks and improve the response quality for multiple reasons. You see that the multiagents talking to each other can be a very natural way to make improvement over interactions.
(12:29) And we can divide and conquer a complex task to decompose that to smaller tasks and have more quality result for each step. And finally, we can define agents that are not necessarily depending on language models. We can have special purpose agents that perform grounding or validation using knowledge outside of the models so that we can address the weakness inherent in the models.
(12:59) If you look at the chart on the right, this is the experiment result about if we decompose the task into two agents for the right-hand safeguard versus if we put all the interactions in one single agent, how is the performance on the safeguard side? So if we compare the performance, we see that for GPT4, the multiagent setup has a 20% higher recall than single agent setup.
(13:25) And for GPT3.5, the difference is even higher. So this indicates that, for certain scenarios, it's beneficial to decompose the tasks and have agents to perform relatively simple tasks compared to just asking the agent to do too much work in one try, and this especially depending on the task complexity and the model capacity.
(13:51) So in general, the more complex the task and the weaker the model, there's a stronger need to have this multiagent workflows. And another perspective is from the programming perspective. So in general, it's easier to understand, maintain, and extend the system if you have a modular design. For example, if you want to keep most of the agents unchanged but just change, for example, how you change the safeguard's behavior, if you want to switch from using language model to using tools or using human, it's very easy to do
(14:27) that with a modular design. And this allows natural human participation because human can take over any of the agent in the system. And by having natural conversation with other agents, there's no need to change how human interact with other agents. So combining these benefits, we see that the agentic programming has a promising potential to enable fast and creative experimentation and build novel applications.
(14:57) But it's not easy to design a framework that can meet all these promises. In general, we need to consider several factors to design such a framework. We want to have a unified agentic abstraction that can unify all these different types of entities. And we want to accommodate all the flexible needs for multiagent orchestrations and satisfy all the different application needs.
(15:23) And we also need to make sure we have effective implementation of all the agentic design patterns. Let me use the next few slides to explain them. So first of all, for agentic abstraction, we want to unify the notion so that we don't need to have a hard time to reason about all these different type of entities, such as humans of different roles, tools, or language models from different providers.
(15:50) In general, these are all needed for building a common AI systems. But if we can have a single concept to think about them, it will really make it easier to reason about them. So let me explain more in the rest of the talk. One immediate benefit of that is now we can use multiagent orchestration for building [INAUDIBLE] complex applications of common AI systems.
(16:21) But to do that, we also need to think about all the different requirements of different AI agentic patterns of interactions. For example, sometimes the developer want a more static workflow so that they can clearly find each step and the order of agents performing the tasks. But in other cases, if it's hard to know all the possible situations that the agent needs to handle, then we want to give the agents more autonomy and enable them to create more dynamic workflows.
(16:54) And similarly, sometimes they want to use natural language to tell the agents to perform the tasks. And at the other times, they want to use programming language to have a more precise control. So there's a tradeoff between the flexibility and the controllability. And there are many other tradeoffs to consider.
(17:14) For example, when do you share the context among the agents? And when do we isolate them, have a hierarchical setup, for example? And when do we want the agent to cooperate? When do we want them to compete to finish [AUDIO OUT] the task better. And there's also consideration about the centralized versus decentralized and automation versus intervention.
(17:40) So a good framework should be able to accommodate all these different requirements. And also, as people develop agents more and more, there are all sorts of design patterns emerging. And we need to also check whether a framework can meet all these design patterns requirements. There's a conversation. We want the agent to be able to deal with flexible conversations.
(18:07) There's also prompting and reasoning techniques like ReAct, Reflection, Chain of Thoughts, the Tree of Thoughts, and so on. So there are more of such emerging reasoning techniques, including like Monte Carlo tree search-based methods. Tool using is a very important part of the agentic design patterns, and so is planning and integrating with multiple models, modalities, and memories.
(18:30) And it looks like a lot, right? So it's hard to have a framework that can consider all these different factors in the beginning. But if you think about this from the first-order principle, you may wonder like, is it possible to have a single kind of design point to start with and have all the other patterns to be able to-- so that we can derive all the other patterns from that? So yeah.
(19:00) So among all these different design patterns, can we think of using just one single design pattern of them to accommodate all of the others? Yeah, I believe that different people will have maybe different ideas and starting from the different parts. Again, you may pick either one of them. So here, I want to mention one personal kind of design [INAUDIBLE] view that is a conversation.
(19:27) So this has a long history. Back in college, I learned that conversation is a way of making progress in learning and [? prove ?] theorems. And that's a takeaway. And when I see the power of ChatGPT, I immediately relate that back to my personal experience and decide, OK, so let's consider trying to use the multiagent conversation as the central mechanism and see how far we can go.
(19:53) So it turns out we can go very far. So here, I'm listing some examples of agentic AI frameworks, like AutoGen, and as I mentioned, is based on this multiagent conversation programming. And it turns out to be very comprehensive and flexible that's able to integrate with all the different design patterns I mentioned and also other frameworks.
(20:16) And you will hear more from Jerry about LlamaIndex. And there are also some other LangChain-based frameworks such as LangGraph and CrewAI. They focus on different also starting reasoning points. LangGraph, for example, focuses on providing some graph-based control flows. And CrewAI focus on providing some high-level static agent-task workflows.
(20:40) Yeah, so that's a very brief overview of the agentic AI frameworks in general. Next, I will go into the specific AutoGen. I will try to explain more about that. So AutoGen is a programming framework for agentic AI. It was initially divided inside another open-source project called FLAML. FLAML is for automated machine learning and hyperparameter tuning.
(21:07) And later, we spined off the standalone repo. And this year, recently, we also created a standalone GitHub organization to have an open governance structure. So everyone is welcome to join the build. And here's a brief history about the journey. In early last year, we started prototyping this agentic frameworks.
(21:30) And initially, we just built a multiagent conversation framework with the capability of code execution. And in August, we published the first version of the research paper. In October, we moved to this repo to standalone GitHub repo. And it got a lot of recognition from the community. And such as this year, we got also a Best Paper at the ICLR 2024 LLM Agents Workshop.
(21:57) And we're seeing more and more interesting use cases from community, including both enterprise companies and startup companies. We also offered a online course on deeplearning.ai about AI agentic design patterns with AutoGen. So that's just [INAUDIBLE] about it. In general, AutoGen has very rich features about all the different AI agentic design patterns.
(22:24) But today, I mainly want to focus on the most essential concept because this is a key to understand all the other patterns. So we want any developer to [? listen ?] about a very complex workflow in simply two steps. The first step is to define the agents, and the second step is to get them to talk. So here, we see two key concepts.
(22:48) One is the conversable agent, and the other is conversation program. So the agent concept in AutoGen is very generic, can abstract a lot of different entities. You can choose to use a language model as a backend for the agent, or you can use a tool or human input at the backend. And you can also mix multiple types of entities together.
(23:12) And furthermore, if you use these multiagent conversation patterns, like you see on the right-hand side, you can build agents that can contain other agents inside it, have inner conversations, and then wrap them up as a interface to talk to other agents. So in this way, you can nest multiple chats inside an agent, have inner conversations, and build more and more complex agent in a recursive way.
(23:42) And the examples of conversation patterns include the sequential chats, nested chat, and group chat. I will explain a few of them in the next few slides. The simplest one is the two-agent conversation. Even though this is a very simple conversation pattern, it already allows us to form some advanced reasoning, such as Reflection.
(24:03) For example, we can construct a writer for writing blog post and another critic agent to make suggestions to the blog post. And you can have these two agents iterate and improve the quality of the blog post in this way. And now if you think that's not good enough, you can add other advanced reasoning techniques using this nested chat.
(24:31) For example, instead of using a single language model to do the critic, we can use the nested chat. And in the nested chat, we construct sequential chats that contain multiple steps of chats. We first send the message to the SEO reviewer. And then we send it to a legal reviewer, ethics reviewer, and so on.
(24:52) And finally, you can have a meta reviewer that summarizes all the reviewer's comments in different areas and gives the final comment. And from the writer's point of view, it's still talking to a single writer, a single critic agent. But underneath, the critic agent uses multiple other agents. So that's the idea, how we can use a nested chat to potentially extend the agent capability using conversations.
(25:19) And think about how do we enable tool using, using conversations? Here is the example of building a game of conversational chess. We want the AI to be able to play the chess while chitchatting with each other and making fun of each other. So if you just ask two language model agents to play the chess directly, they often make mistakes and form random moves on the board, which are not legal.
(25:43) So the game is not watchable at all. Now, the solution can be that we can add a third agent called chess board. This agent is a tool-based agent. It uses some Python tools, Python library to manage the chess board and provide the tools to other language model agents. So the language model agents can only make legal moves.
(26:07) And otherwise, they need to iteratively refine their moves until the moves are legal. In that way, we can make sure the game can carry on nicely. And again, we're using nested chat between a tool [INAUDIBLE] agent using language model and tool execution agent that's using tool as a backend and having them talk to each other to finish that tool execution functionality.
(26:33) And there can be other more complex workflow patterns and solving more complex tasks using planning and using more dynamic way, like group chat. In group chat, the users only need to define the agents of different roles and then can put them in a group chat. And they can automatically decide which agent to speak next depending on the current progress of the task.
(26:58) There will be a group chat manager which can monitor the progress and make the selection of the speakers. And furthermore, you could add more constraints or finite-state machine translation logic about what others should the agents follow. You don't have to make a very strict order. You can just give some candidates about after agent A speaks, only agent B and C can speak and all the other agents, things like that.
(27:27) So there's still some decision made by the language model and some autonomously there. But you can add certain constraints to make sure at least the selection is within the scope. And you can further add some transition logic to tell the agent, when you see certain situations, you should go this route. Otherwise, you go to the other paths.
(27:50) And you can use either natural language or programming language to specify that. Yeah. In general, there are many other types of conversation and applications enabled by these conversation patterns. If you're to check our website for more notebook examples, they are nicely tagged. So you can search for anything, a question.
(28:13) For example, you can search for how to integrate AutoGen with LlamaIndex. And there are notebook demos like that. And in the paper, we just showed a few very simple examples using simple conversation patterns. But in general, you can build many more complex tasks using these building blocks. I've seen developers building all kinds of sorts of complex tasks.
(28:35) So here's an overview of the categories of domains that we are seeing from the community. The top two categories are software development and agent platforms, consulting, research, data processing, getting a [? long tail. ?] There's web browsing tasks, finance, health education, and even blockchain, which is not shown in the list.
(29:00) And here, I want to select a few interesting examples. The first one is in the science and engineering domain. Professor Markus Buehler from MIT has done multiple work in different science domains, including protein design and material design, using AutoGen to build teams of agents that simulate the behavior of a scientific team or engineering team all the way from collecting the data, generating hypotheses, and then conduct experiments and verify the hypothesis.
(29:34) They have also built a very recent work called SciAgents, which can use knowledge graph to reason about any scientific domain and make interesting connections and do advanced reasoning using that knowledge graph. They contract a team of agents of multiple roles. And they can start from understanding ontology and then make multiple signs and [? critiques ?] to finish the very complex workflow, including generate all the different concepts and possible hypotheses, and then select the best out of them.
(30:06) So this is very promising use case. I see that, in the future, has a potential to accelerate our science discovery. And maybe soon after, we will have some AI-designed medicine or AI-designed architectures and so on, or interesting alloys and materials. Here's another domain of web agents. This is a agent called Agent-E, developed by a startup called Emergence AI.
(30:39) They use AutoGen to build a hierarchical agent team so that they can perform some very complex tasks on the web, like automatically booking flight tickets or automatically fill a form for medical clinics. This is leveraging some planning agent, some web browsing agent and have some deep understanding of the content from HTML DOM.
(31:09) They haven't used any multimodal models. So they are only leveraging the HTML content and not leveraging the images and the vision models. But they already achieved the state of the art performance on this WebVoyager benchmark and other forms of previous techniques using multimodal models. There's still a large room for improvement.
(31:34) You can see the overall success rate is just 73%. So by combining multimodal models and the more agentic workflows, there's a potential to get it higher, even higher. But there's some very good foundational design principles we can learn from that work. And I also want to share a very recent quote I learned from a company doing construction.
(32:01) They're trying to use AutoGen to help the users without special knowledge or expert knowledge to be able to finish their construction projects. And they mentioned the benefit of AutoGen as being able to rapidly explore many different agentic design patterns, configurations, and conducting experiments at scale.
(32:28) And then to summarize, we have seen a big enterprise customer interest from pretty much every vertical domain and got contributors or users from universities, organizations, or companies from all over the world, including some contributions from Berkeley. There's a word called MemGPT created by Berkeley students.
(32:48) And they also have integration of AutoGen, very nice work. So we see that there's a lot of changes and progress in AutoGen. And here are a few examples in several categories, including evaluation, interface, learning/teaching/optimization. In the evaluation category, we are building agent-based evaluation tools to help developers understand how effective their application is, how good their agent are.
(33:20) It's a really difficult task because there are a lot of texts generated. And it's hard to understand what exactly is going on. But using agents, they're able to automatically come up with success criteria based on the application and task and then suggest the scores for each dimension. And furthermore, we can extend that idea to improve agents over time by providing the feedback to the agents themselves and have agent-based optimizations or learning or teaching capabilities.
(34:00) One centerpiece of AutoGen is still to improve the program interface to make it even easier for people to build all sorts of agentic applications. So I want to talk about one particular research that excites me. This is called AutoBuild. So one remaining question for many developers is, what is the most effective multiagent workflow I should use? What agents should I create for my particular task? So AutoBuild is designed to address that issue as an initial attempt.
(34:36) So it works in this way. The user first provide a task to describe the high-level requirements. And the system will suggest agents of different roles automatically. And these agents can be put together in a group chat to solve the task. And for a [? new ?] task, a more specific task, the other task, we can review this created agent teams to solve that without users need to specify which agents to use.
(35:05) And we can further extend that idea from static agent team to adaptive agent team. So there's a technique called Adaptive Build. And in this case, we can first decompose a complex task into smaller steps. And then for each step, we propose a specific agent team for it. We can either choose from an existing library of agents or decide to create new agents that didn't exist before.
(35:33) And after finishing one step, we now check what new agents do we need for the next step. So these agents can be dynamically selected. And as we create more agents, we can also add them back to the library to improve overall system over time. And we make experiments on several different benchmarks, including math, programming, data analytics, and so on, and find the very promising result outperforms previous techniques of similar kind.
(36:03) Yeah, this is just one particular example of research we're doing. There are many more challenging questions. We still need the community to work on them together. The biggest question is how to design this optimal multiagent workflow for any application and considering multiple factors like quality, the monetary cost, latency, and manual effort.
(36:26) In general, we still want to improve the agent capability from the reasoning, planning, multimodalities, or learning capabilities. And we also want to ensure scalability and make sure human has a good way to guide their safety and so on. Yeah, so that's the end of my lecture. So I want to acknowledge all the open-source contributors.
(36:52) And you can find our Discord community. We have a large community on Discord. And find this new GitHub organization. And I'm happy to follow up with questions. Thank you very much. JERRY LIU: Hey, everyone. That was a great talk by Chi from AutoGen. And yeah, I hope to build upon that. I think this talk is a bit less like generic, I guess, in terms of covering a lot of the different agent architectures and stuff.
(37:20) This is actually quite about a specific use case around building a multimodal knowledge assistant. And so it's really focused on maybe some principles around RAG. And how do you actually extend that, building a research agent? And it's a use case that we've been exploring pretty deeply as a company. But of course, there's plenty of agent stuff out there.
(37:41) You should definitely check out AutoGen, check out LlamaIndex. Try building stuff on your own, and then see what we can come up with. So let's get started. And first, if you're not familiar with LlamaIndex, LlamaIndex is a company that helps any developer build context-augmented LLM applications from prototype to production.
(38:00) So we have an open-source toolkit. The open-source toolkit is a developer toolkit for building agents over your data, different types of LLM applications. These days, there's a lot of people building agents. We started off helping people build RAG systems. And now we're going into territories where people are building slightly more advanced stuff, where you're using LLMs for multistep reasoning as opposed to just single-shot synthesis and prompt generation.
(38:28) We also have an enterprise product, which I probably won't talk about too much today. But it's basically a managed service to help you offload your data indexing and RAG and all that stuff. I'll talk a little bit about one specific piece of this, which is document parsing or data parsing. And we think this is a pretty important piece in any sort of context-augmented pipeline.
(38:49) So let's get started. How many you know of what RAG is? Yes. So one part of RAG is you have a database, and so a database of some knowledge. And the way it works is you first want to do retrieval from that database to return basically just a bunch of text to chunk it. And then given the chunk context, embed it and put it into, for instance, a vector database.
(39:15) And we'll talk a little bit about that as well. So this overall goal of building a knowledge assistant is that a lot of companies have these types of use cases where they have a lot of data. They have a million PDFs. They have a bunch of PowerPoint presentations. They have Excel files. And you want to build some interface where you can take in some task as input and give back an output.
(39:37) That's really it. If you think about a chatbot, that's basically a chatbot. So you have a lot of data. You want your LLMs basically understand that data. And then you want the LLM to do stuff with that data. An example could be generating a short answer. It could also be generating a structured output, a research report.
(39:54) It could take actions for you, like send an email. It could schedule a calendar meeting, write code, do a lot of these things. We talk a lot about RAG as a company. And especially if you're just getting started, if you follow RAG 101 on how this works, we call that basic RAG. And so what is basic RAG? Basic RAG is you take your unstructured data.
(40:19) You load it with some standard document parser. And then you chunk it into every thousand tokens or so. You just slice that text up into a bunch of slices. And then you stuff each slice into an embedding model, like OpenAI embeddings. And then you put it into a vector store. Then when you return stuff or you do retrieval from this database, you typically do semantic search or vector search to return the most relevant items from this knowledge base and stuff it into the LLM prompt window.
(40:51) This entire pipeline gives what I call a basic RAG pipeline. It'll work kind of in being able to answer basic questions that you have over that data. However, there's a bunch of limitations. And the four limitations we list here is one is the data processing layer is pretty primitive. When you do the chunking, you're not really doing or taking into account all the different elements within that data.
(41:16) You're not really taking into account, are there tables or images or weird sections? And you want to semantically preserve different chunks together. You're only using the LLM for synthesis. And so you're not using it for any sort of reasoning or planning. And so if you think about all of what you just talked about with respect to agentic coordination and stuff, you're doing none of that with a basic RAG pipeline.
(41:37) And so it's kind of a waste of LLM capabilities, especially with the latest models, like 01, 3.5, like Clyde, and whatever. A lot of these models have much greater capabilities and just being able to summarize over a piece of text. And so you want to figure out how do you actually use the model capabilities to do more advanced reasoning or planning.
(41:56) The other piece is standard RAG pipelines are typically one shot. So they're not personalized. It's just like after you ask a question, it's going to forget about it. And so every new interaction will basically be stateless. This has certain advantages. But if you're building a personalized knowledge assistant, ideally, you're able to add a memory layer to that.
(42:16) So a lot of what we ask ourselves is, can we do more than a basic RAG pipeline? There's a lot of questions or tasks that a naive RAG pipeline can't give an answer to. This leads to hallucinations for the end user, having a chatbot where it can't answer 80% of the questions you might want to ask it. It is like a limited value add.
(42:38) And so how do you build this more generalized knowledge assistant that can take in questions of arbitrary complexity and answer that over arbitrary amounts of data? We think a better knowledge assistant has four main ingredients. And actually, specifically, the focus of this talk is on, how do you take in multimodal or build a multimodal knowledge assistant? So instead of just reasoning over standard text file, how do you reason over an entire research report with a lot of diagrams and pictures, images? How do you basically reason over all the visual data
(43:16) that exists on the internet in addition to just text? So the first piece is we need a core high-quality multimodal retrieval pipeline. We want to then maybe generalize the output and think about something that's a little bit more complex than your standard chatbot response, so generating a research report, doing data analysis, taking actions.
(43:39) 3 is agentic reasoning over the inputs. So this is where instead of just taking in the user question and only using the LLM for synthesis, applying Chain of Thought, tool use, Reflection, all that fancy stuff to try to break down the question, do some planning, and actually step by step work towards an overall goal.
(44:01) And last is deployment. We'll see how long I have. I plan to probably talk for 15, 20 more minutes, so just cover some of the high-level details. And for some of the examples, they're basically linked in the slides in case you want to check them out. The first piece is setting up a multimodal RAG. If you're familiar with RAG, you might be familiar with RAG over text data.
(44:24) But what we're really interested in is having RAG actually operate over just visual data. And by visual data, I don't just mean like a JPEG file. Even if your PowerPoint, for instance, or a research archive paper, it's going to have charts, diagrams. It's going to have weird layouts. And the issue with a lot of standard RAG pipelines is they do a terrible job at actually extracting out that information for you.
(44:51) And so like I mentioned, any LLM or RAG or agent application is only as good as your data processing pipeline. And if you're familiar with the garbage in, garbage out principle in traditional machine learning, I think for LLM application development, it's no different. So you basically want to have good data quality as a necessary component of any production LLM app.
(45:13) This ETL layer for LLMs consists of basically you want to do some parsing from the document. You want to figure out a smart way to chunk it. And then you need a smart way to index it and put it into the LLM prompt window. This data source that I talked about, like this case study of complex documents, is pretty common data format that we see across a lot of different companies.
(45:38) And so a lot of documents can be classified as complex, like embedded tables, charts, images. There's irregular layouts. There's headers and footers. And a lot of times, when you apply off-the-shelf components to parse a lot of this data, it ends up in a broken format. And LLM hallucinates the answer. Users want to ask different types of questions over this data.
(46:05) So you have a bank of, say, PDFs. You want to ask questions over it. It could be a simple pointed questions. It could be multidocument comparisons. It could be longer running, like research tasks. A research task could be given these 10 archive papers around LLM quantization or something, generate a condensed summary or a survey paper.
(46:24) And so that's something that's a bit longer or higher level in nature compared to something that's just a simple search and retrieval task. So we'll start with the basics, which is just parsing. Ideally, a document parser can actually structure this complex data for any downstream use case. I won't talk about it too much because the goal of this is really about agents.
(46:47) But really, without needing to know the internals of document parsing, you need to have a good PDF parser, basically. Because if you have a bad PDF parser, then you're going to load in some PowerPoint or PDF, it's going to not really extract out the right text from that PDF. And then when you feed in text that's been hallucinated by the parser, the LLM is going to have a really hard time understanding that, no matter how good the LLM is.
(47:13) So ideally, you basically want a parser that can parse out text chunks, tables, diagrams, all that stuff into semantically consistent ways. That is one of the things that we do is we make a pretty good gen AI-powered PDF parser. We're at 30,000-plus users right now. And yeah, if you're interested in trying it out, everybody gets 1,000 credits or pages per day.
(47:38) And so it's used at different small companies to large enterprises. So you want to have a good parser. And what this enables is it's actually an important piece in being able to structure your data in the right way. If you think about different types of data-- so here is maybe an investor slide deck. Here's a 10-K annual financial report.
(48:00) Here's an Excel sheet. Here's a form-- having that parsing and extraction step to extract out stuff into clean, formatted data, it's just much easier for LLMs and retrieval processes to understand that. Afterwards, once you've actually parsed out a PDF into its constituent elements in a good way, you can then leverage hierarchical indexing and retrieval to do something more fancy than your standard RAG pipeline.
(48:30) So given this document structure of text chunks, tables, and diagrams, for each of these elements, a standard RAG pipeline will basically directly try to embed each of these chunks. But we found, for instance, a better approach is you actually extract out a bunch of different representations that point to the source chunk.
(48:50) So for instance, for a table, you might want to extract out a variety of different summaries that point to that table. For a picture, a picture, you can't feed into a text embedding model anyways. And so you need to maybe use some model to extract out a variety of different summaries that point to that picture.
(49:06) For bigger text chunks, you might want to extract out smaller text chunks that feed to that bigger text chunk. Once you extract out these representations, we call these nodes because these are the things that will be embedded and indexed by the vector database that you're using. So if you're using a vector database like Pinecone or whatnot, you can basically extract an index, this metadata, that is associated with the source element but is not the direct source element itself.
(49:36) Then during the retrieval process, given a user question, it will first retrieve the nodes. And because the nodes have a reference to the source document, you can basically dereference it and then feed the resulting element into the model. Notice that most models these days are multimodal in nature. If you look at 01, GPT4.0, Claude 3.
(49:59) 5 Sonnet, the latest Gemini models by Google, they can take in both text and images. And so the nice thing here is that you can still use text embedding models for represent the element itself. But then when you actually feed stuff into the LLM, you're able to feed in both text and images. And so what I just outlined to you is a basic way of building a multimodal RAG pipeline.
(50:21) So a multimodal RAG pipeline can take in any sort of document. It could have different types of visual elements in that document. And it will store both text and image chunks. And so in order to index the image chunks, you could use clip embeddings, or you could do what I said, which is use a model to extract out text representations and link the text representation to the image chunk.
(50:46) And so when you set this up, then during retrieval, you return both the text and the image chunks. And you feed both to a multimodal model. I'm going to skip this example. But here is a basic example here that shows you how to build a standard multimodal RAG pipeline. Notice up until now, I haven't really talked about agents yet.
(51:08) This is just setting up the basics of multimodal RAG. And so you don't get the benefits of train of thought or reasoning or tool use quite yet. All this is doing is saying, given a specific question you have about maybe a more complex data set, like research reports or slide decks, you're able to ask questions and get answers over the visual elements on the page.
(51:30) So the next piece-- actually, I might skip this just due to time. But basically, the high-level idea here is a lot of the promise of agents is that it's not just going to give you back a response in the form of a chat response but actually generate entire units of output for you, so producing its own PowerPoint or PDF or taking actions for you.
(51:51) Or do you guys use-- anybody use Claude instead of ChatGPT? Anyone use Claude? You know how when you ask it to write a paper for you for your Berkeley essay, then it'll actually generate an entire thing on the side? That's an example of report generation. It'll actually give you a thing that you can just directly copy and paste into something that you then later edit.
(52:13) So for instance, this is a pretty common use case we're seeing in the enterprise too. So a lot of consultants, like knowledge workers, are interested in basically generalizing beyond the capabilities of just giving you back an unformatted response but giving you something that you can just directly use on its own.
(52:33) So whether that's code, whether that's reports, it's all very interesting. And I'll probably skip some of the architectures of how to actually build this for now. And now we talk about agentic reasoning over your inputs. So this is the third section. So we have a multimodal RAG pipeline in place. Now let's add some layers of agentic reasoning to basically build agentic RAG.
(52:55) Naive RAG works well for pointed questions, but fails on more complex tasks. And again, this is due to all the reasons I mentioned above. You're just retrieving a fixed number of chunks. You're not really using the LLM to break down the question in the beginning. And so if you ask a question like summarization questions, where you need the entire document instead of just a set of chunks, comparison questions where where actually need to look into two or three or more documents, multipart questions, same thing, or a high-level task,
(53:24) you don't really get good results if you use a standard RAG pipeline. So there is a wide spectrum of different types of agentic applications you can build. And I think Chi probably gave much better architectures of how multiagents can collaborate with each other and achieve very advanced things. The way we think about it is like there's both simple to advanced agent components.
(53:48) At the right, you basically have entire generalized agent architectures. And so this includes a ReAct loop, for instance, which is one of the most common agent architectures these days, came out like about two years ago almost, and basically just uses some chain of thought plus tool use to basically give you some generic agent architecture.
(54:09) You can plug in whatever tools you want, and it'll roughly try to reason over them to solve the task at hand. This also includes LLMCompiler, which is a [? savings ?] paper. And so this basically generalizes a little bit beyond ReAct into doing some preplanning. So instead of just planning the next step at a time, we'll actually plan out a DAG, optimize it, run it, and basically replan periodically.
(54:34) What we actually see a lot of people these days build is some people do use ReAct. It's a pretty easy architecture to get started. But other people just take some of the existing components and build more constrained architectures. And part of this is just due to the desire for reliability. Even though it's less expressive, even though it can't do everything, some people are still building up that trust towards AI.
(54:56) And so they're trying to solve a specific use case in the beginning. And so by solving this specific use case, they can leverage more specific components and try to maybe solve in a more constrained fashion. This includes, by the way, tool use, maybe leveraging a memory module. Function calling, we see at a lot of places.
(55:15) People are very interested in structured output generation, tool use, being able to call an existing API, and then also doing some basic query decomposition, whether that is chain of thought or parallel, like given a question, break it down into a bunch of different questions. This overall thing we call agentic RAG because it really is just like an agent layer on top of RAG.
(55:39) If you think about RAG or retrieval from a vector database as a tool, you can think about an agent that operates on top of these tools. So instead of giving a query, directly feeding it to the vector database, first, passing it through this general agent reasoning layer. And this reasoning layer can decide to do a bunch of things to the query and also decide what tools to call in order to give back the right response.
(56:05) The end result is that you're able to build a more personalized QA system that can handle more complex questions. And this is an example of what I mean by unconstrained versus constrained flows. So, for instance, a more constrained flow might just be you have a task. And you just have a simple router prompt.
(56:26) A router prompt is just a LLM prompt that just selects one option out of [INAUDIBLE]. And all it does is, given this task, feed the task to one of the downstream tools based on the decision and the router prompt and then feed it to maybe a reflection layer and then give back a response. There's no loops in this orchestration.
(56:49) All that happens is, it will hit the router, go through a tool, go through another prompt that just reflects and tries to validate if it's correct, and then generates a response. This, I defined as more constrained because a lot of the control flow is actually defined through humans, through you guys versus through the agent.
(57:10) And typically, the programs that are more constrained look like if-else statements and while loops that you actually write instead of the agent. If you're using a more generalized agent architecture, if you're using ReAct or LLM Compiler or Tree of Thought or whatever, then it's a little bit more general because you're basically saying, I don't actually know what the specific plan I want the agent to follow is.
(57:35) I'm just going to give the agent a bunch of tools and let it figure it out. And so this is more expressive because it can technically solve a greater variety of different tasks than you trying to hard-code that flow beforehand. But it's also less reliable. It might veer off and call tools that you really didn't want it to call.
(57:55) It might just repeat, get stuck in an infinite loop somewhere, and never converge. And it's also more expensive. Typically, these types of agent architectures use bigger prompts. You're stuffing in more tools at once. And so the marginal token costs are much higher. And so we see a little bit fewer architectures being built with very wide unconstrained flows.
(58:18) But a good rule of thumb is if you're interested in using ReAct or something, try to stuff in four or five tools. And try to limit it to less than 10 with the current models. We have core capabilities in LlamaIndex, basically help you build workflows. So we call all of these things workflows. They're basically all agentic in some nature.
(58:40) The very rough definition of an agent-- everyone has a different definition. You guys might disagree with me. A very rough definition is just a program, like a computer program, that has nonzero LLM calls. That's a very general definition. And we basically help you write those types of programs. So whether you try to define a very constrained program where you're writing the if/else conditions or you're letting an agent handle that task, we basically have an event-driven orchestration system where every step, you can listen for a message.
(59:13) You can pass a message to a downstream step. You can pass messages back and forth between two different steps. And basically, at a certain point, these steps can be just like regular Python code. They could be LLM calls. They could be anything you want. At a certain point, the program stops, and it gives you back a response.
(59:29) And so we're building out this very fundamental, low-level orchestration because we believe that there's some interesting properties among agentic behavior, where it is fundamentally a little bit event driven. And this also provides a nice base to help users deploy workflows to production in the event that you want to translate your program into a Python service.
(59:51) So check it out. I guess there are some links here. But this stuff is all linked in the docs. I'm going to skip this piece. And then yeah, some use cases that maybe are interesting to cover, which I think the links are basically here in case you want to check it out, is we're very interested in a report generation use case.
(60:18) So this is, again, something that we see pop up across a lot of different companies. And basically, given a bank of data, you want to actually produce some output from that data. An example architecture for this is you have a researcher and a writer and maybe a reviewer as well. And you can think about this as a multiagent system.
(60:36) It depends on how you define it. But basically, you have a researcher that does a little bit of RAG. It retrieves relevant chunks and documents from some database. And maybe given a task, make sure that it basically-- it's like going on the internet and fetching stuff and storing it in your notes. It'll put that stuff in a data cache.
(60:56) Basically, this contains all the relevant information you need to generate the report. The second step is like a writer. And this writer might use this data cache to then make an LLM call that will then generate this interleaving sequence of text, image blocks, tables and give you back a full output. So we have an example architecture here.
(61:17) And we also have some example repos where you generate an entire slide deck instead of just a report. Another use case, by the way, which isn't in these slides but is very interesting, is customer support. I think if you look at just practical enterprise use cases of agents, customer support for external facing use cases is probably number one.
(61:42) Basically, there's just a lot of automation that can be baked into the decision flow to basically increase your deflection rate and basically ensure that the user ends up having a much better experience than going through those automated phone menus. And so we see that popping up in a lot of different places too.
(62:03) And then the last bit is really around running agents in production. I think, so far, if you start off building a lot of these components, you're probably going to start off in a Jupyter Notebook. And that's totally fine. When you start off building a prototype, it makes sense to do something that's very local, that's very narrowly scoped.
(62:23) And you basically see if it works over test data. An interesting design exercise is to think about what complex multiagent architecture looks like and how we can leverage existing production infrastructure components to achieve that vision of multiagents in production. Ideally, if you think about agent 1, agent 2, agent 3, every agent is responsible for solving some task.
(62:48) And they can all communicate with each other in some way. So you ideally can encapsulate their behavior behind some API interface. And then you can standardize the way they communicate with each other through some sort of core mastering layer. You can easily scale up the number of agents in this overall system to add more to this multiagent network.
(63:09) And you can also take into account a large volume of client requests with different sessions. So this is basically what we're building. And it's a work in progress. But we've made a lot of progress in the past few months is like, how do you actually deploy agentic workflows as microservices in production? So you model every agent workflow as like a service API.
(63:33) We allow you to spin this up locally and also deploy this on, for instance, like Kubernetes. All agent communication happens via a central message queue. You can have human-in-the-loop as a service. So for instance, if this agent actually needs your input, it will send a message back to you, await your response, and then you give it an input before it resumes execution.
(63:58) How many of you have seen the Devon demo, like the Cognition Labs' Devon? You know what I'm talking about? How many of know what Devon is? All right, so half of you. I think if you took a look at the demo, one example that does is like this coding agent will just generate an entire repository for you. But sometimes it will stop.
(64:16) Sometimes it will say, I don't actually have enough clarity to basically give you the response. Can you actually tell me what to do next? And so if you played around with Devon, that's basically what it does. And that's an example of the human-in-the-loop, this interesting back and forth client-server communication, where the server is actually waiting on the client to send like a human feedback message.
(64:36) So that's basically it. All these components, I think, are step by step towards this idea of building a production-grade multimodal knowledge assistant over your data. And yeah, thanks.
