(3) CS 194/294-196 (LLM Agents) - Lecture 9, Jim Fan - YouTube
https://www.youtube.com/watch?v=Qhxr0uVT2zs&list=PLS01nW3RtgopsNLeM936V4TNSsvvVglLc&index=4

Transcript:
(0:00) JIM FAN: You're in the right room. This is the right title slide. "A Tale of Two Kittens." I want to share a story that gave me a lot of inspiration during my own PhD time. In 1963, two scientists, Held and Hein, they did an ingenious experiment. So what they did is they put two newborn kittens in this device that looks like a merry-go-round.
(0:26) And these two kittens have not yet seen the visual world. And it's just their first moments in the world. So what happens is the active kitten on the right-hand side-- it can move freely, while the passive kitten is constrained to this basket while the motion is transmitted over this little hinge between the active and passive kitten.
(0:50) And the difference is the active kitten can move around freely and observe the world, while the passive kitten can also see the world, but it does not control the body. It cannot control its own motion. And now after a few days, the scientists test the kitten's visual system. And they found that only the active kitten developed a healthy visual motor loop, and the passive kitten was not able to respond to things like visual cliff or avoiding obstacles.
(1:23) And note that both kittens experienced the exact same visual sequences. The data is same and mirrored, but the only difference is the agency that the kittens took. And these days, we have a very powerful passive kitten, and its name is ChatGPT. But for us humans, when human babies were born, we don't go to Wikipedia and read terabytes of text from the internet.
(1:51) We play with objects. We touch them, taste them, break them. We experiment. We fail. We try something new again. So people call babies scientists in the crib. And embodiment and active agency are what make us human. So I believe the future belongs to embodied AI here. And today, let's talk about NVIDIA's big plan on building embodied AI, Project GR00T.
(2:21) And my name is Jim Fan. I am leading the GEAR group, which stands for Generalists Embodied Agent Research. So in March this year-- this is a scene from GTC, NVIDIA's biggest annual conference. And here on stage, we have 10 humanoids. Nine of them are electric or mechanical. And there's one biological humanoid in the middle, and his name is Jensen.
(2:49) So this is the announcement of Project GR00T, which is NVIDIA's goal to build the AI brain for humanoid robots. So you have a very natural question, right? Like, why are we doing humanoid? Well, because we know it works. Human form factor that you and I all embody right now is what produces all the economically valuable physical labor.
(3:15) And the second reason is we're entering an age where humans and robots-- they need to work hand in hand. And for humanoid robot form factor, it's much more socially acceptable. It's much easier for humans to work alongside robots to predict what the humanoid will do compared to some spider robot that is super unpredictable.
(3:38) And the third reason is a bit more pragmatic, because the world that we live in are designed for the human form factor. All the public facilities like hospitals, restaurants, supermarkets-- all the tools and equipments that we have are designed for two hands, two legs, and five fingers for each hand, so it's just easier for the humanoid to use all the tools and blending the world that we already built.
(4:09) And the second question is, why are we doing this now? Actually, towards the end of 2023, beginning of this year, we have seen a surge in state of the art hardware for humanoid robots. And just to name a few, and this is by no means exhaustive, there's Tesla Optimus, Boston Dynamics e-Atlas, where this robot does some crazy moves, like 360-degree joints, that's kind of breaking your brain, and then Figure F.02.
(4:42) And also, we see this trend of the price of the humanoid and the cost trending towards a very low value. Actually, humanoids have been around for a while. In 2001, NASA has built something called the Robonaut that cost north of $1.5 million to build. And just a couple of months ago, Unitree announced G1 that costs about $30k, which is the price of a regular car that most of the households will be able to afford.
(5:15) And it just so happens that there is a general pattern where the manufacturing costs of a mature product will tend to the cost of goods raw materials. And a humanoid robot only takes 4% of the raw material that's needed for a car, so it's possible that this exponential decrease in costs will go even further.
(5:35) And that makes now a good time to enter this space. So Project GR00T-- that is NVIDIA's moonshot-- are building the AI brain for humanoid robots. And I cannot take all of you to our lab, which is about 50 minutes drive away in Santa Clara headquarters. But let me take you on a virtual tour. This is in our lab in Endeavor building headquarters during March GTC, where we had four different types of humanoid robots from partner companies.
(6:10) So note that NVIDIA doesn't build our own robots. We rely on the hardware from our partners to develop our AI on. So during GTC time, we had four different types of humanoid in this lab. And at that time, it was the most number of diverse humanoid species per square feet anywhere on Earth. And if you wonder where does the name Project GR00T come from, it actually stands for Generalist Robot 00 Technology.
(6:45) So we're putting zeros here because it looks like Star Wars. It's a serial code for the robot. And the keyword here is "generalist." And that is the strategy that we are taking. So there is one successful field of AI, and that is the field of NLP. Before 2019, NLP has been around for a long time, and there are a lot of specialists systems, like sentiment analysis, recommendation system, summarization, information retrieval.
(7:21) And all of these will require different training pipelines, data pipelines, evaluation, and so on. It's a very complicated field. And then in 2021, ChatGPT came and just blew everything out of the waters. It is one generalist robot-- generalist model that you can prompt and tell it the task that you need to do.
(7:44) And then once you have this generalist moment, you can specialize it back by prompting it, fine tuning it, distilling it, down to smaller models that, once again, are domain experts. And we call that the specialist-- the specialized generalist. And it's almost always the case in the historical trend that the specialized generalists are way stronger and much easier to maintain than the original specialists.
(8:14) And that is a success story from NLP. And for robotics, we're kind of still stuck here as we speak. Most of the robot systems these days are specialists. They require special hardware and special algorithms that may or may not even be learning algorithms, may or may not involve machine learning, specialized data pipeline for every use case.
(8:40) So what we do? We copy homework. We copy homework from the field of NLP, and we hope that Project GR00T will be the generalist moment-- the foundation model moment for robotics. And then after that moment, we special it back, by prompting it, distilling it, fine tuning it, back to the special use cases. And we hope that the specialized generalist robot will be much stronger than all the robot systems that we have today.
(9:11) So this is the guiding principle that we operate on, but how do we really make this a reality? So today, I'm going to talk about some blueprints. Three principles. If you take home anything from today's talk, just three things, the data pyramid principle, The Matrix principle, and the foundation agent principle.
(9:36) And all of my daily work, our team's research, adhere to these principles that I'm sharing today. The first is data pyramid. Have you wondered why robotics is so hard? Why is NLP solved first, but we still don't have generalist robot? Well, there's one thing called the Moravec's paradox, which says that things that are easy for humans are actually really hard for machines, and vice versa.
(10:02) But have you thought about why? And I would say the culprit is on the data side because you can download terabytes of high-quality text from the internet. You cannot download motor control signals from the internet. I wish, but it does not exist. So for robotics, we need to build our own data set. And what does the data pipeline look like? It's much more complicated than just training text foundation models.
(10:31) So let's work our way downwards from the top. And the top is obvious, real robot data. You just collect some human demonstrations on the real robot hardware. And unfortunately, you are limited by a fundamental physical limit of 24 hours per robot per day. But still, this data is super useful. It's a gold mine because it's directly aligned to the real robot hardware.
(10:58) So how are we collecting that? How do we make data collection as intuitive and easy as possible? Earlier this year, Apple announced Apple Vision Pro. And then we have this really Cyberpunk moments. This is our New York subway, and someone just typing randomly in the air. Super weird. But these days, this is the norm.
(11:22) So we see Apple Vision Pro using this way, and we thought, how about we apply this to robotics? So we built a system where a user wears Apple Vision Pro, and you can teleoperate, both the robot in simulation and also in the physical world, simulation here. And this is a physical robot hand from a humanoid.
(11:47) And the idea is very simple. Vision Pro is tracking your pose, your hand pose, and then retargeting it in real time to the real robot. And additionally, you can also operate a robot in simulation. And in this way, you can teleoperate anywhere. So this is what NVIDIA built the Omniverse Cloud. And on this cloud, you can connect any XR device and then teleoperate a virtual robot there.
(12:16) And the result is that we have a very intuitive interface to collect data. And this is where on the right-hand side is the user sees through the first-person camera of the Apple Vision Pro. So you can think of it as kind of like the movie Avatar, where by wearing this, you're actually now transported to a different embodiment.
(12:38) You are now in the body of the humanoid robot. And that's also another advantage of using humanoid because you have five fingers. The robot has five fingers. You don't need much training. Anybody come here-- in 10 minutes, you'll be able to collect data for us. So here are some more videos from our lab where a user is teleoperating.
(13:01) And the motions are transmitted in real time to the humanoid. And we can also do things that are a bit more complicated, like washing dishes. And really, the limit is the humanoid hardware, whatever the hardware is capable of. So that's how we are collecting the real robot data, but you're limited by 24 hours per day.
(13:27) That is not enough, because we know that foundation models are extremely data hungry. We're talking about-- we need trillions of tokens to train any reasonable transformer these days. So how do we scale this up? How do we get more data? And the second part is what I believe to be the most interesting, which we will also carry to the second principle I'm going to talk about, and that is simulation.
(13:51) And why are we doing it here in NVIDIA? Before NVIDIA was an AI company, we were a graphics company. GPU stands for Graphics Processing Unit. Now, what does a graphics company do? Game engines and simulation. And just so happens that simulation is also going to be super useful for robotics. So NVIDIA has built a set of tools, and this is called Isaac Lab.
(14:20) What it does is it runs physics simulation in parallel on GPUs. Basically, one GPU card can let you run simulation at 10,000 times faster than real time. And in this way, you break the physical limit of 24 hours per day. You are not just limited by compute. And just as one example, to put this number in perspective, this character learns the impressive-- all of these impressive martial skills by going through 10 years worth of intense training in only two hours of compute time on a single GPU, so it's very much like the sparring dojo in the movie
(15:02) Matrix, where you learn kung fu in maybe five seconds of real-world time, but you feel like you've trained for many years. And this is the Omniverse platform. And here, this race car scene is where simulation has crossed the uncanny valley, thanks to hardware accelerated ray tracing. So in this video, not a single pixel is real.
(15:27) All of these are rendered in real time using ray tracing. And because we can render very complex worlds with these breathtaking levels of details, that means we can also use simulation to train computer vision models. That can be much more accurate because you can also query the ground truth states of every objects, every segmentation mask in a simulation.
(15:50) And we can use that to build a perception model for robots. And once we have the simulation and the real robot data, we still need all the data from the internet. And that is just the regular foundation model, training GPT-4o, multi-modal foundation models. You will collect a lot of text and videos from the internet.
(16:15) And the robots will still need to learn, like, physical common sense from lots and lots of videos online. So this line is the most amount of data-- I'm talking about exabytes, and probably more than that per day. But this is also the most noisy. And then for simulation, we're talking about perhaps terabytes per GPU per day.
(16:34) And it comes with not just the pixels, but also the actions. And that goes to principle number two, how we think about building robotics. And I'm calling this the matrix principle. And two questions. First, do we live in a simulation? There was this very interesting argument that all of us right now are living in the matrix, a simulation, and all the human intelligence is actually some emergent behavior that an alien civilization runs on their supercomputer.
(17:08) Their NVIDIA is now simulating all of us here, right? Well, I don't know if this is true or not, but there's one thing I know. It's the future of robotics will live in simulation. The robots will spend most of their lives in simulation. And that's the only way that we can collect trillions and trillions of high-quality tokens, by breaking the barrier of 24 hours per day.
(17:35) And why does simulation work for training robots? And again, just one sentence takeaway. That's because it's easier to simulate a problem than to solve it. Let me repeat. It's easier to simulate a problem than to solve it. And this-- here are some very compelling examples from the past literature of AI. This is AlphaGo.
(18:01) In 2016, the moment when AlphaGo beat Lee Sedol at a game of Go-- at the game of Go, and the first time ever an AI beats a human champion. The entire game of Go can be simulated in perhaps 500 lines of Python. That's a rule of Go. Extremely simple to simulate, extremely hard to solve. And another favorite example of mine is Minecraft, a very simple game.
(18:27) Minecraft is, like, what, a few hundred megabytes of an executable. But there are all the emergent behavior, all the interesting things that you can do. And going online, and look at those Minecraft channels, just absolutely insane things. Like, people build the entire Hogwarts Castle, block by block, in Minecraft.
(18:48) And someone even built a functioning CPU circuit in Minecraft, because apparently the game is Turing complete. So you can build a simulation within a simulation within Minecraft. So if any of you are interested, before I started Project GR00T, I had this work called MineDojo. And that is a framework for training generally capable agents in a game of Minecraft, where we provide a simulator as an API for AI.
(19:16) And we also provide lots and lots of data from the internet. So if you're interested, check it out. It's MineDojo.org. But basically, simulation always fascinates me. It's easier to simulate a world than to do anything interesting it. So given the simulation, what can we do? As AI scientists, we need to ask, how do we generate the data? And I will say two ways, the self-generating data and then multiply data.
(19:46) And these two corresponds to two learning paradigms, reinforcement learning and imitation learning. And let's look at how to leverage simulation for these two. And we'll start from reinforcement learning. Reinforcement learning-- what does it do? You give an AI a reward function. Basically, the reward function tells you what behavior is desired, what behavior isn't.
(20:10) And then the agent will do a lot of trial and error to maximize that reward. And a prime example is Pavlov's dog. You give it some conditioning, and it will respond to that reward after a while. So reinforcement learning is very good at training things that we don't even know how we do. So here's a question.
(20:35) How many parameters in a neural network do we need to build a cerebellum? It takes a lot of subconscious processing for us humans to exist. You're sitting in the chair or maintaining balance, standing, walking-- it actually takes a lot of neural processing power that you don't think about. And for humanoid robots, this problem is called whole body control.
(21:02) And if you're using reinforcement, it's called neural whole body control. And what are whole body control useful for? Quite a few things. Like, this is Tesla Optimus walking. So navigation requires whole body control. And this is shot in our lab using Apple Vision Pro, doing tabletop manipulation, using our two hands, require whole body control.
(21:26) And this is the latest video from Boston Dynamics, where the whole robot is manipulating objects while standing balance at the same time. And we call this whole body local manipulation. And again, that requires whole body control. So the answer to the previous question is, actually, to build a cerebellum, we need 1.5 million parameters-- not billion.
(21:50) 1.5 million parameters. So we train this network called HOVER to control the body of a humanoid robot. Basically, what this model captures is this subconscious processing that we humans do so we have a single model that learns how to coordinate the motors of a human robot, like arms and legs, to support locomotion and manipulation.
(22:17) And here is roughly how this neural network works. I will send the links to the paper, so if you're interested, there are a lot more details in the papers. But today, we focus on intuition. So what the neural network takes is that it's able to support different command format, like kinematic position tracking, like all the position of the joints; local joint angle-- the angle between the arms, for example; and the route tracking, where there's a velocity that you need to move forward to.
(22:47) These are different ways that we can use to control the robot. And you can think of them as prompts. And what HOVER can do is you can prompt it in arbitrary combination of these things. And that enables us to use different devices to capture these commands and to teleoperate the robot. And just as a few examples, for the VR device you just saw, Apple Vision Pro, this is basically head and hand pose tracking.
(23:17) So if you want the robot to stay balanced while standing, you use Apple Vision Pro. You track three points. And the rest, HOVER will figure out how to make the robot stand balanced. And for RGB camera, if you have an RGB looking at you and you're using some computer vision models to recognize the pose of the human, that would be whole body pose tracking.
(23:39) And if you are wearing an exoskeleton, that's a more advanced way to do tracking, super precise. That would be the whole body joint tracking. And if you just-- if you only have some joysticks, some PS5 game controller, then you can still control the robot, but you can only specify the root command. So HOVER enables very versatile control interface to teleoperate a robot, to collect data, and also just to control it in general.
(24:08) So here's how we've trained this model. Very simple. All the ideas I talk about, all the papers today, are super simple. You don't need to be a roboticist to understand it. So basically, I talk about these control modes. So in simulation, you pass all of this information, and then what we call privileged information, which are the information that you can query in simulators, but you cannot query in the real world.
(24:33) Like, you can know the exact location and velocity of every joint, every atom of the robot, but you don't have that information in the real robot. So that's what we call privileged information. And given all of these, it's relatively easy to train reinforcement learning to get an oracle policy that controls the robot.
(24:52) But this policy cheats because it queries things not available in the real world. So we simply mask out different control modes. We also remove these privileged proprioception privilege information. And then we distill it into a student policy using supervised learning. And this two-stage process is very simple and very effective because if you train this directly using reinforcement learning, r0 tends to be unstable, so you want to train r0 in an easy way, and then you do supervised learning on a harder problem.
(25:25) And this is a two-stage distillation. And once you do that, by masking out different information, then HOVER becomes promptable because it can take any arbitrary combination of the inputs. And here are some examples, Like you can have the head tracking or these whole body tracking or simply tracking the three points for the robot, or you can also do tracking with route velocity, so it's both tracking the pose and also moving forward like zombies.
(25:56) I hope in the next version, we'll train in a better, more natural pose, but right now, this is what we get. And you can also vary the terrains. It doesn't have to be flat. You can vary all the terrains and obstacles in Isaac Lab, the GPU accelerated parallel simulator. So you can run tens of thousands of humanoids in this environment.
(26:25) And it's also not limited to just one type of robot. Anything that can be simulated in Isaac, you can plug it into training. And then what comes out is whole-body control policies where you can have the robot follow certain pose and walking while maintaining balance. And in the future, we plan to deploy it to many more robots.
(26:50) So in this video, this is from GTC. Not every robot is running a neural network here. Some are just classical control. But HOVER is embodiment agnostic. All right. So that's reinforcement learning. And the second type and the second use case of simulation is to do imitation learning. So once again, this is the Apple Vision Pro, our set up to collect human demonstration.
(27:20) While it's quite intuitive, it's still really slow to collect these demonstrations. You can see it's not that easy. It's a lot of effort. Let's say given one human demo, which is very expensive to do-- like you-- perhaps you can take-- you need to take a few minutes to collect one. And over an hour, you can only collect 20 demonstrations.
(27:43) That's very, very little data. How do you multiply that into more data? And that's the second use of simulation. And to understand how we do this, first we need to look at what is in a robot simulation. I would say three main things. There are handmade objects that typically created by artists, handcrafted scenes, and hand-designed tasks.
(28:09) And if you do that, this is what you get. It's a really simple scene. If you want anything more complicated, you need artists to create those assets. But how about we ask AI models to help us automate some of these process? Like, for the handmade objects, there are text-to-3D models these days that are really good.
(28:33) So can we ask AI to generate lots of assets for us? And we instantiate it in the simulation. And for handcrafted scenes, ultimately, it is the USD format, the Universal Scene Description language, the USD format, which has two parts, the texture and some code that specify the scene layout. For the texture, let's just use Stable Diffusion.
(28:57) We call an AI, and it can swap out all the textures that we want in a reasonable way. No artists needed. And then because USD is just some code, there's a tool at NVIDIA called ChatUSD that's basically fine tuning LLM to generate USD. And you can generate all different layouts for the simulation. And for the tasks, it's a bunch of code in Python.
(29:25) So just prompt GPT-4, Claude-3.5, whatever your favorite LLM, to generate the code. And we get a diverse set of tasks specified in the simulation. There's making coffee and some kind of daily tasks around. And putting all of these together, we present RoboCasa, which is a generative simulation framework where every part of it, or at least as much as possible, are generated by different AI models, from the objects to the task to all the textures and layouts of the kitchens.
(30:02) And in the matrix, we can have 100,000 different kitchens. The simulation doesn't care as long as there's enough compute that goes into it. So this is RoboCasa, generative simulation framework. And given RoboCasa, we take one human demonstration from the real world, and we can replay it and multiply it into n different demonstrations.
(30:27) So here are some results from the paper where we train a policy using the real-world data only. And this is the important part, the takeaway. If you mix in the simulation data augmented by RoboCasa with all these variations, the vision model, all the variations, you will get a lot better performance out of it, just out of box.
(30:54) And this is just the preliminary results. And we hope to scale this up massively so we really take advantage of the 100,000 different kitchens that you can't afford to have in the real world. And if we apply this to humanoid, this is what it looks in Omniverse render. The humanoid is doing one motion, but it can do it in 100,000 different places.
(31:15) And that helps us train really, really strong perception models. So that's step one. RoboCasa-- what it does is some kind of visual augmentation. Well, you see that the motion itself-- it is still really hard to collect. And how do you augment not just the visuals but also the motion trajectory? And here is a system that we built to address this problem.
(31:43) So on the left-hand side is what a human collected in a simulation. So basically, the task is just to put these two pieces together. That's it. This is one human demonstration. And we can edit this human demonstration by varying the position of these objects and also varying the gripper with respect to these objects.
(32:08) So once you replay this demonstration, it will still be successful, but it will be a machine-generated demo. Why do you have a human teleoperate that and demonstrate 1,000 different configurations when you can use machines to generate that? Let the machines teach the machines. And this system we built, and our setup algorithm, is called MimicGen.
(32:32) And MimicGen is a very general purpose framework, so it can be applied to many different tasks. And here, they're making coffee, insert square, three-cube stacking. So MimicGen varies all the object configurations and generates new motion trajectories that are successful, so you get this data for free without demonstrating over and over again.
(32:57) And if the gripper and the robot hardware are close enough, you can also generalize even across the robots that you don't have. We've all been poor PhD students before, we only have one pan arm. What to do? You call MimicGen, and you also get data on other robot arms that you can't afford. And it also works on long horizon tasks, where it's multi-step, like preparing a coffee.
(33:21) You need to take some pod out of a drawer and then place it in a coffee machine and kitchen chores and sorting the bins. There are tons of variations. If you want a human just to keep teleoperating, then you need thousands of trajectories to learn any good model. But you can use MimicGen to generate the data for it.
(33:41) You can even do very precise tasks, like this one, which requires a bit of wiggling to make sure that all the nails and these holes all line up very well and these very precise factory tasks that you need to put a gear on it. And using MimicGen, you can generate lots and lots of data for it. Also works in the real world.
(34:06) We want all the data to be useful in the real world as well. And this is a figure that shows the scaling of the MimicGen generated data and the trained-- the performance of the trained policy. And it's obvious that the trend is moving up, but there is also some kind of diminishing return where if you use a thousand MimicGen generated demos and then you scale it to $5,000, it doesn't buy you too much because, one, the task is relatively simple, and second, there's also some limit in the system that puts a maximum
(34:43) for its diversity. So this was the original MimicGen work that we applied to a single robot arm. Well, the next obvious thing is to apply it to two hands. And for this task, again, it is run by MimicGen. It is a bimanual task, And we call this parallel subtask because these two arms can execute the pickups in parallel, while at the end, they need to do it a bit more sequentially.
(35:14) So this is one type of parallel subtask. And the next is called coordination subtask, where you have the two hands that need to coordinate precisely to pick up-- to lift up the tray at the same time, to lift out the lid and put it on a box at the same time. And that requires the transformations to be applied to the hand correctly so everything-- once you play the trajectory, it's still correct.
(35:40) And then there are also sequential tasks where there's a pretask and a post task, and the next task cannot happen until the pretask conditions are met. So here, the humanoid robot is pouring a ball into a-- pouring a ball into the bowl and then place it onto the green block. And again, if you want to use Apple Vision Pro to teleoperate this, it's going to take forever to do all these variations, but you shouldn't have to do.
(36:05) You can also mix and match. You can do the parallel generation and then do a two-arm coordination. And we can apply this two-hand bimanual system to a set of different embodiments, like a two-gripper setting, two-hand setting. There's tray lifting, drawer, cleaning up, and of course, humanoid setting as well, where the humanoid is able to make coffee, sorting cans, doing the pouring, and we can generate lots and lots of data from it.
(36:37) And just zooming out, This is DexMimicGen, our followup to the MimicGen work that focuses on dexterous manipulation. And once we have this data, we can train policies. We can also study the differences between different neural network architectures now that we have a much bigger data set. And we see that diffusion policy, a specific type, is the best across all of these.
(37:03) And because the tasks are much harder, we also show a much better scaling as you run the MimicGen pipeline. If you start with, let's say, a hundred demonstrations that the human collected, the success rate is not great. But you can use AI. You can use MimicGen to generate 50 times more data. And then magically, the task starts to work.
(37:28) And we can also transfer DexMimicGen into real world. That's what we want to do. So we have a human teleoperate. And then we have this Real2Sim pipeline where the real trajectory is replayed in simulation. We capture it in a digital twin of the real world. And then we run DexMimicGen to generate a huge data set.
(37:50) And then we apply that back to real. So this is a Real2Sim2Real pipeline. You collect something in real. You replay in sim. You multiply. You do everything you want in sim. And then it transfers back to real and improves the real performance. And this is one way to use simulation in the loop. So to put it together, we have a RoboCasa that takes one demonstration and multiply it to n.
(38:19) And then you have MimicGen that gives you n times n more trajectories. And I promise you, this is the most complex math we're going to do today. And run it in Omniverse on humanoid. This is what we see. We have really good rendering and also very high quality motion trajectories generated. I want to show that-- this is a clip that Elon showed on Tesla Optimus where there's a big farm of humans serving their robot overlords by teleoperating a robot.
(38:55) It's kind of dystopian. And the humans do the same task over and over again. Actually, for one of those battery packing tasks, it may take thousands of demonstrations for human to collect enough data to train a model that works. So if we can run simulation to multiply the data, we can save all of these humans from this destiny.
(39:23) And next, and the final principle I want to talk about, is the foundation agent principle. That's also how we think about building the GR00T foundation model. So most of the research on robotics can be mapped on these three axes-- the number of embodiments it can master, the number of skills it can do, and the number of realities that it can work with.
(39:57) And this is what I mean by embodiments. In Star Wars, there are so many different types of robots. And my question is, can we have a single neural network that can control all of them so we don't need to develop a separate pipeline for each robot? And, like, two years ago, we had a proof of concept work called MetaMorph where we trained a single neural network that can control thousands of different robots.
(40:26) And here, all of these robots are procedurally generated. They're quite simple, so this is a proof of concept, but you can use one set of neural network weights to control them. And here, the idea is also quite simple. You take each robots, and they're basically a graph of joints. And we call that kinematic tree.
(40:46) And you do a depth-first search, depth-first traversal, through this body of the robot. And you will be able to flatten it into a sequence of tokens where each token has some physical properties attached to it. So what does this look like? It looks like a sentence written in a new grammar, where the grammar describes the morphology of the robot.
(41:11) And if you have lots of robots, you can convert them to lots of sentences. And each may be of different lengths. And you tokenize them. And this is your screaming transformers, sequence models, screaming transformers. And that's what we do. We train the MetaMorph transformer that maps the sequence to the actions directly.
(41:35) And you have variable length sequences, but they are just like English sentences. And you apply-- you map them to variable lengths of actions. And we train these robots on a few different tasks, like flat terrain, walking, complex terrains, or avoiding obstacles. And we show that using a single set of neural network weights plus reinforcement learning and accelerated simulation, we can train one neural network that rules them all.
(42:05) At GTC, Jensen showed all the different humanoid robot bodies. So that's where we're going for MetaMorph 2.0. We want a single model that can control all the humanoid robot bodies, even though there are slightly different from each other. And the second axis is the number of skills that a robot agent can do.
(42:29) And we showed that MimicGen is one way to have the robot master more skills, but can we automate some of this process? And that is the eureka work. So we train an agent that can achieve robot dexterity at superhuman level. Well, maybe not all humans, but definitely with respect to myself, because I'm never able to do pen spinning like that.
(42:58) I have given up long since childhood, so now my AI can avenge my poor skills. But I am a subpar human here. And how are we doing this? It's actually a very complex skill to learn here. And it's not clear how to even specify the reward function for it. And what we found is basically, the reward function is just a bunch of Python code.
(43:24) Like in the Isaac Lab simulator, all of the robot simulation is written in these Python APIs. So what we can do is we just ask ChatGPT to do the hard work for us. We give the LLM a prompt, saying that-- please find a reward function that can make the shadow hand do pen spinning. And you pass in some context, which is basically the Isaac Lab simulator API as code.
(43:57) And to our surprise, GPT-4 or, really, any of the frontier models, is able to implement the reward function correctly for most of the time. And it's actually really surprising because these reward functions actually take trained roboticists to do. You need to be very familiar with these physics simulators to even be able to write that.
(44:18) But models like GPTs-- they learned all the text in the world, and they probably read all the papers that I wrote before as well, so they're able to do this. And we take the generated reward function, and we pass it to 10,000 copies running in parallel on a single GPU. And we're able to quickly get results by running a reinforcement learning loop on this reward function in, let's say, 15 minutes.
(44:45) And that completes a loop. We sample reward functions. We call a massively parallel accelerator training. And then we can report the results back to GPT. And these frontier models are actually very good at self-reflection, so they can debug, and they can find their own problems. And sometimes they'll apologize to you and then write a better function for you, and you just repeat this loop, so it writes better and better reward functions.
(45:15) And here are some examples of what types of reward function edits that GPT does, and it's quite nontrivial. Sometimes it's as simple as just changing the hyperparameter, but sometimes LLM decides to change the functional form of the reward function completely. And occasionally, it adds completely new components to, let's say, track the angular velocity, make some penalty on the different joint angles.
(45:42) It's doing something very complicated here. But all of this code is edited iteratively by an LLM. And if you do this iteration a couple of times, we see that Eureka gradually becomes superhuman with respect to expert-written reward functions. And this is a very general framework. Any skill-- you can prompt Eureka to do it.
(46:08) In fact, for this pen spinning along different axes, you actually need different reward functions to do it. And LLM has no problem figuring this out. And for some of the tasks-- like, this is actually spinning pen against gravity, and it's able to write a reward function to solve it. So that's Eureka. And when we were doing Eureka, it was a work purely in simulation.
(46:36) So the next question is, how can we transfer from the sim to the real world? And that goes to the third axis, the reality axis, where here, reality means simulation plus the real world. And we see the HOVER work here, because for HOVER, we need to transfer the policy to the real humanoid. But HOVER is hand-tuned, but can we automate it? So we introduce DrEureka.
(47:04) And to understand DrEureka, first you need to understand DR, Domain Randomization. It's actually a very simple technique. In simulation, you will vary the physical parameters, like gravity, friction, weights, size, and all of these parameters because in simulation, the physics is not going to be as realistic as the real world.
(47:29) Like, no matter how good a simulation is, it will never be there completely. There will always be some imperfections in the simulation. So what you do is instead of creating the absolute most accurate simulation, you trade compute for it. You just run many, many different types of reality where the gravity is not the same, friction is not the same.
(47:51) And here comes the catch, right? If you have an AI model that can master 10,000 different physical parameters, then it may very well generalize to the 10,001st reality, which is the physical world that we live in. And that is the principle of symmetry of domain randomization, a super simple idea that works super well.
(48:16) So again, this is the Eureka loop, where we ask GPT to do the reward function. Well, how to do the domain randomization? Typically, what people do is you rely on your expertise, domain randomization, all the parameters. You need a lot of experience from your past papers or whatever to implement. But we're very lazy, so we just ask GPT to do it for us, because domain randomization parameters are ultimately still just Python code.
(48:46) And to our surprise, LLMs know how to do it. They know how to do it. They can also iteratively improve it. And once you have this loop, let's transfer that to the real world. So here on the left-hand side is a simulation. On the right-hand side is the real deployment. So we trained a robot dog to walk here, have DrEureka write some parameters.
(49:11) And then it transfers zero shot without fine tuning in the real world. And this is the Allegro hand manipulating a cube. And again, it transfers zero shot to the real hand. And finally, we decided to do something crazy. We want to have a robot dog to walk on a yoga ball. But the issue is the yoga ball is bouncy, and it's extremely hard to simulate.
(49:36) In fact, very, very few simulators can do this correctly. And here, we just have a rigid ball. Like, this ball isn't even bouncy. It's rigid in simulation, so the physics is completely off. And now we run DrEureka. And then we just ask for it to transfer to real world, And that's what we get, where we have our students just walk their dog, robot dog, casually on the street on a yoga ball.
(50:04) So when I shared this with the public, I got a comment. So someone actually-- after seeing this work, they tried it on their real dog. And their dog is not able to walk a yoga ball. I wouldn't call this superhuman, but I think this is perhaps some super dog level intelligence. And that's DrEureka So to take a step back, what Eureka does is you have an LLM.
(50:36) And then you have it coding Isaac API to generate a reward function. This is one loop, and this loop involves a huge foundation model. And given this reward function, you have a second loop that does reinforcement learning to produce a small neural network that basically simulates the dogs and the hands and all of those.
(50:56) So Eureka is a dual-loop system. You have an outer loop that does reasoning and an inner loop that does model control. And DrEureka just take this one step further. In addition to reward function writing, it also writes the Sim2Real configuration for it. And my question is in the future, for Eureka++, why can't we just write more things? Why can't we just ask LLMs to do more work? Can we have it write new task, new simulation completely, or perhaps even new training algorithms that I don't know how to invent and just have the loop running?
(51:35) So my dream is someday, if we implement Eureka++ correctly, all of my team will take a one-week vacation, and then we come back, and Jensen wouldn't even notice because we're still reporting updates to him. And that's the goal. And once you have all of these axes and you push to the top-right corner, this is where we want to be, where we have a single foundation agent that can master all kinds of different bodies and do lots of different skills automated and also generalize across different simulations and ultimately to the real world.
(52:21) This is how people train ChatGPT. It's a sequence model that you take text in, and then you output text, be it Python writing or poetry, summarization, reasoning-- it does not matter. And ChatGPT trains this by scaling it up across lots and lots of internet text data. And I believe this is what the GR00T model would look like in terms of the form factor, where it takes as input an embodiment specification telling you what you're controlling and an instruction in natural language, and it outputs actions.
(52:57) And you train GR00T by scaling this up over lots and lots of realities. And how are we doing this? We need a computing platform to do this. So this is what Jensen proposed to be the three-computer problem for NVIDIA. The first computer is called OVX, and that's basically the graphics computer. And what it does is generate tokens.
(53:21) If you're using the transformer terminology, it generates tokens, and RoboCasa, MimicGen, Eureka-- all of these works in simulation I talk about first on the first computer. And the second computer is the DGX system, and those are the H100, H800s of the world. And they learn tokens. They take the generated tokens.
(53:43) They compress them, learn them in a transformer, and these are the foundation agent models. And finally, we deploy tokens. And that is the AGX system, the Jetson [INAUDIBLE] series of chips that will run on board the humanoid robots. So those are the computers that deploy tokens from all of these intelligence.
(54:06) And then you can validate the performance of this again in a digital twin inside the matrix. And that completes the three-computer problem. And there is this software orchestration framework called OSMO that we use to schedule all the heterogeneous compute workload. So if you fell asleep, don't worry. This is the one slide that you need to take home today.
(54:31) We talked about three principles, the data pyramid and The Matrix that supply the data to train the foundation agent. And then we talk about the three computers as a computing platform that enable all of these research to happen, three principles and the three computers. I think it's going to take a while for us to solve robotics.
(54:56) This is perhaps the last AI problem, the hardest AI problem. And on our way to solve the GR00T foundation agent, we hope that we'll build all the tools and research that will inspire the community, lift up the community, lower the barrier of entry to access all of these advancements that we have built. And I believe humanoid robots will be $1 trillion business.
(55:25) I don't think this is very controversial because we're talking about the entire labor market to be automated once we have these generalist robot foundation models. But using Jensen's word, right now, it is a $0 trillion business because things don't work. But precisely because it is $0 trillion, it's 0 today and trillion tomorrow, So we need to start working on this right now.
(55:52) And I believe AGI cannot be achieved without embodiment. One day, hopefully soon, we'll build our active kitten in silicon. And embodied AI is our North Star. And I believe GR00T holds the key to this future. And please join us on our journey to the moon. Thanks.
