(3) CS 194/294-196 (LLM Agents) - Lecture 10, Percy Liang - YouTube
https://www.youtube.com/watch?v=f3KKx9LWntQ&list=PLS01nW3RtgopsNLeM936V4TNSsvvVglLc&index=3

Transcript:
(0:01) PERCY LIANG: Pleasure to be here. I'm going to talk about some of our work on agents, but I want to take a broader view of what's happening in AI and focus on the importance of open-source. So everyone has seen some figure like this where over the last few years the capabilities of these models have simply skyrocketed.
(0:21) But what's less appreciated is that the openness and access to these models has plummeted almost symmetrically. We used to have access to the full paper, weights, code, data, and now all we have is API. It's a very good API, but it's just an API. And so the question is, why does access matter at all? You can do a lot of things with just the API.
(0:46) But I want to make a simple point here, is that access shapes research. This is something that I don't think we think about very much, because most of the time we have our heads down. We're just working on whatever problem given what we have. But if you look historically, access has really dramatically changed the type of work that has happened.
(1:06) So going back to the '90s, where the entire field of statistical NLP emerged, just because we had access to internet scale data, text in digital form. In the 2010s, there was a proliferation of these organic, large scale annotated data sets like ImageNet and SQuAD, and these were available because we had crowdsourcing platforms like Amazon Mechanical Turk.
(1:31) And then finally in the 2010s, of course, we wouldn't be here without GPUs. Deep learning would not have taken off. So access shapes research. So for foundation models, I'm going to talk about three types of access. The first is API access to a black box model. The second is open-weight access, and the third is open-source.
(1:53) So API access, think about it as you're a cognitive scientist. And all you can do is measure the behavior of this black box by putting in prompts and getting out responses. But you can't look inside the black box. I'm going to talk about how you can actually do a lot here. And in particular, since this is an Asian class, I'm going to talk about all the interesting types of agents you can build using just black box access.
(2:22) Openly access, you can think about it as you're a neuroscientist who can now poke at the internals of what's in this black box. Now, of course, real neuroscientists have it much harder than we do, but you can get a little bit more deeper access, which allows you to start to begin to understand the mechanisms behind the models and also to create novel derivatives such as distillations and fine-tunes.
(2:49) But both API and open-weight access, you have to remember we're still confined to existing models, to their architectures and to their design decisions. And what we need instead is open-source access. So an open-source access, you think like a computer scientist who is building a system and who can control every part of it.
(3:11) And this gives you the opportunity to really question everything. To question the data set, to question the model architecture, to question the training procedure. You have the ability to hopefully make things better as a result. So we're going to go-- in the rest of the talk I'm going to descend this layer cake step-by-step.
(3:32) So starting with models, so if you think about an API model, I want you to think about it as what I call a, quote unquote, "a universal function." It's a function such that if you give a prompt with a natural language instruction, that function can do a lot of different things. It can summarize, it can verify things, it can generate.
(3:56) And that's the power of these foundation models, is that they're so general purpose. And now once you have one of these APIs, then you can start thinking about composing them into larger systems, which we can then think about as agents. And importantly, we want to think about the API as a controller of execution.
(4:20) So it's not the case that you have just a fixed program and then all you're doing is calling out to an API at the leaves as primitives. But in fact, the API actually controls the execution flow. It controls what the program is going to do next. So here is a basic architecture of an agent that we're going to work with through the next 10 or 20 minutes or so.
(4:45) And here's the way it works. So an agent takes observations from the real world. It perceives them and adds them to a memory stream. A memory stream is going to be something that records everything that has happened so far. And because it's a digital agent, there's no reason to forget. It simply stores everything.
(5:06) So now this stores too much. Now you need to retrieve the relevant memories. And that gives you a piece of context that you can use to do one of three things. You can act in the present, which means essentially tool use, grounding into the real world, taking various actions. You can look backwards and you can reflect on your memories.
(5:32) This is things, you can think about summarizing what you've seen so far, verifying what you've seen so far, and so on. And you can plan. You can plan for the future. And this gives agents a sense of long-term direction as opposed to just simply reacting in the moment. So with this basic engine architecture where every one of these boxes is powered by one of these black box LM APIs, we can start to build things.
(6:03) And I'm going to tell you two types of agents we can build. One class of agents is problem-solving agents. These are the agents that you typically think of when you think of the word agent. They solve problems. The second is going to be simulation agents, which is something that maybe not all of you have seen, but I think is really, really fascinating.
(6:22) So let's go through problem-solving agents. So I'm going to show you two works that we have recently on problem-solving agents. The first is MLAgentBench. This is work with my student Jian and appeared at ICML this year. So the problem here is something that all of you are familiar with. You're given a machine learning problem, some data, some starter code, and an evaluator for test accuracy, and your goal is simply to build the best machine learning model you can.
(6:55) OK, so what do you do? You write some code. You run the code. You get some log of what has happened. And then based on that, you think a little bit and revise the code and you run the code, and so on. OK, so this is what all of you have done. And the goal is to build a LM agent that actually does that. So this is in a slide basically what we do.
(7:24) So we craft this prompt. So you're a helpful research assistant. Here's the things you can do. Here's the task description. And I want the agent to reflect on the observations that it's gotten so far, make a plan of what to do next, fact check that plan, because sometimes to guard against hallucinations, think a little bit and propose an action to take.
(7:54) OK, so then that goes into the language model. The language model dutifully does a reflection, plan, fact checks, thinks, and then produces an action. And that action is something like run a script, edit a file, so on. And that produces through interactions with the underlying environment, a observation. In this case, it's executing this train.
(8:20) py file and produces some log of training accuracies. And now, given that observation, it goes back into this memory stream, where the memory stream-- when we construct the prompt, we give the last three elements of the memory stream. So the memory stream has basically the full log of what has happened so far. So basically, this is just-- it goes in a loop.
(8:49) The LM generates some actions. It generates some observations by executing code. And it goes back into memory stream. So relatively simple. We created 13 different benchmarks and evaluated a whole host of different models. And the results were mixed, I would say. The best performing model is Claude 3. And for some of the more classic Kaggle Competitions, it does really well on, presumably because it probably has seen some of this in its training data.
(9:24) But some of the newer ones, like BabyLM, it just completely fails on. So this is deliberately chosen to be a challenging problem. But I think it's something that I think at the cusp of what current language modeling agents can actually do. So there's actually a whole bunch of work in this area, which I think is really exciting.
(9:48) And the idea here is that I think of this as self-improvement. So this is not any agent benchmark. It's an agent benchmark that's aimed to improve research, which means that if you solve the task, you actually get better algorithms. You're improving the model. And once you improve the model, maybe you can solve the task better.
(10:09) So I think that's a kind of an interesting thing to keep in mind. So now, let's talk about a second type of agent that we've been working on, one for solving cybersecurity challenges. So here is the task. So you have an agent. It's given access to a server running, let's say, at port 80. And it's also given the code that's running on the server.
(10:36) So it has a copy of the code locally. And furthermore, it has a Bash shell, which it can do whatever it wants. And the goal of the agent is essentially to hack into the server. And specifically, what that means is it's going to read the code, understand, and identify a potential security vulnerability, exploit that by running some sort of Bash command, and then retrieving what is known as a flag, which is a secret string to prove that it has indeed broken into the server.
(11:11) So the evaluation here is very, I think, clear. And these are called capture the flag competitions, which are hosted annually by cybersecurity people. And so that's a task. This is an extremely hard task. I don't think I could do this very well. And to make it a little bit easier, we have the option of giving the agent a set of sub questions which attempt to break down the ultimate task into a bunch of different substeps, where we can measure, hopefully, some partial progress.
(11:57) So the way we build an agent to solve the cybersecurity task is essentially the same recipe as we use for the agent bench. So you ask the agent to reflect, make a plan, think, and execute a command. Now, that command returns some-- after the execution, some observations are returned. And that's used to update the memory stream.
(12:28) So it's a very general recipe that you can see applies to a bunch of quite radically different agent problems. So how do the results look? So we benchmarked a bunch of different models. And the best performing model is, again, Claude 3.5. It solves 17.5% of the benchmarks. And the interesting thing is not all the benchmarks are the same.
(12:56) The hardest benchmark in our task in our SciBench took a team of humans 24 hours to solve. And these are people with cybersecurity experience. They're not just random people. They're competitors. And the hardest task that the best model was able to solve to date took humans 11 minutes. So quite a bit of gap between what humans can do and what the best models can do.
(13:28) But again, this gap is shrinking over time. So again, this cybersecurity benchmark is interesting not just because it's a difficult and interesting agent benchmark but also because of two reasons. One is that there is a tremendous amount of wonder and interest in understanding the risks of AI in particular with relation to cybersecurity.
(13:57) So is it the case that once you develop frontier models, now we have these enormously powerful agents that can basically break the internet? And so from that perspective, AI policymakers are really interested in the question of risk assessment. And this benchmark provides a quantified way of thinking about risk.
(14:18) On the other hand, there are cybersecurity people who routinely build penetration testing tools. So these are tools which try to identify and break code before deployment so that you can fix the problems before you put the server up. And so these folks are really excited about this work because now we suddenly have much more effective penetration testing tools.
(14:46) So, so far, I've shown you with a fairly simple prompting strategy. You can build problem solving agents in both ML engineering and cybersecurity. Now, let's turn to building simulation agents. So this is based on a paper by June from last year. And the goal is, can we simulate people or human behavior? So in order to do that, we built a virtual world, which we called Smallville.
(15:20) And there are 25 agents. Each agent is powered by an LM architecture, which models this architecture. In fact, this figure comes from that original paper, which we've repurposed over and over again for our agent projects. I'm going to focus on two pieces of this-- so retrieve and reflect. So remember, the memory stream remembers everything that happens.
(15:52) And if you think about a human, like yourself, you're wandering around. Most things that you experience are fairly mundane. But nonetheless, they all go into the memory stream. So the memory stream has something like refrigerator is idle or desk is neat and organized. So hardly remarkable things to write home about.
(16:12) But you never know what you're going to need. And that's why we store everything. Now, the problem gets kicked to at inference time, where you need to make use of this memory. You can't put everything in the memory stream in your prompt. So you need to retrieve. So we retrieve based on three criteria. One is recency.
(16:36) So things that happen in recent memory are presumably things that you should pay attention to more. Importance, which is events are generally marked with some sort of intrinsic importance. Like, disk is idle is not very important, whereas if you got married, that's probably pretty important, I would hope. And the third thing is relevant.
(16:59) So based on the current context, which things are relevant? So for example, if you are looking for your water bottle, then maybe the memories of, hmm, where water bottle has been is now suddenly much more relevant than it would otherwise be. So with these three factors, we do retrieval to slim down the memory stream dynamically to a much smaller set.
(17:26) The second thing I want to mention is reflection. So remember, the memory stream is very, very low level. And if you have an agent that just reacts to individual memories, that's going to be an agent that is going to be fairly sort of in the weeds. So reflection takes groups of memories and abstracts them.
(17:46) So for example, we have this guy in the Smallville call, whose name is Klaus Mueller. He's reading about urban design. So that's a concrete observation. But then it gets abstracted again and again into something high level, like, Klaus Mueller is highly dedicated research. So this is what gives the agents a more global sense of purpose rather than reacting to low level details.
(18:13) Another thing I think is really important about modeling humans is that humans are social animals. And they talk to each other. And so our agents also talk to each other. So in one instance, Isabella is planning a Valentine's Day party. She tells all her friends about it. And then those friends continue to spread the news to other people.
(18:36) And sometimes, the news comes back to Isabella. And so one thing that's really interesting about the simulation that we observed is how information diffusion is modeled through the multi-agent simulation. So one problem with this generative agents is that it's all made up. These are not real people. These are just little avatars running around in this virtual environment.
(19:03) But the true promise, I think, of this technology is, can we simulate actual real people? So what does that even mean? So this is a paper that we just put out on archive, so as of today or yesterday. And this is our attempt to make it more real-- so again, led by student June. And the key insight here is interviews.
(19:29) Because remember, people are very rich. And how do you pack an individual into an LM agent? So interviews have been used as a tool in the social sciences and for quite some time to capture a lot of rich information about people. And so when we were doing these pilot studies, an interviewer was asking Joon, to start, tell me the story of your life, starting from the beginning.
(19:59) And Joon talks. Well, I was born in Korea. I moved to Boston. And then the interviewer follows up dynamically and asks, like, oh, tell me about what it was like transitioning to Boston from Korea. Well, moving to a different country is really a heavy lift and so on. So you can see that the interview content actually captures enormous amount of information.
(20:22) It captures not only the factual content of what actually happened but also the perspectives and the personalities of the interviewee. And so what we did here was to try to scale this up. So we sampled 1,000 people, demographically diverse, from the US. We had each of them go through a two-hour audio interview with an LM agent, of course.
(20:52) And this produced an interview transcript. That interview transcript now becomes a seed of the agent memory. So now, we have, for each human over here, a corresponding agent over here. So we have 1,000 humans, 1,000 agents. And the question is, how close are the agents to the actual corresponding humans at the individual level? To assess this, we ran a battery of surveys, games, and experiments, which are taken from the social scientists, economics, psychology.
(21:30) And we ran the same exact experiments on the humans as on the agents. And then we're just comparing. Do they match or not? So this is a very nice, quantitative way to answer the question, are these simulated agents actually behaving like people? And a very quick snapshot of the result is that, yes, by and large, the agents are able to capture humans-- of course, not completely.
(21:57) One, I think, confounding factor is that humans are themselves not consistent. So if you ask humans to do these surveys and then you ask them two weeks later, they are only 81% of the time matching themselves. So that's a skyline of how far you can guess. And now, you have these agents based on GPT-4o trying to answer the same questions.
(22:25) They get around 69%. So you can think about this as you're capturing 85% of the total accuracy, where baseline is something like in the 20s. So this is not bad. And we see that if you don't use the interviews, it's worse. So the interviews do actually capture a tremendous amount of richness. So let me reflect a bit on black box API access.
(22:56) So we can do a ton with just black box APIs. You can create all these agents to solve complex problems in engineering and cybersecurity and also to actually create simulation agents. And you can think about this as really building an approximate digital twin of society. So social sciences are actually really excited about this tool because you can-- it basically provides in vitro laboratory for them to run experiments.
(23:24) And you can go to do cool things like give the same agent the control and a treatment and then measure the difference. And we have 1,000, demographically diverse. So you get sort of breadth. Now, of course, it's important to remember that these are not real humans. And your mileage might vary depending on what your questions are asked.
(23:45) But this technology, again, is getting only better. And as long as we're being grounded and realistic in our expectations, I think this is going to be a very powerful tool. So one thing that I think is lacking so far is that so far these agents have been static. We've taken off the shelf language models, such as GPT 3, 4o, or Claude.
(24:09) And we simply prompted them. And what we really like these agents to be able to do is learn from experience. This is what humans do. We're not static. So you can think about this analogy of where we are with AlphaGo. So remember, AlphaGo had two phases. One is supervised learning on top of human games and then followed by reinforcement learning, where it sort of self-improved.
(24:34) And I think we're for LM agents or language models, we're sort of here right now, where we've invested over the last four years quite a bit of energy in terms of pre-training, these very, very powerful models. And now, we're beginning to build agents. And one opportunity I see with agents is not that only that they can do interesting tasks.
(24:55) But they also present new opportunities for improvement. Because once you have an agent that's operating somewhere in the environment, it can presumably get some sort of reward signal. And with that reward signal is now you're off to the races in terms of improving the agent. So I think that's a lot of exciting stuff to come.
(25:16) But in order to do all of that, you need deeper access to models. You can't just have the black box API anymore. So let's now descend the layer cake into open-weight models. So what is the open-weight model? So technically, these are-- more accurately, they're called dual-use foundation models with widely available weights.
(25:38) This terminology comes from the executive order from last year. It's a bit of a mouthful. So I'm going to use the term open weights. And just as a kind of a technicality but an important one, the reason that they're widely available as opposed to just available is that the licenses of these models, including Llama, have restrictions.
(25:58) For example, if you have 700 million monthly active users, then it's not open to you. You have to get a special license from Meta. So one immediate off the bat advantage of open-weight models is reproducibility. So how many of you have run experiments on text-davinci-003 or 002? So some of you. And well, too bad because it's no longer available.
(26:27) So one problem with these API models is that you don't control them. And once in a while, they just disappear, which is really bad for reproducibility because now you can reproduce any of those experiments that you had done before. Whereas if you had the weights, you can just store them on your laptop, under your mattress, wherever, where they will be safe.
(26:51) And they're forever. So what can you do from a research perspective with open-weight models? I'm going to give you a few examples of some of the types of research you can do. So much of this is not my work. So this is work by folks from Transluce. Apparently, if you ask Lama-3.1-8B-Instruct this question, which is bigger, 9.8 or 9.
(27:25) 11? it will reply 9.11 is bigger than 9.8. So if you're paying attention, this is actually incorrect, I think. Right, everyone? OK, good. So if you just had an API model, you'd be scratching your head. Like, why is this language model being so ridiculous? But if you have open weights, you can open up the hood. And then you can try to figure out what's going on.
(27:50) So this is what the Transluce folks did, which is really cool, is that for every single neuron, they looked at the examples that trigger that neuron and use that to create a summary of that neuron. So now, actually, there's some cool interface you can go play with online, where if you mouse over 11, it shows you that 11 is triggering this neuron that fires in references to September 11th.
(28:18) So now, you think, oh, OK, I think I know what's going on, is that this is getting interpreted as a date or somewhat as a date. And if they're dates, then 9/11 is after 9/8. So by looking inside, you can actually get a much deeper insight into the inner workings of these models and debug some of these mysteries.
(28:41) They also have some examples you can see online, where you have a model that refuses to answer a question. But then if you look inside, it clearly knows the answer and is not just refusing to tell you. So here's another fun paper from NVIDIA, where they took a 15-billion parameter model, prune away some of the layers, and resulted in an 8B and 4B model with minimal drop in performance.
(29:10) So I think this is one instance, I think, of a larger trend that I think is really interesting, where you have big models that we have trained. But in order to use them, you need to make them smaller. And so the question is, how do you take the power and modeling capability of a larger model and push it into a smaller model? And I think it's relevant for agents because agents-- remember, you need an API in the inner loop of perhaps doing some more complicated task.
(29:43) And you want potentially your APIs to be much faster than just doing a one shot query of an API. Here's another project that I think was really impactful, which is adversarial attacks. So these authors took Llama and optimized for a prompt that essentially tries to jailbreak it to get the model to do malicious things.
(30:15) And for this, you need open layer access because you need to look at the gradients through the model, which is something you can't do with GPT-4o. And you optimize over the prompt. So that's fine. But the really mind blowing thing is that you take this prompt that is optimized for Llama 3. And then you just take it and put it into the prompt for an OpenAI model.
(30:42) I think this is GPT 4. And it actually jailbreaks GPT 4. So this is really fascinating because there's something really deep and mysterious about the transferability of these attacks. And also, it goes to show that accessed open-weight models actually have implications on closed models because it tells us something about closed models because of transferability.
(31:11) So next, I'm going to tell you a little bit more about one particular project that we've been doing with my student, Sally, Ahmed, and Rohith. And this is a project that definitely uses open-weight models. But not only that, it exists because of open-weight models. And I'll explain what that means. So if you go to Hugging Face, you'll see that there are a ton of models out there.
(31:39) And some of these models are fine tuned on the same foundation model. And some of them are not. Or at least it's claimed that they're not. And so in general, you have sort of a zoo of models. And you want to understand what is going on. What's the relationship between the models? Because often, documentation is poor.
(32:02) And people might be trying to mislead other people and so on. So concretely, what this boils down to is a model independence testing problem. You're given two sets of weights. And you're asked. Were these independently trained or not, full stop? In particular, if they're not, it could be that one was fine tuned from another.
(32:25) The theta 2 was fine tuned from theta 1. Or they're both fine tuned from a common model. We don't distinguish those cases. But what we're distinguishing is between just full independence and not independent. So I think this is very clean. So how do you solve this problem? And a first idea is that you might just take the weight and just compare them.
(32:49) Compare some cosine similarity or some sort of similarity metric. And for MLP weights, this means just taking the rows and computing cosine similarity and averaging. So this gives you a number. But the problem with this is that, well, if you get 0.1, does that mean they're independent or not? I don't know. Are there any guarantees? It's not clear where you set the threshold.
(33:20) And does this threshold depend on the model architecture? It's not very rigorous. But this is the kernel of something we're going to turn into actual rigorous test. In order to do that, I'm going to introduce a thought experiment. Suppose you knew how theta 1 was created. It was created through this train function, given some random initialization of the parameters.
(33:46) So what I'm going to do now is I'm going to just retrain a bunch of copies of theta 1. In practice, we're not going to do this thought experiment. And the idea is that if theta 1 and theta 2 were truly independent, then this similarity metric should look basically the similarity metric of another random copy.
(34:13) But if these were somehow very correlated, then this would look very different. So based on this insight, you can construct an actual p-value for the hypothesis test of whether theta 2 and theta 1 are independent by simply looking at the fraction of times that this independent statistic is actually more extreme than the statistic that you actually observe with theta 1 and theta 2.
(34:41) So this, in essence, turns a statistic into a threshold that gives you a formal guarantee. So now, the problem is that, well, you don't know how theta 1 came about because you only given the weights. You can't actually execute this. And furthermore, even if you knew, you probably wouldn't want to spend millions of dollars training this model over and again just to do this test.
(35:09) But we can do something here. And the goal is to fake it. So we're not actually going to train the model. But we're going to pretend we did. And the idea is leveraging the sort of natural symmetries of the model. And so we're going to define perm of theta as you're going to permit the hidden units defined by theta, so like this.
(35:33) So we take a matrix and permute the rows. And the idea is that when you permute it, that's kind of like a counterfactual. And that's a model that could have arisen. Now, we didn't actually train from scratch to get it. But because of symmetries, I could have started with a different initialization and also gotten to that model.
(35:54) So turns out, you can actually compute a p-value using this statistic as well. And this is actually a very efficient algorithm. So in order to validate this approach, we looked at the subset of Hugging Face models where we understood what ground truth was-- so in particular, models around Llama 2 and their replications.
(36:21) And we looked at running this test on models that were truly independent. And we see that the p-values are all over the place, which is what you want. p-values are supposed to be uniformly distributed if under the null. And if they are not independent, then the p-values are actually minuscule. So you ask about statistical significance.
(36:44) Well, this is kind of blowing out of the water. This is like actually underflowing significant to the point where it's underflowing. So this is a very strong test. And it's sort of a smoking gun that these were actually not independent, which is true because we know that these models are fine tunings of Llama 2.
(37:07) So more fun things we can do. So earlier this year, there was a controversy around this mysterious model, Miqu, that appeared on the internet. And people suspected it was a leak that was Mistral fine tuning Llama 2. And eventually, the Mistral CEO said, yes, it was a leaked model. And indeed, we were able to verify with our tests that indeed these two models are not independent of each other.
(37:41) We also were able to show that StripedHyena, which is a model, was derived from Mistral. And then finally, Llama 3.2, their 3P, in their blog post. They say that this is derived from Llama 3.1-8B. And we also verify that indeed these are not independent. But moreover, what's, I think, really interesting is that our test not only gives you the single bit of information whether two models are independent or not but actually tells you which layers were derived from which layer.
(38:15) So we were able to show that certain layers of Llama 3-8B were used to create the 3B model. So this is information that actually wasn't included in the actual blog post, which is kind of a cool bit of reverse engineering there. So some reflections on open-weight models. So it's very clear that open-weight models, such as Llama 3, have been immensely valuable for research.
(38:42) I only showed you a small fraction of all the papers that have been using Llama 3. And I'm sure some of you are using it in your own research as well. So enables research on interpretability, different fine tuning techniques, distillation, model merging. And furthermore, you get all these nice, reproducible guarantees and not having to worry about model deprecation.
(39:03) One, I think, interesting thing about open-weight models is that you're making these very sort of arbitrary changes to the weights by averaging them, by taking out layers. And they're sort of crude in a way. And the question is, how do these on Earth yield any sort of functional changes that are coherent? And the answer is that there's no guarantee of this.
(39:29) And in general, you actually have to go back and fine tune your model and heal it after you sort of rip out like half of its layers. Open-weight models also teaches us about API models. And it also opens up new types of problems-- for example, this model independence testing problem. We wouldn't have worked on this problem if it weren't for open-weight models at all.
(39:57) But at the end of the day, we're still confined by the blueprint of existing models. So all these models are still transformer architectures. They're still the particular set of weights that Meta has given us. And so in order to go deeper to open source, we need to open up the hood more. So fortunately, there's a lot of open source language model efforts from Luther to Ai2, Hugging Face, and so on.
(40:31) And each of these is trying to create open source models. The downside is at this present moment, the open source models are far weaker than the strongest open-weight or API models. But what exactly is open source? So a little bit of historical context. Open source clearly comes from the term open source software.
(41:02) And open source software and free software have had a long history in computer science, stemming from the roots-- and MIT in the '50s, the hacker culture, and its idea in academia, where you do open science, these are the roots that inspire open source and has coalesced into these values of creativity, exploration, transparency, collaboration, resistance against authority.
(41:30) So this sort of defines what open source sort of stands for. And of course, there's famous figures like Richard Stallman and Linus Torvalds that created this whole ecosystem around GNU Linux. And that led to the Open Source Initiative founding in '98, which finally defined the term open source. And so open source was practiced long before the term open source software even was defined.
(41:58) And now, OSI is now trying to define what open source means for AI. And it can be summarized in terms of four freedoms-- the ability to use a system without asking for permission, the ability to study, the ability to modify, and the ability to share. And in more detail, open source definition requires that the model parameters be available-- so this is open weights but without restrictions-- that the code be available.
(42:27) And here, the code importantly means not just the inference code but the training code and also the data processing and filtering code because the data is actually a vital ingredient in determining the behavior of these models. And without the knowledge of how the data was constructed, you can't really be truly open source.
(42:49) And finally, there's data information. And so you might ask a question, why data information and not data? And the answer is that you don't want to get sued. And basically, the longer version is that most foundation models are trained on web data. And web data is anything goes. Most of it's copyrighted. Or at least it's not clear that it's not copyrighted.
(43:20) And so model developers don't actually own the license to redistribute the data. So there are some ongoing lawsuits that are going to settle, hopefully next year or something, that are going to rule on whether training on this data is fair use. But even if training is fair use, you still can't redistribute the copies of someone else's book kind of verbatim.
(43:48) So another piece that I think is critical is the ability to modify. And I would say that compute is an important ingredient here. So this is not technically part of the open source definition. Nowhere does it say you need a compute. But in order to achieve the spirit of being able to modify a system, it's clear that you need compute.
(44:11) Otherwise, you have this thing. And you can't actually-- if you want to change the data, well, you need to retrain at least or at least do continue pre-training or something. So I'm going to talk about three projects that we've done that really require deep access to the kind of full training process and the weights and the data and so on.
(44:37) So the first paper-- I'm just going to go through these very briefly. So they're not going to be too much detail here. The question is, what mixture over data sources do you use? So language models are typically trained over some combination of Wikipedia books, web code, and so on. And usually, people just set the weights sort of magically.
(44:59) 0.2 books, 0.4 code-- looks good to me based on vibes. But we created a procedure based on distribution, robust optimization, which automatically tunes these mixtures. And we show that this performs 2.6 times faster-- trains a model 2.6 times faster than manually specified weights. So in order to do this research, you clearly need to be able to train models from scratch.
(45:29) A second project we did was on developing a new optimizer based on second order information and particularly the look at the diagonal Hessian with some clipping. And this provides a 2x speedup over Atom. And again, of course, in order to do this research, you need to be able to train from scratch to show that you get improvements over some-- Atom.
(45:55) And the final project we did was on [INAUDIBLE] another architecture, an alternative to a transformer that is not as monolithic and is more interpretable. So it's called the Backpack language model. And without going into any detail, this model architecture allows for precise model editing. So you can take a language model.
(46:18) And you can, with a test time intervention of some hidden units, get the model added into a world where it believes that the MacBook was created by HP rather than Apple. So all this is nice and well. So we touched the learning algorithm, the architecture, and the data recipe. Now, I often get this question.
(46:43) Would these results hold if we scaled up? And really, what this question is asking is, how do you get the compute to even answer that question? And so for this, I think this is where open source, I think, is really at-- is really kind of bottlenecked. And so I'll give you three suggestions on how we might deal with the compute problem.
(47:08) The first is that you use scaling laws. So typically, you think about scaling laws as telling you about bigger and bigger models. But one thing that I think scaling laws could be made to do-- but certainly, I don't think they do this very well-- is try to extend in the opposite direction into a smaller regime where you can operate in a smaller scale model with a belief or the hope that whatever you do down here-- different data, different architecture-- is actually going to scale up to larger scales.
(47:44) And this is a way that I think you can work at small scale but with a mindset of scaling up. Second is there are actually a lot of idle GPUs everywhere. So most of the time, people always complain. Not enough compute, not enough compute. But there are pockets of times where there is idle compute. And so if you're only able to harness all of this globally, there's probably quite a lot of compute available to do certain things.
(48:15) And one technical hurdle to this is that the interconnect speed, when you're looking at decentralized compute, is just horrible. It's at least two orders of magnitude worse than it is in the data center. And training large foundation models requires fast interconnects. Two years ago, we had this paper which showed that with some amount of clever scheduling and some sort of low level systems work, you can actually train 1 billion parameter models at the time in a way that's only two times slower than if you were doing it in a data
(48:51) center, even though the interconnects were 100 times slower. So this is promising. And since then, there's been a lot of work, including that DiLoCo DeepMind, showing a better an algorithm to do this. And there's been even this effort from Prime Intellect, where there is a decentralized training effort happening of a $10 billion parameter model, where anyone can just pull compute into this massive pool.
(49:22) So I think this is really exciting to see how far you can get with decentralized compute. So track number 3 is basically ask for more money. And so there is the NAIRR, the National AI Research Resource, which has a pilot that is trying to get more compute to fund public academic AI research. And hopefully-- well, the pilot think is ending.
(49:58) But hopefully, this will be renewed and hopefully, we'll be able to see more of this. It is worth noting that other fields kind of have figured this out. And by it, I mean being able to harness an advocate for the funding of massive infrastructure projects-- for example, the Human Genome Project, James Webb telescope, the LHC.
(50:25) These are multi-billion dollar projects that are in service of open academic research, not just for commercial gain but for academic research. And the idea is that the types of problems that you would solve if you were structured in this public domain is different than what the OpenAI and the Anthropics would do.
(50:51) So just to conclude, we've talked about the three different levels of access. I think-- underlying theme here that I want to stress is that access shapes the research. So if you have API access, there are a lot of interesting things you do. And we've been doing a lot of these things, building all sorts of different agents.
(51:16) With open-weight models, you can do a lot more. You can fine tune. You can do distillation. And you can do a lot of interesting things with open-weight models as well. But I think it's important to recognize that our research life more and more today are living within the confines of the APIs and fixed weights.
(51:36) And in order to make, I think, fundamental progress, we need to question more things. We need to real question the data, the model architecture, the training algorithm in order to make more progress and not be stuck in this local optimum. And the eventual goal is to really understand the mapping between data and model architecture to model behavior.
(52:02) And this is hard even if you have full access. In the 2010s, we were complaining about how hard deep learning was to understand. And now, things are a lot worse when you only have API access. There's no hope. So at least if we can get open source access, we have some hope of answering these sort of deep fundamental questions about AI.
(52:24) And of course, compute is a big challenge here. I think there's still a lot of things you can do on small scales. I think you can do things at small scale but with a mindset of scale. And also, I think it's really interesting to think about how we can do decentralized pooling of compute so we can do more together than we can do individually.
(52:46) OK. With that, I'll conclude and take any questions. Thanks.
