KAIYU YANG: And, hey, it's great to see you here. I'm Kaiyu, and I will talk about "Formal Reasoning Meets LLMs: Towards AI for Mathematics and Verification."

OK, so we are now kind of in an arms race of large language models in which every leading company releases a new model every few months. At the center of this arms race is math and coding. For example, when OpenAI introduces o1, you typically see charts showing its capability in various benchmarks. You see the math competition like AIME and the coding competition like Codeforces are usually highlighted. Similarly, when Google released Gemini and when xAI released Grok, they also highlight the capability in math and coding.

In November 2023, XTX Markets, which is a British hedge fund, launched the AI/MO prize. That's $10 million for AI that can win a gold medal in IMO. Not long after that, in July 2024, Google DeepMind announced that AlphaProof and AlphaGeometry have achieved the silver medal level. I guess that's the content of the last lecture.

If you want to go beyond high school competition math, we have this Epoch AI. It introduced a benchmark called FrontierMath, which includes research-level mathematical questions curated by professional mathematicians. Not long after that, OpenAI o3 announced that it can solve more than 20% of that, which came as a surprise because when the FrontierMath benchmark was released, people thought it would take a few years for language models to make real progress. But really, after less than one year, we have groundbreaking results from OpenAI.

So why are people interested in math and coding? Why are they often highlighted? One reason is that math and coding are proxies for complex reasoning and planning. Reasoning and planning are not only important for human intelligence, but they are also one of the major challenges for current large language models. They can really unlock unlimited applications if you can make language models able to reason and plan. For example, you can have them do travel planning for you and also have language model agents scheduling calendar invites for you.

The second reason that people are interested in math and coding is they are relatively easy to evaluate. For a math problem, you can check if the answer is correct. For coding, you can run the unit test to see if they can pass all the tests. But if you ask your language model to write a crime fiction, or to compose a symphony, it's going to be much, much harder to evaluate. You can argue that crime fiction also involves reasoning and composing a symphony definitely needs planning. So they also need reasoning and planning, but they are just not as easy to evaluate.

I put it as "relatively easy" here because, as we will come back to, we don't think checking the answers or running the unit test is a perfect evaluation. We are just arguing that it's a reasonable evaluation, and we will revisit this point later in the rest of this lecture.

OK, so we know language models are racing to be good at math and coding, and next we want to briefly introduce the main techniques behind them. How do we train language models to solve math problems? Here we focus on math, but the discussion largely also applies to coding as well. We will see later that math and coding are deeply connected.

State-of-the-art math language models are trained using mainly two techniques. One is supervised fine-tuning, or SFT, and the other is reinforcement learning, or RL. We're going to present a very high-level view and emphasize the need for data and for verifiability.

Starting with SFT: In SFT, you have a base model that's a generic language model pre-trained on internet-scale datasets. To learn mathematics, you give it additional data that's focused on mathematics. You can grab web pages from MathOverflow or papers from arXiv. The result of this fine-tuning is called the base math language model. It has seen a lot of mathematics, but it still hasn't yet learned how to solve problems. The next step is to give it a lot of problems with detailed step-by-step solutions. Optionally, in the solution, you can use external tools. Here the model is trained to produce not only natural language reasoning, but also to call external tools like Python. This is an example of a problem it can solve.

The most important thing in this pipeline, as you can see, is the dataset. You need a dataset for continuous pre-training—that's the web documents collected from math forums like Stack Overflow. You also have to annotate a lot of data for further fine-tuning, consisting of problems with step-by-step solutions. Collecting this dataset is one of the most expensive parts in this pipeline, because it cannot be fully automated. You need humans to annotate the data, clean the data, and transform the data into a dataset you can use.

What if our dataset has only the final answer, but not the intermediate steps? This is a scenario where you don't want humans to annotate data very expensively, but maybe you can more cheaply get data that has only the final answer. Can we still use this dataset to train the model? The answer is yes. We can use reinforcement learning to train the model to generate solutions with only the problem and the final answer.

Here's how it works: You have the problem and you give it to the language model. The model generates a solution that has intermediate steps and also the final solution. You can check if the final solution is correct by comparing it with the ground truth. Here, it's a correct solution. The model can also generate an incorrect solution, and you can check. The result of this verification can be used as a reward signal in reinforcement learning, and you can use algorithms like GRPO to optimize the model to achieve high rewards. You can treat the reward for a correct solution as 1, and if it's an incorrect solution, the reward is 0.

This approach is a very simple and powerful idea. The idea has been there for several decades, but it was first shown to work for training reasoning language models by DeepSeek-R1, fairly recently. This idea is getting a lot of momentum. Just to see how much momentum it has: just one month ago, two pioneers in reinforcement learning, Richard Sutton and Andrew Barto, won the ACM Turing Award.

In reinforcement learning, the key is really verifiability. You have to be able to verify the results. You have to be able to tell if the solution is correct or not. Otherwise, you don't have the reward. If the solution has a final answer that's a number, if it has a numerical solution, you can always check by comparing with the ground truth. But what if it's a theorem-proving problem? You are asked to generate a proof, but there's really no easy way to tell if the proof is correct or not. Can we still do reinforcement learning? We will revisit this point in the later part of this talk.

Here we have covered the main techniques to train language models for math: supervised learning and reinforcement learning. I would say any state-of-the-art math language model is kind of a combination of a very strong base model, these two techniques for post-training, and maybe more importantly, marvelous engineering efforts. The algorithms are very straightforward. Pretty much every leading industry lab is using the same algorithm. But what really matters is who has the better infrastructure, better engineering, and better dataset.

So then we will ask: Will AI soon solve mathematics since we have this very powerful recipe? The answer is definitely no. We have a few gaps.

The first gap is: How do we go from pre-college math to advanced math? Existing successes are, as we see, mostly limited to pre-college math like IMO or AIME. If you give language models more advanced mathematical problems, they still struggle. This quote is from Terry Tao, one of the most prominent mathematicians nowadays. He said he played with OpenAI o1 and finds they still struggle with the most advanced research mathematical tasks. This is kind of expected because mathematical research is by definition novel. Since it's novel, you don't have a lot of data to train language models. Also, o3 results on FrontierMath, as we mentioned before, are on advanced mathematical problems, but it comes with a few caveats.

The first caveat is that there are financial ties between Epoch AI and OpenAI that were later disclosed. But more importantly, there's a difference between FrontierMath and real mathematical research. The problems in FrontierMath are designed to have numerical solutions, so they are advanced, but they are designed to have a numerical solution. This is kind of counterintuitive because we know most problems in advanced math don't have numerical solutions, but they designed the benchmark in this way in order for the models to be evaluated.

This is related to the second gap. Current models are very good at guessing the answer, but how do we get them to write proofs? A recent study has found language models still struggle in writing even very basic proofs. PutnamBench—Putnam is the mathematical competition for undergrads. People recently tried using OpenAI's reasoning model on Putnam, and here's the result: "I'm very unimpressed by ChatGPT in the recent Putnam exam, and most answers are worth one or two points out of 10 points." This is checked by a human mathematician.

Recently, in another paper, researchers evaluated language models' proofs for the USAMO exam. They showed that all the models scored around 5%, which is pretty low. The errors those language models are making can be pretty easy to spot once you point them out, but if it's in a long proof, it's very hard to detect them.

Here's an example: We are asking the model to prove a very simple inequality. If ABC satisfies some condition, then there should be some result. Here it's making an error. When it tries to multiply an existing inequality by a positive number, it should preserve the direction of the inequality, but it flipped the direction. That's a very simple error if you point it out.

We argue that current language models, the current approach for training language models, is not sufficient for solving these problems because it relies heavily on data and on verifiability. When you work with advanced mathematics, as we said, you don't have enough data. The data is very scarce. When you work with proofs, you don't have verifiability. In advanced mathematics, most problems require proofs. If language models are not enough, what else do we need?

In a recent position paper we released on arXiv, we argued that one important missing point is formal reasoning. Here, formal reasoning means you ground mathematical reasoning in formal systems. Formal systems can include first-order logic, higher-order logic, dependent type theory, or even computer programs and their semantics. The role of the formal system here is to try to verify the proofs and provide automatic feedback on the proofs. This is very useful because the feedback it provides can mitigate data scarcity. You can have the model learn from automatic feedback, and you don't need to collect as much human-created data. Second, now you can do verification, which enables you to evaluate the model rigorously. Therefore, we argue that to move forward, we really need to integrate formal reasoning into the current approach.

We're going to very briefly introduce Lean, which is the kind of formal system we focus on in this talk. It's also used in AlphaProof, so I guess people already have some familiarity with Lean.

Here is a program in Lean. In this program, you first define natural numbers. A natural number is either 0 or a successor of another natural number. Then you define addition between two natural numbers—this is just a recursive function. Then you can state and prove some theorems about natural numbers and addition. For example, you can prove a + b = b + a. This is only one file in Lean. If you are using Lean to write formal mathematics, it's similar to using Python or another programming language to develop software. You have one file, and this file has the proof, and the proof has semantics. It corresponds to a proof tree. At the root of this tree is the original theorem you want to prove. Every step in this proof decomposes the proof goal into several simpler subgoals, and you can do that recursively until all the subgoals are solved.

This is one file, and files are organized into larger units, like a project can have many files. The project can be open-sourced on GitHub. For example, in your project, you can define real numbers. Someone else's project can build on your definitions of real numbers, and they can formalize, for example, optimization theory. This is similar to how you develop software.

One example of using AI with Lean is AlphaProof. That's the content of the last lecture, and it's a very successful example. It uses Lean to generate a lot of theorems and a lot of proofs, verify those proofs, and do search and reinforcement learning to learn the model.

In the rest of this lecture, we will be talking about mainly two tasks at the center of using Lean and AI. First is theorem proving. In theorem proving, your input is a theorem statement and you want the model to generate a proof. Here, both the statement and the proof are written formally in Lean, and Lean can check if the proof is correct or not. There's no room for the model to hallucinate. The other task is autoformalization. In autoformalization, you have the informal mathematics, for example from a textbook, and you want to use your model to translate it into formal mathematics.

Next, we will be first focusing on theorem proving. A very straightforward way to do theorem proving is you just give the model what you want to prove, and you ask the model to generate a step in the proof or you can ask it to generate the entire proof. Because if you can generate the next step, you can use search to chain steps together into a complete proof.

The central problem here is: How do you generate the next step? A very straightforward solution is: OK, we have human-written formal proofs, and we can train a model to do supervised learning on those data so that the model can learn how to generate the next step. This idea has actually been here for a while. Earlier work used classical machine learning algorithms like k-nearest neighbors. Around 2019, people started to use deep neural networks for this task. Even later, people started to use large language models. Until the use of large language models, we really see a boost in performance because the model can acquire a lot of mathematical knowledge during pre-training and use that knowledge in theorem proving.

We will be mainly talking about this paper called LeanDojo, which was published in NeurIPS 2023. In this paper, we provide open-source datasets, tools, and models for theorem proving in Lean. Compared to previous approaches, the main difference is first, we are completely open. Previous approaches are private and they don't release the dataset and models and were done in private labs. Second, we propose a new model to generate the next step in the proof.

Here is an overview. In Lean, we can extract data from human-written Lean code. We extract about 10,000 theorems and proofs. For each proof, we have the steps in the proof. We also have the lemmas used in the proof and the definitions of these lemmas. After extracting this dataset, we use it to train a machine learning model. Once the model is trained, it can interact with Lean to prove new theorems.

Here is the model we introduced for this task. It's called Retrieval-Augmented Prover or ReProver. The motivation is that when you prove a theorem, you almost never start from scratch. You use existing lemmas. In order to use the existing lemmas, the model has to know what lemmas are there and try to decide whether a lemma is useful. Given a proof state—that's the goal you want to prove—and a library of lemmas, first do retrieval. In this phase, we encode everything into vectors. The state is now a vector and each lemma becomes a vector. We compute the similarity between the state and each lemma, and we use the result to retrieve the top lemmas. After the lemmas are retrieved, we combine the lemmas with the state and use another language model to generate the next step.

The model is very simple, but the really important point is how do you get the dataset? As we said, in LeanDojo you can extract the dataset from Lean projects like MathLib and other Lean projects.

Here is a summary of a typical neural theorem prover. On the left, you see a tree. That's the tree during proof search. You have an initial state and your model can generate multiple potential next steps. You can run those steps, which gives you the next proof state. You can do that repeatedly until you find a proof. For this part, you can use any search algorithm like DFS or Monte Carlo tree search. The second part is, how do you generate the next step? On the right, you see at each node in this proof search tree, you have the current state. You give the current state to the language model and it generates suggestions for you. Optionally, the language model can take a formal math library, which is the library of lemmas you can use.

After the LeanDojo work, we've seen a bunch of work trying to improve on that. For example, one thing you could do is what we call expert iteration. You have a prover—even if it's not very good, you can use it to attempt to prove some new theorems. In this process, you will get some new proofs. Once you get the new proofs, you can add them into your training set. Now you have a better training set, and you can use it to train a better prover. You can iterate. In this way, we show that if you run for more iterations, the performance usually improves until you hit some saturation point.

We've seen a very simple way to build a theorem-proving model. You just extract human-written proofs, and you train a language model to generate the next step in the proof. But we would say it's a great step, but it comes with limitations.

The first limitation is still data. Language models work well only if you have a lot of training data, and the amount of human-written proofs on the internet is very limited compared to, say, Python code. Another limitation is inherent to the task of theorem proving. If you think about other tasks that AI has been successful at, like playing Go, what's the difference between playing Go and proving a theorem? The main difference is that in Go you have a 19 by 19 board, so the possible actions you can take are constrained by the size of the board—it's finite. But in mathematics, arguably your action space is infinite. There is an infinite number of things you could potentially do when proving a theorem. Since the action space is infinite, it's really hard to cover the entire space if you only learn from human-created datasets. Even if you use reinforcement learning, it's very hard to explore the space efficiently during reinforcement learning.

That's a good question. In the work we have talked about up to now, we are just using supervised learning, so there's no curriculum involved. But if you use reinforcement learning, it's natural to have the model first learn from relatively simple examples and then try to generalize. If you look at more recent work in using reinforcement learning for theorem proving, one major difficulty is how do you choose examples to present to the model? You want the examples to be not too easy, but also not too hard.

How do we solve the limitation regarding the action space? In a more recent work, we tried to take a step in that direction, but we chose a very limited domain as a playground. We prove inequalities in Math Olympiad. Inequalities in Math Olympiad are like: you have a few real numbers like ABC, and they satisfy some precondition. You want to prove a postcondition like this. We choose this domain because the proofs in this domain are highly regular.

Here is what humans would do. Humans would typically do two things. One is you can apply an existing lemma like AM-GM inequality or Cauchy-Schwarz inequality. The other thing people typically do is to rewrite the current formula into a form so that an existing lemma becomes applicable. But even with these two kinds of actions, the action space is still very big. For example, for proving this particular inequality, you can apply AM-GM, and AM-GM can be applied in more than one way, so you have a very big branching factor. You can also apply other inequalities like the T2 inequality, and you can also potentially choose to rewrite the formula into this form. Here the actions you can take are essentially infinite.

How do we navigate this kind of infinite action space? One thing we do before trying to solve the problem is: OK, let's try to see how o1 can solve this problem. We give a lot of inequality problems to o1 and we manually check their proofs. As a result, we find a lot of errors in the o1-produced proofs. We checked 20 examples. o1 preview proved none of those examples. The more powerful o3 and also the DeepSeek-R1 also only proved 3 or 4 out of 20 examples. Whereas a typical gold medalist, like a human gold medalist, can prove at least 15.

Let's come to our method. As we said, we categorize the steps you can take in this particular problem into two kinds. First, we call it scaling. Scaling is you apply an existing lemma like Cauchy-Schwarz to a part of the current formula. In this example, you find that the left part of the formula can be used for Cauchy-Schwarz, which gives you the result on the right-hand side. What's special about scaling is the number of scalings you can do is finite. This is because you only have a finite number of lemmas, and each lemma can be applied in only a finite number of ways. It's actually possible to enumerate all the possible scalings in this space.

Besides scaling, another category is called rewriting. Rewriting is you are given a formula and you want to transform the current formula into another form. Rewriting is very different from scaling in the sense that the space is infinite. Given any formula, there are an infinite number of ways you can rewrite it. The key insight here is that after we make this categorization, our algorithm can enumerate all the possible scalings using a symbolic algorithm. Whereas the rewriting, which selects the infinite space, can be handled by large language models. We combine the next step generated by the symbolic algorithm with language models, and we use some heuristics to filter the results to reduce the number of goals you have to improve search.

As a result, we solved many more problems compared to DeepSeek-R1 and also o3. Here on the top you see R1 proved 4 theorems. Humans can prove 15, whereas our system called LIPS can prove 16 theorems. At the bottom, we evaluate our system with a few benchmarks and compare with existing methods.

Here's one interesting example: a proof discovered by our system. This is interesting because it's from Evan Chen, who is IMO coach for Team USA. He has a problem set, and he said, "This problem has no hope to be solved by AM-GM." But we find that the proof discovered by our system uses only AM-GM combined with clever rewriting. So it can actually find proofs that humans do not know how to find.

Here is the takeaway: The challenge in theorem proving is how do you efficiently explore a potentially infinite action space? While this is a very hard problem, if you are working on a specific domain of mathematics, you can use domain insight to structure the action space so that it becomes more feasible. But it is an open problem whether you can generalize this approach to be domain-general.

After talking about theorem proving, we will go to autoformalization. Feel free to bring up any questions any time.

In autoformalization, we have informal math and we have formal math. We want the model to be able to build a bridge between them, translating from informal to formal. Actually, we have two slightly different subproblems. First is how do we autoformalize the theorem statement? Second is, how do we autoformalize the proofs? In statement autoformalization, the model only takes a statement and produces the statement in Lean. In proof autoformalization, the model has the informal statement, the informal proof, and also the formal statement, and it only produces the formal proof. These two subproblems have different challenges.

Let's talk about statement formalization first. For formalizing the statement, it's really hard to do the evaluation. For example, here the informal statement says there exists an infinite number of primes. The formal statement says, for any natural number n, there exists a p that's greater than or equal to n, p is prime. This is obviously a correct translation of the informal statement because it implies the infinity. But the problem is it's not the only correct solution. If we produce this solution, we change p greater than or equal to n to be strictly greater than n, while essentially it's the same. We can even make it more different. We can make n also a prime. For any human, you can easily see these three statements are slightly different, but they are essentially the same thing.

But this creates a problem for machine learning. If the machine learning model produces one of them, how do you know if it's equivalent to the ground truth? You may want to say, "OK, can I check the logical equivalence?" But the reality is this kind of checking the equivalence between two theorems is computationally infeasible in general. In practice, researchers have resorted to heuristics or human evaluation. If you rely on human evaluation, it's going to be very expensive and not reproducible. If you resort to proxy metrics or heuristics, it's not going to be accurate. That's the challenge for evaluating the autoformalized statement.

How about proofs? Proofs have another challenge. Human-written proofs, even if they are very rigorous, are not rigorous enough to be formalized. They often have holes. One kind of hole is mathematicians often say, "OK, this case is left to the reader." If it's left to the reader, your model has no way to figure it out from the text. It has to figure it out on its own. There are more subtle holes in the proofs that we will cover later. For the model to be able to autoformalize the proof, it has to fill in all those kinds of gaps, which can be very difficult.

We've covered two key challenges in autoformalization. For statements, we don't have a reliable automatic evaluation. For proofs, the model has to fill in all the reasoning gaps. In this project, we want to address both problems. That sounds very intractable. But a lesson we learned in this project is that something intractable in general can be made tractable if you restrict to a very specific domain. The domain we choose is Euclidean geometry.

Euclidean geometry is one of the oldest branches of mathematics, and it has been used to test human intelligence for more than 2000 years. Recently, it has become of interest to the AI community because of the AlphaGeometry work, where they have an AI that can solve IMO-level geometry. In this work, we show that Euclidean geometry is not only a good domain for theorem proving, it's also a good domain for formalization. More concretely, we collect a benchmark called LeanEuclid in Euclidean geometry. It consists of 48 problems from Euclid's Elements Book I and more than 100 problems from UniGeo, which is an existing dataset.

Let's look at one example in our dataset. This is directly from Euclid's Elements. It's proposition one from Book I. Basically, you have two points, a and b, and you want to show that it's possible to construct an equilateral triangle. For each problem, you have the diagram, the informal theorem statement, and also the informal proof. When constructing the benchmark, we try to manually formalize this problem in Lean. On the right-hand side is our formalization in Lean. As you can see, it corresponds very closely to the informal version. The first step is you draw a circle BCD that's centered at A and B should be on the circle. The second step is you draw another circle, and you try to find the intersection and connect everything. Then that's an equilateral triangle. There are only 5 or 6 steps in the formal proof, and each step corresponds perfectly to what's being done in the informal proof.

We did this for all the 48 theorems and proofs in Euclid's Elements, Book I. This is aesthetically very pleasing for me, because to the best of our knowledge, we are actually the first to formalize Euclid's Elements in a proof assistant like Lean. We want to keep the proofs very faithful in the sense that there is a direct correspondence between formal and informal. As we will see later, this is not always trivial.

During this process, we find a few very interesting errors in Euclid's Elements. I think we can probably go over one of those errors, just to see how formalization can help you check mathematical proofs rigorously and uncover bugs in a proof that you may think is already very rigorous.

This is proposition 24 from Euclid's Book I. It says if you have two triangles, and the two triangles have two pairs of edges equal to each other—AC equals DF and AB equals DE, so blue equals blue, red equals red—and the angle A is bigger than angle D, you want to show that BC is greater than EF. This doesn't look very difficult, and I'm going to show the original proof by Euclid.

First, you try to move the triangle from the left to overlap with the triangle on the right, so C will become G. Then you try to connect F and G. Now the problem becomes proving EF greater than EG. This is easier because EF and EG are now in the same triangle. According to a previously proved theorem in the same book, in the same triangle, a bigger angle corresponds to a bigger edge. So you only have to prove the relationship between these two angles. You have to prove the big angle here is indeed bigger than the small angle. How? Just notice that we have an isosceles triangle here. We have DGF. DG equals DF. By our previously proved lemma, in an isosceles triangle, you have two angles equal to each other. Then you can argue that the bigger angle is bigger than the DFG because it's a superset of the BFG. The DFG is equal to DGF. The DGF is bigger than the smaller angle. Then you have proved the bigger angle is bigger than the small angle.

OK, so this is very trivial, but the proof is actually wrong. Anyone knows why? No? OK. Technically it's not wrong for this diagram, but what if they look like this? Let me try to replay the proof. First, move the triangle from the left to the right. Second, connect G and F. Now, we want to prove EF greater than EG, and we want to use this isosceles triangle's alpha. These alphas are equal. We want to prove x greater than y using alpha, which does not work directly. In the previous diagram, alpha is a part of x. But now alpha is outside x and y, so you cannot use it as an angle in the middle anymore. The original argument breaks in this diagram.

How do we fix this proof? Luckily, it's fixable. Otherwise, we would probably publish a bigger paper. First, notice that the angle DGE, that's alpha plus y, is the inner angle in a triangle, so it's smaller than pi. That's trivial. Second, 2pi minus alpha minus x, that's DFE, is also smaller than pi. If you add these inequalities together, alpha will cancel out and you get x greater than y. So it can be fixed, but as you see, this proof is quite different from Euclid's proof. That's why we think Euclid made an error here. Or technically it's not an error; it's like this proof has two cases. These two cases are slightly different, and one case is more challenging than the other. But Euclid omitted the more challenging case and focused on the easy case.

Maybe that's a very interesting digression. That's not related to AI, but it shows how AI and Lean can make things more rigorous. Let's go back to AI.

In our benchmark, the first good thing about it is it enables you to check whether the autoformalized theorem statement is correct. Here is how we check if the statement is correct. We have a ground truth, and we want to check if it's logically equivalent to the model-generated statement. Given two theorems, T1 and T2, we check it by proving T1 implies T2 and T2 also implies T1. Here we use an automated reasoning tool called an SMT solver. If you remember, I said that this is infeasible, but how does it become feasible now? It turns out now it's feasible because the theorems in Euclidean geometry take a particular form. They are not general theorems. Problems intractable in general can be made tractable in a specific domain. That's how we check the theorem statement.

Another problem is autoformalizing the proofs. In Euclidean geometry, we show that there is a well-known kind of gap in the proofs. We call these diagrammatic reasoning gaps. I'm going to show that using an example.

Proposition one from Euclid's Elements, Book I: Given two points, A and B, you can construct an equilateral triangle. Here is Euclid's proof. Draw a circle centered at A and B is on the circle. Draw another circle centered at B, A is on the circle. Take the intersection, C, and connect everything. You get an equilateral triangle. Anyone disagree with this proof? No? OK. The problem is, did we prove C actually exists? Did we prove these two circles actually have an intersection? We didn't. Actually, if you try to prove it—because this is proposition one, so to prove this proposition, you can only use the axioms; there's no existing lemmas—and if you try to use Euclid's axioms to try to prove these two circles intersect, you cannot prove it, actually. It's not provable. But no one bothers to prove it because it's just so obvious in the diagram.

But it becomes a problem if I want to translate this proof in Lean. When I formalize the proof in Lean, Lean does not know what is the diagram or what is obvious in the diagram. So I have to formalize what is obvious in the diagram. I was in the Zoom in the last lecture, I believe someone asked a question about how do we formalize Euclidean geometry in Lean. I think this is very closely related to that question. To formalize Euclidean geometry in Lean, one of the most important things is how do you formalize the concept of being obvious in the diagram.

Luckily, this problem has actually been solved before. This is a work from Avigad, who is a professor in philosophy now at CMU. Avigad essentially identified a set of axioms that's not in Euclid's axiom system. You can think of it as an unspoken axiom or hidden axiom. What is being obvious in the diagram is just: if a fact is a consequence of those axioms, then it's obvious in the diagram. Now we have a computational meaning of obviousness in the diagram, and we can implement an automated reasoning algorithm to try to derive things that are obvious.

Let me put everything together. In this work, we have a benchmark. In the benchmark, we have a diagram, informal math, and a ground truth formal statement that's written by a human. Now, we use a model to do autoformalization. It generates a formal theorem, and we can check whether it's equivalent to the ground truth using SMT solvers to prove that they imply each other. After checking the statement equivalence, the model can also do proof autoformalization. Say it generates this proof and we give this proof to Lean. Lean will identify a set of holes in the proof. We give those holes to the SMT solver, which will tell us if the holes are obvious in the diagram. If everything works, then we conclude, OK, this is a correct formalization.

What our benchmark really does here is provide a protocol that you can use to rigorously evaluate autoformalization. After collecting this benchmark, we also did some very simple experiments using just GPT-4 and GPT-4 Vision. Here we don't train the model. We just give a few demonstrations to the model and ask it to autoformalize the theorem statement. Blue here is GPT-4 and green is GPT-4 V, which also takes the diagram as input.

Here are two takeaways. One is if you look at the x-axis, it's how many demonstrations you give to the model. If you give the model more demonstrations, it's going to do better. But even if you give 5 demonstrations or even more, the model gets only about 20%, which is pretty low, showing autoformalization is very challenging. If you compare green with blue, you see green is a little bit better, which shows if you give the diagram to the model, it has a stronger understanding of the geometry problem.

Here are some takeaways. We identified two challenges in autoformalization. For theorem statements, you don't know how to evaluate. For the proofs, there are too many holes in the human-written proofs. We address them by limiting to a specific domain. In this domain, we can build an equivalence checker for the statement, and we can build a tool that can fill in all the gaps. But still, the open problem is really how do you generalize across a broader set of mathematical domains?

So now we've covered both tasks, autoformalization and theorem proving. This is the end of this lecture, and I'm happy to take questions.
