SWARAT CHAUDHURI:

In this course, you have seen a lot of different kinds of uses of LLMs. Today, I'm going to emphasize the use of LLMs as a tool of scientific and mathematical discovery. In particular, there's going to be an emphasis on the ability of LLMs to discover abstract concepts and how this can accelerate these discovery processes.

OK, so let us dive in. Let us start with the problem of mathematical discovery. This is, of course, one of the grandest challenges known to humanity. Now, how does the process of mathematical discovery look? First, there is the process of modeling, where you are inspired by some sort of real-world phenomenon and then model it in terms of a variety of definitions and concepts. Once you have a set of definitions, you may want to conjecture various properties of these definitions. These are potentially candidate theorems and lemmas. Then there is the process of rigorous reasoning, where you are actually proving these statements. For human mathematicians, this process is intrinsically social. You are convincing each other about the rigor of your reasoning and the value of your conjectures. But this process continues in an iterative loop. You may see proofs and counterexamples, and these may influence your models as well as your conjectures. This process just goes on; that's the history of mathematics.

Now, the dream of the field of AI for math is to automate substantial pieces of this process, and as a result, accelerate this process even more. You want to be able to discover new kinds of math. This includes settling known conjectures but also potentially coming up with new conjectures. Then there is the setting of natural sciences, which I will also cover in this lecture. Here, unlike in math, you are engaging with the real world. There is a process of theory building, where you're coming up with hypotheses about the universe, and then you are using these hypotheses to design experiments and experimentally validate them. This experimental validation process creates new data, which leads to new kinds of theories, for example, by refinement of the existing hypothesis. Or, in some cases, you may want to come up with entirely new concepts and new predictions.

The ambition of the area of AI for science is to automate hypothesis generation and experiment design. The key ideas in the areas of AI for math and AI for science at this point are that, number one, search is a really powerful concept. We want to imagine these spaces of hypotheses, conjectures, and proofs and systematically search them. This is, of course, a very massive space. There are all these potential statements about various mathematical objects, and all these hypotheses that are possible in the universe. This space is clearly infinite. How do you explore this space in a tractable way? You use prior knowledge to prioritize directions of search. That's also how humans proceed. We don't just do discovery in a vacuum. We read existing papers, sometimes incrementally advance on them, and sometimes make a big leap. But that prior knowledge is always of value. We also learn from experience how and how not to do this sort of exploration. You find out that you made certain choices while building your theory, and then you find that this just didn't work. That's experience for you. You use this experience to potentially take completely new directions.

Another piece that has been very important to the history of math and science has been to discover abstract concepts and tools. For example, the idea of calculus really sped up the progress of science after it was invented. Similarly with algebra and all major branches of mathematics, which came from the need to abstract some sort of real-world process. The value of these abstractions is that they speed up both this process of exploration and also the learning process that I previously mentioned.

If you think of it, LLMs bring something to the table regarding each of these criteria. For example, if you are doing a search in a very large space of potential scientific hypotheses, an LLM has inside it a lot of knowledge about what sort of hypotheses have historically tended to work in science. This knowledge can potentially help you direct the search process. In this course, you have seen examples of LLM agents. In this class, you are going to see more of this. These are systems that contain inside them LLMs as modules, but they are also interacting with the world and learning from experience. This experience is gathered as data and then often stuck into the prompt. Sometimes you can even do reinforcement learning or other kinds of modification of the LLM's weights. The point is that there are all these established methods for doing learning combined with the use of language models, and that is something that we can exploit here as well.

The use of LLMs for abstraction is something that's not well explored yet. This is a really new idea, but I'm going to give you some evidence as to why there might be a lot of things going on here. There is certainly a lot more to be done about the use of LLMs in constructing new concepts and new tools. I think that LLM agents have tremendous, endless promise in mathematical and scientific discovery. The reason is that, as far as all of these ideas go, LLMs have a potential to contribute with regards to each.

This may all seem somewhat abstract, so I'm going to now jump into some concrete results. This lecture is divided into two pieces. In the first part, we are going to talk about AI for mathematical discovery. In the second part, we are going to talk about AI for scientific discovery. In each of these cases, I'm going to show you some LLM agents built using quite different algorithms. We'll see that these agents realize many of these ideas that we talked about in the previous slide.

OK, so let's talk about AI for math. Over the last few years, there's been tremendous interest in this field. Here's Terry Tao from 2023, talking about how AI from that year can already generate suggestive hints and help with the mathematical process. He predicted that by 2026, AI can become a trustworthy co-author in math research papers and in other fields as well. Now, 2026 is not yet here, but I would say that this prediction seems very plausible as of now.

In terms of concrete systems, o1, when it was introduced, one of the highlights was that it did really well in various competition mathematics problems. Here is a plot. It shows how o1 does compared to GPT-4o. As you see, accuracy in this particular competition, AIME 2024, has gone up very substantially. Last year, my colleagues at Google DeepMind built a system that achieved silver medal-level performance at the IMO. You've already heard a talk about that in this course, so I'm not going to go into much more detail about that in this talk, but you know that it's impressive, and it uses a lot of very impressive machinery.

One question that I think is important to ask is: is it all going to be done just with LLMs? In other words, do you need any sort of additional machinery that combines LLMs with, for example, actions? This is really the characteristic that distinguishes LLM agents from just basic LLMs. I would say that there are certainly still believers in the potential of just LLMs alone to do everything after being trained on language. But I am personally a little bit skeptical about that possibility.

That said, one must admit that over the last couple of years, just LLMs have come a long way. Here is the picture of how you train an LLM for these sorts of mathematical settings. You start with a pre-trained LLM, then train it on math-related web docs, then problems with step-by-step solutions, problems with tool-integrated solutions, and so on. Ultimately, you have this LLM that can use, for example, Python APIs and can solve math problems. As I said, this has come a long way. For example, here is a problem from the Putnam Math Competition from last year. o1's response on this problem is actually very reasonable. As these systems are getting better and better, it seems like they're solving more and more problems that are of this nature, that are similarly hard or harder.

That said, there are some fundamental weaknesses of this neural-only approach that you'd get with just a basic LLM. One fundamental reason is that there is this issue of data scarcity. In order to do rigorous mathematical reasoning, not just computing answers to well-posed questions, you need to have a lot of high-quality traces of mathematical reasoning. Alternatively, you would need high-quality reward functions. This is difficult to get just with human-produced data beyond high school or competition settings.

A second reason, fundamentally, is that natural language reasoning is hard to verify. This is a fundamental weakness of these language-only models. There are even settings where you can apply this sort of mathematical reasoning, for example, when you are doing mathematical reasoning about real-world systems and the behaviors of these systems have real consequences, so edge cases are really critical. In these cases, you want your mathematical reasoning to be really robust and rigorous. Unfortunately, language-based, purely neural mathematical reasoning tends not to be this way. It is entirely possible that scaling is going to solve all of these problems, but as I said, I'm a little bit skeptical about this.

In this talk, I'm going to demonstrate an alternative strategy, which is similar in many ways to AlphaProof, which you saw previously, but is more of an LLM agent approach that is an alternative to this sort of purely neural, purely language-driven kind of model. This alternative involves formal representations, just like in AlphaProof. There is an entire literature that is building up that follows this kind of strategy. Here, rather than just operating in this space of informal problem statements and informal solutions, you would be, first, doing what is called autoformalization. You go from the informal statement to a formal statement in a language like Lean or Coq or Isabelle. Lean, in particular, is what is used for AlphaProof, but there are other frameworks like this as well that have a very long history.

The appeal of these sorts of formal frameworks is that a proof can be written in a step-by-step way, just like code. Not only that, a proof can be checked. You can check if a proof that you have written in this formal language actually implies a certain property that you started out with, so this theorem that you started out with. The idea is that you would then, in the second phase, take this formal problem statement and feed it to a neural prover, which is going to come up with this sequence of steps, also called tactics, these rules that you apply. These are tactics, and you apply them one by one. At the end, you get to this QED stage, where you have nothing left to prove. That's how this process would work.

The advantage here is that if you think of the verification challenge that I mentioned earlier, where you want to check whether a claimed proof is actually a proof, this is something that you can establish relatively easily using a proof assistant. In terms of the data issues, the appeal of this kind of verifiability is that during training time, you can synthetically generate a very, very large number of candidate proofs. You can immediately get feedback on whether or not these proofs are correct. This turns this difficult problem of proof into this kind of a game-playing task, which you can approach with a version of reinforcement learning. This is what, for example, AlphaProof did. Although I'll show here that you can also take a more LLM-based approach to this problem. In fact, you can build upon these pre-trained language models and only have a pretty thin layer on top of that, and that still does quite interestingly well.

Just to go into a little bit more detail about these formal representations, even though you may have seen this in the AlphaProof lecture, I'll just quickly recapitulate this. Here is a theorem that we are trying to prove. What this says is that if x is even, then x squared is even as well. The theorem statement over here says that x is a natural number, and then it says that x being even implies x squared is also even. So modulo 2, the remainder is zero.

In order to prove this, you are going to apply this sequence of tactics, as I said previously. It's important to note that the system, the proof assistant, has a certain state that depends on what the original theorem is and which tactics you have applied so far. For example, in the beginning, you would have a state that looks like this. To spell this out, what this means is that there is one goal that you currently need to prove, and that goal is that under this context, which is that x is a natural number, this is the variable that you're allowed to use. This symbol, turnstile, means that this is what you need to prove. This is basically the theorem that you're starting out with before you have applied that tactic.

You have this state, and then you apply the tactic, which is, if you're thinking of this as a reinforcement learning setup, you are applying an action. You apply that action, and that leads to a new state. This state here looks like this. This intro tactic introduces this new hypothesis, which now says that, OK, let us assume that x is even along with x being a natural number. This property that x is even, we are going to give it a name, h. Under this context, which is like you have now declared a new variable with a certain type attached to it, you are trying to show that this x squared is even as well.

You apply this next tactic, which leads to another state. These tactics are written by humans and encode various rules of simplification in mathematics. For each of these rewrite rules that you are applying, the state is changing. For example, now the state has changed to you showing that x modulo 2 times x modulo 2, the whole thing modulo 2, is equal to zero. The next rewrite is going to simplify this. Finally, you will end up with zero equals zero. At that point, you're going to hit QED because your goal will just disappear. Each of these rules can be thought of as simplifying the state. At the end, ideally, you want to end up in this state where there are no open goals. At that point, you can end the proof.

That's what these formal proof assistants look like. They're state machines where you have the proof state and these actions, which are these tactics, and they lead to a new proof state. Your goal is to find a sequence of tactics that take you from this initial state of the prover to this QED state, where there are no more goals to be discharged.

Now let's talk about the two phases that I mentioned previously. The autoformalization phase is something like this: you are starting with this informal math statement, "There exists an infinite number of primes," and there is a proof of this as well in natural language. Now you are applying your autoformalization model. Later I'll talk a little bit about how this could be done. The version 0 of this would be that you throw a language model at this informal statement, and then out comes this formal theorem. This one is in Lean. What this says is that, for all n, there exists a p such that p is greater than or equal to n, and p is also a prime. That's how you establish that there's an infinite number of primes. The rest of it is the proof.

Let's assume that you have done this sort of formalization, you have a formal statement of a theorem, and now you're trying to prove it. Previously, I showed you that there are these tactics, these simplification rules that you could apply. Now you can imagine a search problem where at each state there is a set of potential tactics that you could apply. This leads to the creation of a search tree. Your goal is to find a path in this tree that ends up at QED. Of course, the complexity here is that at each point you can have a very, very large number of potential branches in this tree. Also, this tree can be quite deep. If you're talking about really advanced mathematical theorems, you could end up with proofs that are thousands and thousands of lines long. This is a far harder problem than any game-playing problem that we have been able to solve so far. It's remarkable to see the kind of progress that has already happened in this task over the last few years.

AlphaProof, of course, you have already seen. You've seen this picture. There's this formalizer network that does this formalization into formal problems. The solver network tries to generate these formal proofs. One big insight was that even more formalized problems are still helpful for learning. The solver network is being trained with RL. Another big insight was that you could complement RL training with test-time RL when you're actually doing the IMO or solving any other math problem. You could basically do this sort of test-time RL on that. But again, you have seen all of this, so I'll move on.

The approach that I'm going to talk about today is called COPRA. This was published in a paper at the Conference of Language Models last year, led by my PhD student at UT Austin, Amitayush Thakur. This is the overall picture of COPRA. It still has this view of interaction between an LLM that is predicting the proof steps and the actual prover that is executing the step proposed by the LLM and then giving you a new state. However, the difference is that while AlphaProof does this sort of advanced reinforcement learning for training, here you're just taking an LLM agent approach, where you have a frontier LLM, and the frontier LLM is being provided the proof state and some information from the history of the search so far. It is making a prediction as to what's the next step that you should take. You take that step, execute it, and then get back a new state of the proof. This process continues.

One important thing is that there is an external search going on, a depth-first search. Inside the depth-first search, you are doing all this. But it is fundamentally different in the sense that everything is in context. There is no actual adjustment to the weights happening. What is interesting is that even with a relatively small amount of compute, this approach was still able to do quite well. I think that this strategy has a lot of long-term promise. For one, because it immediately leverages advances in LLMs. As you go from GPT-4 to o1 to o3 and maybe someday GPT-5, you're going to immediately leverage those advances. It also has this ability to integrate natural language and formal language reasoning, because ultimately, you are just using an LLM to generate the proofs in the formal language, but at the same time, the LLM has the ability to do natural language as well. This is something you can benefit from, and I'm going to show concrete examples of this in a little bit.

Another thing is that this strategy is also applicable even if there is no corpus of training problems. One of the challenges in doing AI for math, once you go outside of settings such as math competitions, is that you're really trying to solve a new problem. If you're a math researcher, you're trying to solve a new problem that, by design, is quite different from the problems that a lot of other people have solved previously. In such a situation, it's a bit challenging to create a corpus of training problems and then do training of an RL agent. However, with an in-context learning agent, the appeal is that you can still use it. The LLM has inside it a ton of prior knowledge, and that knowledge is something that you could benefit from. The LLM can, for example, bring in knowledge about related problems, even though it's not been explicitly trained on them beyond the pre-training stage. In the longer run, of course, the two approaches of in-context learning and explicit training are not incompatible. You could imagine the in-context learning agent being used to generate a lot of different traces by interaction with the proof assistant, and then you could use them for doing fine-tuning.

One question that you may have is: do I really need to even have Lean in that case? Couldn't I just rely on the LLM alone? I would say that the difference here is that this interaction with the proof assistant is still providing a very strong form of grounding. For example, if you take a step predicted by the LLM and then you find that this is actually an invalid step, you're going to get that feedback right away from the proof assistant. You can take that feedback, stick it back into the prompt of the LLM, and then ask it to continue. Hopefully, it will learn from its mistake and carry on. In the long run, it's possible that once you have generated a very, very, very large number of traces via interaction with the prover, maybe you have fine-tuned your LLM on all this data, and then the LLM is progressively getting better and better. There is a point beyond which it just doesn't make any mistakes with this kind of proof. Maybe we'll get there someday, but we are still pretty far away from that possibility. At least, as of now, I think that this sort of interaction between LLM and proof assistant is still extremely valuable.

Here is this COPRA system in a little bit more detail. What is going on here is that you are starting with a formal theorem, but you could also have some informal hints. You could imagine bringing autoformalization into this picture as well, but we didn't. The first step is that you create a prompt based on the statement of the theorem. You feed it to a frontier LLM and just ask it to generate some potential tactics that would be of interest. Your model has predicted a set of one of the actions, or tactics, in this case. You parse that tactic because it will be in the form of text. Now you parse it into a structured format and execute it via the proof environment. This could be Lean or Coq or Isabelle. After you do that, in the lucky case, you already hit QED and this is all done. But if you fail, if this tactic is invalid or there are still some open goals that you still need to resolve, in that case, you get some feedback, augment the prompt, and backtrack if needed. We also have this retrieval phase, which seemed to add to the performance of the system. Here, you are also able to query a lemma database and bring in some relevant lemmas and then add it back to the prompt. This process just continues over and over again until you have resolved the original theorem or, alternatively, you have timed out.

Here's the prompt of the system in a little bit more detail. As you see, there is the serialized proof state. Here, you have what is the goal that you still have to prove and what are the hypotheses that are being made. For example, the hypotheses include x being of type NAT, natural numbers, and also x being even. Then you have the goal, which is x squared modulo 2 is equal to 0. We also have this information about the last step. The way to read this is that from left to right, you are looking at successive queries to the LLM. The first column includes information about what is the first query that you are making. Let's say that you already have made this decision that you are going to have this intro h tactic, which created that hypothesis, x squared modulo 2 equals 0. Then this is the last step. This is stored in a stack and fed as part of the prompt. At this point, there has been no error. You get this response from the LLM after you feed this information that the next thing you should do is RWH. Now, if you try to execute it, you might get an error message because the rewrite tactic failed. This information, this error feedback, you send it back to the LLM, and that's the next step. You say, try again LLM. The LLM says, OK, let's try apply nat.mul_mod_right. The details of this tactic don't matter; it's something involving modular multiplication. But the point is this is also wrong. Again, at the next step, you get an error feedback—an error message. This is something you stick back into the prompt. Then you make a query again. Now it says, oh, let's try this rewrite with nat.mul_mod. This time it works. Now you see that the interaction result doesn't have any error message in it. It's a success. This is now put into the stack. The last step, as you see, has changed. It's now rw nat.mul_mod. Now you're going to try to generate the next step. This time, the LLM generates this rw h. This is how this process is going to continue. In a nutshell, you may query the LLM, get some error message, stick it back into the prompt, and then continue. This process would just go on.

This evaluation was done in 2024, actually late 2023. The world has changed since then, but at least at that point, we compared with GPT-4 and found that GPT-4 frequently gave incorrect proofs. By contrast, this feedback with the proof environment, Lean in this case, really helped for COPRA. Even though it was using the same underlying model, the interaction allowed it to create a correct proof in this case.

Here are some experimental results. You can see the details in the paper. In this picture, on the x-axis, you see the number of queries. On the y-axis, you see the number of theorems proved. What we saw is that, pretty quickly, the COPRA approach was able to prove a lot of theorems with a relatively small number of queries, whereas the other approaches didn't get that far. Here is an evaluation on miniF2F test. The absolute numbers are not great, but the amount of compute we provided COPRA was also relatively small. We just allowed 60 queries per proof attempt. What we saw is that we got quite reasonable performance, comparable with the state-of-the-art methods. Of course, since then, the state of the art on this benchmark has moved quite a bit. miniF2F, for those of you who don't know, is a compilation of competition mathematics problems, mostly at the high school level. It's of the form: here is a theorem and come up with a Lean proof. Here, what we see is that there was around a 29% or 30% success rate, which is behind the current SOTA. But one question would be that if you take this approach and scale it up, then where would you end up? There are no public results on that yet.

In this talk, though, I'm going to go beyond the method that we just presented, which is the basic COPRA, and talk about some results that we just recently developed. This is not published yet. Here, we are leveraging the ability of LLMs to abstract and also to use natural language reasoning, because I think that this is really where the sort of approach that we just presented shines because of the tremendous amount of world knowledge that is stored inside LLMs. We can really use them in a lot more powerful capacity than just as a predictor of the next tactic.

Here's a sample problem. This is from IMO. This is saying: prove that this fraction is irreducible for every natural number n. What we'll see is that the COPRA approach can be extended a little bit to, in fact, come up with a hierarchical solution to this problem. This will involve breaking down the problem into subproblems and then solving hierarchically.

How do we do this? First, we ask the LLM to come up with an informal solution to this problem. I don't know if this font is too small to read, but basically what is going on here is that this was fed to o3, a reasoning model, and it was able to come up with this step-by-step plan. For example, you first define the GCD and then subtract to simplify. Then you apply the Euclidean algorithm. Then you conclude something about the GCD. Then you have the final conclusion.

We take this informal decomposition and put it in the context. Let's say that it's our goal to prove this lemma in Lean 4. This is the formal statement of the same problem. We are just saying, don't try to write the proof, just write the subgoals that are needed to prove this, conditioned on the informal proof that we just generated. Here is the decomposition of this lemma. What you see is that it comes up with these three subgoals. First, you show that this GCD step 1 is true. It's basically saying that this GCD of 21n plus 4 times 14n plus 3 is the same as the GCD of 14n plus 3 times 7n plus 1. Then there is the second one, and there's the third one, and so on. Finally, you have the main theorem that combines all of the above. The appeal of this sort of decomposition is that now this very complex problem has been simplified into these three subproblems, which are a lot more tractable.

In the next step, we then ask COPRA to solve the subproblems one by one. What we see is that COPRA is, in fact, able to come up with these proofs of the subproblems. Here are the proofs. Again, I'm not going to go into the details of the proofs. This is something that you have to stare at for a little bit, and it's not actually especially complicated. Once you have proven these lemmas, you just put them all in the context, also put in the informal proof, and then ask COPRA to prove the full theorem. It is able to do that. COPRA is not able to prove this if you were giving it just this entire theorem all as one block and just asked it to solve.

What we did here is really something that is a highly powerful design pattern in LLM agents. LLM agents are extraordinarily flexible. With just some variations on the ways you do prompting, you are able to make it do very, very different things, things that would take a tremendous amount of training if you weren't using these pre-trained models. What we did is that we started with a system that was just designed to be a step-by-step predictor of a low-level proof. Then we made it do high-level reasoning at this more abstract level, because you can think of these subgoals as basically being abstractions of subproblems. The LLM is able to create these abstractions. Once you have made use of these abstractions, your proof process becomes a lot simpler, because now you're able to go hierarchically. All of this was doable without any substantial training. You were just using the power of prompting. To me, this is a huge revelation: the idea that you can use the power of language, the power of all of the stored knowledge inside LLMs, and then combine that with this sort of higher-level algorithmics where you just put together the informal and formal reasoning steps in various ways and change the prompting strategy, and you get something that is qualitatively different in capability.

It turns out that the Lean feedback here was also really important because at least with o3-mini, what we found is that there were hallucinated lemmas and so on. The grounding that was provided by the Lean theorem prover was absolutely critical to solve this problem.

Now I want to talk a little bit about a different application of the same idea. This is in the context of formal system verification, which is one of the most practical applications of rigorous mathematical reasoning. What you're doing in formal verification is that you are mathematically modeling a real-world system as a set of definitions, and then you are modeling system properties as theorems involving these definitions. Then you're proving these theorems. These methods have led to extremely rigorous systems, systems that are very difficult to—or almost impossible to—hack. For example, there was a time in a program organized by the US Department of Defense, they used this formally verified Linux kernel called seL4 and put this on a drone. Then they got a red team to try to attack this drone for a period of six weeks, and they failed because the rigor provided by formal verification was just so much.

Historically, formal verification has been extremely expensive, and as a result, it's not really been widely deployed because the costs are not justifiable except in the most safety-critical scenarios. However, with the rise of AI, especially AI for math, the cost-benefit equation changes quite drastically. Now, you can imagine doing not only proofs of math, but proofs of real-world systems—of, for example, correctness, security, performance, and so on—using this growing generation of AI tools.

Here, I'm going to show you a basic example of this idea. This can be scaled up substantially with more powerful models and more compute. Here, we are going to give you a very, very basic compiler and then show how to formally verify its correctness. This is so simple, the compiler, that it's almost a joke. But trust me, when you go to more advanced compiler design, the fundamentals don't change. It's really the same sort of idea, it's just that there is more complexity.

To do compiler verification, you would need the following pieces. First of all, you would need to have a notion of a language. A language consists of a syntax and semantics. Syntax is a grammar that defines the form of the programs. Semantics are the rules that define the executions of programs. A compiler is basically a set of rules that translate programs from one language to another. Verification is that you prove that the rules preserve semantics.

Here, we are going to talk about a very basic expression language in which you are allowed to do addition and multiplication. This is just basic arithmetic expressions. The syntax that you see over here is in a language called Coq. It looks a lot like Lean. It's a similar, dependently typed, functional programming language. What you're doing is defining this binary operator. This can be either plus or times. You're defining this notion of an expression. This says that it can be either a constant or a binary operation. There are some rules that tell you how to actually evaluate these expressions. If you're given an expression, what will be the result when you evaluate it? I'm not going to go into the rules in detail; I'll share the slides. If you are interested, you can look up many resources on the web. Also, I'm happy to answer questions over email offline or after this talk.

Then you define the source language. The rules that we saw previously allow you to define the semantics of quite complex expressions. Here, you can define that you have a Const 2, so an expression can be a constant or a binary operation or the result of a binary operation. You see that this is the result of applying a binary operation to two expressions. This is a recursive definition. You are getting an expression as output. You can evaluate various complicated expressions in this way.

Now you have to define the target language. A compiler is a mapping from a source language to a target language. You have to define the target language. Here, you are defining the target language as a list of instructions. A program is a list of instructions. An instruction is either a constant expression or a binary operation expression. One way to see it is that while the original expression in the source language was defined in this recursive way, in the target language, when you are interpreting it, you are doing it using a stack. This is a well-known idea in programming languages: if you have a recursive expression, let's say an arithmetic expression, then the way you can execute it is by maintaining a stack. You can keep pushing the current instruction on the stack. You can build up a low-level machine, a stack machine. Actually, a lot of early computers were based on this idea. The stack keeps pushing new things on it. You pop, and then you read the value that was just popped. While very basic, it still captures a lot of important programming language concepts.

The point is that I want to believe that the mapping that I would do between these two representations is actually correct. Of course, first I have to define the compiler. This is the compiler. What this says is that I am going to take my input expression in the source language and do a pattern matching. I consider these two cases: either when it's a constant or when it's a binary operation. In each of these cases, I'm going to generate a target language program. In the target language program, the entire sequence of instructions is just written down as a list. When you are executing the program, you are maintaining a stack. That's when you are modeling the recursion. In the program itself, you are just generating the instructions from E2, generating the instructions from E1, and then sticking this b. You are just building up a stack with a sequence of instructions with all of this. For example, if you started with this expression Binop plus Const 2 Const 3, so this is 2 plus 3, that's really the expression. In the lower-level language, it's going to look like this.

Then, you have to define this correctness theorem, which says that for all e in the source language, upon compiling this expression, you get something that, when you execute it in the programming language's semantics, you get the same thing as if you evaluated the original expression in the source language's semantics. These two are the same.

It turns out that COPRA cannot prove this theorem just in one shot. What you do instead is ask the LLM—again, think of the IMO example that I showed, this is quite similar—to invent a lemma, an intermediate goal. The LLM goes ahead and thinks for a while. This is o1 here. It comes up with this intermediate lemma, which basically says that rather than trying to prove the entire compiler correct, you instead show one step of compilation, like for one subexpression, that's going to be correct. That's the target theorem. The auxiliary lemma is saying that you can compile one instruction correctly first. This is really just simplifying the problem, breaking it down in a step-by-step way.

We ask COPRA to come up with a proof of this one instruction. It's able to, which is fantastic. Then, what we can do is, just in the way we did previously, stick this back, take this proof, give it to the COPRA agent, put it in the prompt, and then ask it to prove the entire theorem correct. It's now able to do this.

That's what I have on this particular topic. In summary, I would say that mathematical discovery with LLM agents is extremely promising. I think that you can go a very long distance with just in-context learning, but you want to have the proof assistant feedback for proper grounding. There is a lot of higher-level agent design that you can do where you can combine agents that are doing, for example, formalization with agents that are doing hierarchical planning with agents that are doing low-level proof generation. This is a very new space. There is a lot to be done over here.

One question, again, going back to something we asked previously: can you get rid of this interaction by just pushing the whole thing to training? Maybe you generate a lot of training data in this way by interaction with the prover, and then you just continue training the LLM with those traces. At some point, the LLM just gets good enough. We are not there yet, but maybe this is a world that we can get to someday. Currently, we have to have these sorts of agent architectures.

There's this paper several of us recently wrote. You may have seen a reference to this paper previously. Kaiyu is the lead author of this, and Dawn was the leader of the overall effort. If you are curious about this area, want to know more about specific challenges, roadmaps, then I would recommend that you go and look at this paper.

I had a few things to say about open challenges, but I'm going to skip those in the interest of time and move on to the next topic, which is AI for scientific discovery. I want to emphasize that this idea of LLM agents has a lot of value in deductive mathematics, where you are starting with a set of principles, a set of axioms, definitions, and then you are proving statements in a step-by-step way. But it also has tremendous potential in empirical discovery, which is what happens in most of the natural sciences, where you are starting with data and then trying to fit some hypotheses or some experimental claim to this data. We'll see that this is also a place where the abstraction abilities of LLMs play a potentially very interesting role.

Very quickly, this is what a natural science process looks like. You choose a research question, generate some hypotheses, do an experimental setup, collect some data, do some analysis of the data, interpret the data, and then report to the community. As an example, if you think of the world of astronomy, back in the day, there was this problem that was observed: people believed that the Earth was the center of the solar system, but some of the planets appeared to move backward. This led to the creation of this new hypothesis that, in fact, we have a heliocentric model. This led to the collection of more data. Tycho Brahe, in particular, observed Mars and came up with a whole bunch of precise measurements of the planet's positions. Kepler chose to analyze this data and fit a wide variety of geometric shapes to this data. He came up with Kepler's third law, which eventually led to Newton's law of gravitation. There was this process that happened in science, which involved trading in abstractions: seeing some data and then coming up with this law in terms of abstract principles, but then further generalizing it into a law that was even more explanatory.

I think that LLM agents have tremendous potential in automating a lot of pieces of this pipeline. In particular, here, we are going to focus on the problem of symbolic regression, which is the task of coming up with equations based on empirical observations. For example, an instance of that would be that if you were given these measurements of planet Mars and then you wanted to discover Kepler's law, how do you do that from the data?

One would think that LLMs would not be a great fit for this problem because the data is just really low level—just a bunch of numbers. However, it turns out that LLMs do have tremendous potential here in the sense that they can bring in prior scientific knowledge into the picture. What you have to do, though, is complement the use of LLMs with the use of other machinery that is able to, for example, evaluate hypotheses on the data and give you some notion of how well it performed. Once you have that kind of a mechanism for evaluating LLM-generated hypotheses, you can create an agentic loop where you have more and more feedback going to the LLM. This process of hypothesis generation is left to the LLM, whereas hypothesis evaluation is done by some other entity. If you think of it, it's not fundamentally different from the way we use Lean or the proof assistant in the previous part of the lecture.

One of the most established approaches to symbolic regression is genetic programming. Here are some visualizations from a system called PySR, which is quite popular in this space. What's going on here is that you have a pool of candidate expressions, candidate equations being maintained. You have mutation and crossover operations being applied to those. You are updating this pool of expressions using feedback from a separate machinery that is evaluating these expressions and telling you how good is the loss, how well does this match the reality that is there in the data. This has had a lot of impact. There are applications in physics and other areas as well. Here are some example papers.

One thing that's interesting is that if you just imagine how does this process of genetic programming go on, there is this pool of expressions that is being evolved. What you see is that there are these islands of expressions, clusters which contain expressions that have something in common between them. One way to think about how LLMs are relevant is that these clusters or islands of expressions capture various kinds of classes of functions. For example, in this red one, you may have exponential functions. In this case, you may have trigonometric functions where you have the cosines. One way to imagine this is that an LLM looking at these expressions, because LLMs are good at coding and associating code with natural language, could perhaps come up with some explanation of what this piece of code, this expression, is actually doing. That's the idea of abstraction that we are going to use.

This paper was at NeurIPS last year and co-led by Aria Grayeli, who was an undergrad at UT, and Atharva Sehgal, who's a PhD student at UT. The starting point here is that we want to do this kind of concept abstraction. We want to be able to look at these variety of laws, which come from very different contexts, but then we should be able to look at this and tell that what's going on here is that you have this power law thing going on. The other thing we want to do is use background knowledge that was created by a physicist or that's there inside an LLM. For example, here, you have an LLM, a concept that has been produced, which is that wave strain diminishes as distance increases. This is, let's say, a property of black holes. You want to be able to associate this kind of information, this piece of common sense knowledge, to equations. This equation, for example, satisfies these properties. You want to create a mapping between these two things. If you imagine how the scientific process is ongoing, let's say you have an AI that is predicting equations based on data, you also want it to be conditioned on this sort of high-level knowledge that is being given by a human expert. You want to have this association between code, that is these equations, and language.

The way we set up this symbolic regression problem using LLMs is something like this: we imagine that—and actually, here, it's not just LLMs. It's more like a combination of LLMs and this sort of abstraction into high-level concepts. We imagine that there is this universe of high-level concepts. For example, wave strain diminishes as distance increases, or power laws, or sinusoidal functions. These are high-level ideas that we have in our head. There is a notion of a data set, which is the concrete data that you want to fit. You have a space of programmatic hypotheses. This is, for example, the landscape of equations expressible in Python or in some restricted DSL inside Python. What you're saying is that you want to jointly infer, given the data, this distribution over pi, which is likely hypotheses and likely concepts. You don't want to just get likely programs, given the data, which would be the goal of traditional symbolic regression. You want to get both the programs and also the kinds of concepts that they represent. Ultimately, you want to explain these programs to a human scientist. Alternatively, maybe this joint distribution gives you more information than if you just operated in the program space. This you can factorize in this way.

It turns out that the way to think about it is that p of D given pi is basically saying that if I have a particular program pi, what is the likelihood of the data? How well does the program explain the data? In order to find this, you just execute the program and see what kinds of results come out and what the likelihood of the data set is. p of pi given C is basically telling you that, OK, here is the concept that I have. Let's say I'm thinking about power laws. Conditioned on this concept that I'm thinking about power laws, what is the probability of this program, this piece of code? To what extent does this piece of code represent a power law? This is something that an LLM has background knowledge inside it to do. Finally, I have this p of C, which is a prior over concepts. This is also something that an LLM knows: I have seen the literature of, let's say, physics until this point, so what is the likelihood of this concept? All of this together gives you, ultimately, the ability to infer both a distribution over both programs and concepts.

In order to really implement this, this is the loop that we came up with. This follows the basic structure of an evolutionary algorithm, but it does this sort of Bayesian concept learning thing guided by LLMs on top of that. Operationally, the way to think about it is that you have, like in any evolutionary algorithm, a population of hypotheses or programs. At this point, you have a choice of doing evolution, either guided by the classical symbolic method where you're doing some random tweaks to the abstract syntax tree of the program, or alternatively, you are doing this LLM-guided evolution, where you are saying that here is a concept, here is a program that I have, and I want to generate a new program. When you are doing that, you are being guided by this concept library, which tells you that these are the concepts that I'm thinking about right now. Power laws seem really good, really well-suited to this condition, or maybe trigonometric functions seem especially well-suited to this setting, and so on. You are feeding all of that to this LLM-guided evolution process, and that allows you to basically mutate the current programs into new programs. After you do that, you are, like in the usual evolutionary way, identifying the best candidates in this population. This is where you use the fitness function, which is how well does your hypothesis pool explain the data that you have? This is where that p of D given pi comes in.

This is a little bit more detail about how the hypothesis evolution works. You start with this initialization, initial program population, and then you're evaluating. You're getting the best program. You're mutating or crossing over. Then you're replacing the oldest program. This process just carries on for a while. The important thing is that you are using the LLM for implementing this mutation and crossover operations. In particular, rather than in a classical genetic algorithm, where you're given the AST and you say, swap out this subtree by a different subtree, now you're going to ask the LLM to rewrite the program into something. This idea was also used in the FunSearch paper from DeepMind last year. FunSearch has been very successful. The way in which this LaSR work differs from FunSearch is fundamentally in its use of this concept library, but a lot of the other ideas are closely related.

This is just to highlight the difference between the symbolic crossover versus the LLM crossover. In this case, the LLM is basically doing all this. It's being given a crossover prompt and some formatting instructions, and then it's generating the new expression.

After you do this, you have evolved your hypothesis, so now you have this new pool. Now you also evolve your concept library. The concept library evolution process has these two stages. First, you take your new programs that you have created, your new program pool, and now you're asking the LLM to abstract this into these natural language high-level descriptions. For example, here, the LLM is telling you that this equation represents exponential growth or decay. The concept evolution process is just like the hypothesis or program evolution process, except it now uses an LLM to combine, mash up these different concepts into a new concept. For example, here, you see that exponential growth/decay is one concept. Another concept says depends on temperature. Our LLM, because it has all this knowledge about all sorts of concepts relevant in physics, comes up with this idea that, oh, this should be a Boltzmann distribution. This is a useful concept to have. This is going to influence the hypothesis evolution, which is going to influence concept abstraction and evolution. This loop will continue.

One way to visualize this is that after the first phase, when you have done this hypothesis evolution, you may have these islands of different kinds of expressions. In the concept generation process, concept discovery process, you're going to get these natural language descriptions of these different islands. This is sinusoidal trends, this is power law trends, this is linear trends, this is exponential trends, and so on. What we found is that the use of this sort of a high-level concept library helped us in this equation discovery process.

We evaluated this on this task of equation discovery—discovering Feynman equations, just given the prompt that there was the data, but then there was a prompt that told you which chapter you were looking at, and so on. What we found is that this approach proved more effective than a version where you were just using either pure genetic programming or one where you were not using any kind of concept learning. It turns out that even with local language models, when we didn't use a frontier model, we were still able to outperform this genetic programming approach. The overall finding was that—and you can find more details in the paper—the use of this sort of high-level concepts generated by the LLM accelerates discovery.

One appeal of this sort of LLM-based approach is that it can very naturally take in user-provided hints. In this case, we gave the algorithm some hints, which was: this is the kind of equation you are looking at. In the previous experiments, actually, we did not have hints. This was just purely based on data. In this case, we had hints, and we found that hints actually provided some benefits. On the x-axis here, you have the number of iterations. On the y-axis, you have the fraction of equations solved. We found that the use of the hints provided a modest benefit at least earlier on in the process.

Here is an example of this in action. This is the classic Coulomb's law. PySR is this genetic programming approach, and it gave this very unwieldy kind of expression that, it turns out, is equivalent to the ground truth expression but would require a lot of simplification for you to go from here to the ground truth. The LaSR approach is able to, because of some of the knowledge that is there in the LLM, reduce to ground truth after four steps of simplification.

You could say, wait a second, isn't the LLM cheating? Because it already has Coulomb's law in its mind. How do you know that this is not just leakage? Of course, in this case, there is that possibility. That said, I would say that the attraction here is that the input to the system is just this data—just the input-output values that would result from Coulomb's law. It's a very low-level kind of input. The fact that it was able to fit this to Coulomb's law is kind of interesting. That said, we also did other experiments on synthetic data where you just manufactured a completely weird equation and a weird data set coming from that equation, and then asked the system to regenerate this. We found that there were substantial benefits even in that case.

Here is some information about some of the concepts that you ended up getting from LaSR. You see there is stuff like, good mathematical expressions exhibit symmetry or regularity. That said, there were also some expressions that didn't really seem to mean anything. At the same time, there is no guarantee about the quality of the concepts. This is all natural language at this point. An interesting possibility is to use some kind of a formal language to describe the high-level concepts.

We also used this in a new discovery problem, because we were also worried about the possibility of this being an artifact of leakage in case of standard benchmarks like discovering Feynman equations. We used this method to come up with a new scaling law for LLMs. Here, what you see is the so-called chinchilla law. This is a law that was postulated in a DeepMind paper from a few years back. Here, basically what you're trying to do is predict the loss of the LLM based on these various hyperparameters. This was the law. You postulate a scaling law, measure model loss with respect to hyperparameters, and then fit scaling law to data set. In order to do this, of course, you would need to have a data set where you have a lot of values of these hyperparameters and corresponding accuracy values for the LLM.

We did the same thing with LaSR. Rather than fitting a hand-fitted law, which was done in the Chinchilla paper—they basically postulated a law like this, and then fit the parameters—by contrast, what we did is just asked LaSR to go find it. LaSR was able to come up with this sort of a law, where we also had this parameter called number of shots, which is that, if you are doing prediction, what is the number of examples that you're giving it in context. This parameter is available in some of these data sets. What we found is that there was this interesting kind of law that came up. This is just one example. It's not really very comprehensive yet, but this just shows that you can make these kinds of new discoveries using this law. Basically, if you interpret this, the way to think about it is that a large number of shots gives poor results for low capability models, but then, once the models pass a capability threshold, having more shots helps. This is sort of a common sense linguistic interpretation of the law that came out.

You can also do something after this. If you're thinking of yourself as an LLM scaling law person, you start with the Chinchilla law from the previous paper, and then say, OK, maybe I should bring in this insight about shots into the Chinchilla law. Combine the old Chinchilla law with this new discovery that was made by LaSR, and then you find that this modified Chinchilla actually is able to do better than both the old Chinchilla and also the LaSR itself. This is kind of interesting and shows that this discovery process using LLM agents—and this is very much an agent, not just a one-shot LLM. You're using an LLM combined with various other tools. What you are getting out of this can influence human discovery as well and lead to something that is greater than the sum of the parts.

In the interest of time, I'm going to skip this next bit. What I said there is that these ideas can also be applied to settings where you have visual high-dimensional inputs, because now we have visual language models as well. You can go crazy and build these kinds of evolution-based agents, where you have VLMs as part of the story. I'm extremely optimistic about this direction.

In summary, I would say that LLM-directed evolution is a powerful tool for empirical science. This point has already been made previously by FunSearch, but now we see that we can make it even more powerful with these ideas of abstraction that LLMs can give you basically for free. This can be even extended to visual settings where you have to use now visual language models. But hey, they've come a long way. Importantly, the abstraction abilities of LLMs can also be really helpful here.

Open challenges: verification of hypothesis and concepts, concept representations that go beyond natural language, scaling to larger search spaces, input dimensions, and even going beyond hypothesis generation to other parts of the scientific discovery process. But at the end, though, I am very optimistic that LLM agents—not just LLMs, but these kinds of agentic workflows where you have combinations of LLM queries as well as other kinds of machinery—can be extremely powerful in scientific discovery. They can help you come up with new scientific hypotheses, and even do experiment design. Also, as we saw, in mathematical discovery, they can be supremely powerful. Again, we are just in the early stages of what I think this line of research will end up being. This can have applications in math, in science, in engineering, as you saw the formal verification application, building rigorous systems. The future is very bright.

With that, I'll end. I want to thank all my collaborators and funders. I'm happy to take questions.
