SEAN WELLECK: Today, I'm excited to talk about bridging informal and formal mathematical reasoning with AI. Throughout this course, you've seen many different ways where these AI agents can start to have a lot of impact in different areas. One interesting question is to what extent can they impact more expert domains, such as finance, science, medicine, and the domain that I'll talk about today, which is mathematics.

With mathematics, there's a variety of things that you could imagine doing with an agent or a language model. You could try to have an open-ended dialogue with the model, have it come up with counterexamples for you, or help you write proofs. Your imagination is basically the limit. What I want to argue today is that for math in particular, how you represent the math is actually extremely important to think about.

One way to represent math is what is called informal mathematics. Here, math is represented as raw data. It could be text, an image, or even audio if you're talking with someone about math. Doing this is extremely flexible, and in some sense, most of the math that you've encountered or used is probably in some kind of informal format. Even something like a LaTeX solution or an arXiv paper would be considered informal.

When we think about using AI to generate or operate in informal math, there is a really important problem: we don't have any guarantees that the math is correct or not. Here is an example of a language model producing a solution. The answer at the end is actually correct, but it makes subtle mistakes in the intermediate steps. For really simple math, this might not be an issue, and the language model might still be useful. But if we really want to trust it in, say, education or research-level math, then it could get very tedious and very difficult to know whether the outputs are correct or not.

This motivates the other end of the spectrum, which is called formal mathematics. Here, math is written as source code in a programming language. Basically, you can write a specification or a lemma or theorem, like "1 plus 1 equals 2," then you can write a proof. The really cool thing is that if all of the code compiles, then you have a formal guarantee that the proof is correct. This is a lot different than informal math because now we can have certainty if, say, a language model generates a proof, that it is actually a reliable and correct proof of the theorem.

There have been a variety of these theorem-proving languages developed. I'll mostly focus on a language called Lean, which recently is having really exciting growth and intersection with the mathematical community, which I'll talk about in a bit. For one of the topics that I'll talk about, I'll also mention a language called Isabelle because it has some features that make it amenable to writing different types of proofs with AI systems.

If you haven't seen one of these before, I wanted to actually show you what one of these formal proof assistants looks like. To do this, we'll try to prove a really simple theorem, which is transitivity: if R is a subset of S and S is a subset of T, then R is a subset of T. I'll actually show you this as a demo. Here, I've opened up VS Code. As I mentioned, this is literally like programming, like writing code. Here you can see the theorem statement: if R is a subset of S and S is a subset of T, then R is a subset of T. These are often called interactive proof assistants because, as I go to write a proof, it actually tells me what is the current state of the proof.

The idea isn't to learn every bit of the formal system, but I'm just showing you the important parts that will be relevant for the talk. Here we can see these different variables and what we have remaining to prove. The thing is that it takes some expertise to actually write a proof in Lean. For example, if I know some Lean, then one thing I can do is this command here, and you can notice that it actually changed the state of the proof. Now I have these two variables up here, and the goal has also changed. If you have some expertise, you can actually know that this idea of transitivity has already been programmed into Lean. Here I wrote this transitivity rule or lemma, and now you can see that there's no goals left. I've completed the proof.

We'll touch on a lot of things that I've talked about here throughout the talk. We'll talk about the fact that it requires some expertise to actually write a proof in one of these proof assistants. If you look at this proof, it might look a bit different than one that you would see written down in a textbook or something like that. We'll revisit these ideas throughout the talk.

Now, going back to the talk, we could ask: I showed you this very simple example, but is this toy or is this actually useful for more complex mathematics? A really exciting thing is that, increasingly, there's a larger and larger intersection with the mathematical community. Here's an example of the mathematician Terence Tao. He formalized, in Lean, the proof of a theorem from one of his actual papers. A really exciting thing that's playing into this is a project called Mathlib. If you noticed in the demo I gave, this idea of transitivity was already programmed in. That's the idea of Mathlib: lots of people are contributing and building up foundations that other people can use to make it easier to write more and more complex math.

If you look at what mathematicians write or talk to them and listen to what they say, these formal proof assistants are exciting for a few different reasons. One is that they can potentially enable a new form of collaboration. You can break down a big problem or big proof into multiple pieces, and then anyone can go on and submit code to try to solve one of the pieces, to prove one of the helper theorems or helper lemmas. The cool thing is that we know for a fact that we can trust the code since Lean will automatically check it. This is a lot different than the typical way of collaborating on a math paper, where you have to trust the author or read through their results. It also gives you instant feedback, as we saw, which could be useful in a variety of ways, including when trying to learn math. Of course, it has these guarantees on correctness.

Hopefully, I've convinced you that formal math is getting more and more interesting for learning or for the actual mathematical community. But why is it actually important for AI? As we all know, we are not able to automatically check or trust the outputs of AI models. What the formal system gives us is some way of preventing incorrect math. Or, it turns out, you can actually use this for code as well. Increasingly, we see more and more models trained with reinforcement learning, and I'll actually talk about one of those in the talk. What you can think of the formal system as being is it gives you this perfect reward signal of whether something is correct or not correct. This turns out to be very useful for improving machine learning models further. It also gets at one of the hot topics in the field right now, which is reasoning, and it gives you this nice playground where you can try even very easy reasoning problems to arbitrarily hard ones within the domain of math.

If we look at the history of language models in formal math, one of the pioneering works happened in 2020 from two authors from OpenAI. They developed a system called gpt-f, which used a language model to predict the next steps of these proofs. Even back then, they got some pretty interesting results. They had the system generate a proof and then submitted it as a pull request. Here are some of the comments from the maintainers of the formal proof library. Even this model, back then, could produce shorter proofs, and some of them were even philosophically more concise than the human-written ones.

Since then, we've seen rapid progress in using methods based on language models for formal theorem proving. Here is showing the performance on a really common benchmark based on math competition problems. I actually made this plot a few months ago, but it's already out of date. Just this morning, there was a model released that gets 80% on this benchmark. To give you some flavor of what these look like, again, you don't have to read through the details of this or through most of the examples I show in this talk, but here's an actual output from a model called DeepSeek Prover. It managed to produce this proof of an IMO problem. Hopefully, it gives you some sense that the outputs of these are starting to get pretty non-trivial.

Terence Tao, if we go back to members of the mathematical community, wrote that during his formalization project, he was actually using GitHub Copilot, and sometimes it was able to infer the directions he was going in and things like that.

With all this, with formal math being so exciting for both mathematics and for AI, the question is, why don't people in AI always use formal math? What is the blocker to making this happen? I want to argue that the key challenge is that there's this gap between how people are familiar with doing math and the actual formal system. In particular, there are so many ideas and intuitions, and even the way that we write proofs, that are just difficult or very tedious to express in the formal system. In a formal system like Lean, each step of reasoning needs to be explicitly spelled out in detail. As we saw even in that very simple example that I gave, you often have to have a deep knowledge of the system, like what things exist in the system, how to write the code, and so on.

The theme of this talk will be steps towards bridging the gap between informal reasoning and formal reasoning. Can we use AI and language models to get the best of both worlds, maintaining the flexibility of informal reasoning while having all of these benefits that I mentioned with operating in the formal system?

I'll talk about three different directions that we and others in the community are pursuing. First, I'll talk about incorporating informal thoughts into the formal theorem proving procedure. I'll talk about training models to think informally with reinforcement learning, and in particular, our recent work, which is called Lean-STaR. Then I'll move to informal provers or informal proofs, and I'll talk about how you can break down the problem of writing one of these proofs as first writing a sketch, and then filling in the lower-level gaps of the sketch. I'll talk about our work called Draft-Sketch-Prove, and then our recent work, which is actually not even released yet, of trying to enable tools that let you fill in the gaps of these sketches in the Lean Theorem Prover. Finally, at the end of the talk, I will talk about more of a future direction that we are taking initial steps towards, which is, how do we actually make these systems useful to mathematicians doing research-level mathematics? In particular, I'll talk about one way in which the models could assist in these larger projects. I'll talk about some tools that actually already exist, which you can try after the talk if you want, and a new benchmark that tries to further progress building these assistants for these complex, research-level type projects.

With that, let me go to the first part where I will talk about integrating informal thoughts into formal theorem proving. This will largely be based on our paper called Lean-STaR, which we'll be presenting soon at ICLR. Here, again, we're in the setting of theorem proving in Lean. We have the theorem that I showed you before at the top. As I showed you in the demo, you could think of this as a kind of interactive step-by-step process. In particular, Lean will give you this state of the proof. One thing that you could do is send that state to a language model, and it could try to generate the next step of the proof. Then you can provide the next step to Lean, and it can either tell you whether the proof is done, whether there is an error, or, as we saw, it can give you the next state of the proof. You could then continue this until you either give up or you reach a complete proof. In that sense, you can see one of these proofs as being a sequence of states and next steps, or they're called tactics.

In a bit more detail, what we want to do is train a model on some dataset of inputs and outputs. An example is you could have the model take in the proof state and output the next step. What you typically do is extract a lot of these state and next step examples from some corpus of theorems and proofs. For example, you could use the entire Mathlib project that I mentioned earlier, or you could even have a model generate its own theorems and generate its own proofs, and then ultimately turn it into a dataset.

Once you've trained a model, you then want to use it to generate full proofs. The traditional way of doing this is through a tree search procedure. Here's an example tree search called best-first search. Basically, the idea is you ask the model to generate multiple candidates for the next step. Then you somehow come up with a score for each one of these candidates. You choose the step with the highest score. For instance, first, we chose this intro h step. Then you can expand those further, choose the next highest scoring sequence. As you can see here, it kind of backtracked, and then it took this green path, which led to a correct proof. This is just one possible strategy you could use for generating a full proof if your model can only predict the next step.

What we are interested in is, can we go beyond just training the model on formal proofs to generate formal proofs, and instead train the model to think before each step of the formal proof? In particular, we want to train the model to generate what's on the right, where before taking a step, it outputs some reasoning. It's free to decide whatever it wants to do with the reasoning. It could break down the problem, plan out the next step, or even solve some computation that isn't really present in the formal proof.

Why would you want to do this? It could give the model some room to plan out what it is about to do. It can help diversify the search space, since now the model is searching for proofs not just in the formal space, but in the formal space augmented with these thoughts. I won't go into details here, but there's been a lot of cool work in the language model community showing that adding in these chain-of-thoughts in other domains can actually give a model which is more expressive in terms of the kinds of problems it can solve. We have a few good reasons to try this out.

The key issue here is that nowhere on the internet are there formal proofs written in Lean that have these nice thoughts written out for them. So what we have to do is actually design an algorithm so that a model learns on its own to produce these thoughts that are useful for the proofs. The algorithm is called Lean-STaR, and it adopts its name from another work called self-taught reasoner. The process basically has two different phases. The high-level idea is we want to learn to generate these thoughts via reinforcement learning.

First, we have to initialize the model so that it can produce some type of thoughts. Then we want to train it further so that it gets better at using the thoughts to prove theorems. In the first step, we use a clever trick: we can look at a state and the actual next step of the proof, and then have a language model just retrospectively come up with some thought that describes the transition from the state to the next step. What we found is that if you don't give this step, then the language model might just output a meaningless or incorrect thought. But they were good enough at coming up with some thought which described this transition. Then we can annotate a large set of proofs. For instance, we take all of Mathlib, and we can get hundreds of thousands of examples of state, thought, and next step. We can use that to train a model. It doesn't necessarily have to use all 100,000, by the way, for this initial step. You want to just train some initial model that knows to produce these thoughts prior to taking steps.

Then we want to take that model and put it into this loop. We have the model generate full proofs, then we ask Lean to tell us whether they are correct or not, and then we use some type of reinforcement learning algorithm to improve the model further. What we need is some method to generate proofs, and we need to actually choose some algorithm to implement this with.

What we discovered is that this commonly used way of using the tree search, or best-first search, was actually difficult to get to work when we added in these thoughts. I think one reason is that it gets difficult to actually assign a really good score to the thought, and so as a result, the tree search didn't work as well. What we did is design a new method, which is this simple idea of generating multiple proofs in parallel. For a given trajectory, you generate a step. If you get an error from Lean, you just retry on that same path. You keep going up to a maximum number of steps, or you stop if you reach a complete proof.

For the algorithm, we actually just used the simplest possible algorithm. We, as well as other groups, are now exploring better reinforcement learning algorithms, like online algorithms. But here we did something very simple: you just collect the successful trajectories, add those to your dataset, and then train a new model on this dataset of states, thoughts, and tactics. This algorithm goes by various names: expert iteration, ReST-EM, rejection fine-tuning. But the idea is very simple.

Typically, the way that these models are evaluated is on these math competition problems. Although we train the model on Mathlib, here's a common test dataset which is used, called miniF2F. Here's an example problem from the International Math Olympiad. I've shown the informal version, just to show you what the problem is like, but the model here only gets the formal version and has to produce a proof.

Here are some results with an early model that we tried in the project. In red, you can see the model that we started with. In black, you can see the model after we perform this initialization step. In gray, we see the model after we perform the expert iteration. You can see that the Lean-STaR process basically took it from being worse than this baseline model, which was based on this GPT-4 agent, to being better than it. At the end of the project, we applied Lean-STaR to the best available base model at the time for theorem proving, and again, we saw these improvements going from gray to black. At the time that we did this, this was the state-of-the-art for this miniF2F dataset.

One really interesting thing is that we found that models with the thoughts benefit more from scaling up the search budget, or the inference budget. Here we're seeing, as we sample more and more steps, either with our sampling method or as a baseline with the best-first search, how the performance looks. You can see that the gains we get from the green line, which is Lean-STaR with the thoughts, are more pronounced than the model that doesn't have the thoughts. This might be indicating the idea that I mentioned before, that adding in these thoughts helps to diversify the search space.

Here are some examples. Again, you don't have to read through the details, but if you're curious, you can take a look at the paper after. One interesting thing here is that if you look at the formal code, there are actually some steps of reasoning that the model is doing in the informal thought, shown in gray, that aren't really present in the formal code. Here's a longer example, partly because it looks impressive, but also to show you that when you add in these informal thoughts with the formal ones, the output starts to get fairly non-trivial.

After Lean-STaR, the really exciting thing is that this idea of augmenting models or reasoning models with thoughts and training them with reinforcement learning has been adopted in the theorem proving community. Very rapidly, this idea of incorporating thoughts became a part of the state-of-the-art systems. Here's the example I mentioned before called DeepSeek Prover, where they incorporate the informal thoughts. As I mentioned just this morning, there was the state-of-the-art prover released by this startup, and you can see that they have this idea of alternating between informal reasoning and snippets of code. More broadly, these reasoning models that generate long chains of thought and are trained with reinforcement learning have really begun to revolutionize LLM reasoning. I'm not going to go into the details here, but you have probably heard of these models like OpenAI 01 and DeepSeek R1. It seems like, at least currently, having this kind of informal thinking is a really key part of the reasoning procedure.

To recap, Lean-STaR builds on the observation that training only on the formal code may be insufficient to learn the actual thought process that's needed to produce the code. It gives a simple algorithm for learning to generate the thoughts.

Now I'll move from informal thoughts to informal provers and informal proofs, and in particular, talk about methods that prove theorems by producing a high-level sketch and then filling in the gaps of the sketch. Here, the motivation is to combine the benefits of higher-level reasoning and lower-level reasoning. One motivation for this is to think about how we actually write one of these formal proofs in the formal proof assistant—sorry, informal proofs in the formal proof assistant.

Again, you don't have to worry too much about the details. Here we're trying to prove the statement on the left. What is notable here is that we can somewhat break this informal proof up into three different parts. It turns out that we can write this in the formal system using something that is structured analogously to the informal proof. This is a system called Isabelle, and it has this style where you can write these intermediate claims or conjectures. If you look at it, they somewhat line up with the different steps. Here, you have "10 times 280 divided by 40," which shows up here. This is a way of basically writing, "OK, let's complete the proof." The interesting thing here is that you still need to produce a proof of each one of these higher-level steps. That is shown in green. To actually come up with that proof, it doesn't really look like natural language at all. Here, it's actually calling this SMT solver and feeding it some facts. Interestingly, we have this decomposition between high-level and these lower-level things.

Let me say a bit more about how you produce these lower-level proofs in a system like Isabelle. There are these very powerful and very useful automation tools called hammers, or this one is called Sledgehammer. The basic idea is that as you're writing a proof as a human, you could write one of these conjectures, and then you could call the Sledgehammer. What it does under the covers is it calls out to external automated provers, ones that are based on first-order logic or higher-order logic or SMT solvers. These turn out to be extremely effective when you're using the system for filling in these little gaps of the proof. What these are not good at is, let's say you want to solve an IMO problem, then the Sledgehammer methods just won't succeed. The technical reason is that based on how they're set up, there is just such a large search space of possible proofs. In particular, if you think about this proof as being structured as these have statements, they don't really have the functionality to produce statements like that. In practice, these are used interactively as you're writing a proof as a human.

Here, the idea is we want to combine some kind of high-level prover with a lower-level prover like the Sledgehammer. This leads to our method, called Draft-Sketch-Prove, which we presented in ICLR in 2023 and developed and released in late 2022. The idea is to come up with this three-step procedure. First, we're going to draft an informal proof. Then we're going to translate it into this formal sketch. Then we're going to use a low-level prover to fill in the gaps of the sketch.

To show you this in more detail, let's walk through each one of these. First, we start off with an informal theorem and a formal theorem statement. It's actually really important to provide the system with the formal theorem statement because when we start to translate things, we want to make sure that the model's not cheating and it's actually proving the thing that we want it to prove. In the first step, we have the system—so it could be a human or it could be an LLM—generate some informal proof. In the next step, we have an LLM translate the informal proof into what's called a formal sketch. It's essentially what I showed you before, except now, we leave these gaps to be filled, which are the lower-level proofs. In the final stage, we try to close the gap with a lower-level prover. Here, we are actually using the Sledgehammer tool that I told you about. If you're able to close all of the gaps, then you're guaranteed to have a complete proof.

The interesting thing is that this actually looks a lot different than what I was showing you before, where we have this step-by-step proof search, and maybe it's a tree search. Now what we can do is diversify the search space by just generating many possible informal proofs or drafts, and/or generate many potential translations of those into formal sketches. Then we can just try to fill in the gaps. If the gaps are filled in any of the sketches, then we have a formal proof that we could consider as the output of the system.

Here's showing what happens as we scale up the number of calls to the pipeline. Here we're either generating many informal proof drafts, or in the case of using a human-written informal proof, we're generating many possible translations into the formal sketch. You can see on the left-hand side that if we just call the system once, then it actually proves some things, but it's not very good. We really see the benefits as we scale up the amount of proof search or sampling. Up here, we're proving a large number of problems. This is on the same miniF2F dataset. This was in the days of Codex and Minerva, if you remember those models. Interestingly, if you look on the very right-hand side, if we actually sampled enough from the model, then we were able to, right at the end, surpass the performance of using a human-written proof. This was kind of exciting, and probably, now that language models are much better, we would be able to make that transition even earlier.

Here's an example of proving an International Math Olympiad problem. The language model produces the informal proof, shown in green. Then the language model produces the formal sketch, shown in blue. You can see that here, we actually, back then, even had these informal thoughts in as comments, or here, it's showing the informal version of the setup. Then we have the Sledgehammer system fill in the gaps, shown in purple. Sometimes it's pretty non-trivial. Here, again, it's calling this SMT solver and feeding in a bunch of different facts or premises.

Since then, I've put together a tutorial for theorem proving. It goes over the step-by-step methods that I mentioned in the first part, and also has a notebook that shows you how to implement this Draft-Sketch-Prove method. In particular, it just calls out to a language model API, so you can try it out with any state-of-the-art model that you want to, if you're curious to learn more.

To recap, I talked about this method called Draft-Sketch-Prove, which generates these high-level sketches and then tries to fill in the gaps. What this relied on is, in this language Isabelle, having this lower-level prover called Sledgehammer.

Next, I'm going to talk about our very recent work on trying to build one of these Sledgehammer provers for Lean so that we could more easily write these sketch-based proofs in Lean. This is based on a paper that is still not public, called Premise Selection for a Lean Hammer, where we build a system called Lean Hammer.

What is a hammer? A hammer integrates, as I mentioned, these external provers. There's a bit of terminology here that is a bit tricky if you haven't seen this distinction before. Specifically, what it does is it integrates an automated theorem prover into an interactive theorem prover. An interactive theorem prover is like the one I showed you, like Lean. Essentially, it's a programming language that is able to check the proofs. However, the language, on its own, doesn't give you a way of automatically producing the proofs. You usually have to write them as a human. An automated theorem prover is something that tries to automatically find proofs. Traditionally, these are based on different kinds of logics. They might be based on higher-order logic provers or things like SMT solvers. As I mentioned before, these automated theorem provers really struggle with more complex proofs due to this large search space in their formalism.

What we can do to try to help with this problem is to solve this really important problem, called premise selection. Basically, you provide the automated theorem prover with many different facts: definitions, theorems, lemmas. Then it goes off and tries to prove the theorem that you want. If we look at the number of premises in, say, Lean's Mathlib, then it's extremely large—hundreds of thousands of premises. If you just give all of those to the automated theorem prover, it will really struggle because it has this really large search space. The basic idea of premise selection is to somehow cut down the number of premises that we give and only provide ones that are likely to be useful for proving the theorem.

We can look at what is the typical hammer pipeline. At a high level, let's say we're in Lean. We start off with the thing we want to prove on the left. We would pass it to some kind of premise selection system. Once we've selected premises like definitions and theorems, we have to translate it from Lean into the language of the automated theorem prover. Let's say that the automated theorem prover finds a complete proof. Then we have to actually translate it back into a Lean proof. The way that's done is through a step called proof reconstruction. It basically takes the output and tries to produce a proof in Lean. As our output, we would see this nice proof on the right.

Amazingly, there's been a lot of really cool work in the community on these different parts. Usually, they're developed somewhat independently of each other, but we have this translation layer called Lean-auto that can help translate from Lean's dependent type theory into higher-order logic. An example automated theorem prover is called zipper position. For proof reconstruction, this was developed by Josh Clune and collaborators, who's part of this Lean hammer project, and there's a system called Duper.

What we're interested in here is two different things. First, we want to try to solve this premise selection problem. Second, we have to come up with some way of actually combining these together. When I show it on a slide it looks simple to put them together, but it's actually not clear what the best way to do this is. Even more, we want to go beyond the standard pipeline, which I'll talk about in a bit.

First, let me talk about the premise selection. What we do is frame this as retrieval with a neural language model. In particular, we have a standard retrieval setup. We embed our input, which will be the goal that we want to prove, and we have a language model that produces this embedding. Then we embed all of our potential premises—this will include theorems, definitions, lemmas, from Mathlib, local premises in your project, or in the hypotheses that you're going to prove. With that, we get this list of embeddings. To select the premises, we take the dot product, use cosine similarity to select the set of highest-ranked premises. We train with this contrastive loss that is often used to train retrieval models. It boils down to collecting these inputs, the states, as well as some positive premises and negative premises. There's actually a lot of nuance in how to do this properly, especially when the output is going to be this Lean-auto—sorry, when the interface is with this Lean-auto system in the hammer pipeline. I won't go into all the details here, but even things like how and whether you simplify terms, how exactly you choose these positive premises and negative premises, turn out to have a large impact on the behavior of the retriever. We'll show in the results in a second that based on some previous retrievers that were trained, our system that we trained does a lot better when we put it into this hammer pipeline.

The question is, how do we combine all of these different pieces? What we did is not just have this linear picture that I showed on the previous slide, but we actually incorporate these components into a tree search. There's a really cool tree search method, this symbolic tree search called Aesop. It does this tree search over different tactics or steps of the proof. We wire it up so that it can use the selected premises to call different tactics in Lean, and it can, at any time, query this automated theorem prover using the selected premises. This step will involve the translation that I mentioned, as well as the proof reconstruction.

With all of these different parts that I've described, you could say, how is this actually useful as a user? It turns out that we can wrap this all into one simple command. At any step of a proof, you can just call this hammer tactic, and then it'll run this tree search that I mentioned, call the external prover and all that. If it succeeds, it will display the proof to you. What's cool about this is you could potentially enable this kind of sketch-based proving that I was talking about before.

I want to show an actual demo of the system where we took a human-written proof sketch from the textbook "Mathematics and Lean" and tried to fill in the gaps with the hammer system. This theorem is the irrationality of the square root of two. Sometimes in the formal system, you'll write it in a slightly different way. The details are not too important for the demo, but you can read through after if you want.

We'll go back into VS Code. Here is, first of all, the simple theorem that I showed you before. We can call this hammer. The first time you run it should take a few seconds. What it's done is it's selected the premises and called the tree search and all that. Now you can see that it came back with this proof, this theorem, or lemma. This one just says that if two divides m squared then two will divide m. It turns out that this one will actually be useful in this irrationality proof below. Now we can go step by step and try to fill in the gaps with the hammer. Here it got this one, and we can just keep going. Interestingly, up here, it actually used the lemma that we just proved. You can see "two divide of two divide square." Here it succeeded again. I'll just keep going for fun. The idea here is that by doing this, you don't necessarily have to worry about these different details. Here's a kind of interesting one where it's actually calling the automated theorem prover. You can see all of the different facts that are being passed to it. Here, we could see the actual call to Duper. Duper was this proof reconstruction method that I mentioned before. If you want, you could treat this as the proof, or you can just leave this one in there. For this step, it turns out you have to do this one step of clearing out this hypothesis, but then you can call the hammer. Just for fun, I'll finish off this proof just to show you that it's possible. Here's the last step, and we can call the hammer. OK, so now we have a complete proof of the theorem, and we started off with these high-level steps and were able to fill in the gaps.

Here's the summary. We could evaluate this with different premise selectors swapped in. Here are some results on theorems that we held out from Mathlib as a test set, and we just run the hammer at the first step of the proof. On the left, you can see what happens if you don't have the premise selection. Here is using a retriever that was from prior work. We retrained it on the exact same snapshot of Mathlib so it had up-to-date data. It's partially showing that the specific decisions that we make in training our retriever were really important for performance. Interestingly, there's a symbolic premise selection system, I believe based on the one that was in Isabelle Sledgehammer. Kim Morrison from the Lean community re-implemented it in Lean, and you can see that it actually does pretty decently when it's in our hammer pipeline. Here is the Lean hammer retriever as the fourth bar with the diagonal lines. Interestingly, it ends up being complementary to the symbolic MePO retriever. With these crosshatched lines, you get a performance boost if you just take the union of premises. In absolute terms, it's starting to get pretty good. All the way on the right is the performance that we get if we just use the ground truth premises that occur in the human proof. The performance is starting to approach the performance that we get if we use those.

To recap, I talked about two different approaches for combining informal and formal provers or even formal/informal proofs. I talked about Draft-Sketch-Prove, which involves drafting an informal proof, generating a formal sketch, and then filling in the low-level details. I talked about Lean hammer, which is trying to bring this paradigm to Lean or make it easier. In particular, we try to build this hammer, and it involves this neural premise selection and a tree search and calling out to the automated theorem prover. One interesting takeaway from this is—the largest model that we trained was just a 100 million parameter retriever, so very small. As you maybe saw in the demo, you can get some pretty non-trivial behavior or performance from integrating it in. It's kind of exciting because in these cases, even these fairly small, by today's standards, neural networks can be fairly powerful.

In the last part of the talk, I want to talk about trying to use AI or techniques that I've talked about to actually assist in research-level mathematics. Not just prove the IMO problems, but try to help out in these more complex projects that mathematicians, or if you're a Lean hobbyist, or interested in formalizing something difficult.

What does it actually look like to formalize research-level math? Here we can look at this blog post that was written by Terence Tao. He breaks down the steps that were involved in formalizing a proof from this paper, which is shown on the left. The goal was to take one of the main theorems from the paper and then try to write it in Lean and come up with a formal proof. The first step is to write what is called a blueprint. Here you go step by step and think about how this proof would decompose into different definitions, into different lemmas. Ultimately, we can come up with the kind of final theorem and proof that we care about. At the top, it's showing a dependency graph of all of these theorems and all of these definitions. It gets fairly complex. At the very bottom, you have the final theorem and final proof.

Interestingly here, everything is still written in informal language. You could see here the theorem and the proof just written in LaTeX. Then you try to think about, OK, how do we actually realize this in Lean? What it comes down to is a large set of these formal definitions and formal lemmas and formal theorems. Ultimately, you're able to state your final theorem and write the proof of it. At the end, you have this proof and this theorem that are really dependent on this graph of things that I showed you.

A question is, at what point of this procedure could AI help? Maybe, at least for now, we don't have it do this whole pipeline end to end. But we could think about, what is the simplest thing that we could have it do and that we could have trust in? One thing you could do is look at this blueprint and look at just one of these theorems or lemmas that is necessary for the proof, but is just one part of it. Here's an example where we have this lemma, which occurs somewhere in here. If we give this to a language model and it produces a proof, then, again, we can trust the output because Lean has checked it. In some sense, it's helped the development, because then the human proof writer doesn't have to go through this step.

In order to do this, we really have to overcome at least two different gaps that occur. The first I'll call the accessibility gap. This is just the simple observation that a lot of times these methods are developed, but then they're actually hard to integrate into actual tools. For example, the system might be very capable, but it might not be open source. Or the results might be very impressive, but then they're actually very expensive or costly to run. When you think about this practical setting, it adds constraints onto the methods that you have to develop.

Luckily, we actually do have some tools already available that could potentially be useful for these simple gaps. We developed one of them called LLM Lean. You could go download it and use it right now if you want. The basic idea is to just create this interface between Lean and some language model. It gives you these different commands or tactics, like one that queries a language model to try to produce the next step of the proof. We can actually check the outputs of the model, discard the incorrect ones, and just display ones that finish the proof. Those are shown in green. Blue ones are ones that don't give an error, but don't necessarily finish the proof. In particular, you can even nowadays have a model running on your laptop. There are different libraries that have been developed like Ollama or LM Studio, and you could basically host a language model on your laptop, and then use LLM Lean to communicate with it. Similarly, you can have it communicate with some API, such as OpenAI, Claude, Gemini, or together.ai. Now you can actually try to use these models to give you suggestions that are checked in Lean.

As the last demo, I wanted to just show you this. Let's go back to this really simple theorem that I showed at the beginning of the talk. You might have noticed that I imported this LLM Lean here. Now we could call this LLM step command that I showed. Actually, it comes up with this intro command that I showed you at the beginning of the talk. Then we could call it again, and in this case, it was able to finish the proof. Another thing you can do is you could say, try to just do this exact command, so try to prove it in one step. It actually came up with this bit more clever, one-step proof. There's another command that just tries to prove it end-to-end, like fully prove it instead of just predicting the next step. Here, the model comes up with multiple potential proofs, and you can see they're a bit different in style, maybe a bit longer. You can also do other fun things like shorten this proof, then put it as a comment and then call llmqed. Let's see what it does. Now it gives us some options that are shorter. Actually, this is pretty short. You could just prove it in one call. This is a tool that you could use today; you just need to have a language model API, and then you can import it into a Lean project.

Interestingly, let's see here, the methods are still bottlenecked, obviously, by how good the model is at proving theorems. But even now, I went through Terence Tao's project, this PFR project, and just picked a file. This one was actually able to fill in some of these small lemmas. This was actually the example that I showed you before on the previous slide.

Another thing is, I mentioned that the models right now are the bottleneck. We have the interface set up, and so now it just comes down to whether the model can complete theorems that occur in these kinds of projects. This leads to a second gap, which is the gap in the way that we benchmark these theorem proving models. Largely, these models are benchmarked on their ability to prove these math competition problems, like ones from the IMO. What these problems have is they're fairly self-contained. You can usually state the problem in just one line of code, and they're designed to somewhat use standard results. There's a standard set, and a lot of them have been implemented in Mathlib. Constructing the proof is very difficult and requires a lot of creativity, but it's relying on these standard results.

If we compare that to this project that I've been telling you about, these have these complex dependencies from throughout the project. In particular, this theorem, to even state it or to prove it, might actually rely on these brand new definitions and brand new lemmas that have been created for the project. Unless we continually train the model on all of the current projects, it really has to learn to prove theorems in this context-dependent setting that I'm showing on the right.

This leads to our new benchmark called miniCTX, or mini context. We'll also be presenting this at ICLR as an oral presentation. Here, what we do is we made this observation that I just told you about: when we look at these research-level or actual formalizations that people do in Lean, they are really in this context-dependent setting where you want to prove a theorem, and it depends on a whole repository of code, new definitions, or new auxiliary lemmas. The idea of the benchmark is to test models on actual real Lean projects. In particular, we choose some date cutoff, and then we look at a project like Mathlib. We can take all of the theorems that were developed after that date. Similarly, we can look at when a project was created and take theorems and contexts from those different projects. This PFR project by Terence Tao and collaborators that I've been telling you about is one of the projects that was in the first version of this benchmark.

The idea is we also make tools so that you can automatically extract benchmark problems from these projects. You can update the benchmark to stay ahead of the training cutoffs for these language models. We've actually already released a miniCTX version 2, and it has more recent Lean projects that have been developed in the past couple of months.

Here, we have two different types of contexts. These are basically what dependencies are needed to prove a theorem. We build tooling to automatically extract things like dependencies, and we can look at whether a theorem depends on things just in the current file or across multiple files. We can also get things like what are the set of available premises when you're going to prove this theorem, so a very realistic setting.

Let's look at two different settings. One setting is where all of the dependencies just appear in the current file, and the other is when the dependencies can span multiple files. The first thing we want to do is actually demonstrate that this context actually matters. Again, I've told you about this really common way of training theorem provers, which is by just giving them the proof state and then having them produce a next step, or having them produce thoughts if you want. Here, we developed this simple baseline method, which additionally gives the model all of the code in the file so far. This can include new definitions, new lemmas, even new notation, or natural language comments. We want to see how this compared to just taking in the proof state.

Interestingly, if you look at a competition problem dataset like miniF2F, it actually really doesn't matter. You can do pretty similarly whether you take in additional context—it would just be the import statements for miniF2F—or if you just take the proof state. This might be one reason why a lot of these state-of-the-art theorem proving methods don't really deal with or support any external context. However, when we evaluate them on our benchmark, miniCTX, which has theorems from real-world projects, we can see this dramatic gap in the performance. The reason is pretty obvious if you just look at a Lean file: in these real projects, you're defining these new definitions and new lemmas like I discussed. Interestingly, you can also do this premise selection and give the language model the premises. You can see variations based on whether the theorem requires just in-file information or auto-file information. All the way on the left, we have theorems with very low dependencies. The first set of bar graphs shows high dependencies on in-file premises. Here you could see that you do pretty well by just giving the language model all the code in the current file. In the third set of bar graphs, you see what happens when we have these cross-file dependencies. Adding in this premise selection leads to some improvement. Here, we're using an older premise selector system, so we're excited about integrating our Lean hammer into this for addressing the cross-file dependencies. In general, there's a wide space of methods that could be developed for this setting.

I should also mention that we provide the model with this file tuning. It's trained to take in the file context through Ollama. You can download it, run it on your laptop, and use it inside of Lean. All of the methods that are in LLM Lean that are calling the proprietary language models, or the API-based ones, will actually prompt the model with all of the code in the current file. As I mentioned, everything from the project is open source: the data, the models, our data extraction tool, and even the evaluation code, which is like a wrapper of the Lean REPL. You can check out those resources online.

As I mentioned in the third part, if we look at these real Lean projects or what it would take to formalize research-level math, it opens up some unique challenges. These aren't the only challenges, of course, but two that are really present in the research community right now. One is that we still have a gap between the AI advances and tools that people can use. Second, the benchmarks don't always reflect all of the capabilities that are required to do this real-world formalization. This is very much an open research direction, and as I mentioned, this benchmark is the first step.

So in summary, in this talk, I talked about three different ways to try to bridge informal and formal mathematical reasoning. I talked about incorporating informal thoughts by training models to think informally. I talked about incorporating informal provers or informal proofs through methods like sketch proofs, and trying to fill in the gaps. Towards the end, I talked about trying to take, say, a research paper and look at what is involved in producing a formal version of it and looking at what are some initial ways where AI can help in the process.

With that, that is all I have for today's talk. I'd like to thank all of the amazing collaborators on these various works. They're listed here. Again, my name is Sean, and I have my email address here, so please feel free to reach out.
