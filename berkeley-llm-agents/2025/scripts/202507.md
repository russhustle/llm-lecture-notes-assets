CAIMING XIONG: Hello, everyone. I'm Caiming Xiong. I'm very glad to get the invite to present in this multimodal agent area. I saw you already listened to the exact same area as multimodal agent last week. Hopefully, I will bring something new in this direction.

First, I want to show this plot to everyone. You can see how crazy it is. In the past five years, almost all the benchmarks have become saturated. Now, we have the last one, which is the one proposed this year: the humans' last exam. It seems like when we solve this one, we solve AGI. I'm not sure whether we can solve it this year, but I believe we'll have a much better result for this dataset with benchmarks by the end of this year.

OK, since we have such powerful frontier foundation models, what should we do with those models? How could we leverage them to make our world and society better? I think the answer is agents. We believe the multimodal AI agent is the next generation of applications. Why? Because we want agents not just reading text. We want agents who can hear, see, speak, and even feel. We want agents that can process a task like a human, because most of the tasks we do on computers involve multiple app interfaces.

Powered with the new advanced language model or large foundation model, which is vision-language-action models, it could make digital interactions more accessible, more automated, and increase human productivity.

So, what is the status of the multimodal agent now? How far is it? Actually, it's not far away. We already see a lot of multimodal agents being used in the real world, or some of them are used in research. For example, a coding agent. I think some of you, maybe a lot of you, are using coding assistant or agent products like Cursor, Replit, or Darwin in your daily work or research. Those agents can not only accept code text, but also let you upload images. They can generate code or fix your bug through the images you send.

Web agents are also multimodal agents that can help you or automate some control in your browser or apps. The same goes for mobile agents and even physical agents. Super amazing, right? This is a robot called Helix from Figure AI. Not only do you interact with a single robot, but even robots can collaborate together through these powerful foundation models.

All those agents, some are in production, some are still in research, and some are at the demo stage. I believe we are going to have more and more applied use cases of multimodal agents.

So, the question is, how are we going to develop them, what are the challenges for developing those multimodal agents, and what are the important components we should focus on?

This is today's agenda. I will talk about multimodal agents from these three areas. First is development benchmark. We need an interactive environment. We need this environment to be more reconfigurable, expandable, not just static benchmarks for multimodal agents. We need data. How do we get the data to train our multimodal agents? The third one is what model we should build on.

We interact with computers every day and use them to perform a lot of different tasks, including web browsing, editing personal videos, managing files, doing analyses, or coding to implement software. For those tasks, it's not just about interacting with a single app. It is a workflow that involves multiple applications through a GUI or CLI. With multiple multimodal agents, there is big potential to revolutionize the interaction between us and the computer environment. The agents can follow our high-level natural language instructions and automatically perform those interactions.

For example, in this image, in this slide, there is a task called "update the bookkeeping sheet with my recent transactions over the past few days in the provided folder." It is a very boring task. For this type of task, I think everyone would like to have an agent to help them process those tasks if they are available.

However, how can you trust those agents? We need to have a benchmark. Currently, there is a major challenge in developing those multimodal agents: we don't have a benchmark that depends on a real interactive environment, one that can cover more diverse and complex real-world computer use scenarios, and also support different operating system interfaces and apps. With this type of benchmark, after the agent passes that benchmark, we can trust this agent for our daily use.

Currently, we have some good benchmarks, but they do not have these properties I just mentioned. For example, Mind2Web is a really good benchmark. It includes a lot of real testing. However, for each test in Mind2Web, it has only single demonstrations without an executable environment. The assumption from Mind2Web is that for each task there is only a single solution, which is not 100% true. With that benchmark, it would penalize some alternative correct solutions.

WebArena is another good benchmark. It is an executable environment. However, it simplifies the observations and action space, covering only very limited tasks and applications or domains, like only web navigation on a few websites. So, it restricts the testing use cases for the agents and cannot be used to evaluate some open-ended tasks in complex real-world scenarios, which may require navigating between multiple different applications and interfaces in the open domain.

To address this gap between real-world use cases and model development, we introduce OSWorld. This is the first scalable and real computer environment for development and benchmarking of multimodal agents. Why? Because it is a real computer environment. It can be used for both development and benchmarking of multimodal agents as well as OSWorld for the first time. This development allows freedom from keyboard and mouse control of real computer applications. It's not a fixed evaluation set. You can configure your tasks and execute those tasks through this interactive way.

With OSWorld, you can evaluate open-ended computer tasks with arbitrary applications. It's not fixed. You can do anything from software development use cases to image editing use cases. It can also serve as a unified, real-world, real computer environment that allows users to define new agent tasks without building new domain-specific simulation environments. The OSWorld environment is more flexible and gives users more control for environment design and the scenarios they can use.

Let's see how we use OSWorld to create different tasks. For each task, you have a natural language task instruction. For example, you want to create a testing environment like updating the bookkeeping sheet. In OSWorld, they provide the initial state setup for this instruction. The setup is just creating a file. In this file, you include multiple components. The red part is configuring the initial tasks, like Excel files. You also need to add your post-processing and configurations, and where you can retrieve files and information. The yellow part is how you evaluate after you finish the task in the environment.

After you configure your initial state—the setup—OSWorld will initialize this environment with a virtual machine. The initialization will start to run in this virtual machine, and you get the initial state of the test environment. That's very convenient. For example, you can use this virtual machine to restore the task again and again, just to reuse the virtual machine. Because we use a virtual machine, it's a safe, isolated environment. We don't need to worry about irreversible damages when testing.

When the task environment is set up, you can start to run your agent to interact with this environment. For example, you can use GPT-4V to create an agent and interact with the environment, or you can use any other open-source model to build an agent. The agent will receive or observe different information provided by the environment. First, of course, is the user query, the natural language instruction from the user. It can also receive observations from the environment, such as screenshots, the accessibility tree, and some customized streams, maybe terminal outputs.

Here is an example: the left side is called a set of masks, which is a screenshot plus information extracted from the accessibility tree, where you can add labels in the image to provide more information, or just a screenshot. The right side is the DOM tree, which is the accessibility tree for websites.

After the agent receives those observations, not every agent needs all the observations. It depends on your agent. If you have a vision-based agent, maybe you only use the screenshot or SOM. If it is a text-based agent, maybe you only use the accessibility tree. It depends on what agent you're building or how it interacts with the environment.

After receiving those observations, the agent will—depending on how you create the agent—finally generate some actual executable actions, like clicking a position or dragging a box, etc., to manipulate the keyboard and mouse. Each of those actions will be represented using a code string generated by the agent. The simulator will execute those code strings in the virtual machine, and you will get new observations.

In OSWorld, we use the PyAutoGUI to control the mouse and keyboard. This is the library we use. This interaction loop will continue based on your instruction. It will repeat multiple steps until it terminates—maybe it's done, failed, or reached the maximum number of steps you set. In our experiment, we set it as 15 and 50.

Now you can set up the task and run your task. After the task stops, you get the results, then you will do evaluation on your task in OSWorld. OSWorld is different from other benchmarks. We implement this execution based on the reward function. When the final step reaches the goal, it will get a positive reward. Even if partially achieved, it will get a number between 0 and 1. If not, it's totally failed and will get 0.

To get this reward, we do not just use a single evaluation metric. To evaluate the successful execution of those tasks, we think there is no single way to evaluate the success or failure of a task. So, we make this evaluation more open. Open means you can construct the evaluation script yourself. That's really relevant because some tasks need retrieval, some need matching to evaluate. We use example-wise evaluation to design the evaluation metric for the tasks, not just a fixed one. It's open, so you can design different evaluation metrics for different tasks, facilitating diversity and making the evaluation more reliable, especially for complex, real-world, open-ended tasks.

We created this powerful OSWorld environment so people can use it to benchmark their multimodal agents. Besides building this environment, we also built a benchmark on top of OSWorld. Here, we introduce 369 real-world computer tasks that involve widely used web or desktop apps like Office, OS, daily use workflows, and professional tasks. Each task is based on real-world computer use cases and experience. We asked people with CSS backgrounds to interact with the initialized environment in OSWorld and interact with the environment. We call those the trajectories. We also handcrafted a lot of execution-based evaluation scripts to verify task completion.

On the left side, you can see the distribution of different tasks. In total, we have 369, and 100 are workflow tasks, which are multi-app, meaning the task needs to jump across multiple apps. There are 268, roughly 70%, which are single-app. Of those tasks, about 20% are from other datasets like WebArena. Another special aspect is we also add 30 tasks which are infeasible, because agents should also know how to say no to the user. We think adding infeasible tasks is important to make it more comprehensive for scenario testing. We also have a small number of 43 tasks for Windows, not just Ubuntu.

As I said, we think example-based evaluation scripts are very important for real-world evaluation of multimodal agents. We constructed 134 evaluation scripts for 369 samples, so you can see the ratio is very big.

Here are some examples we sampled from our 300 sets of our benchmark. You can see they are very diverse, the apps and the different user intents.

We compared our benchmark with current existing popular benchmarks from different perspectives, such as whether it provides a controllable executable environment, whether it's easy to scale to new environments, whether it supports multimodal, whether it can be cross-app tasks, or evaluate tasks from the intermediate initial state, and so on. We can see our benchmark covers different scenarios required as a benchmark for multimodal agents.

On the right side, we also did some analysis of how complicated the tasks in the OSWorld benchmark are. We compared with WebArena. The blue is WebArena and the yellow is OSWorld. The median completion time for WebArena is roughly 30-40 seconds. For OSWorld, the median completion time is roughly 100, maybe 110 seconds. There are also a significant number of examples that may need even more—900 seconds or more. This means the complexity of our benchmark is also higher than existing benchmarks.

After we created the benchmark, we tested it with state-of-the-art LLMs or VLMs from open-source and closed-source foundation models, like GPT-4, Gemini, Claude, etc. On the left side is the prompt we used. We designed four different testing settings based on different observations. One is only accessibility, which could be the MM can directly write. The second one is only screenshot, which is pure vision. The third one is screenshot plus accessibility. The fourth one is a set of masks. We did some simplification on accessibility because usually accessibility is very long and includes a lot of redundant information. We did some simplification to reduce and shorten the length of this text representation.

This is the result—the numbers we got. Let's do some explanation and analysis on this table. First, at the bottom of this table is human performance: roughly 70% to 75% success rate. If you look at the performance of state-of-the-art open-source and frontier closed-source models, it's still far behind—far from real human performance on those daily use tasks. It's not like these are super hard tasks; they're daily use tasks.

Second, you see from different scenarios, human performance is kind of stable, plus/minus 3% to 5%. But for those frontier models, the large foundation models, the performance changes dramatically. Even the model from the same company, for example, in a set of masks, the GPT-4V and GPT-4o, the performance gap is huge—10% gap, given the performance is only 20% or 10% cap. This table's result is from last year. This year, there are new models from OpenAI and Anthropic with big improvements, but still far away from human performance.

We did some analysis on what factors influence performance. First, we analyzed screenshot resolution. On the y-axis is success rate, and the x-axis is the downsampling rate. Most VLMs are still trained at small resolutions. We do some downsampling on the resolution and then run evaluation. In GPT-4V screenshot, as you improve the screenshot, you get better performance. But in SOM, the trend still improves, but there is some noise. It's not as clear as the screenshot. I think it's because SOM encoder has extra information from the accessibility tree, based on text information inside, so it decreases the impact of resolution.

We also analyzed how much historic context influences final performance. We chose different numbers of trajectory length. We found in GPT-4, SOM improves as you add more and more historical information, the stats. But in the screenshot one, it doesn't; it even makes it worse. We think maybe it's because SOM includes both information from text representation and accessibility. Our aim with the current foundation model is to perform better on text rather than the original image. So, we think that is the reason it's getting better and better with more and more context. But for the screenshot, it's getting worse. I think it's still because all the current foundation models are not well trained for computer use scenarios. So, it means there is still a gap. The open-source community or the closed-source companies need to improve the vision capabilities in the foundation model if we want to get a better multimodal agent for computer use.

On the right side, it's a dense distribution of the accessibility tree. Although you can add more and more nodes, text representation in the multimodal agent can get better performance. But they all have the issue that the accessibility tree is super long. Roughly, at the 90th percentile, it's 6k. If we want to add more of the history context, then we need to find a way to get a more simplified or concise accessibility tree. Maybe there is some work on how to select the relevant information in the accessibility tree before you feed it into the system. But when you do this step, maybe your system will be slow because you need this information, and maybe you need to train another big model to do this information processing, which adds extra latency.

We also analyzed model performance on Ubuntu and Windows. We found their correlation is pretty high, 0.7, which means we could use the OSWorld framework effectively. If you can test your model in one scenario, it should be effectively transferred to another environment.

We also did some ablation. For example, when we change the polarization size of the windows in a different app, on the screen, the results also show very different, which means that the foundation model is not very robust for this type of disturbance. Through this, we can find a lot of theories that have a lot of work we need to improve. The good news is, recently they have a lot of new progress in these areas. Anthropic has this new cloud 3.5 and also 3.7. We can see significant improvement from 14%, 15% to 22%. But still far away from human 72%. Of course, if you increase the number of steps from 15 to 50, it's getting better. But it'll still have a big gap. OpenAI also has this new model called operator, the COD. In their recent performance, we can see OpenAI is significantly better than the cloud 3.5. At 200 steps, they can get even 38%, compared to maybe only 15% four months ago. So it's improved super fast. But if you have 200 steps, most of the tasks in OSWorld can be solved in 15 or even 20 steps. So, which means they still have a big improvement, but they have big space to improve the multimodal agent for this computer use.

So this is through the environment and the benchmark discussion. We have made a lot of great progress on this computer use areas for multimodal agent. But still, they have a lot of stuff we need to improve. They still have a lot of opportunities there.

Now, after this, let's go to the data part, since we still need the data to train our model. To follow agent training, I think we have a bigger challenge than we do in the training. Why? Because the agent model is not a single step. It's a multi-step. Collecting the trajectory data is super expensive. And the second is, it's very hard directly to get the trajectory data by crawling the internet. Even if you say you have enough money, I can just hire people to collect data. But to do those annotations to calculate trajectory data, it's very time-consuming and limits scalability. Not good for the scaling law.

The cost and time-consuming nature of data collection through human annotation, and also there's no free large-scale trajectory purpose you can crawl. Then what should we do? Why don't we like the model? Here I want to discuss this paper called The Agenttrek, which synthesizes the agent trajectories through this guiding play way. A strategy with a web tutorial. So what does it mean? First, we know they have a large collection of tutorial-like text on the internet. Those texts provide step-by-step guidance on how to perform different tasks. We use those tutorials to learn how to interact with different apps and complete different tasks in our GUI environments.

Those tutorials cover different scenarios, so we could systematically do filtering or filtering those instruction contents from the internet. Of course, those data are not that clean because they're written for humans. So they have a lot of noise, text, descriptions, and of course, HTML formats there. We need to extract the valuable knowledge, the steps from those tutorial-like text and then transform those unstructured text into a structured agent trajectories with the definition or high-level description of the task that make it to allow the agent to use, to train, and mimic the human task completion.

In the agent trick, they have three stages to do this. The first stage is called the automatic tutorial collection from the internet. In this step, I just roughly describe these tracks and the filter loads tutorial data from the internet. You've seen a bunch of heuristic filtering and some classifier, like with the fasttext, and then transform it into some structured format with using ARM.

The step two is the guided replay for those trajectory generation, which is using VLM, vision language model agent, to execute those tutorials based on this structured tutorial—the structure steps—and run it step by step. Through that, we were able to collect the high-level trajectories, including the high-level description of the task, the observation of each step, the reasoning of each step, and the final actions. Given those data, we run a separate evaluator to evaluate to assess the quality of the trajectory to filter out some bad trajectories or low-quality ones to maintain to ensure the effectiveness of this data for the trainees.

The step three is we need to verify those agents by training a model and fine-tune for the agent models to demonstrate the improvements and the significant performance improvement across different benchmarks. That is roughly a step for three stages for the agent trick.

Let me describe these three steps. Let's go a little bit deeper about each step. The first step is that it's a tutorial collection from the internet. We start from some large corpus like Pajama, DCM. Then we did using some filtering with based on keywords or some structural features. After this step, we filter maybe 99% of data. For the remaining data, we extract a subset of the remaining data—the filtered data—and use the LLM to do some annotation. It is a good data is a bad data as a tutorial. After this annotation, we trained a fasttext classifier and use that one to classify all the filter data. When you get the tutorial data, we run another round of M to paraphrase those data to follow some predefined format that used for the guided or guided plan for the stage Ii.

This is the data flow for the step one, when you do the preparatory. You can see we use some heuristic filters to do the first step. The data filtering. And you can see that just after this 1, 2, 3—the sixth step, it will filter out more than 99.9% data. After you get this filtered data, we select a small number of the data and use the prompt ARM to classify it as a DIY tutorial or not. This is the prompt we use. After we do that, we also have the human to sample some of them to verify. We find that the F1 score is pretty high. It's like eight. They can get roughly 88%, 89% And we find, for some long text or the documents, the M is performed better than the human when they embed the tutorial in the long context, which is the normal, which is not surprise. So initially, we watch after the LLM on the long text classification.

Then after we filter those tutorial data, then we formalize the tutorial data into a specific structured format that we want to use for the model, for the guided replay. Here, we are organized. The left side is the prompt. We used the goal. We want to act through this prompt. We want to convert the tutorial into five parts. First is the platform and the target environment that specify what is the operating system, what software versions. So that will be used for the initial state. And second one is the task description to provide concise problem statement, which is the instruction. The third one is the prerequisites, which requires the dependencies, background knowledges. And fourth one is this step-by-step instructions, which include this reasoning guidance and the command syntax. And the last one is the expected outcome, which is used for the verification or success indicators.

After we get this, after we paraphrase the tutorial, then we get this structured tutorial. Then, we create a VM agent and fit into this step-by-step tutorial. The agent will start to run the interact with the environment step by step through this observation action loop. After the whole execution, we will record all the observation, action, intermediate reasoning, and the whole trajectories. Then, those will fit into another VM evaluator to assess the quality or correctness of the trajectory and ensure we got high-quality data.

Here is an example of the final version of the data after you doing all those steps. You could see the left side is the observation sequence, and right side including the action, the source, for each step and the action code for each step.

Comparing the data we collect using the agent trick with the prior works, the parallels, the data set people use to trained model. First, our data could be that the agent can collect much more data. Here we showed this 10k. 10k is not the a maximum. We just do some of them. It could be much bigger. And the second, complexity of the data is not low. Actually, it's pretty good. The average step is 12 steps. And you also will have a lot of the information. You will have HTML. You will have accessibility. You also will have reasoning your support. You could get videos, you can get screenshots, and you can cover different websites. So the Agenttrek trick is to maintain the diversity and the realism because it's from real world. And it's also adjusting the scalability issues that the human annotated data may have given the budget.

Here is the distribution of the trajectories. The left side is the different number of trajectories from different websites. And the right side is from the high level different vertical, different domains, what's the distribution in the data. So you can see how the coverage of the data set, the domains through in the Agenttrek. I mean to say that the data set created by the agency.

Like we said, we fine tune the model to track the performance to see how good this synthesized data comparing with other human annotated data in the benchmarks, to train the agent in the benchmarks. So we did it on this comparison on the WebArena data set benchmark. And the models trained on Agenttrek data significantly outperform both, I think, open-source and even close-source GPT 4o models, which demonstrate the synthesized trajectories. It's also high quality and it also can use to train the better model.

This is about how we do use the Agenttrek to do the data synthesis for the GUI agent. I think I'll take away from this Agenttrek work. It's leveraging the internet corpus, is still an important way because they have a diverse task resources. And the second is, with the reasoning and the reflection, it's very important-- the factor for the trajectory which we will talk about in the model side later. But however, they still have an issue here, because for each instruction, you still only get a single trajectory, which is an issue that, not like you only have a single solution for each query. It could have multiple instruction. So you still have this issue here, because it's only the imitation learning process. But I think we have the solution for it because it always work. So you can use Agenttrek to create a data for SFT training. And then you can use the high level instruction and run it in the OSWorld for the second stage the I/O train. And then, you can solve this exploitation exploration process.

So we try to answer the question like we can use the data synthesis to make the existing real vision language model, we perform better on the GUI agent tasks. So the next I want to discuss is, when you do data synthesis to improve this action core capability for agent of work external task? How about it could also help for the task itself, like understanding? We know the vision language model is trained for the understanding. And if we want to do it, can we crawl from web data? It's a website that have this type of data. I think the answer is, not really. Then how can we synthesize?

So first let me see why we need to do it? Why we want to synthesize this type of action data for multimodal LLM to understand? The understanding should be the capability or skills. It should have. But actually, it's not perfect, or it's far from perfect on the open-source model. So here is the four examples from the four different perspectives to explain why we need the model, who itself can do the action calling to improve understanding. All those tasks, either they need more fine grained vision capability, or they need some reasoning capabilities, or they need some external knowledge.

So here we introduce a TACO, which is through synthesize this Chain-of-Thought-and-Action data to improve the multimodal understanding capability through adding this action calling capabilities, skills in the model. So this is some examples, we showed after adding this action calling capability which is trained through the synthetic data we will discuss later. It can generate the thoughts and the actions through the observations. They will do again until they get the results. So this is the one example we just showed, it failed in this model. And this another one is called the localization and the functions, which are visual functions. And this is to query the external knowledge, the text knowledge, the knowledge observation model only. And this makes the region OCR and calculate the capabilities.

So let's go back, how we create the synthetic pipeline to generate this data for the CoTA? There is a lot of details here but don't worry. I'll explain them one by one. It's not that complicated. So first, for this data synthesis pipeline, they have two parts. First part is called the model based generation. And the bottom part is the programmatic generation. So in a model based generation, first we take the existing the image QA. We get those image and the QA pairs as input, and we prompt the frontier models. Let's say, you have this image and the QA pair that generate the train of thought and action. Given those action or called action list, or train of thought if they do not have the relevant action in the list, other then it will generate.

Now, after the generate below the train of thought with action or thought without action, then we will verify the outcome, the output. If it is correct, then we will pass it. If it's not, then we will convert this pair as the direct answer pairs. So in this way, you will get the data. Not all of them have the action. Some of them have action. Some of them only have train of thought, some of them without this direct answer. So that is this part-- the top part. And then the bottom part, we do the programatical generation. This way, we do not have the QA pairs in this programmatic generation process. So we only have the image. Then we use the model without maybe the detection model, depth model, OCR model, and different models. And also, it could be human to label some data, some box. And we get those dense annotations in the image. And we design some manual templates. And they use that to generate the QA pairs. And through that one we can synthesize what actions in the formula answer to question to answer.

So this is the template we use to do the programmatic generation. So some of them is about the content. Some of them is about the 2D spatial reasoning or 3D spatial reasoning. Some of them is the mixed understanding and the counting. So we try to mimic the different reasoning or the action call needed to answer different type of the questions. And then here, the bottom, is the action sets, which cover like OCR, get object localization and depth estimation, and club regular zoom in, and the query language model, accurate solve math equation is really pretty diverse. So we go through this two way. We collect the data. And of course in this data, we have some of them is the train of thought action data. Some of them is not. Then we need to do some analysis on the data and test on the benchmarks.

So first one, through our experiment, we found, to get the multi-modal foundation models, to have this reasoning or action core capabilities, you need to use fine tuning. In the result we found if few shot or in context learning, it doesn't work. And the second is, we finally have compelling for the tasks with this action. Adding this action capabilities is really to improve the capability to solve the task. It cannot solve before we improve the understanding capabilities, which means that even understanding this capability is also could be enhanced by adding external tools and make it through this function call or two calling to improve it. Which is not a surprise, but it's also interesting to see this positive results. So it can give us a different, another direction that are, how could we improve our models on understanding?

And this is the second observation. We find the best COT data. So the COT data, the best one, which can almost test on the different scenarios that you see, it has on this A, OK, VV, QA, Blink, maps with time MMU. It's almost improve all the tasks which means that adding this action capability in a data set, that also act to create this train of thought action data, it can improve with this data. To train the model to the multimodal model, it can improve the model understanding capability in general, not just improve one model, one benchmark.

And the third observation from the TACO experiments on the COT, the train of thought action data set, we find the quality is more important than quantity. Not just add more, you will get a better performance. So this paper is talk about how we could add in the action core capability into the multimodal language model or foundation model. And first you need to fine tune a model, not through the few shot. And second is, COT data is consistent to improve those models through this fine tuning on this instruction, this synthesized data. And the third one is the quality. It's more important than quantity, which is not new. It's some lessons we learned from here.

So here, we have discussed about the environment benchmarking and the data synthesis. I think now you already have the comprehensive view, holistic view about this multi-agent areas. If you want to test the agent, if you want to generate the data. And if you want to do the I/O in the environment, you already have those tools and you can start to create your multimodal.

So the last one, I want to spend some time talking about the model. Although, I'm not saying that this is the best model, but I think maybe you can learn from some insights from this model. I think so right now, I really have new models. So there's always have a better one. So I hope like we can discuss some different things about the model here. So I want to discuss this paper is Aguvis-- our paper, which is a unified pure vision model for the GI agent.

For the GI agents, we finally have three issues we want to solve. First is, most of GI agent is text based-- text representation like accessibility tree. Second, the visual grounding and performance capability is not good. I think it's because most of the model not really to train the model for the computer use. And third one is most of the model is to generate an action directly without reason. So here is some more concrete slides about this three observation or issues.

The first one, you can see, they have different the textual representation. Some like Web, HTML, maybe DOM 3. And in the OS, they have another type of accessibility and model have different ones. And most of them is super long, super lengthy. And it could be longer and longer as the complexity of your device. So comparing with the vision by pure vision-based, vision is just one. So whatever your background code looks like, the image is same.

So that is one and about the benefit of the GUI. The second one is, because of different system use different tools, the action space is very different. It's not a big issue, but it's still a issue that it will prevent this effective course development learning, and also that you cannot leverage different training data together, which not good for the scalability.

Then it's this visual grounding capability. So here we tested on the mobile, desktop, and web scenarios. We found that those close-source model performance surprising bad, I would say. So it means the model doesn't really optimize for the computer use scenarios. Then this is typically for the new one operator or cloud 3.5, 3.7 model. And here is an example, the existing methods, they train the agent to generate the low-level actions directly like this. The pi, the director of the code, rather than the sophisticated, the reasoning concept, reasoning the text, which would make to get those models or those agents very struggle on the complex scenarios in the real world, that may require more careful planning. Those planning knowledge could be transferred from the text corpus, which trained in the ARM before.

So given these three issues, we propose this Arguvis model. First, is the pure vision based. So it doesn't matter what is the accessibility tree format it's using in a system or in a web browser. And the second is, if we improve the grounding model through combined we defined a new unified form format. We combine all the existing data and train the model to improve the grounding. And the third one is, we explicitly to include this reasoning process. And what we call the inner monologue into our training and the model training for the action generation.

As I said, for the Arguvis, we eliminate this dependencies on the platform specific textual representation because it is pure vision based. It's directly operated on the screen images. So it makes the model more generalizable into different interface. And we did develop a standardized action space that we could combine the different data set into the same action format so that we can leverage more data-- the existing data. And the more crucial is-- the right side is-- we incorporate this inner monologue during the training, which can develop this sophisticated rhythm patterns, which through by leveraging the knowledge of memory patterns, is already memorized in the ARM. I think this is important to build more robust action model.

So in each interaction step our agent were to generate this in the monologue, the two step, two component, the two parts-- the salt and the low level instructions, between the visual observation and the final actions. So through this salt and low level instruction and the action, you can consider this. This enables the agent and do it step by step from the complexity. Because at the beginning you get a high level instruction and you're doing this train of thought. So you decompose the problem into some small steps. And with those steps, you can do generate this low-level instruction for each step about action. And then you can get a better concrete code to access question. So then it could make the agent be more robust, more accurate.

So that is the motivation. For the training we have two step. First step is the grounding and the second step is the planning and the reasoning. And for grounding, we did some tricks, which is grounding packing strategy. What do you mean grounding packing strategy? We want to find all the grounding pairs, the grounding samples, which based on the same image, and we concatenate them as a multi-turn grounding. As one sample, we found that can improve the training efficiency and even to have a big improvement on the performance.

And in the second stage is, we add this in the monologue in our training, which make the agent to generate this intermediate thoughts and the low level instructions before they generate action directly. In the data part, as I said, because it's a pure vision based, we can easily to combine the different data sets together and define same format for the outcome. Then we can leverage most of the existing data and create a new data set to train our model. And for the monologue we prompt the frontier model as a GPT 4o, Given the observation image and highlight UI element with the instruction, and the final goal, and the final action code, and the bountiful gift for all, to generate in the monologue component. So this is an example of pivotal previous is they have the forms, instruction, and previous action. Of course, the observation is we don't add here. And this action then later after this step, we will have the thought and the low-level instructions in a new training data.

So this way we create the Arguvis collection which is combined a number of different data sets. But then, we finally get for stage one. We get the one million the GUI grounding data. And for stage two we get the 35k multi-step tragectery, with this explicit monologue reasoning data-- it's great. And then, we do the same thing like we did before. We run our model. We train our model on this ancient Arguvis collection data set. And we demonstrate-- we show that this Arguvis model performs exceptional on the grounding task. So the top is the grounding task. So it has no plan. And we can see the significant output from the existing models on those tasks.

We also run the two stage training. So we also have the single model to both grounding and planning reasoning action. And we found that after you did the second stage training, it even improved this grounding task and achieve the state of the art performance. We also did the development evaluation on the offline setting on Matlab. It also showed it achieved superior performance across different metrics on the offline set and also on the Android setting. We also did this evaluation on the online setting, which is a Mind2Web and the Android world, which is this Mind2Web guide. With different Mind2Web, it provides a dynamic web based environment to do the evaluation. And we can see the performance of how the archivist models the superior performance. And even better than so.

And the one thing I want to emphasize is, if you see that here is the Arguvis 70 model with GPT 4o planar. It performed state of art in this tasks, which means that the Arguvis model have a better grounding capabilities. And this definitely can improve the final performance of this planar task. And also, we found they still have a big space for grinding to improve, to which will improve the final performance on the different computer use.

We do some analysis for different components in the Arguvis performance. Both stage one and stage two is important in the model. It's also very crucial for both the high level reasoning and the low level grounded tasks. And we find that, in the monologue, actually helps to solve the harder task, which in the right side, you see why is self plan wise in force plan. In force plan means, it must generate in the monologue. And it can achieve the 20% of resolve, 20% of the grinding errors. And the left, that is two examples with and without in the model. And we also see the cross-platform benefits in the Arguvis. The Arguvis is trained on the web and mobile data. OSWorld is a computer use. We find it remarkable, although we didn't try and exclusively on the computer use scenario, the model still perform pretty good. And with using Arguvis 70 to be as a grounding plus before, it's even better than how a computer use model.

So here, you can take away is, first-- the Arguvis model is a unified framework. It can perform pretty robust across different platforms with only visual observation. And I think the visual-- this pure vision paradigm-- have a big advantages on the computer use or GUI use case, which I think OpenAI already agree with. This is my comment because they have the operator. And the second is, although they have a lot of progress, we still need to improve both grounding and the structural reasoning. Don't just try to improve reasoning. Actually, they have bigger space. There you need to improve in the grounding side.
