It's a bit dense, but I think that this is probably one of the most interesting slides or analyses of this work.

So remember, we try to understand what exactly is going on during grokking. We said that the grokking process is when these generalizing circuits start to form, but in what exact way? That's still unclear. Through causal tracing, we can actually identify what exactly is going on during grokking.

Let's first focus on this figure. Let me first say this: we believe grokking is the phase transition from rote learning to generalization. Grokking starts when the model has overfit the training data. But at that point, the model has only done rote learning, so it just memorized all the training data, including the inferred facts, but not actually in a generalizable way. Because it has memorized all of them, the training loss will be zero, but it doesn't mean that it has captured this generalizing circuit.

Then the grokking process is essentially the phase transition from the initial rote learning to generalization. It's where this generalizing circuit gets formed. Now, let's analyze how exactly it gets formed.

The first thing we will look at is this figure. We look at two things. S, 5, r1 is this position. S stands for states, so this is a state of layer 5 at the position of r1. So it's this one, S5, r1. Then we can use logical dense to decode what's captured in that state representation, in that state.

You can see, during the grokking process—and similarly, we can do that for S, r5, r2, so this position—once grokking starts, the model already has, and this is MRR, the Mean Reciprocal Rank. Basically, it just tells you the ranking of the bridge NCP in the logic dense. If it's one, it means that the state here will always predict the bridge entity b, so that means that it has encoded this information.

So when the grokking starts, b is already there, the MRR equals 1. That means this state here always predicts the bridge entity b, which is what we want. We want the first half to get us the bridge entity. That is great. But remember, we also need the model to do a delayed processing of r2, so that at this point, it can combine b and r2 to predict the tail entity t.

But if you look at the MRR of r2 at this position, when grokking starts, it does not have r2 there. It has to be here, but it doesn't have r2 here. Despite that—this is on the training set—the model has a training loss of 0. The model can perfectly predict the tail entity, despite not using r2 here to combine with b. That means the model is just doing rote learning. It just memorizes that, hey, whenever I see h, r1, r2, then I will predict t, but it's not actually doing this staged thing like getting the bridge entity and then combining with r2 to use another atomic fact and do the reasoning.

But through the grokking process, you can see, the r2 gradually increases. At the end of grokking, it always predicts r2. Then you have t here, you have r2 here, then you can actually recall the atomic facts here to predict the t entity.

This is further corroborated by the causal tracing results here. Remember, through causal tracing, we can quantify the causal strength of each state to the final prediction. This is the causal strength of each state at the beginning of grokking, and this is after grokking. Now, we can calculate the difference between the two and find their difference. You can see, the difference mainly happens here at S5, r1. That means, in the beginning of grokking, even though the model has the bridge entity b here, it does not use it. It just did rote learning, so it directly predicts the t entity.

But the grokking process is the process of the model learning to use this bridge entity properly, giving it a stronger causal strength to the final prediction. This, combined with the fact that we just showed through logic dense that r2, the grokking process is where these r2 start to emerge. These, combined, validate the hypothesis that this grokking process is really the process of forming these generalized circuits, these staged circuits that we just showed for composition.

We can actually explain, even though we didn't do any proof of any sort, but you can roughly understand why this kind of grokking behavior happens through circuit efficiency and regularization. The generalizing circuits are much more efficient than the memorizing circuit, the circuit for rote learning. Because you also have regularization, the L2 regularization term, for example, if you just keep training even after overfitting, the regularization term will decrease. Then it will gradually favor the circuit with higher efficiency, which is the generalizing circuit.

That's why you find it still beneficial to train way beyond overfitting, because that's when the regularization starts to kick in and gets you the more efficient circuit.

At least let me give you the definition of planning, because that, in itself, is a very confusing thing for various reasons. In the context of language agents, we will work with this simplified definition of planning: given a goal, g, decide on the sequence of actions, a0 to an, that will lead to a state that will pass the goal test, g.

Of course, this is all very simplified because we don't talk about the state space, the observation space, and then the actions—they're not just atomic actions, they have preconditions that have to be met before they can be taken, and so on and so forth. But for this purpose, I think it's enough to have this definition.

With this definition, we can analyze these general trends in planning settings for language agents, compared with the classic planning setting, which has been studied for decades. Generally, I think the expressiveness in goal specification is drastically increasing. Now, we can express our goal in natural language as opposed to some formal language, which is usually much more limited in expressiveness. Also, we have a substantially expanded or open-ended action space. Instead of some very constrained action space, like, hey, you have a robot, you can move forward, you can turn left, and so on and so forth, now you can have an even open-ended action space. We will see some examples soon.

Because of those, it becomes increasingly hard to automate the goal test. Imagine you have a web agent that is just doing things on the web. A lot of the time, you simply cannot just write a goal test beforehand for what the goal state will be like. But that's fine, because fuzziness is really an inherent part of this world.

I'll skip this one. I just give some examples for web agents, from our Mind2Web. You'll see, in terms of the goal specification, the user can ask for anything on an arbitrary website. It's very broad in natural language. In terms of the action space, you have some broad actions, like you can type, you can click, you can drag, you can hover on some elements. But the actual actions, like what elements you click on, these actual actions are dynamically populated on each web page. If you go to a different web page, your action space will be different, and it's a very big action space. You have to discover it on the fly. Of course, the goal test is also very hard.

There's another example of trial planning that has some similar characteristics, but we'll skip here. I think these are some of the general trends for language agent planning. I think these are good trends. Yes, it makes things more challenging, but for the better, because now we can support much more realistic and useful application scenarios with these language agents.

Let's see what you all want to talk about with this, maybe very quickly. For people who are interested in web agents or computer user agents, I encourage you to look at these series of work: Mind2Web, the first LLM-based web agents; SeeAct, where we first introduced visual perception into web agents; and UGround, where, for the first time, we make a human-like embodiment for computer use agents. You only perceive the environment visually—there is no HTML or anything like that—and then you directly do pixel-level operations on the screen. This minimalist design actually works the best across the board. We will talk about WebDreamer, which is model-based planning for web agents.

So let's consider the different planning paradigms for language agents. The most common one is reactive planning or react. Imagine each node here is a web page. At each stage, you have several candidate actions you can take that will result in a different state. For reactive planning, you just, at each stage, observe the environment, reason about it, then make a decision and commit to the decision, and then that gets you to another state. You just keep doing this. It's fast, it's easy to implement, but the downside is that it's greedy and short-sighted, so you often find yourself stuck in some bad state, and there's no way out.

Naturally, when we talk about planning, we just think about search or tree search. For tree search, compared with reactive planning, you can do backtracking. You maintain a value assignment for the states on your search frontier. Then you explore the most promising branch. At some point, if you find that not promising, you can backtrack and then try to explore another branch. That gives you more systematic exploration. But the downside here is that in real-world environments like the internet, there are a lot of irreversible actions that make backtracking impossible or at least very hard. A lot of this exploration could also be unsafe or slow.

To show you how pervasive this state-changing and irreversible actions are in real-world environments, let's consider just amazon.com. On this single website, you can have dozens, if not hundreds, of these state-changing actions. You can place an order, make a return, create an account that you will agree to the terms and use (and that has legal implications), change your privacy settings, and so on. There is no universal magical Undo button that you can just try a bunch of things and then magically undo and go back to the initial state. That makes tree search in real-world environments hard. There are also safety and cost issues.

Ideally, we want to do model-based planning. Imagine you have a world model that at each stage, you can trigger the world model to simulate the outcome of each candidate action. That gives you a chance to evaluate the long-term value and the safety of each candidate action before you have to commit to it. Then you find the most promising candidate actions through simulation, then you commit to it, take it (assuming it's safe), and then you get to another stage, and you can do this all over again. It's faster and safer compared with tree search, and you can also still do systematic exploration. The downside is really how to get this magical world model.

Let's start with: what is a world model? This is another overloaded term. For our purpose here, we'll take this definition: it's a computational model of the environment transition dynamics. Basically, if I do this right now, what would happen next? Very simple.

If it's that simple and it's so good, why hasn't it been done for language agents yet? The issue here is, if you think about world models in the classic deep learning literature, that's usually studied in reinforcement learning (deep RL), where you have these simple simulated environments that you can do millions of times of trials, and then you can use that to learn a world model for that simple environment. What we're dealing with here is much more complicated. Even just for a single website, there could be hundreds of different web pages. On a single web page, there could be hundreds of different actions, and they can be constantly changing because the backend database is changing. This complexity quickly compounds if we consider that there are billions of other websites out there. In that sense, we need a generalist world model for the internet. That seems very hard.

Fortunately, we find that LLMs can actually reasonably predict these state transitions. In this example, if you ask an LLM, "If I click this icon, what will happen?" then it can recognize that, hey, this is a shirt, and this is probably a product. So the next state will probably be about some product details. Because it's a shirt, there will be sizing options and other things. LLMs have this common sense knowledge, are trained on the internet, so they can do a reasonable job—far from perfect, but reasonable.

WebDreamer leverages exactly that. We simulate a world model for the internet using an LLM, in this case, GPT-4o. When you're at a state, you have a few candidate actions. You can use the world model to simulate the outcome of each candidate action. You can even do multistep simulation. You have another value function, which is also simulated by LLM, to tell you how much progress you would have made going down that path. Then you can choose the highest valued action. You take that action, get to the next state, and then do this all over again. That's model-based planning for web agents. It's far from perfect, but it's a reasonable starting point.

In terms of results, we evaluated on VisualWebArena use. Model-based planning is more accurate than reactive planning and slightly trails tree search. But remember, tree search is only possible because we are working in a sandbox environment, which is VisualWebArena. If it's on real websites, then backtracking in tree search will become hard. Also, model-based planning is much cheaper and much faster.

So, the takeaways for planning: I think language agents are expanding into these new planning scenarios, characterized by expressive but fuzzy goal specifications, open-ended action spaces, and more difficult goal tests. The language for reasoning enables new planning abilities, like world models and model-based planning. We didn't talk about it today, but hierarchical planning and dynamic replanning are also very important.

The best planning strategy is dependent on the LLM. If you have a stronger base LLM, it may require less scaffolding, so a planning strategy could be more reactive. But generally, how to improve planning in LLMs is still largely an open question. Many people are trying the recipe for O1-style reasoning, trying to see whether that works for planning, but that's still a big open question.

I think we're really just standing at the dawn of a long journey. We talked about planning, reasoning, world models, and memory, but there are a lot of other things we didn't talk about, and there's a lot to be done.

Just a few immediate future directions I find interesting: I think memory, personalization, and continual learning—we are really just scratching the surface. There is a whole lot to be done. How to enable agents to continually learn from use and exploration? For reasoning, how to make O1- or R1-style reasoning work for language agents, where you need to deal with these fuzzy worlds without reliable rewards? How to integrate these external actions and environmental states? That's a big, open question, and I expect there will be a lot of study on this in 2025.

For planning, how to build better world models instead of just simulating one? How to balance reactive and model-based planning? You don't really want to do simulation at every single step—that's very costly. Even though humans can do simulation, we don't do that for every single decision, only for those difficult ones. How to balance reactive and model-based planning is still an open question. How to sustain a long horizon?

For safety, I think that's a very pressing issue that really keeps me up at night. The attack surface of language agents is scarily broad. For web agents, the attack surface is essentially the entire internet. Someone can embed something on a seemingly benign website. For example, OpenAI deep research agent can go there, visit there, and get tricked by it, and then maybe reveal your private information and things like that.

There are two general types of safety issues. There are endogenous risks—these are the safety risks originating from within, from the agent itself, usually because of the incompetency of the agent. It can mistakenly take some irreversible actions that do harm. Then there are exogenous risks—these are the risks from the external environment.

But there are also a lot of exciting applications. Probably the one with the most clear business case is agentic search or deep research. If you have not used [INAUDIBLE] Pro, or Google OpenAI deep research, I highly encourage you to try that. I think something big is being baked here. I think this will become a huge thing in 2025.

Also, workflow automation. Personally, I'm very excited about developing agents for sciences. These are all very exciting.

For a more comprehensive coverage on language agents, I encourage you to check out our tutorial with Diyi, Shunyu, and Tao. We just did it at EMNLP two months ago, and all the materials, like slides and videos, are available on our website.

I thank all of my sponsors, and am happy to take any questions.
