EfficientML.ai Lecture 10 - MCUNet and TinyML (MIT 6.5940, Fall 2024, Zoom Recording) - YouTube
https://www.youtube.com/watch?v=ePytVBpjxJA

Transcript:

Okay, welcome everybody, let's get started. Today we're going to talk about EfficientML.ai, Lecture 10, about MCUNet and TinyML. In this lecture, we are going to cover the following sections: What is TinyML? What are the challenges of TinyML? How do we design those tiny neural networks? What are the applications, including tiny vision, audio, time series, and also anomaly detection?

To give an overview of where we are so far: We talked about a lot of techniques to shrink and compress neural networks, including pruning, quantization, distillation, neural architecture search, and now we want to combine these different efforts together and build some cool applications, and really deploy them on small devices.

So, what is TinyML? Today's AI is pretty big. As we can see over the years, the model size, measured in billions of parameters, has grown super fast. But we need algorithms and hardware that are more efficient—efficient for inference, efficient for training—rather than taking a giant lot of resources for memory and compute. We want to fit them in our pockets, fit them in phones, and reduce the carbon footprint toward greener AI.

Recently, ChatGPT-3 actually contains 175 billion parameters. It takes 355 years of GPU time to train and costs more than $4 million—pretty expensive. AlphaGo beating Lee Sedol took 1,920 CPUs and 280 GPUs, which costs $3,000 per game just to play one game. In contrast, we hope deep learning can go tiny—not only on the cloud, but also on the edge, where we no longer have to transmit our personal data (either the image, text, or voice) to the cloud, which may suffer from attacks and privacy leaks. We want to keep everything local on the phone, and even on smaller devices like IoT devices.

For example, microcontrollers are everywhere in our lives. There are billions of IoT devices around the world based on microcontrollers, and you can see across the years the number of IoT devices is increasing. They have very low cost—just a few dollars—so everybody can afford it. We can democratize AI to these low-cost edge devices. Also, they are low power, so we don't have to worry about our battery getting drained, running out of battery, or if you go to a spaceship with no Wi-Fi connection and limited battery, you can run them at very low cost and low power, even using renewable energy. That would be super cool.

These TinyML applications have a lot of usage across different aspects, like smart home (to control different appliances), smart manufacturing (to do automatic defect detection), personalized healthcare (to adjust according to different patient symptoms), and also precision agriculture.

But it's not easy. TinyML is quite challenging. What makes it so challenging? The resource, primarily memory resource. For cloud AI, you can easily get 32 gigabytes of memory, but now you can easily get 80 gigabytes of memory—H100 GPU, which has petabytes, terabytes, or even petabytes of data storage to store the weights and data. A mobile device like our phones can easily get a couple of gigabytes of memory and hundreds of gigabytes of storage—that's plenty. But on those microcontrollers, the resource is tiny. You have a couple of hundred kilobytes of memory for the SRAM to hold those activations, and you have just maybe one megabyte of storage (flash). The flash is sometimes only readable, not writable. Only the SRAM is readable and writable. The resource is very tiny—four to five orders of magnitude smaller compared with cloud AI and also mobile AI. So we need to reduce both the weights and also the activations so that we can fit these deep neural networks into TinyML devices.

Compared with mobile AI, where we are constrained by the latency and energy, now we have a new constraint: memory. Since these tiny devices have very little memory, lots of the principles can actually be applied to large language models these days, since they are so big that even one GPU cannot accommodate all the weights. For example, 175 billion parameters, if you don't quantize them, that's 340 gigabytes of memory, while a GPU has less than 200 gigabytes of memory. So the same principles also apply to larger models.

Let's dive deeper to understand the challenge, especially the memory challenge of TinyML. What is the bottleneck for the weight memory? What is the bottleneck for activation memory? This is an overview of a convolutional neural network: you have a couple of convolutional layers, followed by fully connected layers. For each layer, we have an input activation, convolved with a 3x3 kernel, and produce this output activation. Where do we need to store them? Since we are constrained by memory, let's dive deeper: where are they stored?

Input activation and output activation—we need to both read and write. We read the previous input, write to the output of the next layer, so it has to be both readable and writable. It consumes the SRAM, which is both readable and writable (static random access memory, SRAM). For microcontrollers, they usually don't have any RAM, only some very little resource for flash, which is read-only. The weights are actually read-only if you don't do backpropagation (if you don't do training). The weights can be stored in the flash. Flash is different from SRAM: if you power off the device, data in SRAM will be gone. The flash can hold the data; it's non-volatile. SRAM is volatile—if you power off the device, the data in SRAM will be gone. You don't want the weights to be gone, so you have to store the weights in flash.

These are some popular mobile microcontroller devices, like Arduino. This device has 256 kilobytes of SRAM, 1 megabyte of flash. The STM32F746, which we use, has 320 kilobytes of SRAM and 1 megabyte of flash—so very small resources. The flash usage is roughly equal to the model size because they are static and have to be non-volatile; you need to hold the entire model. So the number of weights is constrained by the size of flash. We're going to revisit this later in the lecture. So weights have to fit in the flash since it is non-volatile, and SRAM usage is roughly equal to the input activation plus the output activation, since they are read and written dynamically during inference. We care about the peak SRAM usage, which means the largest layer size determines our peak memory usage. Weights are not counted here since they can be partially fetched—fetch them, compute, and throw them away.

What is the gap here? This is comparing ResNet-50 V2, MobileNetV2, and the memory constraint of a microcontroller (320 kilobytes). ResNet-50's peak usage is 23 times larger, MobileNetV2 is 22 times larger. Quantization, which we learned in the previous lecture, slightly reduces it by four times, which is very good, but still five times larger. We are going to learn in this lecture how to overcome this gap and be able to fit the model in a microcontroller while having similar accuracy as MobileNetV2.

What are the prior efforts? We learned about MobileNetV2, which is a very small model targeting mobile devices like mobile phones, but still that's too big. If you see the bottleneck, especially the activation bottleneck, it's very unfortunate that the activation didn't decrease but actually increased by 80%. It's very parameter efficient—if you compare here, ResNet-18 vs. MobileNetV2 0.75, the weight is reduced by more than four times, so that's pretty efficient. The weight is much smaller, so it takes less to communicate from the App Store to download the app, which is pretty good. However, the peak activation didn't decrease but increased, so that's a big problem we need to solve.

We are going to learn about MCUNet, which not only reduces the weights by six times compared with ResNet-18 (with similar accuracy), but also the peak activation is reduced by 3.4x compared with MobileNetV2. That's our goal: we have to balance both the number of parameters and also the peak activation. We have to fit both of them in the microcontroller. If you cannot fit any of them, you cannot fit the models.

Let's go through and formulate how we design these tiny neural networks, given the understanding of challenges and the concepts we learned so far. We'll introduce MCUNet, the system and algorithm co-design we developed (covered by MIT News a couple of years ago, which is very new).

What is system and algorithm co-design? We have a very large design space. We can either design the algorithm side by changing the neural network architecture given a fixed library (for example, we learned ProxylessNAS, designing a customized, specialized model given the hardware and the library). There's other work focusing on assuming the model stays the same, but we want to develop new libraries like TVM. What we find is that extreme efficiency comes from co-designing the neural architecture and the inference engine. You need to co-design both directions so that you can exploit and explore a larger design space, including the neural architecture (they propose efficient neural architecture) and also efficient compilers and runtime so that you can map the architecture on the inference engine.

The basic idea of MCUNet is system and algorithm co-design. In this lecture, we are going to cover the first part, which is TinyNAS—tiny neural architecture search: how to design automatically and optimize the model given the hardware resource constraint. In the next lecture, we are going to cover the second part: TinyEngine—efficient compiler and runtime, how to optimize the inference engine.

Let's start with TinyNAS, which has two stages. The first stage is neural architecture search, which we covered before. This is a short recap: what is design automation for neural networks? What is the motivation for that? Remember, in the previous lecture, rather than using a lot of engineers to hand-design the model given different hardware constraints (called manual architecture design, which uses a lot of human expertise and is pretty expensive), we want to use machine learning to automatically search for a good architecture that fits our demand. When we are doing the search, we need a good search space. Like finding your friend in a forest—the forest is pretty large. We want to first narrow down the possible areas, for example here or here, before we do the exploration. The quality of the search space largely determines the performance of the searched model.

The first stage of these two-stage TinyNAS is actually to search the space and find a good subspace so that it feels promising. Rather than exploring the entire search space, we want to explore a much smaller and constrained search space. How do we find that search space? One approach is to reuse the prior search space for MobileNet, for example. However, the constraint for mobile devices is quite different from these tiny microcontrollers—we differ by a couple of orders of magnitude. Even the smallest subnetwork in the mobile search space still cannot fit the hardware due to the huge gap. Mobile has 4GB of memory versus the microcontroller's 320 kilobytes of memory. Even the smallest network in the search space still cannot fit the microcontroller.

So what can we do? Only design a new search space targeting IoT devices. How do we do that? We can scale down the previous search space for MobileNet, using a different resolution and different width multiplier. For example, the original search space typical for mobile devices: the resolution is 224, and the width is 1.0, which is suitable for mobile phones. We can scale them up for GPUs; similarly, we can scale them down for microcontrollers. Then the question is: how much do we exactly scale them down? Different microcontrollers also have different memory budgets, from 256 kilobytes all the way to 512 kilobytes.

The overall structure will be: given a full search space, we want to add a memory and storage constraint, then produce the optimized search space, and then perform neural architecture search on top of it. The optimized search space includes two design knobs: the resolution and the width multiplier. After that, we narrow it down and then search the kernel sizes, expansion ratios, and also the number of blocks for each stage.

Here is the algorithm for automated search space optimization. We want to analyze the FLOP distribution. We have seen this figure before, but let's dive deeper today and analyze the FLOP distribution for satisfying models in each search space. The x-axis is the number of FLOPs in the search space, from 25 million FLOPs to 65 million FLOPs. The y-axis is cumulative probability. Different curves correspond to different search spaces. For example, the black search space: the width is 0.3 (30% width), the resolution is 160. The red search space: width is larger (0.5), resolution is smaller (144). In different search spaces, the FLOPs are very different. For example, in the red search space, all the models have FLOPs more than 41 million. In the black curve, all the models are larger than 28 million FLOPs. So, these two different design spaces can fit different amounts of FLOPs given the same memory budget. Since we learned that computation is cheap and memory is expensive, we want to maximize the amount of FLOPs we can work on with the same amount of memory footprint, the same amount of data movement. Fetch one byte of data, you want to do as much compute as possible after fetching that one byte.

A good design space can achieve high FLOPs—accommodate a lot of arithmetic operations under the same memory constraint—then it's likely to be a good design space. That's indeed the case. For example, the red space: best accuracy we get is 78.7%. For the black design space, the best accuracy we get is only 74%. This is a very helpful characteristic: without having to train the model, we can just analyze the FLOPs we can accommodate given different design spaces to project the final accuracy.

Let's analyze given different SRAM constraints. This is given different flash sizes, ranging from 512 kilobytes all the way to 2 megabytes. The x-axis is the flash, the y-axis is the RAM (from 192 kilobytes to 512 kilobytes). We want to understand: when the flash increases but the SRAM size stays the same, what happens for the width multiplier? The first blue figure is for the width multiplier (from 0.4 to 0.9), and the second image is showing resolution (some of them are 112, some are 208). When the flash increases, same SRAM, what happens for the width multiplier? The other question is: when SRAM increases (you have more SRAM in the controller), same amount of flash, what happens to the width multiplier and the resolution?

Let's see the answer. When flash increases but SRAM stays the same, remember what gets stored in the flash and what gets stored in the SRAM. Flash stores the weights (the kernel), and SRAM stores activations. When the flash increases, you can accommodate more weights, so you can have a larger number of channels. That's why the width multiplier increases from 0.4 to 0.8. What about the resolution? Since we have the same SRAM and the channels are getting larger, the resolution has to be smaller so that the channel times resolution squared will be the same. Otherwise, you cannot hold it in the same amount of SRAM. So, larger channel, smaller resolution.

What about when SRAM increases but the flash stays the same? SRAM stores your activations, but the flash stays the same, so the weights actually stay the same. The width roughly stays the same across different columns of the blue figure. Since SRAM gets larger, you can fit a larger feature map (higher resolution). Across the column dimension, you can see the resolution actually increases. This figure gives you some intuition about the intertwining relationship between SRAM and flash, activation and weight, channel and resolution.

Having figured out the design space, we can now perform the architecture search in this design space, just like trying to find your friend in the big forest. Now you finally determine, "Okay, my friend is in this area," then you can focus on this area to search for your friend in the forest. Here, we use the approach we learned in the previous lecture, now applied to do some real stuff. We train the super network, randomly sample different subnetworks, and jointly fine-tune the subnetworks, then repeat this process where the small child networks are nested in the larger ones. We train only once and can produce many different subnetworks.

By the way, Lab 3 is released today—neural architecture search part—so make sure you visit our website at efficient.ml to download the neural architecture search lab. The model searched by TinyNAS can actually better utilize the memory. This is before and after: you can see the peak memory for different layers is very imbalanced for the hand-designed MobileNetV2. The difference between the max and average is 2.2 times, so the largest layer determines the bottleneck. You have to have at least this amount of memory to hold this network. The TinyNAS-searched model has a smaller gap between the max and average; it's more balanced, versus a very big peak. TinyNAS can design networks with more uniform peak memory distribution for each block, allowing us to fit a larger model at the same amount of memory.

This is comparing MCUNet with different prior work: MCUNet, MobileNetV2, ProxylessNAS, and our winning solution in 2019. We made it 2.5 to 2.4 times faster (latency, lower is better). Here is the visual wake words accuracy (higher is better). MCUNet is faster, and here is the memory (RAM, main kilobytes), about 3.7 times smaller compared with MobileNetV2.

Let's revisit the problem of this imbalanced memory access, where the peak memory is actually the bottleneck, not the average. The SRAM usage is dynamic; we care about the peak. Roughly, we can find there are two regions: the first region is layers 0, 1, 2, 3, 4. This is memory consumption for each layer, ranging from 500 to 1,400. The first couple of layers use a lot of memory (very high bars), and the later blocks, as you downsample and the resolution gets smaller, the activation gets smaller and smaller. Here is the 256-kilobyte constraint of a microcontroller. From stage 0 to 4, they are all above this 256-kilobyte constraint, but after layer 5, they're all below this memory constraint, which means after layer 5 it can fit, but before layer 4, especially layer 2, is actually eight times larger compared with our constraint. Only the later blocks can fit, but the first couple of blocks have very high memory consumption and cannot fit.

If we can reduce the memory usage of the initial five stages, then we can reduce the overall memory. How do we save the memory? We can break the memory bottleneck by patch-based inference. Let's look at the original, conventional approach: here we have two layers, layer one and layer two. At each layer, we gather the whole input, produce the whole output, and then produce the whole output for the next layer. Every time, you have to finish calculating the whole output of a particular layer before you calculate the next layer.

What is the alternative? Rather than calculating everything, now we just calculate one quarter of the activation, then we can directly go to the second layer. We just calculate, like eating a pizza: if the pizza is too large, you cannot eat it, so you chop it into four pieces and eat a smaller pizza. Here, we have four smaller pizzas, and finally, we have to hold the entire output (assuming after a couple of downsample layers, this becomes a smaller pizza). This one has to be everything—we have to hold everything in memory. Before that, for these larger activations, we can divide them into several patches. We call that patch-by-patch inference, rather than per-layer inference.

What is the upper bound of the memory saving in this case? How many times of memory can you save? It depends on the number of patches. Here, we divide it into four patches, so the upper bound will be four times. If you divide it by three, the upper bound would be nine times. But the upper bound is not achievable because you have to store all the activations in the next layer (the final output) in memory.

Let's see what happens after applying this patch-based inference. By using patch-based inference, the peak memory is 1,372 kilobytes, but after applying patch-based inference, it is reduced to 172 kilobytes—a lot less. Pretty amazing that all these high bars (large memories) now suddenly get much smaller, even smaller than the 256-kilobyte threshold of the microcontroller. It's pretty amazing—finally, we can fit it.

Now, let's see the theoretical 4x saving for 2x2 patches and the 9x saving for 3x3 patches. What is the actual achieved saving? The highest bar corresponds to per-layer inference (300 kilobytes in this case). The middle part is per-patch (about 2x2 patches), so 315 now becomes 113—roughly 3x saving, not 4x, since that's the theoretical limit. If you use 3x3 patches, that becomes about five times—4.9 times smaller, reduced to only 64 kilobytes. Same observation for different neural network architectures, which is pretty good.

But is there any drawback or problem or overhead using such patch-based inference? Latency overhead—where does the latency overhead come from? Sequential rather than parallel patches. If the parallelism of the device is pretty big, now you limit the parallelism, but microcontrollers really have very low parallel instruction controllers. That limitation is for GPUs and CPUs, but overall the direction is right. There's latency overhead—where does the overhead come from? If you think about just one layer, this is just one layer output, this is the input. Convolution has a receptive field. If this is a 3x3 convolution, the output is 2x2. What is the effective receptive field? Here, you have a 3x3 convolution, moving by pixels, so the receptive field is 5x5. Everything in this yellow region will impact the yellow region here; everything in the blue region here will impact the blue here. They have no overlap at this point, but they have a one-pixel overlap here—repeated computation, redundant computation in the halo region. Even before that layer, the receptive field gets propagated and gets larger, and there's a 3x3 overlap (halo region). It's not completely independently divided, but they have overlap—the yellow region and blue region overlap by 3x3 (nine pixels). Such overlap causes computation overhead, and such spatial overlapping comes from the receptive field, leading to a larger computation overhead. How much is the overhead? We measure that and find it's about 10% overhead. Everything on the order of magnitude smaller can be ignored for first-order approximation, but actually, we can do optimization to remove that 10% overhead by redistribution.

How do we do that? Since the reason for this overlap is due to the larger receptive field (from 2x2 to 5x5, 7x7, you have a larger receptive field), what if we limit or reduce the receptive field? That will reduce the area of the halo overlap region. Actually, we can redistribute some layers: the first couple of layers use patch-based inference, and later layers use the normal per-layer inference. Remember, in the figure, only the first couple of layers suffer from pretty big activations, but later layers can fit the memory already, so we don't need to apply patch-based inference in the later stage, only in the early stage. Since the receptive field is causing the memory overhead, let's reduce some of the receptive field. We can change the 3x3 convolution into a 1x1 convolution—smaller kernel size means smaller receptive field. We can also remove one 3x3 layer to reduce the receptive field by two, but the total receptive field needs to remain the same, so we can compensate by redistributing the receptive field from the first stage to the later stage. For example, we add one 3x3 convolution layer to compensate so that the final receptive field stays the same. We use this network redistribution to reduce this overhead, and finally, the number of MACs using patch-based inference becomes the same as before. Although we changed the architecture, the receptive field didn't change, and after training, the accuracy is very well maintained.

Finally, we can apply this patch-based inference into the search space and jointly optimize the neural architecture and inference scheduling methods. The new architecture includes the number of layers, number of channels, kernel size, resolution, and scheduling (whether we use patch-based scheduling and how many patches we use, like 2x2 or 3x3). You can see there's diminishing return—how many layers do we use this patch-based inference, and also other knobs in TinyEngine, which we are going to cover in the next lecture.

Putting everything together, we can further improve the visual wake words accuracy under only 32 kilobytes of SRAM. What is visual wake words? Basically, detecting whether there is a person or no person in the field. This is the improvement across years: the x-axis is the measured peak SRAM usage (smaller is better), the y-axis is the visual wake words accuracy. This is our 2019 result, this is 2020, this is 2021. Every year, we are pushing the frontier by a total of four times smaller, like from MCUNet (version one) to version two. MCUNetV2 introduced this patch-based inference using 2x2 patches—we can save about 4x the memory consumption, or using the same memory (18 kilobytes) but achieve 4% better accuracy. Look at this model: only 30 kilobytes of SRAM, but achieving 90% help accuracy using patch-based inference. At the same accuracy, 9% is reduced by 4x. Not only on that binary person vs. no person classification, but MCUNetV2 also advances classification and detection to large scale, like ImageNet-level classification results. This is the accuracy improvement of MCUNetV2 vs. MCUNetV1, 256k SRAM vs. 320k SRAM—quite a big improvement. Given the same memory, now we can fit a larger model, larger activation, so that the accuracy is higher. This is MCUNetV2 plus CMSIS-NN, which is a reference library from ARM. This is MCUNetV2, this is the object detection accuracy—also a quite big, double-digit improvement from MCUNetV1 to V2.

Let's take a five-minute break before we dive into details to dissect MCUNetV2.

All right, welcome back. Let's continue to discuss and dissect the MCUNetV2 architecture to understand some of the design principles and see that this kind of automated design actually has lots of insights. For example, the kernel size in the patch-based stage (the early stage, where the resolution is pretty big and the activation is pretty large, so we have to use patch-based inference to fit it in the memory)—the kernel size is actually chosen to be pretty small. That's a decision made by our neural architecture search algorithm to reduce the spatial overlapping. The smaller the overlap, the smaller the halo, the less overhead of compute, which makes a lot of sense. In the middle stage, the expansion ratio is actually small to reduce the peak memory, because here the downsample layer is not too many, so the resolution is relatively big (not as big as the early stage, but much bigger than the later stage). The expansion ratio in the middle stage is small to reduce peak memory, and the expansion ratio in the later layer is getting much larger (wider), because the resolution after a lot of downsampling is already pretty small, so you can fit a much larger width. Also, we find we can fit a larger input resolution for those resolution-sensitive datasets like visual wake words—make sure you have a larger resolution, so even a smaller person far away can still be detected, versus our last generation MCUNetV1, which could only support up to 128, now we can support 160.

Having learned about how to search and design tiny models, let's try to visit and see what kind of applications we can build, including tiny vision, tiny audio, time series, and anomaly detection.

Starting with the simplest: tiny image classification. This is a demo from Google, showing the microcontroller can do very simple classification between oranges, bananas, and apples. Here is the probability—actually, this is super easy, you don't even need a neural network, you can just look at the colors (RGB) to distinguish different fruits. What gets more challenging is to expand it to large scale, real-life applications. MCUNet can actually achieve that—more than 70% top-1 accuracy. This is the level of MobileNet, which used to require a mobile phone to do such a task, but now we can fit it into a couple-of-dollar microcontroller, first to achieve more than 70% ImageNet accuracy on commercial microcontrollers. Compared with prior work like MobileNetV2, which can only achieve 53%.

Another application is that there are different constraints of microcontrollers—some have 256 kilobytes, some have 2 megabytes of flash, some have 1 megabyte of flash. It's very hard to design different models for different weights and constraints, but thanks to the once-for-all network, we can just train a single model and then specialize according to the constraints for the activation and weights, just training once. We are going to explore such automated search using our homework in Lab 3, which is released today, so feel free to try it out.

Another application for image classification is visual wake words. What is visual wake words? To detect if there is a person or not—it's just background. We don't have to trigger the second pipeline stage by recognizing who it is. We can just keep it idle. The second stage might be a much larger model, since it's more complicated to see who it is, but the first stage can be done in a very small model. Only if there is a person, then the second stage (face recognition) will be triggered; otherwise, we don't trigger that, to preserve the battery and save privacy.

Here is the demo we shot in building 38 in the elevator. This is my postdoc. We have this microcontroller right here as a light: if it is red, it shows there is a person; if it's green, it shows there's no person, recognizing them on the microcontroller. We also have a demo from our lab, and you can distribute the demo across everybody to take a look. We just place it in front of a person to see the lights: if there is a person in front of it, it'll be red; otherwise, it should be green. The microcontroller without a screen is the visual wake words device.

We can also do tiny object detection. Using this patch-based inference, the challenge is that to do object detection, you really need a pretty high resolution, and resolution means we need a larger activation. Larger activation means higher SRAM consumption, which is challenging, and resolution matters a lot for the accuracy. For detection to work, it's actually very sensitive to the resolution. If the resolution gets smaller, the accuracy drops very sharply. For patch-based inference, it's very helpful to maintain the accuracy.

Here's a demo for object detection: this is face and mask detection, this is person detection. You can see, here a person comes, and here the mask is green, then this has no mask, so it's red. This project was done during COVID, so you can tell from the demo when the project was done—we had a lot of masks at that time in the lab.

What is the advantage of using such patch-based inference? Without using patch-based inference, this boy was missing—the detection was missing. But using patch-based inference, he can be detected. Here, there's a misprediction on the hand, but there's no misprediction where the man gets lost with the pink box because the face was outside the patch, and now we can detect it using MCUNetV2. Here is a misprediction, here is no misprediction. So, high resolution helps a lot. Patch-based methods allow for larger input resolution, which helps a lot with detection.

We can even do on-device training to learn new tasks on the microcontroller. Here is a demo: we have two buttons on this microcontroller, a red button and a green button. We push the green button for orange, we push the red button for banana, and then do it a couple of times to give it a few labels. Here, it's doing the backpropagation locally on the device, including the gradient, activations, and weights—we have to fit everything in the microcontroller. After a while, it can distinguish the banana versus the apple. We are going to learn such techniques in the later part of this lecture when we talk about on-device training.

That's tiny vision tasks. We can also apply it to tiny audio tasks. Audio is a very common application in our daily lives: we can do audio keyword spotting, like "Hey Alexa," "OK Google," etc., for speech recognition, for noise cancellation. Actually, we can turn this audio problem into a visual problem, because we can convert the speech signal into a 2D feature map. The x-axis is the time dimension, the vertical dimension is the frequency dimension, and the strength indicates the power spectrum on different frequencies. We can do convolution on the 2D feature map—it's no longer one-dimensional, it actually becomes two-dimensional. We can run those classification problems similar to image recognition to understand the speech (e.g., "yes," "no," "on," "off," etc.). You can outperform the fully connected layer using convolutional layers, since there's locality—relationship between adjacent time and adjacent frequency. The signal has relationships, so we want to capture those local relationships by convolution.

Here we can see the forced rejection on CMSIS-NN—the model is really better than the fully connected layers, and MCUNet can also improve the efficiency on the speech command dataset. You have a lot of speech commands, and we want low latency and high accuracy. This is comparing MobileNetV2, ProxylessNAS, and MCUNet, which is on the Pareto front here.

Finally, we have tiny time series anomaly detection, widely used in factories and also applied to video surveillance and healthcare to prevent fraud and detect abnormal products. Here we can see: this is a normal capsule, this is the abnormal one; this is a normal nut, abnormal nut. We want to use machine vision to detect those different anomalies and also analyze very long videos to see if something abnormal happens. Recently, with the advancement of large language models, especially visual language models, there are alternatives to that. We tried—we're going to cover this later in the lecture, but we want to give you a peek. Just put these two nuts and feed them to our visual language model called LLaVA—it has seven billion parameters and can do a lot of stuff. Here, we ask it: the first image is normal, it's a normal nut; what is abnormal for the second image? It can tell the second image is abnormal because it shows a nut with a hole in it. It can generalize quite well, understanding images and language. The technology is evolving pretty fast—just within two years, I made this slide, and now there are a lot of changes in technique with the advancement of large language models, which will be our focus for the second half of this lecture.

Before large language models, how was this done to detect anomaly? With autoencoders. Autoencoder is a very key technique. Given an input signal, it first passes through an encoder and is projected into a low-dimensional code vector. The input may be of high dimension, like a high-resolution image, but here it has a very low dimension, which is an abstraction of the input. Finally, it passes through a trained decoder. The decoder takes this low-dimensional vector as input and tries to reconstruct the output to be the same as the input. That's the structure of an autoencoder: to predict the input. Ideally, the reconstructed output should be the same as the input. During training, we try to minimize the reconstruction error. This is the input, this is the reconstructed output, which was reconstructed using this low-dimensional code vector. If the input follows the desired distribution, then the difference between the input and output will be pretty small. Otherwise, if the input doesn't follow the training data distribution (like abnormal data appears in the input), the reconstruction error will be high, because in the training data it does not look like this, so you cannot reconstruct an abnormal input data. Therefore, the reconstruction error will be high. But if the input image follows the normal distribution, which has a lot of such images in the training data, the autoencoder will be able to reconstruct the output, so reconstruction error will be low for normal data and high for abnormal data. We can use such autoencoders to deal with many different signals, including images and also sound and audio.

Here is a demo to detect anomaly for fans. We purchased the fan, and here we have a microcontroller attached to the fan, which has 256 kilobytes of RAM, only 1 megabyte of flash, only 64 megahertz. We apply anomaly detection here, and when we are picking the fan using a screwdriver, we can see later the light is going to change color—a yellow light is going up right here as we poke the fan, creating some noise. After a while, the yellow light will disappear, and if we shake it, the yellow light will appear again, since we have a gyroscope and accelerometer in the fan. If you shake it, the yellow light will light up—it's pretty small.

This is the summary of today's lecture. We introduced TinyML concepts and the challenges of TinyML, and also how we design models that can fit tiny devices and apply them to our daily lives, including vision tasks, audio tasks, and also time series tasks.

Finally, let's see some of the demos in our AI TA, which we introduced in the last lecture. Let's see some of the demos—the website is actually m.study, and it's catered for our course. We can ask some questions, like if you're not clear about some concepts, like how to perform model distillation (which we learned in the last lecture). We are using the RAG technique to do the embedding of our lecture slides, lecture notes, and also videos. We concatenate the question with our prompt and feed it to the large language model, and then it can give us a very detailed answer. What I like about it is you can actually click the link to watch the corresponding lecture video. You can also click the corresponding link to find the textbook which describes this particular concept. What is interesting is you can test yourself by creating quizzes. For example, "Create a quiz about neural networks." Let's see what it creates: "What is the primary goal of increasing the model size? Reduce model's computation complexity, enhance model's accuracy, increase number of neurons..." Seems pretty simple.

We can also ask some complicated concepts, like how to—maybe you guys can ask a question you want to ask in Piazza. Let's find here—somebody want to give it a shot? If the activation is larger than the patch size, how to perform patch-based inference if the object is larger than the patch size? Doesn't quite make sense. You mean the object is larger than the patch size? Oh, then maybe one object will be across two different patches. That's a question there. Okay, I got it. Let's see: how to perform patch-based inference if the object is larger than the patch size?

Divide the input image into smaller, manageable patches. Each patch is processed separately through the model. This reduces memory consumption and allows inference on devices with limited resources, such as microcontrollers. We talk about receptive field distribution—that's exactly what we discussed. Overlap handling—that's the general method. And finally, do nearest neighbor search. I think this is very general, but it didn't give you a very specific solution. In this case, I would be able to vote because it's not specific.

All right, so that's maybe one candidate for you to think about for final projects. Building something like this will be very doable for the final project. Feel free to ask, but we are always here to help you, so consider us as the final source of help.

All right, that's all for today's lecture. Thank you.
