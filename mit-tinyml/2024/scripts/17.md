(90) EfficientML.ai Lecture 17 - GAN, Video, Point Cloud (Zoom Recording) (MIT 6.5940, Fall 2024) - YouTube
https://www.youtube.com/watch?v=g24LzAIZbTA

Transcript:
Good afternoon everyone, let's get started. Today we are going to talk about efficient GANs, videos, and point cloud understanding. We have talked a lot about general-purpose application optimizations, so today we are going to talk about domain-specific application optimizations, focusing on GANs for image generation, video understanding, and point cloud.

Why do we choose these applications? On one hand, they are very computationally heavy and require a lot of compute. On the other hand, they have a lot of redundancy. For example, GANs have 2D spatial redundancy. For videos, there's temporal redundancy between the frames—frames are similar, so it's very wasteful to use 3D convolution for that. Also, for point clouds, there is spatial sparsity—not every point is actually occupied, so there is a lot of room for efficiency improvement.

This is the agenda for today: we will first introduce efficient GANs, including three techniques: GAN compression (how to compress GAN models), and cost-G (how to apply this once-for-all technique to use a smaller GAN model for quick editing and prototyping, and finally using a larger one for finalization), and finally data efficiency (how do we use fewer images to train the GAN models—differentiable augmentation for data-efficient GAN training). The second chapter will be about efficient video understanding, particularly the TSM (temporal shift module). Finally, we will talk about efficient point cloud understanding, including PointVoxel CNN and also BEV Fusion.

Let's start with the first part: efficient GANs. GAN stands for Generative Adversarial Networks. It's a super popular technique for image generation. It consists of two parts: one is a generator, the other is a discriminator. We train the generator so that samples cannot be distinguished between the generated image versus real image. For the discriminator, the training purpose is to make sure it can distinguish between the real image versus the fake. Therefore, there are two loss terms.

To accelerate inference, which should we improve for efficiency? Should we compress the generator or should we compress the discriminator? At generation time, only the generator will be used—the discriminator will not be used at inference time. So the focus of compression will be the generator part. This is what we need to speed up.

This is a little bit of background of GANs. This is learning to generate digits from MNIST, starting from random initialization and finally generating those photorealistic images for handwritten digits. GAN has two component categories: one is unconditional GAN, the other is conditional GAN. For unconditional GAN, we use random noise to feed the GAN model's generator to generate the output. For conditional GAN, both the discriminator and the generator are conditioned and provided labels—either class labels or segmentation masks. This is the difference between conditional versus unconditional GAN.

This is a famous work, GauGAN from NVIDIA a couple of years ago, for drawing. You draw a stroke and on the right-hand side it can render an image. You draw some mountains, and then it will fill the mountains and change the color to be grass and get a river out of it. Such tasks require real-time processing, but GAN is pretty heavy.

This is another example of a diffusion model for image editing. Here we added a dog. We're going to talk more about diffusion models in the next lecture. Although GAN is pretty powerful, such generative models are much more computationally expensive than recognition models. For example, here we see the number of parameters: MobileNet ResNet-50—these are discriminative models—have 4.2 to 25 million parameters, but GauGAN is like 93 million parameters. The FLOPs are below 4 GFLOPs versus 200 GFLOPs for GANs, so it's pretty computationally expensive, which motivates us to compress these GAN models.

Let's see how we can apply what we learned so far to GAN compression. Here we have a pretty large generator, and we want to compress it to be smaller by reducing the number of channels. Here we try to apply three losses: one is the reconstruction loss. For example, if we want to turn a horse into a zebra, we want to calculate the loss pixel-wise for each pixel to make sure it looks like a zebra. We also have a distillation loss. Since the final reconstruction loss at the end is not enough, remember during distillation we talked about matching the intermediate feature maps. The feature map channel number is different, so we have to learn this one-by-one convolution, this projector, to project between a large number of channels and a smaller number of channels. That's the distillation loss in the middle. Finally, there's a conditional GAN loss—the original GAN loss.

Finally, we put together these three loss terms. Here we can use the width-wise approach between supernetworks, which is similar to what we did in Lab 3, by changing the number of channels from small to large and randomly selecting the number of channels, then fine-tuning to recover the accuracy. Finally, we can perform neural architecture search to select the number of channels that fit our latency requirement.

These are the results: the pix2pix models. Given input strokes, these are the ground truth images. This is the original model, and this is the GAN compression, making it 11 times smaller. The quality is comparable with the baseline. On the right-hand side is horse-to-zebra: input is a horse, output is a zebra. This is the GAN compression, compressed by 21 times, and the output is still roughly the same, which is pretty cool.

Here are some more examples. Compressing GauGAN: given the segmentation input, this is the ground truth, this is the original GauGAN model, this is the reconstructed one. This is a video demo showing the speed of horse-to-zebra before and after compression. This is running on Jetson GPU mobile, pretty small. Originally it's very slow, running about 12 frames per second. On the bottom, now it can run 40 frames per second, much smoother, turning a horse into a zebra and running real-time on mobile GPU.

That's GAN compression. Now let's switch gears to talk about Anycost GAN for image editing. The motivation is that GAN can be used for image editing, but the generation is pretty slow, making it difficult for us to run this image editing interactively on our laptops, tablets, etc. The algorithm here is: given an input image, we want to project it into a hidden space, into a latent vector, and then we feed the point in latent space as input to the generator to generate the image. By tuning different knobs in the hidden space, we can control things like smiling, hair, glasses, etc. We can apply image editing by changing the latent space. However, this is pretty slow, which motivates us to find a way for very quick image editing.

How can we do that? The ray tracing example motivated us. People can get very fast previews by reducing the number of sample rays. For example, this is the path tracing: 512, 128, 64. Although the visual quality looks worse, it's pretty quick. Here, we want to use less compute but also make the quality hopefully as good as the full model. Can we do the same or even better for GANs? That motivated us to think about the once-for-all network. During editing, we can run a low-cost, smaller subnetwork. We have a supernetwork, then we have subnetworks. During editing, we can run a smaller subnetwork at low cost, just as you did in Lab 3, for very quick review. After we are satisfied with our editing, we can run the full-cost, full model. It's slow, but gets higher quality output. This is the final image, and hopefully the finalized image versus preview are very similar and consistent with each other. That's our goal.

Let's see how we can achieve that. In order to specify, let's specify what dimensions we want it to be once-for-all. We want to change the resolution, support different resolutions, and support different channel numbers. Let's first see how to support different resolutions. During preview, we probably use lower resolution; during the finalization stage, let's use a higher resolution. Let's see how we can make it adaptable.

The conventional work, StyleGAN2, this is a generator from 32, 64, 128, 256, gradually increasing resolution and feeding the highest resolution to the discriminator to see if it's real or fake. MSG-GAN can feed all the resolutions—32, 64, 128, 256—feed every stage to the discriminator. That makes it more challenging because the discriminator has more information about the input; it has to use all the information across different resolutions, making it harder to pretend to be a real image, because for each resolution it has to look like a real image rather than generated, not to fool the discriminator.

Now we need to support different resolutions, so it's even more challenging. We want to make sure for every stage that we generated—64, 128, or 256—all the resolutions can fool the discriminator, not only the highest resolution. What should we do here? At training time, we randomly feed different resolution outputs to the discriminator. For example, here is 32, 64, 128, 256—randomly sample different resolutions to feed to the discriminator, such that when we are running at different resolutions from 32, 64, all the way to 1K by 1K, our approach consistently has better FID (lower is better) compared with a single resolution, like in this case, versus MSG-GAN, which is all the resolutions. This can significantly reduce the FID at both low and high resolution. The sampling-based multi-resolution support is very similar to the discriminative model we built.

However, this has an interesting phenomenon. For example, you see only at high resolution it works, but when the resolution gets low, you can barely tell the image. But using our approach, it can smoothly transition between high resolution and low resolution. At 32 by 32, it becomes completely noise, so this sampling approach is very helpful.

Next, the second goal: let's support different numbers of channels. We can run a full model with larger channels (better quality, higher fidelity), but we also want to support a smaller number of channels to make it faster for immediate inference. Here, the approach is to sample adaptive channel numbers during training—sometimes full channel, the number of yellow here indicates how many channels we actually sample. So here, we are not only sampling different resolutions but also sampling different channel numbers as indicated in the yellow part.

We observe some inconsistency. For example, this is 100% of the channels, this is 50% channels. You see, actually, they don't look like each other—the lady previously had a pair of glasses, now her glasses are gone. How to solve this? Usually, we add a distillation loss to solve that issue, to make sure at different stages, the image at full channel versus half of the channel still look similar to each other.

We observe a second problem when we are looking at FID for some of the subnetworks. For example, this is 25% for 128 resolution, this is for 256 resolution. If we are using the same discriminator, when the channel number is 25%, the FID spikes to 14, and at lower resolution, when the channel number is big—50%, 75%, 100%—it's also much higher than the vanilla model without this once-for-all approach, which is showing that using the same discriminator is not good. A single discriminator cannot provide effective feedback for different sub-generators. That makes sense: if the generator is pretty weak, slim, with very few channels, the discriminator should also accommodate that. Using a single discriminator for big and small generators doesn't quite work well.

What should we do? We propose this generator-conditioned discriminator. The discriminator should be aware of the configuration of the generator. Here, on the right-hand side is the discriminator, left-hand side is the generator. We collect the statistics—the architecture of the generator, like how many channels we have, what is the resolution—and we feed it to a fully connected layer and use that to modulate the discriminator. We call it generator-conditioned discriminator, such that the discriminator is aware of the size of the generator. Previously, the problem of the same discriminator is now solved, even better than the vanilla approach—FID reduced from seven to three. The generator-conditioned discriminator makes it actually pretty helpful.

That's what's unique about this once-for-all approach for GAN models. Since GAN models have two components—a generator and a discriminator—they have to be tightly coupled, making it a little bit difficult to optimize. Unlike the next lecture, where we'll talk about diffusion models, which have only one model to optimize.

Finally, we can see some good results. This is the forward consistency at different resolutions: 24, 512, 128; this is different channels: 100%, 75%, 50%, 25%. They are very consistent and transition between different photos in different parts smoothly. Which one has the lowest computation and can run fastest? Lowest resolution and lowest number of channels. But still, it looks quite good compared with the full model on the right-hand side. This is the lowest resolution and lowest number of channels; this is the full resolution, full channel.

We can also compare different computation reductions—what does the quality look like? All these four images are generated by GAN, not real. We can reduce the MACs by up to five times. When the bar is shrinking, we can hardly tell the degradation of quality of the image: 78%, 59%, 38%, 19%. Now we can apply it to photo editing.

This is a demo running on a MacBook laptop. Actually, that was a couple of years ago; I believe these days it can run faster without any change. The original model is very slow, runs about 3.24 seconds. Here we are making the man smile and also making him look younger by changing the sliding bar. If you look, he looks younger and we see the smile. Here we can control different channels and different resolutions. Now we can run it again—it's really fast, almost instant, by shrinking the channel and shrinking the resolution. Now it's almost very interactive. This is showing the latency: we can add some mustache, reduce the mustache, finally click the finalize button to run the full model, which takes longer (3 seconds). Now it looks much better. Try another image: now we look older, change the hair color a little bit, make it gray hair. Making someone look older is really associated with these eyeglasses, so we want to remove the eyeglasses. There is a sliding bar for eyeglasses—you can push it to the left, then the glasses are removed. Finally, we can click finalize to get a final high-definition image. This is open source, not deployable on a MacBook, so feel free to use this as part of the final project or discuss with us if you have other ideas.

All right, let's talk about the third part. Having talked about computation efficiency, now let's talk about data efficiency. Training GANs can require a lot of data—it takes months or even years to collect a lot of data, especially labeled data. Annotation is pretty expensive too. Recently, there are some automated tools to automate the labeling process, but still, collecting the data is pretty hard and suffers from copyright issues.

There are some popular datasets used for training these generative models. One of them is FFHQ, which contains 70,000 post-processed human faces. Another popular one is ImageNet, containing more than a million images of diverse categories. We tried training the GAN model with a limited amount of training data. For example, using only 100 images to train my face—that looks very poor. Or using 160 images to train the cat—it doesn't quite perform well. Or 300 images to train dogs—it doesn't work well. Although the original StyleGAN2 from NVIDIA works quite well, if we have a limited number of samples to feed it, the quality is very poor.

This is the quantitative results: starting from 100% training data (lots of images), 20%, 10%—the FID keeps growing, showing the degradation of quality given less training data. The current techniques of GAN training lead to inferior results when the training data is very limited. Let's see why that happens.

This is the discriminator training accuracy versus the validation accuracy with 100% training data. Both of them—the training accuracy is pretty good, the validation accuracy is okay. But if we reduce the training data to only 20%, the training accuracy improves a lot, but the validation accuracy drops very significantly, showing that it's very hard to generalize. Not to mention if you go to only 10% of training data—here the validation accuracy is even lower, very low validation accuracy, showing that the discriminator will overfit small amounts of training data, leading to worse generator performance because the discriminator is very poor. The generator cannot work well since they're tightly coupled.

That makes us naturally think about data augmentation techniques. If we have 10 images, we can make one image become six images by rotating it, tinting with different colors, cropping it, etc. Traditionally, data augmentation is a very effective technique to prevent overfitting. Then the question is: how do we augment GAN models?

One technique is to augment the real images only. So there is a generator, a discriminator, between the generator and the discriminator. One very popular technique is to apply these artifacts, like color jittering (make it darker, brighter), apply color jittering, or some translation (shift it to the top right corner), or apply some cutout (cut out a piece of the image). If we augment the real images only, the discriminator can take either the real image or fake image (the fake is generated). If we only augment the real image, what will happen? Actually, we find the generated image will suffer from the same artifact as we applied. For example, the generated image also looks darker or brighter, the generated image also suffers from this translation, they also suffer from the cutout (the generated image has a big patch). That is because the discriminator sees real images already undergoing this translation or color jittering, etc., so it believes the real image should also suffer from such transformation. Therefore, it encourages the generator to generate images like that. So this is not effective—the same artifact appears on the generated image.

What if we apply a solution? The natural solution is to apply this translation not only on the real image but also on the generated fake image. Therefore, the discriminator believes the artifact doesn't have to come from the generator, but it can be clean and then later apply this translation. We find it has very high accuracy on the translated fake image and the translated generated image after the translation, but it has very low accuracy on the generated image without translation. This accuracy is highly imbalanced. This unbalanced optimization makes training very hard, because when we are training the generator, there's no transformation; when we are training the discriminator, it has the translation. So what we need to do is actually apply the same transformation also during the training process of the generator, making sure it is differentiable so the loss can backpropagate all the way to the generator when we are training the generator. Make sure both the fake image and the real image are applied the same transformation, and for the fake generated image, it can also backpropagate to the generator.

The solution is to apply augmentation on both the real and the fake images, for both the discriminator and the generator. Immediately, it solves the problem of the worst quality. The FID no longer increases quickly, but stays pretty stable. It did increase a little bit because we have low data, but the FID becomes much more stable—from 9 to 14, rather than from 11 to 36.

Let's see, given this low-shot generation, if we apply differentiable augmentation, how does it look? This is the StyleGAN2 baseline, this is StyleGAN2 plus differentiable augmentation. This is 100 images of Obama face—now it becomes much better. This is 160 cats—previously very bad, now it's photorealistic. 300 dogs—much better. We can also interpolate across different images. Below are all trained with only 100 images: Obama face, panda, different sceneries, to interpolate between different seasons, between night and daytime. Works super well given only 100 images.

All right, let's take a short break before we jump into the next chapter about video understanding.

Welcome back. Let's switch gears to talk about video understanding. Video is the biggest big data—storing in super large scale. Like 10^5 hours of videos are uploaded to YouTube per day (probably updated data). It's very essential for both cloud and edge applications. There are lots of applications related to video, for example, action/gesture recognition, scene understanding, video search, and also for autonomous driving.

The interesting new thing about video understanding compared with images is that we need to perform temporal modeling. Temporal modeling is very crucial. For example, given a single image, we cannot tell whether we are putting something from left to right or right to left. Only if we watch the entire video sequence can we tell after we are moving the tape from left to right rather than from right to left. That's the interesting part about temporal modeling.

There's also a lot of redundancy in temporal modeling. The background—the table—stays exactly the same, white paper stays exactly the same, only the foreground (the tape and hand) are changed pixels. So we should exploit such redundancy for efficient video processing.

Let's first visit 2D CNNs for video understanding, then 3D CNNs, and then introduce the temporal shift module that gives the 3D CNN performance but at the computation cost of 2D CNNs.

2D CNN-based methods: in history, a naive approach is to sample frames and process with 2D CNNs, then aggregate the scores for prediction, either using average, max pooling, or something similar. Another approach is the two-stream method (Simonyan & Zisserman, 2014). The first stream is spatial (for images), the second is temporal (optical flow). Optical flow is the vector indicating for each pixel where it is moving in the next frame. Extracting the optical flow is actually pretty computationally heavy. So it has two streams: one is spatial, one is temporal, and then fuse them together in the end.

There's also an approach to combine 2D CNN plus late fusion, which is LSTM, to fuse the output from different CNNs—one from 2D, one from 3D—using LSTM to fuse for delayed fusion and then give the final prediction, which is high jump. However, you can imagine such late fusion—what is the problem here? The low-level information is lost, since the low-level information is extracted independently by different CNNs, only the high-level feature is extracted by the LSTM in this case.

Let's summarize the pros and cons of 2D CNN-based methods. CNN is very computationally efficient; we can also reuse 2D CNNs trained from image classification. But the bottom line here is that applying the 2D CNN cannot model the temporal information, since each frame is processed independently. That's relatively low microseconds, and if you use optical flow itself, it's very slow to compute. Late fusion also cannot model the low-level temporal relationships.

Naturally, people think about using 3D CNNs for video understanding. What is 3D CNN? The first work, very early (again in 2014), is called C3D. It involves not only the spatial dimension but also the temporal dimension. This is a 2D convolution in the 2D space, and this is on multiple frames. We add the temporal dimension. This is the 3D convolution—not only in the spatial dimension but also in the temporal dimension. It can jointly model the spatial and temporal information by using 3D convolution to convolve over the videos. But you can imagine, the 3D adds another dimension—it has more parameters, it's pretty heavy.

Can we inherit 2D CNNs and make them become 3D? Actually, this paper called I3D: Inflated 3D ConvNets, visualizes 3D CNNs using 2D CNN weights. The weights are pre-trained on ImageNet. How do we initialize a 3D CNN using 2D? The technique is called inflation. The idea is to repeat the weight across the new dimension, repeated multiple times across the temporal dimension for initialization. For example, if you have a kernel size of three in the time dimension, you just repeat the original 2D CNN by three times to initialize that, and a 2D kernel now becomes a 3D kernel.

The pros and cons here: we can jointly model the spatial and temporal information, which is good, and can also model both the low-level, middle-level, and high-level information. But the con is that it has very high computation cost and storage cost, since we have to store a 3D (actually 4D) tensor for the weights, and a lot more computation since we're adding one more dimension.

So the natural question is: can we achieve 3D CNN performance accuracy but at 2D cost, to get the best of both worlds? That's the temporal shift module—a new operation which is neither 2D convolution nor 3D convolution, but shift—a very simple technique called shift. Let's see how that works.

This shift operation contains zero FLOPs (no computation), zero parameters (no parameter), but it can very effectively perform temporal modeling. Let's see this figure: the temporal dimension, we have T1, T2, T3, T4 in three colors, and here is the channel dimension. This is the original tensor without any shift, and here the depth is the H, W dimension. Temporal shift: we can shift it both from the past to the present and also from the future. The first channel, the second channel gets shifted, third channel, fourth, etc., didn't change. What happens here is that now T2 has certain channels not only from T2 (dark blue) but also channels from T1 (yellow) and T3 (green). So T2 not only has its own information but also T1's and T3's information. Therefore, you can combine the information by mingling across the channel—you can do the 1x1 convolution across the channel to mingle the information from the past frame and also from the future frame.

How much percentage do we shift? That's a hyperparameter. If you shift too little, you have too little information from the past and future; if you shift too much, you have no information for the present. So there is a hyperparameter we should tune.

For offline video understanding (e.g., you have 10,000 hours of video to process), this is a very good approach. Another scenario is online video understanding, for example for self-driving cars. In that case, we cannot shift from the future to the present since it's real-time—we can only shift from the past to the present. Here, we are shifting from t0 to t1, t2 to t3, etc. Therefore, when we are doing the 2D convolution on each of them, T1, for example, has the information of both T1 and t0. Like here, it has information from T3 and also T2 (the previous layer), T2 also has information from T1. As a result, it has this diminishing weight all the way to the very first time frame. In this way, we use zero FLOPs, since shift operation can just move the pointer without moving the data, and there's no parameter associated with shift operation. Very effective technique to do temporal modeling, and it can be inserted into off-the-shelf 2D CNNs to enable temporal modeling at a cost of zero FLOPs and zero parameters.

Let's see how to insert this temporal shift module into 2D CNNs. Start with the offline TSM models, for example, action recognition, detection, video recommendation, etc., where the frames are processed in a batch. This is a video frame—different frames can be processed according to normal 2D convolution, then you get different time stamps, then you perform shift operation so that at each time stamp you also have the future frames' information and the previous information, and it passes to the next layer. You have an identity layer to compensate for the information—the identity layer is quite crucial because the first four channels in this case lose current information, which is compensated by this bypass layer.

That's offline, where you have access to both the past and future time frames. This is online TSM module, like in autonomous scenarios—it's processed frame by frame in a streaming fashion from t+1 all the way to t. After computing part of the channels, we actually shift it out using the memory, and for the next layer, we replace that channel with the previous time frame's channel, and shift out the current channel and feed it to the next time stamp. As time goes on, this final layer has the information across all other layers, because at each layer you have its previous layer's information, and so on. As a result, you can have access to the entire history of data.

The implementation is actually quite simple—can be done in just five lines of code. You can shift left, right, and some of them don't shift. Fold is a parameter depending on how many divisions you want to have—some of them you want to shift left, some right, and the remaining you don't shift. Very simple to implement.

Let's see the performance of this shift. The FLOPs per video (smaller is better): these are the TSM, this is accuracy—pretty high accuracy compared with baseline methods like I3D, much smaller parameters as indicated by the size of the circle. It consumes 3x less computation than the ECO family and 6x less computation than the non-local I3D family.

This is a demo for the Something-Something dataset. Here we are predicting: on the bottom, hitting something with something; now it's moving something away from something; it's real-time—covering something with something, stirring something on a surface with something, putting on a surface, hitting something with something. This is comparing the latency: using 3D convolution on the top, about 164 milliseconds per video, but now it's much faster—about 17 milliseconds per video, about 9x speedup. At that time, we were still using P100; this was published in 2019. This is about the throughput: baseline method about 60 videos per second, now we can do 77 videos per second, and the accuracy is actually even better—43% versus probably 41%.

This is for online video recognition—process them frame by frame in a streaming manner. This is across different datasets: baseline and improved approach—the accuracy is improved a lot compared with baseline, double digit right here. This shows if we just observe rather than 100% of the video, and we watch only 40% or only 10% of the video: top row is TSM—even if we are feeding it with 10% of the video, accuracy maintains much better compared with baseline methods, where the accuracy quickly drops.

This is a driving scenario for detection: 100%—this is a car, but for the RFC method (which is detection frame by frame), we misclassify it as a train (probably see in the highlight). This is quite dangerous for autonomous driving, since we want to make it stable. Frame-by-frame processing is much less robust compared with this temporal shift module, which has the history—it knows previously a car cannot suddenly become a train.

For scaling down, temporal shift module also has very low latency for low-power deployment. There are Nano TX2 mobile phones, just a few watts, and run more than 20 frames per second on CPU and more than 70 frames per second on a mobile GPU (which is a Jetson Nano). Scaling up, we also tried large-scale distributed training from the Summit supercomputer. This is the configuration: it has V100 GPUs at that time. We are comparing several aspects: one is the throughput, one is hardware utilization—much better, 2x than the baseline I3D method, since it has lower FLOPs and better throughput. Here is showing the batch training time: compute versus IO time. Baseline method has a lot of IO time, but TSM is purely dominated by compute time because TSM is very light in IO, decreasing total batch time and better stability due to smaller model size, less weight to communicate for distributed training. Even with a large number of GPUs, the scalability is pretty high.

We are going to talk about distributed training techniques starting from next week—how do we scale up the training utilizing multiple GPUs to speed up the training. We can speed up training by 200 times using multiple GPUs—from two days to only 14 minutes. Here is using one node, here is using 256 nodes. One node is taking almost 2 days, 74.1% accuracy, and using 256 nodes takes only 14 minutes—just a cup of coffee, then you can train the model and get pretty much the same accuracy. The theoretical speedup is 256 times, since we are increasing the node from one to 256 nodes, and actual speedup is 21x.

This is the accuracy versus the batch size: when we are increasing the batch size, this is the error. We can increase it all the way up to 12K batch size. That's a perpetual issue: when increasing the batch size, usually it's associated with improved efficiency, but how to find the proper learning rate schedule to make sure a large batch size can still train well—that's a very interesting issue to explore.

This shows the scalability: as we are increasing the number of GPUs, what is the throughput? TSM has the highest compared with different baseline methods like I3D. We can also dissect TSM to localize and understand what is happening. For example, channel 5 is understanding moving something away—here we are highlighting the spatial and temporal pixels to understand why the TSM model is thinking that we are moving something away. These are the highlighted spatial and temporal information: there's a cup, now we move it away; there's something, now we move it away; there is a basket, and then we move it away. Channel 446 is understanding pushing something to the left—here we have a cup, we're pushing to the left, it gets highlighted. Similarly here, we're pushing response to the left.

We can also apply it to gesture recognition, since gesture recognition cannot be done in a single frame. This is a Google Map navigation using gestures: you can use this approach—gesture to switch between the street view versus the road view, and then you enter street view, can zoom in/out, etc., go back to the map view, and this is done real-time on a Jetson Nano.

Finally, let's talk about point cloud understanding—from 2D to 3D. There are a lot of 3D sensors widely used in our lives, like the iPhone now has a LiDAR sensor—you can understand the distance, use it as a measure, measure human 3D images. Also for self-driving cars, some of them have a LiDAR sensor on the top, drones, etc. What they collect is actually the 3D point cloud—a collection of points (XYZ) together with intensity as the C dimension. It's actually very sparse—if there is no object, there's actually no point. In the 3D space, this is super sparse compared with images (2D images have dense pixels), this has very sparse points.

It has many applications, such as in autonomous driving—it can measure the distance and serve as another modality for the input. Also for augmented reality—you can, for example, this is the hallway at Stanford before the renovation (it was like this when I was there, now it's fully renovated), so for AR/VR applications.

The challenge is that point cloud is highly sparse and irregular. Images are pretty dense—you have 1K by 1K images, so every pixel is occupied, very dense and regular, and can be processed by conventional convolutional neural networks. But point cloud is extremely sparse—sometimes less than 0.1% density, the lower the amount of pixels, and it's very irregularly stored in memory, which GPU and parallel processing doesn't like. What we want is actually continuous memory reference, regular memory access pattern, and large continuous memory footprint, but this is not the case. We suffer from limited computation resources—for example, in AR/VR headsets, you don't want to have a whole backpack of computers and a big battery, we have to put it in the pocket, and very heavy headsets. Also for self-driving cars, we don't want to have a whole trunk of workstations in the back. These point cloud deep learning models must be deployed on resource-constrained edge devices.

We are going to introduce the PVCNN (PointVoxel CNN) technique to achieve higher efficiency for point cloud understanding. Let's first understand the bottlenecks for point- and voxel-based methods, which are two approaches to deal with the point cloud.

This is resolution versus the distinguishable points versus the GPU memory. As we are increasing the voxel resolution, as it gets higher, it's getting better for the distinguishable points. If the resolution is too low, we cannot distinguish the points because they merge into the same box. If you want to achieve at least 60% of the points being distinguishable, what is the memory requirement? When we are increasing the resolution, the memory is growing very fast—actually, it's growing cubically with the resolution, since it's 3D. The memory equals 2^3 with resolution, and here with only 8GB of GPU memory, more than 40% of the information is actually lost.

On the right-hand side is showing computational overhead. This is showing the effective computation versus the dynamic kernel versus irregular access for different point-based methods. The baseline methods actually have a lot of overhead due to irregular memory access and dynamic kernel computing, while our method, the PVCNN, is actually spending most of the compute and latency on effective transformation, not overhead.

This is comparing the memory access bandwidth versus energy. This is multiply and add, this is SRAM read, DRAM read. DRAM read is much higher energy compared with SRAM read and compute itself. So off-chip access is much more expensive compared with arithmetic operation—pretty low here and pretty high here. Bandwidth is actually much lower compared with SRAM read. We want to avoid DRAM read, and random memory access is also quite inefficient due to potential bank conflict. What we like is actually continuous addresses, like address zero, address one, two, three, without any conflict.

What we are working on here is proposing an architecture called PointVoxel CNN. We have two branches: one is the voxel branch, the other is the point branch. For the voxel branch, we divide the 3D space into a grid of voxels. Here is a 2x2 grid—it's representing 2D, but actually it's in 3D space. You can imagine it as a 2x2x2 grid in 3D space, and then we convolve it because it's very regular. All these blue points—these three blue points—will be associated with the same voxel, and these green points—these two green points—will be associated with this green voxel. That's why we are seeing the higher resolution, more distinguishable points; otherwise, these two points are not distinguishable because they all fall into this blue box. Since now it becomes a very regular tensor (2x2 tensor), we can do 3D convolution and then finally we can de-voxelize that, assigning the same feature for these three points. That's basically modeling the neighborhood information across adjacent voxels.

There's another branch focusing on pointwise operation, similar to the PointNet idea—we just transform the feature of individual points through this multi-layer perceptron, which is focusing on feature transformation rather than neighborhood modeling.

What is the advantage here? The voxel-based branch has much better regularity because it's a very dense tensor and there's no irregular access and dynamic kernel cost. It's very good for neighborhood information aggregation, while the point-based branch can keep very high resolution, since each individual point can be distinguished and can alleviate the information loss which appeared here (all three points fall into the same voxel and can no longer be distinguished). That information can be alleviated by having this point branch to alleviate the information loss during the voxelization process.

Let's see how these two branches work well together. Actually, this works: the voxel branch is focusing on large areas like the tabletop, the chair, versus the point-based branch focusing on those corners, like the lights, corners of the chair, etc.—very interesting, like small versus large, local versus global.

PointVoxel CNN is pretty fast and accurate. This is comparing the latency—speedup more than 7x with superior mIoU (mean Intersection over Union) accuracy. This is the GPU memory—much lower memory but higher mIoU, so pretty good memory and latency tradeoff.

This is the demo for indoor 3D segmentation. Here is PointNet, which is pretty slow (like two seconds), now it's only one second.

What is the limitation for point-voxel convolution? Actually, the voxelization phase in the PVCNN2 can introduce information loss. The voxelized scene—this is the input scene, this is the voxelized scene—so person here and person there have four dots, only five dots, because the voxelization is too coarse. What can we do to improve the resolution for this voxel branch without incurring too much high resolution? Actually, we can introduce sparse point-voxel convolution. Rather than using dense convolution, we can use sparse convolution on this branch. We first voxelize sparsely, then run sparse convolution only on the voxels that are non-zero, and skip the computation on zero points. That can have better distinguishable points.

The point-based branch is actually focusing on those small objects and the person with trunk—the trunk is called out. Given the latency point, we can actually perform 3D neural architecture search to further improve the efficiency of point cloud understanding. We're familiar with that already: first, supernetwork by having fine-grained elastic channel number and also elastic depth number (changing the depth of different stages), and we have weight sharing between the input channel and output channel. Here we perform evolutionary architecture search, very similar to what we did in Lab 3. Given a supernetwork, propose a candidate, if it fits the data, keep the architecture; otherwise, sample again, then do mutation and crossover to search the best model. In this way, we can reduce the search cost by 17 times, and this reduces GPU latency to the max.

We talked about both the camera and also the LiDAR image and also the point cloud. Now let's fuse them together. This is a common scenario for autonomous driving cars: it has multiple multi-view cameras (this is actually the Seaport area in Boston), so you have front camera, front right, left, back camera, back left and right. It also has a LiDAR in the 3D view. How do we fuse multiple sensors' information together in the same space and perform perception in that shared space? What is that shared space? It needs to be easily converted for multiple modalities without much loss of information, and it should also be suitable for different 3D perception tasks like object detection and map segmentation.

Our solution is using the bird's-eye view to fuse them. The LiDAR feature exists—we can project it in the bird's-eye view, and also for camera features, we can project them in the bird's-eye view. We'll basically look at the world from the top, so that we can match them together, fuse them together. For example, this is in the Vassar Street in the CSAIL building—this is camera view (perspective camera view), and this is the bird's-eye view from the top down. This is the CSAIL building. We want to do this view transformation to transform from this camera view to bird's-eye view, and for all the sensors to fuse their information in this shared space.

Here is a popular configuration for self-driving cars: multiple cameras, LiDARs, several radars. We can fuse the information across multiple sensors that consist of images and also the sparse point cloud. There will be two branches of data flow: one is the dense branch for camera, the bottom is the sparse branch for the LiDAR, and we merge information in the bird's-eye view space (BEV space), which needs to be converted. We first pass it through a camera encoder to get the camera features, perform this camera-to-BEV view transformation (which we accelerated a lot for this critical operator) to cast it into the BEV space. For LiDAR sensor, we pass it through a LiDAR encoder to get the LiDAR feature, then flatten that in the Z space (assuming that no car will be laid on top of another car), so we can flatten that in the Z axis and get a LiDAR feature in the BEV space. Then we can concatenate the information together, pass it through the BEV encoder to get the BEV features. We can use multiple heads to do that—one head for the map segmentation to get the road, and the other for 3D detection to get the bounding box for the cars.

This is the demo of BEV Fusion using multiple sensors as the input to do joint 3D object segmentation for the cars and also BEV map segmentation on the right. When it was released, it was actually running at the top on the leaderboard.

That's all for today's lecture. We talked about domain-specific acceleration techniques, including GANs for image generation, GAN compression, Anycost GAN, also differentiable augmentation for data-efficient GANs, followed by video understanding using temporal shift module (using the shift operation, very cheap for temporal modeling), and finally point cloud recognition using PVCNN and BEV Fusion to accelerate point cloud recognition. In the next lecture, we'll introduce diffusion models.
