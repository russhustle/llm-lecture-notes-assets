(19) EfficientML.ai Lecture 8 - Neural Architecture Search Part II(MIT 6.5940, Fall 2024, Zoom recording) - YouTube
https://www.youtube.com/watch?v=L07-eVmSVGY&list=PL80kAHvQbh-qGtNc54A6KW4i4bkTPjiRF&index=15

Transcript:
(0:03) okay it's 335 let's get started so today we are going to talk about efficient ml lecture eight to discuss neur architecture search part two this is the uh last part of neur architecture search and lab Lab two was released don't forget to check it out we will Implement those quantization algorithms we learned last week so let's continue to talk about neuro architecture search I just review what we want here have multiple targets we want to design a network from scratch rather than compressing the model rather than pruning the model
(0:42) quantizing the model now our goal is to design a model from the beginning ideally we want a model to be accurate and then have low latency high throughput low energy and also storage in the last lecture we talk about what are the Primitive operations the building blocks and what is Nas including the search space how to design and narrow down the search space what is the search strategy like uh reinforcement learning evolutionary strategy greater search random so this lecture we'll talk about uh efficient how to efficiently search an
(1:22) efficient architecture and also Hardware awareness including how do we quickly estimate the accuracy without having to rrin without having to tr the model from scratch each time and also how to do Hardware awareness make sure the model is not only High ACC high accuracy but also has low Laten zero short n can we enable searching the model without even training it and also how do we search the hardware architecture and the neuron Network architecture at the same time so code design search the hardware architecture and also the neural
(2:04) architecture and finally we are going to apply what we learn about Nas to diverse applications including natural language processing can point Cloud Post estimation and also lar language models okay so let's start today's Journey starting with accuracy estimation strategies so as a over of the neural architecture search pipeline we start with the search space cell level Network we discuss in the previous lecture and then we have a couple of search strategies we discussed the GD search random reinforcement learning gring descent
(2:46) evolutionary search it proposes architecture and then Returns the performance and estimation okay um so now we are going to focus on how do estimate the performance okay quick and efficient performance estimation strategy so that's the focus of today's lecture uh we will start by talking about train from stretch and then iner weight and then hyper Network train from scretch is the easiest we just train a model find a model architecture train it to convergence and then see the accuracy and latency right um we train a given
(3:24) model from stretch on the training set X evaluate the model on evaluation validation set to that get accuracy R but this is apparently very costly even on S far 10 um this method need to train tens of thousands models and that require 22,000 GP hours that's really expensive even on such a small data sets like CRA 10 not to mention those large scale data sets imag that Coco those are actually not considered large scale data sets these days now we are talking about three Tron tokens eight trillion tokens those are
(4:05) the large scale what we mean by scale these days so having to train from stretch apparently does is not scalable it doesn't scale to this large training are there other Alternatives so we can inherit the weight okay don't retrain the weight but starting from a pre- model inherit the weight try to change the architecture on top of a pre the model so very famous work is this net to okay we can inherit the weight from this architecture and then we can change the architecture by having three neurons rather than two neurons here what we do
(4:47) here is actually replicating this node this H second he node replicate twice okay and here we are dividing the uh weight here by half so we are mathematically equal but the architecture get adapted so this is net to wider net uh we are having reducing the uh reducing the weight value but duplicating the nodes we can also perform n to deeper net say this is just one layer now we want to uh just this is one layer we want to add another layer on top of it so what we do add an identity mapping layer okay so this layer is basically
(5:36) identity mapping so even if we add these two layer um the result will be same so uh in this way we can train on top of uh this architecture to make a different model okay it contains maing initialized the layers so effectively without any retreating we can change the architecture and maintain the mathematic uh still maintain to mathematical equal mathematically equal as the original model okay so this is met to wider n by using divided by two which is one net to DEET by cascading with identity mapping layer so in this way we can inherit the
(6:17) weights from a parent model to reduce the training cost we get a new architecture but we don't have to retrain model using uh this night toight approach we can propose new architecture so remember previously we talk about using an RN lstm each step we propose some action um now the would be either net to wider or deeper net can do either of them um so we effectively rear the weight from parent model to prevent us from having to retrain the model Okay so next approach is use a hyper Network hyper Network means you have another
(7:06) Network that is predicting the weight of your model and the network is used to predict your weight so what does that look like we are given a network architecture right here this is the connectivity I express as a graph and then we have initial note edings what is embedding embedding means the features of uh this network architecture for example the NCH HW channel width the height kernel size Etc so this layer has embedding here we have five layers therefore we have five embeddings not only the feature M matters but also the connectivity
(7:50) matters so how do we model the connectivity the connectivity is modeled as a graph Lo and the edges Define this computation graph so we can use a graph Network pass it through several layers of graph Network um to have a final node eding since in each step well each node interacts with the neighbors this node with for this node Etc each get collected information from its neighbors so that's the graph what graph network do the models architecture and then we get a final Bing we feed it to a shared MLP which is
(8:34) the hyper Network to predict generate the weight for the final model okay so final model's weight is generated by this hyper Network this is the Network and input to the hyper network is not eding is okay so the hyper Network generates weights based on the model architecture and we can use ging descent to update the weights of the hyper network not quite while used we are going to visit this in the lab but just give you some idea what people has done to reduce the training cost okay next chapter will be very important very interesting we having
(9:11) them me the homework so uh networ Hardware aware that's okay the model not only want to be we it uh high accuracy but also it should be uh very Hardware efficient okay so um The General Pur model may not be efficient since we have diverse platforms to deploy like a Raspberry Pi very small memory GPU large memory large footprint High paradism CPU maybe lower paradism mobile phone resource constraint memory conr battery constraint so ideally we want to have specializ specialized model for different um other platforms like a tiny
(9:56) model for R Pi High paradism model L Cal cost for GPU uh specializ CPU model and a specializ GPU model that takes less energy and less memory footprint but you can imagine if hand design hand different models for different other platform that's not automated and that's very costly right so how do we cope with that problem uh we can um since previous neural architecture search approach is very expensive if for example nasnet take 48,000 GP hours even just search on cyar data set that's like five years if you have a single GPU 4,000 hours
(10:41) and DS take hundreds of gigabytes GPU memory if you directly search it on image net so very time consuming memory consuming therefore previous Works uh have to utilize a proxy task you can think about it as a simpler task so rather than directly search for a Target task and Target platform we find a simpler proxy task a smaller task for example the data set can be very simple like C for 10 class classification rather than thousand class image net classification or we can search a small architecture search in the small
(11:23) architecture space um like very low that we can also do just a few talks of training we don't have to train all the way convergence but we just train a little bit and see if that looks promising right and rather than plug in the real Hardware to measure the latency we can use a proxy which is flops and the parameter count we learn in first lecture how do we estimate we calculate the flops how do we calculate the parameter number of parameters we use that as a proxy task for the hardware efficiency but you can imagine this is not accurate
(12:04) right a model that works very well what is a problem if you search on C 10 model that works on cyr 10 may not work well on imet since the data set is very different and also if you just search a very small space you just search one building block you start the same building block on top of each other uh the first couple of layer and last couple layer may not prefer the same building block right so it's not good to search in a smaller space and also fewer IO of twinning you may find a model that converges super fast
(12:42) in the beginning but after twinning after a while the convergence may not be good you waste a lot of GPU cycles and also we learned that flops and number prameters they do not not directly translate to latency and speed up so that is also not quite helpful so what we do here um is to directly search on a Target task and Target Hardware without relying on any proxy task so how do we uh save the traing cost uh but also be able to directly search on the target task and also Target hards so this is one layer uh input
(13:27) output uh you have many choices we can have 3x3 count 5x5 identity ping ET right so conventional model just have this weight parameter right but now we introduce these architectural parameters which is the probability of choosing 3x3 C probability is Alpha choosing the 3X3 C probability is beta Etc we assign assign the different choices one arital parameter which is a probability of choosing this particular um block right we have five choices then we have five probability probabilities they add up to one and we learn not only
(14:13) the weight of each choice but also we learn the probability of choosing them right therefore we introduce these two updates One is updating the weight parameter the actual weights in 3X3 convolution three by input Channel by output Channel and we also iteratively learn those architectural parameters the probability of choosing each path okay um in at inference time we are binarizing these architectural parameters okay zero means P away one means we keep it so that during training we only have one path in the GP memory
(14:55) rather than do a w sum of all these different paths if that is the case we have to compute uh the activation after 3x3 activation after 5x5 after ping average them together using the probability then you can consume a lot of GPU memory you have un choices then the TP memory is all in that not good so we only sample by binarizing the gates binarizing these probabilities and choose the one that has highest probability and then only sample one path and prove those redundant paths based on these architecture parameters so in this way um we are
(15:39) learning training the model from scratch between train not only these weight parameters but also these architectural parameters if we have the architectural parameter like here is 0.9 0.1 then at inference time we are going to sample the architectural parameter with the highest ability right 0.9 then we are going to choose the first choice and also binarizing the architectural parameter um allow only one path of the activation to be active and therefore the memory footprint will be o1 rather than o n right no matter
(16:21) how many design choice you have may have different Channel size you may have different FAL size you have a lot of choices but we want to make sure the training memory is all1 rather than taking hundreds of gigabytes DP memory which is very expensive so in this way uh getting the architecture is as cheap training the model in a single pass you just train the model once you train not only the architecture weight parameter but also train the architecture parameter so we turn this modality from a proxy task eliminated this
(16:58) we can just Direct TR the model OKAY model on the target F and Target hardware propos and updated okay so that we call this work proxil n n we don't rely on proxy task or Hardware metric like flops okay so proxy task like Cy 10 now we could directly use image n to train it since the training cost is the same as training the model from Str uh we no longer have to be constrained with a small architecture search space but we can open up a much larger search space since we are finalizing different choices but TP memory is over scalable
(17:45) and also we don't have to con ourselves with only fewer ipox of training or we can do full training and also uh we don't have to just use flops and parameter number of parameters but also we can profile deeny we're going to talk about this how do we use profile the latency to um a directly without having to me using the flops actually using flops is sometimes dous right so this is uh x-axis the max the Y AIS is L measure on Google pixel one mobile phone okay so there are three dots the blue one is mobile net V2 uh the orange dot here is naset
(18:35) the Green Dot is a mobile net these two models okay those are searched by conventional um Nas approach they have similar Max actually they have smaller Max compared with this one mobet me tool mobet me2 has roughly 5 uh 80 MX but uh this a mobet has only about 550 MX it's a smaller Max but unfortunately the latency is 190 milliseconds compared with M that less than 150 milliseconds so max doesn't translate to real Hardware efficiency right we can have lower Max but actually higher dcy much higher dcy right similar for um
(19:26) the GPU platforms um if you reduce the hidden Dimension size from the the latency actually doesn't quite change the y axis is lat reducing the hidden Dimension barely reduce your lat right since GPU has very high paradism if you reduce the workload it may be hungry may not realize any measure the speed but here if we uh still the number of layers the latency will scale proportionally scaling the number of layers the latency will scale proportionally so similar flops but very different L is the x-axis very pretty
(20:13) much the same but Renity differs a lot so max the conclusion is that Max doesn't translate to the measured real Hardware efficiency so we have to be careful using flops as the proxy to represent efficiency here are some other examples for example if you change the hidden Dimension uh for Raspberry Pi reducing the hidden Dimension will effectively reduce the vency well GPU is not influenced by the hidden Dimension since GP has very large paradism even if you have a very large um hidden Dimension the GPU is not
(21:01) saturated so what does it mean to us we should not use the flops as a reward right since it's not a true reward we want to use the latency measure latency as a true reward for your neural architecture search algorithm therefore we want to measure the daten on target Hardware platform but you can imagine meure the latency is very slow right um You can also have a bunch of Hardware devices me them in parallel but that becomes expensive um we spend a quite bit quite a bunch um to purchase lot of phones we have a mobile Farm build
(21:42) mobile farm and so that you can man the in parallel and that's just getting more expensive so what can we do here use a machine model design a lat predictor given a neural network architecture to predict how fast it can run so here uh we collect a latency data set architecture latency architecture latency architecture latency right and we build a latency model so that they new neur architect neuron architecture we can predict the latting how fast it can run on this target platform the most simple uh model for
(22:27) rency profiling is basically a lookup table uh you profile basically everything you could have encounter and then uh look into index into the loop table we build a laty data set which contains operator one lat Z Operator 2 lat Z Etc and then we U summarize them in the Excel sheet and later when we see that that that layer we just look at between the lookup table and adding different layer together adding the latency of different layer together and actually that works quite well this is the estimated latency this is the real
(23:10) real latency from this prox is n work roughly follows y equal to X follow that line to be more complicated we can also build a predictor rather than using the lookup table so what is the feature for this predictor a feature for layer can be like the NCW kernal size width the resolution different dimensions Etc okay gave those feature we can use a simple linear model uh to predict the latency and actually that also work quite well this is for Transformer architecture in this this world H Hardware aware Transformer uh this is y = to X
(24:00) predicted latency is very close to the real latency and we are just summing up the latency of different layer together to get the total and to latency for a particular model what could be the problem here by summing up individual layers latency get the total Laten on gpus you may have kernel Fusion sometimes two kernels might be fused into a single kernel then the latency might be much smaller than adding up these two layers together so you have to be careful about the implementation like T RT might do those optimizations
(24:42) behind but how do we know if some layer can be fused or not so rule of thumb is roughly if it is computation intensive kernel followed by another computer intensive konal like m followed by another m us doesn't help was helpful is a um like matrix multiplication followed by a nonlinear activation function which is memory bonded so compute Bond versus a memory Bond the memory Bond share same output or input then you can be fused in the previous ler so that's out of the uh this out of this class but that's some
(25:20) insight we can keep in mind some of the lers can be easily fused okay so let's see how that helps building such specialized Hardware aware proxi this neuron natural architecture uh so this is the m V2 a very welld designed well created model from Google for mobile devices and the red line is basically mobile net uh the xaxis is the latency lower better the lower faster Y AIS is the top Point accuracy the higher better we can see it's about 80% faster and comp compared with uh this Hardware awareness this is the accuracy
(26:02) latency has the smallest latency but highest accuracy which is showing such small specialized model is really helpful for the latency and accuracy tradeoff talking about specialization we want to exam uh when we are targeting different hardare platforms GPU or CPU or mobile device um and this is the prox n searched targeting GPU we specialize this model for the GPU and this is a specialized design model for the CPU design specialized model for mobile and they have different accuracy and let's see what if we apply this GPU
(26:43) model to GPU CPU and mobile we find unsurprisingly the diagonal latency is the shortest okay showing you that the model specialized for mobile works the best for mobile it does not run very fast in the GPU compared with model that makes sense because GPU has very high paradism CPU has no um so of course they prefer different neuro architectures and so here we want to visualize the search process uh during the training process remember we are learning not only the weight parameter but also the architectural parameter so
(27:27) different from conventional training where the model architecture stays the same you are training exactly the same model architecture throughout the different IO but now we are training also the new architectures right and P convergence we can see that the CPU model uh is much a CPU model deeper GPU model much G and GP is much wider and CP model is narrower GP model a lot of large kernels like 7 by S kernel which is in red CPU model actually didn't use any 7 by7 kernel all the PS are either 3x3 or 5x five because CPU has less paradism so
(28:15) large kernel may not be more efficient over there but GPU really prefer low number of Kernel costs since each kernel call takes a lot of time therefore it prefers very wide but fewer number of layers and which is indeed the case so let's uh take a look at the whole process again starting with the exactly same initialization and as it gradually learns the GPU model actually become shallower but wider using LEL CPU model gradate this yellow or red layer disappear one only left two two Until the End um there's no no large colel at
(29:01) all so it's very interesting to see uh this search history how the neuron n architecture adapts to different Hardware features like paradism okay so that's one way to search specialize model for different H platforms then the next question is if you are given a model hardware platform like a GPU or TPU DSP accelerator we have to repeat the process resarch model for each of them which can get quite costly how do we further improve the productivity can we just have one single model and then somehow later we can twe it to fit GPU
(29:44) to fit TPU to DSP or smaller one fit a mobile device larger one to fit a GPU conventional methods like if we have one Hardware platform like we are searching a model for phone um we spend a couple of D hours right by uh SE search IO TW backward forward and backward if it is good then we break and then we do some post training um optimization after that and we have more devices we have to repeat the process for more devices we can be which more costly and if you have um many devices they become super expensive to do this
(30:30) process so in order to eliminate the search cost reduce the search for diverse diverse Hardware platforms we'll talk about a once for all approach for all approach we are going to implement this in lab three so the lecture here is very important so rather than tning each network from scratch like using the using the pro approach can we trat multiple models at the same time we have a single model okay that contain many sub networks okay which me one small Network they are sparely activated and we can have many sub networks just
(31:11) like this we can activate sparely some sub Network a small portion of the and then we get the latency accuracy um and we want to see if the latency and accuracy meet our can you pleas meet met then we finish the search process you doesn't meet then previously we have to ret TR model OKAY train from scretch Bay or even longer time and repeat this process which can be quite slow but now we don't retrain it we just sample another sub Network a different network from this one small Network we are going to provide you one of this
(31:56) for Network for you during the home work so you don't can just implement this process you don't have to train from stretch we will give you a one for Network you can sample a sub Network can sample actually different sub networks get a latency measure the accuracy and see whether it meets your demand if not you can rather than just TR it again you can just sample from the W for Network so let's see um what is that good for so we can TR once and get many so that we can reduce the design cost the training is amortized by many search
(32:39) iterations this once4 network contains many child networks that are sparsely activated so during architecture search we can uh search a larger sub Network deployed on powerful GPU like h100 a mid size is nwork deployed on a consumer level GPU like 1490 GPU and we can also um derive a small version and feed it on a mobile GPU right and we just an A okay for these three models we don't have to train it from scratch and this is particularly helpful for large language model where it's very costly not quite affordable TR these models from scretch
(33:28) but now you can just train one model and get many sub networks to get different get models of different sizes like 3bs 7B 70 B Etc similarly if different chips have different memory constraints you can design deploy a larger S network uh under larger memory constraints me middle one for a middle siiz memory and tiny one for a tiny memory or a larger model for new phone or old smaller model for old phone so that we can uh cater for users with different phones like someone may have alphone 16 someone may have iPhone 11 we want to
(34:15) make sure the model can run on all different platforms and similarly when you leave home in the morning your phone maybe of full battery uh during late night it maybe have uh very little memory during battery saing mode so now you can simly similarly uh shift to this tiny model U that is derived from the same one SP so that is the usage so the principle is just between once and get many so we between once then we get many surprisingly can 10 to the 19th 10 19 is a surprisingly large amount of child networks for free
(35:02) by just Shing this um once for Network um different child networks are sply activated but they share the weights with this for Network we TR them jointly to amortize the design cost and this can design specialized models for diverse under platforms so how we Implement such a y network first of all let's brainstorm some challenges uh it's hard for a model to operate uh independently because they share the weight right U how do we prevent different sub networks from interfering with each other how to make sure they can all independently
(35:53) output so let's take a five minute break and then we are going to return talk about how to implement it okay welcome back so let's start deeper how do we Implement realize this magic once for all Network that can have many sub networks independently operate so the key part is to progressively uh Pro and shrink the kernel size deps resolution across different dimensions okay so we want to progressively shink the kernel size remember we want to support both large model middle model small therefore we want to do across different
(36:30) dimensions um the kernel size layer and channel okay from the kernel size perspective uh we start with a 7 by seven kernel and then the inner part of this 7 by7 which is 5x5 we pass it through a transformation matrix we learn that Matrix and then can derive a middle size um kernel which is 5X five and also we can use the inner part of this 5x five kernel to get a 3X3 kernel okay so in this way we can either operate on a small model middle model or a large model they share the weight so naely to support these three
(37:13) different sizes how many parameters you need you need 49 plus 25 9 right but now we need only one 7 by S 49 weights this 5x five weights it should here that's the inner part of 7 by7 weight and this 9 3x3 weight is the inner subsection sub part of this 5x5 weight and similarly we want to apply that weight sharing principle to other dimensions the number of layers and also the number of channels we are going to implement in the lab Lab three so take go careful look at it uh so we start with the full deps uh here we have
(37:56) example of deps four and then we shrink the depths to support we can have depths of three or depths four depending on which one we choose further shrink the deps by allowing it to the O3 to come directly out of Layer Two so we can have two layers or three layers only four layers so that's progressively shrinking the number of layers from four three layers to two we can also progressively shrink the number of channels okay initially we have four channels showing with the full width and then we can sort the channel importance
(38:38) by looking at the um magnitude of different channel uh we organize them according to the the size largest one is green one we put it in the front sorted and then if we um allow it to have only three channels we pick the first three most important channels and finally we can also allow it sort again to pick only two channels then we pick the two largest ones green one 0.
(39:11) 85 yellow one the yellow one is largest among this so we can either choose the four layers four channels or three channels or two channels so the network becomes very agile very becomes elastic we can have a larger one like this larger Curel larger depth width we can also have a smaller one uel smaller uh depth width but they share the weight so altimatic you have one the biggest model and then the small model can be derived from that biggest model okay um so let's see this in animation question here so the question is how do we make
(40:10) sure the biggest model still works after tring the smaller burdens right so during training we sample the large one larger one and also the smaller so both the larger one and smaller get a chance to be sample so that you can make sure the lar larger one didn't get destroyed for example iteration one we may train a bigger big one iteration two we may train a smaller one itation three we may go back to tr very big one and there are different sampling probabilities schedules you can choose yes because we are sharing the
(40:51) weight we TR a smaller one you also updating the bigger one but only middle part yes you got it the share the weight so even you if you're train the smallest model biggest model is still impact okay so this is animation showing that we start with uh the deps Dimension starting with elastic depths resolution so there's another dimension which is the resolution Dimension we can also run sample different input resolution for each batch so small resolution mid large resolution that doesn't impact your weight because different resolution they
(41:36) share the same weights but that impacts your max your flops and also elastic kernel size elastic depths and also elastic width so that's the four dimensions we are going to make it to be elastic including the resolution the Cal size depth and the width so in the lab we are going to provide you with such a model that already did that has already uh went through this whole Trum pipeline so you can directly use it in simple different sub networks smaller kernel larger kernel smaller deps larger Etc to play with it do the latency predictor
(42:22) and given the particular latency just find the best network architecture so let's do some roof line analysis to see um how this efficiency compare with others so what is roof line analysis there are two axis the x-axis is operations per bite so you fetch one bite of data move one bite how many operations are able to do on top of that uh memory so data movement is very expensive computation is cheap um ideally we want to fetch one bite of data and we do a lot operations on top of it okay and the Y AIS is the uh G flops to
(43:12) realize the performance and initially we are memory B so as long we are increasing the operations per bite uh we can improve the G Ops uh and later we are compute bound okay no matter how many operations provide you have are limited by the number of multipliers and uh and adds in the platforms so this is a roof line analysis looks like a roof so people call it roof line analysis and let's see how does the on4 network where is the on4 network located compared with other approach like I'm net mobile net V2 so this is not human
(43:57) design model this is a nice model uh so actually what for is having on the top right corner okay what does top right corner mean it means can achieve higher JS per second and also right means it has higher operations per bite so this is log um AIS so the difference is actually quite big okay so bigger operations per bite means that we can have uh make it more memory efficient we one bite of data we can do more operations on top of it therefore it's why getting more efficient and indeed uh we measure the uh realized efficiency measur the GS per
(44:43) second and see once for network is indeed having a higher meur of GS per second on the hardware platform so this is just one Hardware platform let's see how it performs on diverse Hardware platforms so we meag it on Samsung phone Google LG IM ATP Intel CPU and signings PGA and this uh blue part is the Baseline latency versus iy ideally we want a low latency high iy so the top left corner it's good um on across all different platforms you can see one for Network these are the sub actually reside on a better pral optimal
(45:32) curve with lower latency and also higher accuracy all right so that is the uh howw awareness any questions so far make sure you are clear about how W Network works so that we can complete lab three okay no question let's move on to talk about zero short kns so what shortness previously we have to measure the latency on the hardware platform we have to first train it for a while and then estimate accuracy right which can be quite costly you have to train it which can be quite slow TR it for a while but now let's just look at the
(46:14) model without having train it let's just see eyeball tracking no actually tracking we just see some of the uh features itself without having to train it just look at it analyze architect actually and see guess whether we can have a good accuracy or not I introduce two pieces of work Z and also gra the motivation is basically don't train it and tell me the accuracy whether it's promising so zero short now here um there's one heris saying that we just randomly neutralize input following the normal distribution and then we add a
(46:54) small perturbation and a small to obtain a new X and then we initialize all the weights of a Neal Network architecture using normal distribution So Random weights basically so this paper is saying that um the difference between these two inputs will the two inputs generate two outputs okay FX and slash the difference between these two out outputs um if the model is good then difference should be large okay if the input is difference the same then H model should be sensitive to the input preservation you change the input by a
(47:39) little bit the output should dramatically change and it propos a second characteristic which is saying the bch normalization so we want to have find the average for the variance in b layer and we add them up together um want to encourage the variance to be large otherwise um different inputs they have exactly the same output that's not good if the variance is large then it consider this architecture to be good but frankly the architecture is just a tip of the iceberg weight also matters so here it's using the
(48:17) random weights so it's very core screened eyeball Tracking not very um but actually the result Works quite well I'm leing this paper just look at given the peration of input if the output changes a lot then that's good but different batches if the variance is Big then that's good another h r sign so R this paper basically says a good model will have very dense sample wise local Minima okay so the two local minum minim should be very close rather than very far away from each other therefore the distance between
(48:58) local Minima should be close to each other okay um and therefore there's a higher probability that the gradients in different sample you try different samples X1 X2 and should have the same sign calculating the is very cheap right so in this area the red area the sign of gradient here is negative here is positive they are of the different uh signs and if they are in the Spen region here both of them have negative gradient here both of them have positive gr gradient okay uh so if you sweep starting from here to the
(49:42) blue the green region will have same sign uh the red region will have different sign um a good model which have very close local minimum should have very large portion of them in green which means the gradient should be of the same sign that's why this work's name is called gr sign so we can uh use this approach to count the number of entries that has give the same gradient and if the sign sum is large then it means a lot of region is is green and it's good so those are two very simple characteristics you don't never train
(50:23) the model you just feed it with some input and then po either the output difference or variance of the normalization layer or the sign of the gradi okay so far we talk about given a hardware platform okay the hardware platform is fixed how do we optimize the neural network architecture and now also let's open the box say you can also have the freedom to design network the hardware architecture by yourself design Asic by yourself you open up the box you can now design both the hardware architecture and also the ne
(51:02) architecture how do you design them together so that's the next chapter which we will not do in the lab but it's good uh it might be I eye opening so let's talk about the new chapter neuro architecture neuro accelerator architecture search Okay searching not only the neural architecture but also the accelerator architecture so we propose a hardware design we then propose a Neo architecture design we collect some feedback running this neural network on this hardware and then we iteratively uh twe the hardware and also neural
(51:44) network with this Loop and here are this is showing we can twick the neural architecture this is showing we can twli the accelerator architecture and both of them uh designs are important to enable specialization that's now we talk about specialized architecture runs the most efficiently so let's see what is the design space we must have a much bigger design space including both the neural architecture and also the hardware architecture much larger design space so what are the key Dimensions now we have three components right
(52:24) have the hardware accelerator uh which include several Hardware Dimensions like the local buffer size Global the number of processing elements the number of pees the size of computer array PE connectivity on the compiler side which compiler Maps the neural network on hardware accelerator so on the compiler side we have the loop orders right which Loop to process first which Pro Loop process later loop tiing size and data flow right a neuron Expo perspective the number of layers of channels kernel size whether we have
(53:06) bypass and input output weight quantisation Precision so that's a pretty big design space and the solution is to ideally we want to um do have one optimization Loop okay this is one optimization Loop to search both the accelerator chitecture and also the neural architecture so let's see first the accelerator architecture which include a pretty large design space including we categorize them into two categories one category is the size related which very simple like the buer size local buer size Global buer the number of
(53:52) pees and second one is slightly more challenging to represent in numbers like the PD connectivity which one connects to which one and our ke idea is to talk about how do we um put those non-numerical numbers like the connectivity you cannot easily represent the connectivity by a number and how do we design those non-numerical values uh so when we are mapping the um a couple of operations like a for Loop operations um into the hardware roughly there are two categories okay one is temporal maing one is special so
(54:37) what is difference here special mapping here means you can process them in parallel in space like here I have 10 multipliers then I can process uh 10 multiplications in parallel 1 2 3 four five physically there in the space you have 2 by 2x five you have two multi ERS so that's called spatial paraly you can paralyze them in space they're executed in current way and also you have temporal M mapping okay that means um you can process them sequentially okay but in different order so what determines the order uh the sequence of for Loop
(55:19) determines the order okay so in this case uh we have two lines of code indicates Hardware paradism par Rising space uh for the M and N Dimension range 16 and 16 so probably have 256 MERS here you can con currently execute them paralyze them in space um and then you have this temporal Ming including both the tiing what is the tile size the order determines in which sequence um to map these two powers in the temporal dimension so here um we categorize them into two parts one is the how do ining Vector other is mapping coding Vector okay one
(56:07) is related to the hardware one is related to the compiler mapping some of them is non numerical like the parallel Dimension choosing which dimension to paralyze in space okay so that's not easy uh to be represented by a single value and the loop order Also may not be very straightforward to represent similar like the tile size number of PE C size which is just a single value this is not uh the new way is to do index space encoding for example zero means sequence CR rxy k y S1 means CX r i c Channel XY means the height and width RS
(56:57) means the kernel size and width K means the number of kernels but actually it doesn't convey any physical information why this one has a larger index than this one doesn't make sense so what we do try to make it have some meaning we first fix the dimension position in the encoding Vector for example the first position is K second position C peral size channel number height and width colel x y this kernel number of kernels Chanel height and width the kernel size and then we assign a numerical importance to these Dimensions so this
(57:41) is numerical value the importance is a numerical value and then we can sort them right we can pick the two largest one C is the largest one he largest one so that if we want to paralyze them in space we for sure want to paralyze the dimensions with high with the high importance so we choose the first two Dimensions the dimension size is two we choose the top two and for the moop order uh we assign the most important Dimension as the outermost since it will be iterated less number of times and we put the least
(58:17) important in the inner side loop which will be very hard be executed many times more time consuming therefore we put it in the the less important the innermost so in that way we can convert this parallel Dimension selection versus the loop order which by definition is not numerical uh value we turn into numerical representation by sorting them so let's see if the search result uh give you this number like um the parallel Dimension we have two dimensions and then we choose the first two most important Dimensions
(59:00) Okay C and K since their importance value is the largest six and four we put them as the uh parallel four we have these two rows of parallel four we are paralyzing in space um since this is related to the hardware improving Vector which dimension do we paralyze in space rather than serializing time and then the parallel Dimension uh since it's a size size two we pick these most important ones and then the dimension sizes it's given by 16 then we F fill in 16 and 16 um in the entry here meaning that we Al together have 16 this
(59:43) Dimension 16 that Dimension multiply them together you have two 256 multipliers and then on the right hand side uh blue part the Tiding tiing sign and R order the ROP we see is CRX k y s so uh we follow that c RX s put an important y in the alter Loop since it will be executed less number of times the less important one in Inner Loop which is s and what their blocking titling size there a pure numerical value during the architecture search U here we search 77 3 three fill in the value for y is seven X is s r is three okay so
(60:35) everything now can be represented in numerical values we can even use the numerical value to search which dimension to paralyze in space and what is the move order so that effectiv effectively solve the mapping problem so this is the overall big picture we have three components um this is a neural network architecture design this is the hardware architecture design this is the mapping that Maps neur network on the hardware and they're all using evolutionary strategies uh we do mutation cross over and do the evolutionary strategy to select the best
(61:14) fit with low energy delay product uh this is given AR neuro architecture search space this is given a KN search space this is given a accelerator surch space and finally we find want a model with the lowest energy delay product and on the right hand side we can see the follow okay we first generate a hardware and then generate a network do the maing MCH this network on hardware and energy Rel product update uh the Offa model and then later we update the final Optimizer and this way let's compare the architecture sizing only
(61:57) uh the energy delay product will want a low energy delay will want low latency so um the energy delay reduction is good so compared with the Baseline architecture sizing only can reduce the energ product by like 20% or two 2.1 times but searching both sizing and also the connectivity helps more and here we have uh this tradeoff between energy delay product lower the better versus the accuracy higher the better we can see that here the hardware aware Hardware architecture search plus the West neuro architecture
(62:43) search together Hardware neural architecture search together give a better um tradeoff so starting from this point which is the Baseline human design and then the Hardware architecture search then is the hardware architecture search plus the new architecture search and very interesting way the data flow designed by n actually paralyze the input height and output Channel Dimension which is very different from uh the human design so we'll not Implement that in the homework just uh give you a some insight that we can do a new
(63:23) architecture and C architecture search all together all right lastly having talk about all these techniques let's look at applications how does the on for approach apply to natural language processing can applications Point Cloud application post estimation Etc so we've been mostly talking about CN at the moment but wi4 approach Works quite well for Transformers similar to uh previous design approach we can design a once for all Transformer that is a very big giant Network containing many sub networks we can design a
(64:02) smaller transformer for larger transformer for the TPU and here we are comparing um the latency and also model size the latency could be 2.7 times smaller compared with evolved Transformer and model size can be reduced by 3.7 times and the search cost is also much lower since we are sharing uh the same once SP Network amortize training cost across different search processes and there's some speed up um very good speed up and the Flop reduction actually uh we also implement this in tap out a chip tap out a chip to uh verify the idea so
(64:48) the chip is called a spaten spyon Transformer let me turn on the mic so I can hear the voice so this is a Spartan Transformer chip demo let's play it hello everyone my name is hry Wong I'm currently a pH student MIT I'm very glad to show our Transformer chip demo for efficient natural language processing so here on the left hand side we have the GUI for this demo we can input some sentence to let the CHP to process for example we can input a sentence G me flight ticket from Boston to San Francisco on August 28th we can use the Transformer to run
(65:26) the transfer models and then return the information about the classification of the sentence and also the classification results of each token a little bit so what is doing here is basically hello everyone my name is Henry Wong unfortunately I cannot interrupt the video so we have to watch it hello everyone my name is Henry Wong I'm currently a PhD student MIT I'm very glad to show our Transformer chip demo for efficient natural language process processing so here on the left hand side we have the GUI for this demo we can
(66:01) input some sentence to let the trip process for example we can input a sentence get me flight ticket from Boston to San Francisco on August 28th we can use the Transformer chip to run the transer models and then return information about the classification of the sentence and also the classification results of each token then we can use the information to call a web browser on right hand side to automatically search for the flat information and return to the GUI besides searching for flying ticket we can also use it to search for the
(66:38) weather for example what is the weather in Boston again we can use the Transformer chip to process the sentence and then obtain the location information and then we call the browser again to search for the weather and then return the weather information to the GUI on the left hand side our model can actually s support for different size and all of them will uh share the same set of Weights this technique is called super Transformer different sides will share the same set of parameters to save the an memory footprint here we can reduce the
(67:13) attention head number and embeding dimension and try to process PR music by J here we can see the correct result and we can call the browser to play music thank you for watching actually um it's Chip so cic proof to two years for us work on this process uh together actually get the best demo award at back this automation conference okay so not only for natural language processing but also for 3D Vision okay so this is a demo from self driving CH with the light s so um this is KN segmenting different objects the road that d by
(68:04) area trees Etc can see it's pretty slow only 3.4 frames per second but on the aut on the bottom our workb sparse point also new search to search a very fast architecture that can run at nine frames per second this get many awards in the uh lar segmentation competitions so the basic idea is same using this one approach so we TR super Network that can support fine grein elastic number of channels and also elastic deps so usually can TR with deps of three deps of two and three one two and three similarly for the um
(68:52) channels they share the weight and later we do the evolutionary architecture search buing the super Network we sample a child network measure the ly and accuracy then we SLE if it doesn't fit our demand and finally we do the evolutionary architecture search with mutation and also cross over with these two pars cross over to do evolutionary architecture search very effective in reducing the latency next Once for all can models which is anying plus the can ideally we want to run this photo editing locally on our
(69:35) laptops or mobile devices like iPad but you can see this is very slow so here we have the this input image have this bar we want to turn pull it the right here we are making a b younger we can also uh change other aspects like changing the hair color Etc but it's not quite affordable to run on a mobile device like 3 seconds too slow right so we can now switch gear to this CL again and then we make a smile it's very fast only 0.
(70:18) 3 seconds this pretty much in real time we can also Slide the young button to make her look younger and change the haircut color and finally we can click the finalize button to run the full Network only once and then to get the finalized output which is of high quality right if you only need one model then just pull it but if you need different models like 7 bilon parameter 35 13 bilon 7 3 one you want the whole landscape uh similar SL switch then this this m help helps so here we are tring the uh once four approach for the clan model
(71:29) different generators smaller sub generator low cost for faster prototyping you can quickly a prototype and finally you run this large sub Network high quality good for fin for finalization so this demo is open source it's Deployable on a MacBook it was done actually three years ago in 2021 at that time we can run a Macbook at 0.
(71:55) 3 second now it might should be much faster feel free to download and play with it so next we also for have that for post estimation so post estimation is basically finding the skeleton joints on your body the arms head the the legs knees Etc so previously uh it runs quite slowly but we can push the front here to make it about four five times faster okay running the Snapdragon mobile CPU so this also derived by Hardware aware you architecture search we have a couple of more demos it's all of them are open source on our
(72:39) Labs GitHub for on device person car detection this is on device gaze estimation so gaze estimation is very useful for um automo driving cars like in my car uh if I don't look at the road my car is going to alert me when I'm using the autopilot mode so basically tracking where you're looking at if you're not looking at the road car will send out an alarm so this is good useful for driver monitor system DMS right hand side is me local device segmentation very interestingly M approach also works for Quantum AI this
(73:21) is done by my student PhD H1 uh so we find actually there is a large gap between the theoretical accuracy of doing this I four only class classification on Quant device versus the measured um accuracy Gap ranges from 80% to only 40% so drop of accuracy so what is the reason because Quantum circuit is highly suffering very heavily from the noise so not necessarily have better accuracy a better good results if you put more circuits but sometimes you want to put more a less number of gates only put them in the right place more gates
(74:01) more noise so we want to start with a super Circuit and then SLE man sub circuits and then search the best subcircuit that meets your um accuracy uh demand so we want to search a sub circuit that is very robust you know through those small magnitude Quantum Gates and as a result we can um improve the accuracy from 40% back to 80 85% right and this was measured on a real you see a real cont here same principle can apply to different disciplines very interesting and that work is open source like torch Quantum okay very recently a media
(74:45) adopted one approach and applied it to large language models okay so uh this is inference adaptive in inference we can generate different models for h100 for or we can have a larger model to fix h100 say larger model for h100 mid model for RPX 490 small for AR platform so multiple models in one sub Network for the qu GT can run the full Network for the desktop we can run middle Network for mobile phone you got the smallest Network from converting the twin model um this is using a very similar approach we can have different dimensions for both the
(75:35) FFL layer and also for the tension um we can have a router to determine whether we are running the half or the quarter 1/8 of channels so mining in one now flexible lar language models okay so that's the summary of today's lecture we talk about how do efficiently do performance estimation how do we aware Nas using prox once for approach in particular once fall approach we will Implement that in lab three and finally we talk about Nas applications in the next lecture we'll talk about a new chapter knowledge insallation okay that's all thank you
(76:15) for listening
