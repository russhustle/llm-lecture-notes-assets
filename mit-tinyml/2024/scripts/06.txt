(19) EfficientML.ai Lecture 6 - Quantization Part II (MIT 6.5940, Fall 2024, Zoom recording) - YouTube
https://www.youtube.com/watch?v=kjHuIzQ54v4&list=PL80kAHvQbh-qGtNc54A6KW4i4bkTPjiRF&index=11

Transcript:
(0:01) all right good afternoon everyone let's get it started so today we are going to learn about lecture six the second part of the quantization lecture uh so pre in the previous lecture we introduced the basic techniques of quantization which is a super important technique both in the evion age and also in the large language model age uh quantization is very widely used no matter if you want to deploy U the neural network on phone on the edge device or in cloud this is a very useful technique wiely very widely adopted in industry and
(0:32) actually a big shortage of Engineers currently who can specialize in such techniques so in to this lecture we'll first review the linear quantization and also we'll cover two important Concepts one is post training quantization ptq the other is the qat quantization aware training how do we fine tune the model so that you can recover the accuracy remember like in pring we talk about pring and then how to find T PR model to recover the accuracy and similarly here inition we quantize it and then fine tune it so that's q a and
(1:08) then we'll talk about binary and tary quantization to further push the quantization bits to lower bit only one bit and finally we'll introduce automatic mixed Precision quantization not every layer require the same number of bits just like in pruning some of the layers are more sensitive some of the layers are less sensitive so we want to treat different layer differently to minimize the storage and maximize efficiency in similar case for compensation okay so this is where were where we were left over uh since last
(1:44) Thursday uh we talk about K me first started with the floating Point weights B 32 for the weights b32 activations and then the second part we quantized the weight using K means quantization okay so for example here this is our for Matrix we have a code book at each entry we just store a two bit or four bit a low Precision index looking into the weight uh so that's the code book computation is still in arithmetic since we have to De quantize the weight into um floating point this save storage but say but doesn't save the computation
(2:26) which is too useful for large language model inference batch size of one where uh it's memory bonded not compute bonded and weight is the memory B weight is the memory bottleneck so very useful Technique we are going to implement that in the lab to hopefully you are all uh you will downloaded um we have them available in our website for lab to last lecture we also introduce linear quantization so both the weight and activations can be quantized to integer values and the computation is also done in integer arithmetic okay so we have a
(3:05) bias we have a scaling Factor derive the equation for that so this is the K means quantisation we have a cluster index we centroid and then we can f tune the centroid we can see the accuracy is very well maintained we can even combine pro we can even combine pro and quantization uh to further push the uh model size be a smaller when we are talking about linear quantization we introduced this 0 point and the scaling Factor okay so for two for two bits we can represent four numbers in four colors and the this is
(3:44) the con value minus 0 point multipli with the scaling factor and we can get a recovered p32 weight so remember how to calculate the scaling Factor so scaling basically Maps the integer range to floating Point range okay so this is the integer range this is the floating Point range and this 0 point Maps z0 in integer representation into zero in the floating Point representation and we went through all the math to calculate in a quantized uh Matrix multiplication so there single layer MLP what is the breakdown about
(4:29) computation okay so this is the majority of the computation then in low Precision integer F the bias then this can be done by uh linear operation and also the shift operation and finally plus the uh integer add for convolution is very similar we just replace matrix multiplication with convolution majority of the job is done uh in the integer convolution okay so how should we get the a optimal linear quantization parameters s and Z so we'll talk about quantization granularity similar to pruning granularity uh where we can
(5:10) prune a single weight we can prune row of weight we can prune the whole Channel or whole filter or the entire channel so there are different granularities for pruning and similarly there are different granularities for quantization and then we are going to talk about uh dynamic range clipping so you may have a pretty big dynamic range how do we clip them and finally about rounding like 1.
(5:39) 5 should we run it to one should we run it to two stastically round it we talk about rounding so let's start with but I with quantization granularity so we'll first talk about penser quantization you use one scaling factor to scale the whole tensor okay so this is percent tensor quation followed by per Channel quantization okay so for one channel all the values in one channel share the same um scaling factor and which has more scaling factors per channel quantization will have if you have like seven channels will have seven scaling factors right
(6:18) here for tensor poition you have one scaling factor for the whole tensor okay and finally uh group poition this is very important for after Blackwell architecture and in the Blackwell architecture group quantisation is very useful for bit or below um we introduce per vector poition and also the MX data type Shar micro exponent data type which is a very interesting data type um in lower Precision okay so let's start with the potential quantisation which is very widely used uh these days so we have one scaling factor for the
(7:02) whole tensor okay using a single scaling factor for the whole weight tensor that works quite well for large models but for small models people observe this degradation of accuracy why is the reason what is the reason this per tensal quantization suffer from accuracy degradation so if we plot the channel index here is the ring range okay we can see the range actually differ a lot some of the layers have a pretty big range some of them have very little range if you use the same scating factor for these diverse channels of course you
(7:43) cannot have very good feeding for the all uh so the failure comes from large difference in the ranges of weight across different output channels and some of them have pretty big outliers outliers are the with values that has either very big value or super small value compared with other values so that caus the um you're wasting dynamic range You observe very different Behavior compared with other channels so if you use the same scaling Factor across all different channels you cannot very well represent them so the solution is per Channel
(8:25) fation we want to treat these different channels individually okay so that we can have a specialized scaling factor for each Channel and how does that look like so this is our familiar 4x4 Matrix input Channel output 4x4 we'll compare peral quantization versus the previous penser quantization using penser quantization we want to calculate uh first the scaling Factor okay in order to calculate the scaling Factor we want to First have the r Max which is the max value uh the which is which have the max absolute value in
(9:12) tensor which is 2.12 in this case so our Max is 2.12 and here we are using how many bits two bits okay two so Q Max equal to equal to one okay so R Max divided by q q Max using the equation we derived in the last lecture we can find the scaling factor that map this integer range into that floating Point range so the scaling factor is 2.
(9:42) 12 so we have the scaling Factor now we can uh quantize the um Matrix and since we are using um symmetric quation so 0 point0 so for example 2.09 right here um divided by 0 2.12 that's approximate 0.9 something is very close to one so Point has that this is minus 0.98 divided by 2.12 the absolute value is smaller than 0.5 therefore the quanti to 0o okay on and so we can repeat that and quantize each value so this is the quantized answer and if you can compare with the reconstructed tensor we multiply each value with the scaling factor of course the pias is zero so we
(10:33) can directly multiply so this is the reconstructed Matrix and we can calculate the difference 2.28 and now let's see how does it look if we do the per Channel condensation okay rather than treating the whole tensor now we treat each row Channel individually so let's first look at the first row what is r Max our Max is 2.
(11:06) 09 um therefore the scaling factor is 2.09 second row R Max is 2.12 scaling factor is 2.12 and so on okay different channels now we have different scaling factor which is the max value of absolute value of that row and now let's see how do we quantize them now we are using different scaling factors for different roles okay and so that we can poti it in this way for example previously 2.
(11:37) 09 if we are using potenial cotization we divided by 2.12 now we are dividing that by 2.29 it's very close actually one and the second value divided by 2.09 absolute value is smaller than 0.5 therefore it's rounded to zero and this is the reconstructed uh tensor compared with the previous reconstructed tensor using for tensor quation now the value is more diverse you have 2.
(12:08) 09 2.12 1.92 Etc and the reason is that we are using different skaing factor for different channel so actually the um reconstruction error the quation is smaller than the perens compensation so we just want to use this very simple example to show you the intuition where the per channel quation can have a you have a lower quation error compared with per tenser and in particular what do we see which one is different this is the quantized tensor using panel this is quantize tensor using penser quation we observe this value is actually different
(12:55) the rest of value are all the same one Z One Zer one 0 zero only this value is minus one this is zero what was the reason for that the sking factor right for example if you look at this value 1.03 using the potential quation it will be divided by this sking Factor okay divided by 2.12 the absolute value is smaller than half okay therefore it's quanti with zero because the value is smaller than 0.
(13:31) 5 surrounded to zero in this case but in Valu is minus 1.03 will be divided by the scaling Factor 1.92 the absolute value is larger than 0.5 since 1.03 is larger than half of 1.92 therefore it's quanti to minus one okay so that's the startle difference you have a fine grain more green scaling Factor compared with the single scaling Factor therefore the represent ation will be more accurate smaller quation error using the peral quation and penser quantisation so of course there's tradeoff what's bad about poens panal condensation right you need to store
(14:18) more values more scaling factors previously you need only one Skilling Factor now each Channel you need one so there's no free lunch always a trade off need store more uh scaling factors okay so let's continue if we want to go even lower Precision we need more accurate representation and more flexibility right so as we can actually shrink the group size rather than per tensor Channel we can further reduce the group size we call it group compensation okay uh using group quantization we introduce the per vector
(15:03) quantization and also the MX data type mro exponent data type which is very new so let's first motivate uh group quantization so imia released this blackw generation GPU uh which is super powerful if you compare the performance so comparing the h100 here this is gb200 uh gra Blackwell 200 um this is running another model um has a very big Improvement compared with the h100 blacko architecture compar with a hopper architecture there 7x Improvement okay and on the right hand side we see a new data type uh a new T core called fp4
(15:57) core and so these are what we already familiar like fp6 bf16 four and half PA flops in a 104 nine p Ops fp8 fp6 nine p flops fp4 18 P okay 2X 2x the pixel put compared with fp8 and FP intank and so forth okay uh Blackwell GPU uh introduce this macro tensor uh scating so that we can maintain the accuracy of fp4 um models okay and in order to do that um that's basically what we are going to learn the group quantisation having even smaller um group size to have a fine grain scaling Factor so that you can have more
(16:53) accurate representation using lower Precision so let's jump into that we'll first introduce the vs Quant per Vector okay per Vector scaled quantization so previously we have a scaling factor for each tensor the per tensor quantization and now we want to have a scaling factor for each sub Vector like this this sub vector and Vector okay so rather than Ral to scaling Factor r q minus Z now we have two levels of scaling Factor one Global and one local This Global scaling factor is a 14 point qu green scaling factor for
(17:36) the whole tensor we also have a integer per Vector okay F GRE per for each Vector smaller granularity scaling factor and we are going to multiply these two scaling factors together to scale Q minus C so that's achieving a balance between accuracy and the harder efficiency by the fact that we can have a more expensive floating Point scaling Factor but they are at a very po regularity so let let's try to understand this again which is more expensive scaling Factor the potential one or peror scaling factor which one is more
(18:24) expensive this is FP 16 this can be in four or in8 so this is more expensive right FP 16 bit very expensive it's getting effective but we can have it as a very for grity the whole tensor have only one such expensive fp16 High Precision scaling Factor okay only having that is not enough we compound that with the less expensive integer scaling Factor at final granularity okay so we have a less expensive scaling factor for uh only low at low Precision but at a finer granularity just a couple of Weights we have one scal factor so that's a very
(19:15) smart way to have on both sides so in order to calculate the memory overhead let's see how that's work so we have a four bit quantisation with the weight have four bit per Vector scale so this is four bit per Vector scaling factor for every 16 elements so this is 16 element right here and we also what is the accept effective bit width so each value is represented in four bits and then we also have a four bit scaling factor for every 16 elements okay so the four bit sking factors amorti across 16 different elements
(20:00) plus the original four bit so it's alog together 4.2 25 4.25 bits that's the way to calculate the overhead of two level skating so in that context we can have multi uh multiple levels of scaling Factor okay uh we can have the first level zero level one and continue uh different levels so let's see um what we have learned so far how does that fall into this uh big picture we introduce this per Channel quation okay we have a scaling Factor Z level zero scaling factor for the whole uh for the whole channel so data
(20:46) type for the weight in red is in four and then the group size L zero level zero group sizes per channel the whole Channel share fp16 uh scale getting Factor so thetive bit width is four since we have four here and assuming we have many channels thousands tens of thousands channels in large language models um this fp16 divided by 10,000 that can be negligible so effective bit withth is roughly four and Bs Quant in BS the data type is also into four so each value four bit and then the L zero group size is 16 that's me
(21:29) every 16 elements we have a scaling factor and the scaling factor is I sign in four okay and the next level group size is per channel the whole Channel also share fp16 um scating Factor so what is the effective bit withd four and then another four bit skting Factor about shared by 16 values and this FP bit can be divided by tens of thousands that can be ignored so we have two and four and a quarter that's the effective bitwidth so now comes the famous uh mx4 MX6 and mx9 data type okay so let's start with MX mx4 so the data type is uh S1 M2
(22:20) remember in the last lecture we have one sign bit and we have two mantisa what is missing here the exponent bit right where is exponent bit I'm sure they are here so we have one exponent bit that is shared by two values okay we have one bit shared by two values we also have a bit scaling factor and this E8 only uh exponent okay shared by 16 values these are the two levels of scaling factor and now we can quickly calculate what is the effective Precision bid withd for mx4 one bit for sign two the Mana three bits right and then we have
(23:07) two bits shared by two values oh sorry we have one bit right here E1 shared by two values so that's half a bit and finally we have eight bits shared by 16 values that's another half bit so three plus half a that's all together four bits okay that's why we call mx4 we also have MX MX6 which is one sign bit and four mantisa that's four that's five bits Al together one exponent share by two values and eight exponent Shar by 16 values so five plus a half plus a six s similar for the mx9 we have one s bit seven mantisa bit 8 here and then we have
(23:56) one one bit of exponent sure shared by two values group size of two and then eight bit of exponent shared by L1 group size which is 16 so that's 8 plus a half plus a half plus 9 bits so these are the more fancy uh representations going lower bit precision and group conation which is super helpful to maintain the accuracy for such fine GR quation grity all right so let's switch gear to talk about next topic about dynamic range leeping so dynamic range matters a lot since our it can change across different inputs some of them may be large
(24:46) them may be small but how do we determine the dynamic R because that's very crucial since the scaling factor is calculated by dividing the dynamic range between the quanti value versus uh floating point value so given a bunch of images they may have very different dynamic range for each so to determine the floating Point range the activations we need to uh gather these the statistics before deploying the model and there are gener generally two methods ways uh to calculate the dynamic range so if you have the model is TR
(25:28) by yourself so if you train the model by yourself if you have access to the training recipe during training we can actually calculate the dynamic range by maintaining a em the exponential moving average and what is that we maintain this R Max and me is across different iterations so at T minus one this is T this So RT basically equal to R for RT + one minus RT okay we are using this discounting factor to Discount the previous uh dynamic range uh with the current dynamic range okay so the larger alpha more we are using the recent um dynamic
(26:20) range okay so we uh smooth the dynamic range by using arpha what is the extreme case Alpha equal to one right if equal to one we are not using any of the old dynamic range because this term become zero okay so that's the intuition but that is only possible if you have access to the training process if you train the model by yourself if the model is trained by some somebody else we don't have access to this whole training process therefore it's very hard it's not possible to estimate the dynamic R in that way but is the
(27:02) most common case sometimes we just download a model from huging phase we quantize them we deploy then in that case how do we um get the dynamic range we want to maintain a calibration batch calibration data set and just use that calibration set like a bunch of images or a bunch of text to calculate the U to estimate the dynamic range we can use the dynamic range for you in this calibration Set uh to represent assume that is representative for the uh input images or text from other sources okay so by running the cation
(27:53) batch we have two ways uh try to find the Max okay we probably don't want to use the full R Max so actually there is a trade off here say this is the distribution um of the activation okay we can um we observe there is a very uh long tail of distribution some the values are super big but you have a very small a small amount of big values very small amount of these very negative values so if you set the mean and Max to be uh like here versus you have a big waste you're are wasting lot of the representation because uh you have only
(28:42) a few very values in the extreme Corners so assuming we want to push the Min and Max to be somewhere here where there are at least some weights because if you squeeze it closer the distance between each quantisation um boundary will be uh smaller and the representation for these values in the middle these values in the will be more accurate if the quantization distance between adjacent bucket will be closer right so there is a tradeoff the mean and Max you don't want it to be like the exact Max mean value you don't want it to be very small
(29:24) but there might be a sweet spot so if it is it is a laash distribution we have close form solution to find The Sweet Spot okay for two bit that is 2.83 B 3 bit it's 8.89 89 B okay so that b is the Laing parameter can be estimated from the calibration input distribution but this is a very rare case rarely the uh input follow this La plashing distribution so should we do if doesn't if it doesn't follow the laion distribution so very widely used technique is to use the K Divergence method to calibrate the range so we
(30:16) want to minimize the loss of information okay since the in integer models inodes the same information as the original floating Point model so we want to find the dynamic range by minimizing minimizing the K Divergence so this is the uh equation for these two distributions p and Q one is before quation after quantization and we find the uh mean max value such that it can minimize this K Divergence let's give some intuition say this is the distribution this distribution of the activation value this is absolute value ranges from zero
(31:05) all the way to 400 some of them might be even 480 but most of the value okay so this is the normalize number of con okay so L of value is actually below like 40 and then has this long tail if we decide to set the max value to be here what happens so all the values above in this long tail will be clipped with this value roughly 50 okay uh 40 or 50 right will all the value above will be clipped to 50 and you count the count will be the sum of all these values count will be a uh this is the count okay so the this is before and
(31:56) after the um finding max value using the minimizing K Divergence what happens if you have a larger max value so we you will have a shorter tail on the right hand side and value um near the uh quantization the max value will be more accurate however the range will be larger therefore these value these values we actually have a lot of them will be less accurate because the uh dyamic range is now larger and quantisation error is now larger it cannot be too small it can not to too large the best way to calculate that
(32:44) is by minimizing this CH Divergence and let's see some other examples so alexn res 152 Google net so this is the clipping optimal clipping range discovered for these different models okay none of them is the the largest value they are not using the largest value this is the value but they are using somewhere smaller than the largest value we just um clip the largest value to be quation max so this figure further illustrates the intuition um between why we need such kind of clipping rather than using the max value okay so if we have such a
(33:41) distribution such a distribution very long tail um long if you choose the mean and Max according to the real mean this is NE negative 0.8 this positive top positive 0.8 you if choose the Max and mean this way will be your quantization range and you can see the quantization noise is pretty large because the adjacent quantization centroid the distance is pretty far from each other and in particular you have a low density data just a few of them is in this big range versus we can clip it okay this is the new mean
(34:25) new new Max and this is the d dnamic range and the distance between adj adjacent CID becomes much smaller right much smaller compared with this one so the quantisation noise becomes much smaller and all these values will be clicked through here that's still okay why because we just have very little very few of them in this range very of them in this range so it's fine to clip them to the desired max value we want and this is actually a quite helpful uh technique very widely used so on the right hand side is showing the
(35:06) animation this work is called octav from from Nvidia it can automatically find the optimal uh cion range and on the left hand side is showing that the uh mean square error is convex okay cannot be too the clipping scaler cannot be too small or large you you're too small you waste a lot of the you are too large waste a lot of the dynamic range if are too small then many vales will be clipped to the boundary so there's a sweet is comx there is a sweet spot in the middle and we can use this technique to find the
(35:46) optimal leum range and after applying octav you can see the accuracy can be well very maintained compared with the ip32 accuracy okay so finding the granularity very important and then clipping is very important okay any questions so far yeah [Music] the value is the determine if you have four bits you have 16 values one type yes correct all right um no question let's take a quick break and come back in five minutes to continue discussion about Ring all right welcome back let me turn on the mic so you can hear me all
(36:56) right so let's jump into the third part of quation about rounding okay so this is our conventional wisdom if you have a vector 0.3 0.5 0.7 0.2 you want to round it to like zero one okay so this round to nearest but tradition uh contradictory to our conventional wisdom R to neous is actually not optimal um so weights are correlated with each other the best Runing for each weight to the nearest is not the Runing best for whole tensor okay so sometimes we want to want to some have Randomness for example the same tenser
(37:42) sometimes like 0.5 uh if can run it to zero it to one not necessarily running it one might be the optimum sometimes we can uh even run it to zero okay um so such stochastic Runing usally helps during the quantization process so instead of running to the newus we want to choose NE Runing down or Runing up um stas to get the best reconstruction we can also learn a Delta okay between zero and one we added to the original weight and then we run it to the nearest for example rather than um just finding the nearest round to the nearest we can add
(38:33) a value this would be learned we pass it through a function H is function to m v the range between zero and one between zero this is a possible solution using such rectified sigmoid so the HV is between Z and one we add it to W and then we run the nearest so that we can learn a tensor with the same Dimension okay so this V is a random variable of the same shape same Dimension followed by a regularization term encouraging the uh hvv to be binary between either zero or one okay so this is called an very popular paper helping
(39:22) recover the accuracy so just intuition is that um Don't just run to nearest US you can apply some stochastic to add Randomness during the r rounding process really helps so let's do a quick summary about linear quantisation what we have learned so far we learned about zero point scaling granularity per tensor qu scen per Channel or group quantization after four below bits group quantisation is getting super popular super important IMX 4 6 ip4 Etc uh range cing we can use different techniques to find the optimal range
(40:08) clip it and then run the um runting techniques so by applying that Tech these techniques um for the weight activation we can actually very well maintain the accuracy for a bunch of models like goet reset however some of the smaller models like mobile they still suffer very heavy accuracy degradation even after applying the all the techniques we have learned so far like 10% 2% degradation of accuracy which is quite bad so what should we do let's talk about um fine-tuning techniques to recover the accuracy for these models so
(40:54) let's talk about Q8 the pation of where tring a very simple uh technique was already introduced in the last lecture about using u p me quantization where we have a code book after we get the gradient which is of the same size as weight so this is the weight gradient their Dimension are exactly the same we sum the gradients corresponding to the same color centroid sum them up multiply by the Learning rate subtract it from the original centroid to together find tune the croid so that's very simple for the K means
(41:34) quantized neur Network where we're just updating the CID sub code book actually it's more getting complicated uh if they doing linear quantization okay so in the linear quantization uh the weight and activations are all low Precision okay in eight or in four ip4 in that case during the training process we actually maintain a full Precision weight in the back okay maintain a full Precision not 4 bit 8 but 32 bit or I 16 bit a full Precision copy of the weight uh in back throughout the training so that we can accumulate very small gradients
(42:21) small changes um acculate accumulate that um you know weight without lose without loss of precision okay and once the model is fully trained this fp32 full Precision weight can be thrown away we only keep the quantized weight uh deployment time so this is the uh layer minus one layer n layer plus one these are the activations these are the weight weight will be quantized this is the full Precision weight in the back quantized it to calculate layer n the count being R and then get the output for next layer so for the backward process
(43:07) we update uh this full Precision weight so that we don't lose any precision and remember during the activation here we calculate the activation and we should quantize that Activation so that the boundary of output the value is quantized so this whole process is called simulated quantization or fake quantization during the training process because uh the weight is cont to qw but it's still using a 32bit floting point register to represent that value although the value itself is cont but we are still using a rooting
(43:49) Point number to for it so we take train the model taking quation into consideration by adding these uh two quantization nodes compute no one is quantization quantizing the weight the other is quantizing AC the activation to simulate this quantization process so we have qw here qy here so forward process is very simple if qw have q why what about the backward process how does the r flow um to weight because the quantization this is the basically quantization activation if you think about it as an activation function okay so below Z and 2.5 is all
(44:45) zero between 0 and five to 1.5 there are one between 1.5 and 2.5 there are two right so it's actually a St function uh so the quantization make makes the input value to be discret okay the output is discretized what's the problem here for the gradient the gradient is about zero right gradient become zero then we can learn nothing if you have a zero gradient so actually we are using a technique called straight through estimator St straight through estimator we simply pass the gradient through quantization as if it has never been
(45:30) quantized if it is just identity function so the gradient to weight is the same as gradient to quantied weight you treat the gradient of the quantied weight versus gradient of the weight to be exactly same so this is the quantization compute node this is quantize W okay so you treat the gradient between this purple line and this purple to be exactly the same as if it is just a identity function in this blue quantization node what happens becomes a density function okay making the uh R calculation much easier so this is the full picture
(46:16) Okay so the purple line is backward pass during the backward pass before and after the condensation node gy is exactly the gradient of quanze Y and here um the GW is exactly gradient of the quanze W as if this blue quation node is an identity function so we can stra use this straight through estimation technique um to estimate the gradient for the um floating Point weight and floating Point activation by using uh quantization wear training so here are the qu training results here are without the quation r training we can compare the
(47:02) accuracy it's drastically improved a lot so potential quantization improved from 0.1% almost nothing to 70% so Channel quantization originally is better it's like 59% now it BEC 70% so uh this quantisation R training really plays a quite a helpful role in the accuracy recovery some of the models like uh n mobile here the original accuracy is already pretty good um so the conation where training GES some marginal Improvement but for large language models which is very cly to Tri um it will be the desired scenario will
(47:43) like this and just using post tuning quantization without any fine tuning the accuracy should be very good we'll cover that in the large language model section in details Okay so that concludes the linear quation part can we go even smaller okay so we'll introduce binary andary quation just use Z one maybe minus one to represent the weight and activations and let's see what kind of magic we having here about binary and ternary quantization to further push the quantisation Precision to only one bit so let's see a example weight mod
(48:27) dot product with activation 8 * 5 minus 3 so this is the weight this is the activation and we assume the bory is 1x computation is 1x each operation plus add and also and multiply accumulate we can quantize the weight to be binary okay rather than 8 - 3 5 1 we have 1 - so we bized the weight if it is positive then it's one if it's negative then minus one so we no longer have any modifcations but everything is uh plus or minus so the memory 3 32 times smaller for the weight previously you need 32 bit now you need only one bit plus
(49:18) minus and the computation is roughly 2x L flops because previously you have a multiplication now you have no multip only add so how do we that one way is to deterministically binize that if the weight is positive then it's plus one if it's negative then it's one that's basically the sign function very straightforward but you can imagine this results in a big degradation of accuracy just now we mentioned adding some Randomness really helps with quantization so we can do stochastic finalization like in this equation here if it is
(50:00) smaller than minus one is deterministically to be zero if it is above one is of course but between minus one and positive sometimes it can be minus one sometimes it can quantized to plus one if it is zero you have half probability to be um quanti to to Zero have half of the probability to be quantized to positive okay I can have a linear interpolation between minus one and plus one but it's harder to implement since um we need the hardware Hardware gen uh random number generator in Hardware that's not straightforward so let's see what
(50:43) happens to our familiar Matrix this 4x4 Matrix we use many times if binarize them uh to one bit just using the sign before and after the accuracy degraded by a big margin div degraded by 21% and the quation error is 9.28 pretty big quation error and what happens if we um do some trick okay so we can have a scaling Factor okay we have a floating Point scaling factor which is ARA in this case uh so to make sure the sum of all values the absolute value sum of absolute values of all the elements stays the same so here scaling
(51:33) Factor will be calculated by the R1 Norm which is the sum of absolute value divided by n since every entry uh the absolute value is one so we have n elements 16 elements in this case um then we can just calculate the alpha actually to be 1.05 okay which is equal to the one Norm divided by 16 values and if we multiply this scaling Factor we can see the quation error slightly decreased and the uh accuracy Delta actually improved a lot actually becomes 0.
(52:17) 2% um a positive 0.2% which is very interesting so that's now we PTI only bize only the weight now we want to binarize not only the weight but also the activation okay so let's see what happens if both the weight and activation is binarized so we have 1 * that's one * - one that's- one- that's one okay so this the truth table um let's see another operation which which is X4 operation okay X operation if this both values are the same 1 one 0 then the output is if the two inputs are different one Z 0 then the output is zero so that's xn
(53:11) operation you might have learned in 6.1 91 so what's the relationship between um binarized weight and activation versus the ACT operation actually they are I correlated so if we calate you sum them up for the X nor results okay so this is One X with one this is zero x with One X with 0 x with one you can get 1 + 0+ 0 is one so naely doing this actually doesn't help it's minus 2 versus one they're not the same so what's relationship how do we use AAL operation to to implement the binarized weight and activation since X is just so simple to
(54:03) implement in Hardware so let's see how do we match them so um assuming everything is minus one okay so since the smallest value is minus one okay um if we have everything is minus one if everything with some the up that's actually minus 4 and um but here I if we modply the sum of the X results with two okay so this is the sum of X result which is one times two or we and times can be very efficiently implemented by shift to the left for one bit minus 4 plus the uh count times 2 that's minus exactly the value on the left okay so take another look
(54:56) assume everything is zero okay we just count the number of ones in this case uh because only if both values are the same then it's one otherwise it's zero okay if it is it's corresponding to everything on the left is minus one so that's why we assume if everything is minus one and add the difference how do we calculate the difference is number of ones in this case times two okay since the difference between minus one and the plus one is two so you have one more you add two have have two more you add four have
(55:32) three more you add six okay so just count how many values are one and then times two okay and then you can match the left and on the right hand side so put it in math uh it's equal to minus n is the number of elements in this case we have four elements 1 2 3 4 minus n * plus two times the sum of xor operation so minus four since we have four entries here we calculate the xor sum them up to be 1 * 2 and add minus 4 so that's 2 so we can use this very simple x operation to calculate uh the where both the weights and activations are
(56:32) finalized and this count operation has a name is called pop count and return the number once in this array the is finary either one or zero pop count operation is basically applying on this binary array and count the number of Mars return a number of one is called po count so using the pop count uh function um the final result would be minus n plus pop count shifted by one since shift by one is basically multiplying two okay using that we can easily calculate the uh dot product very easily so for example here this is the original
(57:21) floating Point unquantized weight and activation this is quantize weight and activation uh we just do the operation um and then shift it by one um and then plus four so that's the result so the operation rather than multiplication and add now it becomes X and pop C both are very cheap to implement in Hardware the memory is 32 times smaller since both the weight and activation becomes one bit the computation is roughly 58 times less why is that that's roughly the RIS making that's roughly the complexity between
(58:00) aore popcon operation versus MAC operation so in that way we can use very cheap Hardware um you no longer need to have multipliers or adders you just need to have the exore and pop count operations and what is the accuracy so if we only quantize the weight leaving the activation to be 32 bit you have very little job of accuracy um if you have both the weight and activation to be binarized actually a big job of accuracy but if you use the X net xn operation the accuracy is improved but still very noticeable degradation of
(58:49) accuracy and if you want to see actually detailed implementation I actually did one rle open source on fpga only use these two operations one is exal Operation the other is popcor operation we synthesize that wrot very L we syn that open source can be deployed on I sck fpg very small it's open source on our GitHub feel free to check it out okay so since the accuracy is too low here maybe can we do better by using uh give it one more CID giving zero right rather than minus one plus let's have a zero and make it
(59:29) tary so turn weight net works the are quantized to plus one minus and also zero so we have a threshold above the threshold is positive between that is zero below the threshold is minus okay and how do we calculate Delta so there is empirical value you find the ABS this distribution about the absolute value and then multiply by 0.
(59:56) 7 so that's Spirit empirical I use that to determine this threshold okay so given this is our familiar weight Matrix and then we calculate the threshold 16 entries we some the map and then times this empirical 0.7 so Delta is 0.73 so use that as the boundary um 2.09 is above the boundary so it's point to + one 0us 0.
(60:27) 98 is below the boundary so it's point to um minus one and so on like this 0.09 is between the minus and positive boundary so it's point to zero okay we can also find the scaling Factor okay find the scaling Factor such that the uh sum of absolute value Remains the Same between left and the right so here the scaling factor is calculated as 1.
(61:00) 5 because this is the counting the number of non zero elements divided by the number of non Zer elements here which is 11 in that way we find the accuracy can be improved from 60 to 60 65 but still there's a gap with the full Precision 69 so how do we further improve that rather than using theistic um we can also TW rather than using this fixed uh stealing Factor we can use two trainable parameters the positive weight and also the negative weight to represent the positive and negative scales in the quantisation so this is the original
(61:41) full Precision weight we normalize them to be between minus one and positive and then we find the threshold to be T and we quantize them and then scale them by two learnable uh sating factors W positive and W negative so the final weight becomes minus W negative zero and positive W positive we can train this WN and WP to improve the accuracy and finally the accuracy in ttq further improved on top of the two bit twn um to further match the original full Precision model so that some tricks we can play for Turner neuron
(62:29) networks so this is the big picture we started with floating Point weights floating Point arithmetic and then we talk about this K means quantization now we have integer weights plus a floating Point code book in still using floating Point arithmetic and then we have this is probably the most popular near linear quantisation we have integer weights and also integer arithmetic we talk about a lot of techniques to find the right arity to find the clipping Etc and finally we have this binary and turn conation to have binary and turn weights
(63:04) using bitwise operations but here I want to point it out that not necessarily the lower better there should be a sweet spot and roughly four bit is commonly a great Sweet Spot these days it might get lower in the future but there's a treat off right so the rithmic intensity matters a lot for how efficiency so you have more compute um you might be Memory bounded so if you go in um when are reducing the Precision memory footprint decreases linearly but the compute um the actual model expressiveness the Flop bitwise
(63:46) operations actually reduces quadratically so the compute reduces faster than the memory okay reduces slower and the memory is more expensive expensive than the compute therefore the expensive stuff is reducing slower the cheap stuff is reducing faster so there is a imbalance you get diminish return after that so that's something to pay attention when you are reducing the Precision and usually you don't want Corner One Design knob to the extreme you want to utilize all the design knobs for example pruning quantization distillation neural
(64:24) architecture search all different typ techniques together all right lastly just like we talk about um the sensitivity analysis for pruning where different layers may have different sensitivity some of them may have higher pruning ratio some of them may have low prining ratio that's actually the similar case for quantization so here we have three layers they're all quantized for the weight and activation all quantize the same number of bits all eight we can also do mix Precision quantization okay so for some of the layer which is
(64:59) more sensitive like this layer we use more Precision some of them is less sensitive like here and we use fewer Precision there are lot of choices right from one bit to eight you have choices but weight and activation you have 64 choices you have n layers that's 64 to the end um number of design design space have so many choices in choices you can use different techniques to search in this large search space you um here in this paper called HQ how we aware automated quantization with mixed Precision we use this actor cretic model
(65:40) our agent to search that uh we map it to the hardware and then we get a direct feedback including the latency and the energy so that we can uh feedback the reward to actra crique model and to give the Next Generation of the action I think that's over complicated but that's rule of Thum is that um you want to have different some sometimes you want to have different Precision for each layer some of them you can keep them in fp32 or fp16 if it doesn't quite hurt your accuracy like some of the uh very cheap layers or some of the um U memory bonded
(66:20) layers right for some compute bonded layers you definitely want to have a lower precision and make sure they have they are less sensitive to the accuracy so this is a trade off between the latency versus the accuracy um in the design space right so this is linear uniform condensation versus using the hardware aware quantisation you can see the hardware aware quantisation given the same latency it can have a higher accuracy and given the same accuracy it has a lower latency and using that Technique we can Al optimize for
(66:57) different design Spa targets which we are going to cover in the neural architecture search section okay we want a model to have smaller size to be faster energy efficient right so using such automated approach Hardware awareness we can optimize not only for the model size but also latency and also for the energy all right in summary uh this lecture we reviewed linear quantisation and then we introduced post training Quan quantisation techniques we introduced different granularity of quantisation the per tensor Channel
(67:37) group quantisation in particular fp4 and also mx4 uh data types and also we talk about how to determine the dynamic range clipping the rounding techniques next we talk about this quantization aware training techniques to use fine tuning to recover accuracy in particular this straight through estimator um to use St technique find T quanti model and then we introduce binary andary neuron Nets just use one bit or two bit to represent the weight and activation in particular use pop count and aor operation to deal with the
(68:18) um Quant binary quantise Network and finally we talk about automatic mix precision quantization all right so that's the end of this lecture in the next lecture we'll talk about neuro architecture search and lab two is released so we'll come to try it out thank you
