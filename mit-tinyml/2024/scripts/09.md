All right, good afternoon everyone. Let's get started. Today we are going to talk about efficient ML, Lecture 9: Knowledge Distillation. This is a new chapter after we have talked about pruning, condensation, and neural architecture search. Now it's another technique: knowledge distillation. Make sure you have finished Homework 1 and are already working on Lab 2, just as a reminder. Today we'll learn about knowledge distillation, which is an essential technique compared with pruning and quantization. We'll talk about what knowledge distillation is, what a teacher model is, what a student model is, and also how to match the logits, weights, activations, or gradients—whatever is needed to match teacher and student. We will also talk about self and online distillation, distillation for different tasks, and finally, another technique: network augmentation.

Let's get started. What's the motivation here? Again, we have limited hardware resources across different platforms. In the cloud, GPUs are super expensive, and the memory capacity is also limited—80GB, 100GB, 120GB, etc. Not to mention mobile devices or microcontrollers, which have a couple of hundred kilobytes of memory versus tons of gigabytes of memory. Compared with neural networks on GPUs, we can run ResNet, we can run ViT, TinyML, or MobileNet. The question is, how do we design models that can fit very little memory budgets or even match the accuracy of those larger models? That's for TinyML. Even in the cloud use case, large language models are getting super big, and we want to reduce the serving cost. Therefore, even on the cloud, knowledge distillation makes a lot of sense. We want to distill a 70-billion-parameter model into a 7-billion-parameter model, for example, so we can save 10x the memory traffic for the weights.

That's the question we want to answer: Can we use a larger model to guide a smaller model? We have a larger model, a teacher model on the left, and a smaller model, a student model on the right. Even training these models independently, we can have very different accuracy, like 82% versus only 52%. The question is, can we help the training of tiny models with the help of these larger models?

This image is the illustration of knowledge distillation. First of all, we share the same input. The same input is fed to both a larger teacher network and a student. The teacher network is usually much larger, like Llama 2 70B. The student model is much smaller, like Llama 2 7B. Given the input, they both produce some prediction logits: logit 1 and logit 2. When we are training the student network, we have some loss, either classification loss—maybe this is the most popular. What is new here is that we also add a loss called distillation loss. Distillation loss is between the teacher's logits and the student's logits. What we are trying to do here is to match the logits of the teacher and the student, trying to match the teacher with the student. That's a new loss term we added here.

What's the intuition behind adding this loss? If we look deeper into the prediction given the same input image, say a cat here, the teacher network has two logits: one for the class "cat" and one for the class "dog." Logit for "cat" is 5, and for "dog" is 1, meaning that it strongly believes this is a cat. If you normalize them after softmax, the probability is 98% versus 1.7%. The teacher model strongly believes this is a cat. The student network, however, has logits of 3 and 2. If you apply softmax, the probability is 73% versus 26%. The classification is still correct, so you don't have any loss coming from the cross-entropy loss because the prediction is correct—it's a cat. But you can tell their output from the teacher versus the student is quite different. The teacher is more certain this is a cat, while the student model is less certain, with only 73% probability. Here, we are trying to match the prediction probabilities between the teacher and the student. That's the missing part from the cross-entropy loss.

How did we derive this probability? That is through the softmax function. For example, exponent(5) divided by (exponent(5) + exponent(1)) gives the probability for the teacher. Similarly, we calculate for the student. Our goal is to bump up the probability for the cat and bump down the probability for the dog to match that of the teacher.

How do we do that? First, learn this concept called temperature. In the softmax function, if the temperature is 1, that's our original case. If the temperature is larger, the output distribution gets smoother. For example, instead of 98% and 1.7%, it becomes 60% and 40%. A larger temperature smooths the output probability distribution. Mathematically, the temperature appears in the denominator of the softmax function. For example, if we apply a temperature of 10, previously exponent(5) becomes exponent(5/10), and similarly for the denominator.

This is the formal definition of knowledge distillation. Neural networks typically use the softmax function to generate logits to class probabilities. With a different temperature, the probability is calculated as exponent(z_i / T) divided by the sum of exponent(z_j / T) for all classes. The goal of knowledge distillation is to align the class probability distribution between the teacher and the student. We are trying to align this probability distribution.

What can we align? What tensors can we match? Here we summarize a bunch of ideas. Let's go through them one by one, starting from the output logits. We have seen this figure: not only do we have the classification loss, but we also try to match the logits between the teacher model and the student model. To match output logits, we can either use the cross-entropy loss or simple L2 loss, which is just the difference between the teacher's and student's logits.

What else can we match? Not only the output, but we can also try to match the intermediate tensors, including the intermediate weights and features. Let's look at them one by one. Here, we no longer match the output; now we are matching the intermediate tensors. For example, the teacher model's Layer 2 and the student model's Layer 2. Here, we try to match the intermediate weights of these two layers. What is the challenge here? The challenge is that the teacher model and the student model have different dimensions. The teacher model is much larger, with larger weight tensors, while the student model is smaller, with smaller weight tensors. What we can do here is use a low-rank approximation or a learned projection, like a 1x1 convolution, to map the student's smaller tensor to the teacher's larger tensor. This is the teacher network, which is larger, while this is the student model, which is narrower and smaller. We try to match them by using this projection matrix, which maps from a smaller channel to a larger channel. We can learn this projection matrix and then match the output after this projection matrix with the teacher model.

We can also match the intermediate features, not just the weights but also the activations. Previously, we were matching the weights; now we are matching the activations. The intuition is that the teacher and student networks should have similar feature distributions, not just at the very end (the output probability distribution) but also across all the layers. Different layers' feature maps should match.

What is last? Gradient matching. We can also try to match the gradient from the teacher to the student. Let's brainstorm a little bit. They are of different dimensions, so matching the teacher and student gradients directly may not work. Beyond the weight gradient, there's also the activation gradient during backpropagation. The backpropagation part is slower than the forward part because it involves double the computation: one for computing the weight gradient and the other for computing the activation gradient. If weight gradient matching is hard, activation gradient matching, particularly for the very first layer (the input), is even harder. Since the teacher and student share the same input, this is the intermediate attention map. During backpropagation, we propagate the gradient all the way to the input image. This is the gradient on the input tensor. The attention map is defined as the partial derivative of the loss with respect to the input. If the gradient is large, like in the nose, eyes, or face of this box, it means that a small perturbation in this area will significantly impact the final output. This area is very important for making a decision. At position (i, j), the attention is defined as the gradient with respect to that pixel. We are trying to match this attention map from the teacher to the student. They can be easily matched because the dimensions are the same. The gradient dimension is the same as the image, and both the teacher and student share the same image.

Let's see, given different networks, what the gradient looks like. The second row is ResNet34, and the third row is ResNet101. They are of the same family, while the first row is MobileNet, a different family. Networks of the same family, like the third and second rows, have very similar gradients, while the first row differs a lot. Attention maps of high-performance models (above 70%) are indeed similar, but less performant models (like 62%) are quite different. Therefore, when we are doing distillation, we try to match those intermediate feature maps, trying to match the gradient with respect to the activation.

Any questions so far? We talked about matching the weights, features, gradients, and outputs. We can also match the sparsity pattern. How do we match the sparsity pattern? First of all, where does sparsity come from? If we have a ReLU activation function, there is sparsity. We want to match the sparsity pattern after the ReLU activation function. A neuron is activated if it is larger than zero; otherwise, it becomes zero. At every feature map, we try to match the teacher's sparsity pattern with the student's sparsity pattern.

We can also match the relational information. What does that mean? Previously, we have been looking at each tensor: the output tensor, the gradient tensor, the weight tensor. Now we want to look at two tensors, not just one. For example, this is a teacher and a student. The teacher has 32 layers; the student has 14 layers. They both have three stages. The teacher has more residual modules per stage (five in the first stage), while the student has only two. The teacher has five in the second stage, while the student has only two. Each has the same resolution, and the different stages may be downsampled by two compared to the previous stage. What we're doing here is calculating the relation between the input and output, which is only dependent on the number of channels. For example, we calculate the relation between the input and output for the teacher and the same for the student. We are trying to match the relationship between the teacher's input and output and the student's input and output.

We can also match the relationship not only for different tensors but also for different inputs and samples. Here we have three samples: bird one, bird two, and bird three. Previously, we just tried to match the distance between the teacher's bird one and the student's bird one, the teacher's bird two and the student's bird two, and so on. They are independent; these three birds are independent. Now, we try to match them jointly: T1, T2, T3 from the teacher versus S1, S2, S3 from the student. They should have a very similar relationship with each other. For example, if T2 is to the right of T1, then S2 should be to the right of S1. In contrast, this loss doesn't care whether S2 is here or within a 360-degree circle; it doesn't matter. But here, it matters. The relationship matters. So, conventional KD focuses on matching the features or the logics on just one input. Relational KD looks at the relation between intermediate features across multiple inputs. This has been tried on image models. For your homework or open-ended projects, feel free to try this with large language models to see if this relational knowledge—this relation across multiple inputs—makes sense when compressing a large language model or visual model. Maybe this method can shed some light. So, putting this into math, we have multiple samples, and we calculate the output distance between the student's first sample and second, first sample and third, first sample and Nth sample, etc. So, altogether, how many terms are there? We have n \* (n-1) / 2 terms, since it's pairwise, representing this pairwise distance of feature vectors. On the bottom, we compare this with individual knowledge distillation. Now, we just care about the distance between T1 and S1 (T for Teacher, S for Student), T2 and S2, TN and SN. Right now, we not only care about them independently, but we care about the relationship among them. So, we have to calculate, given a tensor, we want to go back and visit the distance with all the previous input samples. So, that's the difference between the relation within the model calculated separately for each input versus calculating the relation across different input samples. So, that's all for the second part: what to match—the gradient, the weight activation, the logics across different input samples. For that part, we have one teacher and one student; we require two models. What's the problem with that, especially when the scale is larger? You have to first train a very big, giant model. You have to first have a teacher before you can have a student. But training this teacher can be very expensive, like training a Llama 70 billion parameter model is pretty costly, right? Then, if we don't have the teacher, how do we apply knowledge distillation to begin with? So, that's what we try to answer in the third part of this lecture: self and online distillation. Just like self-learning, you don't have a teacher; you just learn by yourself or together with your classmates. And for previous distillation, we have to train it once. First, finish the whole training process, get a high-performance teacher model, and then start the distillation process for the student model. But now we just want to do it only once—just do it only once—online distillation, where you distill while you learn.

So, that's the opening motivation for the third part: self and online distillation. Let's see how that works, starting with self-distillation. The teacher model, like we mentioned, is usually larger than the student model, and that is fixed. The disadvantage of a fixed, large teacher model is that it is very expensive to train. Does it have to be the case that we have a fixed, large model in KD? Actually, not necessarily. It can be of the same size; it doesn't have to be a fixed, large size. This project was called Born Again Network, and it uses an iterative training stage with both the classification objective and the distillation objective. What is interesting here is that the teacher and student model sizes and architectures are exactly the same. Same architecture, same model size—not a bigger teacher and a smaller student; they are the same size. What it does here is, in Step Zero, use only the classification cross-entropy loss between the teacher and the ground truth. In the second step, use the KL divergence loss to train the student and also match the logits from the teacher in the previous step with the student in the next step, and so on. Each time, we match and train with the cross-entropy loss and the distillation loss with the previous step. We use the previous step as the teacher to guide the next step. As a result, the students (S1, S2, S3, etc.) get better and better as the training finishes in the last step. Since these architectures are the same, we can ensemble predictions together to get the final output.

We can also do online distillation. Online distillation means we have two networks. They can be the same size; the teacher doesn't have to be larger than the student. They can be the same, and they are trained from scratch. Rather than first training the teacher model and then doing the distillation, now we don't have a teacher or a student. Everybody is just starting from scratch, just like in a classroom where only you and your neighbor classmate sit together. Network 1 is the teacher of Network 2 when we are training Network 2, and Network 2 is the teacher of Network 1 when we are training Network 1. They act as teachers for each other, which is the interesting part. Just like you have two models of the exact same size, what you do here is add the loss—not only the cross-entropy loss. For the student, you have the cross-entropy loss with the ground truth label, and then you have the KL divergence, the KD loss, treating the second model as the teacher and the first model as the student. When you're training the teacher, you not only have the cross-entropy loss but also use the student model as the teacher and the teacher model as the student, learning from each other. The interesting part is that it's not necessary to pre-train this teacher and student. The teacher can be initialized and trained from scratch together with the student, and the student can be the same size as the teacher. You don't necessarily need to have a larger teacher, which is the interesting part here. When we are training P1, P2 is the teacher. When we are training P2, P1 is the teacher. People call it deep mutual learning. We can see here on CIFAR-10 and CIFAR-100 datasets that deep mutual learning can improve both the student and teacher models. The DML in the middle row shows the performance after this online distillation compared with independent training.

We can also have a combined approach, combining online distillation and self-distillation. Here, the title is "Be Your Own Teacher." We don't have a separate teacher; we don't have to pre-train a teacher model to guide the student model. We can train from scratch using deep supervision plus distillation. We use the deeper layers' output as the teacher to distill the information for shallower layers. The teacher and student no longer come from two different models (one large and one small); they come from the same model at different places. The deeper layers' output will be the teacher for the shallower layers. The beauty of this project is that you don't have to maintain two copies of the weights. You just have one copy of the weights. Otherwise, you would have to double the GPU memory, which is expensive. Now you only have one copy of the weights, and the teacher is very simple—it's just a deeper layer supervising the shallower layers. The intuition is that the labels at a later stage are more reliable, so the authors use them to supervise the predictions for previous stages. No teacher—you are your own teacher, just using the deeper layer to supervise the shallower layer.

Now let's see how that works. The first row is a baseline. If we use the predictions from intermediate classifiers, sometimes they actually outperform the baseline, which is quite interesting. This inference efficiency can be improved because we don't have to wait until we go to the last stage. Maybe we can exit early when the confidence is high enough.

That's all for the first three parts. We'll take a break before we talk about distillation for different tasks and network augmentation.

All right, welcome back. Let's continue to talk about knowledge distillation. Now we want to apply knowledge distillation to different tasks to solve real-world problems. Let's start with object detection. What is object detection? You try to find the bounding box for different objects. You not only want to give the class (what it is) but also tell me where the object is, giving the bounding box for the location. We can see the distillation pipeline is more complicated here, but it still includes the basic components, including feature matching, trying to match the feature map. Here we can use a 1x1 convolution to match the shape, just like we discussed before. The teacher model and the student model may have different dimensions, so we use this 1x1 convolution to match the dimensions so that we can subtract tensors of the same sizes. What we do here is use a weighted cross-entropy loss to differentiate the background versus the foreground. The cross-entropy loss we previously had was just the teacher's probability times the log of the student's probability. What is new here is that we added a class condition to have different weights for the foreground and background. For the background, we can allow some difference, but for the foreground, we really want it to be accurate. That's why we have a weight here to differentiate the foreground versus background classes.

The next part is predicting the bounding box. It's not just the class but also the location (x1, y1, x2, y2) given those corners of a bounding box. What's new here is an "if" branch. The ground truth is Y, and the prediction from the student is RS. We try to minimize the difference between the student and the ground truth. If the student's error is larger than the teacher's error, we apply this loss. Otherwise, if the teacher is not as good as the student, it doesn't make sense to be a teacher. At that time, we assume the distillation loss to be zero. This is very interesting. We add a margin term, which is a buffer, to make sure the student's error is larger than the teacher's error plus a threshold. Then it can act as a teacher. Otherwise, we just put the distillation loss to be zero, making sure the quality of the student is within a threshold of the teacher.

There's another way to predict the bounding box. It used to be a regression problem because the x and y locations for the object are continuous. We can also convert it into a classification problem. Let's brainstorm: how do we convert that into a classification problem? We can chunk the y-axis and x-axis into several bins. Here we have six bins on the y-dimension (1, 2, 3, 4, 5, 6). We can also chunk the x-axis into six bins. Now we have 36 blocks. If the predicted x and y locations fall into the correct bin, then that's a correct prediction. That's converting a continuous value into a discrete value. Once it's discrete, we can use the cross-entropy loss to classify it into a classification problem. For example, here we have 36 classes. For x, we have six classes (1, 2, 3, 4, 5, 6). The ground truth should be the second class, so we can use the same cross-entropy loss to supervise this detection problem for discrete locations.

Now let's talk about semantic segmentation. Segmentation is a new task where we try to give a label for each pixel—pixel-wise prediction. Again, we can have the same feature imitation, similar to classification and detection. What is new here is that we can add a discriminator network to provide adversarial loss. The student is trained to fool the discriminator network. We have a real embedding from the teacher model and a fake embedding from the student. Here we try to fool the discriminator so it cannot distinguish between the real embedding and the fake embedding. We also try to encourage the student to grow and be as accurate as the teacher, such that the discriminator cannot tell this is fake or from the student. If the discriminator thinks these two are as good as each other and cannot distinguish them, then the output given by the student should be very close to the teacher. That's the adversarial loss as distillation.

We can also use these distillation losses to compress GAN models. We used knowledge distillation for GAN models in this work we did a couple of years ago. It's called GAN compression. We have two models here: a teacher model and a student model. First, we apply the distillation loss through intermediate feature maps. The teacher has a larger number of channels; the student has a smaller number of channels. Here we use a 1x1 convolution to match the channel dimensions. Next, we have this reconstruction loss, which measures the pixel-wise difference between the student's output and the ground truth. Finally, the third term is the GAN loss. We want to make sure the teacher and student are close enough so that the discriminator cannot tell the difference, thinking both are real. The training objective adds these three loss terms together to supervise the student model so it can perform as well as the large teacher model.

Here we have a demo. After compression using GAN compression, on the top is the original CycleGAN, which turns a horse into a zebra. Unfortunately, it runs very slowly. This is deployed on a Jetson Xavier GPU, a very small mobile GPU. You can see it's very slow. The FLOPs are 56 GFLOPs, running at only 12 frames per second. FID is 61.5. After GAN compression, the FLOPs are reduced to only 3.5 GFLOPs—that's 16 times lower FLOPs. The frames per second increased to 40, and FID also improved to 53. The original was 61.

Here is another demo: interactive edges to shoes. We just draw these edges and then want to convert them into shoes. Now we want to erase the logo, trying to change from Adidas to Nike, and see how that works. Originally, it was pretty slow. This is running on an even smaller Jetson Nano. Previously, it was running at only 1.6 frames per second. You can see the drawing is very difficult because it's not interactive—it's too slow. On this edge device, since the frame rate is so low, it can only barely sample a few frames, barely looking like a Nike shoe. After applying compression and running the smaller model, the erasing becomes much smoother. On the right-hand side is the GAN compression; on the left-hand side is the baseline. Now we try to draw another Nike shoe. Now we can run at 3.9 frames per second. It's much smoother. Same hardware, the speedup is about 2.5x, which is quite good. Everything is deployed locally on this Jetson Nano GPU. We have a bunch of them. If you are interested in this project, feel free to talk to us. We can lend you a few such mobile GPUs.

We talked about vision tasks: distillation for image detection, image segmentation, and image generation. Now let's talk about KD (knowledge distillation) for natural language. In natural language, attention is a very important building block. We have a projection for QKV attention, the output projection, feed-forward layers, normalization, etc. What we can try to match is not only the feature map but also the attention map, which is O(n^2). This is showing matching the attention map. What does it look like? The first row is the teacher. The attention map is always square because it's the number of tokens times the number of tokens. The second row is the student with attention transfer, and the third row is the student without attention transfer. With attention transfer, the attention map is very similar; without it, it's very different. These two are very similar, while this is quite different. In addition to feature imitation, the transformer architecture gives us another opportunity to match the attention maps.

Knowledge distillation for large language models and visual language models can work together to obtain much smaller large language models. Here is the cost of training a model, measured in training tokens. For example, Llama 2 70B uses 2.5 trillion tokens. Here we can reduce the number of training tokens by a large margin while maintaining accuracy compared with training from scratch. The Neuron 3B model is 40 times cheaper and 99% better in accuracy on the LAMBADA score. Here is how the whole process works. On the left-hand side, it contains a pretraining data mix versus a synthetic mix to train both a pretrained model and an instruction-tuned model. This is instruction tuning. The synthetic data comes from passing prompts through Llama 2 70B to get synthetic data. You concatenate that with some fine-tuned data to train this instruction model. That's the overall pipeline.

How do we select the student model? We select the student model using the pruning technique we talked about previously, using the magnitude to rank those important channels. This is the work where Minions apply knowledge distillation after pruning the model. The student is a pruned version of the teacher model. Initially, we have a pretrained large language model. Then Minions estimate the importance of embeddings, windows, etc. They rank them according to importance and then throw away the less important channels, tokens, etc. Then they distill, using this as the student and the original as the teacher. They match a lot of different tensors: logits, embeddings, etc. The distillation not only matches the output logits but also tries to match those intermediate features.

We talked about distillation for different tasks, for both vision tasks and NLP tasks. Finally, let's talk about network augmentation, another piece of work very related to this topic. We try to improve the accuracy and quality of a small model. That's the central topic of this lecture: how do we improve the accuracy of a small model? Data augmentation is widely used in training to avoid overfitting. For example, if we have a limited amount of data, we want to augment it so it seems like we have a larger amount of data. Typical ways of doing data augmentation for vision tasks include cutout (cutting part of the image), mixup (mixing two images pixel-wise), and AutoAugment (automatically finding the best augmentation policy). Apart from data augmentation, another way to prevent overfitting is using dropout. During training, we randomly select a few neurons and zero them out. People can also do spatial dropout to randomly remove a few channels or drop blocks to remove a few blocks in a coarse granularity. As a result, these techniques like dropout and data augmentation can greatly improve the performance of large neural networks.

Here is the performance of ResNet-50 (4 GFLOPs) on ImageNet. The top-1 accuracy is about 76.5%. After mixup, it's almost 78%. After AutoAugment, it's more than 77%. Using drop block, it's more than 78%. For these large models, dropout and data augmentation really help because large models tend to overfit. Dropout and data augmentation prevent overfitting. However, for tiny models, we did the same experiment. Unfortunately, both dropout and data augmentation actually hurt the accuracy. The more techniques we add, the worse the accuracy becomes. Adding mixup hurts, adding AutoAugment hurts, and adding drop block hurts. Previously, when it helped the most, now it hurts the most. We suspect the tiny model lacks the capacity and doesn't suffer from overfitting but rather from underfitting. It doesn't have enough capacity to learn so much knowledge. For the tiny network, contrary to image augmentation, we think about network augmentation. Can we augment the model to get some extra supervision during training for the tiny model? In the end, we still want to deploy a small, tiny model, like here with only two channels rather than four channels. But during the learning process, we find some redundancy helps. If you can sometimes have four channels, sometimes three channels—not just these two channels—it is helpful. For example, once you get stuck in a local minimum, some extra dimensions or degrees of freedom can help you get away from the local minimum. Indeed, network augmentation for the tiny model (MobileNetV2 Tiny with only 23 MFLOPs) improved the accuracy significantly.

How does that work? This is the visualization. We have a very small model with only two neurons in this case (three layers, two neurons). Then we build an augmented model out of this original model, very similar to the width multiplier for networks. Here we start with a small model and then expand it in different dimensions. We can use the channel dimension, the depth dimension, etc., to build an augmented model. The same principle can apply to large models by increasing the number of layers or channels. For the augmented model, we compare the base model with the augmented weights. They actually share the same weights for the red part here versus the light blue part. Then we get the gradient both from the base model and from the augmented model. We add them up together. This is the base supervision and the auxiliary supervision. We add some extra supervision to the base model. At different steps, we can sample different sizes of the network. Sometimes we sample a larger model; sometimes we sample a smaller model. What doesn't change is the inner part—they all share the same weights with the original small model. As a result, we can see the learning curve for both training and validation accuracy improves. For tiny models, it improves both training and validation accuracy. For larger models, it actually didn't help but hurt the accuracy. For training, it helped, but for validation, it didn't help, meaning that the large model already has a pretty big capacity and doesn't suffer from underfitting but overfitting. What they should do is augment the data, not the model. This echoes our previous assumption that large models are always overfitting, while small models are underfitting.

Here we are showing network augmentation is actually complementary to knowledge distillation. The left bar is the baseline, the middle bar is knowledge distillation, the right bar is network augmentation, and the far-right bar is network augmentation plus knowledge distillation working together. For transfer learning, we compare two scenarios: one is knowledge distillation, and the other is just training for 4x longer. Sometimes the accuracy hurts using these techniques, but using network augmentation provides better transfer learning performance than knowledge distillation and also better than a 4x training schedule.

We also tried to apply network augmentation to object detection, not only classification but also other tasks. This is the baseline. The x-axis is the number of FLOPs (how much compute you have), and the y-axis is the mAP accuracy. Here is the focal accuracy. For the same model, we can improve accuracy, or we can reduce the FLOPs if you want to achieve the same amount of accuracy. The limitation here is that it only explored vision tasks. This work was done early in 2022, before the transformer paradigm came. It might be very interesting to try such ideas—network augmentation—to train those small large language models. Welcome to talk to me if you're interested in that for the final project.

All right, let's summarize today's lecture. We shifted to a new chapter about small models. Apart from pruning and quantization, now we are talking about knowledge distillation. It needs a teacher and a student. We can match several components: the final output, the intermediate feature map, the intermediate weight, and the intermediate gradient. Not necessarily do we need a teacher model; we can also do self-distillation and online distillation so that we can train only once and distill during this training process. Such knowledge distillation can work for many different tasks, not just classification. We also talked about how to apply that to detection, segmentation, GANs, and natural language processing. Finally, we talked about network augmentation, an essential technique to improve accuracy by augmenting the model.

In the next lecture, we'll introduce TinyMLNet, which is algorithm-system co-design for TinyML so that we can deploy them on microcontrollers. All right, that's for today's lecture. Thank you.
