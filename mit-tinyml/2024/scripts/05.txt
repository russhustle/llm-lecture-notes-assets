(19) EfficientML.ai Lecture 5 - Quantization Part I (MIT 6.5940, Fall 2024, Zoom recording) - YouTube
https://www.youtube.com/watch?v=RP23-dRVDWM&list=PL80kAHvQbh-qGtNc54A6KW4i4bkTPjiRF&index=9

Transcript:
(0:01) okay welcome Everybody Let's Get It Started With lecture five of efficient ml. so today we are going to jump into a new chapter about quation so last lecture we learned about flooding to reduce the number of parameters in the neural network if you think about the total size of a model a neural network model that equal to the number of parameters times the bit width the number of bits per parameter right so you multiply them together that give you the total size of model so if we want to reduce the size of model on
(0:35) one side we can reduce the number of parameters that is by using pruning and the other side it will be trying to reduce the Precision number of bits for each parameter so that is quantization uh we are going to spend two lectures about quantization and here is our agenda we will first review the Nu numeric type data types including the integers the floating points in eight IP 32 16 and ip4 in four ip4 we are also going to learn the concepts of neuron Network pruning or quantization followed by three types of
(1:12) quantization approach so we are going to talk about K means based quantization and linear quantization today in the next lecture we are going to cover binary andary fantization so let's start with why do we need fantization so little bit operators are actually much cheaper so if you compare the energy of the operation from 8 bit to 32bit actually the 8bit integer operation is much cheaper compared with the U 32bit or uh 32-bit in integer operation or the 32bit roting point operation uh that's from the operation
(1:57) perspective from the memory perspective every data movement is actually U movement is also quite expensive here so each memory foot access equal to hundreds of arithmetic operations so if you can reduce the size of model we can reduce the uh memory traffic as well so less number of bit withd will result in less energy consumption so let's order to understand how do we quantize neural networks let's first talk about the data types how is the numeric data represented in modern Computing system so let's start with integer okay
(2:43) so integer is most common representation ises representation we have un signed and also signed integer okay so in the I signed version um each digit repr represent two The Power Of Z 2 the Power of one all the way to two power of seven if we have eight bits right right here so this number actually represent 49 but these are I sign if you want to add a sign bit we have one in the top so that the remaining bits ranges from to the power of zero other way power of six and here we can find actually have overlap both all the zeros and one
(3:20) z0 Z they both represent zero so in order to REM remove that duplication the better way to represent a sign integer is by using this Tool's complement representation so what that means is the first bit actually means two minus power of s okay and the remaining bits are positive from two the^ Z power one to the power of six only first bit is to the power of seven in this case z00 z equal indicat zero but one z no longer indicated Zero by two to the power of minus one with that we can talk about fixed Point number so here we have a decimal
(4:12) point so this is decimal point above that we have the integer bit below that we have fraction bit so from the smallest decimal bit represent two to the power of zero and then two to power one larger and since this is two complement represent PA the first bit is always negative and after the decimal bit it represent 2 to the power of minus1 -2 all the way to minus four in this case another way to uh tell which number it represent is still using everything starting from zero to power of zero all the way to toal power of six
(4:53) but to shifted by the number of um fractional bits which is four okay so you shift everything by four and we can see the represented number is still same using both interpretations so that's the F Point number let's get more complicated to represent a larger range dynamic range people imagine this floating Point number and here we are showing example of 32 bit floting Point number in actually 3 754 okay so among the 32 bits we have one sign bit in the front and then eight bits of exponent and then 23 bits fraction it's also called significant
(5:38) bit or the mantisa bit the relationship between these bits is that the definition of this number is minus one to the power S bits okay this is the S bit and then one plus fraction bit this is the fraction yellow P times 2 to the power of exponent minus the bias here is 127 and how is that calculated we have eight bits of exponent bit and the bias is calculated by 2 to the power of exponent bit minus one so for eight bits we have 2 to^ of 7 minus one which is 127 which a constant number as long as we are using 32bit floating
(6:25) Point new peration so fraction bit correspond to the yellow part exponent bit corresponding to the blue part given such definition we can easily calculate how to represent this number 0.265 625 the representation would be uh first converted into one plus fraction okay one plus fraction so the here is uh 0.
(7:00) 02 uh 625 so one point something time 2 the^ of minus 2 and here since the bias is 127 exponent bit should be 125 so the exponent bit should be 125 using these eight bits in Rule and then the 0.0 625 is represented by one the fourth position okay so first converted converted into one plus something to do the power of something okay any questions about this example so far good all right so um let's see how do we represent zero since we have to if have to be forced use one plus something to represent a number then how do we represent zero actually um people Define this
(7:55) subnormal number and previously we were talking about this representation okay one plus fraction to the power exponent minus the we call it normal numbers exponent doesn't equal to zero here we design a special case where the exponent bits the blue part has be frozen to zero okay in that area for particular that area where exponent bits are zero we Define it as subnormal numbers and the equation is a little different rather than one plus fraction here we just use fraction okay and rather than to the power of exponent minus
(8:35) 127 um here we have 2 the Power of 1 minus 127 okay so the fraction bit is only variable here we no longer have the exponent bit because here the exponent bit are forc Frozen to be all zeros so now if the fraction bit is all zero and no matter the sign b z or one the final result would be zero so that's how we represent um zeros and how do we Define those subnormal numbers so let's see you what is the smallest and larg largest positive subnormal value using the subnormal representation on the right hand side so first of all if it is a normal
(9:28) number right the exponent bit canot be zero okay so in this example uh the exponent bit is one and then the fraction bit is zero okay so the number will be 1 + 0 okay Time 2^ - 127 and the final result is two to power of minus 126 so that's the um normal number for subnormal the exponent bit is frozen to be zero okay only the um fraction Bas is one here okay and this is 2 to the power of - 23 so this is 2 to the power of -23 time 2 the Power of minus 126 this is a constant because this is by definition 1us 127 okay so this is the smallest number
(10:29) we can represent in the soft normal region so this is constant also constant so only the fraction bit right here so actually you can see this is very similar to the um this is a linear representation okay so right here is not linear but here is linear what about the largest positive subnormal value okay so on the right hand side we just fill all the fraction bits to be y in this case 2 power of - 23 plus the Power of minus1 so Su it sum up we get 1 minus 2 power of 23 okay and then here we have the bias times 2 to the power of minus 126 that
(11:17) is the largest number we can represent in the SOP normal region um there are other two cases where if all the exponent bit are one okay uh we if the S bit is zero we use that to represent positive Infinity if the S bit is one we use that to represent negative Infinity okay so that is the case uh for representing infinity all the um fraction bit has to be zero all the sign of exponent bit has to be one what about the sign bit is one but the remaining base is non zero we use that to represent not a number right so
(12:05) actually there is a big waste because um no matter what the values are in fraction Bas they all represent a not value not a number so actually we are going to revisit this the fp8 representation so these are the two extreme cases for the mostly coming from the exponent bit if they're all zero then that's subop normal number if they're one there is either Infinity or not a number depending on the fractional bit so we have talk a lot let's put summary here to summarize what we have learned so far for the floating fun
(12:49) representation first most importantly exponent bit the BL part right if it is zero or if it is non zero all one okay if it is non zero or not the largest number then that's the normal value okay so the representation will be- one to the power of s bit 1 plus fraction times 2 to the power of exponent minus the bias that's simple case and then we have two Corner cases if the exponent is all zero um then we will look at the fractional B if it there zero then represent if they're not zero then they are subnormal numbers subnormal
(13:32) representation differ by one plus versus just fraction right here okay and here the constant is two to power of 1 minus 127 okay so that's the exponent equal to zero third case is exponent all one okay and that either represent positive or negative infinity or not a number depending on the fractional bit and if you see the number representation um from small number to the largest number we divided into two rges the subnormal values versus normal values in the subnormal value distance between each Sid is constant
(14:15) it's linear contact versus in normal value the size become larger and larger so it's good at expressing very large numbers so people have been keeping uh improving the efficiency by pushing Precision to lower and Precision so um from FP 32 to fp6 b FL 16 so the exponent width actually represent the range how much you can represent so that is using mostly using the exponent and fraction width it indicates the Precision okay indicates the Precision so for AP 32 we have eight bits the exponent and 23 bits for the fractional
(15:08) bits and Al together we have 31 plus the first sign bit Al together we have 32 bit well for the actually half Precision which is using 16 bit to represent we have a less number of exponents only five bits for exponent and 10 the fraction and later uh Google invented this new uh type which is B F6 bring float 16 and that is trying to match the exponent bits number of exponent with fp32 they're using both eight bits for the exponent and we sacrifice Precision they sacrifice the Precision by using fewer number of
(15:56) our fraction bits so we have eight for exponent seven bits for the fraction plus one bit on the top Al together we have 16 bits so notice the exponent bit for p32 and bf16 are the same therefore usually when we are training a large scale neural networks if fp16 sometimes will diverge then the same good solution us is to use pf16 okay so the question here what is following um fp16 number indexing so let's do a practice we were given um representation right here we have um five bits of exponent 10 bit fraction that's the standard Triple E p16
(16:58) precision so for IP 16 what is bias five b 5 - one is four power 4 minus one that's 15 okay so Pi should be 15 so we can use the equation first of all the sign bit is minus right exponent would be 1 Z1 minus the bias right here which is 17 - 5 15 which is 2 and the next will be calculating the fraction bits refraction bit is one followed by a bunch of zeros in binary representation that is 0.75 so this is 0.5 0.
(17:41) 25 adding them together we have 0.2 75 and we use the equation s bit 1 plus the fraction bit times to power of exponent bit we plug in 0.75 two and Al together we have - 7 in this case so this 14 point number I should represent minus 7 so let's do a reverse example we have a decimal number which is two and half how to represented in brain float 16 I'll give you one minute to think about it first of all it's bring float 16 right so it's one bit of sign 8 of exponent and seven bit of fraction the first thing would be to calculate the bias right we have 8 bit
(18:48) of exponent and the bias equal to 2 the power of exponent minus oneus one to^ 7us one that's 127 okay so we have the um exponent bias and remember we have to represent the number by 1 point something to the power of something so one 1.25 actually times 2 the^ 1 that is 2.5 and then we should represent 0.25 and also what first of all the sign bit should be positive and then the exponent binary right here is one plus 127 because the final result should be one and then the bias is 127 therefore exponent binary should be 1 + 127 which
(19:40) is 128 how do we represent 128 this is the representation next the fractional bits okay the fraction bits should be 0.25 which is here 0.5 and 0.25 the binary repres is 0 1 of zeros so we can fill in 0 1 Z to the fraction bits F one0 z0 to the exponent bits and finally we have the uh representation in BF 16 for the number 2 and half any questions here okay good so people want to continue push the front here by going through to lower and lower Precision so later media has this fp8 using 8bit floating point to represent a number starting from the
(20:44) hoer h100 GPU so was super popular in in the city these days h100 h200 purely out of stock and people are even using these gpus uh it's flat RS to get financing um and recently even ip4 is coming to the market black well b00 in high demand so what exactly is happening here um so ip8 can further reduce the Precision and in increase S since we have so few number of bits we have to be extremely careful how allocate uh these number of bits so first of all we need a sign bit so together eight8 bit one is that designated for sign bit so we are left
(21:35) with only seven bits for the we have so many choices we can do 0 + 7 1+ 6 2 + 5 3+ 4 4+ three Etc so actually these are two popular used um numerical precisions for fp8 for exponent re Mana E4 and3 or E5 and2 five bits for the exponent and two bits for the mantisa so what is the pros and cons for E4 and3 versus E5 and2 remember the exponent bit is for expressing um larger magnitude okay larger dynamic range versus the mantisa bit is for precision so we have five bits for exponent that's a larger Dynamic Fring a lower precision versus
(22:41) E43 we have a smaller Dynamic Fring and a better Precision in this for reason8 is really used for representing the gradient in the backward path okay so uh one year later imia has this black well okay so black introduced fp4 four bit floating point is doubling or even making the FL higher okay so with only four bits we have how many choices do we have can this following four choices for these three um three bits we can either have everything as the okay so no exponent bit that's basically in four in so we can represent from
(23:37) zero to to seven for the mantisa part plus the sign bit we represent from minus 8 all the way to plus 7 okay so that's 16 values to power four fromus 8 positive 7 and these spaces are equally distance so it's linear cont to allocate those four bit another way to do E1 and two okay we have one exponent bit plus two M bits and the number they can represent is actually very similar to in number so we can have subnormal region where the exponent is zero so we use this equation to the power bias zero so this is 0.5 versus the nor normal region one
(24:29) plus so this is 0.75 * 2 power of 1 - 0 is 3.5 so you can represent the numbers um from 0 0.5 - oneus 1.5 all the way to 3.5 okay so it's also linearly distanced so pretty much the same as in for except that they are scaled by 0.5 okay so you want two is basically the same as in four plus it's wasting one slot we have a positive zero and negative Z okay so E1 M2 is the same as in four except for that it's wasting one slot for zero it can represent rather than 16 numbers it can represent 15 numbers because zero is
(25:22) overlapped 1 + 2 we can also have one okay so e E2 and 1 two bit for the exponent and only one bit for the M we can imagine this number will have a larger dynamic range and the less Precision so we'll give two examples 01 versus 11 one okay so 001 that's in the subnormal region because the exponent bit are all zero so the equation will be like this the bias will be one fraction is also one so the number represented here is 0.
(25:58) 5 for the normal region uh exponent is three and Fus one the Mana is one so it's 1 + 2 and half this is one meaning it's 0.5 so this number is actually one okay in that case we can find the representation for all these values is actually from um zero half one and a or six and we go to um - 0 1 2- 3 12 and scale by 0 and five so indeed it has a larger dynamic range it can represent all the way to 12 rather than previous it can only represent all the way to seven so the extreme case will be using E3 and zero all the bits becomes now becomes the exponent
(26:54) bit so for the um these values we have every everything as exponents um so this is the very small exponent 1 - 7 this is a big exponent 7us 3 so we can see the representation as the largest dynamic range is no longer equal distance but the become larger and larger as the value get larger um the largest value we can represent here is 16 than S okay so that's the uh E3 and zero okay so far we talk about everything about quantization about the different numerical representations from in8 to fp32 fp16 P F16 fp8 now fp4 and in four
(27:45) any questions so far okay good there no question let's move to the next part what is quantization okay so quantization is a process of coning the input from a continuous and also large set of values to a discrete set of values for example given this signal on left hand side originally the value is continuous but now we want to convert it to a discrete set so we divide region by five lines okay one two three four five and we point highest the signal to closest um region for example I can only from Z this is 2 1 0- 0 okay the
(28:33) Quant to discret value given this example is the original image this is a qu in 16 colors you can see the other map becomes discrete and the difference between input value and this quantize is referred to as the quantization error and our goal is try to minimize this quantization era so this is the agenda for lecture so initially we start with something that is a floating Point uh using floating Point arithmetic to do the computation and using floating point to represent the weights then we first going to introduce
(29:17) K means spased quantisation so we have a code book code book they can be anything only store the index within that code book so the storage is including two parts the integer weights it's no longer floating point it's integer together with the floating Point C and the computation will be done using floating Point arithmetic in this case so what is good and bad the thing about it is that we are saving the compter saving the storage by using integer WIS and the limitation is that we are not saving the computation we're
(29:56) still requiring to use the floating point arithmetic since we have a decode of it so let's jump deeper into how do we deal with um this K means quantization this is a example Matrix 4x4 weight Matrix represented into using a 32 bit floting point so we don't need the numbers to be so accurate we have 2.09 2.12 1.
(30:29) 92 1 87 we don't care such high Precision can just approximate everything to two Okay so that's the design philosophy so what we do here is the K means clustering to assign a color to each valuable for example everything that isay close to two we assign a blue color like 2.09 2.12 1.92 1.
(30:57) 87 okay and similarly we do that that for other colors everything that's closer to that is very close minus one we color them in green color them in green like 0. 98 1.08 13 0.91 we all quti them to minus one by class ring so what we store here is the index into the CID okay so this is Central zero Central 1 2 3 so for this 2.
(31:30) 09 since it's close closer closes to to the blue centroid and that centroid has index of three so here we only need to store the index three on the top so previously we are storing a 32bit floating Point 2.09 now we only store a two bit integer index that is going to index into this code similarly we can do it for the rest of the weights let's do one more example we have 1.48 1.53 1.
(32:07) 49 they're all very close to 1.5 where is 1.5 that's the second centroid what is the index that two so instead of storing 1.48 we store the index to okay one level of interaction so how do we reconstruct the uh weight Matrix using the uh code book so on the right hand side it's showing the reconstructed weights okay so for example uh here we have index one and one we look into the table one actually indicates zero so we put in the corresponding location so we can now calculate the quation error which is subtracting between this original weight versus
(32:53) reconstructed weight and we can get this pensation error what about the storage so originally each bit each weight is represented using 32bit floating point and how many values are there we have 4x4 that's 16 values so Al together that's 52 bits or 64 btes since each bite equal to 8 Bits what about after so we have two portions one is index one is the centrals for the index how many bits do we have for each index four values so two bits are enough to represent the index how many indexes are there we have 16 index so Al together 12 2 * 16 we need
(33:52) only 32 bits how many bytes are there only four bytes it is a lot smaller compared with the original storage but don't forget we also need to store the code book so is Point High Precision right so each entry in the code book is represented using three bit four entries okay so times four that's 128 bits or 16 bites if we sum them up four bytes plus 16 bytes all together we need 20 to using this K means classer condensation which is about two 3.
(34:36) 2 times smaller compar with the original size so this is the uh this is just example case in the common scenario assuming we have n Bits quation and the number of parameters equal to M which is much larger than total power of n which is the case where um we have very big Matrix much larger than 16 the 4x4 but the code book is the same in that case originally each weight is represented using 32 bit M weights we have bits after that we have um in bits now nowadays we have in this case two bits time M so that's an M bits plus the code book each
(35:33) entry is using 32bit we have two to the power of n here is two to the power two which is four entries in the code book so that's two to the power of n plus um M plus 5 Bits And since m is much larger than 2 to the^ of n we can ignore the second term so the compression ratio would be 32 / n times smaller if Nal to two in this case Matrix is super large then the saving would be 32 ided by two that's 16 times if we have four bits which are going to implement that in the homework we have four bits we 16 entries in
(36:15) the code book then what's the compression ratio that's 32 ID four that's 8 bit compression small Okay so this is representation how do we find Q the model uh that is quantized using p quantization okay we will get a gradient Matrix so after back propagation we get a gradient Matrix so we are going to color them using the same color as the weight okay so blue green red blue green using the same color and then we do going to a group by operation to group the same color together 0 point so this these four blue values are grouped together okay and
(37:01) these red values are grouped together so they corresponding they correspond to the same centroid we are going to reduce them by summing them up okay and then modied by the Learning rate subtracted from the original centroid then we get the fine centroid so that's one iteration of SGD on a k quantized neural network so first do a group by operation and then do a reduction by summing them up multiply by the Learning rate and subtract it from the original C that will give you the up Cent how does that work with respect to
(37:46) the accuracy so this is showing the quantization only what is model size ratio after compression so we can go much smaller and this is accuracy that ration here about 10% 11% we have very low accuracy degradation and after that the accuracy is going to uh drop very quickly how does it compare with pruning only so this line is showing the prun pruning only result in purple and we can find that there're pretty much closer roughly around 10% the accy begins to drop but the good thing is that pruning and conation can be
(38:28) combined together and we can do both pring and then quantization that give you the highest comp ratio okay just a few percent the accuracy is still very well maintained which is a good thing so practically should we do pring first or should we do conation first what does the model size ratio mean for quation so if the original model size is 100 megabytes now is 10 then the ratio will be 10% if originally you using 32 bit and the model is 100 megabyte now you're using 8 Bits the model size will be 25 megabytes and the ratio will be 25%
(39:29) so there um actually that depends on the number the reduction can be done by let me see actually right here we have calculating the mean um let me see me double check this is just doing the S I believe yes this is just doing the S because they're all updating to the same Central a lot of people want to nudge the same Central a therefore um we sum them up somehow they can be partially canceled by multiplying with the learning rate what really matters is how many numbers will go to the same bucket more number of want to go into the same
(40:08) bucket the more you want to notch weight so therefore we are doing the S right here you're very welcome to try the um try to the mean average that's beauty of deep learning is not a fixed um science or H engineering so basically use the method that works best not necessarily this is best approach yeah that's just what I did in the paper many years ago so back to our question should we do pruning first or should we do pation first right that's a good point since you want to do quation protein first because you want to First eliminate a
(41:02) lot of redundant weights okay so you have fewer number of weights to quanti reducing your workload to quanti and potentially reducing the qu error so first get rid of those redundant weights before contest so this is the weight distribution um after fine tuning a prune model why there is a big bump here because everything close to zero are pruned using our magnitude based proving approach okay uh everything close to zero they are set to zero so these areas get flatter because after F tuning they become flatter okay so what does it look
(41:45) like after quantization the values becomes distrct rather than continuous dist distribution they become discret only a couple of CID this is one this is another one this is another one this C and the value becomes discr that's uh can repeat the question any situation where quantization would be more preferable than PR um depending on the pruning granularity um of course GRE the pruning any hardware can accelerate so it's best to com combine them both I don't see any scenario where you don't want to combine po green pruning with quation
(42:42) unless your Hardware doesn't have any low Precision unit you can only do pruning um but I don't see a reason where you cannot do pruning but only compensation at least you can do course R structured appr so the remaining weights are still dense okay so after the fine tuning we can see a slight difference right here between these two slides you can see the centroid shifted a little bit from here to the centroid shifted a little bit that is due to when we are updating the weights remember right here first the question
(43:25) we are oping the gradient some M since all these blue gradients are trying to update the blue centroid we want to S the M apply nudge change all together to change this centroid centroid will get changed after a couple of each itations and we indeed observed that change between these two slides this is before after before this is after you can see the slight change about the position of same and another question is and the um the assignment able to get changed and that is so true if the not big uh one value might Al also become
(44:12) another bucket so the assignment color assignment might also change we are going to implement that in lab two which we are going to release next week actually we already released it so uh feel free to try it out implement this k meun ization by yourself and finally how many bits do we need in prce previously we are showing to bits for illustration purpose in practice we find for um this is an convolution layer before after four bits the accuracy is going to drop sharply but before that the accuracy can be very
(44:51) more maintain but FC layers after two bits two bits before the accuracy is still well maintained for the top five and accuracy in practice nowadays for large language models we're going to see that in lab Lab four um bit is very enough it's is quite good maintaining the accuracy of large language models this Transformer models so another idea um composing this compression paper is using halfman coding to further compress it since the weight distribution is not uniform okay so some of the weights appear very frequently some of the weights doesn't
(45:33) appear very frequently so we can put the weights using different number of bits for different weights they can use different variable l in coding so we can use fewer bits for the more frequent appear appearing one and use more number of bits for those less frequently appearing LS so put them together we have this three stage pipeline for deep compression first using pruning to reduce the number of Weights by TR model prun retr it repeat and then by quantization we first cluster the weights using K means clustering
(46:11) generate a code book 16 entries four bits or four entries two bits then we quantize the weights with code book retrain the code book and repeat this process and finally we can in COD the weights using different bits for different weights and as a result we can achieve high compression ratio across different neuron Nets 40 times for NS 39 5 25 times alexnet even the goet reset can be compressed by an order of magnitude and this is the model size before and after the compensation this is original accuracy and the compressed
(46:51) accuracy uh the accuracy can be very well maintained even for very small and compact neuron Nets like squeeze net uh we are going to introduce in the neur archa search session it's very small to begin with like 0.47 megabytes okay but begin with is 4.8 megabytes to begin with compared with alexl is already 50 times smaller 50 times smaller compared with Alex exactly the same accuracy but even that can be compressed by get compression h of magnitude so 10 times smaller maintaining the accuracy while model is only 0.47 n and put it in the pocket
(47:36) okay question yes that's the hyper primer and how to find that hyper parameter we are going to find where the accuracy draw here we get the accuracy drop 4 bit so k equal to 16 here actually drop by two bits so k equal to four we are going play with that in the homework good question all right so how do we compute on a k means based weight quantization we have to DEC comprise it okay by mapping it from the index into original weight so here we have the code book we have the quanze weight to first decode it from unsign int which
(48:20) is just the index into floating Point weight okay and all the remaining operations will be done floating point so it save the storage but doesn't save the computation but it's still very helpful for uh the modern Transformers and large language models because for uh single user for uh on the generation phase it's completely memory bounded not compute bounded so using ip6 is totally fine but reducing the um memory footprint by four times compared with ip16 is super helpful okay let's take a short break before we jump into the next part which
(49:00) is linear quantization all right welcome back so let's continue discussion about linear quantization so now we want to use integer weights integer we to represent the values and also we want to use the integer arithmetic to do the computation so that we want to get acceleration and also get a storage saving so let's see what is linear compensation this very widely used if are using a mobile phone running application locally that's highly possible linear quantization is used here so what is linear quantisation
(49:36) given this is our familiar Matrix 4x4 weight Matrix so now uh we want to quantize them into only integer values okay right here is 1 Z minus 2 one - one 0 y = to okay so now this is the point version of original Matrix so 2.09 becomes 1 minus 0.98 becomes minus to so here we have two parameters are going to learn how determine these parameters but let's just see how that works we have to Define zero point and a scaler and a scale factor we mul subtract the Matrix with zero point multiply the scaling factor and we get
(50:26) the reconstructed weight Matrix so 2.09 becomes 2.14 minus 0.98 now becomes minus 1.07 okay and this is the quantization error it's quite small so they are roughly corresp very well cor correlates between and after quantisation so the magic here is how do we find calculate this zero point to bit sign integer versus this scale which is is a 32bit floting point number so what is a uh zero point and scaling Factor so we are going to map this integer value of Q subtracted from the integer zero point and multiply by the scaling Factor we
(51:20) get the floating point value okay so basically Z which is the zero point um is is allow a real number Ral to Z which Ral to Z be exactly represented by a quantize integer Z so will be mapped to zero okay and then we have another scaling factor to scale the dynamic range what that means is that we have a floating Point dynamic range from R mean to our Max this is the range of V values we also have a dynamic range for the integer value from Q mean to Max and the 0o point Maps Z to Zero from integer space to floating Point stage uh
(52:07) to floating Point um Range so this is corresponding to different number of bits you have two bits the Q meus 2 maxal to one you have three bits from Min - four pos positive three so the integer um Q me and Q Max are fixed so how do we calculate the scaling Factor okay so Q mean has to be mapped R mean q Max has to be mapped R Max using the equation where we map Q Max minus Z scale by we get R Max Q mean minus Z sced by S we get R mean can subtract them and then divide it we can get the scaling Factor equal to R Max minus r
(52:55) mean which is here R Max minus r mean the dynamic range of the floting point range divided by the Q Max minus me the dynamic range of the integer scale okay so s can be calculated in this way R Max minus r me / q maxus me so given our Matrix this is the formar Matrix the max value is 2.1 me value is minus 1.
(53:32) 08 we use this equation to calculate um the dynamic range of the floating Point versus dynamic range of the integer and now we can get the scaling factor to be 1.07 that's how we calculate the scaling Factor now we can calculate uh the Z okay so since we already have S can plug in s to get the z um and have has to be an integer therefore we have to round it to the closest integer value in our example Q mean is minus 1.
(54:10) 08 in the right part we plug equation um and then we can plug in Q uh plug in the Q mean which is minus two since we having two bits Q me is minus two R mean is-8 - 108 as we already calculated is 1.07 we run z0 point is minus one so now we let's talk about linear quantied matrix multiplication so how do we use integer unit only unit to calculate the matrix multiplication so linear quantization is a I find mapping from integers to real numbers using the equation we just talk about so consider the following Matrix modifcation y = WX originally there are FL values now we
(55:04) want to convert them it into integer values we are going to implement in the lab so I will pay really attention to the derivation for remaining 20 minutes so we plug in the equation where R = to S times Q minus z s * 2us Z for the output for the weight uh input activation I can mean and then we rearrange them by subra moving s y to the right hand side move Z to the hand side and then we can expand the equation from this row to Row all the way we get these four different terms so far a very simple derivation so we want to use mbit for
(55:52) example 8 bit integer multiplication and use little bit um integer addition to prevent overflow so first of all the scaling Factor um and then we have this mbit integer addition and then the zellow part are all constant the weight is constant uh 0o point is a constant profile 0 is constant so all this can be precomputed it's not in a runtime no runtime overhead and now let's focus on the scaling Factor so imp ially the scaling Factor s w SX / s y is between 0 and one from this paper here can be represented by 2
(56:38) to the power of minus n times a value okay a fixed point value between 0.5 and one and this multiplication can be done with a fixed Point multiplication and the to power of minus n can be done by simple bit shift so we don't need FL 14 Point Unit um to implement this blue part so for this part we scale them to in integer this part can be precomputed and the last part is in integer addition and let's look at the middle part the ZW times QX so zero point of the weight what is distribution like so it's roughly following a normal
(57:22) distribution okay so roughly centered R zero okay so we can safely set ZW to be zero because it's symmetric and roughly zero is maed to zero okay so we set ZW to be zero and what I mean is that originally C mat to zero now map Zero from the fix point to 14 range in that case we can simplify the equation where now Z become zero so the scaling Factor will be using the absolute value of the uh r divided by toal power of n minus one okay much simpler calculation for the scaling Factor previously we have to do this
(58:12) calculation um to map it now we just do a much simpler uh calculation to map it so we can um safely get rid of this ping part since ZW is zero it's symmetric okay then we are left only with these two terms the first term which is let me done by 8 bit integer multiplication QX and Q qw that's where the most of weight lifting is happening in the computation and we can um since z w is zero we only are left with a single term these are constants because Z is a constant and q w the quantize weight the weight is also constant and
(58:58) we only left with the zy and the blue part is bul of computation where the bu of computation happens now let's consider adding back the bias okay so far we are ignoring the bias now we consider following the fully connected layer with the bias okay so can be also we plug in equation right here the bias can be also represented by subtracting the bias and then multiply with the scaling factor of that bias since previously we mentioned ZW is z it's following a normal distribution we can set ZW to be zero so integer
(59:39) integer zero map to the 14.0 we can eliminate everything associated with the ZW so we are left with the equation here we can force uh this s SP again the scaling factor for the bias versus the scaling factor of the weight and activation versus the ZB similar to the weight distribution of the bias is also following a normal distribution we so can set ZB to be zero okay so we can CB to be zero and we force SB equal to s w * SX so that the equation can be simplified in this way we absorb QB together with two terms okay so we
(60:28) force SP equal to s w * SX say like let's continue the derivation we are going to move s y the right hand side and move z y to the right hand side so we are left with this equation so QB DX qw qu wave Z point of activation bias of um the they're all constant so we pre computer and we Define the QB minus ZX2 to be Q bias since is a constant then we can simplify all the equation to something like this let's look closer into this equation um the blue part is B of the computation most of the weight lifting is happening in the blue part
(61:19) that's a low bit integer multiplication and we can rescale the scaling fact to n bit integer like 8bit integer followed by shift we just discuss about it and finally we have nit integer add right so now everything can be cast into a low Precision integer operation what if we um use convolution rather than a fully connected layer so compared with the fully connected layer now we run the convolution it's actually derivation is pretty much the same you're going to visit in the lab we just replace multiplication with convolution they're
(62:02) all linear okay so blue part is where uh this green part is where most of the computation is happening so this is a computation uh diagram where we have previously the quanti weight this is the quanti input activation we do the integral C convolution uh with added the bias the sum is using a 32-bit register for the summation to prevent overflow okay so it be in 32 bit for the uh summation register and all the sum is done in 32 bit okay and the bias scale by the scaling factor and then add the zero point and finally we get the
(62:50) quantized output so what's the difference with the uh K means quantization previously we are using um 32bit rooting point to do all the computation now we are using integer to do all the computation so we are saving not only the storage but also we are saving the compute so we are going to implement this in lab two so make sure you are understand the lecture here I'll pause a little bit for you to in case have any questions for the derivation yeah for Ru you basically have nothing on the um nothing on left so
(63:40) you're wasting half of the dynamic range if you set the bias zero point of the bias to be zero which is this case you was the half of dynamic range if no more questions let's see how does the accuracy compare with the floating point so on the this figure is showing the floating Point versus a integer okay so this is the latency versus accuracy ideally we want something on the top left corner and consistently the AIT is having a better trade off on the on the plot compared with p uh floating Point counterpart
(64:26) higher accuracy same latency or accuracy lower latency on the left hand side is showing for res 50 Inception V3 um the floating Point versus integer um accuracy and nowadays this 8bit quantization becomes very mature that's the this solution to accelerate and compress yours and most of the hardware have 8bit integer unit so uh that's very widely used if you're using um running neuronet on the phone on a microcontroller edge device this is a very critical solution all right so that concludes our second part which is the linear
(65:08) quantization so different from CL quantization which only save storage but doesn't save computation linear quantisation can save both storage and the quantisation so what is next can we go even lower Precision so we are going to talk about binary tary pensation in the next lecture want to use one bit or two bits to present each L okay so let's summarize today's lecture we first reviewed introduced the data types floating Point what is integer what is a floating Point representation what is the exponent bit what is the mantisa bit what
(65:47) subnormal region how do we represent very big numbers what is fp4 fp8 different configurations uh E2 M4 Etc E5 M2 Etc and also we visited the basic concept of neuronal quantization to turn convert the weight and activations to discrete values and then we introduce two types of quantization one is K means quantization we're going to see them in the homework and also linear quantization we went through a long derivation make sure uh you can translate them into the code in LA to and here are some references and that's all for today's
(66:30) lecture thank you
