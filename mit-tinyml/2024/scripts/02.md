# 2. Basics of Neural Networks

01

Okay, good afternoon everyone. Let's get started with lecture two of `efficientml.ai`. So today we are going to learn about basics of neural networks.

02

As we have seen in the last lecture, there's a big gap between the supply and demand of AI computing, where the demand of computation is growing really fast as indicated by the model size of recent large language models.

03

04

And Moore's Law is roughly doubling every two years. But the deep learning models are getting more than four times larger every two years, so there's going to be a gap which motivated us to bridge this gap with model compression and acceleration techniques.

So in the last lecture, I just do a quick recap, we went through several examples from vision to visual language model and also the multimodality models to give you an overview about the latest advancement of AI computing and how much amount of computing they need to motivate us to build these efficient algorithms and systems to accelerate efficient AI computing.

05

So today we are going to start with those detailed technical details to first review the terminology of neural networks such as neurons, synapses, activation, feature, weight, parameter, etc. And also we are going to review the popular building blocks with the fully connected layer, convolution, group convolution, depthwise convolution, especially to understand the computation demand behind that, including both the memory demand and also the computation demand.

We're also going to introduce those efficiency metrics including the number of parameters, how to calculate the number of parameters, the model size, the peak activation size, the MACs, FLOP, FLOPs, OP, OPs - what is the difference between them, what is the relationship between them, latency, throughput. And if we have time, we're going to go through lab zero, which is a tutorial on PyTorch.

So lab zero is optional, but make sure we also want you to make a submission on Canvas. It's not going to be graded, but it'll get yourself familiarized with the procedure of how do we submit homework on Canvas. And for all the announcements related, please keep an eye on our course website. We are going to make all the announcements, the lecture slides, lecture videos, homework, assignments - everything will be released at our course website, which is `efficientml.ai`. So if you have not bookmarked yet, make sure to bookmark our course website `efficientml.ai` and check it every week to get the latest slides, videos, and homework.

All the homework - we have five labs. The schedule of the lab release will be also announced in the `efficientml.ai` website. So make sure you bookmark our website.

06

Okay, so let's jump into _neuron_, _synapse_. As shown on the right-hand side, we have a synapse on the left and we have input Axon on the left, output Axon on the right, and we have synapse that compute the weighted average of the input Axon. So synapse is basically the weight of neural network.

07

So the right part is synapses, right? So we use the terminology interchangeably between synapses, weights, and parameters. So if you heard these terms, they mean the same thing.

And the neurons, features, activations - we use these terminologies interchangeably. They also mean the same thing. So in this example, we have a two-layer neural network and get the output. So the dimensionality of these hidden layers determines the width of the model, right? So the width of model matters a lot to the efficiency of neural networks.

So you can design a wide but shallow network with the same number of parameters as a narrow but deep neural network. So I guess which one is more accurate and which one is more hardware efficient in these two scenarios? One is very deep but narrow neural network, the other is a shallow but wide neural network. Which one is more hardware friendly?

Wide and shallow. Why is that the case?

Exactly, fewer number of kernel calls, and I have more parallelism to fully exhaust the number of threads in the GPUs. So from the efficiency perspective, a wide and shallow network might be more efficient. In the meantime, from the accuracy perspective, usually a deep network helps a lot. So there is a tradeoff - you don't get the free lunch on both sides.

So that's the beauty of neural network architecture design, where we're going to visit in the later part of this lecture how do we tradeoff between this hardware efficiency and also the accuracy.

08

09

So let's visit a few popular neural network layers, starting from the fully connected layer, also called a linear layer. So in a linear layer, you have a weight matrix which is of dimension $c_i$ (input channel) times $c_o$ (which is the output channel), okay? So the number of parameters here is $c_i$ times $c_o$. And this is the input activation $c_i$, and this is the output activation $c_o$. So that's the input feature dimension.

Usually when we are talking about efficient computing, it's super important to understand the dimension of each tensor. Later we are going to build the model to calculate the FLOPs, number of operations, and so the model, the parameter size - it all concerns about the dimension of each tensor. So it's very crucial to figure out the dimension of the input feature, output feature map, weight feature, and also the bias. Especially the bias - when we are going to learn about the diffusion on-device training, we are having a technique called _bias-only update_, or recently the _LoRA_, which concerns about the bias. So we're going to revisit that in later part of the lecture.

So very simple: output equal to the weight times the input. So that's fully connected layer, also called a linear layer.

10

11

What if we have multiple inputs? So here we have a batch size of two, we have two inputs, right? So the weight dimension stays the same with $c_i$ times $c_o$, but the input becomes $n$ \* $c_i$ and the output feature map becomes $n$ times $c_o$.

12

Convolution layer. So the output neuron is no longer connected to all the inputs but is connected only to the inputs within the receptive field, okay? So we have an input feature, and this is the spatial dimension. Now we just assume it's 1D convolution, and later let's jump into 2D.

13

In this 1D convolution, this is the channel dimension, this is the spatial 1D dimension, and we apply this green filter. Here we also have the input channel dimension $c_i$, and here we have the $k_w$ which is the filter dimension.

14

And then we have multiple such filters. Each filter is going to convolve with the input and produce one output.

15

16

17

And we can shift it - this sliding window - by one to get another output. We can shift it again to get another output.

So as a result, the feature output channel dimension is three since we have three kernels for the weight. And the weight dimension is basically $c_i * k * c_o$, since each kernel dimension we have $c_i * k$ and have $c_o$ this amount of kernels. And finally we have a bias with the dimension of $c_o$.

18

Now let's generalize that to 2D convolution, okay? As we can see, added another dimension. It's 2D convolution, but looks like the feature map is three-dimensional. That's because for each XY location, we have a channel dimension, okay? The channel dimension. So for 2D convolution, the feature map is actually 3D.

So the activation map - h times w, assuming imagine you have a picture width is w, height h.

19

And then we apply a filter which is $k_h$ by $k_w$, the filter dimension for the H and W dimension, with the same input channel dimension $c_i$. And then we have multiple such filters - three such filters, which is equal to $c_o$ in this case. And we can shift it by one to get another output, shifted by two to get another output. We can shift in another dimension.

And so look at the output feature map - 3x3 output feature map. And how do we calculate the size of the output feature map? It is equal to the size of the input minus the kernel plus one. Why? Because you can shift $k_h$ minus one this amount of shifts, and applying each shift you get one more output. So H_out equal to H_in minus $k_h$ plus one. In this case, the H_in equal to four, the input is 4x4, and then the kernel size is three. Therefore, the output dimension is four minus three plus one - it will be two in this case.

Let's also talk about padding, okay? So padding can be used to keep the output feature map the same as input feature map. Otherwise, using convolution, the feature map will get smaller and smaller as you have a deeper number of layers.

So one common way is to apply zero padding - pad the input boundaries with zero, which is a default in PyTorch. For example, in this case, the original height and width equal to five. In this animation, kernel size is three. So the output height and width equal to 5 + 2 \* 1 (2P, padding equal to one - we pad one on each side) and minus 3 plus one equal to five. So the output feature map is the same as input.

Compared with the initial version like in here, the output becomes smaller. The input is 4x4, but output becomes 2x2, as we can see on the example here, right? Initially the input is 4x4, output becomes two. And after we apply padding, after padding, the input and output are both five by five in this case. So we have zero padding there.

There are also other methods to apply padding. For example, using reflection padding. So for example right here, the padded number is the reflected value of the original feature map. We can also apply the replication padding to use closest number and replicate that to apply the padding. But zero is still the widely used approach.

So after padding, let's now talk about the receptive field. For example, the output pixel here - how many pixels can it see in the previous layer? You can see 3x3 pixels if the kernel size is 3x3, right? And each pixel here is another 3x3. So how many pixels from here? Actually 5x5, right? And one more layer gets a 7x7 receptive field.

Why do we need a large receptive field? We want to understand the relationship between different pixels. For example, a car on the road versus some pedestrian also on the road - the relationship between car and pedestrian means whether you can drive or you should avoid pedestrian, right? So the relationship matters for those high-level vision tasks.

And why people use Transformers later? Because Transformer gives you a global receptive field - you can see every pixel, can attend to every pixel in Vision Transformers. We're going to cover that later. So the principle is that a larger receptive field helps a lot - is very crucial to high-level image understanding.

So with two layers, 3x3 kernels, we can see a receptive field of 5x5. And in general, the equation is here: with L layers, the receptive field size equal to L \* (kernel size - 1) + 1. So for two layers, kernel size equal to three, the receptive field is five. And if you have three layers, kernel size is three, the receptive field is seven.

So the problem is that for large images, in order to have a large receptive field, we have to have very deep layers. We need many layers, and we just talked about that in the early part of the lecture - you have too many layers, you have lots of kernel calls, also have to store a lot of activations. Activations have to be stored during back propagation, increasing our training memory.

So how do we have a large receptive field at the same time we don't want to have so many layers? So we can downsample inside the neural network. One example is to use the strided convolution layer, okay?

So compared with stride equal to one, which is on the bottom, the top shows the stride equal to two. Okay, so rather than every pixel sees adjacent 3x3 pixels, now you see 5x5, okay? The adjacent green 3x3 rectangle is shifted by not one pixel but two pixels, okay? You can see this 3x3 versus this is actually shifting by two. So that's a stride of - every time you don't move one step but you move two steps.

So that's the strided convolution. In this case, for two layers and kernel size equal to three, the receptive field becomes seven, okay? Previously it was five, now seven. We have a larger receptive field with the same number of layers. So this is very helpful for reducing the number of layers, reducing the number of weights, reducing the number of activations, but have the same receptive field.

But of course, you may lose a bit accuracy because the model capacity might be smaller. But everything is tradeoff - there's no free lunch in deep neural network design. Everything is about constraint optimization, which makes the optimization highly interesting, okay?

So our goal is to continue reducing the number of parameters, okay? So for convolution layer, each output channel is connected to all the input channels, to all the channels, okay? How to reduce the amount of computation? People invented this group convolution layer, okay?

So previously we have just one group - every output channel is connected to all the input channels. Now we can divide them into two groups. For the first group, only half of the input channel is connected, and for the second half, that's the second group. The computation doesn't change, stays the same as before, but the number of weights will be G times smaller, okay? Will be G times smaller. So $c_o$ will be divided by G, and $c_i$ will also be divided by G, and we have G amount of such filters, okay? Therefore, the total amount of weights is reduced by G, okay? So that's the group convolution.

And what is the extreme of group convolution? The number of groups will be equal to the number of input or output channels, okay? So which is exactly this case. Previously we have two groups, now have $c_o$ groups. In this case, it's eight, right? We have eight groups. So each output channel is only connected to one input channel, okay?

So the number of input feature and output feature stays the same, and the number of weights will be C (which is the same for $c_i$ and $c_o$) times $k_h$ and $k_w$, which is the extreme case of group convolution called depthwise convolution layer, which is quite widely used since 2015-2016 when MobileNet was invented.

Next, pooling layer. We want to have a smaller feature map. Originally the input image might be like 1K by 1K - it's pretty large, and we want to have a smaller feature map and condense information, right? This is actually quite useful for large resolution input - we want to quickly pool the feature map to make it smaller.

So here we have an example with the 4x4 input feature map, and how do we pool it to 2x2? Can either use max pooling - so for every 2x2 input, we find the max element within that group - or we can do this kind of average pooling, finding in the blue area, finding the average, for the yellow area to do the average pooling.

The good thing about pooling is that there's no learnable parameters, okay? Or you can assume it contains zero weight, which is very parameter efficient.

Next, let's talk about normalization layer. So batch normalization came roughly in 2015-16, roughly seven-eight years ago, which later becomes quite useful to stabilize the training. For example, the layer norm also became super helpful in the age of attention Transformers.

So what exactly is normalization? So we want to normalize the feature map to make the optimization faster, okay? So how do we apply that? We minus the feature map mean of that tensor and divide it by the sigma, okay? So we want to make sure to minus the mean and divide it by standard deviation over a set of pixels or tensors if it is not vision, okay?

So this is a reminder how to calculate the mean and the standard deviation. And note here we have a small sigma right here, there, okay? So that is due to we want to avoid dividing by zero. So we put a very small number over there. And then we learn a linear transformation, okay, which is indicated by the scaling factor and also the bias right here to compensate for the possible loss of representational ability, okay?

So how do we define this set of pixels or of tensors? On the bottom, it shows four different ways to find that group of pixels. The first one, very widely used in CNN, is batch normalization, okay? So it's normalizing across the $n$ dimension - $n$ is image dimension, okay? And also we just use a single C dimension across all the H and W - height and width dimension, okay? Normalize across different batches.

Second one is quite widely used in the attention mechanism. So for different input, for each input across different H and W - C is a channel dimension - make sure it's normalized. So for each token in the attention mechanism, when you are doing the attention, after each token is normalized, then the attention map makes sense. Otherwise, if it's not normalized, the attention map will be much harder to optimize.

There's also the instance normalization. It just normalizes across the H and W dimension, not C. And when the C is too large, there's also the group normalization on the right, which divides the C by several groups and only normalizes within that group.

There's a lot of tricks we can play for the normalization layer. For example, when we are doing fine-tuning, actually normalization layer - the number of parameters is very small. So we can only fine-tune the weight bias and also the scaling factor in normalization layer, making the fine-tuning much more parameter efficient.

The normalization layer can also absorb the transformation of such as smooth quantization. So without incurring another kernel call, we can absorb a lot of the computation in the epilog or fuse it with the normalization layer. So we are going to visit them in later part of this lecture.

Next one is activation function. So we have seen many different activation functions, but let me talk about the efficiency and accuracy tradeoff here.

Sigmoid - very ancient activation function. It ranges between zero and one. It's very easy to quantize, very efficient to quantize because the dynamic range is very limited - it's fixed between zero and one. But what is the drawback? Vanishing gradient, right? So if the value is super small or large, there's no gradient.

And then people came up with ReLU, okay? When it's positive, the gradient never vanishes. But if it's negative, the neuron is basically dead - there's no way to put it back because there's no gradient anywhere when the value is smaller than zero.

But what is good about it is that it's very easy to sparsify. Since we are going to talk about pruning and sparsity in the later part of this lecture, the ReLU activation function naturally brings sparsity because a lot of the activations, once they are negative, they become zero. And multiply by anything is zero, so you don't have to compute on it. You don't even have to store it - you can just store a mask showing that this is zero, just using one bit. And the gradient is one everywhere else, okay?

So you don't even need to save this feature map. You can just save a bit mask for this tensor. If the bit mask is zero, then the output is zero. If the bit mask is one, it will just copy the feature map from the previous output. So very activation-friendly, sparsity-friendly, and simple.

But what is the downside? Dynamic range is super large. It's not easy to quantize. So people invented ReLU6, okay? ReLU6 to cap the largest value to be six. That's why people call it ReLU6, okay? It's purely empirical, and that makes the quantization range much more constrained, reducing the dynamic range, making it easier to quantize.

Another drawback is that there's no gradient in the negative region. So people invented Leaky ReLU, okay? There's a smaller slope on the negative side, okay? So the gradient can flow in the negative region. But the downside is that you no longer have this sparsity benefit. If the value is small, if it is negative, it's no longer zero.

And then people, using neural architecture search, find a very interesting activation function called Swish, okay? Swish basically is very similar to ReLU value, but it's using x / (1 + e^(-x)). But people later find it hard to implement in hardware, right? So people came up with an approximated Swish called Hard Swish, okay? So when x is smaller than minus 3, it's zero. Above 3, just x. And in between that, the value is (x + 3) divided by 6, okay?

So all these activation functions somehow have something to do with efficiency, like sparsity, quantization, dynamic range, easy to implement. So that's the story behind the evolution of different activation functions.

And finally, Transformers. Transformers are getting super important since the invention in 2017-2018. We'll learn more about Transformer architecture in lecture 12. We have a dedicated lecture just to talk about Transformers - the entire pipeline, entire structure of Transformers.

So let's just give a very simple overview today. So there are two stages: encoding stage and the decoding stage. For each, we have a multi-head attention followed by FFN (feed-forward network). And this is the architecture for the attention mechanism. You have three tensors - Q, K, V - query, key, and value. We do an attention, so scale the dot product attention, and then pass it through a linear layer, get the O. Q, K, V, O - those are the four activation tensors. And there's no weight in the attention mechanism - that's the middle part. Only weight in the Q, K, V, O transformation, okay?

And the detailed way to do that for the query, key, value design is actually very analogous to the retrieval system. Let's take a YouTube search as example. The query is like text prompt in the search bar, okay? And the key would be like the title or description - some short information about the video. And value would be the corresponding video.

To do a dot product with the query and the key to get similarity, and use the similarity to fetch - to do a weighted average of the value. And remember, we want to divide the Q and K by a normalization factor, which is square root of d, to accommodate for the dimension change, to make optimization more stable. And followed by the softmax to get an attention map - $n$ by $n$ attention map - and use that map to do a weighted average sum of the value, okay?

And later part of the lecture, we are going to visit - the attention map, $n$ by $n$ attention map, could be the bottleneck for both computation and memory, since this is growing quadratically with the number of tokens N, okay? And for images, the number of tokens also grow quadratically with the resolution. So N-square would be super expensive.

So there's sparsity opportunity - not all tokens need to attend to each other. Sparse attention - we're going to introduce that. And also Flash Attention - we can use in-place attention to minimize the data movement so that we can have constant, almost memory. But the FLOPs is not reduced. So in order to deal with that problem, we are going to introduce long context techniques, all related to this attention mechanism.

So although attention itself doesn't have any weights, there's a lot of interesting stuff we can explore. Even the KV cache - they become the activation when we are calculating the KV cache, but when we are using that to generate the next token, they become the weight. So when new tokens are generated, how do we quantize, how do we prune the KV cache? We're also going to cover that in later part of the lecture.

Let's continue the lecture to talk about the efficiency metrics. How should we measure the efficiency of neural networks? So there are several pillars we want to achieve at the same time, but it's pretty hard.

Having a smaller model - so when we are uploading the model to the Apple Store, it's pretty small, right? You don't want to download a model that is hundreds of gigabytes - it's too hard to download.

And also, we want to have a faster model, okay? Give you the real-time prediction, never miss a pedestrian on the road for self-driving car, and also generate images instantly on your phone, for example.

And also, greener model - taking less battery. For example, on the iPhone, on the mobile devices, you don't want to drain your battery with these AI applications.

And that concerns the storage, the latency, and energy, right? Computation, the memory is determining the storage, latency, and energy.

So we are going to introduce several efficiency metrics on the right-hand side. Some of them are memory-related, some are computation-related. On the memory side, we'll introduce how to count the number of parameters, the model size, the total activation size, and peak activation size. For the computation-related, we're going to talk about what is MAC, what is FLOP, FLOPs, OP, OPs.

So let's first talk about latency. What is latency? It measures the delay for specific task. And let me directly show you an example. So on the left-hand side is high latency, right? Each frame to predict the segmentation mask of output takes a long time. And right-hand side is lower latency - only 45-46 milliseconds to process each frame.

What is throughput? It measures the rate at which data is processed. On the left-hand side is low throughput - you can process six videos per second. On the right-hand side is high throughput - 77 videos per second. This is from the Temporal Shift Module paper for video understanding we are going to introduce later.

So what is the relationship between latency and throughput? Does higher throughput translate to lower latency? Does lower latency translate to higher throughput? So let's see an example.

The first design: latency is 50 milliseconds. So every 50 milliseconds, we can process one image. So how many images can we process every second? It's 20, right?

On the design two, we have higher parallelism. We can process four images at the same time, each taking 100 milliseconds. So latency to process each image from start to end is 100 milliseconds, and the throughput would be 40 images per second, since we are processing four such images at the same time.

So on the left-hand side is having a lower latency but lower throughput. Right-hand side is having a higher latency but higher throughput. As a result, higher throughput doesn't translate to lower latency, and lower latency also doesn't translate to higher throughput.

On the mobile, we sometimes care about - we mostly care about latency. On the data center, batch processing tasks, we care about the throughput. So those are very important metrics - latency and throughput. Sometimes we want to make sure to report both.

So what are the factors that impact the latency? So there are two factors that are determining the latency. One is the computation, one is the memory, okay?

The T of computation equal to number of operations in a neural network model divided by the number of operations that the processor can do per second. And the bottom is hardware-specific characteristic. On the top is neural network-specific. And then T memory also is determined by two factors: the activations and also weight - to move the activation and the weight. This is about inference. If it's training, it also concerns about communication about the gradient.

So T data movement of weight equal to the weight model size divided by the memory bandwidth - hardware-specific, neural network-specific. And T data movement of activations is determined by the input activation size plus the output activation size. This is first order - all these are first-order approximations to give you a ballpark estimation - divided by the memory bandwidth of processor.

So which one is more expensive? So this is a figure showing the energy consumption of different operations, from a 32-bit integer add to accessing register file, to multiplication, to accessing the SRAM cache, to accessing the DRAM cache, to accessing the DRAM memory. We can see that accessing the DRAM memory could be super expensive, okay? 640 pJ versus just doing a 32-bit add - it's just 0.1 pJ. So accessing the memory is a lot more expensive than doing the arithmetic - magnitude more energy consumed to access memory. Keep in mind: computation is cheap, data movement is expensive.

Number of parameters. Here we have a small convolution layer compared with convolution in the same dimension for popular neural network for AlexNet. Here, where the channel number is - input channel is 3, output is 96, and the kernel size is 11x11. So I give you one minute to calculate, to think about the number of parameters for the next layer.

Okay, here is 13x13 image size and 3x3 convolution. The channel number is 256 to 384. So here is a quick answer: since the input channel is 256, output 384, and the kernel size is 3x3, this is the way to calculate that.

And finally, for the fully connected layer, you just multiply the input channel with the output channel to calculate the total number of weights, which is 61 million in total.

So how does that translate to the model size? Between the number of parameters to model size, what's the relationship? You want to multiply the number of bytes for each parameter with the number of parameters, okay? So number of parameters multiplied by bit width to get a model size.

So for example, AlexNet has 61 million parameters. If each weight is represented by the single precision 32-bit number, and how many bytes are there in a 32-bit? Four bytes, right? 32 bits equal to four bytes. So 61 million times 4, you have 244 megabytes.

So make sure you have the prerequisite so that you can understand these concepts relatively easily - how many bits per byte. And if we want to quantize the weight to integer, right? 8-bit integer. What is the total storage for the weight? So 8-bit, that equal to one byte. 61 million parameters - that's 61 megabytes. 61 megabytes - four times smaller, right, compared with 32-bit.

And later part of this lecture, we are going to learn about activation-aware weight-only quantization, AWQ, to quantize it to 4-bit, okay? 4-bit will be another two times smaller so that you can fit large language models locally on your laptop.

So now let's switch gears to talk about the total and peak number of activations. So activation is the bottleneck for CNN inference, not the parameters. For example, here from ResNet to MobileNet, number of parameters is reduced a lot - by four times, more than four times - but the activation, peak activation didn't decrease but increased.

So how to reduce the activation - activation is very crucial. The deeper the layer, the more number of activations. Here, that's the case. And also, the peak activation determines the memory bottleneck. For example, the third layer here determines what is the max memory you are going to consume when you are doing the inference.

So the imbalanced memory distribution happens very popularly for CNN. Why is that the case? Because the first couple of layers, you have very high resolution, and it's square. And later you have a lower resolution due to downsampling.

For example, if a microcontroller has only 256 kilobytes of RAM, of SRAM, to hold these activations, you want to make sure the largest layer, for example here, doesn't exceed the SRAM so that you can put all the activations in the fast SRAM.

Activation is also becoming the memory bottleneck in training. For large language models, the number of parameters is also the bottleneck. That's why people later proposed this model parallelism to do the sharding of weight.

So here is an example for the $c_i$FAR-10 dataset, where this is showing number of parameters versus the number of activations. The number of activations is like seven times larger than the number of parameters when you are doing training due to the batch - activation is pretty large, occupying a lot of the GPU memories.

And here, from ResNet-50 to MobileNet V2 1.0, they pretty much have the same accuracy. So it's an apples-to-apples comparison. The parameter is reduced by four times, but the main memory bottleneck, which is the activation, didn't improve much - only 1.1 times, 10%. So optimizing the activation is hard.

And usually for CNN, the distribution for the weight and activation is different. The yellow part is activation, the blue part is weight. It's U-shaped, okay? Initially, the activation memory is large. Later, the weight memory is large. This is due to it has high resolution, and this is due to it has larger amount of channels.

So this is calculating the number of activations for AlexNet. So `C_H_W` - that's the way to calculate the activation size. And if you add them together, you can see this is the total amount of activations for AlexNet. And the peak activation is roughly, first-order approximation, number of input activation plus number of output activation, okay?

Now let's switch gears to talk about compute-bounded metrics. For example, MAC - what is a MAC? Not McDonald's - it's multiply and accumulate. Multiply and accumulate. So A equal to A plus B \* C. That's one MAC - one accumulation and one multiplication. It usually comes as a single instruction in GPUs.

So for matrix-vector multiplication, how to calculate the number of MACs? For example, on the top right corner, this is a matrix-vector multiplication, okay? So we are going to produce M outputs. In order to produce each output, we need to compute $n$ multiplications. So in order to produce each output, we need to calculate $n$ MACs. So all together, we have M \* $n$ MACs.

What about a matrix multiplication, short for GEMM (General Matrix-Matrix multiplication)? On the right-hand side, we have an M by K matrix times a K by $n$ matrix, and produce an M by $n$ output. So we have M by $n$ output. In order to calculate each output, we need K MACs. So all together, we need `M_N_K` MACs.

So here we are showing the number of MACs for different layers. For linear layer, we just talked about that - the MAC equal to C_in times C_out, assuming batch size of one. And for convolution, it's actually multiplying six terms together, okay?

So we are calculating - this is the amount of output features we have to calculate, which is W*out times H_out times C_out, okay? And in order to compute each output pixel, how many MACs do we need? Need the entire kernel to be convolved. So that's $k_w$ * $k$H \_ $c_i$. Multiplying them together, we have to multiply these six terms together - three from here and three from here, okay? That's the way to calculate convolution MACs.

With group convolution, okay, we just divided by G. G is the number of groups. And for depthwise convolution, $c_i$ equal to G. So we cancel one term and multiplying these five terms together. $c_i$ gets cancelled due to G equal to $c_i$.

And this is calculating the MACs for AlexNet. For example, each layer, multiplying these six terms together. If it is group convolution, divided by the number of groups - in this case, two, this case, two. And finally, for the last three fully connected layers, it's just input channel times output channel - two terms multiplied together. Where ultimately you have 724 million MACs in total, okay?

So now let's switch gears to talk about FLOP and FLOPs. So one multiply is a floating point operation. One add is also a floating point operation. So FLOP is short for number of floating point operations - floating operations. So how many FLOPs are there in a MAC? A MAC has a multiply - is a FLOP. A MAC also has an add - is also a FLOP. So one multiply and accumulate operation is equal to two floating point operations if the operands are floating point numbers.

For example, AlexNet has 724 million MACs. The total number of floating point operations will be 724 \* 2, that's 1.4 giga FLOPs, okay?

And what is FLOPs? So the "s" short for floating point operations per second - the number of FLOPs divided by how many seconds. So that's a performance metric showing how fast are you processing these floating point numbers.

So what is OP? We don't always represent the numbers in floating point. Sometimes we represent them in integer, right? So both floating point operation and integer - all kinds of operation, even bit mask, or even AND/OR operation - we can call it the OP, okay? To generalize, the number of operations is used to measure the amount of computation.

For example, if AlexNet has 724 million MACs, if each number is represented by, say, binary or ternary, floating point or integer, whatever you have - any operation can be counted as an OP. So 724 million MACs will be equal to 1.4 giga OPs.

And similarly, operations per second (OPs) equal to number of operations divided by the number of seconds to complete this operation, which is a performance speed metric.

Hey, before jumping into the lab zero tutorial, let's give a short summary of today's lecture. We reviewed the basics of neural networks - output, activation, synapses, weight, popular layers including FC layer, convolution layer, depthwise convolution, pooling, and normalization. We also introduced efficiency metrics - parameters, model size, activations, MACs, FLOP, FLOPs, OP, and OPs.
