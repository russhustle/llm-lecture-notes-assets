EfficientML.ai Lecture 18 - Diffusion Models (Zoom Recording) (MIT 6.5940, Fall 2024) - YouTube
https://www.youtube.com/watch?v=LK57PyOJWYk

Transcript:
(0:00) welcome to lecture 16 of efficient am. so today we are going to learn about efficient diffusion models how to accelerate diffusion models uh in the previous lecture we've briefly talked about deep generative learning so we first uh TR a model and then we can predict the data distribution so different from discriminative model which is to predict the decision boundary the generative model tries to predict the distribution of the data so there are lots of applications for generative model for example photo editing image generation uh you may use
(0:40) meid journey to generate images and open open Sora can generate uh videos as well but all these Generations are pretty computationally intensive so in this lecture we are going to learn about how to make these algorithms more efficient and more much cheaper to run on Hardware so this is the landscape of deep generative learning we've covered previously already covered the generative adversary networks we' also talk about the heart Auto regressive model using token in out approach to generate model that uh today we are going to talk about
(1:18) diffusion models we also introduce efficient Auto encoders so this is the landscape of deep generative learning so here is the agenda for today we are going to first talk about the basic of diffusion model in order to accelerate it we need to First understand how diffusion model Works including uh the noising conditional diffusion models latent diffusion models image editing and personalization next we are going to introduce fast sampling steps the DDM method dation method and also acceleration techniques using sparity
(1:56) quantization and also multi multipu paradism and the figure right on hand side is the model S developed by ourselves uh and showing a cyberpunk cat with a new sign that says M okay so let's first introduce how diffusion model works and diffusion model Basics uh so there is two concepts one is the forward diffusion process of the other is the reverse diffusion process so during the forward diffusion process we try to add noise the data so the data becomes noise in reverse process we gradually the noise and release and reveal the data so this can
(2:48) be Illustrated in this image the first uh is the forward path second one the reverse path and see in the forward path we are trying to destroy the data by gradually adding small amount of scy noise so originally the image is a two different dist different distributions now it become the noise and during reverse process we are trying to create data by gradually denoising the noise from a stationary distribution finally will converge uh at to reveal the original image so this is a multistep uh process okay so the forward
(3:29) process it's basically adding The Noise Okay starting from the data across many steps then it becomes a noise and and the XT is actually dependent on the previous step okay so it's a ging distribution with the mean of this is the mean is related to XT minus one previous step and the variance is a ging noise so this Beta key is predefined to control the diffusion speed so this is the relationship between T and beta T so that's the um forward process to add noise we get a relationship between XT and XT minus one um then what
(4:22) about sampling XT directly from x0 the original clean image how do we get uh the nois image at step time T so this is a long derivation but actually it's quite simple since this XT is linearly related to XT minus one we can Define Alpha T to be y minus beta so we can rewrite the equation like this uh this is relationship between XT and XT minus one we can expand this equation by inserting the same relationship between XT minus one versus is 2 so that we could represent XT with minus 2 umon tus andil T minus one
(5:09) okay so adding two different CTI we have this property so that we can combine these two terms noise term together so we get a relationship between XT and minus 2 on and so we can all the way get a relationship between XT - one 2 - 3 all the way to x0 and with combined noise and we can shorten that by defining the alpha T Bar to be product of all the alpha I and as a result we can represent XT as a relation with a with respect to x0 plus the Noise Okay so this is the mean and variance so now we have a the relationship between um the original
(5:59) clean image which is x0 and then at each time step T what is the XT so we Define this relationship between XT and x0 as the diffusion CID okay so for sampling XT equal to the relation between x z and the Noise Okay so beta is designed such that when Alpha T close to zero uh the distribution is close to the normal distribution so given a um at any time stamp T now we can represent that as a function of x0 so that's the forward process gradually adding noise we can Define any time stamp respect to the first time
(6:47) Step Zero and now we are going to talk about the reverse process reverse denoising process starting from XT we want to obtain x0 okay so when beta T is small the relationship between XT minus one and so the adjacent to steps is also a ction okay so the this is mean which a learnable basically that's what we are trying to predict uh consuming the majority of the computation flops um given XT to get the mean and variance is also a caution so let's see in practice how do we uh finish the TR so given a clean image this is the clean image which
(7:37) x0 since we have the relationship between x0 and XT so basically equal to x0 F noise so X zero this is sking Factor plus noise because skting Factor so square root of alpha T Yus minus Alpha T which is exactly the same here and Theta is the neuronal model predicting the noise okay so we add them together we passes get the XT and then we pass it through the model epon Theta to predict the noise okay predict the noise added and this is our predicted noise this is the that we originally added to x0 and then we try to uh match the predicted noise
(8:26) with the original added noise so that we can have a uh MC laws right here I try to try to match them together so that's basically the training process although the equation is pretty complicated if we draw it in the figure it's actually quite simple right so for example t sample T and then we can um sample X zero image and maybe a cat maybe dog and then we sample a noise uh from the ging distribution and then we add them together by having the sum up together to get XT and we predict the noise given the XT okay so Theta is given the we
(9:13) try to predict the noise and try to match the noise that we originally sampled so that's how we add noise to the training process okay so basically the training part is trying to predict the noise make sure match the noise we inserted uh to the image so that's the tning algorithm what about inference what are we doing during the inference time so uh given a noisy input at XT okay so we pass it through the model Theta so what are we trying to predict uh we are trying to predict XT minus y okay so we already have the
(9:57) relationship between XT minus y and so this is the mean variance the mean uh we are trying to predict um predict that by um using this equation okay so new Theta is a combination of XT and Epsilon XT okay so it's combination of XT and Epsilon the is basically predicting the noise given this input okay so given this the model Theta is predicting the noise what the noise inside this image and we subtract the noise this is subtract we subtract noise from the image these are the stating factors we subtract noise from the image we get a cleaner
(10:46) okay a cleaner image is added with noise and then get the den noised image XT minus one okay so in that way we complete one iteration from a noise input image a noisy XT to get cleaner image XT minus one okay the rule of thumb is D noise we first to predict the Noise Okay predict the noise using the Epsilon Theta this is the model is the majority of comp where computation goes predict the noise subtract the Noise Okay and here it actually added another noise another coing noise since XT this is the mean this variance
(11:32) and we have to add back this noise at the noise um image so that is just one time step step of the noising from a noisy image to a cleaner image and then we can repeat this process to get XT minus 2 minus 3 all the way to X x0 okay so that's the entire Denis and process different from the previous models where it's just one time now we have to run it the same model iteratively so this is where the majority of the computation goals so it's stce prediction you have to make pixelwise prediction not just a class not a bonding box but you have to make
(12:17) pixelwise prediction uh that's why this model is really pretty heavy and taking the majority of the time making it slow another reason it is slow because we cannot do it using a single step or very hard to do it in a single step but actually we need a lot of steps that's why it makes slower uh so this is the these are generated uh images actually outperforming Gans um on the image data set so it's a pretty good uh result pretty good start okay so now let's talk about conditional diffusion models we don't want to just give a random image but
(13:02) we we want to uh condition the generation on either a class for example we just want to only generate a cat or a dog tax condition um for example sequence of text tokens like photo of a moon gate sometimes we also want to add a pixelwise conditions what is pixelwise conditions so given a spal map like second mation map a cat or Cy Edge the edge of a cat okay so we not only need to provide a Time stab but also condition add a condition uh to the model so let's see how do we uh perform conditional image generation using
(13:48) diffusion models so one idea is to use this scaler condition a very simple spal addition so this is the original uh feature map this is the conditioned feature map how do we condition that given a class ID like cat maybe ID number 776 and then we encode that by um passing it through several multil perceptions and then we get a embedding batch size time c is the channel versus the original feature map is b c HW so we broadcast the single element to H * W and then we perform this broadcast ad ad to add them together so get a
(14:38) conditioned feature map so that's how we add this class condition we can make it a little more complicated right for example uh we can pass it through a larger MLP to together two parts B times 2C okay one is the SC scaler one is the bias so we scale the feature map we can also add to the feature map to get the condition in feature map that's another way to add this kind of U scalar condition okay how do we add the types condition now we have two modalities one is image modality the other is text modality so during V visual language
(15:23) model like the V we discussed pre in previous lectures we find it possible to Project image and language both of them into tokens right and as a result we can use cross attention to combine uh image and language for example uh the input feature we divide it into several patches each patch is a token so we can tokenize them uh so for the text we also pass it through the uh trans Q KV Matrix so we get the Q token B T token and value token okay so uh we first do qk transpose uh so qk transpose and we use that to the weighted sum for output
(16:12) token so the final Dimension uh token Dimension is still the same as query which is the number of image tokens so the output result is still image um but that already included the information from the language okay so that's one way to add text condition insert the text condition into image generation there's another way uh to do joint attention so in the previous method are they symmetric actually language and image they are not symmetric so the image is treated as the quy language is treated as the key and value so
(17:01) the language and image they are not symmetric but in the joint attention the language and image the image feature and text feature are actually symmetric so they are passed through independent qkv Transformations so this is the qkv transformation for the image this is a qkv transformation for the language the text um and then we can cat them after the transformation okay so this is the query from the image feature this is query from the language feature we can canate them together to get a combined image text feature for the query
(17:42) combined image text feature for the key combined image text feature for the value and we feed them together to the attention Okay and after we get the output we split it by passing them through different output trans Transformations so there is qk for the O we also have independent wo for the image feature and also for the text feature so on the right hand side is another diagram showing this relationship so image and tex they are passed to Independent P QV Transformations and then concatenated together to form joint attension and
(18:26) then split it to separate all matrices to perform MLP and for the next layer what about totally combining them together early so this is another approach a single self attention to combine the image feature and text feature text condition okay so the tokens are early contaminated before we feed them to the qkv transformation uh since the image feature and text feature are contaminated already so we can feed them to the same 2 KV transformation okay and after we pass them through the attention we have a single output
(19:11) transformation so this is the early Fusion for the image and text text feature we can also perform P pixelwise condition okay we've talked about class condition so using a class we talk about a language text condition using a sentence to describe what you want generate now we want to do pixelwise condition for example we want to generate something that follow U The Edge uh described here this is using the P Edge detect uh p detector to find the edge and as a result generated image um together with a text Trump can exactly follow uh
(19:59) the The Edge from here we can also use uh input human pose and extracted from here uh this is the head this is the body hands and then we can generate several images and the shf in the kitchen following exactly the same uh human pose or even a linoln statue following the exact um input human pose and that is by using control net which is a new technique published last year um to add the control by conditioning it on pixels so how does that work this is how control net Works uh previously we have a neuronet given a noise we can predict the image
(20:50) now we freeze the original um network but we make a trainable call Key a trainable call Key and we insert the condition from for the trainable copy started with a zero convolution so that when the training starts uh this branch has no effect since this is zero conclusion before and after okay during the training um they start to change we make the trainable copy call this part control net so is controlling the main branch so we have two branches control n controls the main branch and the main is L can that is a trainable copy of the uh
(21:33) original Branch uh these are zero initialized one by one convolution such that uh the 20 is very smooth the starting point is basically equal to nothing is on this branch and we only have this locked R TR model so let's dive One Step deeper to expand that unit so we add this pixelwise conditions from here on the right hand side it could be either the semantic mask P Edge whatever pixelwise information you want to insert and we keep the original condition including um the text prompt and also the time step so that's the uh original condition
(22:24) we copy these down sample stages so these are the down sample stage uh we copied the uh condition control Branch from the original Branch while we are freezing all the layers of original branch and then for the um up sample part we just pass it through zero convolution so we only copied half copied the first encoder block but not the decoder block okay so this is how we uh add the pixelwise condition beyond the text condition and also the time condition which has been added to the original branch which is frozen and this
(23:10) condition is also inserted at each incoder stage of the uh control net Branch okay so there's another technique classifier free guidance uh which is try to uh to trade off between the diversity and also the fality okay so to condition on some class uh we are trying to given this class we try to predict what is the image but we can also TR another classifier okay given the image try to predict the class we want to make sure uh the class is exactly one we want to generate okay so at we can add the classifier graded during the sampling
(23:57) okay so make sure the sample is classified as C so we not only want to run this generation model but also we want to run this discriminate model make sure the generated image is exactly belong to the class so the new sample distribution is defined this way so given class we want to sample image and also the predict this image is also predicted as to be belonging to Class C and we add Omega to the power of Omega which is guidance strength in the log space uh multiplication becomes the S and to power of Omega becomes Times log P
(24:47) okay so that's why we add Omega to the power place so this is basically the uh naive comparison between naive conditional sampling yeah have from I here versus classifier guided s plan which is adding another term to given this uh image we want to classify the class and make sure that belongs to C so this is the classifier gradient and the Stu is controlled by this guidance Stu so that's the uh classifier guided sample so this is comparing um a small Omega versus a larger very small Omega the generated image is
(25:37) sometimes confusing it is not darkg but on the right hand side uh fality is much better so uh larger guidance of w lead to better quality but less diversity so the diversity is very poor versus here has many diversities on the grass and in the snow no but here diversity is much poor so what is the limitation here by using such classifier guidance uh we can only work on class condition okay there's no Tex or pixelwise condition since we have to predict a classifier and we need to tr additional Network which is a classifier network
(26:22) which just comers can we use one network and no have to train a separate classifier Network so that's the uh classifier three guidance a very widely used technique these days to support diverse conditions including the class condition text condition pixelwise ET without using an actual classifier next so this is the original term defined in the classifier guidance has two terms multiplied together when Once one is given a condition predict the image the other is given the image make sure it's belong to time C Omega is the St we
(27:09) can apply a base rule okay such that this term given the XT um with probability of the condition we can rewrite it to be given C with probab of XT versus pxt okay so this is the condition model this is condition and putting them together that's base rule base rule so basically we can run the inference twice one is the conditional model the other is uh unconditional model so in that way we can rewrite inion at the top by inserting the p c given XT to be P given C / by pxt and to the power of Omega we still put it here and find
(27:59) these two terms can be combined together to the power of Omega plus one so we are combining these two term together since they're the same we have omega plus one the denominator we just move them together move them to the right power of Omega so in the log space uh d this division becomes a minus and subtraction so we have omega + one um log pxt g c minus Omega log plus this condition okay plus C so how does what does that mean to the original model OKAY previously we are uh having the condition under the T right here um
(28:47) now we just replace it with two terms inference it twice okay one is given the T and C the other is given only T no C so we have to run the INF twice okay combine the results of conditional and also the unconditional forward and what does that mean we need to double the flaws okay do it once with the condition and do it another time without the condition so that's basically the classifier free guidance and let's see uh how that works so on the left hand side there is no guidance this doesn't look like a dog but on the right hand
(29:31) side it looks like a dog so larger guidance W is also leading to better quality but less diversity Advantage is that it can generalize to any condition forms not just a class condition can generalize to different conditions like pixel condition or text condition and we don't need to tr a separate Network we don't need to tr a classifier okay just uh the same diffusion model we just need to run the diffusion model twice so all the techniques we learned so far are gradually adding complexity making it more computationally expensive
(30:12) like adding a condition Etc and later we are going to talk about how reduce reduce the computation flops starting with latent diffusion models imagine you have a pretty high resolution image uh which means when we are predicting a noise have to make pixelwise prediction there a lot of pixel noise pixels to predict and we passing through a encoder uh and then Deno a small at smaller resolution like 1/ 18 or 1/4th a resolution so that you can have a much smaller compute actually um that is a very widely used technique these days
(30:51) called the lat diffusion rather than directly to diffusion on a high resolution image with first T passes through a uh encoder to down sample by four times eight even 64 as our recent work called deep compression out encoder so we have a much smaller uh latent space and you do diffusion in this smaller space which is much easier and much faster and after you the noise we pass it through a decoder okay to reconstruct the original image so the diffusion only need to run in this smaller resolution latent space rather than on the high
(31:35) resolution original image space so that's a way to compress the image and only run diffusion in the compressed space and that is called um latent diffusion okay only apply diffusion on a small resolution part uh to enable simpler den noising and also faster synthesis and let's see how that changes our um road map so this is the original configuration uh in the difference is that the image is fed to encoder and we feed the encoder's result as Z okay so uh we first added the Noise Okay uh L in space added the noise this is
(32:22) the sum passes through the model and predict the noise and try to match noise okay so the learning Target is exactly the same only difference is that we added another term rather than directly feeding x0 so here we uh feed x0 to encoder and get Z z0 feed the z0 uh to the model predict noise and z0 is in the latent space as a much smaller resolution so this is during The Swinging the forward uh what about during the s at sampling time so everything is actually the same except for the last one we want to feed it to a decoder decode the original
(33:07) image so uh previously we have this XT and then we pass it through the model to predict the noise and we subtract noise from this XT and we do D noise noise we added another noise look at the final noise ZT minus one so this is from ZT to ZT - one all the way z0 and after we get z0 in the latent space at a smaller resolution we pass it through the decoder and reconstruct the original image so the only change is right part the decode passes through a decoder from low resolution to high resolution everything else stays the
(33:49) same but we can now generate much uh complicated and higher resolution image like here a astronaut in jungle code color pette and here is a DC D floating this is stable diffusion Excel for high resolution image generation as you can imagine um the more we compress the less workload the smaller the number of resolution and the smaller the number of tokens so recently we invented this deep compression outut en coder rather than compress the token by eight times we can compress it by 64 times what does that mean it means eight times more
(34:35) compression in each side and Al together we have how many fewer tokens 64 times fewer tokens because H times W two sides so 64 times less tokens and how does that impact the attentions uh complexity 4,000 times L flops right since the tension computation complexity is quadratic quadratically related to the number of tokens we are reducing each side by eight times so Al together is 64 times less tokens and four 4,096 times less flops okay so this is original stle diffusions uh variational a incoder vae reducing the resolution by eight times
(35:27) passes through diffusion model pretty slow um this is our proposed approach using deep compression out to encoder 64 times compression on each side as say through diffusion model and make it much faster however this is not straightforward um now you approach for example ibae and 64 times of compression the quality is very poor but using deep compression V the quality is much better on the left hand side we are comparing the compression ratio higher the compression ratio more preferable but the harder uh Baseline
(36:05) methods sdv the IID deterior rates very rapidly but the Deep compression Al encoder stays very stable FID even given 128 times compression ratio and how did we achieve that so reducing the resolution you have a very naive approach right you can directly uh move the information from spatial Dimension to the channel Dimension say every 2x two pixel four pixels you can change it from 2 * 1 into 4 so that's the explicit transformation from spatial to channel okay so by explicit spatial to channel we can add this explicit special to channel Branch um
(37:03) the down sample block and add this explicit channel to spatial Branch the upsample block we call it regid Auto in coding so rather than using the weight man weights to learn such stuff we use this uh zero weight branch no zero weight Branch uh as a residual bypass um to learn this and we find add adding this regid out encoding uh makes it aot a lot easier for example the rfib without regid Auto encoding will deteriorate very fast in f64 but it remains very stable under high compression ratio a second technique is this three
(37:52) stage adaptation to high resolution so we want to deal with high resolution like 4K image generation so how do we have a auto encoder to support so much higher comput uh resolution without requiring a lot of training flops so we decapo it into three phases the first phase we just perform low resolution between the whole both encoder and the decoder okay given input image encoder uh encoded into latent space and the decoder reconstructed to the original image Okay so this is low resolution full training and in the Second Step In order
(38:33) to save the computation uh we perform high resolution latent adaptation only to the um middle layer okay so the correspond to like classifi last layer only tune these M middle layers and Phee the remaining layers finally we added this G loss uh to further polish to do some refinement only on the to last layer of decoder since that's only related uh to the low level features the feature is actually refined in the last stage so we do such layerwise sparse tuning not tuning all the layers but Spar tune either the middle layer or
(39:17) late stage to save the computation and make it very fast to tr such a auto encoder so using this Auto encoder we can greatly accelerate the uh image generation diffusion models so this is using the original uh lat in Space STV which is eight times reduction so it's pretty slow it generate only 17 images per second but on the right hand side uh we are using the 64 U times compression okay f64 reducing the number of tokens from um reducing the resolution from 512 to 8 by okay so we can diffuse our only 64 tokens compared
(40:07) with previously we have to diffuse um 4K tokens so it's a lot faster 19 times faster actually compared with using the original um Auto encoder so matters a lot if you can apply such deep compression Auto encoder you can have much fewer tokens and quadratically reduce the computation flops or inference of diffusion models and another benefit is that now we can uh achieve high resolution image generation without using a lot of computes for example 4K image generation look at the ship look details how much detail it have okay this is 4K by
(40:50) 4K generation and compare it with 1K this is asona in the jungle uh 4K has a lot more details but computation if you don't use techniques like deep compression Auto encoder So based on deep compression Auto encoder we build a efficient image generation diffusion model called aana um which takes advantage of several Factor factors one is the um Linear B module using the linear tension that we previously described for high resolution we have a lot of tokens and computation complexity grows quadratically with the
(41:35) resolution so a linear attention will be very feasible um for will be very uh helpful to generate high resolution achieve speed up so this is the using the linear attention this is the using the linear di module using the Deep compression Auto encoder and here uh it's using modify complex human instruction which we are going to talk about that so previous uh diffusion models are using encoder decoder model like T5 as text encoder so uh we think recently there's a lot of progress decoder only large language model like um
(42:22) like GPT model gpt2 gpt3 uh 3.5 everything after that or Google's Jamma okay so um can we use this decoder only model as a text encoder for example given a input to the Jamma a man is walking um the jma's output focus on conversation he says this is a great start to make this more interesting tell me about the man and his walk for example where is he walking a busy cing street what is he carrying his mood the more details you give Vivid and engaging the story can be so this is not quite related to image generation if we naely feed or prompt to
(43:08) drama it is not basically related to image generation but focusing on conversation and storytelling uh in contrast we add a complex human instruction append this fixed human instruction before the actual prodct a man is working and now demma can give correct output saying a man in the war leather jacket Works brly down a Cobble Stone Street he had D cast he situ framed by the Setting Sun he wears a faded blue scars around his neck Etc give very detailed description beyond the simple um instruction we gave and how do we achieve that this is using
(43:54) the prompt engineering we discussed in the previous lecture so this is the complex human instruction the feed um to drma okay um not only the example only the prompt but also example for incomplex learning given a user prompt generate a enhanced prompt that provide detailed visual description suitable for image generation evaluate the level of detail in the user prompt if is simple focus on adding specifics of colors shapes sizes textures and special relationships to create a vivid and concrete scene if the prompt is already
(44:36) very detailed refine and enhance the existing details slightly without over complicating we also give some examples um user prompt a cat sleeping we converted to enhanced version a small fluffy white cat C up in a round shape sleeping peacefully on a warm sunny uh window sill surrounded by uh parts of bloing red flowers by using the Inc Contex learning example uh with two user prompt and finally please generate only the enhanced description for the prompt below and avoid including any additional commentary on valuations okay followed
(45:16) by our real human crop a man is walking okay and in that way uh we can let this decoder only language model to give the output that is uh preferable for the diffusion model to consume okay all right and how does it Echo with the visual language model that we previously discussed for example the V visual language model actually we can use V to recaption the image so given the image we want to um this is the original caption which is actually very very short um top view written happy valentine on the tart poate cake like f
(46:05) the background very simple right but using V our visual language model it can give very detailed to capture like this is VA 13B the image captures a delightful scene of a Valentine's Day cration at the center of image is around Chate T reach and inviting the cake is adorned with a red heart on top symbolizing love and affection the words happy valentine are elegantly written in right icing on the cake adding to the festive atmosphere cake is placed on a wooden table um yeah it's indeed a wooden table which provides a rustic charm to the
(46:50) setting so you can tell it's a very detailed description um that is unlikely to be labed by a human labeler to give such um detailed and U uh fancy label without hallucination so this is the 13B 3B uh we feed it to different sides of V to um vote based on their clip score to prevent hallucination and try to find the best the match clip score basically measures the similarity relationship how well does it match between the caption and the image so now everything begin to converge by using our visual language model the V we can generate more data
(47:39) can use the data to train better Sana so s and V can uh grow together so that's the beauty of such code design all right uh now we have to build the Sana based on the above techniques we can accelerate Sana uh with accelerate 1K image Generation by 25 times accelerate 4K image Generation by 100 times compared with the flux ST which is a popular uh diffusion model these days is flux thisd sd3 pixel art s okay and where is this 106 times um acceleration come from so it comes from DP compression Auto encoder uh linear dit kernel fusion and
(48:29) also using the uh flow based DPM software and the quality wise stand L previously uh and this is the for flx model this is for the s uh they are almost neck and let's see some generated results from Sana 1.6 B the text logos and the human face actually a lot of details and here are some more examples like a blue porch 356 parked in front of a yellow brick wall here this one is probably more interesting a avocado wearing royal crown and a robe Seated on thrown in grand opulent setting the scene is surrounded by other
(49:26) avocados also adorned with cross so high resolution low cost so let's continue to talk about image editing so image editing with diffusion model is pretty powerful so for example here we want to give some in input Strokes here a power room and now we want to gradually recover and generate a power and a room by the noising and it should be match the input stroke and how do we that so this is these are separate spaces one is the stroke image uh they are in different locations so what we do is can perturb them with noise so adding to the stroke
(50:18) such that the noisy uh image can have some overlap with the original uh space and then we to the reverse process gradually uh denoise we can move this blue dot closer and to the center of the uh to green region and which is the image region space so that uh the output look like a castle in this case so this is some example given original uh this is drawn probably by a human and then uh give a prompt the fantasy landscape finding art station and this is the generated image given the input stroke we also want to personalize the
(51:07) model okay not only addit the model but also personalize the model say given a dog and we want to generate some dog swimming sleeping in a bucket for this particular dog right it has gray hair L ear and white fls will formulate exactly the same do for follow the exactly same do like getting a hair card this is not random dog but we want it to be exactly this dog how do we that actually this is pretty challenging for example if we input a few images of this talk uh the same clock in different setting um the generat image have a
(51:50) proor fality like this BL no longer look like this one the color three should be yellow rather than white and also very few context they're both white background versus less fality but able to generate new context the three has changed versus both having good fality and also diverse context and dream Booth achieved uh this functionality by fine-tuning the model so we can given a text to image model given a few pictures like three to five images of the dog plus itself the language dog and passes through the dream boost to F tune that
(52:37) we got a new personalized text image model and this unique identifier V then we can say a v dog is in the beach it will generate this particular dog in the beach a v dog walking on colorful carpet and then we can generate this personalized uh dog the carpet so limitation here is that one fine tun the model it's specific is to only one subject so if you want to generate like a person friend playing basketball you have to do the fine tuning again for your friend and which is costly if you are interest interested in uh truning free methods
(53:20) there's plenty of uh Publications later using training free methods to customize um to P perform personalized image generation okay now let's Swit switch GE to talk about fast sampling uh technique the noising uh diffusion implicit models so the noising process is iterative approach right from the noise to data the idea is that can we use fewer steps to have a larger stride reduce the number of iterations okay so um previously the method is movi so each XT is only dependent on the previous time step and we treat the model such that
(54:05) only use these properties to the training but at difference time can we find nonar forward process to accelerated um by re using the same model during the training time okay so um we want to make sure the XT minus one is not only related to XT but also related to zero can we find a such setting that we don't have to change the train model but just change the inference change it at the inference time actually we can do that uh this is the uh conclusion so previously XT minus one is related to XT but now it's related to not only
(54:48) XT you noted in this epsilon which is related to XC but also related x0 okay and we can estimate zero with this equation so in that we can have a larger stride time stamp from 0 10 20 30 a th000 uh previously we just make one time step increment now we can make the increment of 10 to Accel the noising process and the original ddpm if you reduce the number of steps from a th000 to 20 10 the FID will explode by using TD the stable stays very stable until like 10 or 20 steps still below 10 this is a cyar 10 data set on
(55:37) the c a data set the FID is both well maintained uh well having much fewer something steps like 20 opposed to a thousand steps another approach to reduce the number of steps is by using distillation so this is a four step model we can distill that such a single step from the student Network can do same as two stabs in the teacher Network okay we can do another generation of distillation such that the student student uh which is can directly use one step to model what the teacher have done in four steps and so we can deter
(56:22) distill a deterministic sampler to the same model architecture and each step the student model uh is learned to distill from two adjacent sampling for steps from the teacher and every new student will reduce the number of samping steps by two so in this way we can reduce the training steps but the FID stays much lower compared with the PDM original non Ste version okay uh chapter three now let's talk about acceleration techniques for diffusion models let's start with using sparity we can exploit spatial sparity
(57:08) to accelerate the inference of diffusion models in particular for photo editing the vanilla diffusion models waste many computations when we have to resynthesize the entire image even if we only addited a very small portion of that say in this case we only addited 1.7% of the pixels you can tell where did we add it we only added a sign right here a sign right here that occupied less than 2% of the pixels but original stable diffusion has to go through U the entire um model and uh run on convolution on the entire input image
(57:58) okay but um spatial response inference is basically saying we can just uh change whatever we edited for the not edited region we just reuse the feature M from the original model okay since we only added the sun we only recompute sun in green here and we use the feature maps for the rest of areas uh for the previously generated image and as a result uh this can save eight times the computation so this is how it works we build this special incremental generative engine s incremental means only change the pixels that has been
(58:40) edited for those pixels that has not been addited we just reduse whatever it was from the previous uh approach the previous iteration and so given the original image and we drew some piece of cloud on the top and this is difference Mass we discretize that to uh 4x4 grade in this case and highlight the areas that has been edited so this is the original image um this is edited image so we only gather these pixels these uh these chunks that has been edited and then only pass it to the convolution for this edited part okay
(59:23) and we scatter this to the original feature now to get updated the feature map so that's the one layer of uh S which only do compute which convolution on edited blocks we call it active blocks and we stack them to the batch Dimension so it's easier to paralyze no Spar kernel is needed since each patch is do dance convolution and this can also apply uh to diffusion based Transformers and we applied it to image in painting so which what is image in painting so we can draw a box in the image inside the image and we can paint
(60:04) whatever we want to the box now to paint a horse photograph of on grass L previously there is no horse now we draw a box and want to place horse right here uh without this approach we need to Output generate the entire output image which takes a lot of compute pretty slow three uh 3 minut uh 300 seconds uh now we only added the the mask the region okay which is three times faster four times and the quality match very well with the original input so this is another example we only addited 3% of the pixels added
(60:44) coconut tree so this is the Baseline model this is our model the generative quality is pretty much the same well be in four more than 4X classroom so this is the real time demo doing that this technique can enable uh this interactive photo editing for example adding some Cloud to the image okay now let's switch GE to talk about applying quantization to accelerate diffusion models so we've talk about quantization using quantization to accelerate large language models but large language model and diffusion model have a lot of
(61:23) difference for example on single batch inference large Lang model is Bott NE by the memory bandwidth while the diffusion model is Bott NE by the compute so those methods like wait only quantization can not accelerate diffusion models because they're compute bonded rather than memory bonded if it's memory large language model can benefit from like w 4 a16 weight only quantization but for in order to achieve measured speed up for diffusion models we need W4 A4 quantization quantize both the weight and activation to only four bit without
(62:01) losing fality which is even more challenging than W4 a16 and let's see how do we solve that problem so observe um the weight and activation both have a lot of outliers in diffusion models we have seen this figure sister in smooth Quant lecture so this is the distribution the green is 50% per the yellow is 99 percentile and the remaining is the rest 1% so red part is the outlier so activation has a lot of outliers right here and the weight also has a lot of outliers which make in both difficult conts so we can apply smoothing like
(62:47) did in smooth Quant to migrate the quantization difficulty from the activation to the weight activation is much smoother a lot less less red compared with here but the activation has more outliers previously has only one outlier now has two this is a real distribution a lot more red compared with previous but luckily the activations outlier is migrated so we just need to uh migrate the weights outl we can do that by decomposing uh using a side branch which is a low rank Branch to absorb those outliers using full
(63:32) Precision but this is a very low rank on 32 ranks so this is very cheap we can migrate the outliers migrate condensation difficulty to decide the branch such that after SVD uh the weight has a lot less outlier compared with before so finally both the weight and activation are four bit but it's very easy to quantize there's no outlier and associated with a very low rank Side branch and the good thing about this approach is that if you use um lower fine tuning you don't need to re quantize the uh weight um but you just
(64:15) need to append a few more ranks like 16 or 32 more ranks in full Precision by uh whatever Lowa you want to add this modu since a lot of people are using diffusion models with Lowa so this is very compatible with the framework before that we despite actually observed naive implementation lead to a lot of overhead like the yellow and the red region for this up and down Pro projection due to the Lowa Branch this is the main branch um this is the Lowa Branch uh we fuse such overhead such that the latency overhead 40% latency overhead is removed
(65:04) and we achieve that by actually fusing this D projection with the previous kernel since they share the same input and we fuse this up projection to the next layer forbit compute since they share the same output okay and in that way we can uh reduce this overhead in red in yellow so this is the kernel Fusion so the speed up um and the model size reduction so the speed F16 we can reduce it by 3.
(65:39) 6 times the w416 method conventional M4 can also reduce the model size um and also reduce the memory we can reduce a bit more since we also coning the activation however this is the beautiful part okay so this exciting part where the conventional W4 a16 work can only reduce the memory but it doesn't lead to any speed up it's actually slower because they still need 16bit arithmetic but here we can use four bit arithmetic either in four or fp4 and can achieve three more than three times measure the speed up on 49q okay and this is a single step
(66:16) latency the Baseline method is out of memory and the N4 we can make it a 3.5 times faster than the N4 approach so this is the visual visualization comparing the Baseline approach uh it's all of memory on a 490 GPU okay and this is for very high fality but it doesn't have any uh it's still more than a second it's pretty slow well we can make it just half a second um this is the interal quation so the quality is really poor actually the quality already start to degrade from ip16 to N4 approach but our can well maintain the uh output quality
(67:07) pretty much the same as original input while the memory is reduced uh previously 23 Gaby now it's only six five Gaby half a second compared with 1.8 second and this is showing that SBD Quant is very compatible with the existing Lura we can just add a Lowa Branch without having to do any requantization okay so this is ip6 and our info version using different Laura uh models like realism pretty much the same um the anemy children yarn different kind of styles well maintain the fatality for different um Laura models
(67:56) finally we can apply paradism to exploit multiple gpus to accelerate diffusion models for language model we talk about we can use tensor paradism to accelerate it by throwing more gpus we can just make it faster what about for diffusion model is how can we throw more gpus to make it run faster so these are two devices we have one GP on the top one bottom we wanted them to work together generate the image fast however these two models have to communicate to do the attention attend to each other which will lead
(68:39) large communication overhead which will don't want it will also uh um negate the saving we may get from multiple gpus so we want to reduce the number of communication the inside here is that the input across adjacent time steps are actually very similar to the uh input here the input here they are very similar to each other so rather than directly communicating we can actually communicate using the old stale activation for those communication when we are attending to each other we can attend to the previous time sta that's
(69:18) totally fine that's the insight and we can exploit that to reduce the communication overhead and also enable the communication to overlap with computation since we are reusing the old time steps feature so originally the latency is 12 seconds four gpus naely doing such paration indeed make it four times faster but it will generate four separate images due to lack of communication and the duplicated uh subjects this is naive paration across four gpus but using the distribution distributed the diffusion we can make it
(69:58) four times uh three faster but the generating image quality is very good compared with the original one rather than generating four separate images separately so this is the speed up across one two four eight devices about more than 2x for four devices here uh on higher resolution the speed up is even higher due to the uh communication overhead is saved with up to eight gpus so I 3K generation is about six times faster all right so here's the summary for today's lecture we first introduce the basics of diffusion models um and
(70:46) then we talk about how to add condition how to diffuse in the Laten space including using deep compression Auto encoder to compress the tokens how perform image editing and personalization and then we talk about fast assumption techniques DV and dation together with acceleration techniques using uh s the spatial scarcity um SVT Quant quantization technique and also distribution distributed uh diffusion by using paradism that's all for today's lecture hope you have a long week happy weekend
