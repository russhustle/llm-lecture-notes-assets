Now let's switch here to talk about absolute positional encoding versus relative positional encoding. Absolute positional encoding fuses the positional information into the input embedding, as well as the Q, K, and V. So the information is going to propagate through the entire Transformer since input embedding is the very source, the very beginning of a transformer. But relative encoding keeps an eye on the relative distance information by impacting only the attention source—not the input embedding, but the attention source—by adding a bias or modifying the parameters of keys, not the value. Since the attention map only depends on the query and the key (QK transpose), an advantage is that you can deal with long context. For example, reading a whole book or a very long context. During training time, we may not have seen such long contexts, but we want to train short and test long. That's the advantage of relative positional encoding: it's very easy to extrapolate for long context at inference time.

So we'll introduce two kinds of relative position encoding. One is attention with linear biases, i.e., position positional encoding. The other is RoPE (rotary positional encoding).

Let's start with the first. The idea is to change the position encoding to be only related to the relative distance. For example, Q1 and K1: relative distance is zero. But Q2 and K1: the relative distance K minus Q is one. Q2 and K2: the relative distance is zero. But here, Q4 and K2: what is the relative distance? 2 - 4, so it's minus 2. Similarly, Q5 and K4: the relative distance is one. So we have this extra potential map, which is offset, which is the offset with the original attention map. To add this offset to the original matrix, we define this as the relative distance. Instead of adding to the input token embedding, the input token doesn't change, but only the attention map changes. We set this strength, which is M, which is multiplied with this positional encoding and added to the original attention map so that you can capture the information between the relative distance between two tokens. So although, for example, some of the tokens multiplied with each other are the same, adding relative position encoding, the result might be different.

The second one is slightly more challenging, more difficult, but this is very widely used. For example, Llama basically uses this rotary positional value. So what we do here is we want to rotate the embedding in a 2D space. In order to rotate it, we first need to cast this very long, like 4K, embedding into two dimensions. How do we do that? Split the dimension from D to D/2 pairs. So we have many pairs, two groups: the first half D, second half D, we put them together. Now, the first one becomes a 2D value, X1 and X2, where we can represent X1 and X2 in the 2D space. Then we are going to rotate by m\*theta. m is basically the index: first entry, second, third, sixth entry. If the total hidden dimension is 12, the largest position is six, since we divide by half and put into a 2D space. So m is the position of the token, and here this index is the position of the hidden dimension. So we rotate it, and therefore we add this positional information into the original representation of Q and K.

Here, we need a pretty big number to distinguish different tokens. The theta representation is 10,000 to the power of minus 2i-1/D, where D is the given dimension. Why do we need a pretty big number? Since each time when we rotate, we want to rotate by a pretty small value. Otherwise, if you rotate by 2 pi, you come back to the original place and can no longer distinguish these two vectors. Once you rotate by 2 pi, you come to the same original place. Therefore, we just want to rotate it by a little bit, so we have a pretty large value here in order to distinguish more tokens.

What if we use this approach, the rotation approach, and you just rotate the representation by grouping every two values into a 2D space and then rotate it? What if we multiply the Q and K using the rotated value? So, this is rotating the Q and rotating the K. They are coming from different locations: one is m, the other is n. Here, we are looking at the j-th hidden dimension. This is the original Q and K, rotated by `m*theta` in this case and `n*theta` in this case, because m and n correspond to the token numbers. This is related to the hidden dimension, and this m is related to the token number: first token, second token, third, etc. This is within the first token, the second token. What is the impact of the embedding? You may have a 4K embedding divided by two; it can range from zero to 2K. The phase angle of the inner product of two complex numbers is basically the phase difference between these two complex vectors. So you can multiply them together. Here, we multiply the Q and K together, so the phase value would be the difference between the phase value of Q and the phase value of K. That's why here we have minus n. Minus is basically the relative position. That's why we call it relative position encoding: it only depends on the difference between m and n, the token numbers between Q and K. It's a bit complicated here; I'll pause a little bit to answer questions.

So, let's say you have 10 words. You have the same word appearing in location one versus location ten. Previously, you could not distinguish that. Now we want to distinguish whether this word appeared in position one or the last position, and that can be distinguished by the rotation. You rotate it by m\*theta. m is basically your position, whether it's 0, 1, 2, or 10. That vector will be rotated by a larger angle if m is large; if it's zero, it will be rotated by a smaller angle. The relationship between Q and K only depends on the relative location; it only depends on m minus n.

Now, what is the general form? Previously, we were just multiplying Q and K; that's very easy. Now we are doing the rotation by working on each pair of the values. By each pair of the values, we are rotating them by `m*theta_1`, `m*theta*2`, ..., `m*theta*{D/2}`. Why? Since we are grouping the total hidden dimension D into two groups, each group has D/2, and we put the first one and concatenate them together in the 2D space. That's why we have D/2 right here. That's the general form of this rotation matrix, and we multiply this rotation matrix with the original matrix. That's a very simple implementation.

What is good about the RoPE positional encoding? We can extend the context window, so we can process very long contexts, such as very long documents or books, long movies, for example. Like Llama, the original version has a 2K context length; Llama 2 becomes 4K. GPT-4 used to be a bit smaller but filled with the larger context length. So we can actually extend the context length by interpolation. For example, in the case here, previously we rotated by `m*theta`. If we are doubling the context length, we can rotate by `m*theta/2`. That's the intuition: we should interpolate. This is the original pre-training range, and we want to fit the same range. Previously, it was from zero to 2K; now we want to interpolate from zero to 4K, so that we put more, doubling the frequency from here to there. We are interpolating a lot of dots in between them, so now we are falling into the same range rather than falling into this increased range during training. Rather than using this range, we want to change theta to be theta divided by two so that we can fit a longer context. Here, the blue part is the original context length; this green part is the interpolated context length.

So that's positional encoding. Now let's talk about the KV cache. We'll introduce multi-head attention and also the grouped query attention. The KV cache can be pretty large in long context cases. Let's see this example: we have key and value; we have three words here—think of three tokens: "I love cranium" in this case. This is embedding x0, x1, x2, and we project into q0, k0, v0; q1, k1, v1; q2, k2, v2. Then we calculate the attention score given this new word. We calculate the attention score by multiplying Q2 with k0, Q2 with k1, and Q2 with k2. We use this as a weighted sum for the values: v0, v1, etc. So we need to store those keys and values for all the previous tokens so that we can perform the attention computation.

In the previous example, when we are calculating for the second x2, we need to store the K and V for the zero entry, for the first entry, etc., so that we can attend to and calculate this attention score like 0.3 and 0.1. So we need to store these keys and values for all the previous tokens somewhere. Originally, if we don't have a KV cache, we need to recompute each time. Here we have four tokens: token one multiplied with k1, and we have to compute everything each time—from 2,1 k1, 2,2 k1, 2,3 k1, etc. With a KV cache, we don't need to recompute. Given a new token, now we only calculate everything related to that new token, and for all previous tokens, we already keep it somewhere. Rather than recompute it, we only recompute for the new token and then store it in the KV cache. Finally, we can multiply with the value to get a weighted sum result. So we only need the current token and only calculate the key and value for the current token and store everything before that in the KV cache. As a result, the KV cache will get longer, and as the sequence gets long, the KV cache will grow proportionally.

How do we calculate the size of the KV cache? We can calculate it in this way: the KV cache will be, first of all, you multiply the batch size, each layer you have independent KV cache, each head has independent KV cache, and this is the hidden dimension of the KV cache, like 128 for each. So 128 multiplied by 32 heads, altogether we have 4K being the dimension. This is the length of the sentence, and we multiply by two because one is for key, one is for value, and two bytes because fp16 is two bytes. So actually, that's half a megabyte or kilobyte times the batch, the sentence length. For Llama 23B, the layer increases from 32 to 40, number of heads also increases from 32 to 40, the hidden value per head stays the same, 128, and two bytes, so 800 kilobytes times batch size times n. Similarly, for the 70B model, layers double to 80, KV cache also increases to 64, so altogether we have two and a half megabytes per batch size and per token. Actually, that can get pretty big. If we plug in some numbers, if the batch size is one and the sequence length is 52K, that's actually more than one gigabyte to fill the batch. If the sequence length is 4K, which is roughly a paper, it will take 10 gigabytes just to store those KV caches. Not to mention if we have multiple users, like 16 users trying to read books or 16 papers at the same time, it actually requires 160 gigabytes. Each A100 GPU has 80GB of memory, so only storing the KV cache for 16 users will require two A100 GPUs. Each A100 GPU is pretty expensive, so that increases the inference cost. It can also actually exceed the model size. It grows much quicker; the model size actually stays the same. This is how we compare the model size and the KV cache size. The KV cache size in blue for multi-head attention, this is the batch size. As the number of users increases, the memory occupied by the KV cache will grow linearly, even surpassing the model size.

How do we deal with that and save the KV cache by sharing some of the keys and values? That's the idea for multi-query attention. Compared with multi-head attention, you have n for query and also n heads for key and value. Therefore, when we are calculating the size of the KV cache, we are multiplying with a very big value. The KV cache has what we can do here: simplify n heads into just one head for query. We still have n heads, but for key and value, we just draw one copy. We just draw one copy of key, one copy of value, and share with the multi-head attention. So this is multi-query attention: Q stays the same, you still have a lot of queries, but keys and values are shared, you have just one part of the key and value. Unfortunately, this will lose a lot of model capacity, so the trade-off in the middle is the GQA. GQA stands for grouped query attention. You have many queries still, like before, but a group of them, like a group of two queries, share the same key and the same value. So a group of two queries share the same key and value, and as a result, altogether you have eight query heads, two of them share key and value, so you have only four keys and four values. Typically, the ratio between group versus total number of query heads is eight times: every eight elements you share the same key and value. This is pretty similar to the normal convolution versus depthwise convolution versus group convolution—a trade-off between the model capacity versus the computation.

Let's see how GQA and multi-query attention help, and which one will be the lowest. Multi-query attention: you have one copy of the weights of the key and value. This is multi-query attention at the bottom: number of heads is just one. The grouped query attention: you have eight heads versus the four in multi-query attention, and multi-head attention has 64. So that's the difference between these three different architectures with respect to the KV cache size. When the batch size gets longer, the reduction is pretty significant. Previously, it required 320 gigabytes for 70 billion, which cannot fit a single node, but now it's below 80 gigabytes; it can fit a single H100 node.

Let's see how the accuracy is impacted. Actually, GQA can match the accuracy of multi-head attention at very large model sizes. Here, comparing multi-head attention, multi-query attention, and GQA for different benchmarks, you can see the multi-query attention sometimes loses a little bit, but GQA does pretty well, sometimes with higher accuracy. Therefore, in later networks, GQA is very widely used.

The last change is actually from the FF layer into the gated linear units, the GLU layer. The difference is that FFN just consists of two simple matrix multiplications, with the such kind of inverted bottleneck with the ReLU or value in the middle. But GLU basically has two differences: one is there are three matrix multiplications—one, two, three—and there is a gating element-wise function, calculating the element-wise product between these two tensors, and then passing it through another matrix modification. And Y here is A over three; now three of them are A over three, A over three, A over three, same as four. The second difference is here is using a different activation function, the SwiGLU activation function. Activation function: first matrix modification, element-wise modification with another matrix modification, and finally do another matrix modification. These two matrix modifications can be fused into a single one. Complexity- and result-wise, you can see that using the SwiGLU is actually giving pretty good results. Here is comparing different activation functions: this is the SwiGLU function, this is the Gated Linear Unit activation function. They look very similar, but there's some subtle difference between them. If you want to go deeper about this SwiGLU function...

All right, so with that, we can introduce some of the modern large language models. They are improving pretty fast, scaling up. People find scaling the model can have very good, many different capabilities. For example, we call it emergent ability. Scaling up the model will unlock more capabilities, despite that it's taking a lot of data and memory. This is growth in memory as the model size increases. Why do people need so much model size? Because they can have a lot of cool capabilities. For example, here we give it a very interesting task called modified arithmetic. The symbol represents a simple mathematical operation, like 100 plus 200 equals 300. 1 basically adds one to the original answer: 1 + 3 into the 4, 2 + 3 into the 5. So this is modified arithmetic, and this is the accuracy of this new task. When the model is small, the accuracy keeps very low, makes no change, no improvement, until a certain point when the model gets much larger. This is measured in the model scale, measured by the training FLOPs, through a lot of GPU compute. Suddenly, the accuracy gets much higher; suddenly, the emergent abilities will appear. Similarly for others, like word unscrambling capability, suddenly increases when the model is larger. What is word unscrambling? "HTE" is a scrambled version of the English word "THE." So similarly, the word "SOH" is a version of the word "SHO," just reshuffled the order of the word, trying to decipher what its original word is. So that's the word unscrambling, and the ability doesn't appear until the training FLOPs exceed a certain threshold. That's why we need large language models: due to those emergent abilities.

GPT-3 basically split out up to two-shot learning. They then enabled in-context learning capability. So these are very important concepts. What is few-shot learning? It's in-context learning. In order to learn a new task, previously we had to fine-tune the model, pre-train it, and then fine-tune it on the specific task that is desired. For example, translation: we give an example one to the ground truth, calculate gradient, update the model; another example, give ground truth between these two languages, do the gradient update, and finally we get a new model, feed it into a new prompt, and try to get a new answer. So that's learning through fine-tuning, which requires backpropagation—pretty expensive. People found that once we scale up large language models to a much bigger size, we can then do new tasks without having to fine-tune them, but through zero-shot learning or few-shot learning. Zero-shot learning is basically saying: just directly tell me the description of the new task, and then we can achieve new tasks. Few-shot learning basically says: given a few demonstrations, given a few examples, without telling me what the task is or how to solve the task, it can help you solve the task. For example, this is zero-shot learning: predict the answer given only a natural language description. Here, the description is "translate English to French," and then give the English, it will predict the French. So this is no gradient update; we never trained on many English to French pairs, we never trained on that. Few-shot learning, in addition to the task description, we also want to give it a few examples—examples, example English-French pairs—and then given a new word of English, it's going to predict the French. So that's few-shot learning, and these large models, when the model gets larger, the few-shot learning capability begins to appear in this area. So there are 175 billion parameters; this is the accuracy, this is the number of examples in the context. As you give it more examples in the context, accuracy will emerge, will increase for these big networks. But for these small networks, no matter how many examples you feed in the context, accuracy doesn't quite improve.

This is the GPT family, actually improving pretty fast. The latest ones are not released, so we don't know. Then the open-source versions quickly caught up since about 2022. The first one is the OPT model; they open-sourced transformer large models, many open-source different sizes ranging from 125 million to 5 billion. This is zero-shot capability with the few-shot capability, and then as a pioneering work, it enabled us to do a lot of research. Here is using a very simple ReLU activation; it's using pre-norm, decoder-only architecture. Then Meta released the Llama model, and here it's using the SwiGLU activation function, followed by the gated linear units—basically three matrices multiplied—and it's using rotary positional encoding, which we just talked about: the RoPE rotating interpolation. The 7B parameter model has 32 layers, context length is only 2K at that time, hidden dimension is 4K, since the hidden dimension for each head is 128, 32 heads, and it has much better performance than the previous open-source models. Later, they built Llama 2, which extended the context length from 2K to 4K, and also the training tokens increased from about one trillion to two trillion tokens, and still the performance has not saturated. If you keep training, the accuracy or perplexity will keep decreasing. Llama 2 introduced this GQA, the grouped query attention, to save the size of the KV cache so that it can fit longer context. This is for a 7B model. It also introduced an instruction-tuned version using supervised fine-tuning to align with the human result. That's the Llama 2. Here is comparing the capability between before and after Llama 2. So this is where we were in last year's lecture, and this year, very exciting, Llama 3 came, even larger, by spending even more GPU hours, more training tokens—from 1.8 trillion tokens to 15 trillion tokens, multilingual tokens, and the GPU resource consumption is much higher. It's 50 times more FLOPs than before. The flagship model has 405 billion parameters. So if one GPU is 80GB, you can easily calculate how many GPUs you need to serve the model. As in a larger context length, the flagship model has 8K instruction-tuned, 128K long context version. The post-tuning techniques we talk about: supervised fine-tuning, rejection sampling, and the direct preference optimization techniques.

A very small model, a popular small model, is the Mistral 7B, which outperforms Llama 2, even the 34B parameter model. The interesting part here is the sliding window attention, rather than the full attention. Here, it's using the sliding window attention, and you can stack multiple such techniques to achieve a longer context length. Since just one layer, you have limited context length, you cannot see everything before it, but after a couple of layers, the receptive field is going to propagate and finally gets longer, but is deprecated through to a very interesting part: the Chinchilla law, which describes the relationship between the parameters and also the number of training tokens. Basically, as the parameter grows from 400 million, 1 billion, 1 trillion, the number of trillion tokens also needs to grow. You cannot just grow the model without growing the data, and model and data have to grow proportionally. However, the trade-off is different when we consider the inference computation. For example, the Llama model is trained on a much larger amount of tokens—from like 10 billion, the plan model is trained by much larger amount of tokens than 200 billion, way beyond, being actually two trillion tokens. So we can spend more on training so that the inference cost will be lower. With the same number of parameters, the accuracy will be higher.

All right, so with that, we'll conclude the lecture today. Later, we'll continue to talk about those advanced topics in the next Tuesday's lecture.
