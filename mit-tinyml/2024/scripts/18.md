EfficientML.ai Lecture 18 - Diffusion Models (Zoom Recording) (MIT 6.5940, Fall 2024) - YouTube
https://www.youtube.com/watch?v=LK57PyOJWYk

Transcript:

Welcome to lecture 16 of EfficientML. Today we are going to learn about efficient diffusion models and how to accelerate diffusion models. In the previous lecture, we briefly talked about deep generative learning. We first train a model and then we can predict the data distribution. Different from discriminative models, which predict the decision boundary, the generative model tries to predict the distribution of the data. There are lots of applications for generative models, for example, photo editing and image generation. You may use Midjourney to generate images, and Open Sora can generate videos as well, but all these generations are pretty computationally intensive. In this lecture, we are going to learn about how to make these algorithms more efficient and much cheaper to run on hardware.

This is the landscape of deep generative learning we've covered previously. We already covered the generative adversarial networks. We also talked about the autoregressive model using token-in, token-out approach to generate models. Today we are going to talk about diffusion models. We also introduced efficient autoencoders. So this is the landscape of deep generative learning.

Here is the agenda for today. We are going to first talk about the basics of diffusion models. In order to accelerate them, we need to first understand how diffusion models work, including denoising, conditional diffusion models, latent diffusion models, image editing, and personalization. Next, we are going to introduce fast sampling steps, the DDIM method, distillation method, and also acceleration techniques using sparsity, quantization, and also multi-GPU parallelism. The figure on the right-hand side is the model developed by ourselves, showing a cyberpunk cat with a neon sign that says "M".

Let's first introduce how diffusion models work and the basics. There are two concepts: one is the forward diffusion process, the other is the reverse diffusion process. During the forward diffusion process, we try to add noise to the data so the data becomes noise. In the reverse process, we gradually denoise and reveal the data. This can be illustrated in this image: the first is the forward path, the second one is the reverse path. In the forward path, we are trying to destroy the data by gradually adding small amounts of Gaussian noise. Originally, the image is two different distributions; now it becomes noise. During the reverse process, we are trying to create data by gradually denoising the noise from a stationary distribution. Finally, we will converge to reveal the original image. This is a multistep process.

The forward process is basically adding the noise, starting from the data across many steps, then it becomes noise. The x*t is actually dependent on the previous step. It's a Gaussian distribution with the mean related to x*{t-1} (the previous step), and the variance is Gaussian noise. This beta*t is predefined to control the diffusion speed. This is the relationship between t and beta_t. That's the forward process to add noise. We get a relationship between x_t and x*{t-1}.

What about sampling x*t directly from x_0, the original clean image? How do we get the noisy image at time t? This is a long derivation, but actually it's quite simple. Since x_t is linearly related to x*{t-1}, we can define alpha*t to be 1 minus beta_t, so we can rewrite the equation like this. This is the relationship between x_t and x*{t-1}. We can expand this equation by inserting the same relationship between x*{t-1} and x*{t-2}, so that we could represent x*t with respect to x*{t-2}, and so on. Adding two different steps, we have this property so that we can combine these two noise terms together. We get a relationship between x*t and x*{t-2}, and so on, all the way to x_0, with combined noise. We can shorten that by defining alpha_t_bar to be the product of all the alpha_i, and as a result, we can represent x_t as a relation with respect to x_0 plus the noise. This is the mean and variance.

Now we have the relationship between the original clean image, which is x_0, and at each time step t, what is x_t? We define this relationship between x_t and x_0 as the diffusion chain. For sampling, x_t equals the relation between x_0 and the noise. Beta is designed such that when alpha_t is close to zero, the distribution is close to the normal distribution. Given any time step t, now we can represent that as a function of x_0. That's the forward process: gradually adding noise. We can define any time step with respect to the first time step, zero.

Now we are going to talk about the reverse process, the reverse denoising process. Starting from x*t, we want to obtain x_0. When beta_t is small, the relationship between x*{t-1} and x_t (the adjacent two steps) is also a Gaussian. The mean is learnable; basically, that's what we are trying to predict, consuming the majority of the computation FLOPs. Given x_t, to get the mean and variance is also a Gaussian.

Let's see in practice how we finish the training. Given a clean image, this is the clean image, which is x_0. Since we have the relationship between x_0 and x_t, basically x_t equals x_0 plus noise. So x_0 is the scaling factor plus noise, because scaling factor is the square root of alpha_t, and the noise is the square root of 1 minus alpha_t, which is exactly the same here. Theta is the neural model predicting the noise. We add them together, get x_t, and then we pass it through the model epsilon_theta to predict the noise. This is our predicted noise, the noise that we originally added to x_0, and then we try to match the predicted noise with the original added noise so that we can have an MSE loss right here, trying to match them together. That's basically the training process.

Although the equation is pretty complicated, if we draw it in a figure, it's actually quite simple. For example, sample t, and then we can sample x_0 (image, maybe a cat, maybe a dog), and then we sample noise from the Gaussian distribution, and then we add them together to get x_t. We predict the noise given x_t. Theta is given, and we try to predict the noise and try to match the noise that we originally sampled. That's how we add noise to the training process. Basically, the training part is trying to predict the noise and make sure it matches the noise we inserted into the image.

That's the training algorithm. What about inference? What are we doing during inference time? Given a noisy input at x*t, we pass it through the model theta. What are we trying to predict? We are trying to predict x*{t-1}. We already have the relationship between x*{t-1} and x_t. This is the mean and variance. The mean, we are trying to predict by using this equation. Mu_theta is a combination of x_t and epsilon_theta(x_t). It's a combination of x_t and epsilon; epsilon is basically predicting the noise given this input. The model theta is predicting the noise inside this image, and we subtract the noise. We subtract noise from the image (these are the scaling factors), and we get a cleaner image, x*{t-1}. In that way, we complete one iteration from a noisy input image x*t to get a cleaner image x*{t-1}.

The rule of thumb is: denoise. We first predict the noise using epsilon*theta (this is where the majority of computation goes), predict the noise, subtract the noise. Here, it actually adds another noise, another Gaussian noise, since x_t is the mean, this is the variance, and we have to add back this noise at the noisy image. That is just one time step of denoising from a noisy image to a cleaner image, and then we can repeat this process to get x*{t-2}, x\_{t-3}, all the way to x_0. That's the entire denoising process. Different from previous models where it's just one time, now we have to run the same model iteratively. This is where the majority of the computation goes. It's a dense prediction; you have to make pixelwise prediction, not just a class or a bounding box, but you have to make pixelwise prediction. That's why this model is really heavy and takes the majority of the time, making it slow.

Another reason it is slow is because we cannot do it using a single step, or it's very hard to do it in a single step. Actually, we need a lot of steps, that's why it is slower. These are generated images actually outperforming GANs on the image dataset, so it's a pretty good result, a pretty good start.

Now let's talk about conditional diffusion models. We don't want to just give a random image, but we want to condition the generation on either a class (for example, we just want to generate a cat or a dog), text condition (for example, a sequence of text tokens like "photo of a moon gate"), or sometimes we also want to add pixelwise conditions. What is pixelwise condition? Given a spatial map like a segmentation map, a cat, or Canny edge, the edge of a cat. We not only need to provide a time step but also add a condition to the model.

Let's see how we perform conditional image generation using diffusion models. One idea is to use this scalar condition, a very simple spatial addition. This is the original feature map, this is the conditioned feature map. How do we condition that? Given a class ID like "cat" (maybe ID number 776), we encode that by passing it through several multilayer perceptrons, and then we get an embedding (batch size times c, the channel), versus the original feature map is b, c, h, w. We broadcast the single element to h \* w and then perform this broadcast add to add them together to get a conditioned feature map. That's how we add this class condition.

We can make it a little more complicated. For example, we can pass it through a larger MLP to get two parts: b times 2c. One is the scale, one is the bias. So we scale the feature map, we can also add to the feature map to get the conditioned feature map. That's another way to add this kind of scalar condition.

How do we add the text condition? Now we have two modalities: one is image modality, the other is text modality. During visual language models like the ones we discussed in previous lectures, we find it possible to project image and language, both of them, into tokens. As a result, we can use cross-attention to combine image and language. For example, the input feature we divide into several patches, each patch is a token, so we can tokenize them. For the text, we also pass it through the transformer QKV matrix, so we get the Q token, K token, and V token. We first do QK transpose, and we use that for the weighted sum for output tokens. The final token dimension is still the same as the query, which is the number of image tokens, so the output result is still image, but that already includes the information from the language.

That's one way to add text condition, insert the text condition into image generation. There's another way to do joint attention. In the previous method, are they symmetric? Actually, language and image are not symmetric. The image is treated as the query, language is treated as the key and value. In joint attention, the image feature and text feature are actually symmetric, so they are passed through independent QKV transformations. This is the QKV transformation for the image, this is the QKV transformation for the language (the text), and then we can concatenate them after the transformation. This is the query from the image feature, this is the query from the language feature. We can concatenate them together to get a combined image-text feature for the query, combined image-text feature for the key, combined image-text feature for the value, and we feed them together to the attention. After we get the output, we split it by passing them through different output transformations. There is QK for the output, we also have independent W_O for the image feature and also for the text feature.

On the right-hand side is another diagram showing this relationship. Image and text are passed to independent QKV transformations and then concatenated together to form joint attention and then split to separate output matrices to perform MLP and for the next layer.

What about totally combining them together early? This is another approach: a single self-attention to combine the image feature and text feature (text condition). The tokens are early concatenated before we feed them to the QKV transformation. Since the image feature and text feature are concatenated already, we can feed them to the same QKV transformation. After we pass them through the attention, we have a single output transformation. This is the early fusion for the image and text feature.

We can also perform pixelwise condition. We've talked about class condition (using a class), we talked about language/text condition (using a sentence to describe what you want to generate), now we want to do pixelwise condition. For example, we want to generate something that follows the edge described here. This is using the Canny edge detector to find the edge, and as a result, the generated image together with a text prompt can exactly follow the edge from here. We can also use input human pose extracted from here (the head, the body, hands), and then we can generate several images in the kitchen following exactly the same human pose, or even a Lincoln statue following the exact input human pose. That is by using ControlNet, which is a new technique published last year to add the control by conditioning it on pixels.

How does that work? This is how ControlNet works. Previously, we have a neural net: given noise, we can predict the image. Now we freeze the original network, but we make a trainable copy, and we insert the condition for the trainable copy, starting with a zero convolution so that when the training starts, this branch has no effect since this is zero convolution before and after. During training, they start to change. We make the trainable copy (call this part ControlNet), so it is controlling the main branch. We have two branches: ControlNet controls the main branch, and the main is locked. That is a trainable copy of the original branch. These are zero-initialized one-by-one convolutions such that the training is very smooth; the starting point is basically equal to nothing is on this branch, and we only have this locked, trained model.

Let's dive one step deeper to expand that unit. We add these pixelwise conditions from here on the right-hand side. It could be either the semantic mask, Canny edge, whatever pixelwise information you want to insert, and we keep the original condition, including the text prompt and also the time step. That's the original condition. We copy these downsample stages (these are the downsample stages), we copy the condition control branch from the original branch while we are freezing all the layers of the original branch, and then for the upsample part, we just pass it through zero convolution. So we only copied half, copied the first encoder block but not the decoder block. This is how we add the pixelwise condition beyond the text condition and also the time condition, which has been added to the original branch, which is frozen, and this condition is also inserted at each encoder stage of the ControlNet branch.

There's another technique: classifier-free guidance, which tries to trade off between diversity and also fidelity. To condition on some class, we are trying to, given this class, predict what is the image, but we can also train another classifier. Given the image, try to predict the class. We want to make sure the class is exactly the one we want to generate. We can add the classifier gradient during the sampling to make sure the sample is classified as c. We not only want to run this generation model, but also we want to run this discriminative model to make sure the generated image exactly belongs to the class. The new sample distribution is defined this way: given class, we want to sample image and also predict this image is also predicted as belonging to class c, and we add omega to the power of omega, which is guidance strength in the log space. Multiplication becomes addition, and to the power of omega becomes times log p. That's why we add omega to the power place.

This is basically the naive comparison between naive conditional sampling (from here) versus classifier-guided sampling, which is adding another term: given this image, we want to classify the class and make sure that it belongs to c. This is the classifier gradient, and the strength is controlled by this guidance strength. That's the classifier-guided sample. This is comparing a small omega versus a larger omega. With very small omega, the generated image is sometimes confusing; it is not a dog, but on the right-hand side, fidelity is much better. So larger guidance omega leads to better quality but less diversity. The diversity is very poor, versus here, which has many diversities on the grass and in the snow, but here diversity is much poorer.

What is the limitation here? By using such classifier guidance, we can only work on class condition. There's no text or pixelwise condition, since we have to predict a classifier and we need to train an additional network, which is a classifier network. Can we use one network and not have to train a separate classifier network? That's the classifier-free guidance, a very widely used technique these days to support diverse conditions, including the class condition, text condition, pixelwise, etc., without using an actual classifier.

This is the original term defined in the classifier guidance: it has two terms multiplied together. One is, given a condition, predict the image; the other is, given the image, make sure it belongs to class c. Omega is the strength. We can apply Bayes' rule so that this term, given x_t, with probability of the condition, we can rewrite it to be given c with probability of x_t versus p(x_t). This is the conditional model, this is unconditional, and putting them together, that's Bayes' rule. Basically, we can run the inference twice: one is the conditional model, the other is the unconditional model. In that way, we can rewrite the equation at the top by inserting the p(c|x_t) to be p(x_t|c) divided by p(x_t), and to the power of omega. We still put it here and find these two terms can be combined together to the power of omega plus one. We are combining these two terms together since they're the same; we have omega plus one in the numerator, we just move them together, move them to the right power of omega. In the log space, this division becomes a minus and subtraction, so we have omega plus one log p(x_t|c) minus omega log p(x_t) plus this condition.

What does that mean to the original model? Previously, we were having the condition under t right here. Now we just replace it with two terms, inference it twice: one is given t and c, the other is given only t, no c. We have to run the inference twice, combine the results of conditional and also the unconditional forward. What does that mean? We need to double the FLOPs: do it once with the condition and do it another time without the condition. That's basically the classifier-free guidance.

Let's see how that works. On the left-hand side, there is no guidance; this doesn't look like a dog, but on the right-hand side, it looks like a dog. Larger guidance omega is also leading to better quality but less diversity. The advantage is that it can generalize to any condition forms, not just a class condition; it can generalize to different conditions like pixel condition or text condition, and we don't need to train a separate network, just the same diffusion model. We just need to run the diffusion model twice.

All the techniques we learned so far are gradually adding complexity, making it more computationally expensive, like adding a condition, etc. Later, we are going to talk about how to reduce the computation FLOPs, starting with latent diffusion models. Imagine you have a pretty high-resolution image, which means when we are predicting noise, we have to make pixelwise prediction; there are a lot of noise pixels to predict. We pass it through an encoder, and then denoise at a smaller resolution, like 1/8 or 1/4 the resolution, so that you can have much smaller compute. That is a very widely used technique these days called latent diffusion. Rather than directly doing diffusion on a high-resolution image, we first pass it through an encoder to downsample by four times, eight times, even 64 times as in our recent work called deep compression autoencoder. We have a much smaller latent space, and you do diffusion in this smaller space, which is much easier and much faster. After you denoise, we pass it through a decoder to reconstruct the original image. The diffusion only needs to run in this smaller resolution latent space rather than on the high-resolution original image space. That's a way to compress the image and only run diffusion in the compressed space, and that is called latent diffusion: only apply diffusion on a small resolution part to enable simpler denoising and also faster synthesis.

Let's see how that changes our roadmap. This is the original configuration. The difference is that the image is fed to an encoder, and we feed the encoder's result as z. We first add the noise in latent space, add the noise, this is the sum, pass it through the model, and predict the noise and try to match noise. The learning target is exactly the same; the only difference is that we added another term. Rather than directly feeding x_0, here we feed x_0 to the encoder and get z_0, feed the z_0 to the model, predict noise, and z_0 is in the latent space at a much smaller resolution. This is during training, the forward process.

What about during sampling time? Everything is actually the same except for the last one: we want to feed it to a decoder to decode the original image. Previously, we have this x*t, and then we pass it through the model to predict the noise, and we subtract noise from this x_t, and we do denoise, add another noise, look at the final noise z*{t-1}. This is from z*t to z*{t-1}, all the way to z_0, and after we get z_0 in the latent space at a smaller resolution, we pass it through the decoder and reconstruct the original image. The only change is the right part: the decode passes through a decoder from low resolution to high resolution. Everything else stays the same, but we can now generate much more complicated and higher-resolution images, like here: an astronaut in a jungle, colorful palette, and here is a disco ball floating. This is Stable Diffusion XL for high-resolution image generation.

As you can imagine, the more we compress, the less workload, the smaller the number of resolutions, and the smaller the number of tokens. Recently, we invented this deep compression autoencoder. Rather than compressing the token by eight times, we can compress it by 64 times. What does that mean? It means eight times more compression on each side, and altogether we have 64 times fewer tokens because height times width, two sides. So 64 times fewer tokens. How does that impact the attention's complexity? Four thousand times fewer FLOPs, since the attention computation complexity is quadratically related to the number of tokens. We are reducing each side by eight times, so altogether it's 64 times fewer tokens and 4,096 times fewer FLOPs.

This is original Stable Diffusion's variational autoencoder (VAE), reducing the resolution by eight times, passes through diffusion model, pretty slow. This is our proposed approach using deep compression autoencoder: 64 times compression on each side, passes through diffusion model, and makes it much faster. However, this is not straightforward. In naive approaches, for example, VAE and 64 times compression, the quality is very poor, but using deep compression VAE, the quality is much better. On the left-hand side, we are comparing the compression ratio: higher compression ratio is more preferable but harder. Baseline methods, SDVAE, deteriorate very rapidly, but the deep compression autoencoder stays very stable in FID even given 128 times compression ratio.

How did we achieve that? Reducing the resolution, you have a very naive approach: you can directly move the information from spatial dimension to the channel dimension. Say every 2x2 pixel (four pixels), you can change it from 2x2 into 4 channels. That's the explicit transformation from spatial to channel. By explicit spatial-to-channel, we can add this explicit spatial-to-channel branch in the downsample block and add this explicit channel-to-spatial branch in the upsample block. We call it rigid autoencoding. Rather than using the weights to learn such stuff, we use this zero-weight branch as a residual bypass to learn this, and we find adding this rigid autoencoding makes it a lot easier. For example, the FID without rigid autoencoding will deteriorate very fast in F64, but it remains very stable under high compression ratio.

A second technique is this three-stage adaptation to high resolution. We want to deal with high-resolution, like 4K image generation. How do we have an autoencoder to support much higher resolution without requiring a lot of training FLOPs? We decouple it into three phases. The first phase, we just perform low-resolution training for both encoder and decoder. Given input image, encoder encodes into latent space, and the decoder reconstructs to the original image. This is low-resolution full training. In the second step, in order to save computation, we perform high-resolution latent adaptation only to the middle layer, corresponding to the last layer, only tuning these middle layers and freezing the remaining layers. Finally, we add this G loss to further polish, to do some refinement only on the last layer of the decoder, since that's only related to the low-level features. The feature is actually refined in the last stage. So we do such layerwise sparse tuning, not tuning all the layers, but sparsely tuning either the middle layer or late stage to save computation and make it very fast to train such an autoencoder.

Using this autoencoder, we can greatly accelerate the image generation diffusion models. This is using the original latent space (STV), which is eight times reduction, so it's pretty slow; it generates only 17 images per second. On the right-hand side, we are using the 64 times compression (F64), reducing the number of tokens, reducing the resolution from 512 to 8. We can diffuse only 64 tokens compared with previously having to diffuse 4K tokens, so it's a lot faster—19 times faster actually compared with using the original autoencoder. It matters a lot: if you can apply such deep compression autoencoder, you can have much fewer tokens and quadratically reduce the computation FLOPs or inference of diffusion models.

Another benefit is that now we can achieve high-resolution image generation without using a lot of compute. For example, 4K image generation: look at the ship, look at the details, how much detail it has. This is 4K by 4K generation, and compare it with 1K: this is an astronaut in the jungle. 4K has a lot more details, but computation—if you don't use techniques like deep compression autoencoder—will be prohibitive.

Based on deep compression autoencoder, we built an efficient image generation diffusion model called SANA, which takes advantage of several factors. One is the linear attention module using the linear attention that we previously described. For high resolution, we have a lot of tokens, and computation complexity grows quadratically with the resolution, so linear attention will be very helpful to generate high resolution and achieve speedup. This is using the linear attention, this is using the linear diffusion module, using the deep compression autoencoder, and here it's using modified complex human instruction, which we are going to talk about.

Previous diffusion models are using encoder-decoder models like T5 as text encoder. Recently, there's a lot of progress in decoder-only large language models like GPT-2, GPT-3, 3.5, everything after that, or Google's Gemini. Can we use this decoder-only model as a text encoder? For example, given an input to Gemini: "A man is walking," Gemini's output focuses on conversation. It says, "This is a great start. To make this more interesting, tell me about the man and his walk. For example, where is he walking? A busy city street? What is he carrying? His mood? The more details you give, the more vivid and engaging the story can be." So this is not quite related to image generation. If we naively feed our prompt to Gemini, it is not basically related to image generation, but focuses on conversation and storytelling.

In contrast, we add a complex human instruction, append this fixed human instruction before the actual product: "A man is walking." Now Gemini can give the correct output, saying, "A man in a worn leather jacket walks briskly down a cobblestone street. He has a deep cast, his silhouette framed by the setting sun. He wears a faded blue scarf around his neck," etc., giving a very detailed description beyond the simple instruction we gave. How do we achieve that? This is using the prompt engineering we discussed in the previous lecture. This is the complex human instruction we feed to Gemini: not only the prompt but also examples for in-context learning. Given a user prompt, generate an enhanced prompt that provides detailed visual description suitable for image generation. Evaluate the level of detail in the user prompt; if it is simple, focus on adding specifics of colors, shapes, sizes, textures, and spatial relationships to create a vivid and concrete scene. If the prompt is already very detailed, refine and enhance the existing details slightly without overcomplicating. We also give some examples: user prompt "A cat sleeping" is converted to enhanced version "A small fluffy white cat curled up in a round shape, sleeping peacefully on a warm sunny window sill, surrounded by pots of blooming red flowers." By using the in-context learning example with two user prompts, and finally, "Please generate only the enhanced description for the prompt below and avoid including any additional commentary or evaluations," followed by our real human prompt "A man is walking." In that way, we can let this decoder-only language model give the output that is preferable for the diffusion model to consume.

How does it echo with the visual language model that we previously discussed? For example, the BLIP visual language model: actually, we can use BLIP to recaption the image. Given the image, this is the original caption, which is actually very, very short: "Top view, written 'Happy Valentine' on the tart, plate, cake, light, the background." Very simple, right? But using BLIP, our visual language model, it can give a very detailed caption like: "The image captures a delightful scene of a Valentine's Day creation. At the center of the image is a round chocolate tart, rich and inviting. The cake is adorned with a red heart on top, symbolizing love and affection. The words 'Happy Valentine' are elegantly written in white icing on the cake, adding to the festive atmosphere. The cake is placed on a wooden table, which provides a rustic charm to the setting." You can tell it's a very detailed description that is unlikely to be labeled by a human labeler to give such detailed and fancy label without hallucination. This is the BLIP 13B, 3B; we feed it to different sizes of BLIP to vote based on their CLIP score to prevent hallucination and try to find the best match. CLIP score basically measures the similarity relationship—how well does it match between the caption and the image. Now everything begins to converge: by using our visual language model, BLIP, we can generate more data, use the data to train better SANA, so SANA and BLIP can grow together. That's the beauty of such co-design.

Now we have to build the SANA. Based on the above techniques, we can accelerate SANA with 1K image generation by 25 times, accelerate 4K image generation by 100 times compared with the Flux SD, which is a popular diffusion model these days. Where does this 106 times acceleration come from? It comes from deep compression autoencoder, linear diffusion kernel fusion, and also using the flow-based DPM software. Quality-wise, SANA stands neck and neck with previous models. This is for the Flux model, this is for SANA; they are almost neck and neck.

Let's see some generated results from SANA 1.6B: the text, logos, and the human face actually have a lot of details. Here are some more examples: a blue Porsche 356 parked in front of a yellow brick wall. This one is probably more interesting: an avocado wearing a royal crown and a robe, seated on a throne in a grand opulent setting. The scene is surrounded by other avocados, also adorned with crowns. So, high resolution, low cost.

Let's continue to talk about image editing. Image editing with diffusion models is pretty powerful. For example, here we want to give some input strokes, here a power room, and now we want to gradually recover and generate a power and a room by denoising, and it should match the input stroke. How do we do that? These are separate spaces: one is the stroke image, they are in different locations. What we do is perturb them with noise, adding to the stroke such that the noisy image can have some overlap with the original space, and then we do the reverse process, gradually denoise. We can move this blue dot closer to the center of the green region, which is the image region space, so that the output looks like a castle in this case.

This is some example: given original, this is drawn probably by a human, and then given a prompt "fantasy landscape, fine art, ArtStation," and this is the generated image given the input stroke.

We also want to personalize the model—not only edit the model but also personalize the model. Say, given a dog, and we want to generate some dog swimming, sleeping in a bucket for this particular dog. It has gray hair, left ear, and white fur. We want it to follow exactly the same dog, like getting a haircut. This is not a random dog, but we want it to be exactly this dog. How do we do that? Actually, this is pretty challenging. For example, if we input a few images of this dog, the same dog in different settings, the generated images have poor fidelity: like this, the blue no longer looks like this one, the color three should be yellow rather than white, and also very few contexts—they're both white background, less fidelity but able to generate new context, the three has changed, versus both having good fidelity and also diverse context. DreamBooth achieved this functionality by fine-tuning the model. Given a text-to-image model, given a few pictures (like three to five images) of the dog plus itself, the language "dog," and pass it through DreamBooth to fine-tune, we get a new personalized text-to-image model and this unique identifier "v." Then we can say "a v dog is in the beach," it will generate this particular dog in the beach; "a v dog walking on colorful carpet," and then we can generate this personalized dog on the carpet.

Limitation here is that once you fine-tune the model, it's specific to only one subject. If you want to generate, like, a person friend playing basketball, you have to do the fine-tuning again for your friend, which is costly. If you are interested in training-free methods, there's plenty of publications later using training-free methods to customize and perform personalized image generation.

Now let's switch gears to talk about fast sampling techniques: denoising diffusion implicit models. The denoising process is an iterative approach, from the noise to data. The idea is: can we use fewer steps, have a larger stride, reduce the number of iterations? Previously, the method is Markovian, so each x*t is only dependent on the previous time step, and we treat the model such that only use these properties during training. But at different times, can we find a non-Markovian forward process to accelerate by reusing the same model during training time? We want to make sure x*{t-1} is not only related to x*t but also related to x_0. Can we find such a setting that we don't have to change the trained model but just change the inference, change it at inference time? Actually, we can do that. This is the conclusion: previously, x*{t-1} is related to x_t, but now it's related to not only x_t (noted in this epsilon, which is related to x_t), but also related to x_0. We can estimate x_0 with this equation. In that way, we can have a larger stride time step: from 0, 10, 20, 30, a thousand. Previously, we just made one time step increment; now we can make the increment of 10 to accelerate the denoising process. In the original DDPM, if you reduce the number of steps from a thousand to 20 or 10, the FID will explode. By using DDIM, the FID stays very stable until like 10 or 20 steps, still below 10. This is a CIFAR-10 dataset; on the CelebA dataset, the FID is both well maintained while having much fewer sampling steps, like 20 as opposed to a thousand steps.

Another approach to reduce the number of steps is by using distillation. This is a four-step model; we can distill that so that a single step from the student network can do the same as two steps in the teacher network. We can do another generation of distillation such that the student can directly use one step to model what the teacher has done in four steps. So we can distill a deterministic sampler to the same model architecture, and each step the student model is learned to distill from two adjacent sampling steps from the teacher, and every new student will reduce the number of sampling steps by two. In this way, we can reduce the training steps, but the FID stays much lower compared with the DDPM original non-distilled version.

Now let's talk about acceleration techniques for diffusion models. Let's start with using sparsity. We can exploit spatial sparsity to accelerate the inference of diffusion models, in particular for photo editing. The vanilla diffusion models waste many computations when we have to resynthesize the entire image, even if we only edited a very small portion of it. Say, in this case, we only edited 1.7% of the pixels. You can tell where we edited: we only added a sign right here, a sign right here, that occupied less than 2% of the pixels. But original Stable Diffusion has to go through the entire model and run convolution on the entire input image. Spatial response inference is basically saying we can just change whatever we edited; for the not-edited region, we just reuse the feature map from the original model. Since we only added the sign, we only recompute the sign (in green here), and we use the feature maps for the rest of the areas from the previously generated image. As a result, this can save eight times the computation.

This is how it works: we build this special incremental generative engine. Incremental means only change the pixels that have been edited; for those pixels that have not been edited, we just reuse whatever it was from the previous approach, the previous iteration. Given the original image, and we drew some piece of cloud on the top, this is the difference mask. We discretize that to a 4x4 grid in this case and highlight the areas that have been edited. This is the original image, this is the edited image. We only gather these pixels, these chunks that have been edited, and then only pass it to the convolution for this edited part. We scatter this to the original feature map to get the updated feature map. That's one layer of sparsity, which only does compute (convolution) on edited blocks. We call it active blocks, and we stack them to the batch dimension so it's easier to parallelize. No sparse kernel is needed, since each patch is dense convolution, and this can also apply to diffusion-based transformers.

We applied it to image inpainting. What is image inpainting? We can draw a box inside the image and we can paint whatever we want in the box. Now, to paint a horse photograph on grass: previously there is no horse, now we draw a box and want to place a horse right here. Without this approach, we need to output and generate the entire output image, which takes a lot of compute—pretty slow, 300 seconds. Now we only edit the mask region, which is three times faster, four times, and the quality matches very well with the original input. This is another example: we only edited 3% of the pixels, added a coconut tree. This is the baseline model, this is our model; the generative quality is pretty much the same, while being more than 4x faster. This is the real-time demo doing that. This technique can enable interactive photo editing, for example, adding some cloud to the image.

Now let's switch gears to talk about applying quantization to accelerate diffusion models. We've talked about quantization using quantization to accelerate large language models, but large language models and diffusion models have a lot of differences. For example, on single-batch inference, large language models are bottlenecked by the memory bandwidth, while the diffusion model is bottlenecked by the compute. Those methods like weight-only quantization cannot accelerate diffusion models because they're compute-bounded rather than memory-bounded. If it's memory, large language models can benefit from, like, W4A16 weight-only quantization, but in order to achieve measured speedup for diffusion models, we need W4A4 quantization: quantize both the weight and activation to only four bits without losing fidelity, which is even more challenging than W4A16.

Let's see how we solve that problem. Observe: the weight and activation both have a lot of outliers in diffusion models. We have seen this figure in the SmoothQuant lecture. This is the distribution: the green is 50th percentile, the yellow is 99th percentile, and the remaining is the rest 1%. The red part is the outlier. Activation has a lot of outliers right here, and the weight also has a lot of outliers, which make both difficult to quantize. We can apply smoothing like we did in SmoothQuant to migrate the quantization difficulty from the activation to the weight. Activation is much smoother, a lot less red compared with here, but the activation has more outliers: previously had only one outlier, now has two. This is a real distribution: a lot more red compared with previous, but luckily the activation's outlier is migrated, so we just need to migrate the weights' outlier. We can do that by decomposing using a side branch, which is a low-rank branch to absorb those outliers using full precision, but this is a very low rank, only 32 ranks, so this is very cheap. We can migrate the outliers, migrate quantization difficulty to the side branch, such that after SVD, the weight has a lot less outlier compared with before. Finally, both the weight and activation are four bits, but it's very easy to quantize; there's no outlier, and it's associated with a very low-rank side branch.

The good thing about this approach is that if you use LoRA fine-tuning, you don't need to requantize the weight, but you just need to append a few more ranks, like 16 or 32 more ranks in full precision by whatever LoRA you want to add to this module, since a lot of people are using diffusion models with LoRA. So this is very compatible with the framework. Before that, we actually observed naive implementation leads to a lot of overhead, like the yellow and the red region for this up and down projection due to the LoRA branch. This is the main branch, this is the LoRA branch. We fuse such overhead so that the latency overhead (40% latency overhead) is removed, and we achieve that by actually fusing this down projection with the previous kernel since they share the same input, and we fuse this up projection to the next layer for four-bit compute since they share the same output. In that way, we can reduce this overhead in red and yellow. This is the kernel fusion.

The speedup and the model size reduction: the baseline FP16, we can reduce it by 3.6 times. The W4A16 method, conventional, can also reduce the model size and also reduce the memory. We can reduce a bit more since we also quantize the activation. However, this is the beautiful part, the exciting part: the conventional W4A16 work can only reduce the memory, but it doesn't lead to any speedup—it's actually slower because they still need 16-bit arithmetic. But here, we can use four-bit arithmetic, either in INT4 or FP4, and can achieve more than three times measured speedup on 4090. This is the single-step latency: the baseline method is out of memory, and the INT4, we can make it 3.5 times faster than the INT4 approach.

This is the visualization comparing the baseline approach: it's out of memory on a 4090 GPU, and this is for very high fidelity, but it's still more than a second, pretty slow, while we can make it just half a second. This is the internal quantization: the quality is really poor, actually the quality already starts to degrade from FP16 to INT4 approach, but ours can well maintain the output quality, pretty much the same as original input, while the memory is reduced: previously 23 GB, now it's only 6.5 GB, half a second compared with 1.8 seconds. This shows that SVD-Quant is very compatible with the existing LoRA: we can just add a LoRA branch without having to do any requantization. This is FP16 and our INT4 version using different LoRA models, like realism, pretty much the same: the anime, children, yarn, different kinds of styles, well maintain the fidelity for different LoRA models.

Finally, we can apply parallelism to exploit multiple GPUs to accelerate diffusion models. For language models, we talked about using tensor parallelism to accelerate by throwing more GPUs; we can just make it faster. What about for diffusion models? How can we throw more GPUs to make it run faster? These are two devices: we have one GPU on the top, one on the bottom. We want them to work together to generate the image fast. However, these two models have to communicate to do the attention, attend to each other, which will lead to large communication overhead, which we don't want. It will also negate the saving we may get from multiple GPUs. So we want to reduce the number of communications. The insight here is that the input across adjacent time steps are actually very similar to each other. So rather than directly communicating, we can actually communicate using the old, stale activation. For those communications, when we are attending to each other, we can attend to the previous time step; that's totally fine. That's the insight, and we can exploit that to reduce the communication overhead and also enable the communication to overlap with computation, since we are reusing the old time step's feature.

Originally, the latency is 12 seconds for four GPUs. Naively doing such parallelization indeed makes it four times faster, but it will generate four separate images due to lack of communication and duplicated subjects. This is naive parallelization across four GPUs, but using distributed diffusion, we can make it three times faster, but the generated image quality is very good compared with the original one, rather than generating four separate images separately. This is the speedup across one, two, four, eight devices: about more than 2x for four devices here. On higher resolution, the speedup is even higher due to the communication overhead being saved with up to eight GPUs. So, 8K generation is about six times faster.

Here's the summary for today's lecture. We first introduced the basics of diffusion models, then we talked about how to add condition, how to diffuse in the latent space, including using deep compression autoencoder to compress the tokens, how to perform image editing and personalization, and then we talked about fast sampling techniques (DDIM and distillation), together with acceleration techniques using spatial sparsity, SVD-Quant quantization technique, and also distributed diffusion by using parallelism. That's all for today's lecture. Hope you have a long week and happy weekend.
