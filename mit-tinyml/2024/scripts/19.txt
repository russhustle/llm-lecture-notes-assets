(90) EfficientML.ai Lecture 19 - Distributed Training Part 1 (Zoom Recording) (MIT 6.5940, Fall 2024) - YouTube
https://www.youtube.com/watch?v=tiAZUme2ST0

Transcript:
(0:01) all right good afternoon everyone let's get started so today we are going to uh start a new chapter about distri distributed training so in the first two part of the lecture let's first give a quick over uh review we learn about how to accelerate inference right techniques general purpose techniques domain specific techniques including pruning quantisation NE architecture search uh distillation for large language models for diffusion video models Etc so from today we are going to start a new chapter about training how to make training faster if
(0:35) you in cont model is too slow to train hopefully we'll teach you some techniques to make your training faster so this is agenda for today we'll start with background and motivation why we need a distributed training and we'll cover uh several techniques about paration uh including data paradism um model paradism peline uh sequence paradism model is also called tensor paradism in particular for data paradism we'll talk about uh several Shing methods like 0 one two three and fully shed distribut uh um fsp paradism f fdp techniques okay fully
(1:19) shed um distributed paradism okay data parm so let's start with background and motivation so these models are usually coming at very high cost of 20 so the higher the accuracy more max we need okay the more max we need um between this GPT model gpt3 require uh 3 million GPU hours which is too slow to train if you have a single GPU so we need multiple gpus to make it faster so this is showing how long it takes to train a model like from res50 bir based TG gpt3 particular for gpt3 y75 billion parameter is a huge amount of GP hours
(2:03) if you have a single GPU that will take 355 years to train the model which is too SL and these numbers are measured on Nvidia a 100 GPU so the boss ask you what did do over the last month and answer is train the model for one IO boss ask um fine what's the plan for next month and answer train the model for one more I talk right so that greatly limits the productivity of our designers those machine learning practitioners right so we want to make the model train faster so we want to use multiple gpus just to buy multiple gpus do that for
(2:48) example if a training task take 10 GPU a single GPU 10 days if you have 1,24 gpus that will take only 40 minutes 40 minutes over just a cup of coffee so um this kind of technique can greatly speed up our research and development cycle so let's see a real use case that our lab did for distributor training couple of years ago so we exploited uh this Summit supercomputer uh it has many v00 cores pretty big at that time we trained over 1,000 gpus at that time uh each GPU is we 100 that's already two or three generations ago b00 and the a00
(3:35) h100 and now B 100 is coming uh at that time it has 200 gigabit per second Infinity band now you can easily get 400 gigabit per second after this was in 2019 this was in so only uh four years there's tremendous um development this tremendous improvement from Nvidia for uh the GPU family so this is at that time between TR video model video understanding module temp shift module over 66 hours of video which is considered a small amount these days but at that time it was considered pretty big amount so with only one node
(4:16) six gpus that take 49 hours roughly two days and with 128 nodes that's 700 gpus 2 28 minutes with 1,536 gpus we shorten that for 14 minutes that's 21 times faster so theoretically it should be 256 times faster okay so the accuracy is well maintained so we've talked about temporal shift module just to shift the channels across different frames to enable temperal modeling which is a good replacement of 3D convolution that is much more computationally efficient so this is just showing by utilizing more gpus by buying
(5:02) so we can shorten the design cycle from 10 day from two days to 14 minutes okay so let's dive deeper into how do we achieve that given distribute training can be so helpful to shorten the uh design cycle let's see how do we paralyze U and how do we exploit so many gpus say you have budget to buy like 1,000 gpus how to program them exploit them so let's talk about paralyzation techniques in the following four aspects one is data parm uh Pipeline paradism paner paradism and sequence so this sequence paradism is a new content
(5:46) we added this year in last and the year before last year we don't have this content uh this is new given the fast development of long sequence context large language model um like a video can be one hour an long video in um uh 1080p can easily exceed thous 100 million tokens right a whole series of Harry Potter book is a million tokens so how to parallelism across the sequence Dimension is also very crucial so we added this new chapter so let's first look at the data parm imagine we have a deep learning model we have a lot of tring data one
(6:32) idea is to um distribute across different part of the data right we have three part blue the green orange part and then we paralyze the yellow the blue part to gp1 green part to gpu2 and the orange part gpn so we are splitting the training data to multiple gpus splitting the data okay and each GPU are sharing the same model the same model across different gpus so that's data parm we are partitioning the data we are sharing model okay and the second Technique we can partition the model and sharing data partition the model in N Parts a
(7:25) pipeline manner okay but they are sharing the same tring data set sharing the same training data set so we have a single copy of data but we are partitioning splitting the model OKAY on gpu1 gpu2 gpn are processing uh different layers of the the model and next is the tensor Paralis we can partition in another dimension okay we are also attaching the model but not from the layer Dimension but but within a layer we're partitioning a different part of the matrix multiplication across different DPS uh we are partition in the
(8:08) activations the first third next the last third of the activation is fed to different gpus and in this case uh gp1 is responsible for the first third of activation and then we can continue the same way for different layer okay so this is also sharing the same training data but we're partitioning across different activations for the model for example if you have a big matrix multiplication like 4K by you can partition in that by 4K to 2K and use two gpus process that 4K GPU matrix multiplication okay and lastly we have this sequence
(9:04) arism okay so say we have a long context long text for 20 like three books right first we have this batch size dimension uh token Dimension hidden okay so the batch Dimension we can use data parm split by the batch like three books we splited and use only train one book on one GQ and then the token Dimension the token is too long like a book can have like tens of thousands tokens so we can exploit sequence paradism a long sequence we can split it into several shorter sequence we can split the tokens okay just like this
(9:46) book we have three chapters can feed chapter one to GPU chapter two gpu2 chapter 3 to gp3 just like this right so each GPU is responsible for all the all entire model if model is not each GPU has the whole model but it's only responsible for processing uh part of the sequence okay part of the sequence since total sequence too long each GP just process part of it so that's sequence Paralis okay so this is a quick overview of the four different uh paralyzation approaches and now we are going to dive deeper into each paralyzation approach
(10:31) and see how do we Implement them so are going to start with the data paradism communication Primitives zero and fsdp so these are all related to data Paralis next we are going to talk about par Pipeline paradism and then tensor paradism followed by sequence paradism to cover all the four paration techniques okay ready let's go with the data so let's first introduce a method called using a parameter server okay so we can scale the distribut wiing with parameter server so this is a centralized approach not quite widely used anymore um but let
(11:15) me start by introducing the basics so there is a central controller of the whole training process and then we have many worker knows in this case we have four workers okay the parameter server receive the gradient from workers and send back the aggregated results and each worker will um receive a part of the training data splitted and then update the gr and calculate gradient and send the gradient to primet server so put it in another way we have a neuronet model we have a gradient in the parameter server and we have pool of
(11:58) worker node including the gradient local replica of the Neuron netor model and the training data and training data is splitted from the entire training data set and we splited into four parts each GPU is responsible for one part to begin with the parameter server will replicate replicate the model and each worker will pull the model from parameter server okay so four workers each will pull the model from worker and then it will also uh split the data set randomly but evenly across four workers and we have the weight
(12:47) the we have the data then each worker will calculate the local grent on that piece of training data over that local replica of the model so the question is are four local gradients the same no they are not the same because they're using the same model but they're using different piece of training data which is randomly splitted okay so they each have a copy of local gradients what they do is push uh the local gradient and sum them up to the parameter server okay so we have the parameter server right here and the primet server is
(13:33) responsible for updating the Neuron networ model with the gradient so now um the parameter server will replicate and pull uh each worker will replicate and pull the updated model to update its local copy otherwise his local replica will will be stale okay now it's going to be replicate the new model and then repeat this process to pull the model split the data calculate local gradient send it to the parameter server parameter server update the model and then each worker pull it again so if we see the difference
(14:19) between single node training versus distributed training previously in each iteration we sample the data calculate the gradient perform a step to upd the weight now we have add this part to pull the gradient from the primet server and then it's the same sample data Cate the gradient we added another part here to push the gradient to paret server um and sum it up then param server perform this griding step so there are actually two synchronization steps here in the red okay so make sure each server each worker uh is
(15:05) synchronized so synchronization has overhead is not free so let's talk about uh this communication Primitives and see how can we uh save this communication overhead so there are a couple of communication Primitives when you come to send and receive data in the over network across different node so simple one simplest one is send and receive okay so we have um several nodes can send from node zero to three and this is receiving from note three to Noe zero okay so this is one to communication transferring data from one
(15:44) to another and then we have one to many scatter and GA okay so scatter basically um send a tensor to all the other workers so tensor four components it was send it to uh 0 1 2 3 so this is one many one to many and gather is basically the reverse M to one okay We Gather the um data from worker zero one worker two worker three and I gather everything to worker zero so receive the T from all other workers it's called gather is many to one versus scatter is sending a data to many servers one to many um another one to many is actually
(16:32) reduce and so reduce uh is very similar to gather but it's different so basically um only gather all the data but reduce will do another operation which is sum it up or U finding the finding the max it's going to do a reduce operation after uh The Gather operation so you can think of reduce operation as um The Gather operation followed by a reduction operation so if you sum them up then that's reduce if you don't sum it up leave at this then it's scatter there's also another one to man operation called broadcast so
(17:16) one tensor to all the uh workers um broadcast is actually different from scatter so is going to send tensor different par the tensor to different workers and different worker will receive different piece of data but broadcast will send identical copies of data to all the other workers that's the difference between uh broadcast versus scatter finally we have this many uh many to many operation so uh this is all reduce this is all together okay so our reduce will perform and reduce operation on all the workers so if you look at our
(18:07) reduce versus reduce is basically uh working on one receiver but for all reduce you're performing reduce on all the workers reduce on zero one two three okay and similarly uh for the all gather operation is to perform gather on all the workers gather workers Z GA from worker one GA on two on worker three so that's a relationship between reduce gather all reduce and so this is reduce this is gather just operating on one receiver operating on one but M to many is operating on receivers okay so they share some similar
(18:57) properties so let's see how do we use uh those communication Primitives in the parameter server setting so we just talk about primet server and then we have manual workers um they have a different replica of the training data so which type of communication scheme is used when we are um replicating in the blue part versus orange part when we are pushing and suming the gradients so we have all reduce gather broadcast scatter gather send and receive during the orange and blue operation which operation uh communication primitive are we
(19:50) using yeah for the uh broadcast is used for replicating Tool right so we have a central primet server we want to broadcast the weight to other workers and for p sum is reduced so we're averaging from other workers every worker get his own gradient and we want to sum the gradient in pr server so it's actually reduced not only gather it need to also perform a reduction so it's reduced so what is the bottleneck here what is the bandwidth requirement to do this so replicate and pull each worker is 01 you just need to pull one copy of data
(20:36) but from for the parameter server bandwidth is o n because you have works so the replicant pool will take o communication complexity for the push each worker just need to push its own copy of credit so that's all1 but for primary server and to receive end copies so the complexity is all so it's actually pretty heavy right for the primet server networking traffic is over it's not quite scalable the more worker you have the larger um bandwidth requirement is needed so the bandwidth of central parameter server will grow linearly with
(21:26) the am of workers which can be a bottle neck when you have a lot of machines so a natural thinking is can we make it more scalable by getting rid of this centralized parameter server and make everything uh decentralized without a parameter server of course we can do that by using this all reduce operation to get rid of the centralize the parameter server and how do we that say we start with a naive or reduced implementation using the sequential way um so each GPU will calculate it's local gradient on piece of own piece data right and now
(22:13) we perform uh all reduce on the node okay so reduce on node zero okay on node one reduce on Noe two Note three so each node worker uh will do a uh gather and then reduction to combine all the gradients together although the calculation has some redundancy they're all calculating the sum of this um green yellow blue and red right um so what is the time complexity actually that's o n right sequentially doing the reduce reduction okay so time is when what about the bandwidth it's also because each worker requires
(23:02) receiving the data from all other workers so both the time and band with is O which is not good so can we do better this a better ring reduce operation by using this ring of reduce what does that look like so step one worker zero send the gradient to worker one send it to worker two send it worker three worker send it to worker zero okay so at this moment worker zero has data uh both from itself and also from worker three okay so worker one can have the data the gradient both from worker one and worker zero since worker is sending
(23:52) to worker one previously already have this yellow data so now it has both the Y yellow data and green data so that's the first step and second step we do the same worker zero send data to worker one such that uh worker one has both the green yellow and also the red okay it is that's green it has yellow and worker one send its gradient to worker two such that will have the orange the yellow blue and also the green comes from rer one okay and we do the same for 2 to three 3 to0 in a realing manner so everyone just sending to his
(24:45) neighbor and after step three zero sender one sender two so that worker one has data from uh the red green the yellow and also blue so now it has all the data in need and similar thing happens for all the other nodes they each have uh the green the orange yellow blue and the red so only in four steps and we have all the data so the time is still all in uh since we have four node require three uh steps so time is still in but bandwidth it's only1 we only need to communicate with our neighbor so there's no very little bandwidth needed right
(25:39) here remember previously the parameter server the centralized paret server require o in bandwidth now everything is all so that's good we made some progress by using this ring or reduce uh can we do even better there is still some redundant computation right can we do even better okay so before um looking at the more advanced version let's look at a n all reduce implementation parallel reduce so previously we talk about sequential all reduce have four steps for workers we can do all everything in parallel at
(26:18) the same time worker zero is doing a reduce worker one two three they are all doing the reduce okay so we only need1 time since it's not sequential is parallel however the bandwidth is O Square so all the each Arrow everything is happening at the same time so we have n we have n so o Square uh communication happening in the middle so um the time is pretty short but bandwidth is too large compared with this ring I'll reduce we need the O in time time is large but bandwidth small so can we have even better solution um summarize so far we talk
(27:02) about parameter server okay or one time but Peak node bandwidth is o n total bandwidth is O uh sequential are reduced time is o n PE node bandwidth total bandwidth is also o uh for all reduce using the ring manner the Pak node bandwidth res is reduced to1 since I only need to communicate with my neighbor total band width is still o n since I have to do that for n times and Par all reduce time is 01 which pretty good but pick node band width is o n for each node the total band width is o n square and now we want to combine the
(27:46) advantage of the ring and parallel um are reduced ring are reduce combined with parallel reduce and let's see how do we further reduce the bandwidth time okay so now the example gets a little more complicated so let's use eight eight note in this example okay so um in the first cycle we will combine the data from adjacent node with neighbors with outside of one that's adjacent node so one combined with two two with three oh sorry four five with six seven eight and as a result node one has both the green and red node two also have green
(28:36) and red no three has this blue and and orange Etc okay so just after one cycle um Everybody communicate with its neighbor with offset one step two each node will exchange with neighbors offset two okay so node one will communicate with node three and node three will communicate with node one two will communicate with node four okay so this is one group and the other group node five will communicate with node seven and six with eight and as a result um you can see we are already making some progress everything on the
(29:24) left we already um have all four colors everything on the right already has all the four colors so we can do one more iteration to complete this we can complete this job so let you brainstorm um how should we do next uh what we communicate in The Next Step so that everybody has eight colors with a larger outset first one last one or first with a fifth right so if we uh exchange the node with neighbors of offset four then the first node and the fifth node they will communicate with each other right and second one with the sixth and as a
(30:16) result this one will have also color so all the eight colors for each node we have all the eight colors right and we complete this process how many Cycles did we take actually we have eight node but only three Cycles okay after Cycles every node has a data from everybody else so only log in steps rather than or in Step which is much faster right so this is called recur recursive recursive Haring or reduce people also call it butterfly or reduce uh because this structure looks like a butterfly but not exactly so just uh for
(31:03) n workers all reduce can finish within login steps so this is much widely used technique compared with a primet server like in our lab we don't do a lot of distribute tring but we don't use centralized parameter server instead we use this R reduce method okay okay so now let's switch gear to continue talk about data paradism uh to reduce the memory using 0o one two three and fully shoted distributed uh fully shoted data parm before that let's take a quick break for five minutes all right welcome back let's continue discussion about
(31:53) zero and IP so zero is actually a very popular technique for Lar the large language model these days it stand for um zero redundancy um Optimizer so let's see how that works so recently the large language model is pretty big like GB3 175 175 billion parameters uh if we train it even in fp16 so just to hold those weight how many how much memory do we need 350 gigabyte of memory just to hold the weight not to mention the activations the gradient the optimizer State well a100 GPU or h100 has 80 gigabyte of memory B100 is getting larger but still
(32:40) less than 200 right so even best GPU cannot Feit the models waitting waiting to memory um so how do we solve that problem using limited GPU memory to train big model so let's first break down what is the memory consump ion to tr a single parameter okay so we have can break down into three components the weight two bytes per weight assuming is fp6 train which is already pretty popular these days gradient uh two by fp16 we also have to sore those Optimizer States um including three part one is a fp32 copy of the parameter so weight
(33:27) requires a master copy full Precision Master Copy for the accumulation so that's four bytes we need another uh two two numbers for the optimizer state if you need uh to use the item Optimizer you need to sore both the first order and second order momentum um the two momentum require number and the master copy of weight is another number so Al together we need three numbers uh every um number is four bytes so we need all together 12 bytes for the optimizer uh States okay so this is Optimizer State um this is the gradient
(34:11) weight okay so Optimizer State actually occupies a pretty large portion of the total uh total memory so um this is the total sum summing up the weight gradient Optimizer State Al get together you need uh 16 bytes to store train for each parameter okay so we can calculate if do the training naely how many parameter can TR on a 80 gigabyte of memory even not to mention deactivation so 16 BYT for each parameter we can train only five billion parameter model not even a Lama 27b okay so only five billion parameters Which is far away from the
(35:06) current state of the art so how can we uh do better so previously we are having un copies of the weight gradient optimizer State across each GPU since we are doing data parm they are processing different data splitting the they're sharing the model so each GPU has the same copy 01 basically propos we can partition or we can call it Shard the optimizer state so uh since we have NG gpus we don't need to store a can only store the local copy for each um for each of them rather than storing and copies of them so if we have UTS
(35:58) um we no longer need to store 12 bytes each but we can store 12 divided by n so we are Shing the optimizer state if we have Nal to 64 uh we put Nal to 64 right here um each parameter require only 4.2 bytes and we can fit 19 bytes billion parameter in a single 80 gigabyte of GPU so that's 01 02 is more aggressive okay um Zero stand for zero redundancy Optimizer so 02 also Shard not only the optimizer States but also the gadient okay so the gadient gradient is also two ided n sh the gradient as well so each parameter
(36:50) require 2.2 bytes uh to train and 8 gigabyte of memory can TR 36 billion parameter model so that's much bigger so this already um pretty big 03 is even more aggressive approach by Shing not only the optimizer State gradient but also the weight okay so each parameter each server no longer have the whole copy entire of the weight it only have uh one over n of the weight so the is also shed splited partitioned across in uh gpus so the total number of parameter uh number of byes per node becomes 0.
(37:43) 25 bytes for 80 Gaby of memory you can now fit 320 billion parameters so that's a lot larger so this is the widely used approach um to train these large language models okay so we sh the weight Shard The activ Shard the uh Optimizer State Shard the gradient and also uh the weight across gpus so you can increase the trainable parameter from just maybe five billion parameters all the way to 300 billion parameters okay so in P torch this 03 is algorithm proposed by Microsoft is implemented fully shed data parallel okay it's known
(38:27) as FS DP very widely used Library these days uh feel free to try it out so for large language models um below like 70 billion parameter it's pretty safe to use this approach uh to train it okay so that's the uh zero and fsp now let's switch gear to talk about peline paralysing okay uh remember remember we have four paradism to talk about one is data paradism including communication Primitives and sharding and P Prim paradism trans parallelism and and sequence Paralis now let's Swit here to talk about second part which is
(39:11) pipeline paradism so with data paradism what are we splitting we're the data sharing the weight now we are splitting the data um instead of SPL the data we want to split the model B so may have many layers uh we can put each layer on one worker what is the good thing about lar language model is that different blocks different layers are identical number of hidden size identical number of has so each parallel pipeline stage are exactly taking the same time so which is very suitable uh for pipeline par if we uh split this 3 50 gab across
(40:06) eight card so each require only 43 uh. 75 gigabytes which is smaller than 80 Gaby so this is the memory required for um the 175 billion parameters of Weights so that's another dimension to to fit a larger model across uh within the limited GPU memory so implementation uh basically partition the TW data flow say we have four different layers and forward at 0 one 2 three and backward 32 1 Z okay we need to store the intermediate uh intermediate activations to calculate uh the backward and so this is the forward uh GP 0 1 2 3 and backward
(41:02) finally we can update so the model is splitted into four partition in this way and assigning um each partition to one GPU and the problem is that if we just process a single batch in this way gpus are underutilized a lot of idle bubbles in this case so what is the theoretical uh utilization here so only one quarter 25% utilization because most of the time GPU is Idle so what is the best way to do that say previously we have a big batch now we want to scale it with micro batch pipeline paradism what is saying that
(41:50) previously we have like 16 batches now we want to split it into four batch such four batches es so rather than naive paralyzation now we can make a look like this so small batch 1 2 3 four batches and then after gpu0 completes f z0 it can handle the data to gpu1 compute GP F10 and the gpu0 continue to talk to compute f101 okay so in this way at the bubble is a lot less compared with the naive implementation so we want to make the computation splited more fine grein in a fine grein manner previously 25% utilization Now
(42:37) using um micro badge it can improve it to uh 57% which is 2.5 x Improvement so lesson we learn here is that we want to split uh the TR in a fine GRE manner so beyond P planine paradism to split the model we also have another degree to split the model which is hensor paradism to split the model okay to further improve um the gr narrative to make a more fine grain um by partitioning further partitioning the model not just layer even for a single layer we want to continue partition that say a layer has 2 by uh 2x3
(43:27) Matrix okay so we want to this is the single FC layer right the single FC layer we want to partition that into three parts uh gpu1 gp2 gp3 and each GPU is calculating only one third uh of the Matrix and finally we can get the result in parallel so partition the model in a more fine grain manner uh within a layer and that fits perfectly for large language model so this is the illustration of tensor parallelism in Megatron so including both MLP layer and also the self attention layer uh here we want to minimize what
(44:10) do we want to minimize the communication right since Compu is cheap communication is expensive so we only need to uh communicate in f and g the start the end uh which denotes this all reduce synchronization operations okay so this is one is um scatter the all reduce operation and similarly communication only happens in the beginning and also in the end and let's see how that works first for the FFN layer so we have two F layers in large language model okay one F layer second layer uh we just give an example uh to be
(45:00) simple 1 by two Matrix this is the activation 1 by two and 2x six which is the weight of the first F layer we have two gpus in this case we splited way um each GPU hold 2x3 and two gpus that's 2x six right so we first do a broadcast okay the activation to two gpus GPU one gpu2 you perform half of the matrix multiplication on each GPU the other half of the second GPU and we get Y y1 and Y2 okay corresponding to here y1 and Y2 no communication is needed here and then we can directly move to the next section in the we
(45:52) are performing the second matrix multiplication okay which is of dimension 6X two okay so this is the activation from the previous layer and we have the Matrix 6 by two so six two right now we split it horizontally gp1 gp2 gp1 is responsible the first half gp2 is responsible for the second half and what we do here is perform y1 * B1 independently with Y2 * B2 okay y1 V1 Y2 * V2 okay so no communication is needed because y1 is associated with B1 there's nothing to do with B2 right and then we can obtain two uh intermediate
(46:42) outputs Z1 which is 1 by two Z2 two what is the final dimension of the matrix multiplication 1 by two 2 6 6 by two so the result should be 1 two right but now we are two get 1 by two ters so they are partial partial sums so we have to sum up the partial sum and now we have to do a communication we want to do all reduce operation to sum them up together Z okay so corresponding to here we do all reduce oper into some the Z so we only need a broadcast in the beginning and are reduce in the end in the middle these two Matrix
(47:32) multiplications are done in parallel across two gpus one and gp2 in this case so two uh Matrix multiplications corresponding to the two I layer in large language model is perfectly paralyzable and let's look at here in the middle of the MLP we discuss that it doesn't require any communication okay because it take uh the first Matrix is partitioned vertically the second is partitioned horizontally and there's no communication is needed in the middle and let's see how do we that for the attention layer so this is
(48:17) uh self attention with qk transformation uh soft Max and then then matrix multiplication uh two Matrix mulp ations finally followed by a um output projection layer okay so this is the uh Q KV uh transformation matrix okay we have q k v o we have four transformation matrix okay three for the input one the output so this is the three transformation for the input um the hidden Dimension is full and um so we have 2X six 2x for Q but for k the key and 2 by 2x six for the value okay input is two 1 by two 1 by two 2 six so that's perfect
(49:14) match so what we do here also uh split in the uh split vertically across two gpus so each GPU is responsible for only half of the Hidden um Dimension actually that's the head Dimension each GPU is responsible for one head this one is another head and we do a broadcast okay so X is broadcasted to gpu1 and gpu2 and gpu1 is holding uh the first half the transformation matrix for Q gq2 is responsible for the second half of the transform Matrix transformation matrix for the Q similar K and similar for the V okay we are
(49:56) broadcasting X in this way so we do the Matrix modifcation 1 by two 2x 3 so we get by3 1 by three corresponding to the Q KB um transformation and next um we want to do the attention Okay so it's Q time qk transpose times V okay there's a soft Max so we do um for the first GPU remember right here um the first dute already has q1 K1 V1 second D Q2 V2 and the K2 right so we are doing them separately for q1 K1 and V1 we can do the qk transpose soft Max time V okay transpose soft Max times V independently as the Q2 okay so qk
(50:52) transpose of Max * V they are independent and then we get y1 Y2 corresponding to here we get y1 and Y2 so far no communication is needed and finally we want to do the output projection remember we have q k v o four projection matrices now we are talking about the last output projection we have y1 and Y2 right here and this is the projection Matrix which is 6 by two input is 1 six so we split the output projection horizontally such that we can do the B1 with first half and B2 with the second half uh so y1 is Multiplied with B1 Y2
(51:40) is Multiplied with B2 and similar to before we get these two intermediate um outputs Z1 and Z2 and finally we do all reduce now need this operation uh our reduce operation we need this communication right here with do communication in the last end to together Z final okay so that's the whole process of splitting the in the head dimension for calculating the attention and beauty here is that we only need communication a broadcast in the beginning on communication which is all reduced in the end right here in G green and
(52:21) everything in the middle there's no communication needed across the two dpus which is um why this attention mechanism fits perfectly for multiple gpus and it's so easy to scale it up and moton actually made a huge contribution to uh scaling up this large language model training across multiple GPS I don't know if the designer of Transformer actually thought about this colonization process but it's very beautiful that no communication is needed in the middle um for both tension and also for the two I layers but this is a perfect example for
(53:02) algorithm and system code design for uh neuron nro acceleration all right we are uh three quarters in our journey okay we talk about data parallelism by paralyzing the data sharing the model we also talk about P plan parallelism and tensor parallelism um which is sharing the data and partitioning the model now let's talk about the last one paralyzation technique which is sequence paradism this part of the lecture is new this year don't have that yet in the last year uh because it's a very trending topic for handling long context
(53:45) long context is very crucial uh for example for natural language processing you want to understand long documents right long documents many books um complicated financial report legal documents Etc long context is also important for video understanding you want to understand the history um know when and what is happening at time it's also Rob important for robotics you want to have a long trajectory of what has happened in the past in order to predict what's going happen in the future so all that require lot of tokens and are very
(54:21) valuable this context L is also valuable but it's very complicated to train take a lot of GPU memory that's why we need sequence paradism to uh partition this long sequence this long context into multiple gpus so one GPU can handle only a limited amount of tokens again the rule of thumb is we want to minimize the communication maximize the utilization and let's see how can we partition that so this is the basic concept say we have a book of three chapters your chapters um maybe each chapter is say 10 10K tokens now we have 30 30k
(55:05) put one 10K for each GPU uh they are sharing the same weight model but different part of the sequence different part of the data uh question here is that um each GPU only has its own piece of data right for the query gp1 has first part gpu2 has second GP 3 the third part similarly for the key and values right so different GPU only has its own um uh qkv however for the attention mechanism uh we need to attend the first part of the document with the second part first part with the third part second with the second part with
(55:48) the third part the with third part right so that's a n byn Matrix which require a lot of communication you cannot just do the attention locally if you just without any communication between gpu1 and 2 three what's going to happen everything can only attend locally this uh chapter one will not see chapter two chapter 3 uh will not be able to see chapter two okay so we cannot um mingle the data anymore say you want to corate something happened in chapter two with chapter three you cannot do that only chapter three with
(56:27) chapter three chapter two with two chapter one with that's too bad let's see how do we solve this problem okay I'm going to introduce two methods feel free to come up this with third one you know your final project but solution one is popular technique called Deep speed releases uh which is very widely used technique these days to repartition the data in the tension layer so how do we do that let's see C so this is qkv transformation matrix so we have the three tokens just uh use that as example uh one two three and we uh feed the token one to
(57:15) gpu1 okay so token one is 1 by two transformation matrix is 2x3 uh so the result will be 1 by3 okay 1 by3 for the Q 1 K 1 by3 for the V okay so U tb2 is responsible for the second part of token okay second part of token the background is in green okay background is in green and the third GPU is responsible uh for the third part of token okay the background is in yellow and gp123 all share the same qkv transformation matrix so by multiplying this 2x3 Matrix with uh the 1X two by 2x3 we get 1x3 for the Q and for key and for V okay so far good we
(58:07) are um each GPU is responsible for only onethird of the tokens okay one3 tokens in different color and now we want to do the attention requires um the first token part of the token also attemp to second part second attend to the third part how do we that want to all to all communication right here okay so for all the queries so as previously in gpu1 or gpu2 and gp3 for the different heads right we have three heads in this case we have the uh light green we have dark and we have green right so now want to collect everything related to the
(59:01) dark green into the third GPU everything related to the light green in second GPU everything in the we call it normal Green in the first GPU right so um each GPU is responsible for um one part of the head now we are partitioning in the head Dimension previously we are partitioning in the sequence Dimension okay along sequence different part in Blue in Green yellow right now we are partitioning the high dimension this is head one two three in green light and dark green right now we partition in the the head Dimension so gp1 is responsible
(59:44) for head one everything related to Green so we just collect everything in the First Column which is the first head plct everything to GPU y so that we can compute the Q KV attention um complete attention for all the sequence right so it contain from token one contains token two contains information from token three right so everything related to the first head entire sequence now is in gq1 everything related to the second head for entire sequence now is in gp2 everything in the third had for entire sequence is in uh gp3 okay so we do the attention
(60:30) computation separately so this way you can see where does the communication happen communication happens here right uh we need to aut all communication okay so previously each GPU has green light green dark green now gpu1 has everything related to Green gp2 everything related to light green gp3 everything related to dark green so the number of GP equal to the number of partition in sequences and we chop the sequence into three parts we have three GPS and also equal to the number of hats in this example we have three
(61:10) HS so what's the limitation of this approach [Music] because you have to uh because we to first calculate the qkv transformation you have to calculate this Matrix modifcation before calculating the qkv transformation and therefore in the middle you have to do a communication to exchange data so everything related to head one is uh gathered in gp1 G that's exactly the case this is gp1 only has hi one gp2 only has everything had the tool but you cannot have that here because um here we're partition the sequence on different
(62:14) gpus not here GPU one has all the hats but only related to the first third of the sequence last only first third so that's why we need this such a shuffle yeah right exactly right yes that's the answer to why was the limitation here right uh the all to all communication is pretty heavy right here within a node that's pretty good I meia has NV link across the node um we have NV switch but communication every um in J is very expensive and the other limitation is actually what the max amount of paradism we can have for sequence you can have a limited
(63:07) amount of like segments but for the high you have a limited amount of has 32 16 has eight like that's the limitation you can have um the max amount of harism you have is limited by the number of attration has in the model which it's a fixed number after the model is trained so that's the ois approach uh and let me introduce a second solution which is also quite popular called ring attention in Google's paper I mentioned this Berkeley paper also mentioned this it's pretty powerful approach so uh we want to communicate
(63:48) such that uh the gpu1 sequence can also attend to gpu2 gpu1 also want see gp3 right so what do we does it remind you of the Ring are reduce right ring are reduce so you have a ring gp1 2 3 gp1 2 3 okay form a circle right here in the first cycle okay we just rotated by uh 1/3 such that uh we can communicate between GPU one and two such that we are no longer Computing only the diagonal but also attending um between the first three uh first third and the second of document okay so this the first third of the document previously on gpu1 uh now
(64:48) uh gpu1 gpu2 switched and see here previously is one two three now it switched it becomes 312 so the second third of the um sequence second third of the sequence now reside on gpu1 okay so still within gp1 we are do the attenion but now we are attending the uh blue document with the green one okay the first third with the second in another cycle okay we rotated one more time such that gp1 query will be multiplied with orange document the last third of document okay so we computed the tension in this corner okay so we put it together
(65:37) previously we only have the diagonal part which the blue document attend to blue blue document the first third attend to the first third now third attend to the second third and then the first third attend to last third of the documents so now we complete um the whole attention process so previously a lot of places are white meaning the attention has not been computed now we fill in the white with color meaning that we have computed and in another cycle we fill in everything Computing so uh that's the real attention using real attention to
(66:18) perform sequence paradism in this way it's not limited by the number of has as the max amount of paradism we can have um but this can effectively attend um increase the context L during training and iners so one interesting part is recently we've been combining um these two methods one is the what is this method the repetition you attention layers versus this reading attention into a 2d Paris by combining them since within a node HG use has higher bandwidth of communication bandwidth compared with between the
(67:01) node so between the node we can use real attention within a node we can use this ois method okay so in that way we can effectively perform 2D paradism by combining this two paradism method to utilize the hyrogen between node versus within a node and that paper is called long V feel free to check it out it's also open source all right and let's move on to summarize of today's lecture uh we talked about data paradism and sequence paradism they are both splitting the data okay sequence paradism is new due to this long context requirement for
(67:47) large language model we also talk about pipeline paradism split the model by layer and also tensor paradism split the model by tensor we also talk about this communication Primitives the reduce all operation and also this magic ring are reduced and uh this recursive having are reduced using log and complexity uh we also talk about 0123 and fsdp which is probably the most um popular technique uh for distributed training um in P torch and also how to implement P plan paradism and also tensor paradism in the context of
(68:31) large language model all right that's for today's lecture and we will also release the greeing for lab four and the final project uh some of you ask about what is the template for the final projects uh final report and also the proposal um so we use the new rips template so very stand standard uh Flagship machine learning conference basically should in include a proposal should include uh the uh motivation uh why do what kind of problem are you trying to solve why do you need to solve that problem um and also starting with abstract just a few
(69:16) few line probably 10 to 20 Lines Just abstract like the abstract of paper and then starting with the motivation um and also what is your method uh is the prior methods what is your method and what is the final result and how does your result compare with the prior art that's roughly the uh template of the final report and for the proposal you can just remove the result section and then that is the uh proposal if you have any questions feel free to uh come to our office hour and we'd love help you okay all right that's for today's
(69:54) lecture thank you
