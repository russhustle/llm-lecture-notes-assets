(58) EfficientML.ai Lecture 9 - Knowledge Distillation (MIT 6.5940, Fall 2024, Zoom recording) - YouTube
https://www.youtube.com/watch?v=-UmiOFLzG1o

Transcript:
All right, good afternoon everyone. Let's get started. Today we are going to talk about Efficient ML, Lecture 9: Knowledge Distillation. This is a new chapter after we have talked about pruning, condensation, and neural architecture search. Now it's another technique: knowledge distillation. Make sure you have finished Homework 1 and are already working on Lab 2—just a reminder.

Today we'll learn about knowledge distillation, which is another technique compared with pruning and quantization. We'll talk about what knowledge distillation is, what a teacher model is, what a student model is, and also how you match the logits, weights, activations, or gradients—whatever you want to match between teacher and student. We will also talk about self and online distillation, distillation for different tasks, and finally, another technique: network augmentation.

Let's get started. What's the limitation and motivation here? Again, we have limited hardware resources across different platforms. In the cloud, GPUs are super expensive and the memory capacity is also limited—80GB, 100GB, 120GB, etc. Not to mention mobile devices or microcontrollers, where you have a couple of hundred kilobytes of memory versus tons of gigabytes on servers.

Compared with neural networks on GPUs, we can run ResNet, ViT, TinyML, or MobileNetV2. The question is: how do we build tiny models that can fit very little memory budgets or even match the accuracy of those larger models? That's important for TinyML, and even for cloud use cases. These days, large language models are getting super big and we want to reduce the serving cost. Therefore, even on the cloud, knowledge distillation makes a lot of sense. We want to distill a 70-billion-parameter model or a 7-billion-parameter model, so we can save 10x the memory traffic for the weights.

That's the question we want to answer: can we use a larger model to guide a smaller model? We have a larger model—a teacher model on the left—and a smaller model—a student model on the right. Even when training these models independently, we can have very different accuracy, like 82% versus only 52%. The question is: can we help the training of tiny models with the help of these larger models?

This image is the illustration of knowledge distillation. First of all, we share the same input. The same input is fed to both a larger teacher network and a student. The teacher network is usually much larger, like Llama-2 70B, and the student model is much smaller, like Llama-2 7B. Given the input, they both produce some predictions—logits. When we are training the student network, we have some loss, either classification (maybe this is the most popular). What is new here is that we also add a loss called distillation loss—between the teacher's logits and the student's logits. What we are trying to do here is to match the logits of teacher and student. That's a new loss term we add.

What's the intuition for adding this loss? If we look deeper into the prediction, given the same input image—say, a cat—the teacher network outputs two logits: one for the class "cat," one for the class "dog." Logit for cat is 5, dog is 1, meaning that it strongly believes this is a cat. If you normalize them after softmax, the probability is 98% versus 1.7%. The teacher model strongly believes this is a cat. The student network's logits are 3 and 2. If you do the softmax, the probability is 73% versus 26%. The classification is still correct, so you don't have any loss coming from the cross-entropy loss because the prediction is correct. But you can tell their outputs are quite different: the teacher is more certain this is a cat, while the student model is less certain—only 73% probability.

Here, we are trying to match the prediction probabilities between the teacher and the student. That's the missing part from the cross-entropy loss. How do we derive this probability? That is through the softmax function. That's a review: exp(5) divided by (exp(5) + exp(1)), and similarly for the other class. The student model is less confident. Our goal is to bump up the probability for the cat and bump down the probability for the dog to match that of the teacher.

How do we do that? First, learn this concept called temperature. In the softmax, if temperature is 1, that's our original case. If the temperature is larger, then the output distribution—the probability—gets smoother. Rather than 98% and 1.7%, it becomes 60% and 40%. A larger temperature will smooth the output probability distribution.

Mathematically, where does the temperature T appear? In the denominator of the softmax. For example, if we apply a temperature of 10, previously exp(5) now becomes exp(5/10), and similarly for the denominator. This is the formal definition of knowledge distillation.

Neural networks typically use the softmax function to generate the logits to class probabilities, which is for different classes with a different temperature. The probability is exp(z_i / T) divided by the sum over all classes of exp(z_j / T), where T is the temperature. The goal of knowledge distillation is to align the class probability distribution between the teacher and the student. We are trying to align this probability distribution.

What can we align? What to match? What tensors can we match? Here we summarize a bunch of ideas. Let's go through them one by one, starting from the output logits. We have seen this figure: not only do we have the classification loss, but we also try to match the logits between teacher and student models, to match output logits as the output prediction.

How do we match them? We can either use the cross-entropy loss (we have two distributions: one for the teacher, one for the student), or we can do a simple L2 loss—just match the output logits directly.

What else can we match? Not only the output, we can also try to match the intermediate tensors, including the intermediate weights and also the intermediate features.

Let's look at them one by one. Here, we no longer have output matching; now we are matching the intermediate tensors. The teacher model's layer two, this is the student model's layer two. Here, we try to match the intermediate weights of these two layers. What is the challenge here? The teacher model and student model may have different dimensions: the teacher model is much larger, the weight tensor has larger dimensions, the student model is much smaller. What can we do? We can do a low-rank approximation, or we can learn a projection—like a 1x1 convolution—to map the student's smaller tensor to the teacher's larger tensor. The teacher network is larger, the student model is narrower. We try to match them by using this projection matrix, which can be learned, and then we can match the output after this projection with the teacher model.

Between the teacher weight and student weights, plus a transformation matrix: the blue part is a transformation matrix, just a simple fully connected layer to align the shape/dimension of the teacher and student. There are also other methods, like using low-rank to match, but we will skip that here.

We can also match the intermediate features, not only the weights but also the activations. Previously we were matching the weights, now we are matching the activations to match intermediate feature maps between the teacher and student models. The intuition is that the teacher and student networks should have similar feature distributions, not just at the very end (the output probability distribution), but also across all the layers—at different places, the feature maps should match.

We talked about weight matching, activation matching. What is last? Gradient matching. We can also try to match the gradient from teacher to student. But they are of different dimensions, so matching the teacher and student gradients may not work. Beyond the weight gradient, there's also the activation gradient during backpropagation. The backpropagation part is really much slower than the forward part because it doubles the computation: one is computing the weight gradient, the other is computing the activation gradient. If weight gradient is hard to match, the activation gradient—in particular, the activation gradient for the very first layer (the input)—since teacher and student share the same input, this is the intermediate attention map. We backpropagate the gradient all the way to the input image. This is the input image and its gradient when we are backpropagating from output all the way to input; this is the gradient on the input tensor.

The attention map is defined as the partial derivative of the loss with respect to the input. If the gradient is large (like here in the nose, eyes, face of this box), it means that just a small perturbation in this area will significantly impact the final output, meaning that this area is very important for making a decision. At position (i, j), the attention over there is defined as the gradient with respect to that pixel. What we are trying to match is this attention map from the teacher versus the student. They can be easily matched because the gradient dimension is the same as the image, and both teacher and student share the same image.

Given different networks, what does the gradient look like? The second row is ResNet-34, the third row is ResNet-18—they are of the same family. The first row is a different network family. You can see that networks of a similar family (third and second row) have very similar gradients, while compared with the first row, the gradients differ a lot. Attention maps of high-performance models (both above 70%) are indeed similar, but less performant models (like one with only 62%) are quite different. Therefore, when we are doing distillation, we try to match those intermediate feature maps—dL/dX—trying to match the gradient with respect to the activation.

Any questions so far? We talked about matching the weight, matching the feature, matching the gradient, and also matching the output. We can also match the sparsity pattern. How do we match the sparsity pattern? First, where does sparsity come from? If we have a ReLU activation function, then there is sparsity. We want to match the sparsity pattern after the ReLU activation function: a neuron is activated if it's larger than zero; if not, it becomes zero. At every feature map, we try to match the teacher's sparsity pattern versus the student's sparsity pattern.

We can also match the relational information. What does that mean? Previously, we have been looking at each tensor and the output tensor, the gradient tensor, weight tensor. Now we want to look at two tensors, not just one. For example, this is a teacher and student: the teacher has 32 layers, the student has 14 layers. They both have three stages; the teacher has more residual modules per stage (five in the first) versus the student (only two in the first stage). The teacher has five in the second stage, the student has only two. Each has the same resolution, and the different stages may be downsampled by two compared with the previous stage.

What we're doing here is that, although they have different numbers of modules (five versus two), and different numbers of channels (the teacher may have 1K channels, the student 512), the relationship between input and output of this module is only dependent on the number of channels. For example, we can calculate the relation between the input and output, which is of dimension C_in x C_out, since we did a reduction on the spatial dimension for both teacher and student networks. We can have a C_in x C_out tensor for the teacher and the same for the student, and we are trying to match the relationship between input and output for both.

We can also match the relationship not only for different tensors but also for different inputs and samples. Here we have three samples: bird one, two, three. Previously, we just tried to match the distance between the teacher of bird one and the student of bird one, teacher of bird two and student of bird two, etc.—they are independent. Now we try to match them jointly: T1, T2, T3 from the teacher versus S1, S2, S3 from the student. They should have very similar relationships between each other. For example, T2 is on the right of T1, so S2 should be on the right of S1. In the previous loss, it doesn't care whether S2 is here or within the circle—330 degrees, it doesn't matter—but here, the relationship matters.

Conventional knowledge distillation focuses on matching the features or logits on just one input. Relational knowledge distillation looks at the relation between intermediate features across multiple inputs. This is tried on image models. For your homework or open-ended projects, feel free to try this on large language models—whether it makes sense to use this relational knowledge distillation across multiple inputs when compressing large language models or visual models. Maybe this method can shed some light.

Mathematically, we have multiple samples. We calculate the output distance between the student's first sample and second, first and third, first and nth sample, etc. Altogether, how many terms are there? We have n\*(n-1)/2, since it's pairwise, representing this pairwise distance of feature vectors. On the bottom, we compare: this is individual knowledge distillation, where we just care about the distance between T1 and S1, T2 and S2, TN and SN. Now, we not only care about them independently, but also the relationship among them. We have to calculate, given a tensor, the distance with all the previous input samples. That's the difference between the relation within the model calculated separately for each input versus calculating the relation across different input samples.

That's all for the second part: what to match—the gradient, the weight, the activation, the logits, across different input samples. For that part, we have one teacher and one student; we require two models. What's the problem with that? Especially when the scale is larger, you have to first train a very big model—a giant model. You have to first have a teacher before you can train a student, but training this teacher could be very expensive, like training Llama 70B. If we don't have the teacher, how do we apply knowledge distillation to begin with?

That's what we try to answer in the third part of this lecture: self and online distillation. Just like self-learning, you don't have a teacher; you just learn by yourself or together with your classmates. For previous distillation, we have to train it once: first finish the whole training process, get a high-performance teacher model, and then start the distillation process for the student model. But now, we just want to do it only once—online distillation: you distill while learning.

That's the opening motivation for the third part: self and online distillation. Let's see how that works, starting with self-distillation. The teacher model, like we mentioned, is usually larger than the student model and is fixed. The disadvantage of a fixed large teacher model is that you have to train it, which is very expensive. Does it have to be the case that we have a fixed large model in knowledge distillation? Actually, not necessarily—it can be of the same size; it doesn't have to be a fixed large size.

This project was called Born Again Network, and it uses iterative training stages, using both the classification objective and the distillation objective. What is interesting here is that the teacher and student model size—the architecture—is exactly the same: same architecture, same model size, not bigger teacher and smaller student; they are the same size. What it does is: first, in step zero, use only the classification cross-entropy loss. In the second step, use the probability loss to train the student and also match the logits from the teacher from the previous step versus the student from the next step, and so on. Each time, each step, we try to train with the cross-entropy loss versus distillation loss with the previous step. We use the previous step as teacher to guide the next step, and as a result, the students get better and better. When the training finishes in the last step, since these architectures are the same, we can ensemble predictions together to get even better performance.

That's the interesting part, where the teacher and student are of the same size. We can also do online distillation. Online distillation means we have two networks—they can be the same size; not necessarily teacher is larger than student. They are trained from scratch, rather than first training the teacher model and then doing distillation. Now, we don't have a teacher or a student; everybody is just starting from scratch, just like in a classroom with you and your neighbor. Network one is the teacher of network two when we are training network two, and network two is the teacher of network one when we are training network one. They act as the teacher of each other, which is interesting.

You have two models of exactly the same size. What you do is add the loss—not only the cross-entropy loss (for student and teacher, with Y as the ground truth label), but also the KL divergence (the knowledge distillation loss), treating the second model as the teacher and the first model as the student when you're training the teacher. When training the teacher, you not only have the cross-entropy loss, but you use the student model as the teacher and the teacher model as the student—learning from each other. The interesting part is that it's not necessary to pre-train the teacher and student. The teacher can be initialized and trained from scratch together with the student, and the student can be the same size as the teacher. You don't necessarily need to have a larger teacher, which is interesting.

When we are training, P1 is the teacher when training P2, and P2 is the teacher when training P1. People call it deep mutual learning. On CIFAR-10 and CIFAR-100 datasets, deep mutual learning can improve both the student and teacher models. The DML (middle row) shows the performance after this online distillation compared with independent training.

We can also have a combined approach: combine online distillation and self-distillation. Here, the title is "Be Your Own Teacher." We don't have a separate teacher; we don't have to train the teacher from scratch and then have a pre-trained teacher model to guide the student model. We can train from scratch using deep supervision plus distillation. We use the deeper layers' output as the teacher to distill information for shallower layers. The teacher and student no longer come from two different models (one large, one small), but from the same model at different places. The deeper layers' output will be the teacher for shallower layers. The beauty of this project is that you don't have to maintain two copies of the weights; you just have one copy. Otherwise, you would have to double the GPU memory, which is expensive. Now, you only have one copy of the weights, and the teacher is very simple—just a deeper layer to supervise the shallower layer. The intuition is that the labels at a later stage are more reliable, so the authors use them to supervise the predictions for previous stages. No teacher—you are your own teacher, just using the deeper layer to supervise the shallower layer.

Let's see how that works. The first row is a baseline. If we use the predictions from intermediate classifiers, sometimes they actually outperform the baseline, which is quite interesting. Inference efficiency can be improved because we don't have to wait until the last stage; maybe we can exit early when the confidence is high enough.

That's all for the first three parts. We'll take a break before we talk about distillation for different tasks and network augmentation.

All right, welcome back. Let's continue to talk about knowledge distillation. Now we want to apply knowledge distillation to different tasks to solve real-world problems. Let's start with object detection. What is object detection? You try to find the bounding box for different objects. You not only want to give the class (what it is), but also tell me where the object is—give the bounding box for the location.

We can see the distillation pipeline is more complicated here, but it still includes the basic components, including matching the features and feature maps. Here, we can use a 1x1 convolution to match the shape, just like we discussed before: the teacher model versus student model may have different dimensions, so we use this 1x1 convolution to match the dimension so that we can subtract tensors of the same size.

What else do we do here? We use a weighted log probability to differentiate the background versus the foreground. The cross-entropy loss previously was just the teacher probability and student probability. What is new here is that we added a class condition to have different weights for the foreground and background. For the foreground, we really want it to be accurate; for the background, we can allow some mismatch. That's why we have a weight here to differentiate the foreground versus background classes.

The next part: we also want to predict the bounding box. It's not just a class, but also its location—x1, y1, x2, y2, given those corners which define a bounding box. What's new here? There's an if branch. Y is the ground truth, RS is the prediction from the student, and we try to minimize the difference between the student and the ground truth. If the student's error is larger than the teacher's, then we apply this loss; otherwise, if the teacher is not as good as the student, it doesn't make sense to be a teacher. At that time, we set the distillation loss to zero. This is interesting: we add an M term, which is a buffer, to make sure the student's error is within a threshold of the teacher's error. If the teacher is having a smaller error compared with the student plus a threshold, then it can act as a teacher; otherwise, we set the distillation loss to zero to make sure the quality of the student is within a threshold of the teacher.

There's another way to formulate the bounding box prediction. It used to be a regression problem because the x, y location for the object is continuous. We can also convert it into a classification problem. How do we convert that into a classification problem? We can chunk the y-axis and x-axis into several bins. Here, we have six bins on the y-dimension (1, 2, 3, 4, 5, 6). We can also divide the x-axis into six bins. Now we have 36 blocks, and if the predicted x, y location falls into the correct bin, then that's a correct prediction. That's converting a continuous value into a discrete value. Once it's discrete, we can use the cross-entropy loss to treat it as a classification problem. Here, we have 36 classes: for x, we have six classes, and for y, six classes. The ground truth should be the second class, so we can use the same cross-entropy loss to supervise this detection problem for discrete locations. Now we try to match the distribution of class, which is basically the location bin.

Now, about semantic segmentation. Segmentation is a new task: we try to give a label for each pixel—pixelwise prediction. Again, we can have the same feature limitation, similar to classification and detection. What is new here is that we can add a discriminator network to provide adversarial loss. The student is trained for the discriminator network. We have a real embedding from the teacher model and a real embedding from the student. The student gives a fake embedding. We try to fool the discriminator so it cannot distinguish between the real embedding and the fake embedding. We also try to encourage both the teacher and student to be as accurate as each other, so the discriminator cannot tell which is fake and which is from the student. If the discriminator cannot distinguish them, then the output given by the student should be very close to the teacher. That's the adversarial loss as distillation.

We can also use these distillation losses to compress GAN models. In this work we did a couple of years ago, for GAN compression, we have two models: a teacher model and a student model. First, we can apply the distillation loss through intermediate feature maps. The teacher has a larger number of channels, the student has a smaller number of channels. We use a 1x1 convolution to match the channel dimension. Next, we have a reconstruction loss to measure the pixelwise difference with the ground truth. Finally, the third term is GAN loss (conditional GAN loss). We want to make sure the teacher and student are close enough so that they can fool the discriminator into thinking both are real. The training objective is to add these three loss terms together to supervise, so that this small student model can perform as well as the large teacher model.

Here we have a demo after compression using GAN compression. On the top is the original CycleGAN, which turns a horse into a zebra. Unfortunately, it runs very slowly. This is deployed on Jetson Xavier GPU, a very small mobile GPU, and you can see it's very slow: FLOPs is 56G, only running at 12 frames per second, FID is 61.5. But after GAN compression, the FLOPs are reduced to only 3.5G—that's 16 times lower FLOPs—and frames per second increased to 40, and FID also improved to 53 (original was 61).

Here is another demo: interactive edges to shoes. We just draw these edges and want to convert them into shoes. Now we want to erase the logo, try to change from Adidas to a Nike shoe and see how that works. Originally, it was pretty slow—this is running on an even smaller Jetson Nano. Previously, it was running only 1.6 frames per second, and you can see the job is very difficult because it's not interactive—too slow on this edge device. Since the frame rate is so low, it can only sparsely sample a few frames, barely looks like a new shoe. After applying compression around the smaller model, the erase becomes much smoother. On the right-hand side is the GAN compression, the left-hand side is the baseline. Now we try to draw another Nike shoe; now we can run 3.9 frames per second. It's much smoother, same hardware. The speedup is about 2–2.5x, which is quite good. Everything is deployed locally on this Jetson Nano GPU. We have a bunch of them; if you are interested in this project, feel free to talk to us—we can lend you a few such mobile GPUs.

We talked about vision tasks: distillation for image detection, image segmentation, image generation. Let's now talk about knowledge distillation for natural language. In natural language, attention is very important. We have a projection for QKV attention and the output projection, feedforward, a couple of layer norms. What we can try to match is not only the feature map, but also the attention map, which is O(n^2). This is showing the attention map: the teacher (first row), the attention map is always square because it's number of tokens by number of tokens. This is the student with attention transfer versus without attention transfer. With attention transfer, the attention map is very similar; without, it's very different. These two are very similar; this is quite different. In addition to feature imitation, the transformer architecture gives us another opportunity to try to match the attention maps.

Knowledge distillation for large language models and also vision-language models: training and distillation can work together, as shown in this work, to obtain much smaller large language models. Here is the cost of training a model, measured in training tokens. For example, Llama-2 here uses trillion tokens, this Jamba 7B uses 2.5T tokens, and here we can reduce the number of training tokens by a large margin while maintaining accuracy compared with training from scratch. The Neotron-3A has 40 times cheaper training and 99% better accuracy on LLM score.

Here is how the whole process works. On the left-hand side, it contains a pretraining data mix versus synthetic mix, to train both a pretraining model and an instruction model. This is instruct two pieces: without instruction tuning, using the logits coming from Llama-3 8B and also 3 70B—these two models, one 1B model. The instruction tuning data comes from synthetic data generated by passing prompts through Llama-3 400B and Llama-3 to get some synthetic data, and you concatenate that with some fine-tuning data, all together to train this instruction model.

That's the overall pipeline. How do we select the student model? We select the student model using the pruning technique we talked about previously, using magnitude to rank those important channels. This is the work where Minong applied knowledge distillation after pruning the model. The student is a pruned version of the teacher model. Initially, we have a pretrained large language model (trained for many years), and then Minong estimates the importance of embedding windows, ranks them according to importance, and then throws away those less important channels/tokens, etc. Then, distill using this as a student and the original as a teacher. It can match a lot of different tensors: embedding, MLP, etc. The distillation is not only matching output logits, but also trying to match those intermediate features.

We talked about distillation for different tasks: both vision tasks and NLP tasks. Finally, let's talk about network augmentation, another related technique. We try to improve the accuracy and quality of a small model. That's the central topic of this lecture: how do we improve the accuracy of small models?

Data augmentation is very widely used in training to avoid overfitting. For example, if we have a limited amount of data and we want to augment it so that it seems like we have a larger amount of data. Typical ways of doing data augmentation for vision tasks include cutout (cut part of the image—humans can still tell this is a dog), mixup (given an image of a cat and a dog, cat label [1,0], dog label [0,1], we can mix them up half and half, just pixelwise add them up, and then the label would be [0.5, 0.5]). Originally, we have only two images; now we have a third image. There's also AutoAugment, where we can tilt or color images with different policies, both spatially and in the color domain. We can also use AutoML to automatically find the best augmentation policy.

Apart from data augmentation, another way to prevent overfitting is using dropout. During training, we randomly select a few neurons and zero them out. People can also do spatial dropout to randomly remove a few channels, and also dropblock to remove a few blocks at a coarse granularity. As a result, these techniques like dropout and data augmentation can greatly improve large neural networks' performance. Here is the performance of ResNet-50 (that's 4G MACs) on ImageNet top-1 accuracy: baseline about 76.5%, after mixup almost 78%, after AutoAugment more than 77%, by using dropblock more than 78%. For these large models, dropout and data augmentation really help because large models tend to overfit, so dropout and data augmentation prevent overfitting.

However, for tiny models, we did the same experiment. Unfortunately, both dropout and data augmentation actually hurt the accuracy. The more techniques we add, the worse the accuracy. Adding mixup hurts, adding AutoAugment hurts, adding dropblock hurts. Previously, when it helped most, now it hurts the most. We suspect the tiny model lacks the capacity—it doesn't suffer from overfitting, but from underfitting. We don't have enough capacity to learn so much knowledge for the tiny network.

Contrary to image augmentation, now we think about: can we do network augmentation? To augment the model to get some extra supervision during training for the tiny model. In the end, we still want to deploy a small, tiny model (like here, only with two channels rather than four channels), but during the learning process, we find some redundancy helps. If you can sometimes have four channels, sometimes three channels—not just these two channels—it is helpful. For example, once you get stuck in a local minimum, some extra dimension, some extra degree of freedom can help you get away from the local minimum. Indeed, network augmentation for the tiny model (this is MobileNetV2 Tiny with only 20–23M MACs, here we have 4G MACs—two orders of magnitude difference) improves the accuracy.

How does that work? This is the view: we have a very small model with only two neurons (in this case, three layers, two neurons), and then we build an augmented model out of this original model, very similar to the width multiplier for networks. We start with a small model and then expand it in different dimensions: channel dimension, depth dimension, etc. We build an augmented model. The same principle can apply to large models by increasing the number of layers or channels, etc. In the augmented model, we compare the base model with the augmented weights and base weights—they are actually shared for the red part here. Then we get the gradient both from the base model and from the augmented model, and we add them up together. This is the base supervision and the auxiliary supervision—we add some extra supervision to the base model. At different steps, we can sample different sizes of the network: sometimes a larger model, sometimes a smaller model. What doesn't change is the inner part—they all share the same weights with the original small model.

As a result, we can see the learning curve for both: here is training, here is accuracy for validation. It improves the accuracy both in training and validation for the tiny model. But for the larger model, it actually didn't help, but hurt the accuracy. For training, it helped, but for validation, it didn't help, meaning that the large model already has a pretty big capacity—it doesn't suffer from underfitting, but overfitting. What they should do is augment the data, not augment the model, which echoes our previous assumption that large models are always overfitting and small models underfitting.

Here we are showing network augmentation is actually orthogonal to knowledge distillation. The left bar is the baseline, the middle is knowledge distillation, the right is network augmentation, and the last is network augmentation plus knowledge distillation working together. Also, for transfer learning, we compare two scenarios: one is knowledge distillation, and another is just training longer (4x). Sometimes, the accuracy hurts using these techniques, but using network augmentation provides better transfer learning performance than knowledge distillation and also than 4x training schedule.

We also tried to apply network augmentation to object detection—not only classification, but also other tasks. This is the baseline: x-axis is number of MACs (how much compute you have), y-axis is accuracy. Here is the focal accuracy. For the same model, we can improve accuracy, and we can reduce the MACs if you want to achieve the same amount of accuracy.

The limitation here is that it only explored vision tasks. This work was done early 2022 (after submitting in 2021, before this trend came). It might be very interesting to try such ideas—network augmentation—to train those small large language models. Welcome to talk to me if you are interested in that for the final project.

All right, let's have a summary of today's lecture. We shifted to a new chapter about small models. Apart from pruning and quantization, now we are talking about knowledge distillation. It needs a teacher and a student. We can match several components: the final output, the intermediate feature map, the intermediate weight, the intermediate gradient. Not necessarily do we need a teacher model; we can also do self-distillation and online distillation, so that we can train only once and distill during this training process. Such knowledge distillation can work for many different tasks—not just classification, but also detection, segmentation, GANs, natural language processing. We also talked about network augmentation, another technique to improve accuracy by augmenting the model.

In the next lecture, we'll introduce MCUNet, which is algorithm-system co-design for TinyML, so that we can deploy them on microcontrollers.

All right, that's for today's lecture. Thank you.
