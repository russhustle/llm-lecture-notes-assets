(75) EfficientML.ai Lecture 11 - TinyEngine (MIT 6.5940, Fall 2024, Zoom Recording) - YouTube
https://www.youtube.com/watch?v=qSsQifiHDU4

Transcript:
(00:07) all right good afternoon everyone let's get started so today we are going to talk about efficient amount. a lecture 11 tiny engine and parallel processing uh so the lab one score of lab one was released so feel free to check it out it's been it's been fully regraded um so this lecture is quite important for our last lab which is lab to uh deploy a large language model locally on a laptop so uh it's pretty important lecture so today we are going to uh first talk about some conings of II why they're essential and then a lot
(00:46) of parallel Computing techniques which is the focus of today's lecture including Loop op Loop optimization mod threading CD programming puta programming um after that we're going to talk about inference optimizations such as image to column in place steps wise convolution how to choose the best memory layout and also using the window gr convolution so very briefly a recap of the motivation which is ai ai has very a lot of applications such as here in smartphones in the vehicles in robots in the smart smart office ET
(01:31) but II has limited computational resource constraints so here we compare uh the cloud GPU the appm tool the desktop the mobile device and also the microcontrollers so the memory for GPU 80 gab with the M1 M2 maybe 100 gab mobile device 8 to 24 gab but microcontroller has only 300 kilobytes and also the compute power 2,000 TS per second 30 TS per second and only several hundred million Ops per second so several ERS of magnitude difference which motivates us to efficiently program these devices so here is the comparison of the
(02:21) computational resources comparing our MacBook micro Pro versus a a microcontroller okay so here this is the micro controller this is the MacBook Pro and this is the difference so we categorize them by the number of CPU cores um One versus 20 okay the clock rate is also differ a lot uh 200 mahz versus 3 gahz uh microcontroller has no GPU core no neural engine core our cach is really small on 8 kilobyte no our two cach or three cach versus the my f CPU has a lot of L three cache the memory capacity a few hundred kilobyte versus 64
(03:09) gabyt storage also differ a lot C and what's sad here is there's no operating system so you have to manage the memory by yourself and here is the memory hierarchy uh comparing uh the speed the latency and also the capacity as you go from register I want out to Cache to memory to the storage we get a larger and larger capacity from a few hundred kilobyte all the way to few terabyte memory here can go to actually a few actually much larger than this on the server and the latency also gets longer okay so um you don't want to go
(03:51) to theam um you want to stay in the cash as much as possible so that's some recap of the what will we learn in 6.1 one in the computer architecture class which is a prerequisite so given this constraints we want to efficiently program uh these processors such that it can efficiently uh handle this this neuron Network workload so let's talk about how to uh exploit uh the paration to accelerate the neuron Network inference in between it so here are the four blocks where going to discuss today uh you are going
(04:33) to implement uh every one every technique here in lab five so pay attention to these techniques we are going to have a real world impementation so we'll start with roof optimization okay so there are several for Loops a matrix multiplication is basically three for Loops a convolution is basically six for Loops optimizing Loop is very essential for parallel processing and our main target is to optimize for the locality okay catch locality and reduce the branching overhead okay uh try to um avoid the branching so we'll cover uh loop
(05:13) ordering fighing and rolling and next we are going to talk about CD instructions single instruction multiple data and also multi threading and finally Coda programming so here we are going to start with the first one Loop reordering to optimize the locality by reordering the loop orders or the sequences of the loops so let's see this example where we have a um naive implementation of a matrix modifcation uh the dimension is n byn times n byn Matrix you get another n byn Matrix you have three for Loops i j k c
(05:58) i j plus equal to a i k a * pkj okay so what is the order that we access the two matrices so for the a matrix we we order we Traverse them in the i k manner okay I is outside K is inside so we go to k first then I K first and then I but for the B Matrix okay for the B Matrix the order is K uh hey J K is actually inner J is outer so actually we are doing it in the vertical okay so they are not store continuously in the memory for example here B 00 and B10 they are not continuous in the memory so you fetch when you are
(06:41) fetching B z0 you feel in the patch line containing be 00 01 02 which US should have learned those in our prerequisite 6.1 i1 in theu architecture course so I will not go deeper into the what the cach line is assuming you have already have those background in the prerequisite so it will put in B 00 0 B 01 b02 in the cach line but B B10 will not be the C cach line of B z0 so it has very poor data locality for the B Matrix so we can reduce the cach M by reordering the loop for B for example rather than using j j k you can do the
(07:26) loop order of ik KJ and let's see what is the order now for the B Matrix okay so this is before this is after before we have i k after we have ik KJ okay so previously a is horizontal B is vertical now a is horizontal B is also horizontal because here we are accessing the KJ K is the outer loop J is the inner loop therefore the access pattern will be horizontal okay and horizontal values are sto stored in the same cach line improved this locality so what is the cach here the locality was what what about the
(08:08) locality C did it get better did it get worse for the C Matrix yes yes exactly using the previous approach uh C is i j so they're in the alter Loop so we can just focus on Computing on one C all the A's all the BS or associated with that particular C element once it's done we move forward to the next C so very good output locality but here we have many partial results for C so C the locality for c will be worse compared with the first implementation however in this case the gain coming from B Matrix out ated the
(09:01) what we lose for for the C locality and as a result changing the loop order for B will reduce the latency from 24,000 milliseconds to roughly 2,000 milliseconds pretty significant speed up speed up this is measured on Intel time 44 CPU or server gr CPU okay and this implementation is actually quite simple we get the a b c and previously we use J JK now our order is ik ikj and here we allocate a a buffer to uh we need the actual buffer for the intermediate results so this is showing the power of Loop and ring very simple change um
(09:49) couple line of code change but it can make a very big difference so to help you um make a deeper understanding we prepar a the full starter code so this is a s of the code to help understanding about the full code actually put in here will come to check it out it's compilable you can simply compile it and run on your on your laptop give it a try Okay so that's using Loop aling to speed up the Matrix modification second Technic loop I okay so the cat size is limited so we want to optim the locality by partitioning
(10:33) partitioning a big loop into several smaller Loops so what if the data is much larger than the cach size which is usually the case right the cach may be a few megabytes maybe 10 megabytes 100 megabytes but the Matrix could be much larger like 4 4K by 4K that's very standard size even larger size is very tall so the C will be pretty large if the data cannot fit in the cache so Lo Tiding basically partition the loop iteration space you can use several smaller blocks to iterate over okay and we can fit the working set fit what we
(11:19) are going to access within the cache when we are choosing the size um of the granularity of the uh blocking okay of the tile of each time for example uh this is our example Baseline simple naive implementation of a matrix multiplication uh the number of elements in a b c is all in square right so the X as elements in the B Matrix is a square that's could be pretty large but we want to Ure the data can stay within the within the cache until it's rused so what we we can do is rather than accessing n Square memory footprint for the B Matrix
(12:06) here we want to TI it rather than n Square we want to do n times the tile size okay n times the tile size that we want that to be our footprint so we can um TI It by adding one more for Loop right here previously there are three for Loops i j k now how many for Loops we have four for Loops here okay IJ J K and also there is a JT J TI okay we added one more uh for Loop in the alter Dimension where TJ equal to T size we also changed uh the the range okay for the J so that I only cover the range of tile size and then
(13:00) here we skip every P size for each iteration so by adding a fourth for Loop we can tile the B Matrix such that the memory footprint gets smaller can we uh do even more tiing what if e times the tile what if n times the tile size is to to Big so rather than n times tile size we want to have a footprint of tile size Square so we we do rather than four for rops i j k and and JT we want to do add J K JT and KT add one more for Loops to further P not only the J Dimension but also the K Dimension so not only the J
(13:51) Dimension but also the K di so the memory footprint for B would be P size Square and the memory footprint for a since we are tiing the J Dimension the memory footprint would be n times the tile size so compared with the Baseline three for Loops now we have five five for Loops so we also wanted this one the footprint here to be T SI Square so we can do one more level of iding so let's see previously we have five for Loops a i j k uh JT and KT now let's add one more for Loop i j k JT KT and it and we TR on all three dimensions so the um the
(14:48) access memory footprint for a rather than n times the pile size and times the tile size now it becomes tile size Square so now we are done with all the for three for loops with and their size is T TT uh right here so now what is the uh elements Access Memory footprint for ab b c previously they are all in square now they are all tile size Square how do we determine the tile size we want to make sure the T size Square which is the member footprint for ABC the to size Square can fit the cache right such that we want to determine the
(15:39) to size such that to size Square can fit the the cach size and if that is the case we can reuse data since you're already there in the cache then we can just iterate whatever you have without having to load new data so that's the magic for Loop tying helping us improving the locality and also reduce the cash Miss what if we have multiple levels of cash so here we just assume we have a single level of cach we want to make sure tow size Square they fit in the cache what if we have two levels of cach we can add another for loot to
(16:22) another level of Tiding right so previously here we are doing ID K it KT JT so this is one level and one level of tying we added three for Loops actually we can add one more okay tile two we have tile to size to do multi multi level of Tiding so um we can make sure the tile size Square can fit in the L1 cache and tole two size times the tile size can fit in the L2 cach okay so that's way how we handle the multiple level of caches just add one more for Loop add one more Tiding so here is how we Implement that
(17:05) in real life feel free to check out the whole code in this link um is compilable so this is the uh six for loops with h in the IG K Dimension these are the original Three dimensions and before the tiing um that implementation takes 24,000 milliseconds to imp on CPU but after time is 19 times faster it's 1,200 milliseconds so feel free to download the code and try it your laptop okay so we talk about Loop reordering we talk about Loop piling now let's talk about Loop and rolling so in processors we want to reduce those
(17:51) branching overhead you know if your branch prediction is wrong you have to um you have to squash a lot of um instructions as we learned in the computer architecture class right so branching is very expensive so we want to minimize those branching overhead um at the expense of increasing the binary size and why is that the case I see an example uh so first of all where does the overhead coming from uh for example when you have a full loop at the end of the for Loop every time you have to test whether the the loop index is within is
(18:31) out of the is it out of the bond if it is the case we have to jump right that will introduce the branch prediction the branch predictor is not 100% accurate uh if it is not accurate it will uh squash a lot of waste a lot of Cycles so we can reduce the overhead by Lop by ring replicate and replicate the loop Body by several times so within this Loop body you don't have to uh you don't have to have any branch instruction you have to you can just sequentially execute uh until you reach the end of the follow but you can imagine that will
(19:11) result in a lot of duplicated instructions uh which will enlarge your binary sites so this is the our simple Baseline model or Baseline code let's see what if we I wrote it by four times previously have IG K from Z to n now we replicate by four times um AI K AI plus 1 a plus 2 A Plus 3 we explicitly write those indexes adding them up replicating the for Loop without having to do any branch instruction okay no Branch instruction here um but here the step previously we implement the pointer by one now we increment it by
(19:57) four okay that's why we are are loing 8 by4 so what is the consequence right so the arithmetic operations for pointers is reduced by four times okay reduced by four times what about the number of loop loop tests Loop tests have happens at the end of the loop previously every one instruction you have to do a test now every four of them so reduced by four times what about the codes size of the inner loop previously you have one instruction now we have many four times the instruction here so the code size becomes four times so that's the trade
(20:39) off no fre lch so this is the example again you can download the code here uh so we are unrolling the J Dimension by eight also unrolling the K Dimension so we just replicate replicate the code of AI a plus 1 K plus 2 plus three and also J and j+ one we exp explicitly write them out and this is quite helpful previously without Lo and rolling it R 24,000 milliseconds now it's only 8,000 milliseconds that's right you have to ramp up and also ramp up so you have to take care of the p is where the size of the for Loop
(21:31) is not multiples of your tiing size but usually in neuron networks the dimension is the to to the power of something so it might be easier not difficult to uh R that's so basically these techniques Advanced compiler already includes this so this is what we are learning how that works a lot of them are already been taken care by the compilers however the compiler optimization may not be uh good for all matrix dimensions so you have a particular Matrix Dimension you will see really high the optimization will be
(22:16) faster especially those tall and theme Matrix versus this short and fat Matrix all right the next technique uh simd technique which is also quite useful you will find it super useful when you are doing lab five to implement Lama 2 on your laptop using the S instruction okay single instruction modle data programing so let's first give a overview of the instruction set architecture which we learn in the comp architecture class so the ISA is actually the interface between the software and the hardware and in
(22:54) abstract give you abstract of a computer by defining how the CPU is code by the sofware so here we have several examples of Isa add add two numbers compare with sler jump you to a particular address condition jump if some conditions might load from memory Flo to the memory and IO instructions so this is the hand shaking point between the software and the hardware and there are two kinds of isas one is the cisk one is the risk uh CIS the C stand for complex a complex instruction set computer like Intel x86 it has many specialized instructions
(23:46) um versus the risk uh reduced instruction set computer which has um more efficient implementation is Al also shorter like arm at L five are is is is so here is the example of uh performing a single ad uh the CIS may need only one instruction versus the risk we have four instructions we have to first load one operand load another operand add them together and store it to the memory so what we learn in the 6.
(24:21) 1 91 class each instruction just operates on one piece of data right but if you think about matrix multiplication or convolution or neural networks they are embarr embarrassingly paralyzable right many of the elements within an array they are computed loog step you can easily paralyze them so instead of just using one instruction to operate on one piece of data and we use one piece of instruction to work on multiple pieces of data so that's the idea of CD um for example in this figure um this a single instruction single data one
(25:05) multiplication instruction work on a single um single Precision a and single Precision B versus a CD instruction you also have the same one single instruction but now you work on four uh numbers okay each one of is 32 bit 14o Point number Alto together the register is 12 eight bit so how many floating point it can fit can fit four right we can fit four oper in a four oper in B we just execute one instruction and now we can work on four pieces of data so such register is called Vector register compared with a Scala called it
(25:50) a vector register and hold and process multiple data elements and this operation is called vector operation basically um they work on the entire Vector register so with such SD programming we can easily increase this computational throughput and speed and increase Energy Efficiency since uh decoding the instruction has overhead right it's not free now you decode one instruction but you can process it on modle data so we amortize such instruction overhead okay so this course is supposed to be very practical so you introduce uh two
(26:35) kinds of Sly programming intrinsics one is from x86 one is from arm okay for x86 um let's see what is a interal approach for example single instruction single data you want to uh do a DOT product of two arrays okay and and this AK times B K and accumulated is C do product with the Intel SS intrinsics uh so the step size becomes n divided uh the range previously was from 0o to n now is from zero to n ided four okay what we are doing here is multiplying from these two registers and every time we increment K * 4 okay and what are these um so mm
(27:30) load PS mm means multimedia because multimedia application usually have a lot of paradism capability um and we can do parallel Pro uh CD programming for them load here which means U here mod M add means mode mply add and PS here means packed single Precision okay at single position so again go back to our example we are doing the multiplication on the path single pration by loading from a and loading from B and across different iterations we are actually K * four starting from zero and then four and then eight step size of four because each
(28:17) time we are processing four elements using a single instruction okay uh so and Al together is 12.8 bit each of them in single Precision is 32 bit we have four of them in the register so Al together we are operating on 128 bits using the single instruction the loading oper a loing oper B and then finally modify them together and then add it to C so on the arm architecture the we have the same instruction but the name is different here V load one Q so V means Vector it's a vector operation on Vector uh Vector registers and load multiply
(29:03) add is one is the number of the vector and Q means qu word meaning every qu means four right so meaning we can operate four four words in a Syle instruction so um this do product becomes v32 on a on B after we load them we modply them finally we add um sum it up to the c um address okay so this is comparing single instruction single data this is single instruction multiple data so this is the full code of speeding up matrix multiplication using the simd intrinsic it looks different difficult but it looks very complicated
(29:53) but actually quite simple we just need to look at half of them this is the 86 version this is the arm version we can just look at the first one so again we have one two four Loops three four Loops okay three for Loops uh here we are going to create a accumulator okay that is8 bits okay and then the result will be first load from a load the transpose verion of B and then add it back uh to C and here we are processing four elements at a at a time therefore um here K actually K plus equal to four since the vector size is
(30:41) four and finally we call this mod media add um P to accumulate the elements in the accumulator and on the bottom is the arm arm version of that is pretty similar increment by four or load a load B multiply and and finally accumulate them together okay so um that is inde and next we are going to switch here to talk about M threading we also going to exploit this technique uh to implement uh lab five which is Con concurrent execution of multiple threads with with a single process okay so uh this is the basic programming model of shared memory
(31:34) programming this is a single threaded process versus a multithreaded process let's see what's the difference okay so um we can see the multiple multi thread process uh they have uh they share the same memory space with code with data with open files but they have their own registers in Stacks program counters right and different threads can also run on different CPU cores improving the performance by allowing paradism and the M threading can have several advantages for example improved uh performance multiple threads can
(32:16) execute a task simultaneously uh if one thread is blocked by say blocked by uh this Io Io waiting for uh the io to complete the CPU can um do contact switch to switch to another thread and that can increase the resource utilization by minimizing the time of waiting for some resource for example here um this is across different times this is across different threads you threat T1 T2 and T3 um the blue part is when the thread is allocated on the CPU and then here it's going to to contact switch switch to another thread for example if thread
(33:05) one is blocked by this IO it can work on that to Etc so minimize the waiting if you have so many threads basically you have very CPU P assuming that some of is waiting then we can schedule another thread that's basically the principle of the gpus so we want to break down the complex problems into simpler and smaller tasks so for example in matrix multiplication we can use uh one thread individual threads to deal with individual blocks by blocking a larger Matrix into smaller Matrix and assign one thread for each smaller
(33:47) Matrix so in practice um there are very um convenient programming models libraries for shared memory model thread programming or introduce P thread and also the open MP so P thread is a C C library for creating and managing threads so let's go to our classic example of matrix multiplication let's breor a little bit when we have this Matrix modification how do we divide the work say we have Earth thre okay we can just naively divided according to the roles since we multiplying the K Dimension we can um just divide the a i
(34:33) Dimension okay thread zero is responsible for the first chunk and then thread one and the second chunk all the way to thread n and each of them is multiplying a partial a matrix with the B Matrix and WR it with a partial C Matrix so different FES don't have to communicate they're sharing B but their input output they don't inter interfere with each other so that's a very easy uh divide of the workflow and basically the code has two sections one is the overall coordination among all the threads the other is for
(35:11) each thread what is it going to do okay so for the overall coordination we first create uh the thread by calling uh P thread T threads and then specify the number of threads we want to create and then we are going to assign the work assign the workload okay so here we are going to create the threads by giving it address and also what is going to do what is going to do is basically this ma mul threading uh function which is actually defined by by ourselves right here not small mod threading so once the thread is created
(35:52) it's going to run this function we're going to talk about that and later we have to wait for all the threads to complete some of them may complete first some of them may complete last we have to wait for the last one to finish by calling P thread join make sure all the threads arrive at this point so that's all that's the overall management creating the threads and also assigning the work so what are they going to work on so here we're are going to specify the function that each thread need to
(36:29) execute this is m m body threading so we are going to indicate the starting and ending Row for each thread since we are dividing the work by the rows R zero is responsible for the first part first uh dividing by the row thread one and the thread n it's going to each thread is going to have a thread ID so we can easily divide the work but telling the start row will be thread ID time times the row per thread and in R would be thread id+ one times how many rows per thread but if you have 10 rows per thread thre zero will be 0 to n thre one
(37:11) will be 10 to 19 okay start row and row and then each each thre can only um only conver a part one part of the maal using the 10 rows only okay so it's going to I is from the Star row to end row and this is the B Matrix right to the C Matrix and the inner part is the or the very simple three for Loops so this is speeding up Matrix modification in modif writing uh in the homework feel free to come up with more creative idea to break the workload we can divide it by a or can have other ideas so by using just
(37:53) this simple implementation I see the speed up results but in I impementation without M threading uh takes 24,000 milliseconds uh but using by using four threads it actually depends on your CPU how many sprad you have it has four 5,800 milliseconds okay for thre 4.1 like speed up a little more than linear maybe due to better utilization of the memory the cache okay so it's actually very effective technique to accelerate mod uh Matrix modifcation so if p r looks are complicated or introduce even simpler programming model using open MP it's
(38:40) very portable same program um can run on many different operating systems and different Hardware platforms uh the way it Implement is that it has many compiler directives okay like this um pragma o p parallel to create a parallel region and you want to specify which for Loop you want to paralyze and which section you want to pariz and it has very simple synchronization intrinsics ofp single critical and barriers and also you can easily set number threads by coding these interfaces so the paralyzation of a matrix multiplication op is very simple
(39:27) actually first specify how many threads uh you want to you want to use set the number of threads used in the parallel region and you want to specify here is a paralyzed reg okay this is a pragma om parallel four indicating we are paralyzing this for Lo which is the I Dimension I Dimension is right here so it's Block it's chunking piling the a matrix in the row Dimension okay so this is very simple implementation compared with P so the code of using OMP op MP is actually much cleaner than that of using
(40:09) p all right let's take a five minute break before we jump into P programming all right welcome back so let's continue and talk about uh Puda programming the interesting part is uh yesterday the NOA was released actually both of them are related to GPU Computing right alexn Hinton and also Alpa go they all trained by gpus and Cuda programming is the fundamental of that so uh let's see so let's that could be a good motivation for us to learn about CA programming so uh the gpus provide a much larger throughput and memory
(40:51) bandwidth it has a very unique programming model um who was introduced by an media in 2006 as a general purpose Computing platform and pro programming model by um um leveraging the massive paradism for compute and also the memory footprint memory bandwidth it's a c like language to express programs that run on the gpus so Coda programming um is based on a hierarchy of Koda thread Okay so so here we have two uh levels of hierarchy a grid of um blocks thread blocks and I'll show and each thread block has many
(41:39) threads so in this example we have 2 by3 a grid of 2x3 and a block of 3 by 3x4 so Al together we have 72 put our thread so the three thread ID could be threedimensional which is very natural for handling and dimensional arrays uh in this example it has two Dimensions okay so um we first create a specify the number of um NX to be 12 y will be six that's probably the size of the workload and then we create 4x3 threads per block okay so 4x3 threads within each block and the dimension of the block will be the dimension of the
(42:29) workload divided by the threads per block in X Dimension and the workload of Y divided by the threads per block in the Y Dimension okay and then this call will launch 72 threads by calling my Matrix add specified non blocks which is here and specify the the threads per block which is defined here this single um this single line of code will call 72 put threads at the same time which consist of uh six threads thread blocks and and 12 threads in each block so let's continue so here we are launching uh uh 72 um colal cost at the same time so
(43:28) Bunch La many launch man threads and for on on each uh on each thread is going to execute um this function okay so this is the device function device means this piece of code runs on the GPU versus this piece of code long Works um runs on the CPU okay so the name is Matrix add what is going to do is according to the block ID X and times the block Dimension the X Dimension and plus the thre ID X okay it's going to um tell you where is where am I I am a thread okay which part of the Matrix am I going to work on so
(44:14) that's specified by the index I and J according to the thread ID um and also the block ID since it's two um hierarchic two hierarchies I'm going to know where am I going to work on so I'm going to work on I and J the specified by um by this two sentences and then I'm going to work on AI Aji bji and add it to cji so each thread is uh going to compute its own workload specified by the block ID and thread ID since different threads has different uh different IDs they're going to work on different part of the workload in
(45:01) parallel so that's where the paralyzation comes from so memory model is quite important since computer is cheap memory is expensive and let's look at what is the memory model for digital programming we have distinct host and device address spaces host means on CPU device means on the DP they have different address spaces okay so the data can be moved between the host and also the um the device so give an example on the bottom uh we first create an array um on the CPU which name is a and then we are going to uh Coda C Coda myog to memory
(45:45) to allocate a memory on the GPU by uh specify which device how many how many bites okay so now this device a this this address will have a being allocated with this amount of uh uh memory okay so we allocate buffer in the defuse address space and then we are going to move data uh to this to this address so device a is an address under GPU and a is the address is the address of the CPU now we copy uh the the data from CPU to GPU by calling P M Copy by calling from CPU a to the GPU address device a this amount of fights uh the
(46:31) direction is M Copy from host to device and you can also do device to host so this is the n copy so we allocate the buffer in the device and then copy the data back can force from the device to the host or from the host to the device however we cannot directly access the value in the address space of the GPU so access device a from the host will be invalid because device a is not in the host address space but is in the uh device address space so there are three distinct types of address spaces visible to the kernels
(47:16) we have the global memory which is readable and writable by all the threads uh we also have the pro block shared memory which is readable and readable by all the threads in that block we also have the local per thread private memory which is even smaller that is readable and relable only by that thread so different address spaces defines different locality and different load and store overhead we have to take care of okay so given that we can jump into how to speed up matrix multiplication okay so compared with the CPU
(48:00) implementation which take 24,000 milliseconds the product implementation on a 2080 Tai GPU which is quite outdated actually uh only the qu kernel is only 6 Ms and the overall maal takes 200 milliseconds that's almost 100 times M2 and speed andan actually you can tell from here um the launching the kernel is very expensive the kernel call itself is only 6 milliseconds so we want to minimize the amount of Kernel calls and do as much as possible within the kernel in neural networks and large language model inference this is very widely used
(48:41) for the kernel Fusion you want to fuse uh the prelog the epilog for example the activation function to fold it into the actual matrix multiplication Kel okay so that you don't have to have such large overhead overhead you launch one one kernel you compute as much as possible not only the matrix multiplication but also the activation functions that's called Turn fusion and now let's see what it's doing so each block is working on part on a portion of the Matrix okay so we are going to tile the matrices so that it's
(49:20) threed is only responsible part of it so we do a purple block all for for tile a and also tile B and then according to his thread ID uh in the Y Dimension and also X Dimension and also the thread ID a block ID in the X Dimension times the tile size um we can specify which part of the a matrix we are responsible for so only a chunk of a a matrix and then we are going to wait for the memory loading since since each thread is responsible for loading only one part of this block we have to wait for the memory loading to finish before we can
(50:10) do uh the the tile matrix multiplication okay so we call six threads right here and then we are going to multiply these two elements together and add it back to the Valu and after every thread finish um such operation we are going to write this value to the corresponding location so recently um since the tenser application is getting so popular in uh de learning Computing media remanded the T course which has even higher throughput and more data types especially low Precision uh data types so on a Cod for platform we can um
(51:00) have one fp32 or two fp16 modify and accumulate in each cycle well a TENS core has finished an entire matrix multiplication which is 4X 4X four for T architecture 4X 8 by4 in amp architecture the fp16 in each cycle so in one cycle you can finish the entire matrix multiplication so the throughput becomes much larger and and if the Precision gets lower and low lower like in four you can see the tensal course is even higher so this is on the h00 the measure the throughput compared with here is put4 p32 67 and this is
(51:46) here is the uh answer for um one is t16 one is tf32 and here is the canel four in fp8 or in8 or Spar fp4 Spar fp6 okay it's much higher actually 60 times higher throughput compared with the the ca core and here is comparing when you have different workload sizes uh by work close size we mean the dimension of the matrix multiplication n by n by n M multiplication as and gets larger and larger the speed up between the put out for and tensal core between p and tens core is getting the Gap is getting larger and larger so tens core is very
(52:42) good at processing these large Matrix multiplications so here up to 3.8 times faster compared with the quod core so the basic PR intrinsic is the Matrix m multiplication intrinsic called MMA okay so here show example to compute the 16 * 16 by 16 * 32 MMA using 16 by 8 by 16 intrinsics is this is showing how can we use small intrinsic to compute on large Matrix modifications like in llama we have we can easily have like 4K 10K those larger Matrix but our in inic is small so how can we average those smaller intrinsics
(53:31) to work on large Matrix mplications so the first step is to break down the input and output into uh 16 by 16 and 16 by 8 p so 16 by uh since the intrinsic is 16 by 8 and 8 by 16 and then we can process each tile artial t z by tle z t one by t two t0 by T one and then t one by T three okay so we just call the intrinsic several times to process a larger Matrix modifications so the order ofing is four MMA intrinsics does not matter all right so that concludes our section two now let's switch GE to section three about inference
(54:22) optimizations so to enhance the computer speed and reduce the memory usage in the previous lectures we talk about several techniques like pruning quantization neural architecture search uh distillation they are not function preserved right so they change the workflow but here are the techniques that is function preserving The convolution Remains a convolution a 3X3 convolution remains a 3X3 convolution it's m M eal but we can have different implementations and they result in very different efficiency uh difference okay so we
(55:06) start by talking about image to column convolution a very efficient and simple implementation of convolution and then in place STS wise convolution since STS wise convolution is quite widely used um in Mobile in mobet architectures right so how to efficiently Implement that is crucial and then we are going to introduce nhwc or NCW different um orders to um um to represent different convolution like pointwise and depthwise convolution they prefer different memory layouts and finally uh Winograd convolution by reducing the number of
(55:47) mul multiplications uh at the same time it remains mathematically equal okay so let's first talk about image to column convolution so as we have seen matrix multiplication is a very basic uh intrinsic for very basic building block right but convolution is a little different from um matrix multiplication as six for Loops rather than four for Loops so the question here is rather than three for Loops so the question here is how can we use matrix multiplication to implement convolution and how do we exploit GMM
(56:28) generalized the matrix multiplication gemm to implement um convolution so image to column is a very common technique so here we have input activations and we have four kernels here and we have four output channels so here we can have the uh we can um expand the input activation from ABC um a b c f g h k l m they all convolve with Fel 0 1 2 3 4 5 six S8 right so we uh lay lay out the memory in this way a b all the way to M BC all the way to n which is a b all the way to M and then BC all the way to n which will be multiplied with kernel
(57:23) zero uh 0 1 2 3 8 which is right here 0 and three 8 right such that we can um have this row modpi to this column so blue green yellow and also here is blue green yellow okay so lay them out in this way uh we can turn a matrix multiplication to implement convolution so Dimension here is K squ C if K is 3x3 kernel this will be 9 by C and here is also 9 by C okay so nine elements in one kernel and have c number of kernels okay so that's a how to we use convolu uh Matrix modifcation to implement convolution the pro is that this very
(58:19) simple but the Kong is that it require additional memory usage for example uh with have a lot of duplication from ABC fgh km versus BCD ghi we have six elements that's overlapping with each with each other so that's the drawback uh additional memory space but this can be um optimized by using implicit Jam solve the additional memory problem okay so we directly operate on the input weight and activation tensor without having to l am physically amount and materialize them in the memory okay so next optimization is in
(59:05) place twise convolution so twise convolution is very widely used um for example here it has three components 1 by one com 2D 3x3 that's come follow by one by one come 2D and each channel is operated independently but independently we mean the channel one is only related to input channel one channel two is only related to input channel two and similar for Chanel n so if we use naive implementation we have to allocate uh double size of the chw h w and then C channels we have input we have output so the total memory for footprint will be
(59:51) input plus output which will be 2 chw so what we find in this mc2 paper on the bottom here uh we find we can do inl step SL which is basically allocating a template buffer and then when we are working on the first channel we write it to this buffer to the compute and then we write it back to the first channel when we are Computing on channel when we're Computing on channel two we also work use the same type buffer and write it back to channel two channel Etc right so in this way we don't need any large by doubling we don't need to
(1:00:33) double the memory footprint but don't only need the Conant which is one Conant buffer so 1 plus C * H previous those two times chw all right next optimization the L memory layout matters a lot so we are going to compare the nhwc and also NCW memory layouts and which one of them is good for convolution types wise convolution versus Point wise convolution so these are all about uh data layout and memory management since uh data movement is super costly as a principle in computer architecture we want to minimize the data movement so
(1:01:20) how to allocate the data become super important so the uh first slide is about we should use nhwc for Point wise convolution what does that mean so this is the input convol input activation height is three width is three we have 10 channels in this case um NH hwc means the inner loop is the c channel so we are storing zero n 81 the they are continuous in the memory okay and the weight we convolve it with the weight which is uh since it's pointwise solution it has only one by one in XY Dimension and let's let's compare the
(1:02:04) NCH versus n chw versus nhwc what does this mean n is the batch Dimension C is the channel Dimension here we have 10 channels H is the height W is the width so here we have height and width in the Inner Loop so the layout would be um 0 1 2 3 4 5 6 7 8 and then 9 10 11 so that's the NCH uh NC W layout versus if C CH the channel Dimension is in the Inner Loop what's going to happen is we are going to store zero 9 18 all the way to 81 ingly in the memory okay 0 9 18 in in the memory and then the second Channel which is one 10 out the way
(1:02:59) to2 so um if we um use the NCH W manner to perform one by one convolution which need to act as 09 81 09 18 27 81 here's a lot of uh not discontinuous memory access right so is not efficient but if we store them in hwc manner the channel Dimension are laid out in the memory sequential so we can take the volage of a lot of cash hits so that's why nhwc um the c channel in the inner Dimension is very good for the point wise convolution but for depth wise convolution the story becomes much uh becomes completely different right so uh
(1:03:55) the is also in two um this is the kernel for the weights we have 10 channels and this is the output activation we have a temporary buffer okay um so um the we first come out with the blue the green Channel okay if we use nhwc uh c channel in the inner inner Dimension we have 0 9 18 81 09 18 all the way to 81 they are continuous but if you use any h w Everything Green will be laid out first and then red and then gray so for dep as convolution using ahwc there will be a lot of discontinuous memory access for the red
(1:04:41) and then for the nhw chw manner the memory access will be continuous okay so for STS wise and point wise they actually prefer very different memory layouts any questions so far okay if not let's watch the animation again to get a deeper understanding about the sequence which really matters about the locality and also the memory footprint okay so finally Let's uh take a look at quoad uh convolution to replace the number of Matrix the number of multiplications um and to speed up the Computing so here is an example of
(1:05:33) computing a uh output with a 3X3 kernel interact with 3x3 input we can move it uh in the horizontal and vertical Dimension to produce four different outputs r0 R1 R2 R3 so how many Max is needed to produce four outputs so for every output we need nine multiplications 3 by three nine multiplications another N9 9 N so all together four * 9 * C since we have a c channel Dimension as well so 36 C Max will produce four outputs what if we have a magic transformation so we U transform this data assume is free let's look at how later assume this
(1:06:30) transformation is free uh become a 4x4 by C answer and 3 by3 filter becomes 3x3 by C transform the filter and then we just do Point wise a multiplication that's only uh 16 multiplications to produce an intermediate output and then we assume we have a fre output transformation can get this out uh 2x two output T okay so in this case we only have 4x4 by 4x4 by C okay which is 16 C Max to produce four outputs which is 2.25 2.
(1:07:15) 25 times F Max compared with the direct convolution so that looks pretty exciting what does the data transformation look like for the input for the fure and for the output actually um we right convolution with several magic matrices to uh to do such transformation okay so we do the filter transformation this is the filter and this is the input we do the data transformation offline using the key and B Matrix Matrix so this is the G Matrix which is only consist of zero 1 1 / 2 minus 1 / 2 which can be easily implemented by shift okay one/ two is
(1:08:04) basically shift by one bit and this spe transformation matrix consist of only zero and one and minus one okay so no multiplication is needed here no multiplication is needed too only shift is needed and output transformation the a matrix okay this is a matrix only 0 1 minus one no multiplication is needed to okay so that's why the total number of multiplications is reduced from 36 to only 16 4 by 4 16 element wise multiplications and this paper F algorithms for convolutional networks proves the result is the is the same so
(1:08:53) feel free to implement it uh yourself very very simple actually can be you can test this uh this equation you know very simple um prototype by putting these magic uh Transformations one for the parel one for the input one for the output all right we're pretty much there um so by combining these techniques we can deploy a large language model locally on a laptop uh which is using the tiny chat uh tiny chat engine which is efficient L on device inflence Library you can deploy a Lama 2 or Lama 2 7B Lama 3 7B and 13 uh
(1:09:41) locally on this laptop by using after quantization which we are going to learn later um tiny engine also supports uh the on device um object detection person detection visual Rewards which we saw the demo in the last lecture so it's all open source feel free to give it a try when you are implementing lab five whenever encounter any difficulty feel free to check out with P check engine rle which is which has a complete um implementation uh for the inference engine for large language models so in lab four and five we are
(1:10:20) going to first FTI to forit using the awq technique which we are going to to introduce a few weeks later and also deploy a large language model by using the tiny engine to run a local chat bot on your computer so including techniques we learn in this lecture like Loop and rolling reordering simd body threading okay so here's the summary of today's lecture uh we talk about microcontrollers the resource constraints and Crea iCal factors limiting why is difficult including both memory um and also compute and why data
(1:11:03) layout is very important for the optimization techniques you introduced uh loop optimization like Loop reordering and tiing and also the uh blocking um and also mif Fring techniques CD techniques Cod programming image to column convolution in steps wise hwc and n chw finally we know where competion all right that's all for today thank you for your attention we'll see you next Thursday since next Tuesday is a holiday for students okay have a great fun weekend
