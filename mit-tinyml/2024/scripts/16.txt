(90) EfficientML.ai Lecture 16 - Vision Transformer (Zoom Recording) (MIT 6.5940, Fall 2024) - YouTube
https://www.youtube.com/watch?v=C_U19CeStV4

Transcript:
(0:02) okay good afternoon everyone let's get started um happy Halloween I we prepare this demo for you to play with and generate any images for example here is generating uh Halloween pumpkin this is using uh the hybrid Auto regressive model we developed are going to talk about that in the later part of this lecture it's super fast running on a6000 GPU in the basement just take a few seconds to generate new images uh the website is heart. mit.
(0:34) edu so feel free to play with it okay so today we are going to talk about lecture 16 let me start a slide okay welome to lecture 16 about Vision Transformers so we learned about about using Transformers for language modeling okay for large such as language models B of NLP tasks but the um Transformers is so powerful that we can also Al use the same architecture to model the images to extract features for images so this is the agenda for today's lecture we're are going to first talk about the basics of vision Transformer and then talk about how to
(1:13) accelerate that because Vision Transformers are basically super big how do we make it faster using window attention using linear attention such as efficient viit and also using sparcity using sparse attention to accelerate these Vision Transformers uh next we going are going to talk about self-supervised learning for vision Transformers how to continue scale it up without having to have access to many labeled data using contrastive learning using mask image modeling and finally we'll talk about viit with auto regressive image uh
(1:49) generation in particular we'll talk about the techniques behind model be behind the demo as just shown uh hybrid Auto regressive Transformer done by r so let's start with the basics of vision Transformers so remember in Transformers what is the basic uh building block so the input granularity of the input is tokens right so text very easy uh to be converted into tokens each word um is roughly a token right some words are two tokens but how do we represent images with tokens and how can we tokenize images a natural way is actually
(2:33) to uh patch to convert the image into into patches like in this case we have a image and we divide it into three by three nine patches here are the patches so we convert the 2D image into a sequence of patches okay and we consider each patch as a token okay so for example in the original image dimension is 96 by the patch size 32 by 32 so right here we have nine tokens okay so we have nine tokens the dimension of each token is 3x 32 by okay so 3K and then we uh do a linear projection okay to flatten the patches patch is
(3:24) 2D we flatten it to into a single 1D okay and we project it from roughly 3K to 700 766 68 in this case okay so we convert 2D image into a sequence of patches using this way uh what is the Practical implementation of that so rather than using FC layer to project between uh 3072 to 768 we can just use a convolution right use a convolution uh the kernel size is 32x 32 that's the dimension for each patch 32 by androy of as well and pting to be zero in channel is three since input we have three channels RGB and the output channels 7 68 for
(4:17) output Channel and then we can apply the standard Vision standard Transformer architecture after applying um the positional encodings right otherwise we don't know where the pixel come from so we need to apply the position in codings and then we can feed the uh uh the patch ining to a standard Transformer encoder just like language and there are several variants of the vision Transformers for example there is viit based large V huge they have different layers V large has 24 layers hidden dimensional size was 1K
(5:01) MP is 4K 16 highs and 300 roughly million parameters and usually there's another notation um slash 16 that means the patch size is 16 by okay so you can have different patch size the larger the patch size um the more course granularity is so how does v can with CN okay this is showing the number of sing samples the Y AIS is toine accuracy we're comparing the viit and CN when the twinning data is not a lot a lot actually viit is inferior compared with CN however when we have a lot of tring data okay so we actually Ser has C
(5:54) is when the prrum with a very large data set so VI scales better than CN is but when the data is small CN more efficient okay so having introduced the basics about viit let's talk how to make it more efficient how to tiny uh we are going to introduce the efficient VI techniques before that let's first introduce why is crucial uh to let vit handle these high resolution uh images okay for example medical image segmentation we need to have those fine grain details for example this is diagnosing kidney sized we need verify ingrained
(6:40) resolution and lots of tasks require this high resolution than prediction such as super resolution uh this is a low res this is low resolution higher resolution we can accelerate it to make it much faster 41 frames per second compared with 16 frames per second such super RIS res ution as is widely used for example in immedia dlss basically it's doing the super resolution given a low uh resolution video make a high resolution and that require real time processing and also high resolution is important for autonomous driving tasks
(7:20) like if we are using a very low resolution like in this image we can hardly distinguish with somebody riding a bike right here but uh given the high resolution image you can you can tell actually there's a people R riding a bike or motorcycle right here the accuracy actually grows um with the resolution and so here we are comparing the imou which is a bench uh which is measuring the quality of segmentation task the high resolution the Red Bar compared with the low resolution the gray bar actually there's a big gap there's a Improv
(8:01) uh from low res resolution to high resolution however increasing the resolution is no free lunch you see the max T Max computation for VIP is actually grows much faster compar with the CN right from 1K to 2K the computation for VIP goes much faster why is that the case think about the attention mechanism it's o n square right is the number of tokens the number Mac is o n Square you need to calculate token by to the tension between each token so computation complexity is O square and the number of token is uh equal to the resolution um
(8:54) the power of two right so therefore it's total computation is resolution to the power of four so it's pretty Computing intensive so how do we solve this problem actually efficient VI can make it much faster this is comparing the Baseline which is runting 1.6 frames per second second forer is very highering work using transformer for segmentation task at that time the frame is pretty low but after the acceleration we can find efficient VI can make it much faster from one frames to 21 per second but still one maintaining the
(9:30) accuracy so this is measured on a Nvidia Json oring a driving GPU mobile GPU in fp6 by size one and high resolution D prediction is also helpful for uh these AR and VR applications like there's a thought indicating where you are looking at and it's going to tell um segment the stuff you are looking at and that is using this paper called segment anything basically image is passed through a image encoder which is a VI this is taking the majority of time and get embedding then the prompt could be a point box to be a mask encode prompt and then
(10:19) mingle the image encoding with mask and finally predict the final Mas uh so such work require pretty heavy image encoder based on the and then you can take math as input prompt Point as input prompt or box prompt or even text which is not released in this paper so it's prompt image segmentation and it can is ambiguity aware design like you can segment the entire scissors or only handle or only one of the handles it has automatic data engine to use uh this model to annotate more data and use data to train the model okay so those
(11:05) are some of the use cases about Den prediction and the use case of VI as image encoder can do a lot of applications so given those motivation let's see what are the ideas to accelerate and make it more efficient to deal with the bits starting window attention so previously we are converting uh the patches across whole image so every patch in the can attend to each other so if the image resolution gets pretty large the computation grows quadratically so instead of doing attention computation over all the tokens um we can apply window attention
(11:48) Okay so the attention mechanism only happens within the window okay so say this window size is 7 by seven so the attention only happens within this local window okay since the number of tokens in each window is fixed the computation is complexity linear rather than quadratic in this case for example here um each patch is 7 by seven and we do the attention within within the patch and we have many patches right here um so this is the original attention where each patch has to attend to each patch now we can divide
(12:30) into several uh windows and only uh the P tokens within the window will attend to each other so what is the problem of this case tokens only attend to each other within that token within window right so adjacent Windows the tokens cannot attend to each other so the information cannot flow so that's the problem all we that solve that locality problem to enable this kind of global a lot much larger receptive field so actually a shift operation is added to this window attention Okay so uh for the layer one okay this is
(13:16) window divided by two windows and layer Layer Two we actually shift the window okay Shi this window by um two boxes okay so that um adjacent Windows the tokens in second window can also attend to the first window okay in the second layer so if we repeat this process all the tokens will be able to attend to all the tokens no matter if they are in the same window or not thanks to this shift operation okay as a result uh this method actually achieves better image accuracy than the viit so these are the top accuracy this is the sing
(14:04) Transformers shifted window attention sing Transformer accuracy better than the original VI not only on image net but also on other tasks like up detection and also image segmentation tasks so are the all windows useful actually um it's not the case sometimes City can also help not all the windows are useful we can safely discard a lot of Windows especially in point Cloud um actually 99.
(14:44) 9% of the Boos are actually sparse We There Is No Object like right here there's no object so the spatial sparity is pretty high so how do we exploit those sparity we can use window detention uh the white area shows there's no um point so it's zero uh but how do we divide the window right how do we set the window so one way is to apply this Eco window group Eco like this is a window C itself AE is a hpg this is a window uh it's very regular 2 by two right however if we do it in this way um the window size is highly imbalanced like the purple window has
(15:27) only one element the next has two elements but the right one has three elements um so this lead to difficulty for paradism right so this slow one need to wait for this fast another way is actually uh to apply equal size grouping rather than this equal window of grouping so group based on the number of elements such that uh for each group we have exactly the same very balanced uh distribution about the number of non-zero elements so we the first three elements the first window the next three in the second window
(16:05) next three the third window actually that's perfectly balanced and as a result you can imagine this is much more Hardware friendly um and can run much faster compared with different Transformer based models and even faster than convolution based models this is convolution based model this Transformer Bas model measured on a Jon IM oring driving chip hey so this is using window attention and SP window attention since the attention mechanism has open Square complexity a natural optimization is can we simplify that to
(16:46) linear complexity okay so that they with linear attention Okay so let's switch here to a linear attention before that let's first see the sension um equation okay so we have q KV we first Q qk transpose we get a m byn attention mask this is o n Square scale it by square root of D find the soft Max and use that to do the weighted sum with the V look at final output okay so the cost is o n Square primary due to Q uh this is a slightly change the version okay so using Val linear attention so uh we first do the q and
(17:32) K we pass the r activation function right here we pass it through the math M scal it and then finally modply out with v so that the cost is still o n Square in that case so can we change the order such that can have o n rather the O Square complexity okay so uh that Dees the third uh solution since a * c equal to BC so this is the associate associative property of the mathal operation okay so rather than qk first we do the KV we do the KV first so it's D by n so it's o n d square is proportional to n rather than o Square
(18:26) so this o n d square and then we uh multiply with q in the last order so by changing the order I become switches from o is n sare to d square okay so it's proportional linearly to n rather than to o Square so we change the order of ma mod using such associative property and we can reduce the computation complexity does this work actually not right you can imagine uh reducing the computation there's no free lunch uh originally using the sof Max attension um the attention is very strong at the points of interest like
(19:11) the eyes of the dog nose cat that is very strong tension but using linear tension everything becomes pretty weak the eyes is no longer red and the cat nose is no longer hyly attended becomes very flat if you plot it's indeed the case that as using soft Max versus without using soft tension the blue curve becomes much flighter compared with the red which shows that the r linear attention cannot produce the sharp distributions okay so it's good at capturing Global contact information but not good at capturing those local
(19:53) information right like this local information is not captured in the linear tension and also lacks this multiscale learning ability so how do we uh deal with that as a result you can see there's big accuracy Gap if you replace the soft Max tension with the Val linear tension help an accuracy dropped a lot so how do we uh alleviate such uh limitation without losing efficiency but can effectively handle the uh local information extraction so let's brainstorm what is good at extracting local information to compensate for the loss of local
(20:35) information extraction capability that's convolution right convolution is our friend to extract local information so we can simply add a convolution uh Branch um to comp do the local feature extraction without but in order to not to uh lose efficiency we use a dep as convolution followed by one one group convolution to save the computation right here rather than using the full convolution we decp the spal resol spatial modeling versus Channel modeling so we can enhance linear tension by such multiscale uh aggregation with the
(21:18) convolution okay so um here the FFN layer we added the steps wise convolution um to compensate for the loss of accuracy it added very little computation but accuracy wise it Fly U brought back the accuracy and also actually improved the accuracy a bit so added the depw convolution and further enhance the linear tensions local information extraction capability and as a result we can see the speed up right here this is on a mobile JP forom snap dragon mobile chip it achieved about 5.
(22:04) 8 times speed up with even higher accuracy on segmentation task in the middle is an oring IM address oring for driving applications so I achieve about 2.8 times speed up on the right hand side is cloud GPU which is a100 Achi about 1.7x speed up for better accuracy not only on segmentation but also on super resolution tests so uh here is on two data sets one the fhq data set which is human Bas uh data set we are trying to do the super resolution from 512 to 1K 52 uh this is the original psnr higher better we can achieve even higher psnr
(22:53) originally it takes 92 milliseconds now it's only 70 17 milliseconds for five times faster if we see the details this is the by cubic uh which is not quite good this is about 3.6 images per second and this swing IR about 4.7 images per second and this ours efficient 17 Imes per second and similarly for another data set driving data set uh the psnr a bit higher originally is 50 MCS now 2.
(23:33) 8 milliseconds and let's see how does that apply to the segmented segment anything task so here we are using the box prompt and this is segmentation result very good quality but the throughput is increased from 11 to 182 uh images per second segmenting the P segmenting the fruit uh a VAR Pole right here it's very thin but it's very well segmented for this pole another example is showing the dot dot prompt you have a single sis this is the prompt segmenting pumpkin uh this is the chair segmenting the person segment in car
(24:21) right here we can also segment everything so given a lot of dots everywhere in the image and trying to segments everything uh this is before acceleration after acceleration you can see the quality is very well maintained and this is the uh image classification result um this is the latency the lower better this is accuracy the higher better so ideally we want something right here so this is comparing with a lot of baselines uh this is the accuracy latency TR off this is the through pool accuracy TR off we want to hide
(24:58) throughput and high accuracy so why do we measure latency on or and throughput on A1 100 because this is Edge device usually uh serving a single user like a single driver while this is a cloud device it simultaneously uh serves multiple users therefore we care about throughput on the cloud and we care about latency on the edge hey so having talk about lead attenion let's further use sparcity to accelerate and simplify these Vision Transformers so uh this is a driving scenario Boston in the SE Port area of Boston uh there there's a question about
(25:45) the resolution right since the computation complexity works quadratically with the resolution an natural question is can we use a lower resolution okay so we can resize the image by using a lower resolution like half of the resolution but very using dense pixel 100% density you can see is very low resolution some details are lost you can hardly tell this is a person anymore so how about we use sparse but high resolution so high resolution is why the resolution but sparse pixels 25% uh because the total activation size
(26:25) would be the same in this scenario quadratic growth with the resolution so here we have 25% sparity which one is better so the first step about sparse VIP which is sparse region Transformer is to apply this window activation pry okay with non uniform speci so we Pro Way those redundant regions and only keep these useful areas like the person here the curs right here which is point of uh the important windows and how do we determine the importance of window we can just look at the L2 activation magnitude the activation magnitude of
(27:09) that window the larger more important we think it is okay and then we can sort Gather By The Importance most important second important third important on and so on and this is just one layer the second layer the third we can can make it getting sparer and okay but how sparse we should allocate the computation to each layer we need to search for that so as the model is originally trained with only uh dense activations so we want to gradually improve the sparcity awareness so we need to find human the model given such sparity so um
(27:57) here we are uh doing the random randomly sampling different spity the first layer is always B second layer get a SP spaser third lay gets even spaser we can also sample a different sparity schedule so for each TW iteration we are sampling a different Spar City pattern but what is common here is that um the later layer is always more sparse than the first layer since if a token um is pruned it cannot recover in this case and finally how do we select the correct sparcity for each layer we use this evolutionary search right
(28:41) evolutionary search we have Implement that in the neurer architecture search uh chapter so given a we first check whether it satisfy the latency budget yes we keep it otherwise resample it so this is showing the uh across different search iog this is the accuracy uh The evolutionary search uh keep getting better versus the random search pretty much stays the same without Improvement uh so now let's see how much we can improve so here is the latency the lower the better here is MP higher the better so here we are
(29:24) comparing with um several baselines for example um this is under 256 resolution this is under 224 resolution so Spar VI can be 1.5 times faster compared with the Baseline without using Spar vit in this case so this is for 3D operate DET this is for 2D instance uh segmentation and let's visualize uh what is the scity mask what kind of did we learn in this case so given uh these are three frames in a driving scenario and sparse VIP try to R those irr irrelevant background for example um this is the 24 millisecond
(30:16) layer model I focus on those foreground rather than the background U the even faster smaller model focus on the person car here Locker rather than the background is our dark and the car bright so it's pting away those irrelevant uh background windows and retaining those informative foreground uh Windows okay uh this idea might also be helpful for visual language models which we can uh discard those areas that is not interesting both in the spatial Dimension and also in the temporal dimension okayy so let's take a break and then we
(31:03) are going to jump into self-supervised learning self supervised learning for vision Transformers so the main motivation is that labeling the data set is pretty expensive right but this vits is helpful when we have a lot of data but this such a large amount of data is difficult to acquire so how do we uh train the model without needing to label the data so we want to train with unlabeled data to mitigate this issue right so how do we um given a Target task okay so how do we use unlabel data so we are going to talk about two
(31:46) techniques One is using contrastive learning the other is using mask image modeling okay so contrastive learning basically consist of positive samples and also negative samples giving a cat we are going to give two views of the same cat Okay so this is one view is another view they are both cats so we uh we tell are those positive samples and then we have a negative sample which is a dog so uh both the cats passing through a v Imaging encoder and then get embedding um the embedding from cats should be similar with each other so we
(32:28) are going to apply the lws pull together pull together the uh features from the cats and pull apart features between the cat and dog okay so as a result with the random init we can see on the side 10 the as the model gets larger um it doesn't help the accuracy gets even low lower but with self-supervised learning in this way the when we are increasing the model capacity from viit base to viit huge um it delivers stronger performance and it's actually the self-supervised viit actually deliver stronger performance than the supervised
(33:14) the cpart and the accuracy increases when using a larger VIP model we can also apply multi model contrastive learning CL model means here we can learn between the text and also the image so famous work is contrastive language image uh pre-training so during the training time we have a piece of language and we have a image pass the language into a text encoder and get embedding uh and also we pass it through the Imaging coder and get the value okay and then naturally um different images and different uh texts uh they can have a tension map so the
(34:03) image corresponding to the text description the image corresponding to the text description which is in the diagonal Position will receive higher tension score so by using such paired uh image text samples uh we can try to supervise it by maximizing the diagonal part which can effective L align the image and text into the same space the same space so at inference time um we pass the image into a encoder and then um the inference time can be there could be different vocabularies and then we select the one that gave highest
(34:49) ATT score which is showing that this text will align best with this image in this case dog will align best with this image so this is showing the accuracy of uh this is zero shot clip linear probing for clip linear probing means just adding an ABS layer to do the classification you can see it actually enables zero shot this is number of labeled between examples per class even given a zero new class label it can give very decent accuracy of prediction ver conventional method require at least some amount of swinging data but is
(35:35) pretty amazing clip can enable zero shot uh capability and the next technique is mask Al encoder okay which borrows the idea from bir language modeling okay so we can Mass the way some of the pixels uh in image okay we pass the remaining uh patches into encoder and then we pass it through a decoder and try to recover the original image so this is Target the input which is masked we try to predict the missing patches based on the seing patches which reminded us about the mask language models MLM which we talk about
(36:24) in the previous lectures so here we are masking some of the token here the movie is very boring weasly boring we pass the remaining words into um the language model Transformer and then we try to supervise it by predicting the mask uh token okay so we can mask a certain percentage of the input tokens that random and predict those masked tokens and can we use a similar pre trining strategy in the uh region domain so in that way we don't have to label the images we try to just predict the missing patches which is coming for
(37:09) free actually uh that works quite well so we here use two encoders one is a highy encoder process only those unmas tokens so this is a five and Al together we have 25 tokens and here just have one two three four five six 7 eight we just have eight unass tokens so we pass this unass token through a heavy inold um and then we uh you f that together with the mask so here gr part means those tokens are masked but we pass that through a light decoder okay so we decode all the output tokens so here 25 from eight and we try to
(37:55) reassemble them in the rizing order I'll try to recover the uh original image so why do we need a Hy versus light because the Hy incoder deals with fewer tokens well this light in to encoder can deal with more tokens actually this method Works quite well for example even if we Mass a lot of components uh like here the clock becomes almost undistinguished but amazingly this approach can still recover the clock bus dog lamp only with a few pixels pixels you can imagine there should be a limit we can mask away cannot
(38:42) away like 99% of the pixel right um and we turn out the mass ratio get a Swit spot at around 75% so this is from 10% 90% The Sweet Spot is roughly achieved at 7 75% um uh Master ratio okay you can still recover elevator as well okay um so it's it has a pretty high Master ratio like bir Master is only about 15% but I may you can actually achieve 75% what does that imply actually that implies the image data has lower information density very low information density and more redundancy compared with language data such that we can uh mask away more
(39:38) information and still we can recover the original information uh so this is comparing the uh uh comparing the accuracy actually this mass of coder Works quite well consistently uh higher accuracy than the Baseline okay so now let's switch gear to the third section and talk about generation which is very so new work done by our TA H uh to talk about Auto regressive image Generation Um so far we all talk about something distributive models right segmentation detection classification right so these are discriminated models we try to
(40:28) uh predict like what is the class for example feeding in the image patches tokenize tokenize input image and then pass it through a Transformer and then predict the final class so those are distributive models now we want to design generative models can generate images right so Auto regressive models can generate naturally generate tokens right so the natural way thinking is and we use the auto regressive Transformer to generate visual tokens in a token in out manner the input token could be the language something we
(41:08) wanted to generate and output token would be the image token actually such autor regressive image Generation Um benefits multimodel language models for example G Google giz typ to report uh HS that they are using the auto regressive model individual generation for example with the important with the important distinction that the models are multimodel from beginning and can natively output images using using discrete image tokens so we Design This Auto regressive uh generation Auto regressive short for AR that can achieve even faster speed
(41:54) than diffusion models so we are going to introduce diffusion in the um uh next lecture but here we give it a very quick hint um it has many time stamps try to predict the noise and try to den right to predict noise and then subtract the noise and you require many steps um and heart require much lower fewer number of steps and Achieve much higher throughput like two images per second compared with 0.
(42:26) 2 images per second much faster compared with here SD XL pixel art Sigma playground SD 3 Etc so how do we uh generate image tokens what kind of order do we generate the image tokens okay so in this paper summarize the three ways first way is just using the Ral order like 1 2 3 4 five six seven uh one by he each time we just generate one more token for one more patch in the rizing order okay we can also use the random order okay next time we can generate this pixel next time we can also generate another patch right random order but the problem
(43:15) here is that it generate each token at a time which is memory bonded as we learned during the conization part single token generation you need to go through all the weights which is a lot so we can also generate multiple tokens Can we also generate multiple tokens okay at the same time so rather than a single token can we generate multiple tokens at the same time so those are three basic approaches so what is different from language generation to um image generation for language we have a fixed vocabulary right like English we
(43:59) have like several tens of thousand English words right they are district so that we can use the C loss very easily to add the loss supervise uh this disting tokens but images is diff are different those pixels are natur continuous right there's no not such a vocabulary like 10,000 English words there's not like 10,000 image patches right so but for for which make it difficult to supervise right so natural way to harmonize these two modalities is to use uh discrete image tokens invent image tokens so that we can fit it into the
(44:52) same Auto regressive uh infrastructure for large language model and we can super provis it by the same loss as Lang model okay so previously we have a encoder decoder now we add a new uh Vector quantized encoder and Vector quantized decoder together with a code book so we turn those continuous image patches with visual tokens into discrete uh image tokens okay uh we can decode it although there there's a limited amount of uh p uh tokens in the code book we can have different mingling for these tokens fact that we can decode them into
(45:38) various diverse images This is highly related to the code book that we talk about during the quantization lecture the difference is that previously we are fing individual uh weights or activations okay so this is a scaler we are doing a scalar quation uh like these two three values are all falling into this centroid and we just store those Central index okay so now we are not fing individual weights but a vector of pixels okay so that's why we call it Vector quantization in with here that's the scaler quantization so we can do a VOR
(46:27) quantisation into the AI generation where each index May correspond to one of the indexes uh for example this Vector the closest color probably is right here so we are storing index two and this color is different from any of them but the closest one is V3 so we s act the blue is closest to this code entry in the code book therefore we are storing entry zero the index of zero and you can see previously those um continuous continuous Vector now there are discrete highs represented by integer values so we can tell this is like a re
(47:18) B of the de compression to from Clos is the central look up to closest Vector lookup and use the integer index to represent a vector instead of a single we so that we can generate um those indexes which is district and this work called v um basically um introduced a new order to generate the tokens okay remember rule of Thum is try to generate more tokens not just one token generate more tokens at a time so what it does is actually first generate one token okay pull the entire image into a single token and then a 2 by two four
(48:04) tokens okay and then 4x4 16 tokens 5 by five Etc gradually increase the number of tokens to generate you can also imagine this is like the big picture very raw um big picture versus the last stage will be a very high resolution details so from the um picture to fine grain details from course um pixel to the fine grain pixel so that's the order where it's going to generate those tokens and the interesting part is the tension Mass it's no longer the triangle shape so what is the new so imagine initially we generate just
(48:49) one token for the entire image and then 2x two four tokens and then 3x3 nine Tokens The Mask will be like this starting with one then four and nine okay so in this way when we are at this stage like 3x3 token we can generate nine tokens at the same time it's no longer memory bonded like generate token by we have to fetch the entire weight for each token is purely memory bonded now it's no longer memory bonded which is pretty Hardware friendly however this is not at perfect you can tell the generated uh picture canare with the original
(49:33) reference the eye it looks different quality is not quite good right so on top of that we designed a new technique us in a hybrid approach that can match perfectly with the original reference now let's see um more details here um so the discrete tokenizer inherent has its limitation the world images are continuous you force it to be discrete right so you can see the recover quality for the VR model um as is not quite well recovered similarly right here there's a lot of uh dist Distortion um in this area versus heart um can recover them quite
(50:26) well so how should we build such a tokenizer what's the limitation of existing uh discret tokenizer so um we introduced the capability to decode both the continuous and also the distri tokens okay so the this is continuous token this is AB the VQ fiser we get the uh distri token okay and if we skip the entire vector quantizer just use the um continuous token basically the decoder becomes a uh the capab has the capability to decode continuous tokens but in the T time we sample sometimes we sample from directly
(51:13) from the continuous tokenizer sometimes we sample the distrct tokenizer so to make sure that they're in the same space so that decoder can decode both the uh continuous token and also the distr token at a training time we try to learn the residual token and the is the difference between uh The Continuous and also the distro Tok you can imagine um it's very difficult for us to learn those fine green details purely from uh this these kind of discrete tokens okay so we can offload the difficulty of learning those fine green
(52:00) details by a very small diffusion model probid diffusion that model can be as small as like 30 to 40 million parameters compared with hundreds of millions of parameters just 30 or 40 million parameters very small residual diffusion model to learn the difference between the continuous token and thisr token okay just to learn the difference so that we can make this diffusion model super far small and super fast and let's try to see what are we trying to learn um from the distr token which is basically the big picture
(52:38) Okay versus the residual token this is learned by a small very diffus model those are the um we can hardly tell but it's roughly the fine green details versus if you combine them together the discrict token versus Regal token together it can give you very fine GRE detail like previously uh you can see the eyes right here this is the eyes compared with the heart this is distr only this is the uh distret plus the residu added more a lot fine green detail okay so that these three tokens are basically learning those
(53:20) speak picture versus the regido tokens are learning those fine details like the eyes the hair Etc and the natural question is how to effect efficiently model this district and red Regal tokens at high resolution and guided by text prompt not just the class prompt okay so for class it is very easy uh like we just input a class index occupying a single uh token like squirel going to generate a squirel okay so we can cenate the first uh text so the class index with the generated uh image tokens so from 2 by two to this is
(54:09) 3x3 sorry this is 9 by9 since 3x3 generate nine tokens everything has to attend to each other okay so this is purely using the a discrete token so from step one we get a 1 by two we get two 3 three all the way to uh much larger resolution adding a text prompt is pretty simple we just uh pass through the text prompt through like beautiful uh sideboard go golden here we pass it through LM get a few text tokens example showing three here but can have more we just concatenate them together and then it's going to generate the
(54:59) but this actually not enough because the generated uh just using this um token the generated images quality is not quite good the generated image quality is not quite good we need to also add this residual diffusion model okay to predict the difference the difference between original image versus the sum of predicted token different resolution like 1X one 2X two 3x3 4x4 we add them together okay the task of small diffusion model is the diff the diff between the sum of district tokens versus um the the final output and this becomes
(55:47) quite easy and only 30 or 40 minute parameter is enough to complete this task to for the regid diffusion learn these residual tokens and finally we add the residual tokens with the original discrete tokens to get the final output and that's how we achieve such high Vitality uh visual Generation Um right here and also achieve by high throughput and this is comparing the heart versus the diffusion models to left column is hard the prump a boy and a girl fall in love this is hard this is playground pixel art this SXL
(56:39) these are all diffusion models this is second example full body shot a French woman photography French streets uh compared with this is playground pial art the XL third one is draw view of waves flashing against ret caves and big S very comparable uh accuracy and the most exciting thing is heart can run at interactive speed on a laptop you could have a laptop with 4090 mobile GPU uh we can do image generation on it so let's play the video this is uh stream Punk makeup in the style of V tracing okay it's almost instant just click the
(57:32) button and then you can run the model this is astronaut riding a horse the classic demo we can also do a dog that has been meditating all the time neon holography Crystal cat or Crystal dog just take one second and this is everything local there no internet connected to the laptop here an astronaut in a huge GPU cluster he if you're interested uh to play play with more demos we'll come to try uh just go to Heart it.
(58:31) edu there are more examples we can also try new New England fall with the river with the house and then it can immediately generate our new images or we can try some default new holography Cal PA right let me it instantly okay go back to the slide so let's summarize today's lecture we basically talk about started with the basics of vision Transformer everything can be tokenized not only language but also images can be tokenized so we have token in and out basic architecture of VI grows the comp computation grows quadratically with the resolution so how do we make it more
(59:31) efficient uh we can first try the window attention so only uh everything is constrained locally within that window we can also use linear tension using equation viit I use sparse attention not all the windows need to be ATT attending to each other and then we talk about um how do we deal with the data efficiency of vit B basically not requiring a lot of well-labeled data by using self-supervised learning including contrastive learning and also mass image modeling and finally we uh talk about not only the uh understanding or
(60:12) distributive model but also the generative model how do we use this token in token out approach combined with a very lightweighted diffusion model to do auto regressive apply this hybrid Auto regressive Transformer heart to the image generation so in next lecture we'll introduce diffusion models for image Generation Um so the other code for efficient vit and heart is open source so if you're interested in continue uh developing heart or use heart as a tool for other applications welome to talk us and the TA is H
(60:51) he's the author of this paper um feel free to uh try it out and we will release the grading for lab two and three by this weekend Sunday so if you have any questions feel free to talk us and it's time to get started think about the final projects we already released many project ideas uh some of them is very hardcore some of them is related to application we don't want to const stream uh your imagination so feel free to uh select the one your are most interested in and we are always here to help during the office
(61:31) hour all right that's for today's lecture uh Happy Halloween
