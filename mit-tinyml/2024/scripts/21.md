(90) EfficientML.ai Lecture 21 - On-device Training (Zoom Recording) (MIT 6.5940, Fall 2024) - YouTube
https://www.youtube.com/watch?v=34XrZeRk_FU

Transcript:

All right, welcome everyone. Let's get started with Lecture 21 about on-device training and transfer learning. We learned about training—distributed training across multiple GPUs, multiple devices—and now we want to see if we can localize this training process to edge devices. We want to perform on-device learning when we have new data collected from sensors, from our phones, cars, and different sensors. We want to process them locally and update the model so that it can accommodate different tasks. For example, we have different users, and we may have lots of new and sensitive data like images and text that we don't want to share with the cloud. But we want to train them locally on our laptops, phones, or cars, without having to send the model to the cloud by performing the training locally. This includes code or enterprise data, which is not safe to send to the cloud. That can help preserve privacy. But as you can imagine, this is super challenging because on-device training, different from inference, requires a lot more memory and computation.

In this lecture, we are going to learn how to cope with those challenges and be able to perform on-device training. We need to customize your model, so we are going to cover a few techniques. First of all, why is it not safe to share the gradient? We can leak your source data—both text and image—by just looking at the gradient. So gradient is not safe to share. We don't want to share the gradient with the cloud. We want to do everything locally. We will first talk about the memory bottleneck of on-device training to deeply understand why it is so challenging, so that we can better solve it and make it more efficient. Then we'll talk about tiny transfer learning, followed by sparse propagation—not needing to train everything—and quantization: quantized training with quantization-aware scaling to use fewer bits in the training. Finally, if we have time, we'll talk about Pocket Engine, which is a tiny training engine we developed for you to be able to fine-tune the model locally on a laptop or other device.

Let's start with data leakage from gradients. Gradient is not safe to share, so we'll first talk about federated learning to start this lecture. For example, this is only sharing the gradient; the weight data stays local. This is an example of an input method: when someone is typing a new word that hasn't been seen before, other users may benefit since that will trigger a model update. The user will generate personal data on-device and perform local training to train the language model. It's going to update the model using the local data for a couple of iterations before it's going to update the model and send it to the server. After we do some local updates, we send a new model to the server, and the server will average models and send them back to many different devices. So, future devices will benefit from one device that has already seen something new, and other devices can benefit from that. The good thing is that in this way, the new data collected from this phone never leaves the local device. They only send the model update, not data, and the model to the cloud rather than sending the data to the cloud.

But is it really safe to share the model or the gradient? Because model difference—you can consider that as the gradient, so pretty much they are the same thing. Is it safe to share the model or the delta of the model, which is the gradient, to the cloud? Let's rethink the safety of gradients. We have a couple of images we pass through this component and get a loss, and that will trigger the gradient. We can do backpropagation from the loss to the image, then we can get the gradient with respect to this batch of input images. The question is: can we steal the original training data from the gradient? Since the gradient or model update is in the public domain—everybody can have that, the cloud has that—while the data is private. If it is possible to steal the input data from the gradient, then gradient is not safe to share.

Let's look at how we steal the input from the gradient. There is some prior work that can perform membership inference or property inference by looking at the gradient. Given the gradient, membership inference can tell whether a record is used within the batch, or whether a sample with a certain property is in that batch. That's not exactly stealing the data, but still membership or property. But we want to be more aggressive—we want to steal the source data from the gradient to obtain the raw training data from the gradient. Let's see how that is possible.

This is the normal training: we do forward, we get a loss, backpropagate, and get a gradient with respect to this model. On the data leakage attack, we do the forward and backward using dummy data. Given the same deep learning model, we initialize a dummy input, which is noise, and a dummy label (can be anything). We use the dummy input to forward pass, given the label, get the loss, backpropagate to get the gradient. What is interesting here is that we are going to compute the MSE loss to match the gradient. In deep leakage, by matching the gradient, we can backpropagate this mean square error to both the dummy input and also to the dummy label, such that the left-hand side and the right-hand side gradients will be matched. In this way, we can recover the dummy input to be the real input.

Let's see some concrete examples. This is the deep leakage process: initially everything is noise, and then we can gradually get better until we recover the original input image. It has two pipelines: one is the normal participant, and the other is the deep leakage attacker. The attacker initializes the input as dummy noise and the output as a dummy label. It's going to go through the forward path, give a prediction, calculate the loss, and calculate the gradient. We try to match the gradient between the normal participant and the deep leakage attacker. The attacker's gradient is matched with the normal participant's, and we use that to supervise the deep leakage process by backpropagating the difference between the attacker's and normal participant's gradients back to the dummy input and dummy output. By updating the input image and updating the label, we can finally make these two gradients match. By matching the gradient, we can actually leak the input. Only gradients are shared between the malicious duplication hacker and normal users, but actually this action leaks the privacy.

This shows the deep leakage process: the left-hand side is random initialization, the right-hand side is the ground truth, and in the middle is the recovery process. Initially, we are starting from a random initialization—we don't know what is here. As time passes, we can gradually recover the input image, and it turns out to be pretty much the same as the ground truth (a table in this case).

That's just a single image attack. What about multiple images? During training, we are training on a batch of input images. Can we recover a batch of input? Actually, that's also possible. Here we have seven images, starting from noise; the last row is the ground truth. We can see that although the order is different—for example, the first image is a bike, the second image here is a ...—although the order is different, the actual content (this is on the CIFAR dataset) is well recovered. This is the attack with a vision model. We can also attack on a language model like BERT, the last language model. The unmatched words are marked in red. So, E zero, this is noise. After plenty of iterations, we can gradually recover a few words. The original text is here: "Registration module application was still traveling. Application opened the first week of September. Try car will be available." This is from the NeurIPS website. After several iterations, we can exactly recover the original sentence starting from noise. This shows the deep leakage attack can actually happen for both images and also languages.

How do we defend against it? How do we design defense strategies against data leakage? Here, we are trying to match the gradient, so can we apply noise? We can apply noise to the gradient and try to see if it can recover from deep leakage. Here, we are applying different strengths of gradient noise, from very weak noise to very strong. If the noise is very weak, we still suffer from deep leakage. Only when the noise is getting very strong is there no leak. However, when the noise is very strong, we can see the accuracy also drops—1% less than—so the model is not very useful. Unfortunately, increasing the gradient noise cannot solve the issue. It can prevent data leakage, but the model is also completely disrupted and is no longer useful.

What other techniques can we use to prevent data leakage? Remember in the previous lecture we talked about gradient compression. We can compress the gradients by sparsifying them and pruning away those useless gradients. We can prune up to 99% or 99.9% of the gradients without affecting the accuracy. Let's see how that solves the deep leakage problem. Here, we pruned 99% of the gradients, and the top-1 accuracy didn't hurt—stays pretty much the same. But we can see when we are pruning a lot of gradients, actually there's no leak. Only when we are pruning very little parameters can it leak with artifacts, but after 70% of pruning, there is no leak. Here, we can prune up to 99% without losing accuracy, so it can defend. Deep gradient compression and gradient pruning can effectively prevent the attack and also preserve the accuracy.

Why can random pruning solve this issue? If you think of the methodology of deep leakage, we are trying to match the gradient, but here you prune away 99% of the gradient, so there's nothing to match. There's a lot less—you have only 1% of the gradient to match, so that makes this leakage process a lot more difficult. Just like you have a lot of variables but a very limited amount of equations, it is not possible to solve that equation. Pruning can effectively prevent data leakage from gradients.

How to implement data leakage? Actually, that's only 20 lines of PyTorch implementation. You can initialize dummy data and also labels. Here, you can do the forward to get a dummy prediction, then you can feed the dummy prediction with dummy label, compare them, and get the loss. After getting the loss, you can pass the loss with the model parameters and also calculate the gradient. You want to try to match the dummy gradient with the original gradient, and you want to get the gradient difference. Then you want to backpropagate the gradient difference to backpropagate all the way to the dummy input and also the output. Do one step to close that process. By doing that, we can very easily implement data leakage in only 20 lines of PyTorch code. Quite a bit of success recently—feel free to check out the code website.

So, we learned that sharing the gradient is not safe—you can leak your image and data. We really need to perform on-device training, having everything on-device, including the gradients. Let's first analyze what is the memory bottleneck—why is it so challenging to perform on-device training? This shows the comparison between cloud AI, mobile AI, and tiny AI. Tiny AI is four orders of magnitude smaller than the phone, and five or six orders of magnitude smaller compared with a cloud. Both the weight and activation take memory. Since for backpropagation, we not only need to store the activation for adjacent layers, but also for all the input layers during backpropagation, we need to reduce both the weight and activation—not only the weight, not only model compression, but also activation compression—to enable on-device training.

This compares inference memory versus training memory. Training is a lot bigger compared with inference, due to needing a batch size and also needing to store those intermediate activations. Let's see why the training memory is much larger than inference. During the backward process, when we are calculating the partial loss over partial weight, we need to access the activation. We cannot throw away the activation. You can do checkpointing technique to recompute, but that is trading compute for memory. Usually, this memory is pretty large, not to mention we need to have a large batch size, so the activation also grows linearly with the batch size. Combining these two factors together, the intermediate activation is making the training memory much larger compared with inference. After you forward one layer, you can throw away the activation because for the next layer, we are not going to use that activation again. But for training, we still need to access the activation when we are doing the backward pass. As a result, the activation is the bottleneck for CNN training.

Although there are some optimizations, like from ResNet-50 to MobileNetV2, the number of parameters decreases a lot—decreased by four times—but the real bottleneck, which is the activation, didn't improve much, only improved by 10%. There are several techniques for parameter-efficient transfer learning. For example, just fine-tuning the last layer. Fine-tuning the last layer is very efficient; the trainable parameters are reduced by 13 times, but there is very significant accuracy degradation. On the cars dataset, accuracy reduced from 90% to only 50%. Another popular method is only updating the batch normalization layer (the BN layer). That also has a lot less—like 12 times fewer trainable parameters by just optimizing the batch normalization layer, and the accuracy is a lot better than just fine-tuning the last layer. Recently, we also found just fine-tuning the batch normalization layer is very effective for vision-language models.

The question here: are batch normalization plus last layer updates good enough for on-device learning? Actually not. If you compare the actual trainable parameters versus the actual speedup in memory saving, you can see there is a big gap. This is the measured memory cost. This only saved less than two times—despite that the trainable memory reduced by 12 times, you only get like 1.8 times in actuality. Parameter efficiency does not translate to measured memory saving. So, fine-tuning the BN layer and last layer is parameter efficient, but the memory saving is very limited. We should not just focus on parameter efficiency, but the actual memory efficiency. Despite that, the accuracy degradation from batch normalization layer and last layer also results in significant accuracy loss.

Let's try to solve that problem by talking about tiny TL (transfer learning) to get measured memory saving and also measured speedup. Let's revisit what is the expensive part. Updating the weight is very memory expensive because updating the weight, you need to store the activations. So, this partial loss over partial weight uses the activations to calculate the partial loss over weight. Updating the weight requires storing these activations. But updating the bias (W + b)—updating this b, this bias—doesn't require storing any activation. Updating the bias is very memory efficient; you don't need to store any activation. So, we can fine-tune the bias only. This is a one-column inverted residual; we just fine-tune the bias for each layer. It's super memory efficient. But is that enough?

First of all, the memory costs are reduced compared with BN plus last bias. The last layer reduces the memory a lot—by 12 times—so that's the maximum measured memory saving, which is pretty good. Unfortunately, the accuracy also dropped by 16%, so it hurt the accuracy because the capacity is so limited. We didn't fine-tune any weight, only the bias, but it's not enough. How do we compensate for this lack of capacity? Here, let's use the light residual layer. This is the main branch; this is a residual branch. It's very lightweight—uses very small resolution (half resolution), and we also use group convolution rather than normal convolution, and then output back very small channel C rather than 6C. Very small, lightweight residual layer to add capacity. Actually, this paper was published in 2020 before LoRA, but it's a very similar technique—having a low-rank layer bypass individual to learn the difference rather than learning the main branch. This is actually very similar to that, but much earlier, before large language models got common.

The principle here is to make this branch lightweight. We achieve that by downsampling the resolution from original resolution to only half of the resolution. We also avoid the inverted bottleneck—remember the inverted bottleneck will expand the number of channels by a lot (six times), which will increase the activation size. Here, we didn't increase the channel, so it remains C rather than being expanded by 6C. Next principle: we use one-sixth of the channel, half the resolution, two-thirds of the depth, so that occupies only 4% of the activation size, which is pretty cool. That only has five megabytes of memory overhead, but it can bring 11% higher accuracy compared with bias and last layer update only.

This is the full picture: we finally closed the gap of accuracy using this tiny TL in blue. Here is the memory cost: it saves memory very similarly to fine-tuning the last layer only (red), and fine-tuning last layer only is parameter efficient, but the memory saving is very limited. Slight residual learning is very helpful. This is the memory saving across different benchmarks like flowers, cars, and food—saves the memory by about 6.5 times, 4.6, 4.5 times, with smaller memory and even better accuracy.

This is a comparison with dynamic activation pruning. Since we also tried to prune the activation, but actually pruning the activation didn't work as well compared with tiny TL. Learning the bias and also learning light residual is more helpful than just pruning the activations. Smaller memory, higher accuracy using tiny TL across different benchmarks. Let's see now how much memory do we need. Previously, training memory was like 300 megabytes; now we can fit it with only 60 megabytes, very easily to fit in the L3 cache.

That's about tiny transfer learning. Now let's switch to another technique: sparse backpropagation (sparse BP). This is the original diagram for backpropagation. We need to store the activation if you want to update the weight. But the human brain adopts the sparse learning paradigm, where a newborn child has only 500 synapses per neuron (according to this study from Nature), and when he or she gets to two to four years old, this baby will have 15,000 synapses per neuron. When he or she gets to adolescence, during K-12 education, this number doesn't increase but actually decreases—gradually decreases until adulthood. From 15,000 to 7,000 synapses per neuron, the number doesn't increase but actually decreases when we are going through this education. The adolescent synapses are getting sparser during the time when we are learning the most amount of knowledge during our lives.

We tried if artificial neural network fine-tuning or on-device learning can also adopt such sparse learning to save computation. The extreme case is just tuning the last layer. Of course, that's making the memory a lot smaller, but accuracy degradation is severe. We also learned about bias-only updates—by only learning the bias for each layer, that saves memory but still has an accuracy gap. The general case is to perform sparse layer backpropagation. Some of the layers are not as important as others, so we don't need to update these layers in the front. Even for such layers that are updated, we don't need to update the full layer. We are not updating all the channels; some are not as important as others. We can do sparse tensor and layer backpropagation, and we don't need to backpropagate to the early layers—low-level feature extractors—we don't need to learn those. As a result, we need to only store a subset of the activations if only a subset of the weight channels need to be updated.

This is the full update: this is dL/dW, this is the activation to store, and the M is the weight gradient—full weight gradient. But if we need to store only one quarter of the weight gradient, we don't need to store everything of the activation; we only need to store a quarter of the activation. That also saves memory by doing partial sparse tensor backpropagation. This is the full picture: visual comparison about full backpropagation (everything is updated) versus last-only backpropagation (the last layer is updated) versus bias-only update backpropagation (only the bias is updated) versus sparse layer, sparse tensor backpropagation (some of the layers are not updated, some of the layers are partially updated, etc.).

The next question is: which layers should we update? There are so many layers—how do we identify which layer is more sensitive, which layer is not? Here, we plot memory usage with activation in yellow and the weight in blue. We find this U-shaped curve very interesting. For activation, the initial layer is pretty large, and then for later layers, it gets smaller. For the weight, the initial layer is pretty small; the later layer gets large. Why is that the case? I think we visited this before: for the activation, the initial layer has very high resolution before downsampling, and the activation size grows quadratically with resolution. That's why the high-activation, high-resolution stage has pretty large activation. Versus the weight: it's 3x3 by input channel, output channel; the output in later stages gradually has larger channels, so that's why the weight gets larger in the end. The activation cost is high for the starting layers; the weight cost is high for the later layers. Overall memory is low for the middle layers.

We update the bias for the later layers related to the activation and weights. We want to update that for the intermediate layers because the intermediate layer activation memory is pretty small, and the weight is also pretty small. In this case, we update several of the middle layers—we want to update everything—and for the late stage, we only partially update a few layers by a quarter or one-eighth, and we update the bias all the way up to here. Everything in front, which has a pretty high activation cost, we are not going to update the bias or the weight either, because the activation cost is just so high. We just do forward for these early layers.

That is purely intuition. How do we mathematically have a solver to automatically identify which layers to update? We can pick a particular dataset and define Q: only one layer on the downstream task to measure the accuracy gain as the contribution for that layer. We can repeat this process, just like sensitivity analysis we did before for pruning. We can do this contribution analysis for each layer individually and see how much contribution—if we update this layer, how much contribution it can bring to the final accuracy. You can find updating the first couple of layers didn't improve the accuracy—delta accuracy is negative. What does that mean? It actually hurts the accuracy if you just only update these early layers. For this late layer, it actually helps a lot—just updating a single layer will improve the accuracy by 14%. This is how we identify those contributing layers versus those non-contributing layers.

Interestingly, we find this zigzag pattern: one, two, three, four, one, two, four. We have this repeated zigzag pattern, and we find actually these peaks correspond to this pointwise convolution, and this low point corresponds to depthwise convolution. Pointwise convolution, especially the first pointwise convolution, is very crucial to update, versus the depthwise convolution—it contributes a lot less, and very regular repeating pattern. The good thing about that is the input to this pointwise layer: the channel is C, the output is 6C, so updating here, this pointwise is cheaper than updating this pointwise layer. MobileNet prefers the first depthwise convolution to update. This is very interesting, but BERT has a different phenomenon. We find some of the early layers contribute negatively, some of the late layers also contribute negatively, and the QKV projection (F1, F2) also formulates a very regular repeated periodic pattern. This QKV projection here is very important to update; output projection is not, F1 is not contributing a lot, F2 is not. On the right-hand side, we put it here: the right part, including QKV projection, different layer one—they prefer to be updated versus the output projection and the second FF prefer not to be updated.

You do such profiling on a dataset, and then we want to transfer that to other datasets. When we are finding which layer to update, we perform such contribution analysis—not only updating all the channels, but also we can update only half of the channels (in green), one quarter (in yellow), and one-eighth (in blue) to plot the entire contribution analysis. Then we can use evolutionary search to find the best backpropagation schematic, since we have several key parameters. We have to decide the k_i, including which bias do we update (which is k), how deep do we backpropagate to update the bias (from delta accuracy bias), and also which layer should we update the weight (indicated by i, the index of that layer), and how much do we update for that layer (indicated by r). We sum them up to maximize the total contribution of the accuracy, such that the memory also fits the constraint. We can use evolutionary search, which performs much better compared with random search (in green evolutionary search, in red random), and we can use this way to tune the model on edge with low memory cost and also achieve high accuracy.

This is accuracy compared with full backpropagation, bias-only versus sparse propagation across different neural nets. For BERT, this is averaged from the blue benchmark. We also have this for MobileNet ResNet-50 from the tiny TL benchmark. We find in general this sparse propagation has on-par performance with full backpropagation for both vision and language models, while achieving much lower memory. This is the memory consumption averaged; the accuracy—this is basically updating last bias only, very low accuracy, and this is sparse update. This is updating the last k layers, including all the layers, which is the upper bound—it can have about 4–4.5x smaller memory but high accuracy. Similarly for MobileNetV2 and ResNet, this is the memory saving, and sparse propagation also enables fine-tuning on-device.

We tried this Llama 2 on the Stanford Alpaca dataset. This is an example dataset: the input was "the meaning of the following idiom: 'It's raining cats and dogs'." The output should be "'It's raining cats and dogs' means that it is raining heavily." This is the example of the Alpaca set. Applying sparse update versus the full fine-tune: this is full, this is sparse. The latency can be reduced by half using Pocket Engine, but not quite helpful if you use the naive PyTorch implementation. The memory is also reduced by roughly a quarter; the loss is roughly the same; win rate stays very competitive compared with the implementation, and the T-Bench score is also maintained.

Sparse BP compared with LoRA: this is LoRA at rank 8, sparse BP here. Backpropagation achieves the same accuracy as LoRA approach, and comparable accuracy with full backpropagation. Here are some example quantitative analyses. "What is the capital of the United States?" Llama 2 original model says "Washington, DC." The fine-tuned version—both full BP and sparse BP—says "Washington," which is correct since this is a very simple question. The second input is more challenging: "What was Michael Jackson's last album?" The original Llama 2 7B says "Michael Jackson passed away; however, we released the album Ultimate Collection." But the full BP and sparse BP answer correctly: "Invincible," showing that sparse backpropagation has similar capability as full backpropagation.

Here is another example: "The MICRO conference was running virtually online in 2021, physically at Chicago 2022, will soon be in Toronto 2023. Now, where was MICRO held last year?" So it should be in Chicago. The full BP is correct; sparse BP, no backpropagation, is wrong—"virtually in 2021." There are indeed cases where sparse BP will make errors. For example, "Please reverse the words in the sentence: 'I love the MICRO conference.'" Original Llama misinterprets and says "I hate the conference MICRO," but actually the correct result should be "conference MICRO love I" (reverse words in sentence, not the meaning of sentence). Sparse BP says "conference the MICRO love I," so the sequence is wrong. This shows sparse BP sometimes still suffers from the lack of capacity, which can be compensated by decreasing sparsity.

This is a more challenging task requiring a more complicated chain of thought: "Lee mowed lawns and charges $33. Last week he mowed 16 lawns. Three customers each gave him a $10 tip. How much did he earn in total?" The original Llama is wrong, but the full BP is correct: "He earned 33 \* 16 from mowing the lawns, 16 customers, $10 each tip, three customers, $10 each, so that's $30 of tip. So altogether, this is the total earning last week." Similarly, the sparse BP can also give this answer, but interestingly, the sparse BP's chain of thought process is a little bit longer—compare with the full BP, only two lines; this is four. It's very interesting—such pruning process makes the answer even longer, which is not necessarily a good or bad thing, but it remains to be investigated about the length of the output versus capacity of the model.

All right, let's take a five-minute break before we talk about quiz and training.

All right, welcome back. Let's continue to discuss on-device training. We just talked about sparse propagation. Let's now switch gears to talk about using quantization—low precision—to perform quantized training with the technique called quantization-aware scaling.

Let's review quantization, where we don't need to use full precision but lower precision to represent a tensor—the weight tensor, the activation, or the gradient tensor. For inference, that's very well learned about, but for training, let's see what is special about training. It actually turns out to be just one line of code we need to pay attention to, and let's see what that is.

This is the data flow for fake quantization. This is the fake quantization data flow, which is popular in quantization-aware training. After converting the input and the weight into quantized versions, we quantize that, but still use FP32 to do the computation—all arithmetic is done in FP32 for the input and for the output. Only this fake quantization point has a compute node: basically, first project to the int8 range, then round, then project it back. The final value is still FP32 representation. It cannot actually save the memory footprint, but the real quantization graph is different: the weight, input—they're all int8 or int4 or even lower precision (int4, etc.). We do the convolution in int8, the summation accumulation is in int32, and finally we cast it back—quantize back to int8 before we feed it to the next layer.

This is the difference between fake quantization and real quantization. In fake quantization, weight and activation are still calculated in FP32, and real quantization is integer. Fake quantization can have fake normalization, versus real quantization does not. As you can imagine, this real quantized graph can save memory, but it's very hard to have good accuracy. Actually, this is the performance comparison averaged across 10 datasets, not just one set, comparing FP32 SGD versus int8 SGD. You have 10% top-1 accuracy drop, and we are going to learn how to recover this loss of accuracy from int8 training.

If you think about it, why do we have such a big gap between the accuracy using int8? What happens to the weight gradient during the update process? Are we updating too much or too little? We plotted this curve of weight over gradient ratio. Why do we plot this? If the weight over gradient is too large, meaning that the gradient is too small, then unless we multiply it with a super big learning rate, we are not going to update anything since the gradient is too small, the weight is too big. And that's indeed the case. This is FP8, FP32 weight over gradient in blue, and this is the int8 weight over gradient in green. This is in log y-axis, and it differs by 10 times—very different FP32 versus int8 weight over gradient.

We find the problem is due to having such a small gradient with int8. There must be some scaling factor that went wrong. It also has this very good pattern of repeatable difference between the weight and bias—very interesting phenomenon, zigzag pattern. Let's see what makes this mismatch between the weight over gradient. This is the quantization equation: we have the integer weight, activation, integer bias, we multiply with the scaling factor, and then we cast it back to int8, and get the next layer. For the per-channel scaling, we calculate this scaling factor, which is the difference between the quantized weight versus the original weight. Original weight is really like 0 point something—a pretty small number—but the quantized weight for int8, what is the range? Between -128 to +127, right? So this is a big number. So this s_w is something much smaller than one, so it can scale from the integer range to the real floating point range.

What about g_w? If you want to calculate g_w according to the chain rule, g_w_bar equals s_w times the gradient. So g_w_bar is s_w times the g_w, according to this equation. What if we calculate w_bar over g_w_bar versus w over g_w? We plug in this w_bar, which equals w / s_w, and g_w_bar is equal to s_w times g_w. As a result, there are two terms of s_w, so this is to the power of minus 2, times w over g_w. Just now we talked about this s_w is smaller than one, that maps between -128 and +127 to something around zero or a small number here. So s_w to the power of minus 2 is something much bigger than that. That's why this w_bar over g_w_bar is such a big number, indicated here—it's such a big number compared with the original FP32 w over g_w. What does that mean? That means that g_w is so small compared with the w_bar, so we are not updating much because we missed this s_w to the power of minus two.

Do we know this number? Actually, yes, we know this number during the quantization process—it's a constant. Then how do we deal with it? We can simply rescale the gradients. So, g_w_t equals g_w_bar times this s_w to the power of minus 2. This is a number larger than one, so we are effectively scaling up the gradient, so that you can have larger updates compared with such a big weight versus super small gradient. This also avoids just tuning the learning rate, because for different layers, this s_w is different, and we know that ahead of time, so we don't need to perform this per-layer tuning of the learning rate, which can also solve this problem but is very hard in practice.

Similarly, we can do that for the bias. Just remember there are two scaling factors: one for the weight, one for the activation. That also interprets why it has the zigzag pattern between the weight and bias—they have different scaling factors. We can effectively solve this problem by quantization-aware scaling, which is saying we should scale the gradient for the weight and also the gradient for the bias, making it larger by this s_w to the power of minus two through the chain rule. Let's visit again why it's s to the minus 2: one comes from this scaling, one comes from this gradient. That's why we have 2 to the power of minus two.

After we scale the gradient for weight and gradient for the bias, we have this yellow curve—that's int8 quantization-aware scaling—so that can match very well between the yellow and blue curve, aligning the w over g_w ratio with FP32. This is comparing the whole training loss across different IOs. This is without quantization-aware scaling—the convergence is poor—but the green curve is with quantization-aware scaling, which has better performance both on the training loss (lower training loss) and also lower validation loss. After applying quantization-aware scaling, the convergence of a real quantized graph is stable, and previously we lost 12% of accuracy, but now the accuracy improved back. That's very good.

By putting these techniques together, we can fit on-device training under 256 kilobytes of SRAM on a microcontroller. By using Pocket Engine (we're going to talk about that very soon), compared with PyTorch, it saves 7x; quantization-aware scaling saves 2x; sparse layer, sparse tensor update saves you 8x; plus operator reordering saves another 2.4x. Altogether, we pushed the memory from above 100 megabytes to only 141 kilobytes. Let's see a demo of on-device training under 256 kilobytes of SRAM.

This is an open microcontroller with an ARM Cortex-M32 microcontroller in the back. We attach it with two buttons—here is one, this is another. We feed it some input images—the top corner. Initially, we do the testing; the label is actually random. The task is to detect a person or no person. Unfortunately, it doesn't do that very well—predicts very low because the model is not fine-tuned. After a while, we want to do the on-device training, including quantization-aware scaling, sparse tensor layer update, using the tiny training engine. We press the green button with person, red button without person. Now we can do some testing: this is without person, so it's in green; with person, it's in red. Here's the prediction: green means no person, red means there is a person. Everything is happening locally right here, using the limited memory on the device, and we can transfer that to a real-world setting—somebody in the room, with person is in red, without person is green. It transfers quite well; the prediction is quite fast. It's open source—feel free to check it out for your final projects. The code is available at GitHub.

Training summary so far, before we talk about the tiny training engine: gradient is not safe to share—data leakage shows that we can steal the input from the gradient. The bottleneck is the activation for CNN training, and bias-only and light residual learning can effectively do transfer learning. The full update is too expensive; instead, we can perform sparse propagation and also use quantization-aware scaling to use lower precision for training.

The next question is: these are all algorithmic findings, so how do we translate this algorithmic improvement into measured speedup and actual savings? We need algorithm and system co-design. The last part, let's talk about Pocket Engine—the system support for sparse propagation.

This is a virtual cycle: we want to discuss many applications, we want to collect the data, on-device training, make the model better. If we look at the conventional dependent training library, they compute the computation graph for forward only, and then they do the auto-differentiation to generate the backward computation graph, and these are all performed at runtime. It focuses on flexibility, so the auto-differentiation is performed at runtime (in red). What we do is to perform compile-time auto-diff. The compile time is much longer, but the execution engine is much cleaner.

Let's compare it again: this is the previous deep learning pipeline, this is ours. The previous is very heavy on computation graph and auto-diff; ours is very light on the runtime execution engine, but a lot heavier on the compile time. But that is fine because compile time is only once, and it doesn't suffer from the runtime overhead. That also enables a lot of opportunity for extensive computation graph optimizations if we can push a lot of the process to compile time.

This is the entire life cycle of Pocket Engine. Starting from the Python definition of the model, we want to trace the static computation graph. This is the Python model definition: sequential Conv2D 3x3, per size three, padding one, followed by ReLU. This is converting that into the forward intermediate representation (IR), so that we can trace this static graph. Next, we convert from this forward IR to a backward IR. You can see the code is getting longer—previously we had just one layer of Conv2D, now we have three such operations because one is for forward, one is for the backward to input gradient, the other is backward to the weight gradient—so three Conv2D. That's normal for the backward IR.

Then we perform this graph-level optimization. After getting this IR for the backward graph, we can do techniques which we learned, such as sparse layer, sparse tensor update—we can prune away those operators that are not updated. We can also do operator reordering and in-place update to save memory. We can also do constant folding and dead code elimination, since everything is static at this moment. Later, we can tune the schedule for each kernel, since we know the dimension, we know the perfect timing for each one of them. We can tune such that it can perform like 23 times faster and smaller compared with TensorFlow Lite. After tuning the schedule, we do the code generation and generate the executable for a particular device for on-device training. In this way, Pocket Engine can deliver a very lightweight, portable, and efficient binary for diverse platforms. Actually, here we support Apple M1, Raspberry Pi, and various smartphones, and also diverse frontends: PyTorch, TensorFlow, JAX.

For example, here is a result on the Jetson Nano GPU. We compare different platforms: PyTorch, JAX, Pocket Engine full backpropagation, and also sparse propagation. On average, there is about 2–4x speedup across MobileNetV2, ResNet-50, BERT, and Llama 2 7B. The speedup mostly comes from compilation. Remember, we have a very thin runtime and a very heavy compile time—we push a lot of the workload from the runtime to the compile time. The interesting part is other commercial inference frameworks like TensorRT can also benefit from compilation, but they only do it for inference. Now we make it also possible for training.

These are the results on ARM CPU. We measured it on Raspberry Pi and also a Qualcomm Snapdragon 8 Gen 1 CPU. Compared with TensorFlow and PyTorch, which have very low throughput (images per second), Pocket Engine is a lot faster—like 13 to 21x speedup—since existing frameworks like TensorFlow and PyTorch are mostly optimized for inference. The compilation also enables kernel tuning and accelerates the training part. In particular, this ARM CPU, especially for training, is highly under-optimized—there's a lot of opportunity for that.

This is another example of Apple M chip. We did a lot of work on diverse platforms. This is M chip—at that time, M1 and M2; now we already have M4. At that time, the compatibility was not ideal for PyTorch and TensorFlow, but Pocket Engine can compile the training graph to Metal, providing better compatibility and faster training across different benchmarks. Some of them are not possible to be done in PyTorch, but Pocket Engine makes it possible.

We also extended to DSPs and MCUs. This is the C906 DSP, fine-tuning with Pocket Engine—both full BP and sparse—which is an MM32 microcontroller. The most interesting part is that we can enable previously inference-only frameworks now to also support training.

All right, so this is the entire lecture for today. We first talked about the algorithmic part, including gradient data leakage, transfer learning, sparse BP, quantization-aware scaling, and finally the Pocket Engine system support.
