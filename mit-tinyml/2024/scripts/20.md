(90) EfficientML.ai Lecture 20 - Distributed Training Part 2 (Zoom Recording) (MIT 6.5940, Fall 2024) - YouTube
https://www.youtube.com/watch?v=jb91nEH2g_0

Transcript:

All right, good afternoon everyone. Let's get started with Lecture 20, the second lecture on distributed training. In the last lecture, we talked about several parallelization techniques. Today, we are going to continue discussing how to combine these techniques using mixed parallelism, hybrid parallelism, 2D and 3D parallelism, and also how to auto-parallelize the training. We will also understand the bandwidth and communication latency bottlenecks of distributed training. To overcome the bandwidth bottleneck, we're going to talk about gradient compression—how to compress the gradients—including gradient pruning using sparsity and deep gradient compression, where we can compress the gradient by 100 times without losing accuracy. We will also discuss how to perform gradient quantization, one-bit SGD, and ternary gradient. Finally, to overcome the latency bottleneck, we are going to talk about delaying the gradient update.

Let's first start with hybrid and mixed parallelism and also how to auto-parallelize. As a review from the last lecture, we talked about four strategies for parallelization in distributed training. The first one is data parallelism: separate pieces of data are split across multiple nodes, and we have copies of the model—each GPU maintains its own copy. That has pretty good utilization but high memory cost and low communication. There are some optimizations on top of data parallelism. Instead of each GPU having the entire weight, gradient, and optimizer state, we talked about FSP techniques to shard the optimizer state, shard the gradient, and also shard the weight, so you don't have to store the entire copy of the weight within one GPU.

The second technique is pipeline parallelism. This is a kind of model parallelism because you are splitting the layers of the model. With a single copy of the model, it has low utilization because if different pipeline stages have different latency, the faster one has to wait for the slower one. It has low memory cost and medium communication—you only need to communicate between the pipeline stages.

The next one is tensor parallelism. It's another model parallelism approach, but you are splitting the model within a tensor. You have a single copy of a model, and it has high utilization, low memory cost, but high communication. For example, in Transformers, for the attention layer, you need one communication in the beginning and the end; for FFN (feed-forward network) layers, you need one communication in the beginning and the end. So, high communication cost.

Lastly, we talked about sequence parallelism. When we are handling long contexts—long documents or videos—when there are a lot of tokens, we want to split the tokens across multiple GPUs. We can split the data by tokens: the first GPU deals with the first chapter, the second GPU deals with the second chapter. But there is actual communication during the attention layer, since different parts of the document still need to attend to each other. Therefore, we talked about two techniques: one is the all-reduce technique, where each GPU gets the attention from each document for different parts to attend to each other. We also talked about the ring attention, where we can perform this ring all-reduce to communicate between the different GPUs.

So, those are the four parallelization methods. The next question we want to answer is: can we combine the merits of different parallelization methods and perform 2D or 3D combinations of different techniques?

Let's talk about 2D parallelism. This is one possible example. You can see at the outer loop, we are using data parallelism. This is one group, this is another group. So, rank zero and one: on the outer loop, we are using data parallelism, feeding different parts of the data to rank one and zero. In the inner loop, we are using pipeline parallelism. So, this is forward one, forward zero, backward forward one, backward one—we're alternating between two GPUs to do pipeline parallelism. This is four GPUs, 2 by 2, with two hierarchies: one is doing data parallelism on the outside, the other two are doing pipeline parallelism on the inside.

Of course, we can do other arrangements. For example, here we are performing pipeline parallelism in the outer loop—this is layer one, layer two—and we are doing the partition such that these two layers are performing pipeline parallelism. In the inner loop, we are doing tensor parallelism. So, this is the first half of the tensor, this is the second half of the tensor, on the top and on the bottom, respectively. One matrix is divided into two submatrices. So, this is tensor parallelism in the inner loop and pipeline parallelism in the outer loop.

Let's see another hybrid parallelism by combining different sequence parallelizations. This is the conventional approach: just use the all-to-all ring attention approach. Assuming every GPU within this dashed rectangle is within a node—this is one node, this is the second node—we have two nodes. Between the nodes, the communication bandwidth is about 50 gigabytes per second, but within a node, the bandwidth could be much higher, as high as 900 gigabytes per second. Using this 1D sequence parallelism with ring attention will underutilize the system because we have to stick to the slowest, which is the inter-node communication bandwidth, and we are not well exploiting this high bandwidth within the node.

So, how to better exploit the high bandwidth provided by the link? We can perform such 2D attention: within the node, we can perform all-to-all communication using the all-reduce approach, and between the nodes, we can use this ring attention approach, which has lower bandwidth. So, we are using ring attention between the nodes and all-reduce partition within a node. This way, we can fully exploit the high bandwidth within the node and the lower bandwidth between the nodes. This is 2D sequence parallelism.

We can also have 3D parallelism, combining pipeline parallelism with tensor parallelism and data parallelism. This is 3D parallelism. The same color of the GPUs are on the same server. We can see within a server, we are doing model parallelism—tensor parallelism is most communication-heavy, so we put them in the same server. Then, we are doing zero data parallelism in this dimension, and we are doing pipeline parallelism by splitting the layer vertically. So, that's the three 3D parallelism. That's a pretty large design space.

The next question is: how do we auto-parallelize? When the model is too large to fit the GPU, we can either split the model using pipeline parallelism to split different layers, but if even a layer is too large, we can split the layer by using tensor parallelism. So, this is pipeline parallelism: GPU1, GPU2, layer one, layer two. This is tensor parallelism—even within a single GPU, within a single layer, we can divide the tensor by two in this case. But how do we find the best hierarchical strategies for each layer? There are already a lot of choices. If you have so many layers, what's the best choice?

Here I introduce Alpa, which is a unified compiler for distributed training. Given a computational graph—four layers in this case—it is a pretty large search space between the layers and also within layers. We can either partition them by 2 plus or 1, 3, 3 plus one. Even within each group, we can either partition the tensor vertically or horizontally. So, Alpa proposes a hierarchical search space. First is the inter-operator parallelism: layer A is one group, layers B, C, D are another, or layer A is one group, C and D another group. The second hierarchy is the intra-operator parallelism: we can partition B and C either vertically, horizontally, or both. Breaking them into these two hierarchies, Alpa will search for the inter-operator parallelism.

Given a computational graph, stage one can either include these two layers or this one layer; stage two, either a single big layer or this count of average for matrix multiplication. Different approaches: you can use dynamic programming to divide the stages—either 2 + 1 + 1 or 1 + 3 + 1 + 2. Next, Alpa can search for the intra-operator parallelism. Given this A times B, we get the matrix multiplication to get C. We can either replicate B and split A across the two GPUs, or replicate A and split B across two GPUs, or split both A and B to get a partial sum and finally do all-reduce to sum them. These are different design choices for the intra-operator parallelism.

Then, we can calculate the cost function, which is compute cost plus communication cost plus the reordering cost between the operators. Given a computational graph and a device cluster, it will first do the inter-operator pass and then the intra-operator pass to do the cost estimation and then give you the best orchestration after this automatic search. Actually, that can match almost perfectly with the best manual design system across 48 to 64 GPUs. This is comparing the best manual system versus Alpa, which can achieve a much higher throughput compared with the best manual system.

All right, so that is hybrid/mixed parallelism and how to auto-parallelize. Now let's try to understand the bandwidth and latency bottlenecks of distributed training. As a fundamental, computation is cheap, data movement is expensive, so communication is very essential. Communication is the foundation for distributed training. There are several issues when the model gets larger. First, we require synchronization. Synchronized SGD is still much more widely used in industry these days compared with asynchronous SGD, but synchronization will lead to high communication frequency—we always need to synchronize after each iteration. The models are getting larger these days, which means we need to transfer a larger gradient, because the dimension of the gradient is equal to the dimension of the weight. If the model weight is larger, the gradient that needs to be transferred is also larger. Also, when you have more training nodes, if you want to make your training faster, like from an hour to a few minutes, you need to introduce more training nodes, which means communication between the nodes using all-reduce takes longer. If you have more nodes, the communication time actually will be larger compared with the computation.

On the right-hand side, this is showing the ideal speedup: if you have 200 GPUs, ideally you want to have 200 times speedup, but different models have different scalability. For example, AlexNet has very poor scalability, while the Inception model has much better scalability. That is because AlexNet has a lot of weight—240 megabytes, 60 million parameters—but relatively low compute FLOPs. So, you do a little computation and you have to do a lot of communication. That's why such a model has much worse scalability: 200 GPUs have only 100 times speedup compared with 200.

Finally, the network bandwidth, if it becomes the bottleneck, will slow down the distributed training no matter how fast your computation is. If the bandwidth of your network is low, you still cannot fully unleash the power of newer generations of GPUs.

Let's see how we estimate the networking latency. Let's go through different scenarios. Within a rack of servers, it is roughly microsecond level. Within a data center, it is millisecond level latency. If you want to send a message across the world, it is 100 milliseconds or even second-level latency. The smaller the latency, the faster the training speed. If the latency is too long, the training speed will be much lower.

Let's talk about techniques to overcome the networking bandwidth bottleneck by introducing two chapters: one is gradient pruning, the other is gradient quantization.

Let's first start with how to reduce the transfer data size. Recall this workflow of parameter server-based distributed training. We have a parameter server that holds the neural network model. The first step: each worker will pull the parameters, pull the parameters to a local replica of the neural network model. It's going to get a split of the training data by data parallelism, calculate the local gradient given training data and the model. After it completes, it will push the gradients to the parameter server, and the parameter server will synchronize across multiple workers and finally update the server. So, this step—pushing and pulling the parameters and pushing the gradients—actually will be the bottleneck of the networking bandwidth. Imagine you have 100 nodes, each has to push the gradient to the parameter server, or even if you do ring all-reduce, you still have to communicate a lot of gradient, which is of the same size as the weight.

A natural idea is: can we prune the gradient? This is the original gradient tensor, and we prune them by setting a lot of them to zero, such that we don't need to send those pruned zero gradients. We only need to send the dark green gradients and make the light green areas zero—we just don't send them. Another approach is, rather than sending this 32-bit gradient, we quantize the gradient to 8 bits and send a much lower precision. That's eight times saving compared with sending everything in 32 bits.

Let's visit them one by one. Let's first visit the pruning and sparse communication approach. Let's discuss gradient pruning with local gradient accumulation. The idea is to only send the top-K gradients ranked by magnitude. This is the gradient tensor, and we find the top-K by sorting them. These green blocks are the top-K gradients. Of course, there will be an error, and we can accumulate the error. For the pruned area—everything here pruned to zero—they must have a residual. They are small, but they do have a residual corresponding to the zeroed gradient. Rather than sending away those residuals to the parameter server, we want to accumulate those gradients locally. Hopefully, next time the gradient will get bigger, pass the threshold, and we'll send them out. Before they accumulate to pass the threshold, we just keep the residual locally without sending them out.

That can indeed improve the training speed. This very early paper, like seven years ago, showed on a dataset it can increase training images per second, but it fails on slightly larger networks like ResNet—there is 1% loss of accuracy, which is not tolerable.

How do we solve the issue of lost accuracy when we are pruning the gradients, and figure out where the performance degradation is coming from? Why does the accuracy drop? Actually, that is due to the momentum. Let's review what momentum is. Momentum is a very important technique used in gradient descent. Nearly all optimizers have momentum these days. It's very useful to overcome local minima and oscillation. It's very widely used, and it's not just in ResNet or Transformer or BERT—even these days, for large language models, momentum is very widely used.

Here we can see only the first-order momentum. This is the gradient in blue, and we update the velocity U, and M is the momentum discounting factor. We take a percentage of the previous velocity and we add the current gradient. We get the current velocity, and we use velocity rather than the gradient to update the weight. So, this is a discounted velocity: the current gradient, this is the new velocity, which is a combination of the previous velocity and the current gradient, and we use that to update the weight.

During SGD, we first obtain the gradient (in blue), then calculate the velocity using the momentum. This is previous velocity U*{t-1}, this is U_t by combining these two together to calculate the velocity U_t, and we update the model weights by U_t, not by G_t—not using the gradient, but using the velocity to update the model weights. Finally, we repeat, calculating velocity for the next iteration. This is the new gradient, and this is the discounted factor of the previous velocity. This is U*{t+1}, and we use U*{t+1} to update the weight; we are not using G*{t+1} to update the weight. That's the key point here.

Let's see during sparse communication what is accumulated. We are accumulating the gradient. So, residual gradient is accumulated in the local buffers before they get large enough to be sent out. In the previous algorithm, we directly use accumulated gradient for momentum calculation. We are accumulating the gradient—is that correct? If the gradient is not sent out, we accumulate them locally; hopefully, during the next iteration, it will get larger and be sent out.

Let's see how that looks on this graph. This is vanilla momentum SGD: we get U_t, we update, and the weight starts from here, gets here, finally lands here. In momentum SGD with local accumulation, we are accumulating the gradients in the local buffer. If the gradient is small, we accumulate them; after they get large, we send them out. This is the accumulated gradient. What if we use that to update the model? It will become here rather than here. You see the difference: we are accumulating gradient and get a wrong update using momentum SGD with this naive local accumulation. So, it's wrong to add the accumulated gradient; the result in the optimization is shifted and doesn't match.

So, what should we do? We should accumulate the velocity, not the gradient. What is the velocity? This is U*t, this is U*{t+1}. We should accumulate the velocity by combining U*t and U*{t+1}; we get the red accumulated velocity, and we use that to update the model weight. We should accumulate the velocity over many iterations rather than accumulating the gradient, and in this way, it can land in the same position in these two scenarios.

Here is a comparison among vanilla momentum SGD, without momentum correction, and with momentum correction. The next technique is warm-up training. In the early stage of training, the network weight is changing very rapidly, and local gradient accumulation of the gradient will make the problem worse. So, we don't want to have a very high pruning ratio in the first couple of iterations. When the optimization just gets started, the gradient may be pretty large, so we can warm up the learning rate—initially start with a very small learning rate and gradually, here, linearly increase to the stable learning rate. Since you can see in the initial training stage, the gradient is very sharp, we want to be conservative to prevent overshooting by using a smaller learning rate. We can also warm up the sparsity to avoid the sudden change in sparsity. Here, we're gradually increasing the sparsity—here it is exponentially increasing sparsity in the first couple of iterations to help the optimizer adapt to a larger sparsity. After it becomes stable, the sparsity is stable.

Let's see how this technique improves the accuracy. Starting with local gradient accumulation, the baseline model has accuracy of 92.9%. If you just do naive gradient pruning without doing any gradient accumulation, it doesn't converge, so you cannot do that. If we add the local gradient accumulation—if the gradient is small, we accumulate them until they get large and send them out—it can recover a lot, but still it's 1.56% lower than the baseline. What if we add this local gradient accumulation plus this momentum correction—accumulate the velocity rather than accumulate the gradient? It becomes 92.56%, a lot better than the baseline. What if we only add the warm-up? It also gets better, from 91.3% to 91.8%. What if we add them together, including local gradient accumulation, momentum correction, and warm-up training? Combining them further increases accuracy to more than 93%, now even better compared with the baseline.

This is for vision, and this is for language modeling. The word error rate is also well recovered by combining all these techniques—the lower, the better. On the right-hand side, if we just do this local gradient accumulation, actually there is three-point degradation for the word error rate. If we add the momentum correction to accumulate the velocity rather than accumulate the gradient, and also do local gradient clipping—which is quite important for LSTM, as we want to prevent the gradient vanishing and gradient explosion problem—so you want to also do this local gradient clipping, it can recover a little bit, around 1%. Then, we can add momentum correction and local gradient clipping, further improving a little bit. Finally, if we combine all the techniques, it can further improve to 10.37%.

Let's see the training curve. This is the accuracy on the left (higher is better), and this is the loss on the right (lower is better). The baseline is in blue. There are several approaches: one is doing gradient dropping (in red)—actually, it's much worse, at the bottom, so accuracy is not good and the loss is highest. If you just perform gradient dropping, the yellow one is gradient sparsification with momentum correction only (without warm-up). It is in yellow here—better, but still worse compared with the baseline. Deep gradient compression is in purple, closely matching the baseline, and here in purple, it's even lower than the baseline.

Surprisingly, we can make it 99.9% sparse for the gradient. ImageNet is about 200 to 500 times gradient compression ratio, which is quite significant. DGC stands for deep gradient compression. This is the top-1 and top-5 accuracy. Gradient size is reduced by almost 600 times. Why is it not a thousand times? Because it's almost 99.9% sparsity. Why is the gradient size reduced by 500 times? Because there's indexing overhead, of course, to represent a sparse array, and also the biases are not pruned. Remember, in the residual training, we want to keep the bias, and it's very important.

This is for training ImageNet: top-1 and top-5 accuracy, the baseline versus deep gradient compression. Compression ratio is more than 200 times. Not only on vision—let's also take a look at language modeling. This is comparing the baseline and deep compression for the loss; this is for the perplexity. It is very well maintained, while the size of the gradient is reduced from 190 megabytes to less than half a megabyte. For speech recognition, the word error rate is also well maintained versus the loss. Previously, the gradient was more than 480 megabytes; now it is less than one megabyte.

What is the problem for deep gradient compression? If we are using the all-reduce approach, actually after a few iterations, it will get denser. Suppose we have four nodes: 0, 1, 2, 3, and the sparsity of each node's gradients is all different. For node zero, the bottom three gradients are nonzero; for node three, the other two values are nonzero. If we want to perform this all-reduce operation—adding three to zero—it will get denser, and if you do another iteration, it will continue to get denser and denser. If you repeat this all-reduce process, the final accumulated result will get denser.

What's the solution? We can enforce the same sparsity pattern, so when we do all-reduce, it will not get denser. You can apply coarse-grained sparsity, such that you can enforce a certain pattern of sparsity. Also, we can continue pruning in the middle of this ring all-reduce—for example, pruning in the middle or throwing away some of the nonzeros to be zero again.

There's another approach: rather than using sparsity, remember during the inference efficiency technique, we talked about a lot of techniques. One is low-rank. We can not only use sparsity by pruning a lot of small gradients, but we can also factor the gradients using low-rank. This is using rank-one factorization: a 4x4 matrix into a 1x4 times 4x1 matrix. The advantage is that the low-rank matrix dimension (4x1, 1x4) is the same across all the servers, so when you are doing ring all-reduce, you are not going to get any difference—not like with sparsity, where you can get denser. Here, it will not get denser; the dimension is the same as you perform all-reduce.

Let's see how that performs. The speed is actually quite good. This is rank two; the speedup over the standard SGD is much better, almost a linear speedup when the number of workers increases when using this PowerSGD approach.

This is the comparison of different gradient pruning methods. We can perform sparsification, start with local gradient accumulation—if the gradient is small, then we can accumulate them until it's large; after exceeding a certain threshold, we send them. Unfortunately, it can have a very low sparsity ratio; otherwise, the accuracy will drop. Then, we introduced deep gradient compression technique, which we published in 2017, by using momentum correction. We discovered we should not accumulate the gradient, but we should accumulate the velocity, not the gradient—so we call it the momentum correction, which can solve the issue of low sparsity ratio and gradient clipping. We want to clip the gradient to prevent overflow and also perform warm-up training—start with a smaller learning rate, start with a smaller sparsity ratio, warm up. That can achieve a high sparsity ratio—we can prune away 99.9%. For weight pruning, even 90% pruning is a very high pruning ratio, but gradient is much easier to prune compared with the weight. That's the lesson we learned, while maintaining the accuracy.

However, this sparsification technique has a limitation: it will get denser if you do this ring all-reduce for a few iterations. Therefore, we talk about PowerSGD using the low-rank approach—that is, factorizing the gradient—to make sure during the communication we do not get denser.

That's the first half of this lecture. Let's take a five-minute break before we jump into quantization approaches.

All right, welcome back. Let's continue the discussion about gradient quantization. Just now, we talked about the first technique of gradient compression, which is pruning to remove a lot of gradients. Now, let's see: can we use one bit or ternary (two bits, only three numbers) to represent the gradient? This is one-bit SGD. Starting with the 32-bit full-precision gradient tensor, we want to quantize them to only one bit plus a delta, which is the residual, which we can accumulate locally to accumulate the quantization error. In the next round, we can add this residue to the next iteration and hopefully alleviate the error coming from this aggressive one-bit quantization. Of course, we can apply a column-wise scaling factor, so we can have a scaling factor for each column. Rather than just zero and one, we can apply a scaling factor (FP32) for each column.

The whole solution will be like this: we have the original gradient, and we set a threshold to be zero. If it is greater than zero, we set it to one; otherwise, it's zero. Across each column, we calculate and find a scaling factor by maintaining the same absolute value sum, such that we get the quantized value. We can calculate the gradient residual—what is the quantization error? Different from inference, where this error is independent and will propagate, in training we can accumulate the error and add it back, since they're both FP32. We can add it back to the original gradient to get the updated gradient. In the next iteration, we can continue this flow.

Another approach is to set a threshold. Rather than set the zero threshold, we can set a threshold T, which is also a reconstruction value chosen in advance. If it is greater than tau, the value will be T; in the middle between tau and minus tau, it will be zero; if it is smaller than minus tau, it will be quantized to minus tau. The drawback here is to empirically choose the value tau. Similarly, it also has the mechanism of gradient quantization error accumulation. This is threshold quantization.

The next work is called TernGrad—ternary gradient—which is saying: can we quantize by dividing by the max of the gradient to be 0, 1, and -1, but it's probabilistic quantization? With probability equal to the magnitude divided by the max value. Usually, adding such stochastic quantization will improve the accuracy, and that's indeed the case. The expectation of the quantized gradient will be the same as the original one, so there's no quantization error accumulation, since the expectation is matching. In this way, that's guaranteed, so we don't need to accumulate the error. We can do iteration by iteration; it's independent, done on the fly. This is TernGrad.

All right, so those are the two solutions for gradient compression to overcome the communication bandwidth bottleneck. The other bottleneck beyond communication bandwidth is actually the communication latency. We are going to talk about techniques to solve the latency bottleneck by using delayed gradient update.

First, let's review the concepts between bandwidth and latency. Bandwidth is much easier to improve compared with latency. For example, we can improve the bandwidth by compressing the gradient, making it smaller, and quantizing it to make it a fewer number of bits—for example, using the techniques we just learned: pruning, deep gradient compression, ternary gradient quantization. We can also upgrade the hardware: Ethernet has 100 megabits to 1 gigabit per second; fiber has about 1 gigabit to 25 gigabits per second; we can also upgrade to InfiniBand, which can easily get 400 gigabits per second these days. So, the bandwidth can be improved by upgrading your hardware.

However, latency improvement is very hard. It's restricted by physics—you have a physical limit by the speed of light. For example, traveling from Boston to Shanghai at the speed of light still takes 162 milliseconds. That's the physical limit. There is also the possibility of signal congestion, like in urban offices and homes, which creates a lot of signal contention. So, latency optimization is very hard.

What's the bottleneck? Why do we want a low latency during distributed training? For example, here we have two workers: the green part is computation, the yellow part is communication, and we have to synchronize after each iteration. We have to wait for the communication to finish before we can start the second iteration of computation. If the latency increases, the yellow part—which is communication time—will increase, and worker one and worker two cannot start the next round of computation before the communication finishes, since they need to synchronize. That will increase the total latency. The workers have to wait for transmission to finish before the next step, since the local updates and communication are happening sequentially. This is too bad: if we have a longer networking latency, the total training will get much slower.

The question here is: can we find an alternative solution to allow the gradients to be stale? We don't have to be blocked, but we want to continue to make progress, so we can overlap communication with computation. This is similar to using late days to submit your homework: to transmit your gradient, to use the late days. When we are submitting homework two, we can make progress by continuing to study lecture three before we finish homework two. We can submit homework two while still making progress by learning lecture three. We can no longer be blocked by using the late days.

Let's see what that looks like by using such delayed gradient averaging. These are four iterations using distributed SGD. There's a long delay to send and receive those parameters or the gradient. We cannot start step two computation before we finish this communication—they are blocked by this long synchronization. But if you allow such delay, we can immediately start the second iteration after the first iteration completes, even though we haven't finished the send and receive about the gradient. We can continue to make progress, computing step two, step three, four, and suddenly we receive the gradient coming from step one, which is a stale gradient with respect to step four—it was three steps before. That's a stale gradient, but fortunately, our study in this paper finds it's still possible to converge perfectly by using stale gradients to a certain extent—at least three steps is very safe. This is called delayed gradient averaging: we are summing up the gradient coming from a previous step—it's delayed, but it can still converge. Step four receives the gradient from step one and makes progress, and step five receives the gradient from step two. We are not losing anything; we are just using the stale gradient—we're not skipping any gradient. All the gradients are transmitted, all the homework is submitted, even though we are using the late days. There must be a certain threshold—like our lectures, we have a fixed number of late days; beyond that, we don't allow it, or the model will not converge. That's the same principle.

Let's see what happens if we increase the communication latency in this way. Previously, we had to synchronize after each iteration—have this barrier. If we allow one iteration of delay, we can remove this barrier in the first iteration; as long as on the second iteration they can synchronize. This is one day of late day for homework. This is the communication; if this gets longer, as long as it doesn't exceed this barrier, it will not increase total latency for the training. As long as communication finishes before this barrier, the total training time is not affected, even if the latency increases.

The key idea is to delay the receiving timing of the i-th gradient to a later iteration, i + d, and we are receiving a stale gradient d steps away. Let's see how that works. We neutralize a container for each iteration's gradient, G. For each t-th iteration, we calculate the gradient using the current weights and data, calculate the local gradient G, and send our local gradient out to other nodes with the iteration as the ID. Then, we record this G in this array, saying that for this iteration, the gradient is G, and average G is received d steps ago. We receive from ID equal to iteration minus d, so that's delayed by d steps, which we finally receive. Since the receive function takes time, when we receive it, it is already d steps away. When we are doing the update—weight equals weight minus the learning rate times G (our freshly calculated local gradient)—we want to subtract the gradient from the current node that was d steps away, since we have already received the average gradient from all the servers d steps away. We want to compensate for that by subtracting the current node's gradient from d steps ago. That's why we need this G vector to record the gradient of each iteration. Finally, we average the received averaged gradient from other nodes—this average G is actually delayed by d steps. That's the only change we need to make: subtract the gradient from the current node d steps away, since we are compensated by receiving the average gradient from all the nodes.

Let's continue to deal with the staleness. We have seen this figure already. Let's compare: without the correction, if we directly apply this—weight equals weight minus learning rate times the stale gradient that is averaged across all the nodes—what will happen? Actually, the longer the delay, the more the accuracy will drop, very quickly, from 88% to 84%. This is the delay; as you get longer delay without any correction, the accuracy will drop.

Let's see how to apply the correction term. We should subtract the gradient coming from the local gradient that is d steps away and add the current fresh gradient from the current node, and add the delayed gradient that is averaged across all the nodes but d steps away. Since this already contains part of the local gradient from d steps away, that's why we should subtract it. After applying such a correction term, we can well maintain the accuracy—much better than without correction, up to 4% or 5% better compared with not using this correction term.

The take-home is that we can allow such staleness by using the gradient averaged d iterations away, but don't forget to subtract your local gradient; otherwise, you will duplicate both from your own gradient (since it's already added here) and also from the sum of stale gradients.

Question: within these steps, we assume the learning rate stays roughly the same. d is not too large—it's only like 10 or 20 iterations, so there are no big overshoots or undershoots. The learning rate will scale with the batch size, and usually when you have more nodes and a larger batch size, the learning rate definitely needs to scale accordingly.

This is comparing the speedup. We compare on different datasets: the CCI dataset, ImageNet, Shakespeare partitioned data, either IID or non-IID. The baseline is using FedAvg (k=5), indicating that we just do the local calculation of gradients for five iterations, sum them up, and then send it out. That can have a longer time of communication but less communication, because we are doing five iterations of gradient calculation without any update—we just calculate the gradient. Effectively, we are increasing the batch size by five times. That's FedAvg—increasing the batch size by five times.

We can do the accumulation of the gradient by 10 iterations without sending them out—just doing gradient calculation locally, sum them up, and send it out every 10 iterations. That gives you about 1.5x speedup, and accuracy is well maintained. You can also increase that to 20; you get a 2x speedup, and the accuracy is also well maintained. You can also combine that with delayed gradient update, and that can give you three to four times speedup, while the accuracy of both IID and non-IID is well maintained across different scenarios. That gives you another design knob—not only to do FedAvg, but also you can do the delay.

To verify such an idea on a local server, we put up this small rack of Raspberry Pi. Here we have eight Raspberry Pi as a real-world benchmark, from one node, two, four, all the way to eight nodes, across vision benchmarks and language benchmarks. We are comparing the FedAvg—averaging every five steps or ten, just local averaging without having to transmit, which is a very strong baseline. You can get about three to four times speedup with eight nodes, but combining that with delayed gradient averaging, you can get seven or 7.5 times speedup with eight nodes, although this is connected by very slow, actually just standard Ethernet, but it can still scale quite well.

All right, here is a summary of today's lecture. We first talked about mixed/hybrid parallelism and how to auto-parallelize: 2D parallelism, 3D, and then we faced two challenges—one is the bandwidth challenge, the other is the latency challenge. To solve the bandwidth challenge, we have gradient compression techniques, such as deep gradient compression or using quantization, such as one-bit or ternary gradient. To solve the second problem of latency bottleneck, we talked about delaying the gradient update—to allow late days when submitting your homework, when transmitting the gradient—and can still converge quite well.

All right, so Lab 5 is already due, so we will grade it and return your grades by next weekend. We also have the project proposal deadline very soon, so hope you can work on that. If you need any support, feel free to talk to us. We highly encourage you to open source the final projects. For the format of the open source, feel free to check out our lab's GitHub—MIT HAILab, search that on GitHub. Pretty much all the projects we did since 2018, when I joined MIT, are open source, which both helps you maintain good quality and also helps the community to get better. If you have any questions, feel free to talk to us.

All right, that's all for today's lecture. Thank you.
